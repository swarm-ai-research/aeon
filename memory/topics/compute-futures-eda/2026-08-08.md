# Compute-Futures EDA — 2026-08-08

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-08.csv` (144 rows, 4 modes)
**Run:** 2026-08-09
**Prior filed report:** 2026-08-07 (single sweep-day diff — no missing intermediate CSV).

## Top findings

1. **[HIGH — fresh, cross-mode 3-seed cluster] `maxCurve` fires at 25% in basket AND synth≡x402 simultaneously, all anchored by the SAME three seeds (202608083 + 202608087 + 202608088).** 9 outliers per mode = 3 seeds × 3 roles. Both-tail cluster: seed 202608083 anchors the LOW tail (basket 0.2375, synth/x402 0.0950), seeds 202608087 + 202608088 anchor the HIGH tail (basket 0.2685/0.2605, synth/x402 0.1075/0.1045). Fence for basket [0.2431, 0.2576] on IQR=0.0058; fence for synth/x402 [0.0969, 0.1034] on IQR=0.0026. Ratio basket/synth ≈ 2.5× at every quantile — the two modes are the SAME draw with different scale multipliers (confirmed via row-level inspection: seeds' rank order in maxCurve is identical basket vs synth). First cross-mode same-seed HIGH concurrent fire in the filed-run window. Rotation-not-persistence class updated: yesterday's diffuse-anchor day (n=8+ seeds) SNAPS to today's tight 3-seed anchor (n=3 concurrent across 3 modes).

2. **[HIGH — fresh, cross-mode 3-seed cluster (same physical rows)] `maxCurve` synth ≡ x402 at 25%, seeds identical to finding #1.** Reported as one finding per synth ≡ x402 role-level equivalence (verified again today). Same three seeds, same LOW+HIGH bimodal pattern. This means today's maxCurve fire spans THREE distinct pricing regimes (basket small-price, synth small-price, x402 small-price w/ x402Total layer) but ONE seed-triple — the anchor cluster is upstream of pricing mode.

3. **[MEDIUM — fresh, cross-mode 2-seed cluster] `maxSpot` fires at 16.67% in basket AND synth≡x402 simultaneously, both anchored by the SAME two seeds (202608088 + 2026080812).** 6 outliers per mode = 2 seeds × 3 roles. Both-HIGH-tail cluster: basket [0.2217, 0.2366]; synth/x402 [0.0887, 0.0946]. Fence basket [0.1988, 0.2175]; synth/x402 [0.0795, 0.0870]. Note seed **202608088 is dual-column** — it anchors maxCurve HIGH tail (finding #1) AND maxSpot HIGH tail (this finding) in all three small-price modes. Seed 2026080812 is 10-char padded (part of the [[compute-futures-seed-padding-bug]] 3-seed group); this is its second consecutive filed run as an anchor (yesterday spread minSpot).

4. **[MEDIUM — under 10% but new all-time low] Spread `role_pnl` 8.33% with seed 202608083 retail at −$27,701 — DEEPEST single-row spread loss in the filed-run window (surpasses yesterday's −$18,285 by $9,416).** 3 outliers, ALL retail: 202608083 retail −$27,701 + 202608087 retail −$13,776 + 202608081 retail −$13,227. Retail-role loss concentration in spread now holds for THREE consecutive filed runs (08-06 top-3 all retail / 08-07 top-2 all retail / 08-08 top-3 all retail). Below the MEDIUM ≥10% threshold (SKILL rule) so not promoted, but the retail-role structural pattern is now n=3 filings — first candidate to promote to a structural finding if it holds a fourth day.

5. **[MEDIUM — under 10% but seed 202608083 super-anchor] Spread `realizedAbs` 8.33% all from a single seed (202608083 across all 3 roles at $88,319).** New spread realizedAbs high (previous top-quartile ceiling was in the $60ks). Same seed 202608083 that anchors basket/synth/x402 maxCurve LOW tail AND spread role_pnl deepest-loss. **Cross-mode super-anchor:** seed 202608083 shows up in 5 outlier positions across 4 modes today (basket maxCurve LOW, synth maxCurve LOW, x402 maxCurve LOW, spread realizedAbs HIGH × 3-role, spread role_pnl retail LOW). Return of the single-seed super-anchor class after 08-07's diffuse-anchor day. Anchor seed series: 07-28 202607286 → 07-30 202607301 → 07-31 202607314 → 08-01 202608012 → 08-03 202608038 → 08-06 four concurrent (top 202608064 at 15 pos) → 08-07 diffuse (top 202608078 at 6 pos) → **08-08 202608083 at 5 pos (super-anchor returns, narrower than 08-06)**.

6. **[LOW — collapse, 4 concurrent] All THREE of yesterday's fresh spread MEDIUM column-fires collapse to zero today.** Spread `settlementLegs` 16.67% (08-06 → 08-07 persist) → **0.00%** (persistence breaks at n=3, matching yesterday's basket-minCurve n=3 test). Spread `minSpot` 16.67% (08-07 fresh) → **0.00%**. Spread `maxSpot` 16.67% (08-07 fresh) → **0.00%**. Spread mode goes from 3 concurrent MEDIUM fires to ZERO ≥10% column-outliers today (aside from sub-threshold role_pnl 8.33% + realizedAbs 8.33%). Complete outlier-surface flip: yesterday spread held every fresh MEDIUM; today spread is calm and the small-price modes (basket + synth + x402) hold every fresh HIGH+MEDIUM.

7. **[LOW — collapse, correlation snap-back at n=1] 08-07's within-mode strong pair spread `realizedAbs × maxCurve` = +0.897 → 08-08 = +0.211.** Snap-back to zero-firing on the second filing after appearing — one-filing rotation-artifact class re-confirmed (matches 08-03's `realizedAbs × maxSpot` triple which also collapsed on next filing). Zero strong within-mode pairs |r|≥0.8 in ANY mode today. Zero-firing streak resumes at n=1 filed run. Rotation-not-persistence class holds for within-mode strong pairs (0 pairs have crossed n=3 in the filed-run window).

8. **[LOW — spread minSpot rotates seeds] 08-07's spread minSpot 16.67% anchor seeds (2026080711 + 2026080712 padded) rotate off; today's spread minSpot values for those seeds sit at 41.71 + 45.16 respectively (inside today's fence [39.55, 45.64]).** Same physics as 08-06 → 08-07 spread `maxCurve` collapse: seed values didn't change (deterministic draws from RNG), but the *other* 10 seeds shifted enough to widen the IQR fence past them. Structural evidence that 12-seed sample is too small for stable fences per [[compute-futures-12-seed-sample-too-small]].

9. **[LOW — synth `role_pnl` = 5.56% retail-loss cluster mirrors spread pattern] 2 outliers, both retail: 202608081 retail −$20.59 + 202608082 retail −$17.43.** Under threshold, but shape matches finding #4 (retail-role concentration in loss tail). Reported for the retail-cross-mode structural-pattern angle. Note: this is REPORTABLE (SKILL only excludes synth/x402 role_pnl from severity ranking when the distributions are identical — the two-mode identity means it's counted once, not double, but the retail-cluster pattern still surfaces).

10. **[LOW — recurring, 23rd filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608081`–`202608089`), 36 rows 10-char seed (`2026080810`–`2026080812`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]]. Padded-seed 2026080812 anchors basket + synth + x402 maxSpot HIGH tail today (finding #3) — another data point for the padding-fix PR's motivation (padded seeds continue to over-represent in outlier columns: 2/2 = 100% concentration in maxSpot fires today).

11. **[LOW — non-promotable jitter] x402Total mean $328.37 (up $1.99 vs 08-07's $326.38).** Sum $11,821.40 (up $71.78 vs 08-07). Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 → 08-06 $340.13 → 08-07 $326.38 → **08-08 $328.37**. Oscillates within $322–$346 range across seven filings; no trend, no promotion. Top-end today $368.06 (down from 08-07's $389.47). Bottom-end today $271.99 (new low, below 08-07's $282.65).

12. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −5.21e−15 (basket), +6.95e−13 (spread), −7.33e−16 (synth/x402). Std 1.93e−14 (basket), 6.97e−12 (spread), 7.14e−15 (synth/x402). All ≥11 orders under the |mean| > 10 / std > 100 gates. Basket sign flipped + → − vs 08-07, magnitude similar. Spread sign flipped − → +, magnitude flat. Synth/x402 sign held (−), magnitude down ~28%. Zero wallet_sum_pnl outliers in any mode today (yesterday basket fired 8.33% at seed 202608076 x3; today's basket has zero — dust cluster rotates too).

13. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). Row-level inspection confirms basket/synth are the same draws with a ~2.5× price-scale multiplier at every quantile — new cross-mode identity observation (basket is not row-identical to synth like x402 is, but the RANK ordering per seed is identical, so anchor seeds transfer 1:1).

14. **[PASS] Row/column shape unchanged vs 08-07.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −5.21e−15  | 1.93e−14  | OK |
| spread    | +6.95e−13  | 6.97e−12  | OK |
| synthetic | −7.33e−16  | 7.14e−15  | OK |
| x402      | −7.33e−16  | 7.14e−15  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-07's spread `realizedAbs × maxCurve` = +0.897 snaps back to +0.211 (below threshold). Zero-firing streak restarts at n=1 filed run. Rotation-not-persistence remains the default class for within-mode strong pairs — no pair has crossed n=3.
- **Cross-mode 3-seed maxCurve cluster (202608083 + 202608087 + 202608088)** — same three seeds anchor maxCurve outliers in basket AND synth AND x402. Since synth ≡ x402 at row level and basket/synth rank order is identical per seed (verified today, cross-scale ratio ~2.5× at every quantile), the maxCurve pattern is upstream of mode — a per-seed input-draw quirk that surfaces at every price scale. Genuine cross-mode structural signal (distinct from a pooled artifact — the fires are per-mode identifiable, not just pool-driven).
- **Seed 202608083 super-anchor** — appears in 5 outlier positions across 4 modes (basket maxCurve LOW × 3-role, synth maxCurve LOW × 3-role, x402 maxCurve LOW × 3-role, spread realizedAbs HIGH × 3-role, spread role_pnl retail LOW). One seed drives 15 of today's outlier rows (13 unique physical rows given synth ≡ x402). Anchor-cluster class returns after 08-07's diffuse day.
- **Retail-role loss concentration in spread continues for 3rd consecutive filed run** — top-3 spread losses on 08-06 all retail; top-2 on 08-07 all retail; top-3 on 08-08 all retail. Sub-threshold today (spread role_pnl at 8.33%) so not promoted, but n=3 filings gets close to the n=3 promotion rule. Watch 08-09 — if a 4th consecutive filing shows retail dominance in spread losses, promote to structural finding.
- **x402 `settlementLegs × x402Total` continues to alternate around zero.** 17-filing series: +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → **+0.179**. Sign flips continue; no interpretive story. Alternation-not-persistence class holds.

## Row count and shape vs 2026-08-07

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface FLIPS from spread-mode dominance (08-07) to small-price-mode dominance (08-08).** 08-07: 3 fresh spread MEDIUM + 1 spread LOW correlation. 08-08: 0 spread ≥10% column-outliers (2 sub-threshold), 2 basket HIGH+MEDIUM, 2 synth≡x402 HIGH+MEDIUM. Complete mode-role reversal in a single sweep-day diff. First filing in the 23-run window where the outlier surface completely relocates modes overnight.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's maxCurve + maxSpot outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **NEW OBSERVATION: basket ≡ synth up to scale** — basket and synth per-seed rank order is identical for maxCurve, maxSpot, minSpot, minCurve; only the scale multiplier differs (~2.5× basket vs synth). This is stronger than the general regime artifact — it means outlier anchors transfer 1:1 between basket and synth by construction. Worth documenting whether this is a deterministic function of the deployer's price-draw code or a coincidence at n=12 seeds.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 10+ orders under threshold in every mode. Notable excluded pairs today: basket `wallet_sum_pnl × seed` = −0.646, synth/x402 `wallet_sum_pnl × seed` = +0.643. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 23rd consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]]. Padded seed 2026080812 anchors basket + synth + x402 maxSpot HIGH tail today (100% padded-seed concentration in maxSpot fires); additional motivating data for the padding-fix PR.
- **12-seed outlier_pct rotation-not-persistence.** 08-07's three fresh spread MEDIUM fires ALL collapse to 0 today (settlementLegs / minSpot / maxSpot). 08-07's single strong within-mode pair (spread realizedAbs × maxCurve = +0.897) snaps back to +0.211. Fresh HIGH fires today: basket + synth≡x402 maxCurve at 25% (concentrated on 3 seeds). Fresh MEDIUM fires today: basket + synth≡x402 maxSpot at 16.67%. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode single-seed anchor pattern RETURNS to concentrated** — series 07-28 202607286 → 07-30 202607301 → 07-31 202607314 → 08-01 202608012 → 08-03 202608038 → 08-06 four concurrent (top 202608064 at 15 pos) → 08-07 diffuse (top 202608078 at 6 pos) → **08-08 seed 202608083 at 5 pos (super-anchor returns, narrower than 08-06)**. Anchor-count class oscillates: single (07-28→08-03) → multi (08-06) → diffuse (08-07) → super-single (08-08).
- **x402 `settlementLegs × x402Total` alternation continues** (17-filing series ±). No promotable trend.
- **x402Total oscillates within $322–$346 mean range across seven filings.** No trend. Today's $328.37 sits between 08-03 low ($322.85) and 07-31 high ($346.10). New bottom-end low today at $271.99.
- **Retail-role concentrates spread's largest losses — n=3 consecutive filings.** 08-06 top-3 all retail; 08-07 top-2 all retail; 08-08 top-3 all retail (deepest is 202608083 retail at −$27,701, new all-time filed-window low). One more consecutive filing (08-09) makes n=4 and this promotes from carry-over to structural finding.
