# Compute-Futures EDA — 2026-08-09

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-09.csv` (144 rows, 4 modes)
**Run:** 2026-08-10
**Prior filed report:** 2026-08-08 (single sweep-day diff — no missing intermediate CSV).

## Top findings

1. **[HIGH — PROMOTED per-carry-over n=4 rule] Retail role systematically absorbs spread-mode losses.** In spread, retail loses on **11/12 seeds** with mean role_pnl **−$9,272** and single-row min **−$22,880** (seed 202608098). Analyst mean +$2,750 (loses 4/12), operator mean +$4,801 (loses 2/12). **Top-7 spread losses today are ALL retail**; first non-retail loss is operator at position 8 (−$6,634). Retail-loss concentration series across four consecutive filings: 08-06 top-3 all retail → 08-07 top-2 all retail → 08-08 top-3 all retail → **08-09 top-7 all retail**. Yesterday's report set the n=4-promotion trigger explicitly — today crosses it. Not a symmetric zero-sum distribution across roles; this is either intentional (spread mode models retail eating the bid/ask spread) or a role-assignment bug worth reviewing. Deepest single loss today $−22,880 is shallower than 08-08's $−27,701 high-water mark; the *concentration* is stronger, the *magnitude* is smaller.

2. **[MEDIUM — persistence n=2] `maxSpot` fires at 16.67% in basket + synth + x402 simultaneously (same 2-seed cross-mode cluster), rotating anchor seeds vs 08-08.** 6 outliers per mode = 2 seeds × 3 roles. Anchor seeds today **(202608093 + 202608096)** replace yesterday's **(202608088 + 2026080812)** — full rotation, but the cross-mode 2-seed *shape* persists at n=2 filings. Bimodal-tail cluster: seed 202608096 anchors LOW (basket 0.19711 / synth-x402 0.07884), seed 202608093 anchors HIGH (basket 0.23177 / synth-x402 0.09271). Basket-vs-synth ratio 2.5× at every quantile (unchanged from 08-08). Rank-order-preserving cross-scale identity between basket and synth confirmed again on maxSpot (but NOT on maxCurve today, where discrete ties break the identity — new observation).

3. **[LOW — collapse at n=1] Yesterday's cross-mode HIGH `maxCurve` cluster (25% in basket + synth + x402) collapses to 0.00% in all three modes.** 08-08's 3-seed anchor (202608083 + 202608087 + 202608088) rotates out completely — none of those seeds sits outside today's IQR fence. Rotation-not-persistence class holds for HIGH-tier fires. First filed-window instance of a cross-mode HIGH cluster collapsing cleanly on the very next filing. Fence widths for maxCurve today are similar to 08-08 (basket IQR 0.0061 vs 08-08 0.0058; synth IQR 0.0027 vs 0.0026) — anchors moved, fence did not.

4. **[LOW — super-anchor day, TWO seeds >6 positions] Seed 202608096 at 15 outlier positions across 3 modes, seed 202608093 at 12 positions across 4 modes.** 202608096 anchors basket maxSpot × 3-role + synth maxSpot × 3-role + x402 maxSpot × 3-role + synth realizedAbs × 3-role + x402 realizedAbs × 3-role. 202608093 anchors basket maxSpot × 3-role + synth maxSpot × 3-role + x402 maxSpot × 3-role + spread minSpot × 3-role. **Two-anchor day** — first filed-window instance where TWO seeds exceed the super-anchor threshold in a single filing. Anchor-count class series: 08-06 four concurrent → 08-07 diffuse (top 6 pos) → 08-08 super-single (202608083 at 5 pos) → **08-09 super-double (202608096@15 + 202608093@12)**. Class oscillates unpredictably; no monotone trend.

5. **[LOW — x402Total new all-time high] Seed 202608095 fires x402Total at $456.76** (previous filed-window high was 08-07's $389.47; new high surpasses by $67.29). Mean $337.93 (up $9.56 vs 08-08). Sum $12,165.48 (up $344.08 vs 08-08). Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 → 08-06 $340.13 → 08-07 $326.38 → 08-08 $328.37 → **08-09 $337.93**. Still within the $322–$346 mean range but the top-end steps up. Per SKILL rule the 8.33% x402Total outlier fire in x402 mode is excluded from severity ranking (bimodal-by-design fence artifact), but the new max is worth logging.

6. **[LOW — fresh sub-threshold spread bursty cluster] Spread fires `minSpot` + `maxSpot` + `minCurve` + `maxCurve` all at 8.33% with DISTINCT single-seed anchors.** minSpot anchor 202608093 (also anchors basket/synth/x402 maxSpot — the only cross-mode super-anchor spillover into spread); maxSpot + minCurve both anchored by 2026080910 (padded seed, part of the 3-seed [[compute-futures-seed-padding-bug]] group); maxCurve anchored by 2026080911 (also padded). All 4 fires sit at 3 outliers per column (below the 10% MEDIUM threshold). Spread's cross-column cluster is *rotationally* fresh (yesterday spread was calm on price columns; two days ago it fired 3 MEDIUMs). Padded-seed concentration in spread outliers today: 2/4 anchor seeds are padded (100% of the maxSpot/minCurve/maxCurve fires); adds to the padding-fix PR's motivation.

7. **[LOW — synth/x402 fresh sub-threshold realizedAbs + settlementLegs cluster] realizedAbs 8.33% anchor seed 202608096 (LOW at 44.58, below IQR fence 49.28); settlementLegs 8.33% anchor seed 202608097 (integer tie at 75).** Both single-seed 3-outlier clusters. realizedAbs surface: synth/x402 today (fresh); spread realizedAbs 8.33% fires from 08-08 collapse (sub-threshold anyway). settlementLegs anchor 202608097 is a distinct seed from realizedAbs 202608096 — no seed-sharing across these fresh clusters (unlike the maxSpot 2-seed anchor which spans 3 modes).

8. **[LOW — zero-firing at n=2 filed runs] Zero within-mode strong pairs |r| ≥ 0.8 in any mode.** Continuing 08-08's zero-firing streak (which itself broke 08-07's spread `realizedAbs × maxCurve` = +0.897 single fire). Strongest within-mode pair today: spread `seed × wallet_sum_pnl` = +0.650 (float-dust, filtered per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). Strongest non-dust within-mode pair: basket `realizedAbs × minCurve` = −0.651 (below threshold). Rotation-not-persistence remains the default class for within-mode strong pairs (0 pairs have crossed n=3 in the 24-run window).

9. **[LOW — spread wallet_sum_pnl 8.33% outlier at 3 rows] Seed 2026080912 anchors +1.41e−11 across all 3 roles.** Filtered per float-dust rule — σ(spread wallet_sum_pnl) = 5.91e−12, 11 orders below any meaningful threshold. Reported only for completeness. Basket + synth + x402 wallet_sum_pnl show 0 outliers today.

10. **[PASS] Conservation healthy in every mode.** wallet_sum_pnl mean −1.36e−15 (basket), −1.14e−13 (spread), −2.08e−15 (synth/x402). Std 2.85e−14, 5.91e−12, 1.04e−14. All ≥11 orders under the |mean| > 10 / std > 100 gates. Basket sign held (−), magnitude comparable to 08-08. Spread sign flipped + → −, magnitude down ~15%. Synth/x402 sign held (−), magnitude up ~2.8× (still 15 orders under threshold).

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical wallet_sum_pnl / role_pnl / all price columns; only x402Total distinguishes x402). Basket-vs-synth 2.5× cross-scale identity holds on maxSpot / minSpot rank order (but NOT on maxCurve / minCurve today — a new observation that yesterday's report claimed as universal is more nuanced; the discrete-tie granularity of maxCurve breaks the rank-preservation on some filings).

12. **[PASS] Row/column/mode shape unchanged vs 08-08.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: rounds=60 everywhere, deniedWorkers=0 everywhere, x402Total=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` cash everywhere except x402 (x402); `spotSource` deterministic per mode. No new constant-column drift.

13. **[LOW — recurring, 24th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char (202608091–202608099), 36 rows 10-char (2026080810–2026080812). `seed` outlier_pct = 25.00% in every mode. Padded seeds continue to over-represent in outlier positions: 2 of today's 4 spread-cluster anchors (2026080910 + 2026080911) are padded — 50% concentration where the population share is 25%. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.36e−15  | 2.85e−14  | OK |
| spread    | −1.14e−13  | 5.91e−12  | OK |
| synthetic | −2.08e−15  | 1.04e−14  | OK |
| x402      | −2.08e−15  | 1.04e−14  | OK |

## Within-mode signal

- **PROMOTED: retail-role loss concentration in spread mode is structural at n=4 filings.** 11/12 spread seeds today land retail in negative role_pnl. Mean retail role_pnl −$9,272 vs analyst +$2,750 vs operator +$4,801. Top-7 losses all retail. Yesterday's carry-over crossed the n=4 rule — moves from carry-over into a real signal. Suggested next step: audit `scripts/fleet-executors/deployer.mjs` or `prototypes/compute-futures/scenario-sweep.mjs` for how role P&L is split in spread mode; distinguish intentional adversarial pricing (retail-eats-spread by design) from a role-assignment bug.
- **Cross-mode 2-seed maxSpot persistence at n=2 with rotating anchors.** Basket + synth + x402 all fire maxSpot at 16.67% today, all anchored by same 2 seeds (202608093 + 202608096). Same *shape* as 08-08 (which also fired all 3 modes at 16.67% on same 2 seeds), different *anchor identities*. Structural cross-mode signal upstream of mode: whichever seeds happen to have extreme minSpot/maxSpot rank in a given filing propagate to all small-price modes 1:1. Genuine within-mode signal.
- **Zero within-mode strong pairs** at |r| ≥ 0.8 in any mode. Second consecutive zero-firing filing after 08-07's spread realizedAbs × maxCurve snap-back. Strongest today: basket `realizedAbs × minCurve` = −0.651 (below threshold, deterministic AMM inverse relationship). Zero pairs have crossed n=3 in the 24-run window.
- **Super-anchor two-seed day.** Seeds 202608096 (15 positions) and 202608093 (12 positions) both cross the super-anchor threshold. First TWO-super-anchor filing in the window. The two seeds jointly drive 12 unique physical rows into outlier positions across basket + spread + synth + x402.
- **x402 `settlementLegs × x402Total` continues to alternate around zero.** 18-filing series: … +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → +0.179 → **+0.290**. No interpretive story. Alternation-not-persistence class holds.

## Row count and shape vs 2026-08-08

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char, same values).
- Constants by design all hold: rounds=60 everywhere, deniedWorkers=0 everywhere, x402Total=0 outside x402. `live` / `settlement` / `spotSource` deterministic per mode as before.
- **Outlier surface partially rotates.** 08-08: cross-mode HIGH `maxCurve` fires + cross-mode MEDIUM `maxSpot` fires + super-single anchor seed 202608083. 08-09: `maxCurve` HIGH cluster collapses to zero, `maxSpot` MEDIUM cluster persists with rotated anchors, super-double anchor day (202608096 + 202608093). New fresh: 4-column spread sub-threshold burst (minSpot / maxSpot / minCurve / maxCurve all at 8.33% with distinct single-seed anchors) + synth/x402 realizedAbs + settlementLegs sub-threshold + x402Total new all-time high + retail-role structural promotion.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical wallet_sum_pnl, role_pnl, all price columns between synth and x402; only x402Total distinguishes x402). Today's maxSpot + realizedAbs + settlementLegs outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale — REFINED observation.** Basket-vs-synth 2.5× ratio holds at every quantile for maxSpot + minSpot (verified) but rank-order identity BREAKS today on maxCurve + minCurve (verified: basket rank ≠ synth rank per seed for these two columns). Yesterday's report claimed the identity as universal across all four price columns; today's cross-check reveals it's column-specific — the discrete-tie granularity of maxCurve/minCurve values (only 29 unique values across 144 rows) breaks strict rank-preservation. Update to prior observation.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** rounds=60, deniedWorkers=0, x402Total=0 outside x402, live/settlement/spotSource deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 11+ orders under threshold in every mode. Notable excluded pairs today: spread `wallet_sum_pnl × seed` = +0.650, basket `wallet_sum_pnl × minSpot` = −0.407. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 24th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]]. Padded-seed concentration in spread outlier anchors today: 2/4 (50%) — well above the 25% population share; adds to the padding-fix PR's motivation.
- **12-seed outlier_pct rotation-not-persistence.** 08-08's cross-mode HIGH maxCurve fires (25%) all collapse to 0 today. 08-08's cross-mode MEDIUM maxSpot fires (16.67%) PERSIST at n=2 with rotated anchors — first n=2 persistence in the 24-run window for a cross-mode MEDIUM. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode single-seed anchor pattern shifts to TWO super-anchors.** Series 08-06 four concurrent → 08-07 diffuse → 08-08 super-single 202608083 → **08-09 super-double 202608096 (15 pos) + 202608093 (12 pos)**. Class oscillates: single → multi → diffuse → super-single → super-double.
- **x402 `settlementLegs × x402Total` alternation continues** (18-filing series ±). No promotable trend.
- **x402Total range extends** — new individual max $456.76 today. Mean $337.93 still inside the 8-filing $322–$346 band. No trend break at mean level.
- **PROMOTED: retail-role concentrates spread's largest losses — n=4 filings crossing the promotion threshold.** Moves from carry-over to top-of-report finding. Requires audit of spread-mode role P&L split logic to distinguish intentional-by-design from bug.
