# Compute-Futures EDA — 2026-07-24

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-24.csv` (144 rows, 4 modes)
**Run:** 2026-07-24
**Prior filed report:** 2026-07-21 (3-day gap; 07-22 CSV exists but was not filed, 07-23 has no CSV)

## Top findings

1. **[HIGH] Basket `realizedAbs` outlier_pct 25.00% — first time the column has flagged ≥ 10% for basket in the current record.** 9 of 36 rows outside IQR whiskers [$190.58, $265.29]: three rows at $153.45 (low tail), three at $275.51 and three at $276.27 (high tail). Mean $225.45, std $32.03, range $153.45–$276.27. Distribution across 12 distinct per-seed values: bulk cluster in [$213, $234] (24 rows across 8 seed values), then three seeds pulling to the tails ($153, $275, $276). Prior basket `realizedAbs` outlier_pct: 0% on 07-21, 07-20, 07-18, 07-17, 07-16, 07-12, 07-10. This is a new-to-column-mode HIGH — the first basket realizedAbs flag on record. Escalation gate: if it holds ≥ 10% next run, move to persistence-candidate class. See [[compute-futures-12-seed-sample-too-small]].
2. **[MEDIUM — persistence-candidate] Spread `minSpot` outlier_pct 16.67% — THIRD identical-magnitude appearance in 14 days.** 6 of 36 rows below IQR whisker: three at $40.73, three at $41.50. Mean $43.41, std $1.25, range $40.73–$45.14. Prior spread `minSpot` filings: 07-10 (16.67%, mean $42.97/std $1.41/range $40.08–$44.80), 07-21 (16.67%, mean $43.48/std $1.65/range $39.75–$45.68), today (16.67%, mean $43.41/std $1.25/range $40.73–$45.14). **Per 07-21's escalation gate ("if either column stays ≥ 10% next run, move both to persistence-candidate class"), spread `minSpot` clears the persistence threshold — three identical-magnitude flags out of three filed runs since 07-10.** The exact-duo partner basket `settlementLegs` collapsed to 0.00% today, so only `minSpot` graduates; the duo pattern (07-10 ⇄ 07-21) does not survive the 07-24 filing. Structural reclassification: spread `minSpot` outlier_pct is no longer 12-seed rotation noise — treat as a recurring within-mode signal until either the magnitude drops below 10% or the seed count widens per [[compute-futures-12-seed-sample-too-small]].
3. **[MEDIUM — collapse] Both 07-21 MEDIUMs partially cleared; the exact-duo re-run pattern from 07-10 does not survive.** Basket `settlementLegs` 16.67% → 0.00% (all 6 whisker tags disappeared: 84-row rows gone entirely, distribution tightened from {77:3, 78:3, 79:3, 80:12, 81:6, 82:6, 84:3} on 07-21 to {77:3, 78:3, 79:3, 80:9, 81:6, 82:6, 83:6} today — max shifted 84→83). Spread `minSpot` held at 16.67% (see finding 2). Net: the (basket settlementLegs 16.67% + spread minSpot 16.67%) duo that re-matched 07-10 on 07-21 does NOT re-match today — only the `minSpot` half held, and it graduates to persistence-candidate solo. The joint reappearance was a two-run 07-10↔07-21 event, not a three-run pattern.
4. **[MEDIUM — new] Basket `role_pnl` σ widened +30% vs 07-21.** μ = −$0.71 (was −$1.12), σ = $37.73 (was $28.93). Range −$85.49 to +$76.88 (was −$71 to +$56 on 07-21). Alternating pattern from prior filings: 07-17 σ≈$29 → 07-18 $28 → 07-20 $34 → 07-21 $29 → today $38 — three widening steps in the last four filings; no longer clean alternation. Not tied to a specific outlier column; a distributional-shape shift under the same 12-seed cohort. Watch for a fourth consecutive widening next run.
5. **[LOW — streak break] x402 `settlementLegs × x402Total` sub-threshold sign held positive two runs in a row for the first time in 8 rotations, magnitude 4×.** Series: 07-10 +0.216, 07-12 −0.315, 07-16 +0.732, 07-17 −0.604, 07-18 +0.028, 07-20 −0.110, 07-21 +0.156, today +0.657. **First non-flip in the 8-run record** — same sign as 07-21 AND magnitude jumped from +0.156 to +0.657 (roughly 4×, still sub-threshold). Companion pair `wallet_sum_pnl × settlementLegs` in synth/x402 = +0.757 today (was float dust in prior filings; excluded from |r|≥0.8 reporting per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). The sign-flip signature that characterized the pair as "cleanest 12-seed-jitter" for seven rotations no longer holds; if it flips back to negative next run, restore the jitter classification, otherwise this becomes a signal worth investigating.
6. **[LOW — eighth consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong-pair candidate. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, today. Non-wallet_sum_pnl within-mode near-misses: synth/x402 `seed × realizedAbs` = −0.710 (fresh, absent in 07-21 report); spread `maxCurve × minSpot` = −0.696; basket `realizedAbs × minSpot` = −0.589; basket `seed × realizedAbs` = −0.545. All sub-threshold. `settlementLegs × x402Total` (see finding 5) is the notable churn signal, not any of these.
7. **[LOW — recurring] Seed 25% encoding artifact unchanged, 14th consecutive filed run.** 108 rows 9-char seed (`202607241`–`202607249`), 36 rows 10-char seed (`2026072410`–`2026072412`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
8. **[LOW — recurring] x402Total inside the ~$100 stable band, fourteenth consecutive run.** Mean $325.55 (down $10.99 vs 07-21's $336.54 — first mean-down move after 07-21's mean-up drift), range $268.24–$388.63 (band widened both ends: low 268 vs 285, high 389 vs 378), sum $11,719.72 across 36 x402 rows. No x402Total outlier flag (bimodal-by-design exclusion holds). Fourteen-filing window: mean between $321 and $337; range envelope ~$268–$389.
9. **[LOW — recurring] Spread wallet_sum_pnl absolute range 1.86e−11.** absmax 9.78e−12; Σ σ across modes: basket 3.73e−14 (up from 2.01e−14 on 07-21), spread 5.81e−12 (up from 4.24e−12 on 07-21, ends the drift-down trajectory 07-18 5.86e−12 → 07-20 5.65e−12 → 07-21 4.24e−12 → today 5.81e−12), synth/x402 8.66e−15 (down from 1.20e−14). Still float dust 12+ orders under any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.43e−14 (basket), +7.29e−13 (spread), −2.11e−15 (synth), −2.11e−15 (x402). Std 3.73e−14 (basket), 5.81e−12 (spread), 8.66e−15 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. No conservation drift signal in any mode.
11. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-21.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
13. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.43e−14  | 3.73e−14  | OK |
| spread    | +7.29e−13  | 5.81e−12  | OK |
| synthetic | −2.11e−15  | 8.66e−15  | OK |
| x402      | −2.11e−15  | 8.66e−15  | OK |

## Within-mode signal

- **Basket `realizedAbs` HIGH — 25.00% (9/36), first appearance in the record for this column in this mode.** Three-way tail split: 3 rows at $153.45 (low), 6 rows at $275.51/$276.27 (high). Bulk 24 rows compressed into [$213, $234] over 8 distinct per-seed values. The seed pulling the low tail ($153.45) is a single seed × 3 roles; the high tail is two seeds × 3 roles each. This is the first HIGH in basket realizedAbs across the filed record — either a genuine within-mode regime shift or a 12-seed sampling coincidence where three of twelve seeds happen to land far from center. Escalation gate: hold ≥ 10% next run → persistence-candidate.
- **Spread `minSpot` MEDIUM persistence-candidate — 16.67% (6/36), third identical-magnitude appearance across three filed runs since 07-10.** Range $40.73–$45.14 around $43.41 mean/std $1.25. Six low-end whisker tags at $40.73 and $41.50. Compare 07-10 ($40.08–$44.80, μ$42.97/σ$1.41) and 07-21 ($39.75–$45.68, μ$43.48/σ$1.65). All three filings show tight std (~$1.3–$1.6), all show 6 low-tail whisker rows, all show ~$40.7–$45.7 range. This is a stable within-mode signal, not rotation — the third recurrence promotes it out of the 12-seed jitter class. Root-cause work now warrants sweeping the spread-mode spot generator over a wider seed count per [[compute-futures-12-seed-sample-too-small]] to distinguish (a) real bimodal spot distribution from (b) 12-seed sampling floor.
- **Duo pattern from 07-10↔07-21 does NOT survive to today.** Basket `settlementLegs` half collapsed 16.67% → 0.00%. Only spread `minSpot` half held. The one-run duo re-match on 07-21 was event-driven, not a three-run structural signal. Kept as a carryover: the two columns can drift together on individual runs but neither implies the other over multiple filings.
- **Correlation surface — no strong pairs, expected sub-threshold churn.** Basket: strongest non-wallet_sum_pnl pairs `realizedAbs × minSpot` = −0.589, `seed × realizedAbs` = −0.545, `realizedAbs × settlementLegs` = −0.511 — the newly-flagged basket realizedAbs also anchors the three strongest basket correlations, consistent with the column having shifted distribution. Spread: strongest is `maxCurve × minSpot` = −0.696 (unchanged sign vs prior runs; not previously reported as a churn signal). Synth/x402 (identical): `seed × realizedAbs` = −0.710 (near threshold; not present at |r|>0.5 on 07-21). x402-only `settlementLegs × x402Total` = +0.657 breaks 7-run sign-flip streak (see finding 5).
- **role_pnl scale per mode.** spread σ = $7,015 (μ = −$515) — narrowed sharply from $10,464/−$511 on 07-21, breaking the 3-run widening streak 07-17 $9,610 → 07-18 $8,898 → 07-20 $9,892 → 07-21 $10,464 → today $7,015. Basket σ = $37.73 (μ = −$0.71) — widened from $28.93/−$1.12 on 07-21 (see finding 4). Synth/x402 σ = $12.08 (μ = −$0.47) — widened from $10.25/−$0.55 on 07-21. Two of three modes widened, one narrowed sharply — no clean fleet-wide trend.

## Row count and shape vs 2026-07-21

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated sharply.** 07-21 fired 2 MEDIUMs (basket settlementLegs 16.67%, spread minSpot 16.67%) — the settlementLegs half collapsed to 0.00%, spread minSpot held and graduates to persistence-candidate. Today's headline is **1 HIGH (basket realizedAbs 25.00%, first appearance)** + 1 MEDIUM persistence-candidate — full column-mode substitution AND a severity escalation vs prior filing. No column-mode pair from 07-21 survives at ≥ 10% today except spread minSpot.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; eight consecutive filed runs with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under the threshold everywhere. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 14th consecutive filed run. One-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — challenged today for the first time. Spread `minSpot` clears the third-run persistence gate at identical 16.67% magnitude across 07-10, 07-21, 07-24. Basket `realizedAbs` at 25.00% is one filing away from the same class. Rotation-not-persistence still holds as the default classification for other columns but the pattern is no longer clean; a wider seed count is the discriminator per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` sign-flip streak** — broke today after 7 consecutive rotations. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (today). Same sign held for the first time; magnitude 4×. If today's +0.657 flips negative next run, restore jitter classification; if it holds positive, treat as an emerging structural pair.
- **Spread role_pnl σ 3-run widening streak** — broken today. $8,898 (07-18) → $9,892 (07-20) → $10,464 (07-21) → $7,015 (today). Widening trajectory does not extend; spread role_pnl σ back near 07-18 shape.
