# Compute-Futures EDA — 2026-07-07

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-07.csv` (144 rows, 4 modes)
**Run:** 2026-07-08

Two-day gap since prior report (2026-07-05): the 2026-07-06 CSV landed but the daily skill missed its 06:00 slot on ISS-006 (day-16 morning-batch silence), so no 2026-07-06 findings file exists. Today's diff is therefore against 2026-07-05.

## Top findings

1. **[MEDIUM] basket `maxCurve` outlier_pct = 16.67% (6/36)** — six rows sit at the top of a very tight cluster (μ=0.238, σ=0.0056, range 0.2275–0.2500, only seven unique values). IQR fires on the upper knot. New (mode, column) pair vs 2026-07-05, which flagged basket `wallet_sum_pnl` 16.67% and did NOT flag basket `maxCurve`. Same 12-seed IQR rotation shape as prior days. No underlying regime change. [[compute-futures-12-seed-sample-too-small]]
2. **[MEDIUM] synthetic/x402 `maxCurve` outlier_pct = 16.67% (6/36 each)** — six rows in each mode sit at the upper knot of another tight cluster (μ=0.0995, σ=0.0038, range 0.095–0.105). Same physical cluster reported twice per the synthetic ≡ x402 tautology. Yesterday's synthetic/x402 `minCurve` 16.67% flag is 8.33% today; today's `maxCurve` 16.67% is new. Same 12-seed rotation. [[compute-futures-12-seed-sample-too-small]]
3. **[LOW] spread top pair `realizedAbs × wallet_sum_pnl` r = −0.752 (near the 0.8 gate)** — new leader, biggest |r| we've logged in any mode-run. Mechanically it's the meaningful-signal × float-dust pattern: `wallet_sum_pnl` in spread has σ ≈ 6e−12 (machine epsilon accumulated over 60 rounds), and larger `realizedAbs` runs accumulate more of that dust with a consistent sign. Not a conservation break — the conservation gate (|mean|>10 or std>100) still holds by twelve orders of magnitude. If it repeats twice more it might be worth noting as a numerical-drift telemetry signal, not as strategy P&L structure.
4. **[LOW] x402Total range cooled to $277.07–$371.02, mean $327.97, total $11,806.85** — down from 2026-07-05's $296.55–$420.73, mean $336.33, total $12,107.70. Third consecutive movement (2026-07-03 $273.65–$366.67 → 2026-07-05 $296.55–$420.73 → 2026-07-07 $277.07–$371.02). Oscillates within a ~$100 band, not trending. `settlementLegs × x402Total` r = −0.110 today (down from +0.557 on 2026-07-05, +0.685 on 2026-07-03) — the x402-specific structural link continues to weaken across runs. Sub-threshold either way.
5. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows carry 9-digit seeds, 36 rows carry 10-digit (`2026070710`, `2026070711`, `2026070712`). Outlier detector flags `seed` at exactly 25% in every mode, eleventh consecutive run. One-line pad in `scenario-sweep.mjs` still pending — see [[compute-futures-seed-padding-bug]].
6. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans −3.2e−15 → −1.8e−12, std 1.6e−14 → 6.0e−12 — float dust across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate. spread std widened slightly vs 2026-07-05 (6.0e−12 vs 4.7e−12) but still 13 orders of magnitude below the gate.
7. **[PASS] No within-mode correlations at |r| ≥ 0.8.** Top per mode: basket `minSpot × wallet_sum_pnl` −0.565, spread `realizedAbs × wallet_sum_pnl` −0.752, synthetic `minCurve × minSpot` +0.507, x402 `realizedAbs × x402Total` +0.548. `minSpot × maxCurve` (2026-07-05's near-leader across three modes) has collapsed everywhere today (basket +0.107, spread −0.377, synthetic/x402 −0.382). Watch abandoned.
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.34e−14  | 2.19e−14  | OK |
| spread    | −1.82e−12  | 6.04e−12  | OK |
| synthetic | −3.24e−15  | 1.59e−14  | OK |
| x402      | −3.24e−15  | 1.59e−14  | OK |

## Within-mode signal

- **role_pnl scale per mode (regime unchanged):** spread std ≈ $6,832 (μ=−$550.34), basket std ≈ $29.41 (μ=−$0.72), synthetic/x402 std ≈ $10.53 (μ=−$0.24). Spread narrowed vs 2026-07-05 ($8,107); basket and synthetic/x402 also tightened slightly. All three still well inside 12-seed jitter and spread continues to dominate pooled stats by three orders of magnitude — per-mode stratification remains the right default.
- **spread `realizedAbs × wallet_sum_pnl` r = −0.752** — closest any mode has come to the 0.8 gate this run window. Not a real signal at the P&L level (wallet_sum_pnl is float dust) but a real numerical-artifact correlation: larger absolute settlement volume accumulates larger absolute floating-point conservation error. Note for the numerics ledger, don't treat as strategy structure.
- **`minSpot × maxCurve` watch abandoned.** Yesterday's near-leader across three of four modes (+0.593 to +0.682) collapsed everywhere today. Confirms it was 12-seed noise, not a nascent floor/ceiling coupling.
- **x402 mirrors synthetic exactly at the role level** — role-level numeric columns differ by 0.0. The only x402-specific structure this run is the `realizedAbs × x402Total` r = +0.548 and `maxSpot × x402Total` r = +0.542 pairs — plausible (bigger trades → more x402 fees), sub-threshold.

## Row count and shape vs 2026-07-05

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged: 9/12 nine-digit, 3/12 ten-digit).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single- and low-double-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold. (None today, but return-visits expected.)
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string. One-line pad in `scenario-sweep.mjs` still pending.
- **12-seed outlier_pct instability** — mode-column pair that fires shifts each run: 06-26 spread minCurve 25% → 06-28 spread settlementLegs 33% → 07-03 synthetic/x402 settlementLegs 16.67% → 07-05 basket wallet_sum_pnl 16.67% + synthetic/x402 minCurve 16.67% → 07-07 basket maxCurve 16.67% + synthetic/x402 maxCurve 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `realizedAbs × minSpot`: [−0.06 (06-26), −0.745 (07-03), +0.048 (07-05), −0.467 (07-07)]; spread `realizedAbs × wallet_sum_pnl`: new near-leader today at −0.752. Consistent 12-seed jitter until any pair confirms twice.
