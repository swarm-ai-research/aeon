# Compute-Futures EDA — 2026-07-21

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-21.csv` (144 rows, 4 modes)
**Run:** 2026-07-22
**Prior filed report:** 2026-07-20 (day-over-day; no gap this run)

## Top findings

1. **[MEDIUM] Basket `settlementLegs` outlier_pct 16.67% — third basket appearance at ≥ 10% in the record, exact duo match with 2026-07-10.** 6 of 36 rows outside IQR whiskers [79.75, 81.25] × 1.5 → [77.5, 83.5]: three rows at 77, three rows at 84. Distribution {77:3, 78:3, 79:3, 80:12, 81:6, 82:6, 84:3}. Prior basket `settlementLegs` ≥ 10% appearances: 07-10 (25%), 07-16 (16.67%), today (16.67%). Full column rotation over 11 days: 07-10 basket 25% → 07-12 spread 16.67% → 07-16 basket 16.67% → 07-18 spread 25% HIGH → 07-20 none → 07-21 basket 16.67% — five appearances across four rotation events. Third-basket-appearance is a *within-mode* recurrence, not just a fleet-wide rotation; escalation gate: fourth basket appearance at ≥ 20% next run reopens the persistence question. See [[compute-futures-12-seed-sample-too-small]].
2. **[MEDIUM] Spread `minSpot` outlier_pct 16.67% — second appearance in the record, exact duo match with 2026-07-10.** 6 of 36 rows below IQR whisker 41.02: three rows at 39.747, three at 40.823. Mean $43.48, std $1.65, range $39.75–$45.68. Prior spread `minSpot` ≥ 10%: 07-10 (16.67%) — collapsed to 8.33% on 07-12 and stayed ≤ 8.33% through 07-20. Today's magnitude, IQR shape, and 6-of-36 count are numerically indistinguishable from 07-10 (that run: mean $42.97, std $1.41, range $40.08–$44.80, six low-end whisker tags). Combined with finding 1, **the exact (basket settlementLegs 16.67%, spread minSpot 16.67%) duo from 07-10 recurs 11 days later** — first time in the current record a two-mode-two-column MEDIUM pair from a prior run reappears with the same magnitudes. Structurally interesting; still 12-seed rotation cohort until a third joint appearance.
3. **[MEDIUM — collapse] All four of 2026-07-20's MEDIUMs dropped below the 10% gate within one seed rotation.** 07-20 filed: spread `realizedAbs` 16.67% → today 8.33% (3/36 rows, mean $53,844, range $37,737–$97,818); spread `minCurve` 16.67% → today 0.00%; spread `maxCurve` 16.67% → today 0.00%; basket `minCurve` 16.67% → today 8.33% (3/36). Zero-persistence outcome: not one 07-20 MEDIUM held magnitude today. Combined with today's two new-to-mode MEDIUMs (findings 1, 2), the outlier surface rotated from a spread-curve/realizedAbs cluster to a basket-legs/spread-spot duo — a full column-mode substitution. 12-seed rotation-not-persistence rule holds for the fifth consecutive filing.
4. **[LOW — seventh consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong-pair candidate. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, today. Sub-threshold within-mode churn on the x402 pair set: `settlementLegs × x402Total` = +0.156 (was −0.110 on 07-20 — **seventh consecutive sign-flip in seven rotations**, unbroken); `seed × x402Total` = +0.352 (was −0.447 on 07-20 — 0.80 swing, sign flipped); `maxSpot × x402Total` = −0.433 (was −0.233 on 07-20 — same sign, roughly doubled); `realizedAbs × x402Total` = −0.241 (was −0.583 on 07-20 — same sign, weakened); `minSpot × x402Total` = −0.228 (was −0.114 on 07-20 — same sign, roughly doubled); `wallet_sum_pnl × x402Total` = +0.066 (was +0.635, float-dust artifact per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]], excluded). Two structural pairs same-sign-strengthening two runs in a row (maxSpot, minSpot × x402Total), neither crosses 0.5 — log-and-watch.
5. **[LOW — recurring] Seed 25% encoding artifact unchanged, 13th consecutive filed run.** 108 rows 9-char seed (`202607211`–`202607219`), 36 rows 10-char seed (`2026072110`–`2026072112`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
6. **[LOW — recurring] x402Total inside the ~$100 stable band, thirteenth consecutive run.** Mean $336.54 (up $15.38 vs 07-20's $321.16 — first mean-up drift in three filings), range $285.63–$378.49, sum $12,115.36 across 36 x402 rows. No x402Total outlier flag today (bimodal-by-design exclusion holds anyway). Band remains ~$285–$378 over the thirteen-filing window.
7. **[LOW — recurring] Spread wallet_sum_pnl outliers 0%, tightened further.** absmax = 7.73e−12 (07-20: 1.55e−11 — halved). Σ σ across modes: basket 2.01e−14, spread 4.24e−12, synth/x402 1.20e−14. Spread σ continues the drift-down trajectory (07-18 5.86e−12 → 07-20 5.65e−12 → today 4.24e−12); still float dust 12+ orders below any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
8. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +9.21e−15 (basket), −2.25e−12 (spread), −7.49e−16 (synth), −7.49e−16 (x402). Std 2.01e−14 (basket), 4.24e−12 (spread), 1.20e−14 (synth/x402). All 12+ orders under the |mean| > 10 / std > 100 gates.
9. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff role_pnl| = 0.0, max |diff realizedAbs| = 0.0, max |diff minSpot| = 0.0, max |diff maxSpot| = 0.0). Only `x402Total` distinguishes x402.
10. **[PASS] Row/column shape unchanged vs 07-20.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
11. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +9.21e−15  | 2.01e−14  | OK |
| spread    | −2.25e−12  | 4.24e−12  | OK |
| synthetic | −7.49e−16  | 1.20e−14  | OK |
| x402      | −7.49e−16  | 1.20e−14  | OK |

## Within-mode signal

- **Basket `settlementLegs` MEDIUM — 16.67% (6/36), third basket appearance ≥ 10% in the record.** Whisker tags at 77 (3 rows) and 84 (3 rows) around a 79–82 core. Prior filings: 07-10 (25%, low-end whisker tags at 78), 07-16 (16.67%). Within-mode recurrence is the sharper signal here than the fleet-wide `settlementLegs` rotation; the column has now flagged basket three times over 11 days, with the two recent appearances at the same 16.67% magnitude.
- **Spread `minSpot` MEDIUM — 16.67% (6/36), exact numerical rerun of 07-10.** Range $39.75–$45.68 around $43.48 mean/std $1.65; six low-end whisker tags at $39.75 and $40.82. Compare 07-10: range $40.08–$44.80 around $42.97/$1.41, six low-end tags. Both the shape and count line up almost exactly 11 days apart — the underlying spread minSpot regime looks noisier at the low end again after 11 days of ≤ 8.33% flags.
- **Exact-duo reappearance (basket settlementLegs 16.67% + spread minSpot 16.67%) from 07-10.** First time in the current record a two-column-two-mode MEDIUM pair from a prior filed run reappears with the same magnitudes on the same run. Not yet reclassifiable — a third joint appearance would move both columns out of the 12-seed rotation cohort. Structural gate: if either column stays ≥ 10% next run, the duo becomes the first candidate for a re-examine.
- **Collapse of all four 07-20 spread cluster MEDIUMs.** Spread `realizedAbs` 16.67% → 8.33%, spread `minCurve` 16.67% → 0%, spread `maxCurve` 16.67% → 0%, basket `minCurve` 16.67% → 8.33%. The 07-20 headline (three simultaneous spread curve/realizedAbs MEDIUMs) fully rotates out in one CSV, reasserting the rotation-not-persistence pattern. Within-mode correlations behind those columns: spread `realizedAbs × maxCurve` = +0.136 (07-20 +0.23 — weakened), `realizedAbs × minCurve` = +0.119 (07-20 −0.53 — sign flipped, magnitude collapsed), `realizedAbs × maxSpot` = +0.014 (07-20 +0.178 — collapsed). Correlation surface collapses in lockstep with the outlier surface.
- **role_pnl scale per mode (regime unchanged).** spread σ = $10,464 (μ = −$511) — widened from $9,892/−$532 on 07-20, third-consecutive widening in this streak (07-17 $9,610 → 07-18 $8,898 → 07-20 $9,892 → today $10,464); basket σ = $28.93 (μ = −$1.12) — narrowed from $34.5/−$1.44, back near 07-17's $29.4 shape (alternating pattern intact); synth/x402 σ = $10.25 (μ = −$0.55) — narrowed from $12.3/−$0.22. Spread continues to dominate pooled stats by two–three orders of magnitude — per-mode stratification remains the right default.
- **x402 sub-threshold pair drift.** Two same-sign-strengthening pairs across the 07-20 → today rotation: `maxSpot × x402Total` −0.233 → −0.433 (magnitude roughly doubled), `minSpot × x402Total` −0.114 → −0.228 (magnitude roughly doubled). Neither crosses 0.5; log-and-watch. Meanwhile `settlementLegs × x402Total` completes the seventh consecutive sign-flip in seven rotations (+0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156) — the pair remains the cleanest 12-seed-jitter signature in the correlation surface.

## Row count and shape vs 2026-07-20

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated from spread-curve/realizedAbs cluster to basket-legs/spread-spot duo.** 07-20 fired 4 MEDIUMs on spread (realizedAbs 16.67%, minCurve 16.67%, maxCurve 16.67%) + basket (minCurve 16.67%). Today: all four collapsed to 8.33% or 0%; two new MEDIUMs (basket settlementLegs 16.67%, spread minSpot 16.67%) that together exactly rematch the 07-10 duo. Net: 0 HIGH (unchanged), 2 MEDIUM (down 2, entirely different composition). No column-mode pair from 07-20 survives at ≥ 10% today.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; seven consecutive filed runs with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 12+ orders under the threshold everywhere. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 13th consecutive filed run. One-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted fifth consecutive filing. Today's exact-duo rerun of 07-10 (basket settlementLegs 16.67% + spread minSpot 16.67%) is the first joint reappearance in the current record and warrants a next-run watch, but does not yet break the rotation classification. Escalation gate: either column stays ≥ 10% next run → move both to persistence-candidate class. See [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (today). **Seventh sign-flip in seven rotations.** Continues 12-seed jitter classification.
- **Spread role_pnl σ three-run widening streak** — $8,898 (07-18) → $9,892 (07-20) → $10,464 (today). Not yet a filed finding class; note for next run.
