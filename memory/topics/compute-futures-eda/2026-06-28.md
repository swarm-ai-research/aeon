# Compute-Futures EDA — 2026-06-28

**Source:** `memory/gitlawb-compute-futures-proofs/2026-06-28.csv` (144 rows, 4 modes)
**Run:** 2026-06-29

## Top findings

1. **[MEDIUM] spread `settlementLegs` outlier_pct = 33.33% (12/36)** — IQR collapses to 0.75 because 18 of 36 spread rows sit at exactly 80 legs (q1=79.5, q3=80.25), so the fence flags every row at 76, 77, 78, 81, 82. The underlying range is 76–82 (mean 79.58, std 2.4) — normal jitter, not a regime change. This is the degenerate-IQR brittleness called out in the 2026-06-26 carry-over: with 12 seeds and a peaked distribution, single-column outlier_pct values swing wildly from day to day. Yesterday spread's 25% flag was `minCurve` (today 0%); the day before, `maxSpot` was at 8.33% in all four modes (today 0%). Next step unchanged: bump seed count to ≥30 in `prototypes/compute-futures/scenario-sweep.mjs` so the IQR stops being threshold-sensitive.
2. **[LOW] spread `wallet_sum_pnl × minSpot` r = −0.745 (new today)** — strongest within-mode correlation of the run, just below the 0.8 reporting bar. Don't promote: spread `wallet_sum_pnl` spans [−1.57e−11, +1.23e−11] — pure float noise — so this is correlation of noise against a seed-monotone column (`minSpot × seed` r=+0.681 in spread). The basket `wallet_sum_pnl × minCurve` companion was −0.736 in the 2026-06-20 baseline, +0.087 on 2026-06-26, +0.630 today — same noise-vs-input-driver oscillation. Watch for it to cross 0.8, but don't act on sub-threshold movement.
3. **[LOW — recurring] Seed-encoding artifact unchanged.** Three seeds are 10-digit (`2026062810`, `2026062811`, `2026062812`); the other nine are 9-digit. Outlier detector flags `seed` at exactly 25% in every mode (same as every prior run). One-line fix in `scenario-sweep.mjs` still pending.
4. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans −1.04e−12 → −2.07e−15, std 6.24e−15 → 7.38e−12 — float dust across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate.
5. **[PASS] No within-mode correlations at |r| ≥ 0.8.** Strongest pairs by mode: spread `wallet_sum_pnl × minSpot` −0.745 (see #2), spread `seed × minSpot` +0.681, basket/synthetic/x402 `seed × minSpot` ≈ −0.648, basket `wallet_sum_pnl × minCurve` +0.630. None survive the 0.8 bar; the recurring `seed × minSpot` pair is the seed-encoding artifact bleeding through.
6. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402 (μ=$336.84, σ=$25.96, range $287.19–$385.65 — slightly hotter than 2026-06-26's $278–$366).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.14e−14  | 2.23e−14  | OK |
| spread    | −1.04e−12  | 7.38e−12  | OK |
| synthetic | −2.07e−15  | 6.24e−15  | OK |
| x402      | −2.07e−15  | 6.24e−15  | OK |

## Within-mode signal

- **role_pnl scale per mode (regime unchanged):** spread std ≈ $9,582 (μ=−$610), basket std ≈ $28 (μ=−$1.36), synthetic/x402 std ≈ $10.6 (μ=−$0.22). spread continues to dominate pooled stats by three orders of magnitude — per-mode stratification remains the right default.
- **basket has a sub-threshold structural triple:** `seed × minSpot` −0.648, `seed × maxSpot` −0.646, `wallet_sum_pnl × minCurve` +0.630. The first two are the seed-encoding artifact (9-digit seeds cluster minSpot around one value, 10-digit seeds around another); the third is float-noise × input. No action.
- **x402 mirrors synthetic exactly at the role level** — the role-level numeric columns differ by 0.0. The only x402-specific signal is `x402Total`, which is independent of every other column (max |r| with other cols = 0.171, against `maxCurve`).
- **spread `settlementLegs` cluster is degenerate not anomalous:** 18/36 rows at value 80, 6 at 81, 3 each at 76, 77, 78, 82. Std=2.47 on a mean of 79.58. The 33% outlier flag is an IQR artifact, not a count anomaly. By contrast, basket `settlementLegs` (IQR=2.0, 12 at 82, 9 at 81, 6 at 80, 3 each at 74, 78, 84) flags only 8.33% — the same underlying spread, slightly wider IQR.

## Row count and shape vs 2026-06-26

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded YYYYMMDD+index seed string. One-line pad in `scenario-sweep.mjs`.
- **12-seed outlier_pct instability** — confirmed again today: yesterday's top spread flag (`minCurve` 25%) is at 0% today, today's top spread flag (`settlementLegs` 33%) was at 0% yesterday. Treat any single-column outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — basket `wallet_sum_pnl × minCurve` has been [−0.736, +0.087, +0.630] across [baseline, 2026-06-26, today]. All three are float-noise × seed-driver products of a 12-seed sample; ignore until one crosses 0.8.
