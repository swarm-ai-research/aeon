# Compute-Futures EDA — 2026-07-18

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-18.csv` (144 rows, 4 modes)
**Run:** 2026-07-19
**Prior filed report:** 2026-07-17 (day-over-day; no gap)

## Top findings

1. **[HIGH] basket `maxCurve` outlier_pct 33.33% — first-ever per-mode column at ≥ 20% and third consecutive strengthening run.** 12 of 36 rows tag IQR whiskers (6 low at 0.2455/0.2465, 6 high at 0.272/0.274, around a 0.256–0.261 core). Trend across the last four filed runs: 07-15 0% → 07-16 8.33% → 07-17 16.67% → today 33.33%. Doubles at every step. This is the FIRST (mode, column) pair in the record to both persist AND strengthen across three consecutive runs — every prior flag rotated within one seed cycle. Mean has been level (0.256–0.261) since 07-16 while the tail keeps thickening. Consistent with the underlying `maxCurve` level-shift near 07-14/07-15 (mean jumped 0.239 → 0.265 that week) settling into a new bimodal seed distribution. Watch: same-column persistence at ≥ 20% on next CSV escalates from "structurally shifted distribution" to "seed-population change worth explaining"; rotation back to <20% keeps it inside 12-seed IQR-tight-band jitter per [[compute-futures-12-seed-sample-too-small]].
2. **[HIGH] spread `settlementLegs` outlier_pct 25%.** 9 of 36 rows tag whiskers. Distribution today {77: 6, 79: 9, 80: 15, 81: 3, 82: 3} — 77 is the whisker outlier (q25=79, q75=80, IQR=1, lo=77.5). Yesterday spread settlementLegs was 0% with a more even {78: 6, 79: 9, 80: 6, 81: 9, 82: 6} distribution. Compression from a 5-value to a 4-value distribution with a single low straggler is the classic 12-seed IQR-tight-band shape. Note this is the settlementLegs flag rotating BACK into spread — 07-12 spread settlementLegs was flagged, 07-16 basket settlementLegs was flagged, 07-17 no settlementLegs flag anywhere, today spread again. Rotation pattern intact for settlementLegs.
3. **[MEDIUM] spread `maxCurve` outlier_pct 16.67% — new column for spread.** 6 of 36 rows tag whiskers; range 68.34–73.96 around a 70.85 mean. Spread has never previously flagged maxCurve at ≥ 10% in the record. Paired with finding 1 (basket maxCurve HIGH), two of four modes now flag maxCurve on the same run — first cross-mode co-appearance of maxCurve since synth/x402 pair 07-16/07-17 (which rotated to 0% today). If maxCurve continues to flag in both basket and spread next CSV, this becomes a systemic shape claim rather than per-mode jitter.
4. **[MEDIUM] basket `minCurve` outlier_pct 16.67% — new column for basket.** 6 of 36 rows tag; range 0.111–0.120 around a 0.116 mean. Yesterday was 8.33% (sub-threshold), today crosses the 10% gate. Same seed-level IQR-tight-band shape as prior filings.
5. **[LOW — fifth consecutive filed run at zero |r|≥0.8].** No mode fires any strong-pair candidate today. Streak: 07-10, 07-12, 07-16, 07-17, today. Sub-threshold churn worth noting: x402 `settlementLegs × x402Total` collapses to +0.028 (was −0.604 on 07-17, +0.732 on 07-16, −0.315 on 07-12, +0.216 on 07-10 — **fifth sign-flip in five rotations**); x402 `maxCurve × x402Total` = −0.522 (was +0.447 yesterday — sign flipped again); x402 `seed × x402Total` = +0.559 (yesterday −0.069, so a 0.63 swing); spread `realizedAbs × minCurve` = −0.708 (was −0.771 yesterday, persistent seed-shape correlation across every recent run); spread `maxSpot × minCurve` = +0.674 (was subthreshold, persistent). None strengthens two runs in a row.
6. **[LOW — collapse of 07-17 leaders]** Yesterday's four-mode `maxSpot` MEDIUM cluster (basket/spread/synth/x402 all 16.67%) reverted to 0% in every mode today. Yesterday's `minSpot` HIGH cluster in basket/synth/x402 (25% each) also reverted to 0% everywhere. Yesterday's synth/x402 `maxCurve` MEDIUM (the celebrated "first persistence") **rotated to 0%** — persistence broke at run 2. The curve tail (basket maxCurve, spread maxCurve, basket minCurve) fully replaced the spot tail from 07-17 within one seed rotation. 12-seed rotation pattern intact.
7. **[LOW — recurring] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202607181`–`202607189`), 36 rows 10-char seed (`2026071810`–`2026071812`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending [[compute-futures-seed-padding-bug]].
8. **[LOW — recurring] x402Total inside the ~$100 stable band, tighter today.** Mean $322.56, range $285.22–$361.99, sum $11,612.26 across 36 x402 rows. Tenth consecutive run inside the stable envelope since 07-03. Range tightened materially vs 07-17 ($272.51–$441.33 → $285.22–$361.99) — no x402Total outlier flag today (yesterday was 8.33%, excluded per bimodal-by-design rule anyway). Mean drifted $6 lower ($328.63 → $322.56).
9. **[LOW — recurring] wallet_sum_pnl outliers collapsed toward zero.** Today: basket 0%, spread 8.33%, synth/x402 0%. Yesterday: basket 16.67%, spread 25%, synth/x402 33.33%. Big drop in raw counts; underlying magnitudes still float dust (basket σ 6.34e−14, spread σ 5.86e−12, synth/x402 σ 1.27e−14). Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — the count collapse just reflects that today's float-dust distribution happens to have a tighter IQR.
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.26e−15 (synth/x402) → −1.45e−12 (spread); std 1.27e−14 (synth/x402) → 5.86e−12 (spread). All twelve-plus orders under the |mean|>10 / std>100 gates. Spread `wallet_sum_pnl` σ widened again vs 07-17 (5.37e−12 → 5.86e−12) — third consecutive run of drift-up (3.22e−12 → 5.37e−12 → 5.86e−12) but still float dust twelve orders below any real gate.
11. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-17.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold. Live/settlement/spotSource per-mode assignments unchanged.
13. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +9.18e−15  | 6.34e−14  | OK |
| spread    | −1.45e−12  | 5.86e−12  | OK |
| synthetic | +1.26e−15  | 1.27e−14  | OK |
| x402      | +1.26e−15  | 1.27e−14  | OK |

## Within-mode signal

- **basket `maxCurve` HIGH — 33.33% (12/36), third consecutive strengthening run (8.33 → 16.67 → 33.33%).** Six low-tail rows tag 0.2455/0.2465 (= 2 seeds × 3 roles), six high-tail rows tag 0.272/0.274 (= 2 seeds × 3 roles), around a 0.256–0.261 core. Four of 12 seed values now sit in the whisker. The underlying `maxCurve` mean level-shifted from 0.239 (07-13) to 0.265 (07-15) and has held at 0.256–0.261 since — the tail is thickening on a stationary mean. First (mode, column) pair to persist AND strengthen across three consecutive runs. Whether this is (a) a new seed-population regime finally saturating IQR whiskers or (b) 12-seed IQR-tight-band artifact reaching its geometric maximum resolves on the next CSV; sustained ≥ 20% next run escalates.
- **spread `settlementLegs` HIGH — 25% (9/36).** Distribution collapsed from 07-17's even five-value shape {78:6, 79:9, 80:6, 81:9, 82:6} to today's four-value shape {77:6, 79:9, 80:15, 81:3, 82:3}. The mode of 80 doubled its count (6 → 15), IQR shrank from 3 to 1, and the previously-absent value 77 appeared as a single-tail 6-row cluster. This looks like a genuine seed-scheduling shift (fewer distinct settlementLegs values, tighter mass) rather than seed jitter — but the same shape rotated OUT between 07-16 basket and today spread, so classify with the rotation cohort until it holds two runs in the same mode.
- **spread `maxCurve` MEDIUM — 16.67% (6/36).** First spread `maxCurve` flag in the record. Range 68.34–73.96 around 70.85 mean. Paired with finding 1 (basket maxCurve HIGH), this is the first same-run maxCurve co-appearance in two different price scales.
- **basket `minCurve` MEDIUM — 16.67% (6/36).** Yesterday 8.33% (sub-threshold), today crosses 10% gate. Range 0.111–0.120 around 0.116 mean. Same tight-band shape.
- **role_pnl scale per mode (regime unchanged).** spread σ = $8,898 (μ = −$450) — narrowed slightly from $9,610/−$592 on 07-17, inside 12-seed jitter; basket σ = $43.3 (μ = −$0.69) — widened from $29.4/+$0.04; synth/x402 σ = $14.5 (μ = −$0.27) — widened from $9.5/−$0.15. Spread continues to dominate pooled stats by two–three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold with big cross-day swings.** `settlementLegs × x402Total` = +0.028 (was −0.604 on 07-17, +0.732 on 07-16, −0.315 on 07-12, +0.216 on 07-10 — fifth consecutive sign-flip). `maxCurve × x402Total` = −0.522 (was +0.447 on 07-17 — sign flipped, matching magnitude). `seed × x402Total` = +0.559 (was −0.069 on 07-17 — 0.63 swing). `minSpot × x402Total` = −0.206 (was −0.477 on 07-17). `maxSpot × x402Total` = +0.248 (was −0.105 on 07-17 — sign flipped). Every pair jitters, none strengthens two runs in a row — consistent with 12-seed churn.
- **wallet_sum_pnl outliers collapsed but still float dust.** basket 0% (down from 16.67%), spread 8.33% (down from 25%), synth/x402 0% (down from 33.33%). σ(wallet_sum_pnl) 1.27e−14 (synth/x402) to 5.86e−12 (spread). Excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

## Row count and shape vs 2026-07-17

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated from spot-side to curve-side.** 07-17 fired MEDIUM+ flags on minSpot (basket/synth/x402 × 25%) and maxSpot (basket/spread/synth/x402 × 16.67%) plus maxCurve (basket 16.67%, synth/x402 16.67%). Today: every spot flag reverted to 0%; curve side widened (basket maxCurve 33.33% HIGH, basket minCurve 16.67% new, spread maxCurve 16.67% new); settlementLegs re-entered via spread at 25% HIGH; the synth/x402 maxCurve "first persistence" from 07-17 collapsed to 0%. Net: one new HIGH (basket maxCurve), one returning HIGH (spread settlementLegs), two new MEDIUM (spread maxCurve, basket minCurve), and full collapse of the 07-17 wide spot cluster.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; five consecutive filed runs with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — the historical rotation-not-persistence rule broke today with a partial correction: basket `maxCurve` is now the first (mode, column) pair to persist AND strengthen across three consecutive runs (8.33 → 16.67 → 33.33%), while the 07-17-headlined synth/x402 `maxCurve` "first persistence" already rotated to 0%. Half of yesterday's persistence signal held (basket direction), half rotated (synth/x402 direction). Escalation gate for basket maxCurve: fourth run ≥ 20% escalates from geometric-doubling artifact to structural signal; rotation to <20% classes today as the tallest 12-seed IQR spike observed to date per [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (today). Every rotation sign-flips. Continues 12-seed jitter classification until any pair confirms two runs in a row.
