# Compute-Futures EDA — 2026-07-19

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-19.csv` (144 rows, 4 modes)
**Run:** 2026-07-20
**Prior filed report:** 2026-07-18 (day-over-day; no gap)

## Top findings

1. **[MEDIUM — RESOLUTION] basket `maxCurve` collapsed 33.33% → 0% in one seed rotation, resolving the 3-run strengthening streak as 12-seed IQR jitter, not signal.** Yesterday's headline finding (07-16 8.33% → 07-17 16.67% → 07-18 33.33%, doubling every step, called out in MEMORY.md as "the first (mode, column) pair to persist AND strengthen 3× in a row") reverts to zero today: min 0.2405, max 0.2700, q25 0.2436, q75 0.2558, iqr_out=0. Mean drifted down 0.2588 → 0.2500; std held (0.0081 → 0.0083). Yesterday's report set the explicit escalation gate: "rotation to <20% classes today as the tallest 12-seed IQR spike observed to date." Rotation happened. Classification resolved: **the streak was the geometric maximum of a 12-seed IQR-tight-band artifact**, not a structural regime shift per [[compute-futures-12-seed-sample-too-small]]. Next step: keep [[compute-futures-12-seed-sample-too-small]] top of the priority list — the false-positive persistence-story cost two runs of narrative before rotating out; a 24-seed or 36-seed sweep would flatten these IQR artifacts.
2. **[MEDIUM] basket `settlementLegs` outlier_pct 16.67% — new column for basket** (yesterday 0%). Distribution today {75:3, 76:3, 80:9, 81:6, 82:6, 83:9}, q25=80, q75=82.25, IQR=2.25, whiskers at 76.63/85.62 → 75 and 76 tag as low outliers (6 rows total). This is settlementLegs rotating BACK into basket after last flagging on 07-16 (18/36 mass at value 80 gave the same 16.67% flag). Same column rotated through spread yesterday at 25% HIGH, today reverts spread to 0% and re-fires basket. Second week running that settlementLegs plays this basket↔spread swap. Watch: a third settlementLegs flag in the same mode (either basket or spread) within a week escalates from rotation to persistence.
3. **[MEDIUM] spread `realizedAbs` outlier_pct 16.67% — returning column.** History across last 7 days: 16.67% (07-13) → 8.33% → 0% × 3 → 8.33% (07-18) → 16.67% today. Std jumped 13,784 → 19,791 (+43%); range widened to 27,064–94,434 (was 20,857–78,114). Two 3-row clusters tag the whiskers (86,013–94,434 high, 27,064 low). Not new to the record but re-entering the flag zone after a five-day sub-threshold stretch. Consistent with spread's dollar-scale volatility not seed jitter, but still 12-seed IQR-tight-band shape.
4. **[LOW — sixth consecutive filed run at zero |r|≥0.8].** No mode fires any strong-pair candidate today. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, today. Sub-threshold x402 churn continues: `settlementLegs × x402Total` = +0.696 (was +0.028 yesterday, −0.604 on 07-17, +0.732 on 07-16 — sixth consecutive rotation with no two-run persistence at |r|≥0.5); `maxCurve × x402Total` = +0.180 (was −0.522 yesterday — sign flipped, magnitude collapsed); `seed × x402Total` = −0.291 (was +0.559 yesterday — 0.85 swing, largest single-day swing tracked); `realizedAbs × x402Total` = −0.605 (was subthreshold yesterday, first appearance at |r|≥0.5); `minSpot × x402Total` = +0.524 (was −0.206). Every pair jitters, none holds two runs in a row.
5. **[LOW — full curve-side collapse]** Yesterday's three curve-side flags all reverted: basket `maxCurve` 33.33% → 0% (finding 1), basket `minCurve` 16.67% → 0%, spread `maxCurve` 16.67% → 8.33% (sub-threshold). Spread `minCurve` also sub-threshold at 8.33%. No curve-side column flags anywhere at ≥10%. Complete rotation of the 07-18 curve cluster.
6. **[LOW — recurring] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202607191`–`202607199`), 36 rows 10-char seed (`2026071910`–`2026071912`). `seed` outlier_pct = 25.00% in every mode, same as every filing since 06-20. One-line pad still pending per [[compute-futures-seed-padding-bug]].
7. **[LOW — recurring] x402Total inside stable band, sum drifted up.** Mean $326.22, range $257.27–$414.52, sum $11,743.80 across 36 x402 rows. Eleventh consecutive run inside the stable envelope since 07-03. Range widened vs 07-18 ($285–$362 → $257–$415) but mean drifted $4 up ($322.56 → $326.22). No x402Total outlier flag today. Excluded from severity per bimodal-by-design rule anyway.
8. **[LOW — recurring] wallet_sum_pnl outliers.** Today: basket 0%, spread 8.33%, synth/x402 16.67%. σ(wallet_sum_pnl) still float dust (basket 2.18e−14, spread 5.56e−12, synth/x402 1.01e−14). Excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
9. **[LOW] Spread wallet_sum_pnl σ drift-up streak broke.** Prior three-run trend (3.22e−12 → 5.37e−12 → 5.86e−12 across 07-16/17/18) reversed today to 5.56e−12 — first tick DOWN. Still float dust twelve orders below any real conservation gate. Streak treated as concluded until a fresh two-run drift-up reappears.
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.57e−15 (basket) to −1.26e−12 (spread); std 1.01e−14 (synth/x402) to 5.56e−12 (spread). All 12+ orders under the |mean|>10 / std>100 gates.
11. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-18.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold. Live/settlement/spotSource per-mode assignments unchanged.
13. **[PASS] No CRITICAL or HIGH findings.** No schema drift, no row-count change, no conservation break, no persistent (≥20%) per-mode column flags. Overall flag count materially DOWN vs yesterday (yesterday had 4 flags ≥10% after exclusions: basket maxCurve HIGH, spread settlementLegs HIGH, spread maxCurve MEDIUM, basket minCurve MEDIUM; today has 2: basket settlementLegs MEDIUM, spread realizedAbs MEDIUM). Quiet run.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.57e−15  | 2.18e−14  | OK |
| spread    | −1.26e−12  | 5.56e−12  | OK |
| synthetic | +3.74e−15  | 1.01e−14  | OK |
| x402      | +3.74e−15  | 1.01e−14  | OK |

## Within-mode signal

- **basket `settlementLegs` MEDIUM — 16.67% (6/36).** Distribution {75:3, 76:3, 80:9, 81:6, 82:6, 83:9}. Low-side twin cluster at 75 and 76 (3 rows each = 1 seed each × 3 roles) form the outlier tail; mass concentrated at 80/81/82/83 in a right-skewed shape. Column-rotation match: last basket settlementLegs flag was 07-16 at same 16.67%. This is settlementLegs rotating basket → spread → basket → spread on alternate weeks; two-week cadence intact.
- **spread `realizedAbs` MEDIUM — 16.67% (6/36).** Range widened to $27k–$94k, std +43% vs 07-18. Two 3-row whisker clusters ($86k–$94k high, $27k low) around a $50k core. Returning column after a five-day sub-threshold stretch. Consistent with spread's dollar-scale volatility, not seed jitter.
- **role_pnl scale per mode (regime unchanged).** spread σ = $10,799 (μ = −$326) — widened from $8,898/−$450 on 07-18; basket σ = $26.8 (μ = −$1.09) — narrowed from $43.3/−$0.69; synth/x402 σ = $9.2 (μ = −$0.42) — narrowed from $14.5/−$0.27. Spread continues to dominate pooled stats by two–three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific sub-threshold pairs with the largest single-day swings tracked.** `seed × x402Total` = −0.291 (was +0.559 on 07-18 — 0.85 swing, LARGEST single-day rotation tracked to date). `settlementLegs × x402Total` = +0.696 (was +0.028 — 0.67 swing, back into strong-positive territory). `maxCurve × x402Total` = +0.180 (was −0.522 — sign-flipped again, sixth flip in six rotations). `realizedAbs × x402Total` = −0.605 (subthreshold yesterday, first appearance at |r|≥0.5). None strengthens two runs in a row — continues 12-seed jitter classification.
- **wallet_sum_pnl outliers today.** basket 0%, spread 8.33%, synth/x402 16.67%. σ(wallet_sum_pnl) 1.01e−14 (synth/x402) to 5.56e−12 (spread). Excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

## Row count and shape vs 2026-07-18

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface collapsed and rotated toward settlementLegs + realizedAbs.** 07-18 fired flags on maxCurve (basket 33.33% HIGH, spread 16.67% MEDIUM), settlementLegs (spread 25% HIGH), and minCurve (basket 16.67% MEDIUM). Today: every curve-side flag reverted to 0% or 8.33%; spread settlementLegs 25% → 0%; basket settlementLegs re-fires at 16.67% (last flagged 07-16); spread realizedAbs re-fires at 16.67% (last flagged 07-14 at 8.33%, 07-13 at 16.67%). Net: zero HIGH findings (was 2 HIGH yesterday), two MEDIUM (was 2 MEDIUM), and full collapse of the celebrated basket maxCurve 3-run strengthening streak per finding 1.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; six consecutive filed runs with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — the "basket maxCurve 3-run strengthening" storyline collapsed today from 33.33% back to 0%, resolving it as 12-seed IQR-tight-band jitter reaching its geometric maximum, exactly as the 07-18 escalation gate specified. This is the second high-profile persistence-narrative to break within a week (07-17 celebrated synth/x402 maxCurve "first persistence" broke at run 2; 07-18 celebrated basket maxCurve "first persistence + strengthening" broke at run 4). Escalates urgency of the `scenario-sweep.mjs` seed-count widening per [[compute-futures-12-seed-sample-too-small]] — false-positive narrative burn is now measurable in operator attention. See also priority-list item "Widen `scenario-sweep.mjs` seed count".
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → +0.696 (today). Every rotation big magnitude swings, no two-run stability. Continues 12-seed jitter classification.
