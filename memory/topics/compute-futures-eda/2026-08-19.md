# Compute-Futures EDA — 2026-08-19

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-19.csv` (144 rows, 4 modes)
**Run:** 2026-08-19
**Prior filed report:** 2026-08-18 (daily cadence resumes; no gap this run).

## Top findings

1. **[HIGH — CROSS-MODE OUTLIER SURFACE EXPANDS, first HIGH-tier cluster in class] synth/x402 `maxCurve` outlier_pct jumps to 25.00% (9/36 rows), qualifying as HIGH per the ≥20% threshold.** Nine rows (3 seeds × 3 roles) land in the upper `maxCurve` tail of synth: seeds **202608192, 202608193, 202608194**, all with maxCurve values 0.0955–0.1065. Mirror-identical in x402 (per verified [synth ≡ x402] equivalence, counted as one anchor cluster of 9 rows, not 18). This is the first per-mode outlier cluster to breach HIGH severity (≥20%) since the daily cadence began. **Not** a `x402Total` bimodal artifact (excluded from ranking by SKILL rule) — this is genuinely the maxCurve column in synth. Interpretation: 3 seeds out of 12 (25% seed-level) happen to land in the maxCurve tail; with n=12 seeds and IQR fencing, small clusters propagate hard to per-row pct. Requires either wider n (per [[compute-futures-12-seed-sample-too-small]]) or per-seed dedup before percent calculation. Companion basket minCurve/maxCurve at 16.67% each (MEDIUM) point to the same underlying seed-cluster mechanism, just at smaller magnitude.

2. **[HIGH — SUPER-ANCHOR RETURN, largest single-seed concentration since 08-16's 202608165] Seed 202608194 anchors 19 non-dust outlier positions across all 4 modes.** Breakdown: basket minCurve LOW (3 roles), basket maxCurve HIGH (3), synth minCurve LOW (3), synth maxCurve HIGH (3), x402 minCurve LOW (3), x402 maxCurve HIGH (3), spread role_pnl LOW (1 — retail at **−$26,855**, deepest single-row loss across all modes today). Also anchors two spread top-5 gains: operator +$14,739 and analyst +$8,746 from same seed 202608194 (cross-role divergence within same seed — one seed produces both deepest retail loss AND top operator/analyst gains, indicating extreme intra-seed distributive skew). Total 19 non-dust positions = **36% of all 53 non-dust outlier anchors today**. Class series: 08-13 super-single dense (13 pos) → 08-14 hyper-single (14 pos) → 08-15 five-anchor rotation → 08-16 super-single 202608165 (18 pos) → 08-18 distributed multi-anchor (max 3/seed) → **08-19 super-single 202608194 (19 pos, new peak)**. Distributed-then-concentrated oscillation continues, not a persistent pattern. Zero overlap with 08-18's top anchor 2026081812.

3. **[MEDIUM — extension, promoted class n=12 consecutive filed runs, retail mean stabilizes] Spread `role_pnl` retail mean **−$10,290** (essentially flat vs 08-18's −$10,506, +$216 = 2% shallower); top-5 losses hold at 5/5 retail purity.** Top-5 losses: 202608194 retail **−$26,855** (deepest single loss ever recorded in this class) / 2026081911 retail −$25,167 / 202608195 retail −$16,135 / 202608192 retail −$11,637 / 2026081912 retail −$10,440 (5/5 retail — purity holds from 08-18). Top-5 gains: 2026081911 operator +$19,051 / 202608194 operator +$14,739 / 202608194 analyst +$8,746 / 202608195 analyst +$8,303 / 2026081912 operator +$7,767 (0 retail in top-5 gains — same 08-18 pattern). Per-role: retail mean **−$10,290** (std $8,848, min −$26,855, max +$3,632); operator mean +$5,593 (up from +$4,644); analyst mean +$3,317 (down from +$4,336). Extends [[compute-futures-spread-retail-loss-concentration]] from n=11 to **n=12 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18 → 08-19). Class direction (retail-losers, operator/analyst-winners) and 5/5 top-5 purity both hold. Notable: two seeds (2026081911 and 202608194) each anchor BOTH a top-5 loss (retail) AND a top-5 gain (operator/analyst) — extreme intra-seed distributive skew rather than the earlier per-seed net-loss pattern.

4. **[MEDIUM — class n=1 → n=2 promotion, byte-identical multiplier holds] basket/synth spot-price multiplier CONFIRMED at 2.5000 exact for 2nd consecutive filed run.** Byte-identical constant on both sides: `minSpot` per-seed ratio range **[2.500000, 2.500000]** std 1.30e-15; `maxSpot` per-seed ratio range **[2.500000, 2.500000]** std 7.25e-16 (both 12/12 seed pairs). Curve/realizedAbs ratios follow with tiny per-seed variance: `minCurve` mean 2.4951 (per-seed 2.4778–2.5119, std 1.05e-02), `maxCurve` mean 2.4997 (per-seed 2.4949–2.5051, std 2.90e-03), `realizedAbs` mean 2.5490 (per-seed 2.3782–2.7971, std 1.31e-01, wider because P&L-driven). Class series: n=7 at 3.0250 exact (08-10 → 08-16, filed runs) → step to 2.5000 exact at 08-18 (n=1) → **2.5000 exact holds at 08-19 (n=2)**. The 08-18 promotion of new anchor 2.5000 held over the daily gap. Basket/synth per-seed rank order preserved; mechanism remains `basket = synth × k` with k byte-identical for spot columns. Fresh class candidate for [[compute-futures-basket-synth-3025x-multiplier]] rename to a `2.5x-multiplier` anchor once n≥5 held.

5. **[MEDIUM — CROSS-MODE ≥10% MEDIUM COUNT BREAKS from 0 to 4 (3 pair-collapsed clusters)] Curve-column outlier surface expands across all 4 modes.** Distinct clusters (with synth/x402 as one mirror): **basket minCurve 16.67%** (fresh — was 0 in 08-18, seeds 202608194+202608196), **basket maxCurve 16.67%** (fresh — was 0, seeds 202608192+202608194), **(synth/x402) minCurve 16.67%** (holds from 08-18, seeds 202608194+202608196), **(synth/x402) maxCurve 25.00%** (fresh HIGH tier per finding 1, seeds 202608192+202608193+202608194). Cross-mode MEDIUM series: 0 (08-13) → 2 (08-14: spread minSpot+maxSpot) → 0 (08-15) → 0 (08-16) → 0 (08-18) → **3 pair-collapsed clusters (08-19)**. The 3-consecutive-zero streak breaks decisively, with the curve column emerging as the today's stress axis. Root anchor: seed 202608194 present in ALL 4 clusters. Same underlying 3-seed cluster mechanism as finding 1.

6. **[LOW — cross-mode float-dust sign regime FLIPS again, 3rd sign-regime rotation in 3 filed runs] Conservation `wallet_sum_pnl` mixed: basket + spread positive, synth + x402 negative.** Per-mode means: basket **+2.32e−15** (vs 08-18 −1.15e−14; **sign FLIPS − → +**, magnitude decreases 5×), spread **+1.77e−12** (vs 08-18 +1.26e−12; sign HOLDS positive, magnitude grows 1.4×), synth **−2.92e−15** (vs 08-18 +4.44e−16; **sign FLIPS + → −**), x402 **−2.92e−15** (identical to synth by [synth ≡ x402]). All four modes remain 12+ orders under the |mean|>10 / std>100 gates. Third consecutive filed run with sign-regime rotation (08-16 all-negative → 08-18 mixed → 08-19 different-mixed). Per-mode `wallet_sum_pnl` IQR outlier percentages: basket 0% (holds), spread 16.67% (down from 08-18's 25%), synth 8.33% (down from 16.67%), x402 8.33% (mirror). All float dust; excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

7. **[LOW — n=11 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-18's n=10 to **n=11**. Highest today: **synth `maxSpot × settlementLegs` = −0.7302** (mirrored in x402 by [synth ≡ x402], sub-threshold structural), **spread `minCurve × settlementLegs` = −0.6961**, **spread `wallet_sum_pnl × settlementLegs` = −0.6847** (float dust, excluded), **basket `maxSpot × settlementLegs` = −0.5973**, **spread `realizedAbs × minSpot` = −0.5235**. x402 `settlementLegs × x402Total` today **+0.4802** — 26-filing series … +0.293 (08-15) → −0.112 (08-16) → +0.036 (08-18) → **+0.480 (08-19)** — sign HOLDS positive for the **first same-sign consecutive pair** in this class after 6 flips; magnitude jumps 13× from +0.036 → +0.480 (largest same-sign step observed). x402 `realizedAbs × x402Total` today **+0.0638** — collapses from 08-18's −0.4048 (0.47-magnitude swing), returns near-zero after two consecutive filings of |r|>0.4. n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced.

8. **[LOW — 3rd consecutive expansion, both boundaries at fresh extremes] x402Total range EXPANDS further to $256.38–$388.05 (range $131.66, up +$11.76 from 08-18's $119.90).** LOW **$256.38** — drops another $8.37 below 08-18's $264.75 (new all-time floor in daily series; also drops **$6.28 below** 08-15's prior floor $262.66). HIGH **$388.05** — climbs $3.40 above 08-18's $384.65 (new peak; edges past 08-15's $384.55 by $3.50). Mean **$334.35** (up $6.88 from 08-18's $327.47); std $35.07 (narrows slightly from $40.50). Series: $83.55 (08-16) → $119.90 (08-18) → **$131.66 (08-19)**. Both boundaries continuing to extend outward — mean-reverting oscillator not converging; may be regime-shifting. LOW anchor **seed 202608197** (3-role tie at $256.38); HIGH anchor **seed 202608195** (3-role tie at $388.05). x402Total outlier_pct = 0.00% in x402 mode today (expansion still fits inside IQR fence; excluded from ranking regardless per SKILL bimodal rule).

9. **[LOW — sharpest reversal in class, back to severe under-index] Padded-seed contribution COLLAPSES to 1.9% (vs 25% row-share).** Padded seeds (2026081910/11/12) hold 36 of 144 rows (25%). Today they contribute **1 of 53 non-dust outlier positions (1.9%)**: only 2026081911 with 1 position (spread role_pnl LOW). 2026081911 also anchors x402Total nothing today (08-18's LOW anchor drops out). Class series: 08-12 padded 42% over → 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% mildly over → 08-16 3.2% severely under → 08-18 50% severely over → **08-19 1.9% severely under**. Sharpest single-day swing in class (50% → 1.9% = **48-point drop**). Continues rotation-not-persistence pattern; padded encoding does not systematically over- or under-select — the swing is a rotation, not structural bias. Reinforces [[compute-futures-seed-padding-bug]] fix motivation (removing padding removes the surface entirely).

10. **[LOW — recurring, 32nd filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608191`–`202608199`), 36 rows 10-char seed (`2026081910`–`2026081912`). `seed` outlier_pct = 25.00% in every mode (IQR flags the 3 padded seeds as tail across all modes). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

12. **[PASS] Row/column shape unchanged vs 08-18.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift beyond the finding-4 multiplier hold.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +2.32e−15  | 2.69e−14  | OK |
| spread    | +1.77e−12  | 4.44e−12  | OK |
| synthetic | −2.92e−15  | 1.43e−14  | OK |
| x402      | −2.92e−15  | 1.43e−14  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). Sign regime rotates for the 3rd consecutive filed run (mixed → mixed → different-mixed).

## Within-mode signal

- **HIGH outlier tier reached for the first time — synth/x402 `maxCurve` at 25.00%** (finding 1). Driven by 3-seed cluster {202608192, 202608193, 202608194}. Curve-column outlier surface expands across all 4 modes with seed 202608194 as common anchor (finding 5).
- **Super-anchor seed 202608194 with 19 non-dust positions across 4 modes** (finding 2). Largest single-seed concentration since 08-16's 202608165 (18 pos). Also anchors both spread's deepest retail loss (−$26,855) and two of spread's top-5 gains (operator +$14,739, analyst +$8,746) — intra-seed distributive skew rather than uniform seed-level outcome.
- **Retail-role loss concentration in spread — n=12 consecutive filed runs, retail mean stabilizes at −$10,290** (finding 3). Class direction and 5/5 top-5 purity both hold. Two seeds (2026081911, 202608194) each anchor both a top-loss (retail) and a top-gain (operator/analyst) row.
- **basket/synth 2.5000× multiplier confirmed at n=2 filed runs** (finding 4). Byte-identical for spot columns (std < 1e-14 per-seed), tight for curve columns, wider for realizedAbs. New anchor holds cleanly across the daily gap.
- **Cross-mode ≥10% MEDIUM count breaks from 0 to 3 pair-collapsed clusters** (finding 5), all curve-column-driven, all rooted in seed 202608194 as common anchor. Fresh cross-mode drift, not a mirror-only artifact.
- **Zero within-mode strong pairs (|r|≥0.8) — n=11 consecutive filed runs** (finding 7). n=12 seeds decisively insufficient. x402 `settlementLegs × x402Total` posts first same-sign consecutive pair after 6 flips (magnitude 13× jump).
- **x402Total range extends outward for 3rd consecutive filing** (finding 8) — both boundaries at fresh extremes. Not a mean-reverting oscillator anymore; may be regime-shifting.
- **Padded-seed contribution collapses to 1.9% under-index** (finding 9) — sharpest reversal in class (48-point drop in 2 days). Continues rotation-not-persistence.
- **Conservation float-dust sign-regime rotates for the 3rd consecutive filed run** (finding 6). Still all float dust, no threshold breach.

## Carry-overs

- 25% seed encoding artifact (n=32 filed runs, pending [[compute-futures-seed-padding-bug]] fix).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design.
- [synth ≡ x402] role-level identity (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).

## Row count and shape vs 2026-08-18

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold.
- basket/synth multiplier constant 2.5000 held over the daily gap (finding 4).
- **Outlier surface SHIFTS from 08-18 → 08-19.** 08-18: 0 HIGH, 2 MEDIUM (retail-loss n=11, basket/synth multiplier step). 08-19: **1 HIGH** (synth/x402 maxCurve 25% + super-anchor 202608194), **3 MEDIUM** (retail-loss n=12, multiplier holds, cross-mode MEDIUM count breaks to 3 clusters). First HIGH-tier outlier since daily cadence began.
