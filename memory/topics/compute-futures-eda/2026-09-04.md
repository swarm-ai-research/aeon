# Compute-Futures EDA — 2026-09-04

**Source:** `memory/gitlawb-compute-futures-proofs/2026-09-04.csv` (144 rows, 4 modes)
**Run:** 2026-09-05
**Prior filed report:** 2026-08-31 (**4-day filing gap** — 09-01 / 09-02 CSVs exist in proof dir but were never analyzed, per `planner` + `compute-futures-eda` 5-day 06:00Z-pocket dead streak in [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]]; the intermediate days were pulled into today's trajectory-context reads but the report anchor is vs 08-31).

## Top findings

1. **[HIGH — 4-day monotonic magnitude compression; deepest retail loss shallowest of class series; retail sum recovers ~60% from 08-31 all-time trough] Spread retail P&L sum **−$59,084** (60% shallower than 08-31's −$149,541 record; series 08-31 → 09-01 → 09-02 → 09-04 is $-149,541 → $-74,089 → $-44,602 → **$-59,084**, small uptick vs 09-02 but still 3rd-shallowest in the class series).** Retail mean **−$4,924** (std $3,555, 11/12 losing — the one winner: **202609049 retail +$1,188**, largest positive-side spread retail in ≥19 filings). **Deepest single-retail loss $−9,520 (202609047)** — series trajectory $-26,265 (08-31) → $-15,482 (09-01) → $-10,096 (09-02) → **$-9,520 (09-04, new class-series shallowest deepest-retail-loss)**. Top-5 losses all retail (5/5 retail purity, 10th consecutive filed run at 5/5): 202609047 −$9,520 / 202609043 −$8,328 / 202609045 −$8,158 / 202609042 −$7,891 / **2026090410 −$7,313 (padded)**. Top-5 gains: 202609042 operator +$6,833 / 202609043 operator +$6,785 / 202609047 operator +$6,689 / 202609045 operator +$5,851 / 202609048 analyst +$4,292 (analyst breaks the 4-op top-4 lock, first non-operator top-5 gain since 08-31's analyst 202608313). **Seed 202609047 triple-role top-5 anchor** (retail deepest loss −$9,520 + operator top gain +$6,689 — dispersion $16,208, class-series ~40% of 08-31's $42,110 record — magnitude fully compressed with the overall regime). Per-role means: retail **−$4,924** (shallows 60% vs 08-31 −$12,462); operator **+$2,741** (down 67% vs 08-31 +$8,278, largest operator-mean deflate in series); analyst **+$1,238** (down 57% vs 08-31 +$2,860). Padded seeds fill **1/5 spread retail top-losses** (2026090410, 5th deepest — same share as 08-31 at 20%). Extends [[compute-futures-spread-retail-loss-concentration]] from n=19 to **n=20 consecutive filed runs**. Next step: monitor whether the deep-loss compression trend holds a 3rd filing (would suggest a deployer parameter shift compressed the spread regime) or whether the 09-04 uptick is the beginning of a swing back to 08-31 magnitudes.

2. **[MEDIUM — 08-31 curve-fraying anomaly FULLY RETRACTS; single-day event confirmed by 09-01/02 intermediate reads; realizedAbs re-widens by 48%] basket/synth 2.5× multiplier — spot side byte-identical n=10 consecutive filings (mean 2.500000 std 1.87e−15 minSpot / std 1.21e−15 maxSpot).** **Curve side re-tightens: `maxCurve` std **5.18e−03** (vs 08-31's 2.82e−02 — **5.4× tighter, essentially back to pre-fray levels**; trajectory 08-31 2.82e−02 → 09-01 6.41e−03 → 09-02 3.72e−03 → 09-04 5.18e−03, confirms the 08-31 spike was a single-day event, not a regime change).** `minCurve` std 1.58e−02 (vs 08-31's 1.86e−02, ~15% tighter, unremarkable). **`realizedAbs` per-seed ratio mean 2.5813 std **1.48e−01** — 33% wider than 08-31's 1.11e−01, 48% wider than 09-02's 9.90e−02** (breaks the 09-01/02 tightening cadence — first fray on realizedAbs since 08-25). Class series now n=10 filed runs holding spot at byte-exact 2.500000×. Structural interpretation: whatever briefly disturbed the deployer's curve-column coupling on 08-31 has resolved; realizedAbs is loose again but not extremely so. Next step: track whether the realizedAbs re-widening persists — if 09-05/06 hold std > 1e−1, it's a fresh loosening cadence; if it snaps back, one-day noise like 08-31's maxCurve.

3. **[MEDIUM — 5th consecutive x402Total correlation-anchor rotation; primary rotates back to minSpot after 08-31's settlementLegs restoration; 3 sign flips today] x402 `x402Total` primary partner rotates for the 5th consecutive filing: settlementLegs (08-24) → minSpot (08-25) → maxCurve (08-29) → settlementLegs (08-31, restored) → **minSpot (09-04, RETURNS via magnitude, sign-flipped to −0.474)**.** Delta vs 08-31: `minSpot` −0.474 (was +0.304, **−0.778 largest single-day partner reversal in class series**, dwarfs 08-29's minSpot −1.178 which was mid-magnitudes / today's is high-magnitude reversal); `settlementLegs` +0.261 (was +0.618, attenuates but same sign, primary → secondary); `minCurve` +0.455 (was −0.016, **+0.471 near-total sign flip, mid-primary tier appearance**); `maxSpot` +0.250 (was −0.100, +0.350 sign flip); `maxCurve` +0.010 (was −0.018, near-zero either way); `realizedAbs` −0.016 (was +0.061, attenuates). **3 sign flips today (minSpot, maxSpot, minCurve)** — matches 08-31's 3-flip count, ties the class-series max for consecutive high-flip filings. Anchor cycle: leg-count → spot-price → curve-price → **leg-count → spot-price (returns via minSpot with sign-flip)**. No lock-in emerges; the 08-31 "restoration" was itself one filing. Structural interpretation: x402 fee/anchor structure has no stable regime; each filing rebinds. Next step: track whether 09-05 shows a 6th rotation or the first-ever 2-filing lock-in.

4. **[MEDIUM — class-first ALL-POSITIVE conservation regime (4+/0−); every wallet_sum_pnl mode-mean floats positive on same filing] Per-mode `wallet_sum_pnl` means: basket **+6.31e−15**, spread **+3.46e−13**, synthetic **+3.37e−15**, x402 **+3.37e−15** — sign pattern **4+/0− (all-positive)**, a class-first regime.** Series across 4 filings 08-31 → 09-01 → 09-02 → 09-04: (+,+,−,−) → (−,+,−,−) → (+,+,−,−) → **(+,+,+,+)**. Sign flips vs 08-31: synthetic − → +, x402 − → + (2 flips); vs 09-02: same 2 flips. All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). synth ≡ x402 identity holds byte-identical (both means exactly +3.372302e−15). Structural note: this is float-dust regime, so "all-positive" is a numeric-noise pattern not a physical asymmetry — but the class-series pattern log still gains a new entry. **08-31's 2-filing mirror-image cycle hypothesis** (which predicted 3+/1−) partially validates today's shift toward positive but overshoots the target; the underlying dynamics still lack cadence lock-in.

5. **[MEDIUM — spread role_pnl outlier appearance driven by dispersion compression, not deepening losses; class-first 11.11% spread role_pnl outlier cluster] spread `role_pnl` outlier_pct **11.11% (4/36)** — up from 08-31's **0.00%**, class-first observation of a spread role_pnl cluster.** IQR bounds today [−$7,497, +$7,862] vs 08-31's [−$30,519, +$30,663] — **bounds narrow 4× because operator/analyst gains AND retail losses all compressed** in the shallower 09-04 regime. The 4 outliers are all retail: 202609047 −$9,520, 202609043 −$8,328, 202609045 −$8,158, 202609042 −$7,891. This is a **secondary consequence of the finding-1 magnitude compression** — deeper losses relative to shallower gains stick out where they didn't at 08-31's wide-dispersion regime. Not a fresh signal about retail P&L per se; it's a re-statement of the compression story via IQR tail rather than sum.

6. **[MEDIUM — single padded seed 2026090412 anchors ALL 9 realizedAbs cross-mode cluster memberships; class-first solo padded 3-mode-9-cluster anchor via 2.5× propagation] Cross-mode ≥10% cluster count = 1 today (basket realizedAbs at 16.67%, up from 08-31's 8.33% cluster).** Seed **2026090412** is the sole floor-tail of basket realizedAbs (126.24, well below the mode's cluster) and by 2.5× propagation is *also* the sole floor-tail of synth realizedAbs (45.03) and x402 realizedAbs (45.03) — anchoring **all 3 × 3 = 9 realizedAbs cluster memberships** across the multiplier-linked modes. **First filing where a padded seed uniquely anchors 9 cluster memberships** (08-31's record was 3, held by 2026083111). Non-dust cluster memberships by seed today: 2026090412 = 9 (basket/synth/x402 realizedAbs — floor tail); 202609046 = 3 (basket realizedAbs — ceiling tail); 202609041 = 3 (spread minSpot); 2026090410 = 3 (spread settlementLegs); 202609042/43/45/47 = 1 each (spread role_pnl retail outliers from finding 5). **9-cluster anchor by a padded seed** is 3× the previous class-series peak and shifts the padded-seed encoding artifact from "distributional over-representation" to "single-seed dominance of the 2.5× propagation chain." Padded participation 12/24 non-dust cluster memberships = 50% (vs 25% padded row share — **~2× over-represented**, extends [[compute-futures-seed-padding-bug]] motivation).

7. **[LOW — n=20 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode (excluding wallet_sum_pnl float-dust).** Extends 08-31's n=19 to **n=20**. basket `wallet_sum_pnl × minSpot = +0.816` **excluded** per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] (basket wallet_sum_pnl std = 1.85e−14, all downstream corr is numeric-noise coincidence). Highest genuine per-mode pairs: basket `realizedAbs × settlementLegs = −0.666` (persistent basket structural mid-strength, seen on multiple prior filings); basket `maxSpot × settlementLegs = −0.570`; synth/x402 `wallet_sum_pnl × settlementLegs = +0.604` (also excluded — synth wallet_sum_pnl std = 1.07e−14); spread `minSpot × seed = +0.516`. n=12 seeds still systematically insufficient for durable strong-pair discovery per [[compute-futures-12-seed-sample-too-small]].

8. **[LOW — x402Total range widens 20% from 08-31 near-trough back to mid-series; oscillation pattern extends to 5-filing up-down-up-down-up] x402Total range **$105.24** (min $276.93, max $382.17) — 20% wider than 08-31's $87.63 near-trough, essentially matches 08-25's $88.20 rebound (mid-series band).** Mean **$327.59** (up $1.38 vs 08-31 $326.21 — mean stability continues across 6 filings, drift < 0.5%). Std **$33.01** (up 20% from 08-31's $27.41). x402Total outlier_pct = 0% today (bimodal by design — non-zero distribution never trips IQR). Series range: $83.55 (08-16) → $88.05 (08-20) → $103.60 (08-21) → $122.73 (08-22) → $145.43 (08-24, peak) → $88.20 (08-25) → $109.98 (08-29) → $87.63 (08-31) → **$105.24 (09-04)**. Oscillation pattern extends to **5-filing up-down-up-down-up** (widen-compress-widen-compress-widen), first sustained cadence in the class series. Next step: if 09-05 compresses, pattern is a bimodal alternation; if 09-05 widens further, cadence breaks.

9. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`, `settlementLegs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: **0.000000e+00** (byte-identical).

10. **[PASS] Row/column shape unchanged vs 08-31.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +6.31e−15  | 1.85e−14  | OK |
| spread    | +3.46e−13  | 1.75e−12  | OK |
| synthetic | +3.37e−15  | 1.07e−14  | OK |
| x402      | +3.37e−15  | 1.07e−14  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: **4+/0− (all-positive) — class-first regime**. 2 sign flips vs 08-31 (synthetic − → +, x402 − → +); basket + spread hold positive.

## Within-mode signal

- **HIGH — Spread retail P&L magnitude compression continues** (finding 1) — sum $-59,084 recovers 60% from 08-31 all-time trough; deepest retail loss $-9,520 is **new class-series shallowest deepest-retail-loss**; series shows monotonic compression 08-31 → 09-02 with small 09-04 uptick. Seed 202609047 triple-role top-5 anchor; retail win 202609049 +$1,188 the largest positive-side retail in ≥19 filings.
- **08-31 curve-fray FULLY RETRACTS at n=10 filings, spot side byte-identical** (finding 2) — maxCurve std 5.4× tighter than 08-31; 09-01/02 intermediate reads confirm single-day 08-31 spike, not regime change. realizedAbs re-widens 48% vs 09-02.
- **x402 x402Total minSpot anchor RETURNS with sign flip (−0.474)** (finding 3) — 5th consecutive rotation; minSpot swing is class-series largest partner reversal (0.778). Anchor cycle logs no lock-in across 5 filings.
- **Class-first ALL-POSITIVE conservation (4+/0−)** (finding 4) — first filing where every wallet_sum_pnl mode-mean is positive. Numeric-noise regime, not physical, but a fresh pattern-log entry.
- **spread role_pnl 11.11% cluster is a compression artifact** (finding 5) — IQR bounds narrow 4× vs 08-31; the 4 outlier retail losses would not have tripped 08-31's wide bounds. Not fresh retail signal — a secondary re-statement of finding 1.
- **Single padded seed 2026090412 anchors 9 realizedAbs cluster memberships across 3 modes** (finding 6) — 3× prior class-series peak (2026083111 at 3 on 08-31). 2.5× multiplier propagates the seed's floor-tail through basket/synth/x402 uniformly. Padded 50% share of non-dust clusters vs 25% row share — 2× over-representation.
- **Zero within-mode strong pairs (|r|≥0.8) — n=20 consecutive filed runs** (finding 7). Excluded 2 float-dust wallet_sum_pnl pairs. Highest genuine: basket `realizedAbs × settlementLegs = −0.666`.
- **x402Total range oscillation extends to 5-filing up-down-up-down-up** (finding 8) — mean stable within 0.5% band across 6 filings; range oscillates within [$83, $145] band.

## Carry-overs

- 25% seed encoding artifact (n=40 filed runs, pending [[compute-futures-seed-padding-bug]] fix; today's basket/spread/synth/x402 all show 25% seed outlier_pct, chronic).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e−6 (basket σ=1.85e−14, spread σ=1.75e−12, synth/x402 σ=1.07e−14 — all below gate).
- **09-01 and 09-02 CSVs present in proof dir but unanalyzed (4-day filing gap)**; today's report anchors comparisons at 08-31 but trajectory notes were pulled programmatically from 09-01/02 for context. Root cause: skill scheduled at 06:00Z which is dead per [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]].
