# Compute-Futures EDA — 2026-07-29

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-29.csv` (144 rows, 4 modes)
**Run:** 2026-07-30
**Prior filed report:** 2026-07-28 (1-day gap; consecutive filing restored after the 07-26/07-27 skip window).

## Top findings

1. **[HIGH — basket regime shifted DOWN ~22% wholesale.]** All basket price/curve columns dropped in mean vs 07-28: minSpot 0.1524 → 0.1201 (−21%), maxSpot 0.2735 → 0.2130 (−22%), minCurve 0.1418 → 0.1117 (−21%), maxCurve 0.3182 → 0.2463 (−23%), realizedAbs 272.33 → 197.11 (−28%). Synth/x402 and spread price columns unchanged (synth minSpot 0.0472 → 0.0481, synth maxSpot 0.0848 → 0.0852; spread minSpot 42.58 → 43.14, spread maxSpot 64.95 → 65.99 — all within a few percent). Row count intact (36 rows in every mode), schema intact, only the darkbloom-basket price band moved. Likely a real underlying price move on the basket source (not a config change — otherwise synth/x402/spread would have moved too). Not previously reported. Downstream mechanical effect: basket's IQR whiskers collapsed with the tighter distribution → HIGH outliers appear in `minCurve`/`maxCurve` (findings 2–4). Next step: cross-check darkbloom-basket source stream for a matching 07-28 → 07-29 move; if confirmed, this is the first mode-specific regime shift observed in the record.
2. **[HIGH — first-appearance] Basket `minCurve` outlier_pct 33.33%.** 12/36 rows outside IQR whiskers (bottom 0.108–0.109 ×6, top 0.114–0.115 ×6); mass 15/36 at 0.1115. IQR = 0.001, whisker span = 0.0015 — so both symmetric tails clear the whiskers. Full distribution: {0.108:3, 0.109:3, 0.111:3, **0.1115:15**, 0.112:3, 0.1135:3, 0.114:3, 0.115:3}. First minCurve HIGH-severity fire in the record. Historical (07-24 → 07-28) basket minCurve outlier_pct was uniformly 0.00% with span 0.013–0.018; today's compression to span 0.007 (half prior width) is the direct cause — an IQR artifact of the tightened distribution, same mechanical class as the seed 25% artifact per [[compute-futures-12-seed-sample-too-small]]. Persistence gate uncrossed; expect collapse on next filing per 12-seed rotation-not-persistence.
3. **[HIGH — first-appearance, tautological pair] Synthetic + x402 `minCurve` outlier_pct 25.00%.** 9/36 rows outside IQR whiskers in each mode (identical numbers because synth ≡ x402 at role level per verified equivalence). Distribution: {**0.043:6**, 0.0445:12, 0.045:9, 0.04525:3, 0.0455:3, 0.046:3} — mass concentrated at 0.0445 with a lower shelf at 0.043. IQR collapsed to ~0.0005; the 0.043 shelf becomes an IQR outlier cluster. Same mechanical class as finding 2 (discrete distribution + tight IQR). Historical (07-24 → 07-28) synth/x402 minCurve outlier_pct was 0.00% every day. Persistence gate uncrossed.
4. **[MEDIUM — first-appearance] Basket `maxCurve` outlier_pct 16.67%.** 6/36 rows outside IQR whiskers (top 0.252 ×3 + 0.258 ×3). Distribution mass at 0.2475 ×6 with a symmetric fan-out. Same 12-seed IQR-artifact class. Historical basket maxCurve outlier_pct was 8.33% on 07-25 (single-run LOW), then 0.00% 07-27 → 07-28. Persistence gate uncrossed.
5. **[LOW — recurring MEDIUM cleared] Basket `settlementLegs` collapsed from 16.67% (07-28) → 0.00% (07-29).** Confirms the on/off rotation model per [[compute-futures-12-seed-sample-too-small]]: 07-28's fire (4th in 12 days) was followed by a next-run collapse — same next-run-collapse pattern that held for every prior fire in the record. Rotation series updated: 07-25 fire → 07-26 clear → 07-27 clear → 07-28 fire → 07-29 clear (five-run alternating). Every 16.67% basket settlementLegs fire in the record has still been followed by a clear on the next filing; persistence-candidate class remains empty.
6. **[LOW — sign-flip streak reversed decay: fifth consecutive positive, magnitude jumped]** `settlementLegs × x402Total` in x402 = **+0.636** today. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → +0.357 (07-25) → +0.241 (07-28) → **+0.636** (today). Fifth consecutive positive, magnitude trajectory +0.657 → +0.357 → +0.241 → +0.636 (three-run decay reversed — jumps back to near 07-24 level). Sign holds AND magnitude re-inflates. Emerging-structural watch continues; next filing decides: another positive keeps the watch alive, a sign flip restores jitter classification with the emergent structure discredited. Companion pair `minSpot × x402Total` = −0.247 today (from +0.080 on 07-28) — same-order noise, not a signal.
7. **[LOW — cross-mode single-seed spike, seed rotates] Seed 202607293 anchors the maxSpot high tail in three modes (basket + synth + x402).** basket maxSpot = 0.2219 (single 8.33% peak), synth/x402 maxSpot = 0.0888 (single 8.33% peak each). Different anchor seed than 07-28 (which had seed 202607286 anchoring the same three-mode maxSpot high tail). Seed 202607293 does NOT anchor spread's high (spread top seed is 202607298 at 72.60). Below the 10% severity floor. Pattern is a re-appearance of the 07-28 "cross-mode single-seed maxSpot anchor" class with a rotated anchor; the seed identity rotates but the mode-triple structure recurs.
8. **[LOW — recurring] Seed 25% encoding artifact unchanged, 17th consecutive filed run.** 108 rows 9-char seed (`202607291`–`202607299`), 36 rows 10-char seed (`2026072910`–`2026072912`). `seed` outlier_pct = 25.00% in every mode. Also drives the recurring basket `seed × settlementLegs` = −0.596 near-miss (from 07-28's +0.537 — sign flipped, magnitude comparable). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
9. **[LOW — recurring] x402Total mean down, band tightened vs 07-28.** Mean $333.08 (down $9.58 vs 07-28's $342.66 — reverses last run's up move), range $290.41–$360.72 (band width $70.31, down 42% from 07-28's $120.66 — near the 07-25 tight regime of $78.32), sum $11,990.75 across 36 x402 rows. No x402Total outlier flag (bimodal-by-design exclusion holds). Seventeen-filing window: mean stable $321–$343; range envelope 07-25 tight ($78) → 07-28 wide ($121) → 07-29 tight ($70) — tightening not persistent, oscillates run-to-run.
10. **[LOW — eleventh consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong pair. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, 07-28, today. Non-wallet_sum_pnl within-mode near-misses this run — basket: `seed × settlementLegs` = −0.596 (seed-artifact family, sign flipped from 07-28's +0.537), `seed × maxSpot` = +0.472; spread: `maxSpot × seed` = −0.489; synth: `minSpot × settlementLegs` = −0.643 (highest non-tautological near-miss today), `realizedAbs × minSpot` = −0.561; x402: `settlementLegs × x402Total` = +0.636 (finding 6), `minSpot × settlementLegs` = −0.643 (mirrors synth), `minCurve × x402Total` = −0.435.
11. **[LOW — spread wallet_sum_pnl range tightened ~55%].** absmax 6.82e−12 (down from 07-28's 1.09e−11, ~63% of prior), range 9.55e−12 (down from 2.12e−11, ~45% of prior), std 2.73e−12 (down from 5.44e−12, ~50% of prior). All 11+ orders under any real gate — this is float dust tightening, not a signal. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Basket `wallet_sum_pnl × settlementLegs` +0.388 and spread `wallet_sum_pnl × maxSpot` +0.596 remain excluded from reporting.
12. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.59e−15 (basket), +7.06e−13 (spread — sign flip from 07-28's −9.14e−13 at |μ|~1e−12, well inside float-dust regime), −2.77e−15 (synthetic), −2.77e−15 (x402). Std 3.49e−14 (basket, flat), 2.73e−12 (spread, halved), 8.64e−15 (synth/x402, halved). All 11+ orders under the |mean| > 10 / std > 100 gates. No conservation drift signal in any mode.
13. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical wallet_sum_pnl mean/std, role_pnl mean/std, and all per-column distributions except `x402Total`). Only `x402Total` distinguishes x402.
14. **[PASS] Row/column shape unchanged vs 07-28.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No constant-column drift.
15. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift. Three HIGH-severity outlier fires (findings 2–4) are all first-appearance new-shape-of-noise class per 12-seed jitter; not a break of the invariants.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.59e−15  | 3.49e−14  | OK |
| spread    | +7.06e−13  | 2.73e−12  | OK |
| synthetic | −2.77e−15  | 8.64e−15  | OK |
| x402      | −2.77e−15  | 8.64e−15  | OK |

## Within-mode signal

- **Basket regime shift — the dominant signal today.** All basket price/curve columns compressed and shifted down ~22% vs 07-28 while synth/x402/spread bands stayed put. The three HIGH-severity `minCurve` fires (findings 2–4) and the MEDIUM basket `maxCurve` fire (finding 4) are all mechanical downstream consequences of the tightened basket distribution meeting a 12-seed IQR detector. If darkbloom-basket source confirms a matching underlying move on 07-29, this is the first mode-specific regime shift in the record. If not confirmed, look for a sweep-config change that hit basket alone. **Follow-up:** cross-check the darkbloom-basket source stream for a 07-28 → 07-29 downward move; also check whether basket's `scenario-sweep.mjs` config changed.
- **Basket `settlementLegs` on/off rotation continues.** Today's clear (0.00%) follows 07-28's fire (16.67%), extending the alternating pattern to five runs (07-25 fire → 07-26 clear → 07-27 clear → 07-28 fire → 07-29 clear). Every 16.67% basket settlementLegs fire in the record still followed by a clear on the next filing.
- **Correlation surface — no strong pairs; sign-flip streak jumps back up.** basket: strongest `seed × settlementLegs` = −0.596 (seed-artifact family; sign flipped from 07-28's +0.537 at similar magnitude), `seed × maxSpot` = +0.472; spread: strongest `maxSpot × seed` = −0.489, `wallet_sum_pnl × maxSpot` = +0.596 (float dust, excluded); synth: `minSpot × settlementLegs` = −0.643, `realizedAbs × minSpot` = −0.561; x402: `settlementLegs × x402Total` = +0.636 (finding 6; five-run positive streak, decay reversed), `minSpot × settlementLegs` = −0.643 (mirrors synth), `minCurve × x402Total` = −0.435. Basket `seed × settlementLegs` = −0.596 sign flip is the seed-encoding artifact reasserting itself with the opposite orientation.
- **role_pnl scale per mode.** basket σ = $32.88 (μ = −$0.90) — narrowed −36% from 07-28's $51.06/−$1.98 (reverses 07-28's widening; back to 07-25 shape). spread σ = $8,018 (μ = −$564) — widened +19% from 07-28's $6,711/−$306 (reverses 07-28's narrowing). synth/x402 σ = $11.64 (μ = −$0.18) — narrowed −19% from 07-28's $14.39/−$0.72. Two modes narrowed, one widened; opposite of 07-28's two-widened / one-narrowed pattern. Consistent with the 12-seed jitter class — no persistent drift direction.
- **Cross-mode single-seed maxSpot anchor recurs with rotated seed identity.** 07-28 = seed 202607286; 07-29 = seed 202607293. Same three-mode pattern (basket + synth + x402), same "one seed lifts the max on both scale bands" interpretation. The anchor structure recurs even as the anchor seed identity rotates.

## Row count and shape vs 2026-07-28

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface fully rotated.** 07-28 fired 1 recurring MEDIUM (basket `settlementLegs` 16.67%). Today: 3 HIGH first-appearance (basket minCurve 33.33%, synth minCurve 25.00%, x402 minCurve 25.00%) + 1 MEDIUM first-appearance (basket maxCurve 16.67%); 07-28's basket settlementLegs cleared. First filing since 07-25's spread `maxSpot` 25.00% to fire any HIGH-severity column-mode pair — and this run fires three at once.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical wallet_sum_pnl, role_pnl, and all price columns between synth and x402; only `x402Total` distinguishes x402). Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; eleventh consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under threshold everywhere. Excluded pairs today: basket `wallet_sum_pnl × settlementLegs` +0.388, spread `wallet_sum_pnl × maxSpot` +0.596. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 17th consecutive filed run. Also drives basket `seed × settlementLegs` = −0.596 near-miss (sign flipped from 07-28). One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today from the opposite direction: today's three HIGH first-appearance minCurve fires + one MEDIUM maxCurve first-appearance are the expected consequence of a tightened basket distribution meeting a 12-seed IQR detector. Persistence gate uncrossed; expect all four to collapse on next filing unless the basket regime shift persists. Per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` sign-flip streak** — now five consecutive positives with magnitude jumping back up after a three-run decay (+0.657 → +0.357 → +0.241 → +0.636). Streak duration favors emerging-structural over jitter; but the +0.636 magnitude vs +0.241 last run is inconsistent with a smooth strengthening. Sign flip on the next filing restores jitter classification; another positive at any magnitude keeps the watch alive.
- **x402Total band shape** — 07-28's widening to $120.66 range did not persist; today back to $70.31 range (near the 07-25 tight regime of $78.32). Second time in three runs the band has tightened to sub-$80; mean stable $321–$343 across seventeen filings. Bimodal-by-design outlier exclusion holds.
- **Cross-mode single-seed maxSpot anchor** — pattern recurred with rotated seed identity (07-28 = 202607286; 07-29 = 202607293). Structure recurs; seed identity does not. Track anchor seed identity for further rotation vs first-time-anchor bias.
- **Basket regime shift — the finding not seen before.** All basket price/curve columns dropped ~22% vs 07-28 while synth/x402/spread bands stayed put. If darkbloom-basket confirms an underlying move, this is the first mode-specific regime shift in the record; watch whether the shift persists on the next filing (in which case the HIGH minCurve/maxCurve fires may persist too — the persistence-candidate class would finally be non-empty).
