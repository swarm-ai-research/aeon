# Compute-Futures EDA — 2026-08-29

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-29.csv` (144 rows, 4 modes)
**Run:** 2026-08-29
**Prior filed report:** 2026-08-25 (4-day filing gap — CSVs for 08-26 and 08-27 exist but were never analyzed; 08-28 CSV never emitted; today's comparisons all vs 08-25).

## Top findings

1. **[HIGH — cross-mode ≥10% cluster count COLLAPSES from 3 → 0; class-first breadth trough; anchor column vacates entirely] The 3-mode `realizedAbs` cluster from 08-25 (basket 25%, synth 33.33%, x402 33.33%) fully dissolves to 0%/0%/0% today — no column in any mode registers a ≥10% distributional-tail cluster today.** 08-25's HIGH-tier anchor (`realizedAbs` on 3 modes) evaporates; synth/x402 realizedAbs collapses from IQR [64.66,112.46] with 12 outside rows → IQR [63.90, 105.07] with 0 outside rows; basket realizedAbs collapses from IQR [157.15, 289.40] with 9 outside rows → IQR [167.37, 268.03] with 0 outside rows. Cross-mode ≥10% cluster count series: 08-22 = 0 → 08-24 = 3 (settlementLegs) → 08-25 = 3 (realizedAbs, class-first 3-mode span) → **08-29 = 0 (class-first return to 0 after two consecutive 3-cluster filings)**. 08-25's prediction ("watch for rotation vs persistence") resolves as **rotation to absence** — the HIGH-tier class rotates OFF entirely, not to a fresh column. Below-threshold clusters at 8.33% (3 rows): basket `settlementLegs` (3 rows at leg=75, all seed 202608292 triple), spread `maxCurve` (3 rows at maxCurve=68.83, all seed 2026082910 triple) — both single-seed triples, i.e., structurally locked to one seed's 3-role expansion rather than genuine distributional tails. Next step: watch 08-30 CSV for whether cluster count stays at 0 (regime change) or rebounds (single-day trough).

2. **[HIGH — SURPASSES the prior 3.0250× regime's n=7 invalidation-cadence threshold; multiplier holds byte-identical] basket/synth spot-price multiplier at **2.5000 exact** for **8th consecutive filed run** — the 2.5× regime now becomes the new empirical durability floor, exceeding the prior 3.0250× regime's max survival cadence (n=7).** `minSpot` per-seed ratio **[2.500000, 2.500000]** std **2.01e-15** (TIGHTER than 08-25's 1.82e-15); `maxSpot` per-seed ratio **[2.500000, 2.500000]** std **8.79e-16** (essentially unchanged from 08-25's 8.14e-16, ~8% looser). Curve columns diverge unchanged direction: `maxCurve` mean **2.4990** (std **3.86e-03**, ~39% TIGHTER than 08-25's 6.28e-03 — reverses 08-25's mid-loosening); `minCurve` mean **2.4909** (std **1.56e-02**, ~78% TIGHTER than 08-25's 7.14e-02 — reverses 3-filing monotonic loosening trend). `realizedAbs` per-seed ratio mean **2.5261** (std **5.09e-02**, ~25% TIGHTER than 08-25's 6.80e-02). Class series post-rename: n=1 (08-18) → n=5 (08-22, rename threshold) → n=7 (08-25, matches prior regime's invalidation-cadence) → **n=8 (08-29, first cadence beyond 3.025× regime's max survival)**. Next step: promote 2.5× regime to **durable-floor** status; the [[compute-futures-multiplier-invalidated-at-n-7]] fragility flag no longer applies to the 2.5× regime — validation cadence needs re-anchoring at n=8+. Curve-column reversal (both min and max tighter) is fresh signal — 08-24 → 08-25 showed monotonic min-loosening; 08-25 → 08-29 fully reverses.

3. **[MEDIUM — extension, n=18 consecutive filed runs; retail mean SHALLOWS another 17% vs 08-25; 11/12 losing (restores 08-22/08-24 all-losing-except-one pattern after 08-25's 10/12)] Spread `role_pnl` retail mean **−$7,554** (17% shallower than 08-25's −$9,091), **11/12 retail losing**; the one winner is unpadded seed 202608292 (+$8,988, largest single retail win in 4 filings).** Sum-of-retail spread P&L: **−$90,646** across 12 seeds (17% shallower than 08-25's −$109,096; matches 08-16's −$109k trough in absolute magnitude but pulls back). Top-5 losses: **seed 202608293 retail −$19,055 (interim class-series-high loss for the 4-filing window; below 08-25's 202608257 −$25,260 all-time-record)** / **2026082912 retail −$18,485 (padded seed anchors 2nd-deepest loss; class-second padded-seed top-2 appearance)** / **202608296 retail −$16,690** / **2026082911 retail −$14,400 (padded)** / **202608299 retail −$9,962** — 5/5 retail (**8th consecutive filed run at 5/5 top-5 retail purity**), and **3 of 5 anchor seeds have padded-encoding representation (2026082912, 2026082911 direct; retail loss cohort proportional to padded 25% share for the first time in 3 filings)**. Top-5 gains: 202608293 operator +$13,951 / 2026082912 operator +$10,653 (padded) / 202608291 operator +$10,614 / 202608298 analyst +$9,023 / **202608292 retail +$8,988 (first non-operator in top-5 gains in 4 filings; first retail-win top-5 appearance since 08-25's padded rows)**. Per-role means: retail **−$7,554** (std $8,072, min −$19,055, max +$8,988); operator **+$4,004** (down 35% from 08-25's +$6,173, largest role-mean drift today); analyst **+$2,434** (up 59% from +$1,535 — 08-25's role-mean collapse reverses). **Seed 202608293 dual-role top-5 anchor** (retail deepest loss + operator top gain) — dispersion **$33,006** (retail −$19,055 → operator +$13,951), 4th consecutive filing with a multi-role top-5 anchor but below 08-25's $39,158 all-time record. Extends [[compute-futures-spread-retail-loss-concentration]] from n=17 to **n=18 consecutive filed runs**.

4. **[MEDIUM — 3rd consecutive filing with x402Total correlation-anchor rotation; 08-25's near-threshold minSpot pair COMPLETELY REVERSES SIGN in single filing; new anchor is maxCurve on the curve-price side, no longer spot-price side] x402 mode `x402Total` primary partner rotates for the 3rd consecutive filing: settlementLegs (dominant through 08-24) → minSpot (08-25 at +0.785 near-threshold) → **maxCurve (+0.564, 08-29)** — with 08-25's minSpot pair FLIPPING SIGN to **−0.393** (−1.178 magnitude swing, largest single-day x402Total-partner reversal in the class series).** Full x402 × x402Total delta vs 08-25: `minSpot` **−0.393** (was +0.785, −1.178 sign flip); `maxSpot` **+0.487** (was +0.579, −0.092 unchanged direction); `maxCurve` **+0.564** (was +0.134, +0.430 rise to primary anchor); `settlementLegs` **+0.223** (was +0.372, −0.149 continues attenuation from 08-24's +0.458 peak); `realizedAbs` **+0.250** (was −0.065, +0.315 sign flip); `minCurve` **−0.327** (was −0.160, −0.167 deepens negative). Anchor moves twice in 4 filings: leg-count → spot-price → curve-price — no persistence at any anchor, no 08-25's threshold-crossing continuation. Structural interpretation: today's x402 fee regime binds fee to the top-of-curve (`maxCurve`) rather than the spot floor (`minSpot`) — first filing to place primary anchor on the curve rather than the price side. Zero within-mode strong pairs (|r|≥0.8) means every anchor is sub-threshold — pattern is direction-of-attention, not statistical significance. Next step: track anchor identity across 3+ future filings to see if the rotation cadence is a genuine regime cycle or a small-n IQR artifact.

5. **[MEDIUM — x402Total range EXPANDS 25% vs 08-25; new interim series LOW (below 08-25's floor); no HIGH-anchor spike] x402Total range **expands to $109.98** (min $267.61, max $377.59) — 25% wider than 08-25's $88.20 compression; new interim-series-LOW $267.61 sits **$6.81 below 08-25's floor $274.42** but **$11.23 above 08-19's all-time-low $256.38**.** HIGH $377.59 — **rises $14.97** above 08-25's $362.62 (partial recovery, still $61.14 below 08-24's all-time HIGH $438.73). Mean **$327.47** (up $0.74 from $326.73 — mean stable across 4 filings). Std **$28.85** (up 6% from $27.23 — mild widening). x402Total outlier_pct = **0%** today (was 0% on 08-25, both post-08-24's 8.33% spike collapse). Series range: $83.55 (08-16) → $88.05 (08-20) → $103.60 (08-21) → $122.73 (08-22) → **$145.43 (08-24, peak)** → **$88.20 (08-25, tied 2nd-narrowest)** → **$109.98 (08-29, mid-band)**. Range oscillation continues (widen → compress → widen) — 4-filing pattern is now up-down-up.

6. **[MEDIUM — super-anchor cluster COLLAPSES to modest 4/3/2 tier; halts 08-25's partial recovery; 3rd-tier trough within the 5-filing series] Top seeds by non-dust outlier positions: **202608293 = 4, 202608292 = 3, 2026082910 = 2** (single-anchor with narrow follower band).** Anchor seed **202608293** hits the full outlier-cluster set for spread (`maxCurve` upper tail + `role_pnl` extreme retail loss + `settlementLegs` matched non-tail) plus cross-signal alignment (also drives finding 3's top spread retail loss). Follower seed **202608292** anchors the sole basket `settlementLegs` low-tail cluster (leg=75 triple). Padded seed **2026082910** returns to the anchor set at position 3 (2 positions from spread maxCurve triple). Anchor magnitude series: 22 → 18 → 6 → 3 → **9 (08-25 partial recovery)** → **4 (08-29, class-trough continues; 55% below 08-25's recovery)**. Anchor identity does NOT persist between filings — **7th consecutive full-collapse of prior day's super-anchor set** (08-25's 202608257 + 202608259 + 2026082511 all drop to 0 today). Cross-signal alignment holds (spread P&L anchor = distributional-outlier anchor) — 3rd consecutive filing where the spread-P&L anchor also tops the outlier count, but at diminishing magnitude.

7. **[LOW — n=17 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-25's n=16 to **n=17**. Highest today ex-seed-encoding: **basket `realizedAbs × minCurve` = −0.726** (structural mid-strength, fresh at this magnitude — 08-25 basket had no |r|≥0.6 pair); **synth/x402 `realizedAbs × minCurve` = −0.680** (persistent structural mid-strength, mild attenuation from 08-25's −0.665 direction); **basket `realizedAbs × maxCurve` = +0.649** (fresh basket pair); **synth/x402 `minCurve × maxCurve` = −0.644** (fresh mid-strength appearance); **synth/x402 `realizedAbs × maxCurve` = +0.630** (companion to −0.680 minCurve pair); **spread `wallet_sum_pnl × minSpot` = +0.602** (float-dust dependent side, excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). 08-25's near-threshold `minSpot × x402Total = +0.785` fully evaporates (see finding 4). n=12 seeds still systematically insufficient; [[compute-futures-12-seed-sample-too-small]] motivation continues.

8. **[LOW — conservation regime shifts to 1-positive-3-negative after 08-25's mirror-image 3-positive-1-negative] Per-mode `wallet_sum_pnl` means all near-zero float dust: basket **+7.48e−15**, spread **−2.68e−12**, synth **−2.59e−16**, x402 **−2.59e−16**.** Sign pattern: basket POSITIVE, spread + synth + x402 NEGATIVE (vs 08-25: basket + synth + x402 +, spread − — **3 sign flips today, exact mirror-image of 08-25's sign pattern**; the mode-quartet swaps positive-count 3→1). All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). Per-mode `wallet_sum_pnl` IQR outlier percentages: basket 8.33%, spread 8.33%, synth 8.33%, x402 8.33% (each mode has 3 float-dust IQR outliers; excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). Chronic-mirror-image regime: 2-filing sign-pattern cycle emerges (08-25 = 3+/1−, 08-29 = 1+/3−; predict 08-30 = 3+/1− if cycle holds).

9. **[LOW — padded seeds return to disproportionate top-loss placement after 08-25's bimodal split] Padded seeds (2026082910/11/12) hold 36 of 144 rows (25%). Today they contribute **2 of 9 non-dust outlier positions (22.2%)** — near-proportional (was 27.3% on 08-25 near-proportional, 0% on 08-24 total absence).** Padded seed 2026082912 anchors the 2nd-deepest spread retail loss (−$18,485); padded seed 2026082911 anchors the 4th-deepest (−$14,400). **2 of 5 spread retail top-losses are padded seeds** (40% overrepresentation vs 25% share). But padded seed 2026082910 anchors the 4th-deepest retail loss? No — 2026082910 retail = −$871 (near-dust). Both anchor spread maxCurve outlier cluster (finding 1 sub-threshold). Class series: 08-19 severely under → 08-25 proportional AND bimodal (both top-anchor AND retail-win) → **08-29 top-losses-heavy (2/5 top losses padded), no retail-win placement**. Padded encoding no longer holds a retail-win side today — 08-25's bimodal placement was a single-filing event, not a durable pattern.

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`, `settlementLegs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: **0.000000e+00** (byte-identical).

11. **[PASS] Row/column shape unchanged vs 08-25.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +7.48e−15  | 3.14e−14  | OK |
| spread    | −2.68e−12  | 6.69e−12  | OK |
| synthetic | −2.59e−16  | 1.11e−14  | OK |
| x402      | −2.59e−16  | 1.11e−14  | OK |

All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: basket POSITIVE, spread + synth + x402 NEGATIVE (3 sign flips vs 08-25 — exact mirror-image of 08-25's 3-positive-1-negative pattern; 2-filing cycle hypothesis pending 08-30 confirmation).

## Within-mode signal

- **HIGH — Cross-mode ≥10% cluster count COLLAPSES 3 → 0** (finding 1) — realizedAbs 3-mode anchor evaporates; below-threshold 8.33% clusters (basket settlementLegs, spread maxCurve) are single-seed triples, not genuine tails.
- **HIGH — basket/synth 2.5000× multiplier holds at n=8 post-rename** (finding 2) — SURPASSES prior 3.025× regime's n=7 invalidation cadence; the 2.5× regime becomes the new empirical durability floor. Curve columns tighten in both min and max (reverses 08-25's monotonic loosening).
- **Retail-role loss concentration in spread — n=18 consecutive filed runs, retail mean shallows another 17% to −$7,554, 11/12 losing (restores 08-22/08-24 pattern after 08-25's 10/12)** (finding 3). Class direction holds. **Seed 202608293 dual-role top-5 anchor with dispersion $33,006** — 4th consecutive multi-role anchor filing but below 08-25's $39,158 all-time record. Padded seeds fill 2/5 spread retail top-losses (40% overrepresentation vs 25% share).
- **x402 x402Total correlation-anchor rotates for the 3rd consecutive filing** (finding 4) — settlementLegs → minSpot (08-25 +0.785 near-threshold) → **maxCurve (+0.564, 08-29)**; 08-25's minSpot pair FULLY REVERSES SIGN to −0.393 (−1.178 magnitude swing, largest single-day partner reversal in series). Anchor moves from spot-price side to curve-price side.
- **x402Total range EXPANDS 25% to $109.98** (finding 5) — new interim series LOW $267.61 (below 08-25's floor), HIGH $377.59 partial recovery; mean stable at $327.47. Range oscillation continues (widen → compress → widen).
- **Super-anchor cluster COLLAPSES to 4/3/2** (finding 6) — 55% below 08-25's 9/9/9 recovery. 7th consecutive full-collapse of prior day's anchor identity. Seed 202608293 doubles as spread P&L + outlier-count anchor.
- **Zero within-mode strong pairs (|r|≥0.8) — n=17 consecutive filed runs** (finding 7). Highest ex-seed: basket `realizedAbs × minCurve` = −0.726 (fresh basket appearance); synth/x402 `realizedAbs × minCurve` = −0.680.
- **Conservation regime is EXACT MIRROR-IMAGE of 08-25** (finding 8) — 3 sign flips today, positive-count 3→1; 2-filing cycle hypothesis pending 08-30 verification.
- **Padded seeds return to top-loss-heavy placement** (finding 9) — 08-25's bimodal (top-anchor + retail-win) placement fully reverts; today 2/5 spread retail top-losses padded; 22.2% outlier participation near-proportional.

## Carry-overs

- 25% seed encoding artifact (n=38 filed runs, pending [[compute-futures-seed-padding-bug]] fix; today's basket/spread/synth/x402 all show 25% seed outlier_pct, chronic).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.
- 2026-08-26, 2026-08-27 CSVs remain unanalyzed in the proof dir (4-day filing gap noted; today's comparisons are all vs 2026-08-25); 08-28 CSV never emitted (deployer daily-cadence hole).
