# Compute-Futures EDA — 2026-07-25

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-25.csv` (144 rows, 4 modes)
**Run:** 2026-07-25
**Prior filed report:** 2026-07-24 (1-day gap; consecutive runs)

## Top findings

1. **[HIGH] Spread `maxSpot` outlier_pct 25.00% — first appearance in the record for this column in this mode at ≥ 20%.** 9 of 36 rows outside IQR whiskers: three at $63.27 (low tail, seed 202607257), three at $69.30 (high tail, seed 202607258), and three at $74.95 (far high tail, seed 202607255 — sits ~$5 above the next-highest seed). Mean $66.90, std $2.84, range $63.27–$74.95. Bulk 24 rows compressed into [$64.81, $67.29] across 9 seeds. Prior spread `maxSpot` outlier_pct: 07-17 16.67% (only prior filing; part of that day's "all four modes at 16.67%" cluster), 0.00% on every other filed run since 06-28. **This is the first ≥ 20% outlier flag on spread `maxSpot` in the filed record**, and the first stand-alone (mode, column) HIGH on `maxSpot` in any mode. Anchored by a single seed (202607255) pulling a $5 gap to the next-highest bin — check whether that seed's high tail persists tomorrow. Escalation gate: if it holds ≥ 10% on 07-26, move to persistence-candidate class; if it collapses (as the 07-24 basket `realizedAbs` HIGH did today), reclassify as 12-seed jitter noise per [[compute-futures-12-seed-sample-too-small]].
2. **[MEDIUM — collapse] Basket `realizedAbs` HIGH 25.00% → 0.00% in one seed rotation.** Yesterday's headline first-appearance HIGH (9/36 rows outside IQR at $153/$275/$276 tails) fully collapses today. Distribution today spreads evenly across 12 seed values from $158.55 to $286.65 (mean $235.27, std $41.08) — no rows tag IQR whiskers. Escalation gate from 07-24 ("hold ≥ 10% next run → persistence-candidate") FAILS at the first re-check. Reclassify as one-run alarm-without-signal per the [[compute-futures-12-seed-sample-too-small]] jitter class. This is the sixth (mode, column) HIGH-or-MEDIUM in the filed record that collapsed within one rotation (07-10 basket settlementLegs 25% → 0%, 07-12 spread settlementLegs 16.67% → 0%, 07-17 basket settlementLegs 16.67% → 0%, 07-24 07-21 basket settlementLegs 16.67% → 0%, today's basket realizedAbs). Same alarm-without-signal shape as every prior single-run flag.
3. **[MEDIUM — collapse of persistence-candidate] Spread `minSpot` 16.67% → 0.00% — three-run streak BREAKS at the first re-check after 07-24 promotion.** Yesterday's report graduated spread `minSpot` out of the 12-seed jitter class based on three identical-magnitude 16.67% filings (07-10, 07-21, 07-24). Today spread `minSpot` outlier_pct = 0.00%. Distribution spreads evenly across 12 seed values from $41.76 to $46.12 (mean $43.57, std $1.38) — no rows tag IQR whiskers. Persistence-candidate reclassification does NOT survive. Structural implication: the three-run persistence rule was too permissive; 12-seed IQR outlier_pct is more rotation-dominated than the 07-10/07-21/07-24 clustering suggested, and no (mode, column) at ≤ 16.67% survives promotion out of the jitter class. Reclassify spread `minSpot` back to rotation-jitter until either (a) a wider seed count validates the pattern per [[compute-futures-12-seed-sample-too-small]] or (b) a four-run streak accumulates.
4. **[MEDIUM — recurring] Basket `settlementLegs` outlier_pct 16.67% — re-appears after 07-24 collapse, extends the on/off rotation pattern.** 6/36 rows outside IQR whiskers today; distribution {76:3, 77:3, 78:3, 80:9, 81:12, 83:6}. Rotation series 07-09 through today: 8.33 → 25.00 (07-10) → 0.00 (07-12) → — → 16.67 (07-16) → 0.00 (07-17) → 16.67 (07-20) → 16.67 (07-21) → 0.00 (07-24) → 16.67 (07-25). Pattern is a stable on/off with 16.67% as the on-state; today's re-fire matches the 07-16/07-20/07-21 shape. Consistent with the 12-seed rotation-not-persistence rule reasserted by finding 3 — the column can hit 16.67% repeatedly without ever clearing the persistence gate because it collapses between fires.
5. **[LOW — near-threshold] Basket `realizedAbs × settlementLegs` = +0.776 — fresh near-miss pair, first appearance at |r| ≥ 0.7 in the basket correlation record.** Not previously reported for this pair. Coincides with today's basket `settlementLegs` re-fire (finding 4). Below the |r| ≥ 0.8 reporting threshold but the two columns are structurally linked (realizedAbs scales with settlement activity), so treat as a real signal candidate to watch. If it holds ≥ 0.7 next run AND settlementLegs re-fires, consider promoting to a within-mode structural finding.
6. **[LOW — sign-flip streak: third consecutive positive]** `settlementLegs × x402Total` in x402 = **+0.357** today. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → **+0.357** (today). **Third consecutive positive run** since the 7-rotation sign-flip streak broke on 07-24. Magnitude drifting down (+0.657 → +0.357), sign held. If tomorrow flips negative, restore the pre-07-24 jitter classification with a two-run positive interlude; if it stays positive at any magnitude, promote to emerging structural pair. Companion pair `minSpot × x402Total` in x402 = +0.745 today (near threshold, second-highest x402 pair ever; watch alongside).
7. **[LOW — ninth consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong-pair candidate. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, today. Non-wallet_sum_pnl within-mode near-misses this run: basket `realizedAbs × settlementLegs` = +0.776 (new — finding 5); x402 `minSpot × x402Total` = +0.745; synth/x402 `settlementLegs × maxCurve` = −0.700; synth/x402 `maxSpot × settlementLegs` = +0.608. Basket `seed × minSpot` = −0.558 continues to reflect the seed-padding artifact per [[compute-futures-seed-padding-bug]].
8. **[LOW — recurring] Seed 25% encoding artifact unchanged, 15th consecutive filed run.** 108 rows 9-char seed (`202607251`–`202607259`), 36 rows 10-char seed (`2026072510`–`2026072512`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
9. **[LOW — recurring] x402Total tightens inside the ~$100 stable band, 15th consecutive run.** Mean $322.55 (down $3.00 vs 07-24's $325.55 — second mean-down move in a row after 07-24's $10.99 drop), range $288.14–$366.46 (band **narrowed both ends** vs 07-24's $268.24–$388.63 — width $78.32 vs $120.39, first tightening in three runs), sum $11,611.86 across 36 x402 rows. No x402Total outlier flag (bimodal-by-design exclusion holds). Fifteen-filing window: mean between $321 and $337; range envelope $268–$389 unchanged (today's tighter range fits inside).
10. **[LOW — recurring] Spread wallet_sum_pnl absolute range 2.05e−11 (~2× 07-24's 1.86e−11).** absmax 1.14e−11 (up from 9.78e−12 on 07-24); Σ σ across modes: basket 3.21e−14 (down from 3.73e−14 on 07-24), spread 5.92e−12 (up from 5.81e−12 — essentially flat), synth/x402 7.38e−15 (down from 8.66e−15). Still float dust 12+ orders under any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]; the basket `wallet_sum_pnl × maxSpot` +0.738 and `× minSpot` +0.702 correlations remain excluded from reporting.
11. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.47e−14 (basket), +1.17e−12 (spread), −1.48e−16 (synth), −1.48e−16 (x402). Std 3.21e−14 (basket), 5.92e−12 (spread), 7.38e−15 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. No conservation drift signal in any mode. Sign flip on basket mean (07-24 +1.43e−14 → today −1.47e−14) is float-dust noise at |μ| ~3e−14 across 36 rows — not a signal.
12. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
13. **[PASS] Row/column shape unchanged vs 07-24.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.
14. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.47e−14  | 3.21e−14  | OK |
| spread    | +1.17e−12  | 5.92e−12  | OK |
| synthetic | −1.48e−16  | 7.38e−15  | OK |
| x402      | −1.48e−16  | 7.38e−15  | OK |

## Within-mode signal

- **Spread `maxSpot` HIGH — 25.00% (9/36), first appearance in the record for this column in this mode at ≥ 20%.** Three-way tail split: 3 rows at $63.27 low (seed 202607257), 6 rows at $69.30/$74.95 high (seeds 202607258 and 202607255). Bulk 24 rows compressed into [$64.81, $67.29] over 9 seeds. Seed 202607255 sits at $74.95 — a $5 gap to the next-highest seed at $69.30 — anchoring the high tail. This is the first stand-alone (mode, column) HIGH on `maxSpot` in the filed record; prior maxSpot flags were all 16.67% or lower and confined to the 07-17 "all four modes 16.67%" cluster. Escalation gate: hold ≥ 10% next run → persistence-candidate; collapse → jitter noise.
- **Basket `realizedAbs` HIGH collapsed — persistence-candidate class remains unreached.** Yesterday's 25.00% flag anchored by seeds 202607255 (low $153.45) and 202607257/202607254 (high $275.51/$276.27) does not survive today's rotation: those exact seeds now sit at $221.14, $244.25, and $283.20 respectively — inside the whisker range. Full distribution today spreads $158.55 → $286.65 across 12 seeds with no gaps > $32. Confirms every prior single-run HIGH-or-MEDIUM (mode, column) in the filed record has collapsed within one rotation.
- **Spread `minSpot` persistence-candidate collapsed — three-run streak was not structural.** Yesterday's promotion out of the 12-seed jitter class based on three identical 16.67% filings does not survive today's 0.00%. The 07-10/07-21/07-24 cluster reads in hindsight as three coincident jitter fires, not a persistent bimodal spot distribution. Root-cause path is now cleaner: seed-count widening per [[compute-futures-12-seed-sample-too-small]] is the ONLY way to distinguish rotation-jitter from real bimodal distribution — no accumulated same-magnitude count under 12 seeds can suffice.
- **Basket `settlementLegs` outlier_pct on/off rotation continues.** Today's 16.67% is the fifth 16.67% fire in the last ten filings against four 0.00% zeros in between. Distribution {76:3, 77:3, 78:3, 80:9, 81:12, 83:6} tags 6 whisker rows (76×3 low, 83×6 high with 77/78 near-borderline). The (basket, settlementLegs) pair remains the highest-frequency 16.67% offender in the record; every fire so far has been followed by a next-run collapse to 0.00%, so the persistence gate has never held here despite frequent fires.
- **Correlation surface — no strong pairs, two fresh near-misses.** Basket: strongest non-wallet_sum_pnl pair `realizedAbs × settlementLegs` = +0.776 (finding 5, fresh); other basket near-misses `maxSpot × maxCurve` = −0.561, `seed × minSpot` = −0.558 (seed-padding artifact), `minSpot × maxSpot` = +0.509. Spread: strongest `minCurve × maxCurve` = +0.574, `realizedAbs × minCurve` = −0.566 — no near-threshold pairs. Synth/x402 (identical): `settlementLegs × maxCurve` = −0.700, `maxSpot × settlementLegs` = +0.608. x402-only: `minSpot × x402Total` = +0.745 (highest x402 pair today, near-miss). `settlementLegs × x402Total` = +0.357 continues the third consecutive positive (finding 6).
- **role_pnl scale per mode.** basket σ = $31.60 (μ = −$0.14) — narrowed from $37.73/−$0.71 on 07-24, does NOT extend the three-widening-steps trajectory from 07-24 (07-17 $29 → 07-18 $28 → 07-20 $34 → 07-21 $29 → 07-24 $38 → today $32); alternation-ish shape resumes. spread σ = $7,103.91 (μ = −$733.85) — essentially flat vs 07-24's $7,015/−$515 (+1.3% σ move, μ drifted more negative but well inside the historical range); 07-24's sharp narrowing after four consecutive widening steps holds. synth/x402 σ = $9.58 (μ = −$0.34) — narrowed from $12.08/−$0.47 on 07-24. Two of three modes narrowed today; only spread held near yesterday's level.

## Row count and shape vs 2026-07-24

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated fully.** 07-24 fired 1 HIGH (basket `realizedAbs` 25.00%) + 1 MEDIUM persistence-candidate (spread `minSpot` 16.67%). Both collapsed to 0.00% today. New today: 1 HIGH (spread `maxSpot` 25.00%, first appearance) + 1 MEDIUM recurring (basket `settlementLegs` 16.67%). Zero column-mode pairs survive at ≥ 10% across 07-24 → today — full substitution, second consecutive filing with 0/0 carryover across the ≥ 10% band.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; ninth consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under the threshold everywhere. Excluded basket pairs today: `wallet_sum_pnl × maxSpot` +0.738, `× minSpot` +0.702. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 15th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today. The 07-24 promotion of spread `minSpot` on a three-run streak did NOT survive one rotation. Structural conclusion strengthens: at 12 seeds, NO count of same-magnitude 16.67% fires reliably crosses out of rotation-jitter — the class only breaks with a wider seed count. Persistence-candidate class remains empty as of 07-25; the only two attempted promotions in the record (07-24 spread minSpot; 07-24 basket realizedAbs HIGH) both collapsed on next re-check.
- **x402 `settlementLegs × x402Total` sign-flip streak** — broken 07-24, held broken today. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → +0.357 (today). Three consecutive positives; magnitude drifting down but sign held. Same-sign persistence at any magnitude tomorrow (regardless of drift direction) promotes to emerging structural pair; a sign flip restores jitter classification.
- **Spread role_pnl σ shape** — 07-24's sharp narrowing to $7,015 (breaking the 3-run widening streak) holds today at $7,104. No new widening trajectory; no return to the pre-07-24 $9,000–$10,500 band.
- **Basket `realizedAbs × settlementLegs` = +0.776** — fresh near-miss pair, first candidate at |r| ≥ 0.7 in basket. Watch alongside basket settlementLegs on the next filing — if both re-fire, promote to a real within-mode structural signal.
