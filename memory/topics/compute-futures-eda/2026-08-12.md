# Compute-Futures EDA — 2026-08-12

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-12.csv` (144 rows, 4 modes)
**Run:** 2026-08-13
**Prior filed report:** 2026-08-11 (single-day gap).

## Top findings

1. **[MEDIUM — fresh, cross-mode 2-seed cluster, LOW-tail only, MODE-ROTATED from 08-10] synth/x402 `settlementLegs` fires at 16.67% (n=6 per mode), anchored by seeds 202608126 (76 legs) + 202608129 (75 legs).** IQR fence [76.88, 83.88] on q1=79.5 / q3=81.25; both anchors below fence. All 3 roles hit at each anchor (2 seeds × 3 roles × 2 equivalent modes = 12 outlier positions, 6 physical rows counted twice per [synth ≡ x402]). Mode-rotation: `settlementLegs` fired at 16.67% in **basket** on 08-10 (anchored 202608106 + 202608109), collapsed to 0% on 08-11, and today resurfaces in **synth/x402** with fully disjoint anchor seeds. No mode carries `settlementLegs` from one filing to the next — same rotation-not-persistence class as maxSpot/maxCurve. Basket `settlementLegs` today at 0% (0 outliers, tight cluster 76–83 with n=1 at 76).

2. **[MEDIUM — extension, promoted class n=6 consecutive filed runs] Spread `role_pnl` top-5 losses 100% retail; new anchor 202608122 retail −$24,241 (no new depth floor).** Top-5 today: 202608122 retail −$24,241 / 202608124 retail −$13,667 / 202608121 retail −$9,109 / 2026081211 retail −$9,044 / 202608128 retail −$7,779. Retail mean spread P&L **−$5,984** vs analyst +$1,316 / operator +$3,370 — bimodal split persists (retail std $9,387 vs analyst $5,332 / operator $4,746). Extends [[compute-futures-spread-retail-loss-concentration]] from n=5 to **n=6 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11, 08-12). **First-time counter-signal:** the SINGLE top gain today is also retail (202608127 retail +$14,006) — retail is bimodal, not uniformly-losing. Loss-floor did not deepen this filing (08-11 was −$24,696; today −$24,241, ~$455 shallower). Under IQR fence, spread `role_pnl` at 2.78% today (n=1, the −$24,241 anchor above); the promoted class rides on the role-conditional distribution, not the pooled IQR fence.

3. **[MEDIUM — new all-time RANGE-EXPANSION on BOTH sides simultaneously] x402Total range expands to $249.56–$429.30 (std $45.40).** New all-time LOW (\$249.56, 3-role tie at seed 202608127, below 08-11's $274.10 and 08-10's $269.61 prior floor). New all-time HIGH ($429.30, 3-role tie at seed 202608124, above 08-11's $425.61 prior ceiling by $3.69). Mean $330.41 (+$7.84 vs 08-11's $322.57). Sum $11,894.81 (+$282.35 vs 08-11's $11,612.46). Std $45.40 (up from $39.54 on 08-11). **Both-sided range expansion is new** — 08-11 widened HIGH-only; today widens on both. Ten-filing range history: $269.61 (08-10) → $274.10 (08-11) → **$249.56 (08-12)** on the LOW side; $381.61 (08-10) → $425.61 (08-11) → **$429.30 (08-12)** on the HIGH side. Sub-threshold at IQR (x402Total outlier_pct = 0% in x402 mode today — bimodal-by-design per SKILL exclusion), but the range-expansion signal is a separate class.

4. **[LOW — fresh sub-threshold, cross-mode 1-seed LOW-tail] Cross-mode `maxCurve` LOW cluster at 8.33% in basket + synth + x402 (spread separately at 8.33% with a different anchor).** Anchor for basket + synth + x402: padded seed **2026081211** (basket 0.2855 / synth 0.094 / x402 0.094). Basket/synth ratio at 2026081211 = 3.037 (within the wider 3.02× band). Spread `maxCurve` LOW anchor is DIFFERENT: 202608125 at 67.426 — spread does not share the padded-seed LOW anchor. Same shape as 08-11's cross-mode 1-seed `minCurve` finding (LOW-tail, 8.33% sub-threshold, 3-of-4 modes share anchor). Under-fire; watch for 2026081211 crossing to 2-seed status next filing.

5. **[LOW — full collapse] 08-11's cross-mode 2-seed `maxSpot` cluster (202608119 HIGH + padded 2026081110 LOW) fully collapses.** Today: zero `maxSpot` outliers in any mode. Rotation-not-persistence continues on `maxSpot`: 08-08 fired → 08-10 collapsed → 08-11 resurfaced with rotated anchor pair (both-tail split) → **08-12 fully collapses again**. Anchor-count series 2 → 0 → 2 → 0. Column-rotation across cross-mode ≥10% MEDIUMs: maxSpot (08-11) → settlementLegs (08-12).

6. **[LOW — promotion-ready, n=3 filed runs] basket/synth scale-ratio HELD at 3.025× exactly for `maxSpot`/`minSpot`.** Three-filing series (08-10, 08-11, 08-12) — meets the n=3 promotion criterion. `maxSpot` basket/synth ratio: min=max=3.0250 across all 12 seeds today (deterministic constant). `minSpot` basket/synth: min=max=3.0250 (deterministic constant). `maxCurve` basket/synth: 3.0147–3.0372 (mean 3.0212, small per-seed jitter — consistent with 08-11's 2.964–3.045 band). `minCurve` basket/synth: 2.9000–3.2254 (mean 3.0209, wider band). `realizedAbs` basket/synth: 2.7988–3.6400 (mean 3.1635 — wider still). The 08-08→08-10 jump (~2.5×→3.02×) reads as a real deployer-code price-draw change, corroborated at n=3 stability. **Promotion candidate for next filing** — write atomic [[compute-futures-basket-synth-3025x-multiplier]] with the 3-filing evidence.

7. **[LOW — n=4 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, **08-12 zero**. Extends 08-11's n=3 promotion-criterion hit to n=4. Highest within-mode pair today: spread `wallet_sum_pnl × maxSpot` = 0.742 (float-dust excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). Non-dust top pair: spread `realizedAbs × settlementLegs` = 0.605 — sub-threshold. n=12 seeds systematically insufficient to stabilize any within-mode |r|≥0.8; motivation for [[compute-futures-12-seed-sample-too-small]] fix reinforced at n=4.

8. **[LOW — anchor concentration shape] Two-super-anchor day (2026081211 at 9-pos + 202608129 at 8-pos), with 202608126 (6-pos) as third-tier.** Full concentration list: 2026081211 (9 — cross-mode `maxCurve` LOW), 202608129 (8 — synth/x402 `settlementLegs` LOW + synth/x402 `role_pnl` outliers), 202608126 (6 — synth/x402 `settlementLegs` LOW), 202608125 (3 — spread `maxCurve` LOW), 2026081210 (2 — synth/x402 `role_pnl`), 2026081212 (2 — synth/x402 `role_pnl`), 202608122 (1 — spread `role_pnl` retail floor). Class series: 08-10 super-single dense (202608106, 15 pos) → 08-11 tri-super (9+9+9, disjoint) → **08-12 two-super (9+8, disjoint)** + long-tail. **Padded-seed over-representation:** padded seeds (2026081210/11/12) contribute 13 of 31 outlier positions (42%) vs their 25% row-share — over-indexed as anchors, driven by 2026081211's cross-mode `maxCurve` cluster.

9. **[LOW — recurring, 26th filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608121`–`202608129`), 36 rows 10-char seed (`2026081210`–`2026081212`). `seed` outlier_pct = 25.00% in every mode. Padded 2026081211 anchors cross-mode `maxCurve` LOW today (9 positions). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

10. **[LOW — non-promotable jitter] Conservation sign flip in spread only.** `wallet_sum_pnl` mean today: basket **−1.60e-14** (vs 08-11 −2.87e-15; sign held, magnitude ~5.6× larger, still 15 orders under gate); spread **+2.04e-12** (vs 08-11 −7.89e-13; **SIGN FLIPPED** − → +, still 13 orders under gate); synth **+3.77e-15** (identical to 08-11 within precision); x402 **+3.77e-15** (identical to synth by [synth ≡ x402]). Std held near 08-11 in every mode. All modes remain 11+ orders under the |mean|>10 / std>100 gates. Per-mode wallet_sum_pnl outliers surface at 0%/0%/0%/0% today (IQR-fenced; previously 25%/16.67% on 08-11) — dropped everywhere. σ still ≥8 orders under the 1e-6 threshold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

11. **[LOW] x402 `settlementLegs × x402Total` continues to alternate around zero.** 20-filing series: +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → +0.179 → +0.475 → +0.558 → **+0.255**. Sign held +; magnitude down ~54% vs 08-11's +0.558. Alternation-not-persistence continues.

12. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). basket/synth ratio 3.025× (matches 08-10, 08-11).

13. **[PASS] Row/column shape unchanged vs 08-11.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.60e−14  | 4.84e−14  | OK |
| spread    | +2.04e−12  | 4.98e−12  | OK |
| synthetic | +3.77e−15  | 1.21e−14  | OK |
| x402      | +3.77e−15  | 1.21e−14  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=4 consecutive filed runs.** Extends the n=3 zero-firing signal hit on 08-11; still no within-mode pairwise relationship stabilizes past 0.8 at n=12 seeds. Highest non-dust pair today: spread `realizedAbs × settlementLegs` = 0.605.
- **Cross-mode 2-seed synth/x402 `settlementLegs` cluster (202608126 + 202608129, both LOW-tail)** — genuine cross-mode structural signal driven by per-seed input-draw quirk that surfaces identically across the two equivalent modes. Mode-rotates from 08-10's basket-only cluster. Anchor-count matches 08-11's maxSpot (2 anchors), but split is single-tail (LOW-only) not both-tail.
- **Cross-mode 1-seed `maxCurve` LOW cluster (2026081211)** — sub-threshold at 8.33%, anchored by a padded seed. Extends the padded-seed anchor concentration class. Basket/synth ratio at anchor = 3.037 (within band).
- **Retail-role loss concentration in spread — n=6 consecutive filed runs.** Extends promoted [[compute-futures-spread-retail-loss-concentration]] to n=6. First-time counter-signal: single top gain today is also retail (202608127 +$14,006). Retail bimodal — carries both the deepest losses and (occasionally) the deepest gains, unlike analyst/operator which stay closer to the mean.
- **basket/synth `maxSpot`/`minSpot` scale-ratio = 3.025 (deterministic) at n=3 filed runs.** Promotion-ready; write atomic [[compute-futures-basket-synth-3025x-multiplier]] next filing with the 3-filing evidence. `maxCurve`/`minCurve` ratio 3.02× with small jitter; `realizedAbs` ratio 3.16× with wider band.
- **Anchor-count class:** two-super-anchor day (2026081211 + 202608129), 9+8 disjoint positions. Padded seeds over-index at 42% of outlier positions vs 25% row share.
- **x402 `settlementLegs × x402Total` alternation continues** (20-filing series, +0.255 today, sign held +, magnitude down ~54% vs 08-11). No promotable trend.

## Row count and shape vs 2026-08-11

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface HELDS constant but ROTATES from 08-11 → 08-12.** 08-11: 0 HIGH, 3 MEDIUM (cross-mode maxSpot at 16.67%, retail-loss n=5 promoted, cross-mode minCurve sub-threshold). 08-12: 0 HIGH, 3 MEDIUM (cross-mode synth/x402 settlementLegs at 16.67%, retail-loss n=6 extends, x402Total both-sided range expansion). Total MEDIUM count held at 3; ≥10% column set fully rotated (maxSpot → settlementLegs). No column persists at ≥10% cross-mode in both filings. Column-rotation across cross-mode ≥10% MEDIUMs across the last four filings: settlementLegs (08-10) → maxSpot (08-11) → settlementLegs (08-12) — synth/x402 flavor this time, not basket.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's settlementLegs + role_pnl + maxCurve outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (3.025× exactly for maxSpot/minSpot, ~3.02× for curves)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable across 08-10, 08-11, 08-12 (n=3 promotion-ready).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers surface at 0%/0%/0%/0% today (dropped from 08-11's 25%/16.67%/0%/0%).
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 26th consecutive filed run. Padded 2026081211 anchors cross-mode `maxCurve` LOW today (9 positions); padded-seed over-representation at 42% of outlier positions. Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-11's cross-mode maxSpot MEDIUM collapses; 08-11's spread retail-loss promotion holds and extends to n=6. Fresh MEDIUM this filing is synth/x402 settlementLegs (mode-rotated from 08-10's basket-only). Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration ROTATES again** — series: 08-10 super-single dense (202608106, 15 pos) → 08-11 tri-super (9+9+9, disjoint) → **08-12 two-super (9+8, disjoint) + long-tail**. Concentration class: super-single → tri-super → **two-super**. Anchors fully rotate; zero overlap with 08-11's anchor set.
- **x402Total range expands on both sides** — new all-time LOW ($249.56) AND HIGH ($429.30) simultaneously. Ten-filing range: $322–$429 mean band; extremes now $249.56–$429.30. First both-sided expansion filing.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=6 (08-12).** First counter-signal today: retail also holds single top gain (+$14,006). Retail is bimodal, not uniformly-losing.
