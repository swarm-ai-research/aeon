# Compute-Futures EDA — 2026-06-26

**Source:** `memory/gitlawb-compute-futures-proofs/2026-06-26.csv` (144 rows, 4 modes)
**Run:** 2026-06-27

## Top findings

1. **[HIGH] spread `minCurve` outlier_pct = 25% (9/36)** — three seeds (`202606261`=38.12, `202606267`=40.97, `202606262`=41.85) sit outside the IQR fence [38.57, 40.46] built from the tight middle cluster (q1=39.28, q3=39.75). Each seed contributes 3 rows (one per role), so "25%" = 3 distinct seeds × 3 roles out of 12 seeds × 3 roles. Small-sample caveat: with only 12 seeds the IQR is brittle — 3 tail seeds is enough to cross the HIGH threshold even when the underlying distribution is well-behaved (skew 0.92, kurt 0.89). Next step: bump seed count from 12 → ≥30 in `prototypes/compute-futures/scenario-sweep.mjs` so per-mode IQR stops being threshold-sensitive to single-seed jitter.
2. **[MEDIUM] Outlier-tail composition shifted across every mode** — the baseline (2026-06-20) flagged `minSpot` at 16.67% in every mode and `settlementLegs` at 8.33% in three modes. Today: `minSpot` 0% everywhere, `settlementLegs` 0% everywhere, but `maxSpot` 8.33% appeared in all 4 modes (was 0% in baseline). Means and stds for these columns are within ~1% of baseline — the underlying distributions barely moved; the IQR-tail composition is jitter from a 12-seed sample. Treat the baseline's recurring `minSpot` carry-over as not a recurring pattern after all; today's `maxSpot` cluster is the same artifact wearing a different hat. Next step: stop reporting these in carry-overs until seed count rises.
3. **[MEDIUM] basket gained `wallet_sum_pnl` IQR outliers (8.33%)** — basket was clean for `wallet_sum_pnl` in baseline (per the floating-point-dust carry-over, only synthetic/x402 showed dust). Today basket has 3/36 IQR-flagged rows but the absolute range is [−4.35e−14, +8.22e−14], i.e. pure FP noise around zero. Conservation gate is mean |1e−14|, std 3.6e−14, both far under the 10/100 thresholds. Don't escalate — same mechanism as synthetic/x402 dust, now also visible in basket because today's basket residuals happen to spread wider than yesterday's.
4. **[LOW — recurring] Seed-encoding artifact unchanged.** Three seeds are 10-digit (`2026062610`, `2026062611`, `2026062612`); the other nine are 9-digit. Outlier detector flags `seed` at exactly 25% in every mode (same as baseline). Fix is still pending in `scenario-sweep.mjs` (pad index width).
5. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans 5e−15 → −2.4e−12, std 1.7e−14 → 5.7e−12 — float noise across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate.
6. **[PASS] No within-mode correlations at |r|≥0.8.** Strongest within-mode pair: basket/synthetic/x402 `minSpot × maxCurve` at +0.680/+0.684/+0.684 (was +0.680 baseline — stable). spread topped out at `realizedAbs × minCurve` r=+0.555. Prior-day basket `wallet_sum_pnl × maxCurve` r=−0.736 has collapsed to +0.087 today — it was sample-jitter, not real structure.
7. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max abs diff across all role-level numeric columns = 0.0).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −9.83e−15  | 3.57e−14  | OK |
| spread    | −2.39e−12  | 5.69e−12  | OK |
| synthetic | +5.09e−15  | 1.66e−14  | OK |
| x402      | +5.09e−15  | 1.66e−14  | OK |

## Within-mode signal

- **spread minCurve cluster (real value-distribution, NOT mode artifact):** three seeds produce minCurve at the distribution edges (38.12 / 40.97 / 41.85) vs a tight cluster of nine seeds in [39.27, 40.08]. Same per-seed minCurve replicated across all 3 roles, confirming this is a sweep-level (not role-level) input. If real, it points to two regimes inside the spread spot-generator. With 12 seeds we can't distinguish "true bimodality" from "random IQR fence breach"; revisit when seed count rises.
- **role_pnl scale per mode (same regime as baseline):** spread role_pnl std ≈ $9,874 (μ=−$674), basket std ≈ $29 (μ=−$0.53), synthetic/x402 std ≈ $10.4 (μ=−$0.31). spread still dominates pooled stats by 3 orders of magnitude. Per-mode stratification remains the right default.
- **role-level three-way structure in spread:** today's per-seed signs are mixed across roles; the prior baseline's "operator/analyst long, retail short" pattern is not evident in this single-day snapshot. Don't make a longitudinal claim from two days of data.
- **Strongest within-mode correlations (all below the 0.8 reporting bar):** `minSpot × maxCurve` ~+0.68 in basket/synthetic/x402; spread `realizedAbs × minCurve` ~+0.56. Worth watching: if `minSpot × maxCurve` climbs above 0.8 in synthetic/x402 it'd be a real structural link from the synthetic spot-generator.

## Row count and shape vs baseline

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged).
- Constants by design (`rounds=60`, `deniedWorkers=0`, `x402Total=0` outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed again (max |diff| = 0.0 across role_pnl, wallet_sum_pnl, realizedAbs). Only `x402Total` (μ≈$329, σ≈$24, range $278–$366) distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode. Flag only when these change.
- **`wallet_sum_pnl` IQR outliers in any mode at single-digit percentages** — floating-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold.
- **Seed-encoding 25% outlier in every mode** — artifact of unpadded YYYYMMDD+index seed string in `scenario-sweep.mjs`. Recurring. Fix is a one-line pad.
- **NEW carry-over from this run:** with only 12 seeds, single-column outlier_pct values in the 8–25% band are too brittle to interpret as real distribution change — they swing with which 1–3 seeds happen to land in the IQR fence. Treat the outlier_pct table as a coarse alarm, not a daily delta signal, until seed count crosses ~30.
