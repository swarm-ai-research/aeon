# Compute-Futures EDA — 2026-08-03

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-03.csv` (144 rows, 4 modes)
**Run:** 2026-08-04
**Prior filed report:** 2026-08-01. 08-02 sweep exists (`2026-08-02.csv`) but the 08-02 06:00Z compute-futures-eda slot was inside the ISS-020 morning-batch outage cluster and never ran; comparisons here span two sweeps.

## Top findings

1. **[LOW — but the 13-filing zero-streak breaks, and it breaks in three modes simultaneously] `realizedAbs × maxSpot` fires |r| ≥ 0.8 in basket / synthetic / x402.** Basket r = −0.853, synthetic r = −0.832, x402 r = −0.832 (synth ≡ x402 rows). First within-mode strong pair since 07-10 across 14 filings including the un-filed 08-02 CSV. Two-day trajectory sign-flipped through zero to land here: 08-01 basket −0.198 → 08-02 +0.210 → **08-03 −0.853**; synth/x402 −0.254 → +0.162 → **−0.832**. Spread stays uncoupled (+0.043). Meanwhile 08-01's "deepening watch" pair `realizedAbs × settlementLegs` went the *other* way: 08-01 basket −0.656 / synth −0.695 → 08-02 basket +0.760 / synth +0.745 (a same-mode-cluster near-miss on the un-filed sweep that would have been the streak break if 08-02 had been analyzed) → **08-03 basket −0.224 / synth −0.250 (collapsed)**. Different pair crossed the gate; the mode-column-cluster identity rotates as the rotation-not-persistence class predicts, but the underlying "realizedAbs coupled to a price column" structure held across three consecutive sweeps. Next step: if `realizedAbs × maxSpot` holds or deepens on 08-04, promote from LOW-per-SKILL-gate to a MEDIUM structural finding — this is the first serious candidate for real structural signal after 13 filings of pure rotation.

2. **[MEDIUM — fresh outlier cluster, primary anchor n=5 by consecutive filings] Basket `role_pnl` = 13.89% + synth/x402 `role_pnl` = 11.11% + synth/x402 `realizedAbs` = 16.67%; single seed 202608038 anchors positions in all three fires.** Basket outliers: seed 202608038 retail −$86.48 (worst) + 202608038 analyst +$59.57 (best); the same seed anchors *both tails* of basket `role_pnl`, plus synth/x402 role_pnl retail low (−$30.85), plus synth/x402 realizedAbs high ($88.36) — four outlier positions across three modes and two columns. Secondary anchors: seed 2026080311 (basket role_pnl −$51.56 + synth/x402 role_pnl −$17.40 + synth/x402 minCurve 0.0415), seed 202608037 (basket role_pnl −$64.98 + synth/x402 role_pnl −$22.83), seed 202608035 (same pattern as 037). Anchor pattern series 07-28 (202607286, 1 seed / 3 modes) → 07-30 (202607301, 1 seed / 2 modes) → 07-31 (202607314, 1 seed / 6 mode-columns) → 08-01 (202608012, 1 seed / 5 mode-columns) → **08-03 (202608038, 4 positions across 3 modes + secondaries)** — n=5 by filed cadence, and today the pattern intensifies from "one seed dominates one tail direction" to "one seed dominates *both* tails of a single mode-column AND leaks into other modes". Fix path unchanged per [[compute-futures-12-seed-sample-too-small]]: widen seeds or add persistence-across-filings dedup. All three fresh MEDIUM outlier fires reduce to a single mechanism (seed 202608038's draw sits outside the 12-seed IQR in whichever column touches it).

3. **[MEDIUM — mode-collapse artifact resets in basket, doesn't migrate] 08-01's HIGH basket `settlementLegs` 41.67% fully collapses to 0.00%.** Basket settlementLegs distribution 08-01 {81:21, 80:6, 82:3, 83:3, 79:3} → 08-02 {81:12, 79:9, 80:6, 82:3, 78:3, 75:3} → **08-03 {80:15, 77:6, 74:3, 76:3, 79:3, 81:3, 82:3}**. The mode value shifted from 81 to 80 and the distribution spread from range [79,83] to range [74,82] — IQR widened from 0.25 (08-01 bounds [80.375, 81.375]) to 3.0 (08-03 bounds [72.5, 84.5]), so 15 rows at the mode value now sit comfortably inside the bounds. Same 15/36 concentration (41.67%) as 08-01, same tightest-single-column mode-collapse in the 20-filing window, but IQR-narrowing artifact no longer fires. Notably 08-01's mode-swap-from-spread pattern did not repeat: spread settlementLegs on 08-03 is well-distributed {78:12, 80:6, 84:6, ...} and unaffected. Class-general fix (tie-robust statistic) would suppress the artifact both directions; the raw 15/36 concentration itself is the underlying signal to investigate.

4. **[MEDIUM — the x402 settlement fire fully redistributes then re-fires] x402 `x402Total` = 16.67% (6 outliers, top-of-band).** Two seeds at the high end: seed 202608033 = $376.06 × 3 rows + seed 202608039 = $385.19 × 3 rows. IQR bounds [$267.32, $371.75]; both seeds sit just above. Excluded from severity ranking per SKILL (bimodal-by-design, low-tail is the always-outlier design), but noted here as the second sighting of this pattern in the 20-filing window (07-31 also fired 6 outliers at the high end; 08-01 fully redistributed to 0). x402Total top-end $385.19 recovers most of the way from 08-01's $383.47 but still under 07-31's record $409.32. Sum $11,622.48 (down $598.47 vs 08-01, down $869.60 vs 07-31 — three-filing decline trend continues); mean $322.85 (down $16.62 vs 08-01, third consecutive drop: 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 — a $23.25 two-filing decline).

5. **[LOW — recurring, 20th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608031`–`202608039`), 36 rows 10-char seed (`2026080310`–`2026080312`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

6. **[LOW — recurring] Basket σ(role_pnl) *widens* to $30.17 while the primary anchor takes both tails.** Series (7-day σ): 07-25 → 07-28 → 07-30 → 07-31 $35.75 → 08-01 $28.47 → 08-03 $30.17. Not a fresh MEDIUM by σ alone — the widening is driven by seed 202608038's two-tail split (both the worst and second-best row in basket). Spread σ(role_pnl) tightens sharply to $8,288 (from 08-01's $9,086) — smaller both tails (min −$21,085 vs 08-01's −$18,201; max +$14,579 vs +$14,109 — actually deeper on the min side but tighter overall variance). No spread `role_pnl` outlier fire (5.56%).

7. **[LOW — 20th consecutive filed run at zero |r| ≥ 0.8 through 08-01; STREAK BROKEN today].** Prior streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, 07-28, 07-30, 07-31, 08-01 (13 filings). Today three modes fire simultaneously per finding 1. First zero-streak break in the skill's history. Finding 1 tracks the promotion path.

8. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −5.94e−15 (basket), +2.53e−12 (spread), +8.03e−16 (synthetic), +8.03e−16 (x402). Std 2.74e−14 (basket), 6.78e−12 (spread), 6.42e−15 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Signs shifted vs 08-01 in basket (− ← +) and synth/x402 (+ held); spread stayed positive. Synth/x402 σ tightened 40% from 08-01's 1.07e−14 to 6.42e−15 (float-dust regime; not interpretive).

9. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, and all price columns except `x402Total`).

10. **[PASS] Row/column shape unchanged vs 08-01.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −5.94e−15  | 2.74e−14  | OK |
| spread    | +2.53e−12  | 6.78e−12  | OK |
| synthetic | +8.03e−16  | 6.42e−15  | OK |
| x402      | +8.03e−16  | 6.42e−15  | OK |

## Within-mode signal

- **`realizedAbs × maxSpot` fires |r| ≥ 0.8 in three modes.** Basket −0.853, synth/x402 −0.832. Structural interpretation: within a 12-seed draw, the seeds that generate high `realizedAbs` (high aggregate settlement flow) tend to be seeds where the price band closed lower — consistent with mean-reversion inside the rolling window (heavy activity → drawn-down top-of-band). Spread doesn't participate (r = +0.043), consistent with spread's price scale being 3–4 orders larger and driven by different rebalancing dynamics. This is the first same-shape strong-pair firing across three modes in the skill's history.
- **The 08-01 "deepening watch" pair `realizedAbs × settlementLegs` intermediates then collapses.** 08-01 basket −0.656 → 08-02 basket +0.760 (near-miss + sign-flip; would have broken the streak if 08-02 had been analyzed) → 08-03 basket −0.224. Synth/x402 mirror: −0.695 → +0.745 → −0.250. Rotation-not-persistence class holding for the *pair identity*, but the underlying cluster ("realizedAbs strongly coupled to something") persists across all three sweeps — three consecutive filings with realizedAbs at the center of the strongest within-mode correlation, only the coupling partner rotates (settlementLegs → settlementLegs → maxSpot).
- **Cross-mode single-seed anchor pattern intensifies to n=5.** Seed 202608038 dominates *both* tails of basket `role_pnl` (worst −$86.48 and best +$59.57) — first time in the pattern's history a single seed anchors opposite tails of the same mode-column. It also leaks into synth/x402 `role_pnl` low tail and `realizedAbs` high tail, giving 4 outlier positions across 3 modes and 2 columns. Interpretation: this seed drew both the largest-in-magnitude retail loss and the largest analyst gain in the basket mode, and the same seed produced the largest realizedAbs in synth/x402. Consistent with a heavy-tail draw at the seed level that leaks into every column IQR touched by that seed's rows. Fix: seed-count widening suppresses the entire class.
- **08-01's basket `settlementLegs` HIGH does not migrate.** Spread stays healthy today (well-distributed 7-value distribution); the 07-31 → 08-01 spread-to-basket mode-swap did not repeat as a basket-to-spread swap. The 15/36 basket concentration at value 80 today has the same raw shape as 08-01's 21/36 at value 81, but a wider surrounding distribution absorbs it inside IQR bounds. Class-general fix would still remove both cases.

## Row count and shape vs 2026-08-01

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface migrates from settlementLegs (08-01) to role_pnl + realizedAbs (08-03).** 08-01 fired 1 HIGH (basket settlementLegs 41.67%) + 1 fresh MEDIUM (basket maxCurve 16.67%) + 1 pattern MEDIUM (anchor n=4). 08-03 fires **0 HIGH + 3 fresh MEDIUM (basket role_pnl 13.89% + synth/x402 role_pnl 11.11% + synth/x402 realizedAbs 16.67%) + 1 pattern MEDIUM (anchor n=5)**. The 08-01 fires (basket settlementLegs + basket maxCurve) both collapsed to 0%. Rotation-not-persistence class holding across the full surface — the *columns* rotate, the *anchor pattern* deepens.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, and all price columns between synth and x402; only `x402Total` distinguishes x402). Same-row outliers in synth/x402 for `role_pnl` and `realizedAbs` this filing are the same physical rows, counted once per SKILL rule.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 10+ orders under the threshold everywhere (highest is spread's 6.78e−12). Excluded pairs today: basket `wallet_sum_pnl × seed` = −0.471, basket `wallet_sum_pnl × minCurve` = +0.418, synth/x402 `wallet_sum_pnl × seed` = −0.465, synth/x402 `wallet_sum_pnl × minSpot` = −0.437. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 20th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence.** Both 08-01 MEDIUM column-fires (basket settlementLegs 41.67 → 0, basket maxCurve 16.67 → 0) collapsed today; three fresh MEDIUM column-fires opened (basket role_pnl 0 → 13.89, synth/x402 role_pnl 0 → 11.11, synth/x402 realizedAbs 0 → 16.67). Anchor pattern MEDIUM re-fired (5th filing).
- **Cross-mode single-seed anchor pattern PROMOTED to MEDIUM at n=5** — series 07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314 → 08-01 seed 202608012 → **08-03 seed 202608038**. Today's anchor is the strongest yet: both tails of basket `role_pnl` plus leaks into synth/x402. Fix path: widen seeds per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` remains jitter.** Series (14 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 (08-01 broke the 6-positive streak) → **08-03 +0.036** (essentially zero; jitter). No interpretive story.
- **x402Total three-filing declining trend.** 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 (mean); sum drops $869.60 across the same span. Top-end $385.19 also third-consecutive under 07-31's record $409.32. Worth a watch — if 08-04 continues the decline, promote to a MEDIUM structural finding.
- **08-02 sweep exists but was not analyzed** — the 08-02 06:00Z compute-futures-eda slot was inside the ISS-020 morning-batch outage cluster (per memory index). The 08-02 CSV shows the intermediate state (r `realizedAbs × settlementLegs` +0.760 basket / +0.745 synth-x402 — a near-miss on the streak break that would have led today's finding-1 narrative to be "second consecutive strong pair, watch for structural"). Backfill the 08-02 sweep only if pattern verification on 08-04 fails and the intermediate is needed.
