# Compute-Futures EDA — 2026-07-08

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-08.csv` (144 rows, 4 modes)
**Run:** 2026-07-09

## Top findings

1. **[LOW] First |r| ≥ 0.8 crossing in the run window: synthetic/x402 `wallet_sum_pnl × settlementLegs` = −0.874.** Same physical pair reported twice per synthetic ≡ x402 tautology at the role level. Prior day (2026-07-07) was +0.501 in both modes — a flip from mild positive to strong negative in one run. Mechanically this is the same float-dust-vs-settlement-volume shape flagged yesterday in spread (`realizedAbs × wallet_sum_pnl` −0.752): `wallet_sum_pnl` in synthetic/x402 has σ ≈ 8.85e−15 (machine epsilon accumulated over 60 rounds and 75–83 settlement legs), and more legs accrue more signed float error. Not a strategy P&L signal — conservation gate (|mean|>10 or std>100) still holds by fourteen orders of magnitude. Watch: if this pair re-crosses ≥ 0.8 tomorrow in synthetic/x402, log as numerical-drift telemetry. [[compute-futures-12-seed-sample-too-small]]
2. **[LOW — collapse of yesterday's near-leader] spread `realizedAbs × wallet_sum_pnl` = +0.190 (was −0.752).** Yesterday's biggest-|r| pair collapsed within one run window — same 12-seed jitter shape that has moved this pair between {−0.06, −0.745, +0.048, −0.467, −0.752, +0.190} across the last six runs. Confirms the correlation churn carry-over. spread top pair today is `settlementLegs × seed` +0.499, well under gate.
3. **[LOW — cluster resolution] Basket `maxCurve` outlier_pct 16.67% → 8.33% and synthetic/x402 `maxCurve` 16.67% → 0.00%.** Yesterday's two [MEDIUM] flags both self-resolved on the next seed rotation, consistent with the 12-seed alarm-without-signal carry-over. No underlying regime change either day.
4. **[LOW — recurring] x402Total range $301.08–$400.45, mean $337.81, total $12,161.17.** Fourth consecutive movement inside the ~$100 band (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400). Mean drifts within ±$5 of $335. `x402Total × settlementLegs` r = −0.237 today (was −0.110 on 07-07, +0.557 on 07-05); the structural link continues to churn sub-threshold. Not trending.
5. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows carry 9-digit seeds, 36 rows carry 10-digit (`2026070810`, `2026070811`, `2026070812`). `seed` outlier_pct = 25% in every mode, twelfth consecutive run. One-line pad in `scenario-sweep.mjs` still pending. [[compute-futures-seed-padding-bug]]
6. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean ranges from −4.4e−13 (spread) to +2.3e−15 (synth/x402); std 8.85e−15 (synth/x402) → 6.15e−12 (spread). All twelve-plus orders of magnitude under the |mean|>10 / std>100 gates. spread std tightened slightly vs 07-07 (6.15e−12 vs 6.04e−12; essentially identical at this scale).
7. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 role-level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402.
8. **[PASS] No CRITICAL/HIGH/MEDIUM findings.** Every per-mode column outlier is ≤ 8.33% (below the 10% MEDIUM gate). No schema drift, no row-count drop, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −5.70e−15  | 2.02e−14  | OK |
| spread    | −4.36e−13  | 6.15e−12  | OK |
| synthetic | +2.27e−15  | 8.85e−15  | OK |
| x402      | +2.27e−15  | 8.85e−15  | OK |

## Within-mode signal

- **New leader crosses gate: synthetic/x402 `wallet_sum_pnl × settlementLegs` = −0.874.** First time any pair crosses |r| ≥ 0.8 in the run window we track. Reported once physically — the second is the synthetic ≡ x402 tautology. Interpretation is numerical, not strategic: `wallet_sum_pnl` σ ≈ 8.85e−15 (float dust), settlementLegs range 75–83, and each leg contributes signed rounding error to the conservation sum. Larger leg count → larger accumulated |dust| in a consistent direction. Add to the same numerics-drift ledger as yesterday's spread near-leader.
- **role_pnl scale per mode (regime unchanged):** spread σ = $8,209 (μ = −$552), basket σ = $32.2 (μ = −$1.3), synthetic/x402 σ = $12.2 (μ = −$0.41). Spread widened vs 07-07 ($6,832) but stays well inside 12-seed jitter and continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **Yesterday's spread `realizedAbs × wallet_sum_pnl` −0.752 collapsed to +0.190.** Same 12-seed churn pattern that repeatedly promotes then demotes float-dust-vs-volume pairs. Watch abandoned.
- **x402-specific structural pairs sub-threshold:** `maxSpot × x402Total` +0.408 (was +0.542), `minCurve × x402Total` +0.425, `minSpot × x402Total` −0.568. All plausible (bigger trades → more x402 fees) but jittering below 0.6.

## Row count and shape vs 2026-07-07

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column set diff).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold. `live` is True in basket/spread, False in synthetic/x402; `settlement` is `cash` everywhere except x402 (`x402`); `spotSource` is `darkbloom-basket`/`darkbloom-spread`/`synthetic`/`synthetic` per mode.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single- and low-double-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold. Today: 8.33% in synthetic/x402.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string. One-line pad in `scenario-sweep.mjs` still pending.
- **12-seed outlier_pct instability** — the (mode, column) pair that fires shifts each run: 06-26 spread minCurve 25% → 06-28 spread settlementLegs 33% → 07-03 synth/x402 settlementLegs 16.67% → 07-05 basket wallet_sum_pnl 16.67% + synth/x402 minCurve 16.67% → 07-07 basket maxCurve 16.67% + synth/x402 maxCurve 16.67% → 07-08 (no column crosses 10%; every flag stayed at 8.33%). Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `realizedAbs × wallet_sum_pnl`: [−0.06 (06-26), −0.745 (07-03), +0.048 (07-05), −0.467 (07-07-early), −0.752 (07-07), +0.190 (07-08)]. Synth/x402 `wallet_sum_pnl × settlementLegs`: +0.501 (07-07) → −0.874 (07-08). Consistent 12-seed jitter until any pair confirms twice.
