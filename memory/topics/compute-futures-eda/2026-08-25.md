# Compute-Futures EDA — 2026-08-25

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-25.csv` (144 rows, 4 modes)
**Run:** 2026-08-26
**Prior filed report:** 2026-08-24 (daily cadence resumes; no gap).

## Top findings

1. **[HIGH — cross-mode ≥10% cluster ANCHOR ROTATES from `settlementLegs` → `realizedAbs`; basket joins the cluster for the first time in the class series; 3-mode span (basket + synth + x402) is a class-first breadth event] `realizedAbs` outlier cluster surfaces at **33.33% (12/36) in synth AND x402** (mode-mirror pair) AND **25.00% (9/36) in basket** — first-ever 3-mode co-cluster on a single distributional column.** synth/x402 realizedAbs IQR bounds [64.66, 112.46] with 12 rows outside (values ~53.67 / 61.30 / 113.90 / 131.09). basket realizedAbs IQR bounds [157.15, 289.40] with 9 rows outside (values ~145.12 / 153.05 / 339.03). 08-24's `settlementLegs` HIGH cluster (33.33% synth/x402 at leg=81) fully COLLAPSES to 0% in every mode today — the leg=81 concentration drops from 18/36 rows to 9/36 in synth/x402, IQR widens back out, cluster dissolves. Anchor-column rotation confirms the HIGH-tier class is column-rotating rather than persistence-holding. Cross-mode ≥10% cluster count: 08-22 = 0 → 08-24 = 3 → **08-25 = 3** — count HOLDS at 3 but on a completely different column and now spans a third mode. Not P&L-driven; realizedAbs is the |sum of PnL| conservation counter, so a cross-mode concentration on it plausibly reflects a stochastic seed alignment on realized-round magnitudes rather than a config change (spot columns unchanged, seeds identical structure). Next step: watch 08-26 for whether realizedAbs cluster persists or rotates again — 08-24's settlementLegs 1-day rotation predicts rotation.

2. **[HIGH — CROSSES the n=7 invalidation-cadence threshold of the prior 3.0250× regime; multiplier holds byte-identical] basket/synth spot-price multiplier at **2.5000 exact** for **7th consecutive filed run** — the exact cadence at which the previous 3.0250× regime broke per [[compute-futures-multiplier-invalidated-at-n-7]] (regime was invalidated at n≥7 after starting to hold at n=3).** `minSpot` per-seed ratio **[2.500000, 2.500000]** std **1.82e-15** (byte-identical, effectively unchanged from 08-24's 1.42e-15); `maxSpot` per-seed ratio **[2.500000, 2.500000]** std **8.14e-16** (byte-identical, TIGHTER than 08-24's 1.03e-15 by ~20%). Curve columns diverge: `maxCurve` mean **2.4977** (std **6.28e-03**, essentially unchanged from 08-24's 5.48e-03); `minCurve` mean **2.5370** (std **7.14e-02**, ~3× LOOSER than 08-24's 2.37e-02 — 08-24 was 2× looser than 08-22; monotonic loosening trend across 3 filings). `realizedAbs` per-seed ratio mean **2.5505** (std **6.80e-02**, ~33% TIGHTER than 08-24's 1.01e-01 — reversing 08-24's widening back toward 08-22's 1.26e-01 range). Class series post-rename: n=1 (08-18) → n=2 (08-19) → n=3 (08-20) → n=4 (08-21) → n=5 (08-22, rename threshold) → n=6 (08-24) → **n=7 (08-25, matches the invalidation-cadence threshold of the prior 3.025× regime — first survival at that cadence)**. Next step: at n=8 (08-26 CSV) the 2.5× regime would surpass the 3.025× regime's max survival cadence; treat n=8 as the new empirical durability floor if it holds.

3. **[MEDIUM — extension, n=17 consecutive filed runs; retail mean SHALLOWS 25% vs 08-24; 2/12 retail rows WIN (first non-all-losing retail day in 2 filings)] Spread `role_pnl` retail mean **−$9,091** (25% shallower than 08-24's −$12,150), **10/12 retail losing** (breaks 08-22/08-24 two-day all-losing streak); the 2 winners are **BOTH padded seeds** (2026082511 retail +$826, 2026082512 retail +$3,747) — first filing where the retail-loss regime is partially broken by the padded-seed encoding.** Sum-of-retail spread P&L: **−$109,096** across 12 seeds (25% shallower than 08-24's −$145,796; matches 08-16's −$109k range). Top-5 losses: **seed 202608257 retail −$25,260 (new interim class series peak, deeper than 08-24's −$24,360 by $900 and than 08-22's class-record 202608228 retail −$24,618 by $642)** / **202608251 retail −$16,376** / **2026082510 retail −$15,928 (padded seed in top-5 losses; class-first padded-in-top-5 appearance since 08-20)** / **202608255 retail −$15,270** / **202608252 retail −$11,432** — 5/5 retail (**7th consecutive filed run at 5/5 top-5 retail purity**). Top-5 gains: **202608257 analyst +$13,898 (new interim analyst gain peak, replaces 08-24's 202608243 +$12,805)** / 202608251 operator +$11,762 / 202608257 operator +$10,904 / 202608255 operator +$10,850 / 202608258 operator +$10,460 (0 retail in top-5 gains — 17-run pattern holds). Per-role means: retail **−$9,091** (std $8,286, min −$25,260, max **+$3,747** — first positive retail max in 2 filings); operator **+$6,173** (down $41 from +$6,214, effectively unchanged); analyst **+$1,535** (down 66% from +$4,553, largest role-mean shift today). **Seed 202608257 triple-role top-5 anchor** (retail loss + analyst gain + operator gain) — 3rd consecutive filing with a triple-role top-5 seed after 08-22's 202608228 and 08-24's 202608245; within-seed dispersion **$39,158** (retail −$25,260 → analyst +$13,898), **new class-series HIGH, surpassing 08-22's 202608228 record $37,492 by $1,666**. Extends [[compute-futures-spread-retail-loss-concentration]] from n=16 to **n=17 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18 → 08-22, 08-24 → 08-25).

4. **[MEDIUM — first-ever fresh non-zero correlation approaching |r|≥0.8 threshold on the x402Total counter, big vs 08-24] x402 mode `minSpot × x402Total` jumps to **+0.785** (from +0.053 on 08-24; **+0.732 magnitude swing, largest single-day x402Total-partner shift in the class series**), just $0.015 shy of the strong-pair threshold.** x402 `maxSpot × x402Total` also jumps: **+0.579** (from −0.169; +0.748 swing, magnitude flip). Other x402 × x402Total shifts vs 08-24: `settlementLegs × x402Total` **+0.372** (was +0.458, −0.086 attenuation), `realizedAbs × x402Total` **−0.065** (was +0.330, −0.395 attenuation), `wallet_sum_pnl × x402Total` **+0.198** (was +0.373, dust-side, excluded), `minCurve × x402Total` **−0.160** (was +0.206, sign flip), `maxCurve × x402Total` **+0.134** (was +0.053). The correlation-anchor for x402Total now shifts from `settlementLegs` (08-16 → 08-24 dominant partner series with 6+ filings hovering ±0.06 → +0.54) to **spot-price pair (`minSpot` + `maxSpot`)** — first filing where x402Total co-varies with the spot-price signal at moderate-strength magnitude. Structural interpretation: today's x402 pricing regime binds x402 fee closer to the underlying spot level, whereas prior regimes bound fee closer to leg count. Not yet a `|r|≥0.8` reportable pair. Watch next filing to see if `minSpot × x402Total` crosses 0.8 (would be the first-ever within-mode strong pair in the class series).

5. **[MEDIUM — x402Total range COMPRESSES 39% vs 08-24 peak; matches 08-20's exact range] x402Total range **contracts to $88.20** (min $274.42, max $362.62) — 39% below 08-24's series-peak $145.43, matches 08-20's $88.05 within $0.15 (2nd-narrowest range in the 7-filing series).** HIGH $362.62 — **drops $76.11** below 08-24's all-time HIGH $438.73 (largest single-day HIGH retracement in the series; 08-24's HIGH-anchor 202608241 spike does not repeat). LOW $274.42 — **drops $18.87** below 08-24's floor $293.29 (new interim series LOW; $18.04 above 08-19's all-time-low $256.38). Mean **$326.73** (down $5.21 from $331.95 — mean essentially unchanged; contraction is symmetric around the mean). Std **$27.23** (down 40% from $45.56, largest single-day std collapse in series). x402Total outlier_pct = **0%** today (was 8.33% on 08-24) — no HIGH-anchor seed spike. Series range: $83.55 (08-16) → $119.90 (08-18) → $131.66 (08-19) → **$88.05 (08-20)** → $103.60 (08-21) → $122.73 (08-22) → **$145.43 (08-24, peak)** → **$88.20 (08-25, tied 2nd-narrowest)**. Range contracts one day after peak — matches 08-19 → 08-20 pattern; the range-widen/compress oscillation continues.

6. **[MEDIUM — super-anchor cluster expands to TRIPLE-9 tie, partial recovery from 08-24 trough; padded seed joins the anchor set for the first time in 3 filings] Top seeds by non-dust outlier positions: **202608257, 202608259, 2026082511 tied at 9 positions each** (fresh triple-anchor tie).** All 3 anchor seeds hit the full outlier-cluster set for their mode (basket realizedAbs + basket seed-encoding + spread seed-encoding + synth/x402 realizedAbs + synth/x402 seed-encoding). Fourth-ranked seed 202608251 at 6 positions (basket-only cluster set). **Seed 202608257** is the same seed that drives finding 3's triple-role P&L anchor — cross-signal alignment (both the spread P&L extreme AND the distributional-outlier count anchor land on the same seed). Padded seed **2026082511** returns to the anchor set after 08-24's total-absence trough (0%). Class series: 08-19 single 19 → 08-20 single 22 (all-time peak) → 08-21 DUAL 18/18 → 08-22 single 6 → **08-24 DUAL 3/3 (series trough)** → **08-25 TRIPLE 9/9/9 (partial recovery, roughly halfway back to 08-21 level)**. Anchor magnitude series: 22 → 18 → 6 → 3 → **9** — first up-tick after 4-day downtrend, halts the "quiet ebb" regime. Anchor identity still does NOT persist between filings (6th consecutive full-collapse of prior day's super-anchor: 08-24's 202608244 + 202608246 drop to 0 positions today).

7. **[LOW — n=16 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-24's n=15 to **n=16**. Highest today ex-seed-encoding: **x402 `minSpot × x402Total` = +0.785 (finding 4; first-ever x402Total-partner pair to approach 0.8)**; **spread `wallet_sum_pnl × maxSpot` = +0.751** (float-dust dependent side, excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]); **spread `realizedAbs × maxCurve` = +0.722** (structural mid-strength, appears fresh — 08-24 had spread `realizedAbs × maxCurve` in the mid-range, today jumps toward threshold); **synth/x402 `minCurve × realizedAbs` = −0.657** (persistent structural mid-strength, unchanged direction vs 08-24's −0.665); **basket `maxCurve × settlementLegs` = +0.607** (basket-only mid-strength, fresh vs 08-24's basket range). n=12 seeds still systematically insufficient; [[compute-futures-12-seed-sample-too-small]] motivation continues.

8. **[LOW — conservation regime returns to MIXED 3-positive 1-negative after 08-24 mixed] Per-mode `wallet_sum_pnl` means all near-zero float dust: basket **+8.61e−15**, spread **−5.76e−13**, synth **+4.76e−15**, x402 **+4.76e−15**.** Sign pattern: basket + synth + x402 POSITIVE, spread NEGATIVE (vs 08-24: basket +, spread +, synth −, x402 − — 3 sign flips today). All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). Per-mode `wallet_sum_pnl` IQR outlier percentages: basket 0%, spread 0%, synth 0%, x402 0%. All float dust; excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. The 08-24 spread 8.33% wallet_sum_pnl outlier cluster does NOT rotate to another mode today — rotation cadence breaks (was: basket 08-22 → spread 08-24 → nothing 08-25).

9. **[LOW — padded seeds fully MIRROR the retail-win side of the spread distribution for the first time in class series] Padded seeds (2026082510/11/12) hold 36 of 144 rows (25%). Today they contribute **9 of 33 non-dust outlier positions (27.3%)** — near-proportional (was 0% on 08-24, 9.1% severely under on 08-22, matches 08-20's ~21% mildly under band).** Padded seed 2026082511 anchors 9 outlier positions (tied for #1 super-anchor, finding 6). Padded seed 2026082510 anchors the 3rd-deepest retail loss (−$15,928, first padded appearance in spread top-5 losses since 08-20). Padded seeds 2026082511 and 2026082512 anchor **BOTH positive retail rows** (+$826 and +$3,747) — first filing where the padded encoding lands on the retail-win side of the spread distribution (all prior filings put padded seeds on the retail-loss side or dust). Class series: 08-19 severely under → 08-20 mildly under → 08-21 mildly over → 08-22 severely under → 08-24 total absence → **08-25 proportional AND asymmetric (both top-anchor AND retail-win)**. Fresh signal: padded encoding today drives BOTH extremes in spread (top-anchor cluster + retail winners) — no rotation pattern in the class series predicted this bimodal placement.

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`, `settlementLegs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: **0.000000e+00** (byte-identical).

11. **[PASS] Row/column shape unchanged vs 08-24.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +8.61e−15  | 2.97e−14  | OK |
| spread    | −5.76e−13  | 4.96e−12  | OK |
| synthetic | +4.76e−15  | 1.18e−14  | OK |
| x402      | +4.76e−15  | 1.18e−14  | OK |

All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: basket + synth + x402 POSITIVE, spread NEGATIVE (3 sign flips vs 08-24; mixed-sign regime continues, but the negative-sign mode rotates from synth/x402 pair → spread).

## Within-mode signal

- **HIGH — `realizedAbs` cross-mode co-cluster on 3 modes (finding 1)** — basket 25%, synth 33.33%, x402 33.33%; class-first 3-mode span; 08-24's settlementLegs cluster fully collapses. Anchor column ROTATES filing-over-filing.
- **HIGH — basket/synth 2.5000× multiplier holds at n=7 post-rename** (finding 2) — matches the exact invalidation-cadence threshold of the prior 3.025× regime; spot columns byte-identical (std 8.14e-16 tightest ever), curve columns continue asymmetric drift.
- **Retail-role loss concentration in spread — n=17 consecutive filed runs, retail mean SHALLOWS 25% to −$9,091, 10/12 losing (breaks 2-day all-losing streak)** (finding 3). Class direction holds; **seed 202608257 triple-role top-5 anchor with new class-series HIGH dispersion of $39,158** — surpasses 08-22's 202608228 record by $1,666. 3rd consecutive filing with a triple-role top-5 seed anchor.
- **x402 `minSpot × x402Total` = +0.785, near-threshold** (finding 4) — first-ever fresh moderate-strength x402Total-partner pair on the spot-price side; +0.732 magnitude swing vs 08-24. Structural anchor shifts from settlementLegs → spot pair.
- **x402Total range COMPRESSES 39% to $88.20** (finding 5) — matches 08-20's $88.05 within cents; largest single-day HIGH retracement in series ($76 drop). Mean stable at $326.73, extremes symmetrically contract.
- **Super-anchor cluster expands to TRIPLE 9/9/9 tie (202608257 + 202608259 + 2026082511)** (finding 6) — partial recovery from 08-24 trough (3 → 9); halts 4-day downtrend. Seed 202608257 doubles as spread P&L anchor.
- **Zero within-mode strong pairs (|r|≥0.8) — n=16 consecutive filed runs** (finding 7). Highest ex-seed: x402 `minSpot × x402Total` +0.785; spread `realizedAbs × maxCurve` +0.722 (fresh mid-strength appearance).
- **Conservation regime shifts to 3-positive-1-negative** (finding 8) — 3 sign flips vs 08-24; wallet_sum_pnl 8.33% rotation cadence (basket 08-22 → spread 08-24 → nothing today) breaks.
- **Padded seeds land bimodally in spread — both top-anchor AND retail-win** (finding 9) — class-first asymmetric placement; near-proportional 27.3% outlier participation but with novel bimodal roles.

## Carry-overs

- 25% seed encoding artifact (n=37 filed runs, pending [[compute-futures-seed-padding-bug]] fix).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers there excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.
- 2026-08-23 CSV remains unanalyzed in the proof dir (2-day gap noted in 08-24 filing carries forward as archival hole; today's comparisons are all vs 2026-08-24).
