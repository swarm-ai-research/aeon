# Compute-Futures EDA — 2026-07-30

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-30.csv` (144 rows, 4 modes)
**Run:** 2026-07-31
**Prior filed report:** 2026-07-28 (2-day gap; 07-29 CSV exists in the proof stream but was not analyzed by this skill on that day — same skipped-day pattern as the 07-26/07-27 gap.).

## Top findings

1. **[LOW — persistence-gate uncrossed] Basket `settlementLegs` outlier_pct COLLAPSED to 0.00%.** 07-28 fired at 16.67% (fourth 16.67% fire in twelve calendar days); today back to 0.00% (distribution {79:6, 80:12, 81:12, 82:6}, tight in the 79–82 window). Rotation series 07-09 → today, incorporating the skipped days: 8.33 → 25.00 (07-10) → 0.00 (07-12) → 16.67 (07-16) → 0.00 (07-17) → 16.67 (07-20) → 16.67 (07-21) → 0.00 (07-24) → 16.67 (07-25) → 0.00 (07-26 unfiled) → 0.00 (07-27 unfiled) → 16.67 (07-28) → **0.00 (07-30)**. Every fire since 07-09 still followed by a next-run collapse — persistence gate remains uncrossed per [[compute-futures-12-seed-sample-too-small]]. Persistence-candidate class stays empty. Next-step: hold classification; expect a re-fire within 1–3 filings on rotation.
2. **[LOW — cross-mode single-seed anchor, second consecutive filing] Seed 202607301 anchors basket `realizedAbs` AND x402 `x402Total` simultaneously.** basket `realizedAbs` = $312.11 (single 8.33% outlier at the top of the tail; next-highest 2026073012 at $275.09); x402 `x402Total` = $400.72 (single 8.33% outlier, new record high for the metric in the 17-filing window — supplants 07-28's $401.89 as top-end after rounding). Distinct from 07-28's cross-mode `maxSpot` anchor (seed 202607286 across basket/synth/x402); different seed, different columns, but the same structural pattern: **one seed dominates multiple modes' high tails simultaneously**. Second consecutive filing with a cross-mode single-seed spike. Track whether the "one seed lifts multiple mode tails" pattern re-fires on the next filing — if so, promote to MEDIUM for investigation (n=3 is the persistence threshold per the 12-seed jitter class).
3. **[LOW — sign-flip streak: FIFTH consecutive positive; magnitude bounce REVERSES the three-run decay.]** x402 `settlementLegs × x402Total` = **+0.540** today. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → +0.357 (07-25) → +0.241 (07-28) → **+0.540** (today). Fifth consecutive positive; magnitude trajectory +0.657 → +0.357 → +0.241 was monotonically decaying toward zero, but today's +0.540 breaks the decay with a sharp bounce back into the >0.5 band. Escalation gate holds: one more positive filing at any magnitude keeps the emerging-structural watch alive; a sign flip on the next filing reclassifies back to pre-07-24 jitter. Companion pair `x402Total × wallet_sum_pnl` = −0.592 (dust-excluded); `minSpot × x402Total` = −0.292 (down from +0.080 → sign flip).
4. **[LOW — spread minSpot fresh 8.33% outlier, single-seed] Seed 202607307 is the sole spread `minSpot` outlier at $39.78** (next-lowest values ~40.29). Below the 10% severity floor but not previously reported for spread `minSpot`. Same seed shows up in the top-2 x402Total tail ($352.17), a second-tier cross-mode anchor. Watch alongside seed 202607301 for the "cross-mode seed anchor" persistence question.
5. **[LOW — fresh spread near-miss, seed-artifact family] Spread `seed × realizedAbs` = +0.714** (up from 07-28's +0.552). Seed-encoding artifact per [[compute-futures-seed-padding-bug]] — the unpadded 9-char/10-char seed split creates a spurious ordering; not structural. Highest single-mode near-miss below the |r| ≥ 0.8 threshold this filing, and would trip the threshold on the next incremental widening — but only via the artifact.
6. **[LOW — eleventh consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong pair. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, 07-28, **today**. Non-artifact-family near-misses: basket `minCurve × maxCurve` = +0.521 (curve-band structural, weakening from prior mid-0.6s), `maxSpot × minCurve` = −0.536, `realizedAbs × wallet_sum_pnl` = −0.601 (dust-excluded); spread `minSpot × maxSpot` = +0.671, `maxSpot × maxCurve` = +0.663, `maxCurve × minSpot` = +0.653, `realizedAbs × minCurve` = −0.682 (spread-band structural cluster near +0.65 in the price-band, stable); synth `wallet_sum_pnl × settlementLegs` = −0.714 (dust-excluded), `seed × minSpot` = −0.602, `minSpot × settlementLegs` = −0.602; x402 `settlementLegs × x402Total` = +0.540 (finding 3), `x402Total × wallet_sum_pnl` = −0.592 (dust-excluded).
7. **[LOW — recurring] Seed 25% encoding artifact unchanged, 17th consecutive filed run.** 108 rows 9-char seed (`202607301`–`202607309`), 36 rows 10-char seed (`2026073010`–`2026073012`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
8. **[LOW — recurring] x402Total mean DOWN $25, sum DOWN $918, range widens to record-widest vs 07-28.** Mean $317.17 (down $25.49 vs 07-28's $342.66 — reverses the +$20.11 07-28 move; new low for the 17-filing window), range $270.35–$400.72 (band width $130.37, up from $120.66 → **widest band in the 17-filing window** at both ends: low-end $270 is new record-low, high-end $400.72 essentially tied with 07-28's $401.89), sum $11,418.20 across 36 x402 rows (down $918). Bimodal-by-design outlier exclusion holds; the single 8.33% flag on x402Total is finding 2's seed-201 anchor at the top of the non-zero distribution.
9. **[LOW — recurring] Spread `wallet_sum_pnl` absolute range 1.42e−11 (~67% of 07-28's 2.12e−11 — spread tightened).** absmax 1.06e−11 (down slightly from 1.14e−11 on 07-28); Σ σ across modes: basket 3.99e−14 (up ~26% from 3.17e−14), spread 4.31e−12 (down ~21% from 5.44e−12, second consecutive tightening), synth/x402 1.21e−14 (down ~13% from 1.39e−14). All 12+ orders under any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Excluded pairs today: basket `realizedAbs × wallet_sum_pnl` = −0.601, spread `wallet_sum_pnl × settlementLegs` = −0.408, synth/x402 `wallet_sum_pnl × settlementLegs` = −0.714, x402 `x402Total × wallet_sum_pnl` = −0.592.
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.49e−14 (basket), −1.40e−12 (spread), −2.41e−16 (synthetic), −2.41e−16 (x402). Std 3.99e−14 (basket), 4.31e−12 (spread), 1.21e−14 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Signs held negative in every mode (basket flipped from −1.84e−15 on 07-28 to −1.49e−14 today, same sign; spread flipped from −9.14e−13 to −1.40e−12; synth/x402 sign-flipped from −3.37e−15 to −2.41e−16 magnitude decrease — all inside the float-dust regime, not signals).
11. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical wallet_sum_pnl mean/std, role_pnl mean/std, all per-column distributions except `x402Total`). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-28.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.
13. **[PASS] No CRITICAL, HIGH, or MEDIUM findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift, no per-mode column at outlier_pct ≥ 10% (excluding recurring `seed` artifact and bimodal-by-design `x402Total`). Second consecutive filing with **no HIGH-severity column-mode pair** since 07-25's spread `maxSpot` HIGH (25.00%) collapsed on 07-28.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.49e−14  | 3.99e−14  | OK |
| spread    | −1.40e−12  | 4.31e−12  | OK |
| synthetic | −2.41e−16  | 1.21e−14  | OK |
| x402      | −2.41e−16  | 1.21e−14  | OK |

## Within-mode signal

- **Basket `settlementLegs` on/off rotation collapsed on schedule; distribution tightened to 79–82 window.** 07-28's 16.67% fire → today's 0.00% is the fifth clean off-cycle in the record after a fire. Distribution today {79:6, 80:12, 81:12, 82:6} tighter than 07-28's {78:6, 80:6, 81:15, 82:6, 83:3} (no 78 low, no 83 high). Every basket `settlementLegs` fire since 07-09 has collapsed on the next filing — persistence gate remains uncrossed at n=12 seeds.
- **Cross-mode single-seed anchor pattern rotates to seed 202607301.** Second consecutive filing with a single seed dominating multiple modes' high tails. Today: 202607301 anchors basket `realizedAbs` ($312.11) + x402 `x402Total` ($400.72). 07-28: 202607286 anchored maxSpot in basket/synth/x402. Different seed, different columns each filing — but the *pattern* persists two filings running. Cross-mode single-seed anchoring is not a mode-specific artifact and not a bug in outlier detection; it's real seed-level variance shining through the small-n distribution. If it re-fires on the next filing at n=3, promote to MEDIUM for investigation.
- **Spread price-band structural cluster stable near +0.65.** `minSpot × maxSpot` +0.671, `maxSpot × maxCurve` +0.663, `maxCurve × minSpot` +0.653 — three-way structural cluster in the spread-mode price band, stable at ~+0.65 across filings. Not a strong pair (all below +0.8) but consistent within-mode signal — this is the "real correlations survive the per-mode view" band that distinguishes spread from the pooled-cross-mode artifacts. Spread `realizedAbs × minCurve` = −0.682 is the strongest single spread near-miss (excluding seed-artifact).
- **Basket curve-band correlation weakened.** `minCurve × maxCurve` = +0.521 vs prior mid-0.6s (spread's equivalent −0.541). Weakening basket curve-band signal is not new, but merits watching — if it drifts below +0.3 it becomes a candidate for regime-artifact reclassification.
- **role_pnl scale per mode: two modes narrowed, one widened; opposite of 07-28's pattern.** basket σ = $33.62 (μ = −$0.36) — narrowed 34% from 07-28's $51.06/−$1.98 (reverses 07-28's widening); spread σ = $7,264 (μ = −$382) — widened 8% from $6,711/−$306; synth/x402 σ = $11.91 (μ = −$0.06) — narrowed 17% from $14.39/−$0.72. Two narrowed, one widened is the mirror of 07-28's two-widened, one-narrowed pattern. Cross-filing σ oscillation is consistent with the 12-seed sample variance, not a regime shift.
- **x402 correlation surface bounces from decay.** `settlementLegs × x402Total` +0.540 breaks the +0.657 → +0.357 → +0.241 decay trajectory. 07-28's fresh near-miss `realizedAbs × x402Total` +0.641 collapsed to +0.263 today (did not survive — reproduces the pattern that fresh single-run near-misses collapse on the next filing). 07-25's `minSpot × x402Total` +0.745 (already collapsed to +0.080 on 07-28) further to −0.292 today — full sign flip completes the collapse.

## Row count and shape vs 2026-07-28

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated fully.** 07-28 fired 1 MEDIUM recurring (basket `settlementLegs` 16.67%). Basket `settlementLegs` collapsed to 0.00% today — recurring MEDIUM extinguished for this filing. No new MEDIUM fires. Second consecutive filing with **zero HIGH** at ≥ 20% (fourth in the last five filings; 07-25 was the last HIGH fire at spread `maxSpot` 25.00%).

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical wallet_sum_pnl, role_pnl, and all price columns between synth and x402; only `x402Total` distinguishes x402). Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; eleventh consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under the threshold everywhere. Excluded pairs today across modes: basket `realizedAbs × wallet_sum_pnl` −0.601; spread `wallet_sum_pnl × settlementLegs` −0.408; synth/x402 `wallet_sum_pnl × settlementLegs` −0.714; x402 `x402Total × wallet_sum_pnl` −0.592. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 17th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today. 07-28's basket `settlementLegs` 16.67% MEDIUM collapsed to 0.00%; 07-28's fresh x402 `realizedAbs × x402Total` +0.641 near-miss collapsed to +0.263. Persistence-candidate class remains empty; every prior single-run HIGH-or-MEDIUM promotion attempt in the record has collapsed on next re-check. Only breaks with a wider seed count per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` sign-flip streak** — now **five consecutive positives**; decay trajectory (+0.657 → +0.357 → +0.241) broken by today's bounce to +0.540. Sign holds; magnitude decay is not monotone. One more positive keeps emerging-structural watch alive; a sign flip on the next filing reclassifies back to jitter.
- **x402Total band shape** — 07-28's mean-up + top-end record ($342.66 / $401.89) partially reverses today: mean DOWN to $317.17 (new 17-filing low), range width widens to $130.37 (new 17-filing widest) with new low-end $270.35, top-end essentially tied at $400.72. Bimodal-by-design outlier exclusion holds; the 8.33% x402Total flag is the seed-201 top-of-band single-seed anchor from finding 2.
- **2-day filing gap 07-29.** CSV exists (`memory/gitlawb-compute-futures-proofs/2026-07-29.csv`, 27,060 bytes) but no report was filed on 2026-07-30. Cross-referenced rotation series in finding 1 skips 07-29. Same pattern as the 07-26/07-27 gap. Skill's "latest CSV only" policy skips intermediate days — if backfilling matters for persistence-gate calls, consider a batch pass.
- **Cross-mode single-seed anchor pattern** — new pattern-watch, second consecutive filing. 07-28: seed 202607286 anchors maxSpot in basket/synth/x402. Today: seed 202607301 anchors basket `realizedAbs` + x402 `x402Total`. Different seed, different columns, but same "one seed dominates multiple modes' tails" structural pattern. If re-fires on the next filing, promote to MEDIUM.
