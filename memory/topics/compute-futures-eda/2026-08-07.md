# Compute-Futures EDA — 2026-08-07

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-07.csv` (144 rows, 4 modes)
**Run:** 2026-08-07
**Prior filed report:** 2026-08-06 (no missing intermediate CSV in the proof stream — 08-06 → 08-07 is a single sweep-day diff).

## Top findings

1. **[MEDIUM — 2nd consecutive filing at 16.67%] Spread `settlementLegs` = 16.67%, same rate as 08-06 but the anchor seeds rotate.** 6 outliers today from 2 seeds: 202608076 x3 at value 83 (high tail) + 2026080710 x3 at value 75 (low tail). Yesterday's fire was 202608064 + 202608065 both at value 78 (both left-tail). IQR bounds [78.5, 82.5] on IQR=1.00 (narrowed from 08-06's [78.13, 83.13] IQR=1.25); the tighter fence now catches BOTH extremes (75 below + 83 above) rather than just 78. Column-persists-but-seeds-rotate is a new persistence sub-shape distinct from the 07-31/08-01 anchor-persistence pattern. Track whether 08-08 fires the same 16.67% rate a third time — if yes, promotable to HIGH pattern-MEDIUM per column-persistence rule.

2. **[MEDIUM — fresh column-fire] Spread `minSpot` = 16.67%, both 10-char padded seeds anchor the low tail.** 6 outliers from 2026080711 x3 at 38.93 + 2026080712 x3 at 37.36 (mean 42.17, IQR bounds [39.55, 45.64]). Both anchor seeds carry the 10-char pad prefix (part of the [[compute-futures-seed-padding-bug]] 3-seed group), so the fire concentrates on the padded-seed sub-cohort — a testable claim: if padding is causing draw-space distortion, then padded seeds should over-represent in outlier columns at a rate > 3/12 = 25% baseline. Today's spread minSpot fires at 2/2 padded seeds = 100% concentration; sample too small to conclude but worth flagging as a padding-related draw-space finding for the fix-widening PR.

3. **[MEDIUM — fresh column-fire] Spread `maxSpot` = 16.67%, bimodal both-tails cluster.** 6 outliers from 202608071 x3 at 61.69 (low tail) + 202608074 x3 at 69.62 (high tail). IQR bounds [61.92, 69.58] on IQR=1.53. Both-tail fire pattern (opposite of 08-06's maxCurve which was 3 seeds pulling both tails; today's is 2 seeds pulling one each). Combined with finding #1 and #2, spread mode fires THREE fresh 16.67% column-outliers today across `settlementLegs` + `minSpot` + `maxSpot` — the outlier-surface migration from single-column concentration to multi-column spread continues (07-30/07-31 single-column dominance → 08-06 single HIGH column + secondaries → 08-07 three concurrent MEDIUM columns in spread mode).

4. **[LOW — within-mode strong pair emerges] Spread `realizedAbs × maxCurve` = +0.897.** First |r| ≥ 0.8 within-mode fire since 08-03's 3-mode `realizedAbs × maxSpot` triple. Zero-firing streak snaps at n=1 filed run (08-06 was the restart-day at zero pairs). This pair is structurally plausible — within spread mode, higher maxCurve (price band top ~40–74) should mechanically drive higher realizedAbs (total absolute realized value ~$22k–$62k) via wider rebalancing swings. All three roles per seed share identical realizedAbs + maxCurve values, so the correlation is a per-seed volume/band-width relationship (not a role-level artifact). Prior 08-06 spread `realizedAbs × maxCurve` was under threshold (not reported by 08-06's correlation_explorer at 0.8 gate) — this is genuinely fresh, not persistence.

5. **[LOW — collapse] 08-06's HIGH spread `maxCurve` 25.00% (9 outliers) → 08-07 0.00%.** Rotation-not-persistence pattern re-confirmed for the 22nd HIGH/MEDIUM column-fire in the filed-run window. Yesterday's 3-seed cluster (202608065 + 202608066 + 2026080612 anchoring both tails) fully disperses today; those three seeds' spread maxCurve values today are 70.05 + 70.13 + 74.44 — all inside today's fence [67.65, 76.72]. Not because the seed values changed (deterministic draws), but because the *other* 9 seeds shifted enough that the fence widened.

6. **[LOW — collapse, persistence hypothesis fails at n=3] 08-06's MEDIUM (2-filing) basket `minCurve` 16.67% → 08-07 0.00%.** Sequence: 08-04 (un-analyzed CSV showed 16.67%) → 08-06 (16.67%) → 08-07 (0.00%). Two-filing persistence at same rate broke on the third-filing test. Basket now has ZERO column-outliers today outside seed encoding + wallet_sum_pnl float dust — cleanest basket day in the filed-run window. Column rotation continues; SKILL's rotation-vs-persistence class holds as the default hypothesis (persistence requires n≥3 confirmation, and no column has crossed it yet).

7. **[LOW — anchor pattern DISSOLVES from concurrent to diffuse] 08-06 had 4 seeds concurrent with top-seed at 15 positions; today has 8+ distinct anchor seeds with top-seed at 6 positions (only via synth ≡ x402 duplication).** Excluding seed-encoding and float-dust exclusions, anchor progression: 07-28 n=1 seed → 07-30 n=1 → 07-31 n=1 → 08-01 n=1 → 08-03 n=1+secondaries → 08-06 n=4 concurrent → **08-07 n=8+ diffuse**. Today the ranking is 202608078 at 6 (from synth+x402 maxCurve duplicate) then a flat tie of 7 seeds at 3–4 positions each. Pattern-class rotates from "concurrent multi-anchor" back toward "no dominant anchor" — the underlying interpretation (12-seed sample too small for stable IQR fences) is the same across all three shapes (single-anchor → multi-anchor → diffuse). Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].

8. **[LOW — non-promotable jitter] x402Total mean $326.38 (down $13.75 vs 08-06's $340.13).** Sum $11,749.62 (down $495.19 vs 08-06). Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 → 08-06 $340.13 → **08-07 $326.38**. Oscillates within $322–$346 range across six filings; no trend, no promotion. Top-end today $389.47 (down from 08-06's $396.20; still under 07-31's $409.32 record). Bottom-end $282.65 (new low for the 22-filing window's non-zero x402 draws).

9. **[LOW — retail-role loss cluster in spread] Spread `role_pnl` = 5.56% (under 10% threshold, but worth noting).** 2 outliers, both retail: seed 202608073 retail at −$18,285 (new single-row loss max) + seed 2026080712 retail at −$17,857. Both surpass 08-06's max loss (202608066 retail at −$17,798); today's max is $487 deeper. Not a promotable finding — n=2 and under 10% — but the pattern of "retail role concentrates the largest spread losses" now holds across 08-06 → 08-07 (top-3 spread losses on both days all retail).

10. **[LOW — recurring, 22nd filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608071`–`202608079`), 36 rows 10-char seed (`2026080710`–`2026080712`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]]. Finding #2 hints padded-seed sub-cohort may over-concentrate in some spread outlier columns — one more data point for the padding-fix PR's motivation.

11. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.18e−14 (basket), −1.09e−12 (spread), −4.51e−16 (synth/x402). Std 1.58e−14 (basket), 6.10e−12 (spread), 9.96e−15 (synth/x402). All ≥11 orders under the |mean| > 10 / std > 100 gates. Basket sign flipped − → + vs 08-06, magnitude down ~28%. Spread sign held (−), magnitude up ~2× (still float dust regime). Synth/x402 sign flipped + → −, magnitude similar. Basket wallet_sum_pnl fired 8.33% "outlier" at seed 202608076 x3 = 4.24e−14 — excluded from ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

12. **[PASS] Schema intact.** All 17 columns present (`seed`, `mode`, `live`, `settlement`, `spotSource`, `role`, `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `rounds`, `settlementLegs`, `x402Total`, `deniedWorkers`). Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, and all price columns except `x402Total`).

13. **[PASS] Row/column shape unchanged vs 08-06.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.18e−14  | 1.58e−14  | OK |
| spread    | −1.09e−12  | 6.10e−12  | OK |
| synthetic | −4.51e−16  | 9.96e−15  | OK |
| x402      | −4.51e−16  | 9.96e−15  | OK |

## Within-mode signal

- **One strong pair |r|≥0.8 today: spread `realizedAbs × maxCurve` = +0.897.** First within-mode fresh fire since 08-03's triple. Structurally plausible (higher spread price-band top drives wider rebalancing volume); worth watching whether 08-08 sustains — if yes, first genuine within-mode structural signal in the filed-run window; if no, adds to the rotation-not-persistence pile.
- **Spread's three concurrent 16.67% column-fires (settlementLegs + minSpot + maxSpot) are anchored by SEVEN distinct seeds.** No overlap: settlementLegs at 202608076 + 2026080710; minSpot at 2026080711 + 2026080712; maxSpot at 202608071 + 202608074. Contrast with 08-06 spread maxCurve where 3 seeds (202608065 + 202608066 + 2026080612) drove one column with 9 outliers. Today's spread outlier surface is column-wider but seed-shallower — no seed anchors more than one spread column-fire. Fits the diffuse-anchor class from finding #7.
- **08-06's cross-column same-seed pattern (spread maxCurve + role_pnl driven by same 3 seeds) does not repeat today.** Today's spread `role_pnl` extremes (retail 202608073 + retail 2026080712) don't overlap with today's spread column-outlier anchors (202608076 + 2026080710 for settlementLegs, 2026080711 + 2026080712 for minSpot, 202608071 + 202608074 for maxSpot, 202608072 for minCurve). Only 2026080712 appears in both `role_pnl` (as retail-loss anchor) and `minSpot` (as low-tail anchor) — n=1 overlap is coincidence at this seed count. Yesterday's "same 3 seeds drive maxCurve AND role_pnl extremes" hypothesis does not carry.
- **x402 `settlementLegs × x402Total` collapses back to zero.** Series (16 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → **+0.031**. Sign flips continue; no interpretive story. Alternation-not-persistence class re-confirms.

## Row count and shape vs 2026-08-06

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface migrates from spread `maxCurve` HIGH + basket `minCurve` MEDIUM + spread `settlementLegs` MEDIUM (08-06) to spread `settlementLegs` MEDIUM (persistence) + spread `minSpot` MEDIUM (fresh) + spread `maxSpot` MEDIUM (fresh) + spread `realizedAbs × maxCurve` LOW-signal (fresh) (08-07).** Basket now zero column-outliers; synth/x402 only maxCurve 8.33%. Column concentration RE-shifts to spread mode entirely (three of today's four notable fires are spread-mode) — first time in the filed-run window that one mode holds all fresh MEDIUM fires.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, and all price columns between synth and x402; only `x402Total` distinguishes x402). Same-row outliers in synth/x402 for `maxCurve` this filing are the same physical rows, counted once per SKILL rule.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 10+ orders under threshold in every mode (highest today is spread's 6.10e−12). Notable excluded pairs today: basket `wallet_sum_pnl × minCurve` = −0.635, synth/x402 `wallet_sum_pnl × minCurve` = −0.521. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 22nd consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]]. Finding #2 hints padded-seed sub-cohort may over-concentrate in some spread outlier columns; one more datapoint for the padding-fix PR.
- **12-seed outlier_pct rotation-not-persistence.** 08-06's HIGH spread `maxCurve` collapsed to 0. 08-06's MEDIUM basket `minCurve` collapsed to 0 after 2-filing persistence (n=3 hypothesis fail). 08-06's MEDIUM spread `settlementLegs` PERSISTS at same 16.67% but with rotated seeds — new sub-shape "column persists, seeds rotate". Two fresh MEDIUM column-fires open (spread minSpot + spread maxSpot). Multi-anchor pattern (08-06's n=4 concurrent) DISSOLVES to diffuse-anchor (n=8+ today).
- **Cross-mode single-seed anchor pattern SHIFTS FURTHER to DIFFUSE** — series 07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314 → 08-01 seed 202608012 → 08-03 seed 202608038 → 08-06 four concurrent (202608061, 202608064, 202608065, 202608068 with 202608064 top at 15 positions) → **08-07 diffuse (8+ seeds, top 202608078 at 6 positions via synth+x402 maxCurve duplicate; no true anchor)**. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` alternation continues** (see within-mode signal above). 16-filing series still oscillates ±; no promotable trend.
- **x402Total oscillates within $322–$346 mean range across six filings.** No trend. Today's $326.38 sits between 08-03 low ($322.85) and 07-31 high ($346.10).
- **Retail-role concentrates spread's largest losses** — 08-06 top-3 all retail (−$17,798 / −$6,259 / −$6,148); 08-07 top-2 all retail (−$18,285 / −$17,857). n=2 filings; not promotable yet. Worth watching whether this becomes a structural role-vs-role finding in spread mode.
- **08-06's cross-column same-seed pattern (spread maxCurve HIGH + role_pnl retail losses driven by same 3 seeds) does not repeat.** Today's spread role_pnl anchors don't overlap with column-outlier anchors (n=1 coincidence overlap at 2026080712). Interpretation from 08-06 was "structural or coincidental at n=12" — today's data votes coincidental.
- **spread `realizedAbs × maxCurve` first fresh within-mode |r|≥0.8 fire since 08-03.** Zero-firing streak snaps at n=1 filed run. If 08-08 sustains, it becomes the first candidate for a genuine within-mode structural signal in the filed-run window; if 08-08 collapses, it joins 08-03's `realizedAbs × maxSpot` as another single-filing rotation-artifact.
