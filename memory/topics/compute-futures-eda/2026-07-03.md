# Compute-Futures EDA — 2026-07-03

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-03.csv` (144 rows, 4 modes)
**Run:** 2026-07-03

## Top findings

1. **[MEDIUM] synthetic/x402 `settlementLegs` outlier_pct = 16.67% (6/36 each)** — six rows in each of synthetic and x402 sit at 76 legs, below the IQR fence [77, 85] built from a tight upper cluster (q1=80, q3=82). Same physical cluster reported twice per the synthetic ≡ x402 tautology. Underlying distribution is 76–82 (μ=80.17, σ=2.06) — normal jitter, not a regime change. This is the same 12-seed IQR brittleness that made 2026-06-28's spread `settlementLegs` 33% flag disappear today (spread 0% now, fence [75.75, 85.75] covers the full range). Yesterday's spread flag → today's synthetic/x402 flag → same alarm-without-signal pattern. Next step unchanged: bump seed count in `prototypes/compute-futures/scenario-sweep.mjs` to ≥30 so per-mode IQR stops flipping between modes each run. [[compute-futures-12-seed-sample-too-small]]
2. **[LOW] spread `realizedAbs × minSpot` r = −0.745 (new top pair)** — strongest within-mode correlation of the run, just below the 0.8 reporting bar. Notable because this pair is real values (realizedAbs order $10^4, minSpot order 40) — unlike 2026-06-28's top pair (`wallet_sum_pnl × minSpot` −0.745 in spread), which was noise × input-driver. Yesterday's noise-driven pair collapsed today (r=−0.06); today's realizedAbs pair is a more plausible structural link between spread exposure and spot floor. Watch: if it crosses 0.8 twice in a row it's real.
3. **[LOW] x402 `settlementLegs × x402Total` r = +0.685** — x402-specific structural link, sub-threshold. Only appears in x402 because `x402Total` is zero elsewhere; more settlement legs → more x402 charges is mechanically plausible. Same reading as 2026-06-28's `x402Total × maxCurve` +0.171 was: real but not strong enough to promote. No action.
4. **[LOW — recurring] Seed-encoding artifact unchanged.** Three seeds are 10-digit (`2026070310`, `2026070311`, `2026070312`); the other nine are 9-digit. Outlier detector flags `seed` at exactly 25% in every mode. Ninth consecutive run with this pattern. One-line pad in `scenario-sweep.mjs` still pending — see [[compute-futures-seed-padding-bug]].
5. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans 1.55e−15 → −4.55e−13, std 8.11e−15 → 4.79e−12 — float dust across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate. Both basket and synthetic/x402 are tighter than 2026-06-28 (basket std 1.72e−14 vs 2.23e−14, synthetic/x402 std 8.11e−15 vs 6.24e−15).
6. **[PASS] No within-mode correlations at |r| ≥ 0.8.** Top per mode: spread `realizedAbs × minSpot` −0.745 (see #2), spread `seed × minSpot` +0.028 (was +0.681 yesterday — collapsed), basket/synthetic/x402 `minSpot × maxCurve` around −0.55 to −0.65, x402 `settlementLegs × x402Total` +0.685 (see #3). The prior recurring `seed × minSpot` seed-encoding signature that showed up as +0.68 in spread yesterday is at +0.028 today — 12-seed noise again.
7. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402 (μ=$322.33, σ=$24.27, range $273.65–$366.67 — slightly cooler than 2026-06-28's $287–$385).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.55e−15  | 1.72e−14  | OK |
| spread    | −4.55e−13  | 4.79e−12  | OK |
| synthetic | +4.61e−15  | 8.11e−15  | OK |
| x402      | +4.61e−15  | 8.11e−15  | OK |

## Within-mode signal

- **role_pnl scale per mode (regime unchanged):** spread std ≈ $8,204 (μ=−$474), basket std ≈ $26.89 (μ=−$1.18), synthetic/x402 std ≈ $10.16 (μ=−$0.46). spread continues to dominate pooled stats by three orders of magnitude — per-mode stratification remains the right default. All three modes' role_pnl scales are ~15% tighter than 2026-06-28's ($9,582 / $28 / $10.6) — coincidence within a 12-seed sample; not a regime shift.
- **spread top-pair rotated from noise to signal:** yesterday spread's highest |r| was `wallet_sum_pnl × minSpot` −0.745 (noise × driver), today it's `realizedAbs × minSpot` −0.745 (real × driver). Same magnitude, different physical claim. Real if it holds two days.
- **x402 mirrors synthetic exactly at the role level** — role-level numeric columns differ by 0.0. Only x402-specific structure is `settlementLegs × x402Total` r=+0.685 (see finding #3) and the x402Total distribution itself.
- **settlementLegs jitter mode-hops each day:** 2026-06-26 spread minCurve 25% → 2026-06-28 spread settlementLegs 33% → 2026-07-03 synthetic/x402 settlementLegs 16.67%. Underlying std holds ~2 legs across the same 76–84 range in all runs; the IQR fence lands on different tails each day because 12 seeds are too few to stabilize q1/q3.

## Row count and shape vs 2026-06-28

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded YYYYMMDD+index seed string. One-line pad in `scenario-sweep.mjs`.
- **12-seed outlier_pct instability** — confirmed again today: spread's 33% `settlementLegs` flag from 2026-06-28 is 0% today; synthetic/x402 that were 0% yesterday are 16.67% today. Treat any single-column outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `wallet_sum_pnl × minSpot` went [+0.362 (06-26), −0.745 (06-28), +0.362 (07-03)]; spread `realizedAbs × minSpot` was −0.06 yesterday, −0.745 today. All within 12-seed jitter until confirmed twice.
