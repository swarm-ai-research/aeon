# Compute-Futures EDA — 2026-08-18

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-18.csv` (144 rows, 4 modes)
**Run:** 2026-08-18
**Prior filed report:** 2026-08-16 (two-day gap; no `2026-08-17.csv` was emitted).

## Top findings

1. **[MEDIUM — n=7 promoted class BREAKS, deterministic step 3.0250 → 2.5000] basket/synth `minSpot`/`maxSpot` ratio drops from 3.0250 exactly to 2.5000 exactly.** Byte-identical constant on both sides: `minSpot` basket/synth min=max=**2.500000** (std=0, 12/12 seed pairs); `maxSpot` basket/synth min=max=**2.500000** (std=0, 12/12 pairs). Curve/realizedAbs ratios follow proportionally: `minCurve` mean **2.4949** (vs 08-16's 3.0180, −17.4%), `maxCurve` mean **2.4969** (vs 3.0241, −17.4%), `realizedAbs` mean **2.5877** (vs 3.1364, −17.5%). Series held byte-identical **3.0250** across 08-10 through 08-16 (n=7 consecutive filed runs); breaks cleanly to **2.5000** at n=8-would-be. Not schema drift (column set unchanged), but a deterministic scale-factor step — most likely a deployer-side config change in `scenario-sweep.mjs`'s basket-vs-synthetic multiplier constant. Class [[compute-futures-basket-synth-3025x-multiplier]] promoted at n=7 now invalidated; motivates a fresh class at n=1 with new anchor 2.5000 (or a config-audit finding if the change was unintentional). Basket/synth per-seed rank order preserved (multiplier is uniform), so the mechanism is `basket = synth × k` with k stepping from 3.0250 to 2.5000.

2. **[MEDIUM — extension, promoted class n=11 consecutive filed runs, DEEPENS SHARPLY] Spread `role_pnl` retail mean **−$10,506** (deepens $2,540 = 32% vs 08-16's −$7,966); top-5 losses restored to 5/5 retail purity after 08-16's 4/5 mixing.** Top-5 losses: 202608181 retail **−$20,757** / 202608188 retail −$18,307 / 202608182 retail −$17,334 / 202608183 retail −$15,506 / 2026081811 retail −$13,392 (5/5 retail — purity restored, deepest single-day loss cluster since 08-14). Top-5 gains: 202608181 operator +$15,663 / 202608189 analyst +$11,272 / 202608182 operator +$10,805 / 202608188 analyst +$9,718 / 2026081811 analyst +$9,301 (0 retail in top-5, so single retail-gain seed 202608184 at +$2,606 slots at rank-6). Per-role: retail mean **−$10,506** (std $6,892, min −$20,757, max +$2,606) — deepens by $2,540 vs 08-16's −$7,966; operator mean +$4,644 (up from +$3,969); analyst mean +$4,336 (up from +$1,839). Extends [[compute-futures-spread-retail-loss-concentration]] from n=10 to **n=11 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18). Class direction (retail-losers, operator/analyst-winners) holds; purity slip observed 08-16 reverses. Under IQR fence, spread `role_pnl` at only **2.78% (1 row)** today because retail-loss mass shifts into the body of the distribution rather than the tail. Retail seed anchors on 202608181 (single most-negative row across all four modes today).

3. **[LOW — cross-mode float-dust sign regime SHIFTS, n=2 persistence class BROKEN] Conservation `wallet_sum_pnl` sign turns MIXED: basket negative, spread/synth/x402 positive.** `wallet_sum_pnl` mean per mode: basket **−1.15e−14** (vs 08-16 −6.66e−15; sign HOLDS negative, magnitude grows 1.7×), spread **+1.26e−12** (vs 08-16 −1.11e−12; **sign FLIPS − → +**, magnitude similar), synth **+4.44e−16** (vs 08-16 −3.37e−15; **sign FLIPS − → +**, magnitude decreases 8×), x402 **+4.44e−16** (identical to synth by [synth ≡ x402]). All four modes remain 12+ orders under the |mean|>10 / std>100 gates. Prior two-day all-negative persistence (08-15 + 08-16) breaks with 3-of-4 modes flipping positive; class returns to mixed-sign regime. Per-mode `wallet_sum_pnl` IQR outliers: basket 0% (unchanged), spread **25.00%** (up from 0%, all float dust below 1e-11), synth 16.67% (up from 8.33%), x402 16.67% (up from 8.33%, symmetric via [synth ≡ x402]). All float dust; excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

4. **[LOW — n=10 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, 08-12 zero, 08-13 zero, 08-14 zero, 08-15 zero, 08-16 zero, **08-18 zero**. Extends 08-16's n=8 to n=9 filed → **n=10** counting through the missing 08-17. Highest within-mode pairs today: spread `wallet_sum_pnl × settlementLegs` = **+0.6219** (float dust, excluded), basket `wallet_sum_pnl × seed` = +0.6321 (float dust), basket `maxSpot × wallet_sum_pnl` = −0.5210 (float dust), spread `realizedAbs × minSpot` = +0.4900 (structural sub-threshold), synth `minCurve × wallet_sum_pnl` = +0.4602 (float dust). x402 `settlementLegs × x402Total` today **+0.0361** — 25-filing series … +0.293 (08-15) → −0.112 (08-16) → **+0.036** (08-18) — sign flips again + → − → +, magnitude collapses to near-zero (7th sign-flip in 25 filings, effectively decorrelated today). x402 `realizedAbs × x402Total` today **−0.4048** (drops from 08-16's +0.7248, a 1.13-magnitude swing — largest single-filing swing observed for this pair). n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced.

5. **[LOW — asymmetric expansion, LOW drops below 08-15 floor] x402Total range EXPANDS to $264.75–$384.65 (range $119.90, up +$36.35 from 08-16's $83.55).** Range reverts to near 08-15's $121.89 after the 08-16 sharp narrowing. New LOW **$264.75** — drops −$30.12 from 08-16's $294.87 and touches **just $2.09 above 08-15's $262.66 floor** (essentially retesting the recent bottom). New HIGH **$384.65** — climbs +$6.23 from 08-16's $378.42 and **exceeds 08-15's $384.55 by $0.10**, so both boundaries now sit at fresh recent-extreme neighborhoods. Mean **$327.47** (essentially flat vs 08-16's $323.04, +$4.43). Std $40.50 (widens from 08-16's $26.03). Shape: **symmetric expansion in both directions** (LOW drops and HIGH climbs), reversing 08-16's asymmetric convergence. LOW anchor 2026081811 (3-role tie at $264.75); HIGH anchor 202608182 (3-role tie at $384.65). x402Total outlier_pct = 0.00% in x402 mode today (unchanged) — expansion still fits inside IQR fence; excluded from ranking regardless per SKILL bimodal rule.

6. **[LOW — anchor concentration RESETS to distributed multi-anchor pattern] No super-anchor today; 10 non-dust outlier positions spread across 5 seeds (max 3 per seed).** Anchor breakdown (non-dust, non-seed): **2026081812 (3 positions across basket/synth/x402 role_pnl LOW)**, **202608184 (2 positions, synth+x402 minCurve LOW)**, **202608188 (2 positions, synth+x402 role_pnl LOW)**, **2026081810 (2 positions, synth+x402 minCurve LOW)**, **202608181 (1 position, spread role_pnl LOW)**. Total 10 non-dust outlier positions. Class series: 08-13 super-single dense (13 pos) → 08-14 hyper-single (14 pos, quadruple-mode) → 08-15 five-anchor rotation (18+18+12+12+12) → 08-16 super-single dense (202608165, 18 pos, quadruple-mode) → **08-18 distributed multi-anchor (max 3 per seed, five seeds firing)**. Sharpest anchor-decentralization since 08-15 five-anchor rotation. Zero overlap with 08-16's super-anchor 202608165 (0 positions today) or 202608166 (0 positions).

7. **[LOW — over-index, sharpest single-day reversal in class] Padded-seed outlier positions surge to 50% (vs 25% row-share, 08-16's 3.2% under → 08-18's 50% over).** Padded seeds (2026081810/11/12) hold 36 of 144 rows (25%). Today they contribute **5 of 10 non-dust outlier positions (50%)**: 2026081812 (3 pos, basket+synth+x402 role_pnl LOW) and 2026081810 (2 pos, synth+x402 minCurve LOW). 2026081811 contributes 0 non-dust positions but does anchor x402Total's LOW at $264.75. Class series: 08-12 padded 42% over → 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% mildly over → 08-16 3.2% severely under → **08-18 50% severely over**. Class continues rotation-not-persistence with sharpest reversal in class direction; padded encoding does not systematically over- or under-select — the swing between 3.2% (08-16) and 50% (08-18) across a 2-day gap is a rotation, not a structural bias. Reinforces [[compute-futures-seed-padding-bug]] fix motivation (removing padding removes the surface entirely).

8. **[LOW — recurring, 31st filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608181`–`202608189`), 36 rows 10-char seed (`2026081810`–`2026081812`). `seed` outlier_pct = 25.00% in every mode. Today's most-anchored padded seed is 2026081812 (3 non-dust positions). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

9. **[LOW — cross-mode ≥10% MEDIUM count HOLDS at 0 for 3rd consecutive filed run] Zero cross-mode ≥10% MEDIUM non-dust outliers today (synth/x402 `minCurve` at 16.67% is a matched-symmetric pair via [synth ≡ x402], tracked as one anchor cluster on 202608184/2026081810, not a fresh cross-mode drift).** Cross-mode ≥10% MEDIUM count series: 0 (08-13) → 2 (08-14: spread minSpot + maxSpot) → 0 (08-15) → 0 (08-16) → **0 (08-18)**. Spread `minSpot`/`maxSpot` MEDIUMs from 08-14 remain a single-run appearance not persisting. Synth/x402 minCurve 16.67% today is expected — it's the six-row cluster of 202608184 (0.042) and 2026081810 (0.0425) undershooting the fence by the two lowest curve values; same rows appear in both modes by [synth ≡ x402], counted as one anchor group.

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

11. **[PASS] Row/column shape unchanged vs 08-16.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift beyond the basket/synth multiplier step (finding 1).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.15e−14  | 2.35e−14  | OK |
| spread    | +1.26e−12  | 2.93e−12  | OK |
| synthetic | +4.44e−16  | 6.44e−15  | OK |
| x402      | +4.44e−16  | 6.44e−15  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=10 consecutive filed runs.** Highest today: **spread `realizedAbs × minSpot` = +0.4900** (structural sub-threshold, spread-mode price-scale coupling), **x402 `realizedAbs × x402Total` = −0.4048** (LARGEST SINGLE-FILING SWING for this pair, drops from 08-16's +0.7248 = 1.13-magnitude swing). Excluding float-dust `wallet_sum_pnl` pairs and by-design pairs, no correlation approaches the threshold. n=12 seeds decisively insufficient.
- **Retail-role loss concentration in spread — n=11 consecutive filed runs, DEEPENS SHARPLY, PURITY RESTORED.** Retail mean **−$10,506** (deepens $2,540 = 32% vs 08-16's −$7,966); top-5 losses 5/5 retail (up from 4/5), top-5 gains 0 retail (down from 1 retail) — 08-16's cross-role mixing reverses fully. Single retail-gain seed 202608184 slots at rank-6 in gains, not top-5. Class direction and purity both hold; magnitude in the deep-oscillation zone (below the recent −$5,984 → −$7,966 shallow floor).
- **basket/synth scale-ratio BREAKS from 3.0250 to 2.5000 at deterministic step** (finding 1). Byte-identical constant on both sides. n=7 promoted class ends. Fresh scale-factor anchor 2.5000 begins at n=1 as of 08-18. Requires deployer-side audit — either intentional config change or unintentional drift.
- **Conservation float-dust sign-regime returns to MIXED** — spread/synth/x402 flip from negative to positive while basket stays negative; two-day all-negative persistence class (08-15 + 08-16) broken. Still all float dust, no threshold breach.
- **x402Total range expands symmetrically** — from $83.55 to $119.90 (+$36.35). LOW retests near 08-15's $262.66 floor (only $2.09 above); HIGH sets a fresh recent high at $384.65 (edges past 08-15's $384.55 by $0.10). Both boundaries at recent extremes → mean-reverting oscillator not converging.
- **Anchor concentration RESETS to distributed multi-anchor** — no super-anchor today; 10 non-dust outlier positions spread across 5 seeds (max 3 per seed via 2026081812). Zero overlap with 08-16's super-anchor 202608165.
- **Padded-seed contribution SURGES to 50% over-index** — sharpest single-day reversal in class (08-16 3.2% under → 08-18 50% over). Continues rotation-not-persistence pattern in padded-vs-unpadded outlier distribution.
- **Cross-mode ≥10% MEDIUM count HOLDS at 0 for 3rd consecutive filed run** — synth/x402 minCurve 16.67% is the matched-symmetric [synth ≡ x402] pair (same rows), not a fresh drift.

## Row count and shape vs 2026-08-16

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Basket/synth multiplier constant SHIFTS from 3.0250 to 2.5000** (finding 1) — not a schema change but a numeric constant that had been deterministic across n=7 filings. Byte-identical on both sides of the step. If unintentional, this is a deployer regression to flag; if intentional, worth confirming with the deployer owner.
- **Outlier surface SHIFTS from 08-16 → 08-18.** 08-16: 0 HIGH, 1 MEDIUM (retail-loss n=10). 08-18: 0 HIGH, **2 MEDIUM** (retail-loss n=11 deepens, basket/synth multiplier break). Two-day gap between reports because no 2026-08-17 CSV was emitted.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's synth+x402 minCurve LOW cluster (202608184/2026081810) and synth+x402 role_pnl LOW cluster (202608188/2026081812) are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (multiplier STEPPED from 3.0250 to 2.5000 today)** — per-seed rank order still identical; multiplier remains uniform across all seeds/roles but the constant value changed. See finding 1. Promoted class [[compute-futures-basket-synth-3025x-multiplier]] invalidated at n=7; fresh 2.5000-anchor class begins at n=1.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers at 0%/25%/16.67%/16.67% today (spread jumps from 0% → 25%; synth+x402 symmetric via [synth ≡ x402]) — all float dust.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 31st consecutive filed run. Today's most-anchored padded seed is 2026081812 (3 non-dust positions). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** Cross-mode ≥10% MEDIUM count holds at 0 for 3rd day; x402 `settlementLegs × x402Total` sign-flips 7th time (magnitude collapses to +0.036 near-zero); the retail-loss promoted class carries forward and deepens; the basket/synth multiplier class breaks. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration migrates to distributed multi-anchor** — series: 08-13 super-single (13 pos) → 08-14 hyper-single (14 pos, quadruple-mode) → 08-15 five-anchor rotation (18+18+12+12+12) → 08-16 super-single dense (202608165, 18 pos, quadruple-mode) → **08-18 distributed multi-anchor (max 3 per seed, five seeds firing 10 total non-dust positions)**. Anchors fully rotate; zero overlap with 08-16's anchors.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=11 (08-18).** Retail mean deepens back into oscillation-mid at −$10,506 across 08-12 → 08-13 → 08-14 → 08-15 → 08-16 → 08-18 (−$5,984 → −$9,473 → −$11,092 → −$7,793 → −$7,966 → **−$10,506**). Purity restored 5/5 losses + 0/5 gains after 08-16's cross-role mixing.
- **x402Total range oscillates asymmetrically** — 08-13 $263.71–$373.84 → 08-14 $287.28–$398.93 → 08-15 $262.66–$384.55 → 08-16 $294.87–$378.42 (narrows) → **08-18 $264.75–$384.65 (expands, both bounds at recent extremes)**. LOW retests 08-15 floor within $2.09; HIGH edges past 08-15 ceiling by $0.10.
- **Source gap 2026-08-17** — no CSV emitted for 2026-08-17 (present dates: 08-13/14/15/16, then 08-18). Two-day analysis gap; findings reference 08-16 as prior. If the deployer was expected to emit daily, worth cross-checking whether 08-17 was a run-skip or a proof-write failure.
