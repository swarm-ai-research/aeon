# Compute-Futures EDA — 2026-07-27

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-27.csv` (144 rows, 4 modes)
**Run:** 2026-07-28
**Prior filed report:** 2026-07-25 (2-day gap; the 2026-07-26 CSV exists in `fleet-state` but was not analyzed — this run does not backfill it).

## Top findings

1. **[MEDIUM — recurring, mode migration] Spread `settlementLegs` outlier_pct 16.67% (fresh; 07-25 was 0.00%).** 6/36 rows outside IQR whiskers today. Distribution {76:3, 77:3, 79:3, 80:3, 81:15, 82:3, 83:6}; mass concentrated at 81, tails 76×3 low and 83×6 high. This is only the **second-ever** filed spread `settlementLegs` fire in the record (first was 07-12 at 16.67%, which collapsed to 0.00% next run). Same rotation shape as (basket, settlementLegs) — see finding 2 for the paired collapse. Escalation gate: hold ≥ 10% on next run → persistence-candidate; collapse → jitter noise per [[compute-futures-12-seed-sample-too-small]]. Next step: watch 07-28 CSV for spread settlementLegs stability.
2. **[MEDIUM — collapse of prior HIGH] Spread `maxSpot` 25.00% → 0.00% in one rotation.** 07-25's first-ever ≥ 20% (mode, column) fire on `maxSpot` fully collapses today. Seed 202607255 — yesterday's anchor at $74.95 with a $5 gap to the next-highest bin — now sits at $64.74 mid-distribution. Bulk range today [$61.87, $71.93] across 12 seeds; no gaps > $3.63; no rows tag IQR whiskers. Escalation gate from 07-25 ("hold ≥ 10% on 07-26 → persistence-candidate") FAILS at first re-check. Reclassify as one-run jitter noise per [[compute-futures-12-seed-sample-too-small]]. **Seventh single-run HIGH-or-MEDIUM (mode, column) in the filed record that collapsed within one rotation** — every prior first-appearance HIGH-or-MEDIUM has collapsed at the first re-check (07-10 basket settlementLegs, 07-12 spread settlementLegs, 07-17 basket settlementLegs, 07-21 basket settlementLegs, 07-24 basket realizedAbs, 07-24 spread minSpot promotion, today spread maxSpot).
3. **[MEDIUM — collapse of prior MEDIUM] Basket `settlementLegs` 16.67% → 0.00%.** 07-25's recurring MEDIUM does not survive today's rotation. Distribution today {77:9, 78:6, 79:9, 80:3, 81:3, 82:3, 83:3}: wider low-side mass and no whisker tags. On/off pattern for (basket, settlementLegs) continues — rotation series 07-09 → 8.33 → 25.00 (07-10) → 0.00 (07-12) → 16.67 (07-16) → 0.00 (07-17) → 16.67 (07-20) → 16.67 (07-21) → 0.00 (07-24) → 16.67 (07-25) → 0.00 (today). Pattern remains a stable on/off with 16.67% as the on-state; the fire has now migrated to (spread, settlementLegs) — see finding 1.
4. **[LOW — sign-flip streak broken at three] x402 `settlementLegs × x402Total` = −0.394 today.** Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → +0.357 (07-25) → **−0.394** (today). Third-consecutive-positive streak broken exactly per the 07-25 escalation gate ("if tomorrow flips negative, restore the pre-07-24 jitter classification with a two-run positive interlude"). Restores jitter classification. Companion pair `minSpot × x402Total` today +0.030 (was +0.745 near-miss on 07-25 — also collapsed). Alternation resumes.
5. **[LOW — near-miss collapse] Basket `realizedAbs × settlementLegs` = +0.195 today (07-25 fresh near-miss at +0.776).** Escalation gate from 07-25 finding 5 ("if it holds ≥ 0.7 next run AND settlementLegs re-fires, promote to within-mode structural signal") FAILS at first re-check: neither condition holds (r dropped 0.58 in one rotation; basket settlementLegs outlier_pct collapsed to 0.00%). Reclassify as one-run near-miss noise. Reinforces the rotation-not-persistence conclusion.
6. **[LOW — recurring] role_pnl σ widened in all three mode groups.** Basket σ = $37.71 (up from $31.60 on 07-25); spread σ = $11,007.54 (up from $7,103.91 on 07-25, **+55%**); synth/x402 σ = $12.24 (up from $9.58). First filing since 07-24 where all three widened together. Spread σ back above the pre-07-24 $9k–$10.5k band; 07-24's sharp narrowing to $7k does not hold. Spread role_pnl range today [−$28,504, +$16,398]; basket [−$86.93, +$74.91]; synth/x402 [−$29.74, +$26.39]. μ per mode all inside the historical envelope.
7. **[LOW — recurring] x402Total right-shifted band.** Mean $339.68 (up $17.13 vs 07-25's $322.55; largest single-day mean move since 07-16 to 07-17). Range [$308.11, $371.66] vs 07-25's [$288.14, $366.46] — low bound lifted +$20, high bound +$5. Sum $12,228.57 across 36 x402 rows (up from $11,611.86). Sixteenth consecutive run within the [$268, $389] envelope; today is a right-shift with narrower low bound.
8. **[LOW — recurring, 10th filing]** No mode fires |r| ≥ 0.8. Strongest within-mode pairs today: basket `seed × maxCurve` −0.561, `seed × maxSpot` +0.534; spread `seed × minSpot` −0.578, `realizedAbs × maxCurve` +0.521, `seed × minCurve` +0.519; synth/x402 `seed × maxCurve` −0.556, `seed × maxSpot` +0.534; x402-only `realizedAbs × x402Total` +0.492 (strongest x402 non-seed pair today), `maxSpot × x402Total` +0.409, `settlementLegs × x402Total` −0.394 (finding 4).
9. **[LOW — recurring, 16th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char (`202607271`–`202607279`), 36 rows 10-char (`2026072710`–`2026072712`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +2.03e−14 (basket), +1.45e−12 (spread), +2.19e−15 (synth/x402). Std 2.20e−14 (basket), 1.03e−11 (spread), 9.28e−15 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Basket mean sign-flipped positive vs 07-25 (−1.47e−14 → +2.03e−14) — float-dust noise at |μ| ~2–3e−14 across 36 rows, not a signal. Spread `wallet_sum_pnl` outlier_pct 8.33% (3/36 rows), excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
11. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-25.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402; `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.
13. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +2.03e−14  | 2.20e−14  | OK |
| spread    | +1.45e−12  | 1.03e−11  | OK |
| synthetic | +2.19e−15  | 9.28e−15  | OK |
| x402      | +2.19e−15  | 9.28e−15  | OK |

## Within-mode signal

- **Spread `settlementLegs` 16.67% MEDIUM (fresh, second-ever fire).** Distribution {76:3, 77:3, 79:3, 80:3, 81:15, 82:3, 83:6}, tags 6 whisker rows (76×3 low, 83×6 high). First fire since 07-12; 07-12's 16.67% collapsed to 0.00% at next re-check, so the base-rate expectation is a same-collapse on 07-28. Watch alongside (basket, settlementLegs), which continues its on/off rotation on the opposite phase this filing.
- **Spread `maxSpot` HIGH collapsed — persistence-candidate class remains empty.** Yesterday's 25.00% flag anchored by seed 202607255 (yesterday $74.95 far-high) does not survive today; that seed now sits at $64.74 mid-distribution. Full bulk range today [$61.87, $71.93] with no gaps > $3.63. Third attempted promotion in the record after 07-24's basket `realizedAbs` HIGH and 07-24's spread `minSpot` three-run persistence-candidate — all three collapsed on next re-check. Reinforces the 12-seed rotation-not-persistence rule per [[compute-futures-12-seed-sample-too-small]].
- **Basket `settlementLegs` on/off rotation continues.** Today's 0.00% (fifth zero in the last eleven filings) matches the on/off shape — never accumulates a persistent fire. Every 16.67% fire in this pair collapses on next re-check; the persistence gate has never held here.
- **Correlation surface — no strong pairs, none of yesterday's near-misses survive.** Basket `realizedAbs × settlementLegs` +0.776 → +0.195 (finding 5). x402 `minSpot × x402Total` +0.745 → +0.030. x402 `settlementLegs × x402Total` +0.357 → −0.394 (finding 4). Strongest today: spread `seed × minSpot` = −0.578 (regime with `seed`-padding artifact still bleeding through); basket `seed × maxCurve` = −0.561; synth/x402 `seed × maxCurve` = −0.556. First filing with **zero** ≥ 0.7 non-`seed` within-mode pairs since 07-24.
- **role_pnl σ triple-widening.** basket +19% ($31.60 → $37.71); spread +55% ($7,104 → $11,007 — largest single-day spread σ move in the recent record); synth/x402 +28% ($9.58 → $12.24). First all-three-widening filing since 07-24-vs-07-21. Spread returns to its pre-07-24 wide band; the 07-24 narrowing to $7k did not hold across two rotations. μ per mode all inside historical envelope (basket −$0.60, spread −$519.15, synth/x402 −$0.50).

## Row count and shape vs 2026-07-25

- Rows: 144 → 144 (no change; 2-day gap over an unfiled 07-26 CSV).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated fully.** 07-25 fired 1 HIGH (spread `maxSpot` 25.00%) + 1 MEDIUM (basket `settlementLegs` 16.67%). Both collapsed to 0.00% today. New today: 1 MEDIUM (spread `settlementLegs` 16.67%). Zero column-mode pairs survive at ≥ 10% across 07-25 → today — third consecutive filing with 0/0 carryover across the ≥ 10% band. The (mode, settlementLegs) fire migrated from basket to spread — same-column, opposite-mode rotation on a single re-check.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; 10th consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under the threshold everywhere. Basket spread `wallet_sum_pnl` outlier_pct 8.33% each excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 16th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today. Third attempted promotion in the record collapsed on first re-check (spread `maxSpot` HIGH). At 12 seeds, NO count of same-magnitude fires reliably crosses out of rotation-jitter — persistence-candidate class remains empty. Seed-count widening per [[compute-futures-12-seed-sample-too-small]] is the only unblock.
- **x402 `settlementLegs × x402Total` sign-flip streak** — 3-consecutive-positive streak (07-24/07-25/…) broken today at −0.394. Restores the pre-07-24 alternation series. Same-sign persistence tomorrow at any magnitude would restart the streak; alternation continuation would confirm jitter.
- **Spread role_pnl σ shape** — 07-24's sharp narrowing to $7,015 does NOT hold; today back to $11,008, above the pre-07-24 $9k–$10.5k band. Reclassify the 07-24 narrowing as a one-run dip rather than a regime shift.
- **Basket `realizedAbs × settlementLegs` = +0.776 near-miss** — collapsed to +0.195 in one rotation; promotion criterion fails at first re-check. Same alarm-without-signal shape as every prior single-run near-miss.
