# Compute-Futures EDA — 2026-08-24

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-24.csv` (144 rows, 4 modes)
**Run:** 2026-08-25
**Prior filed report:** 2026-08-22 (**2-day gap** — the 2026-08-23 CSV exists in the proof dir but was never analyzed; this filing compares to 08-22, not 08-23).

## Top findings

1. **[HIGH — first HIGH tier in 4 filings; cross-mode ≥10% cluster count 0 → 3, sharpest single-day re-inflation of the class series] `settlementLegs` outlier cluster surfaces at **33.33% (12/36) in synth AND x402** (mode-mirror pair) — the sole HIGH cluster today.** synth/x402 settlementLegs distribution collapses onto value **81 with 18/36 rows (50%)**, driving Q1=80.75, Q3=81.25, IQR=0.5 → IQR bounds [80, 82] — every row outside {80, 81, 82} counts as an outlier (12 rows at 78/79/83). basket and spread settlementLegs remain diffuse (IQR 2.5 / 4.25, 0% outliers). No prior filing in the daily-cadence series has ever reported a settlementLegs outlier cluster — this is a **fresh distributional-shape appearance**, class-first. Not a P&L-relevant column, but the concentration is genuine and stark (50% of synth+x402 rows share one leg count). Cross-mode ≥10% cluster series: 3 (08-19) → 3 (08-20) → **5 (08-21, peak)** → 0 (08-22) → **3 (08-24)** — full recovery from the 08-22 trough. Class jump from *3-consecutive-days no HIGH tier* (08-19 → 08-22 daily-cadence, excluding 08-23 unanalyzed) to a HIGH tier back. Next step: probe why synth/x402 settlementLegs collapses to 81 today — likely a stochastic seed alignment on the sweep RNG rather than a config change (basket + spread still diffuse under the same seed set).

2. **[MEDIUM — extension, n=16 consecutive filed runs; retail mean DEEPENS 17% vs 08-22] Spread `role_pnl` retail mean **−$12,150** (16.6% deeper than 08-22's −$10,424), 12/12 retail rows losing (**second consecutive all-losing retail day**).** Sum-of-retail spread P&L: **−$145,796** across 12 seeds. Top-5 losses: **seed 202608245 retail −$24,360** (new interim near-peak, $258 shallower than 08-22's class-record 202608228 retail −$24,618) / **202608242 retail −$21,507** (2nd deepest today, would be the 2nd class-series record if 08-22 wasn't a filing) / 202608243 retail −$20,420 / 202608247 retail −$17,230 / 2026082411 retail −$15,677 (**5/5 retail — 6th consecutive filed run at 5/5 top-5 purity**). Top-5 gains: 202608243 analyst +$12,805 / 202608242 operator +$12,454 / 202608245 analyst +$11,812 / 202608245 operator +$9,880 / 202608247 operator +$8,763 (0 retail in top-5 gains — 16-run pattern holds). Per-role means: retail **−$12,150** (std $7,590, min −$24,360, max **−$3,169** — no non-losing retail row, matching 08-22); operator **+$6,214** (down $102 from +$6,316, effectively unchanged); analyst **+$4,553** (up 52% from +$2,989, largest role-mean shift today). Extends [[compute-futures-spread-retail-loss-concentration]] from n=15 to **n=16 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18 → 08-22, 08-24). Notable: **seed 202608245 triple-role top-5 anchor** (retail loss + operator gain + analyst gain) — second consecutive filing with a triple-role top-5 seed after 08-22's 202608228; within-seed dispersion **$36,171** (retail −$24,360 → analyst +$11,812), second-highest in class series (just $1,321 below 08-22's 202608228 record $37,492). **Four seeds** (202608245, 202608242, 202608243, 202608247) each anchor both a top-loss (retail) AND a top-gain (operator/analyst) — 4-way intra-seed skew unchanged vs 08-22.

3. **[MEDIUM — extension, n=6 filed runs post-rename] basket/synth spot-price multiplier byte-identical at **2.5000 exact** for **6th consecutive filed run** — reinforces [[compute-futures-basket-synth-2.5x-multiplier]] promoted to full class anchor 08-22.** `minSpot` per-seed ratio **[2.500000, 2.500000]** std 1.42e-15; `maxSpot` per-seed ratio **[2.500000, 2.500000]** std 1.03e-15 (12/12 seed pairs both columns, byte-identical, essentially unchanged vs 08-22 std bounds). Curve ratios TIGHTEN further: `maxCurve` mean 2.4991 (std **5.48e-03**, effectively unchanged from 08-22's 5.23e-03), `minCurve` mean 2.4872 (std **2.37e-02**, roughly 2× LOOSER than 08-22's 1.11e-02 — the only curve-ratio widening today). `realizedAbs` mean **2.5447** (std **1.01e-01**, ~20% LOOSER than 08-22's 1.26e-01, and mean drifts back up from 2.5001 to 2.5447 — closest to the invalidated 3.025× regime's tail). Class series post-rename: n=1 (08-18) → n=2 (08-19) → n=3 (08-20) → n=4 (08-21) → **n=5 (08-22, rename threshold met)** → **n=6 (08-24)**. Multiplier holds; only the `realizedAbs` per-seed ratio distribution widens today.

4. **[MEDIUM — first non-mirror MEDIUM tier since 08-21; basket-only cluster] basket `minCurve` outlier cluster at **16.67% (6/36)** — sole MEDIUM cluster, first non-mirror-pair MEDIUM in 3 filings.** basket minCurve values 0.1055 (n=3 at seed 202608246) + 0.1145 (n=3 at seed 202608247) fall outside IQR bounds [0.1097, 0.1148]. Distribution modally concentrated at 0.1105–0.1115 (24/36 rows); the two 3-row tails at both extremes count as outliers. Extremely modest deviation ($0.0050 spread from IQR bound), but crosses the 10% threshold. This is the *first* MEDIUM cluster on `basket/minCurve` in the daily-cadence class series; 08-22 flagged basket/minSpot at 8.33% sub-MEDIUM, today `basket/minSpot` is 0% outliers.

5. **[LOW — n=15 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-22's n=14 to **n=15**. Highest today ex-seed-encoding: **basket `maxSpot × realizedAbs` = −0.665** (was −0.617 in synth 08-22 approximate class; regime pair); **synth/x402 `maxSpot × realizedAbs` = −0.617** (structural mid-strength repeatedly); **spread `seed × minCurve` = +0.544** (seed-encoding artifact class per [[compute-futures-seed-padding-bug]]); **basket `seed × maxCurve` = −0.494**; **basket `minSpot × wallet_sum_pnl` = +0.530** (float-dust dependent side, excluded from structural interpretation per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]). n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation continues to reinforce.

6. **[LOW — sign flip returns positive; series holds oscillation band] x402 `settlementLegs × x402Total` FLIPS back to **+0.458** (vs −0.060 on 08-22, +0.542 on 08-21).** Series: −0.112 (08-16) → +0.036 (08-18) → +0.480 (08-19) → +0.311 (08-20) → +0.542 (08-21) → **−0.060 (08-22)** → **+0.458 (08-24)**. Same-sign hold breaks last filing, resumes this filing. Magnitude 0.458 is near the class median. Other x402 × x402Total pairs today: `realizedAbs × x402Total` **+0.330** (was −0.558 on 08-22 — full sign flip, magnitude 0.89 swing), `wallet_sum_pnl × x402Total` **+0.373** (float-dust dependent side, excluded), `maxSpot × x402Total` **−0.169** (was −0.679, magnitude collapse), `minCurve × x402Total` **+0.206** (was small, now mid-strength). Correlation pattern reshuffles heavily vs 08-22 — consistent with the settlementLegs shape change reshaping downstream partial correlations.

7. **[LOW — series HIGH extends to fresh series peak, floor at second-highest] x402Total range widens to **$293.29–$438.73** (range **$145.43**, up $22.70 from 08-22's $122.73 — 3rd consecutive expansion; matches 08-19's series peak of $131.66 within $14 and now surpasses it — new **series-peak range**).** HIGH **$438.73** — climbs **$42.80** above 08-22's peak $395.93 (new all-time series HIGH, largest single-day HIGH advance in the 6-filing series). LOW **$293.29** — rises **$20.09** above 08-22's floor $273.20 (LOW retracts from 08-22's fresh-interim-low; still $36.91 above 08-19's all-time-low $256.38). Mean **$331.95** (down $14.04 from $345.99 — mean retracts despite peak advance; HIGH is a single-seed spike). Std **$45.56** (up 19% from $38.20). Series range: $83.55 (08-16) → $119.90 (08-18) → $131.66 (08-19, prior peak) → $88.05 (08-20) → $103.60 (08-21) → $122.73 (08-22) → **$145.43 (08-24, new peak)**. HIGH anchor **seed 202608241** (single seed, all 3 roles clustered ~$405–439); LOW anchor **seed 202608244** (single seed, all 3 roles ~$293–302). x402Total outlier_pct = 8.33% today (3/36 in x402 mode) — surfaces the HIGH-anchor seed 202608241 triple as an outlier cluster; **excluded from severity ranking per SKILL constraint** (x402Total is bimodal by design within x402).

8. **[LOW — anchor magnitude COLLAPSES from single 6 → dual 3, no seed holds ≥4 positions] Top seeds by non-dust outlier positions are **202608244 and 202608246 tied at 3 positions each** (dual anchor, both drop further from 08-22's single-anchor 6).** 202608244 anchors: basket role_pnl −$108 (deepest role_pnl loss today, single outlier), synth/x402 role_pnl −$42, spread minCurve tail. 202608246 anchors: basket minCurve tail, synth/x402 minCurve tail (both feeding finding 4), synth/x402 settlementLegs tail (feeding finding 1). 08-22's single 6-anchor **202608229 collapses to 0 non-dust positions**; 08-22's second-tier 202608228 (4 positions, triple-role spread anchor) **also collapses to 0**. Class series: 08-13 single 13 → 08-14 single 14 → 08-15 5-anchor rotation → 08-16 single 18 → 08-18 distributed max-3 → 08-19 single 19 → 08-20 single 22 (all-time peak) → 08-21 DUAL 18/18 → **08-22 single 6 (prior series trough)** → **08-24 DUAL 3/3 (new series trough)**. Anchor magnitude has now trended DOWN 4 consecutive filings (22 → 18 → 6 → 3) — sustained "quiet ebb" regime; anchor identity does NOT persist between filings (fifth consecutive full-collapse of prior day's super-anchor).

9. **[LOW — padded seeds fully absent from non-dust positions, most extreme swing of the class series] Padded seeds (2026082410/11/12) hold 36 of 144 rows (25%). Today they contribute **0 of 6 non-dust outlier positions (0%)** — most extreme under-index in the class series to date.** Even 2026082411 (which appears in the spread top-5 losses at −$15,677) is a P&L row, not a distributional-outlier position — it doesn't survive the mode-mirror + dust exclusion filter. Class series: 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% mildly over → 08-16 3.2% severely under → 08-18 50% severely over → 08-19 1.9% severely under → 08-20 21.1% mildly under → 08-21 28.6% mildly over → 08-22 9.1% severely under → **08-24 0% total absence** (new class-series extreme). Sixth consecutive day the swing direction reverses (over → under → proportional → mildly over → severely under → total absence). Continues rotation-not-persistence pattern; padded encoding does not systematically over- or under-select. Reinforces [[compute-futures-seed-padding-bug]] fix motivation (removing padding removes the surface entirely).

10. **[LOW — conservation regime returns to mixed after 08-22 all-negative] Per-mode `wallet_sum_pnl` means all near-zero float dust: basket **+3.91e−15**, spread **+1.56e−12**, synth **−1.22e−15**, x402 **−1.22e−15**.** Sign pattern: basket + spread FLIP + (vs 08-22 both negative); synth + x402 pair HOLDS negative. All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). Per-mode `wallet_sum_pnl` IQR outlier percentages: basket 0%, spread **8.33%** (3/36 — first non-zero for spread after basket held it 08-22), synth 0%, x402 0%. All float dust; the spread 8.33% outlier cluster is float-magnitude only (magnitude ~1e−11 vs spread median ~1e−13); excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Rotation continues: which mode carries the wallet_sum_pnl 8.33% shifts between filings.

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: 0.000000e+00 (byte-identical).

12. **[PASS] Row/column shape unchanged vs 08-22.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +3.91e−15  | 2.80e−14  | OK |
| spread    | +1.56e−12  | 6.03e−12  | OK |
| synthetic | −1.22e−15  | 1.56e−14  | OK |
| x402      | −1.22e−15  | 1.56e−14  | OK |

All four modes 11+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: basket + spread FLIP + (vs 08-22 both negative); synth + x402 pair HOLDS negative — mixed-sign regime returns after 08-22's all-negative day.

## Within-mode signal

- **HIGH — synth/x402 `settlementLegs` distributional collapse (finding 1)**. 18/36 rows at value 81 drives 33.33% IQR-outlier cluster in each mirror mode; class-first appearance of a settlementLegs cluster. Not P&L-relevant, but a genuine sweep-side shape event worth watching next filing for persistence vs one-off stochastic seed alignment.
- **Retail-role loss concentration in spread — n=16 consecutive filed runs, retail mean DEEPENS 17% to −$12,150** (finding 2). Class direction and 6th consecutive 5/5 top-5 retail purity hold; **second consecutive all-losing retail day** (max retail −$3,169). Seed 202608245 achieves triple-role top-5 anchor with $36,171 within-seed dispersion (second-highest in class series).
- **basket/synth 2.5000× multiplier holds at n=6 post-rename** (finding 3). Spot columns byte-identical; curve columns still very tight; `realizedAbs` per-seed ratio widens ~20% vs 08-22 (only widening today).
- **basket `minCurve` MEDIUM cluster at 16.67%** (finding 4) — first-ever MEDIUM tier for basket/minCurve; small-magnitude concentration at the modal tails.
- **Zero within-mode strong pairs (|r|≥0.8) — n=15 consecutive filed runs** (finding 5). n=12 seeds decisively insufficient. Highest ex-seed-encoding: basket `maxSpot × realizedAbs` = −0.665; the x402 `settlementLegs × x402Total` pair FLIPS back positive at +0.458 after 08-22's break (finding 6), and x402 `realizedAbs × x402Total` swings +0.89 in magnitude (finding 6).
- **x402Total range EXPANDS to new series peak of $145.43** (finding 7) — HIGH boundary alone advances $42.80 to $438.73 (new all-time series HIGH); LOW retracts $20 from 08-22's fresh-interim-low. 3-consecutive-day expansion.
- **Super-anchor collapses again to DUAL 3-position tie (202608244 + 202608246)** (finding 8) — new series trough; 4-consecutive-day downtrend in anchor magnitude (22 → 18 → 6 → 3). "Quiet ebb" regime continues.
- **Padded-seed contribution 0% — most extreme under-index of the class series** (finding 9) — 6th consecutive filing with reversed swing direction, first *total absence* filing.
- **Conservation regime returns to mixed** (finding 10) — basket/spread flip + after 08-22's all-negative; synth/x402 pair holds −. Spread `wallet_sum_pnl` picks up the rotating 8.33% outlier cluster (basket held it 08-22). All float dust.

## Carry-overs

- 25% seed encoding artifact (n=36 filed runs, pending [[compute-futures-seed-padding-bug]] fix).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers there excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.
- 2-day comparison gap: 2026-08-23 CSV exists in the proof dir but was never analyzed — this filing's comparisons are all vs 2026-08-22.
