# Compute-Futures EDA — 2026-07-09

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-09.csv` (144 rows, 4 modes)
**Run:** 2026-07-10

## Top findings

1. **[MEDIUM] spread `realizedAbs` outlier_pct 16.67% (was 0.00% yesterday).** Six of 36 spread rows land outside the IQR whiskers: min $35,359, max $69,625, mean $47,895, std $10,977. Same 12-seed alarm-without-signal shape as prior runs (the fired-column rotates each day: 06-26 spread minCurve 25% → 06-28 spread settlementLegs 33% → 07-05 basket wallet_sum_pnl 16.67% → 07-08 synth/x402 wallet_sum_pnl/realizedAbs/maxSpot/settlementLegs all 8.33% → today's spread realizedAbs 16.67%). Underlying spread role_pnl std $9,545 (was $8,209 on 07-08, $6,832 on 07-07) — real magnitude still within regime jitter. Watch: if spread realizedAbs > 10% recurs tomorrow, escalate to signal candidate. [[compute-futures-12-seed-sample-too-small]]
2. **[LOW] x402 `wallet_sum_pnl × x402Total` r = +0.881 — second consecutive day with a wallet_sum_pnl float-dust correlation crossing gate.** σ(wallet_sum_pnl) in x402 = 1.21e−14 (mean 5.55e−17). Yesterday's crossing was synthetic/x402 `wallet_sum_pnl × settlementLegs` = −0.874; today that pair is +0.225 (collapsed within one run), replaced by `wallet_sum_pnl × x402Total`. Both are the numerical-drift phenomenon logged in [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — larger throughput (more legs, higher x402Total) accumulates more signed rounding error in a consistent direction. Not a strategy P&L signal — conservation gate (|mean|>10, std>100) holds by twelve orders of magnitude. Per the durable claim, filter `wallet_sum_pnl` correlations from the reportable set until σ(wallet_sum_pnl) > 1e−6.
3. **[LOW — collapse of yesterday's leader] synthetic/x402 `wallet_sum_pnl × settlementLegs` −0.874 → +0.225.** Same 12-seed churn shape that has repeatedly promoted and demoted this class of float-dust pair. Reconfirms the "wallet_sum_pnl correlations are float-dust" claim.
4. **[LOW — cluster resolution] Yesterday's four synth/x402 8.33% outlier columns (wallet_sum_pnl, realizedAbs, maxSpot, settlementLegs) all dropped to 0.00%.** Basket `realizedAbs` 8.33% → 0.00%. Today's fired columns are different (minSpot/maxSpot/maxCurve at 8.33% in most modes). Consistent with the "12-seed outlier_pct instability" carry-over.
5. **[LOW — recurring] x402Total range $282.51–$386.87, mean $315.68, total $11,364.61.** Fifth consecutive movement inside the ~$100 band (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400 → today $283–$387). Mean drifts within ±$25 of $335 across five runs; today the lowest of the five (μ $315.68). `x402Total × settlementLegs` r = +0.143 (was −0.237 on 07-08). Structural link continues to churn sub-threshold. Not trending.
6. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows carry 9-digit seeds (202607091–9), 36 rows carry 10-digit (`2026070910`, `2026070911`, `2026070912`). `seed` outlier_pct = 25% in every mode, thirteenth consecutive run. One-line pad in `scenario-sweep.mjs` still pending. [[compute-futures-seed-padding-bug]]
7. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.58e−14 (basket) → +3.32e−13 (spread); std 1.21e−14 (synth/x402) → 6.41e−12 (spread). All twelve-plus orders of magnitude under the |mean|>10 / std>100 gates. spread std 6.41e−12 (was 6.15e−12 on 07-08; essentially identical).
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff| = 0.0 across role_pnl, wallet_sum_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
9. **[PASS] No CRITICAL/HIGH findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift, no cross-mode column addition.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.58e−14  | 1.77e−14  | OK |
| spread    | +3.32e−13  | 6.41e−12  | OK |
| synthetic | +5.55e−17  | 1.21e−14  | OK |
| x402      | +5.55e−17  | 1.21e−14  | OK |

## Within-mode signal

- **spread `realizedAbs` MEDIUM outlier cluster (16.67%).** Six of 36 spread rows fall outside IQR whiskers; underlying spread role_pnl σ = $9,545 today vs $8,209 (07-08) vs $6,832 (07-07). Real widening at the day-scale, but the 12-seed jitter carry-over says a single day of >10% outlier_pct in a new (mode, column) is alarm-without-signal until it repeats. Log for tomorrow.
- **wallet_sum_pnl × x402Total r = +0.881 in x402.** Float-dust correlation on σ≈1.2e−14 signal — the second consecutive day a wallet_sum_pnl pair crosses gate at |r| ≥ 0.8, with the specific counterpart rotating (settlementLegs 07-08 → x402Total 07-09). Not a strategy claim.
- **role_pnl scale per mode (regime unchanged):** spread σ = $9,545 (μ = −$740), basket σ = $27.3 (μ = −$0.82), synthetic/x402 σ = $10.6 (μ = −$0.03). Spread widened again vs 07-08 ($8,209) but stays well inside 12-seed jitter and continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold:** `minSpot × x402Total` −0.631 (was −0.568 on 07-08), `maxSpot × x402Total` −0.452 (was +0.408), `minCurve × x402Total` +0.069 (was +0.425), `maxCurve × x402Total` +0.213 (was ~+0.4). Still jittering; no candidate strengthens two days in a row.

## Row count and shape vs 2026-07-08

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column set diff).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold. `live` is True in basket/spread, False in synthetic/x402; `settlement` is `cash` everywhere except x402 (`x402`); `spotSource` is `darkbloom-basket`/`darkbloom-spread`/`synthetic`/`synthetic` per mode.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — the |r| ≥ 0.8 signal on wallet_sum_pnl is a numerical-drift artifact per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Two consecutive days now (07-08 settlementLegs, 07-09 x402Total). Filter until σ(wallet_sum_pnl) > 1e−6.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string. One-line pad in `scenario-sweep.mjs` still pending.
- **12-seed outlier_pct instability** — the (mode, column) pair that fires shifts each run: 06-26 spread minCurve 25% → 06-28 spread settlementLegs 33% → 07-03 synth/x402 settlementLegs 16.67% → 07-05 basket wallet_sum_pnl 16.67% + synth/x402 minCurve 16.67% → 07-07 basket maxCurve 16.67% + synth/x402 maxCurve 16.67% → 07-08 (all flags stayed at 8.33%) → 07-09 spread realizedAbs 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `realizedAbs × wallet_sum_pnl`: [−0.06, −0.745, +0.048, −0.467, −0.752, +0.190, +0.219]. Synth/x402 `wallet_sum_pnl × settlementLegs`: +0.501 (07-07) → −0.874 (07-08) → +0.225 (07-09). x402 `wallet_sum_pnl × x402Total`: crosses gate for the first time at +0.881 today. Consistent 12-seed jitter until any pair confirms two runs in a row.
