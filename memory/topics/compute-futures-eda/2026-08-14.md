# Compute-Futures EDA — 2026-08-14

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-14.csv` (144 rows, 4 modes)
**Run:** 2026-08-15
**Prior filed report:** 2026-08-13 (single-day gap).

## Top findings

1. **[MEDIUM — extension, promoted class n=8 consecutive filed runs, deepening] Spread `role_pnl` retail mean −$11,092 (deeper by $1,619 vs 08-13); top-5 losses 100% retail; top-5 gains 0% retail.** Top-5 losses: 2026081411 retail −$19,447 / 202608141 retail −$17,027 / 202608149 retail −$16,510 / 202608145 retail −$14,282 / 2026081412 retail −$13,240. Top-5 gains: **2 operator + 3 analyst, 0 retail** (202608149 operator +$12,239 / 2026081412 analyst +$10,411 / 2026081411 analyst +$10,333 / 2026081410 operator +$9,366 / 2026081411 operator +$8,877). Per-role: retail mean **−$11,092** (std $5,216, min −$19,447, max −$4,547) — deeper by $1,619 vs 08-13's −$9,473, deeper by $5,108 vs 08-12's −$5,984; operator +$6,211 (std $3,258, up from 08-13's +$5,246); analyst +$3,617 (std $4,615, up from +$2,620). Extends [[compute-futures-spread-retail-loss-concentration]] from n=7 to **n=8 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11, 08-12, 08-13, 08-14). Loss-floor shallower by $3,657 vs 08-13 (−$23,104 → −$19,447), but retail mean still deepens; 08-12's single-day bimodal counter-signal (retail also top gain) remains fully retracted at n=2. Under IQR fence, spread `role_pnl` at 0% today (n=0 — retail losses cluster tightly enough that no single row escapes the 1.5×IQR fence).

2. **[MEDIUM — NEW cross-mode ≥10% cluster, 2 new MEDIUMs] Spread `minSpot` AND `maxSpot` both fire at 16.67% outlier_pct (6/36 rows each, disjoint anchors).** 08-13 had zero cross-mode ≥10% MEDIUMs; today reintroduces two. **Spread minSpot** fence [41.377, 44.592], 6 outliers split disjoint LOW/HIGH: LOW anchor 202608141 at 41.170 (3-role tie), HIGH anchor 2026081411 at 45.727 (3-role tie). **Spread maxSpot** fence [61.121, 68.413], 6 outliers both HIGH: HIGH-adjacent anchor 202608144 at 69.852 (3-role tie, fence+1.44), HIGH-extreme anchor 202608149 at 74.359 (3-role tie, fence+5.95). Both columns fire simultaneously — first time in the filed series that spot-price outliers surface in spread mode. Column-rotation across cross-mode ≥10% MEDIUMs across the last six filings: settlementLegs (08-10) → maxSpot (08-11, basket/synth/x402) → settlementLegs (08-12, synth/x402) → zero (08-13) → **spread minSpot + spread maxSpot (08-14)**. First time spread-mode spot prices carry MEDIUMs in this series.

3. **[LOW — n=5 consecutive filed runs, CEMENTED promoted class holds exactly] basket/synth scale-ratio HELD at 3.0250 exactly for `maxSpot`/`minSpot`.** Five-filing series (08-10, 08-11, 08-12, 08-13, 08-14). `maxSpot` basket/synth: min=max=3.0250 (deterministic constant, 36/36 seed × role pairs, floating-point noise in the 15th decimal). `minSpot` basket/synth: min=max=3.0250 (deterministic constant, 36/36 pairs). `maxCurve` basket/synth: 2.9949–3.0355 (mean 3.0206, jitter tightens from 08-13's 3.0149–3.0329). `minCurve` basket/synth: 2.9888–3.0787 (mean 3.0160, jitter widens slightly from 08-13's 3.0000–3.0444). `realizedAbs` basket/synth: 2.8399–3.4452 (mean 3.1564, similar band to 08-13's 2.9157–3.4330). Consistent with promoted note [[compute-futures-basket-synth-3025x-multiplier]].

4. **[LOW — n=6 consecutive filed runs, near-fire SIGN FLIPS + collapses] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, 08-12 zero, 08-13 zero, **08-14 zero**. Extends 08-13's n=5 to n=6. Highest within-mode pairs today: **spread `minSpot × settlementLegs` = −0.616** and **spread `realizedAbs × settlementLegs` = −0.611** (both near-fires below threshold; new to top-pair board — 08-13's leader was x402 `settlementLegs × x402Total` = +0.771). x402 `settlementLegs × x402Total` today **−0.2702** — 22-filing series: … +0.475, +0.558, +0.255, +0.771, **−0.270** — SIGN FLIPS + → − and magnitude collapses ~3× (from |0.771| to |0.270|). Non-spread top: basket `minCurve × wallet_sum_pnl` = −0.570 (float dust, excluded), synth `wallet_sum_pnl × minSpot` = −0.704 (float dust, excluded). n=12 seeds still systematically insufficient to stabilize any within-mode |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced at n=6.

5. **[LOW — both-sided expansion, translation shape] x402Total range shifts UP $23–25 on both sides: $287.28–$398.93 (std $28.84).** 08-13 was $263.71–$373.84 (range $110.13); today $287.28–$398.93 (range $111.65, range-magnitude near-identical, but both endpoints shift up ~$25). New LOW $287.28 above 08-13's $263.71 by **+$23.57** (single-day floor lift); new HIGH $398.93 above 08-13's $373.84 by **+$25.09** (single-day ceiling lift). Mean $337.58 (+$10.58 vs 08-13's $327.00). Sum $12,152.76 (+$380.61 vs 08-13's $11,772.15). Std $28.84 (down from $37.94, tighter dispersion). LOW anchor: 202608143 (3-role tie at $287.28); HIGH anchor: 202608149 (3-role tie at $398.93, ALSO the top spread operator gain +$12,239 seed). Shape distinct from 08-13's symmetric contraction — today is parallel-translation upward. x402Total outlier_pct = 8.33% in x402 mode (3-role tie at 202608149 HIGH ceiling, above upper fence $398.69), excluded per SKILL bimodal rule.

6. **[LOW — anchor concentration shape SHIFTS to hyper-single with quadruple-mode fan-out] One hyper-single anchor 202608144 (14 outlier positions across all 4 modes).** 202608144 fires in: basket maxCurve HIGH (3-role tie at 0.3130), spread maxSpot HIGH-adjacent (3-role tie at 69.852), synth role_pnl retail LOW (−38.001, 1 position), synth maxCurve HIGH (3-role tie at 0.1035), x402 role_pnl retail LOW (−38.001, mirror of synth), x402 maxCurve HIGH (3-role tie mirror of synth). Secondary anchor 202608149 (6 positions: spread maxSpot HIGH-extreme 3-role tie + x402Total HIGH 3-role tie). Tertiary tier: 202608141 (3) + 2026081411 (3) at spread minSpot disjoint ends. Class series: 08-10 super-single dense (202608106, 15 pos, single-mode) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → 08-13 super-single dense (202608137, 13 pos) → **08-14 hyper-single (202608144, 14 pos, quadruple-mode fan-out)**. Distinguishes from 08-13's single-mode super-single: today's leader spans all 4 modes because it also carries the extreme synth role_pnl retail loss (which by [synth ≡ x402] doubles into x402). Anchors fully rotate — zero overlap with 08-13's set (202608137/202608132/2026081311).

7. **[LOW — anti-pattern] Padded-seed outlier positions UNDER-INDEXED at ~12% vs 25% row-share.** Padded seeds (2026081410/11/12) hold 36 of 144 rows (25%). Today they contribute only 3 of 26 non-wallet_sum_pnl outlier positions (~11.5%) — 2026081411 alone (all 3 at spread minSpot HIGH); 2026081410 zero; 2026081412 zero (its basket settlementLegs=74 3-role tie sat inside the fence [73.875, 86.875]). Contrast: 08-12 padded seeds over-indexed at 42%; 08-13 proportional at 25%; **08-14 under-indexed at 11.5%**. Class continues rotation-not-persistence — anchor selection is seed-idiosyncratic and does not concentrate systematically on the padded encoding.

8. **[LOW — cross-mode LOW cluster, sub-threshold] synth+x402 `settlementLegs` = 76 3-role tie at 202608149.** Both synth and x402 minimum settlementLegs = 76 at seed 202608149 (3-role tie in each mode — 3 rows counted once per SKILL synth ≡ x402 rule). Fence in both modes [74.5, 86.5] → 76 is inside fence, does NOT fire as outlier. Sub-threshold cross-mode LOW-tail cluster. Same 202608149 that carries the extreme spread maxSpot (74.36) AND x402Total HIGH (398.93) AND top operator spread gain (+$12,239) — this seed anchors four distinct sub-signals despite firing "only" 6 outlier positions. Basket separately shows 202608123 at settlementLegs=74 as a 3-role tie LOW that also stays inside basket's fence [73.875, 86.875].

9. **[LOW — recurring, 28th filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608141`–`202608149`), 36 rows 10-char seed (`2026081410`–`2026081412`). `seed` outlier_pct = 25.00% in every mode. Today padded 2026081411 anchors spread `minSpot` HIGH (3 positions), otherwise padded seeds under-indexed. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

10. **[LOW — non-promotable jitter] Conservation float dust.** `wallet_sum_pnl` mean per mode: basket **+1.42e−15** (vs 08-13 −5.18e−16; **SIGN FLIPPED** − → +, magnitude ~3× larger), spread **+1.84e−12** (vs 08-13 −6.70e−13; **SIGN FLIPPED** − → +, magnitude ~3× larger), synth **+8.63e−16** (vs 08-13 −2.21e−15; **SIGN FLIPPED** − → +, magnitude ~3× smaller), x402 **+8.63e−16** (identical to synth by [synth ≡ x402]). All three non-x402 modes sign-flip in the same direction (− → +) at similar magnitudes; all modes remain 12+ orders under the |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outliers: basket 8.33% (float dust), spread **0%**, synth 16.67% (float dust), x402 16.67% (float dust). σ still ≥8 orders under 1e-6 per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

12. **[PASS] Row/column shape unchanged vs 08-13.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.42e−15  | 3.78e−14  | OK |
| spread    | +1.84e−12  | 5.00e−12  | OK |
| synthetic | +8.63e−16  | 1.63e−14  | OK |
| x402      | +8.63e−16  | 1.63e−14  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=6 consecutive filed runs.** Highest today: spread `minSpot × settlementLegs` = −0.616, spread `realizedAbs × settlementLegs` = −0.611. x402 `settlementLegs × x402Total` collapses from +0.771 (08-13 near-fire) to **−0.270** — sign flip + 3× magnitude collapse; alternation-not-persistence continues on that pair.
- **Retail-role loss concentration in spread — n=8 consecutive filed runs, promoted class deepens.** Retail mean $1,619 deeper vs 08-13 (−$9,473 → −$11,092); 5/5 top losses retail, 0/5 top gains retail (2 operator + 3 analyst).
- **basket/synth `maxSpot`/`minSpot` scale-ratio = 3.0250 (deterministic) at n=5 filed runs.** Cemented; consistent with promoted [[compute-futures-basket-synth-3025x-multiplier]].
- **Spread `minSpot` + `maxSpot` fire simultaneously as cross-mode ≥10% MEDIUMs at 16.67% each.** First time spread-mode spot-price outliers appear in the filed series; anchors disjoint (minSpot at 202608141/2026081411; maxSpot at 202608144/202608149).
- **x402Total range translates upward on both sides** — LOW +$23.57, HIGH +$25.09, range magnitude near-identical ($110.13 → $111.65), std tightens ($37.94 → $28.84). Distinct from 08-13's symmetric contraction shape.
- **Cross-mode ≥10% MEDIUM count rises 0 → 2.** Both are spread spot-price columns; first appearance of spread-side spot MEDIUMs in the filed series.
- **Anchor concentration jumps to hyper-single, quadruple-mode fan-out (202608144 at 14 positions across all 4 modes)** — distinct from 08-13's single-mode super-single dense.
- **Padded-seed under-indexed at 11.5% vs 25% row-share** — inverse of 08-12's 42% over-representation; rotation-not-persistence class continues.

## Row count and shape vs 2026-08-13

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface EXPANDS from 08-13 → 08-14.** 08-13: 0 HIGH, 1 MEDIUM (retail-loss n=7 with counter-signal revoked). 08-14: 0 HIGH, **3 MEDIUM** (retail-loss n=8 deepening + spread minSpot 16.67% NEW + spread maxSpot 16.67% NEW). Cross-mode ≥10% count rises from 0 to 2. Retail-loss promoted class carries forward and deepens; two fresh spread spot MEDIUMs appear.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's synth+x402 role_pnl retail outlier at 202608144 (−$38.00) and synth+x402 maxCurve HIGH cluster at 202608144 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (3.0250× exactly for maxSpot/minSpot, ~3.02× for curves)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable across 08-10, 08-11, 08-12, 08-13, 08-14 (n=5).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers at 8.33%/0%/16.67%/16.67% today — all float dust.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 28th consecutive filed run. Padded 2026081411 anchors spread `minSpot` HIGH today (3 positions). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-13's zero cross-mode ≥10% MEDIUMs give way to today's 2 spread spot-price MEDIUMs; 08-13's near-fire x402 settlementLegs × x402Total (+0.771) sign-flips and collapses to −0.270; only the retail-loss promoted class carries forward and deepens. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration migrates to hyper-single, quadruple-mode fan-out** — series: 08-10 super-single dense (202608106, 15 pos, single-mode) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → 08-13 super-single dense (202608137, 13 pos, single-mode) → **08-14 hyper-single (202608144, 14 pos, all 4 modes)**. Anchors fully rotate; zero overlap with 08-13's anchor set.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=8 (08-14).** Retail mean deepens for 3rd consecutive filing (−$5,984 → −$9,473 → −$11,092 across 08-12 → 08-13 → 08-14). Operator+analyst absorb the residual gain; today shows 2 operator + 3 analyst in top-5 (last filing was 4 operator + 1 analyst).
