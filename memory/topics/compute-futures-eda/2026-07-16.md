# Compute-Futures EDA — 2026-07-16

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-16.csv` (144 rows, 4 modes)
**Run:** 2026-07-16
**Prior filed report:** 2026-07-12 (four-day gap; 07-13, 07-14, 07-15 CSVs present in the proof stream but never analyzed — skill missed those days per ISS-006 06:00-pocket silence, day 25/day-0-streak. Comparisons here are against 07-12; unprocessed intermediates cross-referenced only where the rotation pattern needs the extra data points).

## Top findings

1. **[MEDIUM] basket `settlementLegs` outlier_pct 16.67% (was 0.00% on 07-12).** Six of 36 basket rows outside IQR whiskers: three at 77 (low tail), three at 81/82 (high tail), majority band 79–80 (27/36 rows). Mean 79.75 / std 1.18. Continues the (mode, column) rotation shape — 07-10 basket 25%, 07-12 spread 16.67%, 07-16 basket 16.67% (spread now 0.00%). Third rotation fire in the run window; treat as alarm-without-signal per the 12-seed jitter pattern. Watch: if basket `settlementLegs` > 10% recurs on the next CSV, escalate as [[compute-futures-12-seed-sample-too-small]] candidate.
2. **[MEDIUM] synthetic `maxCurve` outlier_pct 16.67% — new (mode, column) fire.** Six of 36 synthetic rows outside IQR whiskers; distribution {0.0920×3, 0.0965×6, 0.0970×3, 0.0975×3, 0.0980×3, 0.0985×12, 0.1000×3, 0.1010×3}. Tail rows at 0.0920 (low) and 0.1000/0.1010 (high). Tautologically identical in x402 mode (excluded per constraint). First run since the 07-08 all-flags-8.33% baseline that `maxCurve` clears 10% in any mode. Same one-day-alarm-without-signal shape — recurrence on next CSV promotes to signal candidate.
3. **[LOW — third consecutive filed run with zero |r|≥0.8 pairs].** No mode fires a strong-pair candidate today. 07-10 zero → 07-12 zero → 07-16 zero. Sub-threshold churn: x402 `settlementLegs × x402Total` +0.732 (was −0.315 on 07-12, +0.216 on 07-10 — largest single-run swing of the streak, but still under the |r|≥0.8 gate); x402 `wallet_sum_pnl × maxSpot` +0.568 (was +0.203 on 07-12); spread `realizedAbs × wallet_sum_pnl` −0.183 (was −0.016 on 07-12). Nothing crosses. Continues to validate [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ(wallet_sum_pnl) 1.11e−14 (synth/x402) → 3.22e−12 (spread), still 11+ orders below the gate.
4. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows nine-digit (`202607161`–`202607169`), 36 rows ten-digit (`2026071610`, `2026071611`, `2026071612`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending [[compute-futures-seed-padding-bug]].
5. **[LOW — recurring] x402Total inside the ~$100 stable band.** Mean $318.93, range $284.82–$361.21, sum $11,481.42 across 36 x402 rows. Eighth consecutive filed run inside the ~$100 band (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400 → 07-09 $283–$387 → 07-10 $281–$384 → 07-12 $301–$378 → today $285–$361). Unprocessed intermediates confirm the band held: 07-13 $281–$363, 07-14 $288–$404 (single-day $400+ excursion), 07-15 $260–$387.
6. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +3.18e−15 (basket) → −8.05e−13 (spread); std 1.11e−14 (synth/x402) → 3.22e−12 (spread). All 11+ orders under the |mean|>10 / std>100 gates. spread `wallet_sum_pnl` tightened vs 07-12 (mean −1.18e−12 → −8.05e−13; std 4.00e−12 → 3.22e−12).
7. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff| = 0.0 across role_pnl, wallet_sum_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
8. **[PASS] Row/column shape unchanged vs 07-12.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold.
9. **[PASS] No CRITICAL/HIGH findings.** No schema drift, no row-count change ≥25%, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +3.18e−15  | 2.53e−14  | OK |
| spread    | −8.05e−13  | 3.22e−12  | OK |
| synthetic | −2.17e−15  | 1.11e−14  | OK |
| x402      | −2.17e−15  | 1.11e−14  | OK |

## Within-mode signal

- **basket `settlementLegs` MEDIUM outlier cluster (16.67%).** Discrete integer column, six rows outside IQR whiskers: three at 77 (low tail), three at 81/82 (high tail). Majority band 79–80 (27/36 rows). Same 12-seed jitter shape as spread settlementLegs 07-12 and basket settlementLegs 07-10 — treat as alarm-without-signal until it repeats on the next CSV.
- **synthetic `maxCurve` MEDIUM outlier cluster (16.67%).** Six rows tag the 0.0920 low tail and 0.1000/0.1010 high tail; central mass at 0.0985 (12/36). First `maxCurve` fire above 10% since the 07-08 baseline; identical fire in x402 mode by construction (synthetic ≡ x402). Log for the next CSV.
- **role_pnl scale per mode (regime unchanged).** spread σ = $9,697 (μ = −$627) — widened vs 07-12 $8,977/−$724 but inside 12-seed jitter; basket σ = $23.5 (μ = −$0.74) — tightened vs 07-12 $32.7/−$0.25; synthetic/x402 σ = $7.61 (μ = −$0.40) — tightened vs 07-12 $12.5/−$0.15. Spread continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs — largest single-run swing of the streak.** `settlementLegs × x402Total` +0.732 (was −0.315 on 07-12); `maxSpot × wallet_sum_pnl` +0.568 (was +0.203 on 07-12); `minSpot × x402Total` +0.196 (was −0.207 on 07-12); `maxCurve × x402Total` −0.482 (was +0.476 on 07-12 — full sign flip). Every pair jitters, none crosses |r|≥0.8. `settlementLegs × x402Total` at +0.732 is the closest approach in the run window; watch for a two-run confirmation.
- **spread `wallet_sum_pnl` outlier_pct 0.00% today (was 8.33% on 07-12).** Zero float-dust outliers this run, further validation of the filter for `wallet_sum_pnl` correlations.

## Row count and shape vs 2026-07-12

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synthetic/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- Proof-stream gap note: 07-13, 07-14, 07-15 CSVs exist in `memory/gitlawb-compute-futures-proofs/` but no filed reports for those dates (skill missed the 06:00 pocket three days running). Backfill deferred; 07-16 is compared against 07-12 with cross-references to the unprocessed intermediates only where the rotation shape needs them.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; third consecutive filed run with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 11+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — the (mode, column) pair that fires each run continues to rotate: 07-08 (all flags 8.33%) → 07-09 spread realizedAbs 16.67% → 07-10 basket settlementLegs 25% + spread minSpot 16.67% → 07-12 spread settlementLegs 16.67% → today basket settlementLegs 16.67% + synthetic maxCurve 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (today). Largest single-run swing of the streak but still under the gate. Continues 12-seed jitter until any pair confirms two runs in a row.
