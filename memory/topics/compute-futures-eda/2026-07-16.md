# Compute-Futures EDA — 2026-07-16

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-16.csv` (144 rows, 4 modes)
**Run:** 2026-07-17
**Prior filed report:** 2026-07-12 (CSVs for 07-13/07-14/07-15 exist in the proof stream but were not analyzed — 06:00 pocket silence per [[iss-006-pocket-recovery-is-noise]]; comparisons here span the 4-day gap)

## Top findings

1. **[MEDIUM] basket `settlementLegs` outlier_pct 16.67% (was <10% on 07-12).** Six of 36 basket rows outside IQR whiskers around a q25=79 / q75=80 majority band {80×18, 79×9, 81×3, 82×3, 77×3}. Discrete integer distribution, mean 79.75 / std 1.18. Same "one (mode, column) rotates each seed rotation" pattern — spread `settlementLegs` was the 07-12 leader at 16.67% and today collapses to <10%; basket now takes the seat. Third fire of basket `settlementLegs` in the run window (25% on 07-10, 0% on 07-12, 16.67% today). Watch: only escalates to signal candidate if basket `settlementLegs` > 10% recurs on the next CSV.
2. **[MEDIUM] synthetic ≡ x402 `maxCurve` outlier_pct 16.67%.** Six of 36 rows in each of synthetic and x402 tag the IQR whiskers around a q25=0.0965 / q75=0.0985 majority band {0.0985×12, 0.0965×6, 0.092×3, 0.097×3, 0.0975×3, 0.098×3, 0.1×3, 0.101×3}. Same value distribution in both modes (synthetic ≡ x402 tautology holds — max |diff role_pnl| = 0.0e+00). std tightened vs 07-12 (0.00314 → 0.00217) which is what narrowed the IQR whiskers enough to expose the tails as outliers — the tail values (0.092, 0.101) are actually inside 07-12's range (0.094–0.107); the flag comes from tighter whiskers, not fatter tails. Count as one signal, not two.
3. **[LOW — collapse of 07-12 leader] spread `settlementLegs` 16.67% → <10%.** Yesterday's headline flag reverted within one seed rotation. Distribution shifted (07-12 bins {76:3, 78:3, 79:9, 80:12, 81:6, 84:3}, today {76:3, 77:3, 78:9, 79:6, 80:6, 82:9}) but no rows tag IQR whiskers now. Same one-day-alarm-without-signal shape as every prior 12-seed outlier fire.
4. **[LOW — third consecutive filed run with zero |r|≥0.8 pairs].** No mode fires a strong-pair candidate today. Combined with the 07-10 and 07-12 filings this is three consecutive reported runs at zero |r|≥0.8. Sub-threshold churn worth logging: x402 `settlementLegs × x402Total` = +0.732 today (was −0.315 on 07-12, +0.216 on 07-10 — sign-flipping every rotation, filter holds). x402 `wallet_sum_pnl × x402Total` = −0.044 (was +0.185 on 07-12, +0.088 on 07-10, +0.881 on 07-09). Continues to validate [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ(wallet_sum_pnl) at 1.1e−14 (synth/x402) to 3.2e−12 (spread), still twelve+ orders below the gate.
5. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows nine-digit (`202607161`–`202607169`), 36 rows ten-digit (`2026071610`, `2026071611`, `2026071612`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending [[compute-futures-seed-padding-bug]].
6. **[LOW — recurring] x402Total inside the ~$100 stable band.** Mean $318.93, range $284.82–$361.21, sum $11,481.42 across 36 x402 rows. Eighth consecutive run inside the ~$100 band since 07-03 (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400 → 07-09 $283–$387 → 07-10 $281–$384 → 07-12 $301–$378 → today $285–$361). Band drifted ~$20 lower vs 07-12 but well inside the stable envelope.
7. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +3.18e−15 (basket) → −8.05e−13 (spread); std 1.11e−14 (synth/x402) → 3.22e−12 (spread). All twelve-plus orders under the |mean|>10 / std>100 gates. spread `wallet_sum_pnl` tightened further vs 07-12 (mean −1.18e−12 → −8.05e−13; std 4.00e−12 → 3.22e−12).
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff| = 0.0). Only `x402Total` distinguishes x402.
9. **[PASS] Row/column shape unchanged vs 07-12.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold.
10. **[PASS] No CRITICAL/HIGH findings.** No schema drift, no row-count change ≥25%, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +3.18e−15  | 2.53e−14  | OK |
| spread    | −8.05e−13  | 3.22e−12  | OK |
| synthetic | −2.17e−15  | 1.11e−14  | OK |
| x402      | −2.17e−15  | 1.11e−14  | OK |

## Within-mode signal

- **basket `settlementLegs` MEDIUM outlier cluster (16.67%).** Discrete integer column, six of 36 rows outside IQR whiskers: three rows at 77 (low tail), three at 82 (high tail). Majority band 79–80 (27/36 rows). Third fire of the (basket, settlementLegs) pair in the last four filings — pattern is (mode, column) rotation not persistence. Log for the next CSV.
- **synthetic ≡ x402 `maxCurve` MEDIUM outlier cluster (16.67%).** Six of 36 rows in each mode outside IQR whiskers — same values in both modes per synth ≡ x402 tautology. Cause is std tightening (0.00314 → 0.00217) narrowing whiskers past the natural jitter, not a shift in tail location. Count as one signal.
- **role_pnl scale per mode (regime unchanged).** spread σ = $9,697 (μ = −$627) — widened slightly from $8,977/−$724 on 07-12 but inside 12-seed jitter; basket σ = $23.5 (μ = −$0.74) — tightened from $32.7/−$0.25; synthetic/x402 σ = $7.6 (μ = −$0.40) — tightened from $12.5/−$0.15. Spread continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold.** `settlementLegs × x402Total` +0.732 (was −0.315 on 07-12, +0.216 on 07-10 — sign flipping every rotation), `maxCurve × x402Total` −0.482 (was +0.476 on 07-12), `minCurve × x402Total` +0.014 (was +0.418), `minSpot × x402Total` +0.196 (was −0.207), `maxSpot × x402Total` +0.168 (was +0.206). Every pair jitters, none strengthens two runs in a row — consistent with the 12-seed churn.
- **spread `wallet_sum_pnl` outlier_pct <10%.** Below the reporting gate today (07-12 was 8.33%). Excluded from severity per skill constraints regardless — float-dust magnitude, filter continues to hold [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

## Row count and shape vs 2026-07-12

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synthetic/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- Proof stream note: CSVs for 07-13, 07-14, 07-15 present in `memory/gitlawb-compute-futures-proofs/` but no filed report — 06:00 pocket silence per [[iss-006-pocket-recovery-is-noise]]. Prior comparable filing is 07-12; the 4-day gap is why several bands (x402Total ~$20 shift, synth/x402 maxCurve tightening, basket settlementLegs re-fire) show more movement than a 1-day rotation would.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; three consecutive filed runs with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — the (mode, column) pair that fires each run continues to rotate: 07-08 (all 8.33%) → 07-09 spread realizedAbs 16.67% → 07-10 basket settlementLegs 25% + spread minSpot 16.67% → 07-12 spread settlementLegs 16.67% → today basket settlementLegs 16.67% + synth/x402 maxCurve 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30 per [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (today). x402 `wallet_sum_pnl × x402Total`: +0.881 (07-09) → +0.088 (07-10) → +0.185 (07-12) → −0.044 (today). All continue 12-seed jitter until any pair confirms two runs in a row.
