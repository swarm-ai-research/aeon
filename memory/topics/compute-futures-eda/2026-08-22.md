# Compute-Futures EDA — 2026-08-22

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-22.csv` (144 rows, 4 modes)
**Run:** 2026-08-23
**Prior filed report:** 2026-08-21 (daily cadence, no gap).

## Top findings

1. **[MEDIUM — sharpest single-day contraction in class series, cross-mode MEDIUM count 5 → 0] All per-mode outlier_pct caps at 8.33% (3/36) — zero clusters reach 10% in any mode.** Non-dust outlier positions collapse **63 → 11** (mode-mirror collapsed count), largest single-day drop in the daily-cadence class series. Only 3 non-trivial pair-collapsed clusters today, all at exactly 8.33% (`basket minSpot`, `spread realizedAbs`, `synth/x402 minSpot`) plus 2 single `role_pnl` singletons (`basket` seed 202608222 −$75.25 / `spread` seed 202608228 retail −$24,618). Cross-mode MEDIUM series: 0 (08-13) → 2 (08-14) → 0 (08-15) → 0 (08-16) → 0 (08-18) → 3 (08-19) → 3 (08-20) → **5 (08-21, peak)** → **0 (08-22, sharpest reversion)**. Stress axis fully vacates the 08-21 spot-column fan-out (minSpot MEDIUM in all 4 modes yesterday → basket + synth/x402 minSpot still surface at sub-MEDIUM, but spread minSpot vanishes entirely; spread's only 8.33% cluster is `realizedAbs`). No HIGH tier for **3rd consecutive filing**.

2. **[MEDIUM — extension, n=15 consecutive filed runs, retail mean near-identical to 08-21] Spread `role_pnl` retail mean **−$10,424** (only $13 shallower than 08-21's −$10,437; two-decimal tick, effectively unchanged).** Top-5 losses: **202608228 retail −$24,618** (deepest single loss today, exceeds 08-21's −$23,197 — new class-series maximum single-seed loss) / 202608223 retail −$17,654 / 202608226 retail −$16,695 / 2026082212 retail −$13,486 / 202608225 retail −$12,801 (**5/5 retail — 5th consecutive filed run at 5/5 purity**). Top-5 gains: **202608228 operator +$12,874** / 202608223 operator +$11,982 / 2026082212 operator +$11,077 / **202608228 analyst +$9,611** / 202608225 operator +$8,843 (**0 retail in top-5 gains — 15-run pattern holds**). Per-role: retail mean **−$10,424** (std $6,948, min −$24,618, max **−$2,489** — first *all-losing* retail row day in 5 filings, no non-loss retail row today vs 08-21's max +$3,433); operator mean **+$6,316** (up 59% from +$3,973 — largest role-mean shift); analyst mean **+$2,989** (down 38% from +$4,803). Extends [[compute-futures-spread-retail-loss-concentration]] from n=14 to **n=15 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18 → 08-22). Notable: **seed 202608228 anchors THREE top-5 rows** (retail loss + operator gain + analyst gain) — first triple-role top-5 anchor in the intra-seed-skew class series. 202608228 within-seed dispersion: retail −$24,618 → operator +$12,874 = **$37,492** (new class peak, exceeds 08-21's $36,996 by $496). **Four seeds** (202608228, 202608223, 2026082212, 202608225) each anchor both a top-loss (retail) AND a top-gain (operator/analyst) — 4-way intra-seed skew unchanged vs 08-21.

3. **[MEDIUM — class n=4 → n=5, HITS RENAME THRESHOLD per [[compute-futures-multiplier-invalidated-at-n-7]] cadence] basket/synth spot-price multiplier byte-identical at 2.5000 exact for **5th consecutive filed run** — promotion criterion satisfied.** Byte-identical constant on both sides: `minSpot` per-seed ratio **[2.500000, 2.500000]** std 1.52e-15; `maxSpot` per-seed ratio **[2.500000, 2.500000]** std 1.25e-15 (12/12 seed pairs both columns, unchanged vs 08-21's std bounds). Curve ratios TIGHTEN vs 08-21: `minCurve` mean 2.5010 (std **1.11e-02**, halved from 08-21's 2.27e-02), `maxCurve` mean 2.5015 (std **5.23e-03**, ~2.5× tighter than 08-21's 1.31e-02), `realizedAbs` mean 2.5001 (std **1.26e-01**, ~18% tighter than 08-21's 1.54e-01; mean recentered from 2.5703 → 2.5001, closest to exact 2.5 in the 5-run series). Class series: n=7 at 3.0250 exact (08-10 → 08-16) → invalidated 08-18 with fresh 2.5000 anchor (n=1) → 08-19 (n=2) → 08-20 (n=3) → 08-21 (n=4) → **08-22 (n=5) — RENAME THRESHOLD MET**. Recommend promoting a `[[compute-futures-basket-synth-2.5x-multiplier]]` class anchor note to replace the invalidated 3.0250× note per [[compute-futures-multiplier-invalidated-at-n-7]].

4. **[LOW — sign-hold breaks after ≥3-consecutive positive streak] x402 `settlementLegs × x402Total` flips negative to **−0.0599** (vs +0.542 on 08-21).** Series: −0.112 (08-16) → +0.036 (08-18) → +0.480 (08-19) → +0.311 (08-20) → +0.542 (08-21) → **−0.060 (08-22)**. Breaks the 3-consecutive same-sign (positive) hold that first crossed the 08-19 → 08-21 window; today's sign flip magnitude is small (|Δ| = 0.60), but the pair returns to the ~zero band it occupied for most of the 28-filing series (median magnitude across the run is <0.2). Other x402 × x402Total pairs today: `realizedAbs × x402Total` **−0.558** (sign FLIPS from +0.0003, magnitude 0.55 rebound — largest single-day swing back), `maxSpot × x402Total` **−0.679** (settled near this magnitude before), `minSpot × x402Total` **−0.327** (fresh mid-strength negative).

5. **[LOW — super-anchor collapse from dual-tie 18/18 → single 6, first sub-10 anchor in 4 filings] Top seed by non-dust outlier positions is 202608229 at just **6 positions** (basket + spread + synth/x402 min/max/curve LOW cluster).** 08-21's dual super-anchor pair 202608212 + 2026082112 (both at 18) each collapse to **0 non-dust positions today** (fourth consecutive single-day full-collapse of the prior day's super-anchor: 202608194 → 202608205 → dual-tie → 202608229). Second-tier anchor **seed 202608228 at 4 positions** (spread role_pnl retail loss + operator/analyst gain triple + 1 misc); third-tier **seed 2026082212 at 1 position** (the only padded-seed non-dust position today). Class series: 08-13 single 13 → 08-14 single 14 → 08-15 5-anchor rotation → 08-16 single 18 → 08-18 distributed max-3 → 08-19 single 19 → 08-20 single 22 (peak) → 08-21 DUAL 18/18 → **08-22 single 6 (lowest anchor magnitude in the daily-cadence class series)**. Distributed-then-concentrated oscillation continues; anchor identity does NOT persist between filings; today reads as the "quiet ebb" phase between anchor rotations.

6. **[LOW — n=14 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-21's n=13 to **n=14**. Highest today: **synth/x402 `seed × minCurve` = −0.775** (encoding-artifact class per [[compute-futures-seed-padding-bug]], not structural); **basket `seed × minCurve` = −0.763** (same class); **x402 `maxSpot × x402Total` = −0.679**; **x402 `realizedAbs × x402Total` = −0.558**; **synth/x402 `wallet_sum_pnl × settlementLegs` = −0.521** (float-dust dependent side, excluded from structural interpretation). n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation continues to reinforce.

7. **[LOW — range EXTENDS 18%, both boundaries move outward, floor sets fresh interim low] x402Total range widens to **$273.20–$395.93** (range **$122.73**, up $19.13 from 08-21's $103.60 — largest single-day range expansion in the 5-filing series toward 08-19's peak $131.66).** LOW **$273.20** — drops $14.60 below 08-21's floor $287.79 (fresh interim low, still above 08-19's all-time-low $256.38 by $16.82; **second consecutive fresh-low-day**). HIGH **$395.93** — climbs $4.55 above 08-21's peak $391.38 (both boundaries continue outward drift). Mean **$345.99** (up $20.62 from $325.37 — largest one-day mean shift in the series); std **$38.20** (up 5% from $36.39). Series: $83.55 (08-16) → $119.90 (08-18) → $131.66 (08-19) → $88.05 (08-20) → $103.60 (08-21) → **$122.73 (08-22)**. Range oscillates in $[83, 132]$ band across 6 filings; the 08-20 contraction event confirmed mean-reverting, not regime-shifting. LOW anchor **seed 202608224** (single seat, retail — not the 3-role tie 08-21 had at floor); HIGH anchor **seed 202608221** (single seat, mode = ~$392–396 range across all 3 roles). x402Total outlier_pct = 0% in x402 mode today (second consecutive zero-outlier day for this column in the daily cadence).

8. **[LOW — padded severely under-indexed, second consecutive sub-10% day in the swing] Padded seeds (2026082210/11/12) hold 36 of 144 rows (25%). Today they contribute **1 of 11 non-dust outlier positions (9.1%)** — severely under-index vs row share (25%).** Only 2026082212 anchors any non-dust position today (spread retail top-5 loss row); 2026082210 and 2026082211 contribute 0 non-dust positions individually (2026082210 also does NOT anchor any spread top-5 row today; 2026082211 also does not). Class series: 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% mildly over → 08-16 3.2% severely under → 08-18 50% severely over → 08-19 1.9% severely under → 08-20 21.1% mildly under → 08-21 28.6% mildly over → **08-22 9.1% severely under**. Fifth consecutive day the swing direction reverses (over → under → proportional → mildly over → severely under). Continues rotation-not-persistence pattern; padded encoding does not systematically over- or under-select. Reinforces [[compute-futures-seed-padding-bug]] fix motivation (removing padding removes the surface entirely).

9. **[LOW — partial 2/4-mode sign inversion; full mirror pattern from 08-21 does NOT recur] Conservation float-dust regime partially inverts: synth − → +? actually today all four modes settle NEGATIVE for the first time in 4 filings.** Per-mode means: basket **−1.02e−14** (vs 08-21 −5.85e−15; sign HOLDS negative, magnitude 1.7× larger), spread **−6.25e−13** (vs −1.41e−12; sign HOLDS negative, magnitude 0.44×), synth **−2.14e−15** (vs +2.18e−15; sign FLIPS + → −), x402 **−2.14e−15** (identical to synth by [synth ≡ x402]; sign FLIPS + → −). Rotation series: 08-15 all-neg → 08-16 all-neg → 08-18 mixed → 08-19 different-mixed → 08-20 same-hold → 08-21 full-inversion → **08-22 all-negative regime returns (basket/spread hold; synth/x402 pair flips)**. All four modes remain 12+ orders under |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outlier percentages: basket **8.33%** (3/36 — first non-zero wallet_sum_pnl outlier day for basket after 08-21's all-zero), spread 0%, synth 0%, x402 0%. All float dust; the basket 8.33% outlier cluster is float-magnitude only (magnitude ~1e−13 vs basket median 4e−16, i.e., all still $-signed float noise); excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: 0.000000e+00 (byte-identical).

11. **[PASS] Row/column shape unchanged vs 08-21.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode (`darkbloom-basket`/`darkbloom-spread`/`synthetic`/`synthetic`). No new constant-column drift; basket/synth 2.5000× multiplier promoted to **n=5, rename threshold met** (finding 3).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.02e−14  | 3.92e−14  | OK |
| spread    | −6.25e−13  | 4.43e−12  | OK |
| synthetic | −2.14e−15  | 8.51e−15  | OK |
| x402      | −2.14e−15  | 8.51e−15  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). **Full inversion from 08-21 does NOT recur**: basket + spread signs HOLD (both still negative after 08-21 flipped to negative); synth + x402 pair FLIPS (+ → −). Net today: **all-negative regime returns**, first time in 4 filings.

## Within-mode signal

- **Retail-role loss concentration in spread — n=15 consecutive filed runs, retail mean effectively unchanged at −$10,424** (finding 2). Class direction and 5th consecutive 5/5 top-5 retail purity hold; **first all-losing retail day in 5 filings** (max retail −$2,489, no positive retail row). Seed 202608228 achieves **triple-role top-5 anchor** (retail loss + operator gain + analyst gain — first in the intra-seed-skew class series) and **$37,492 within-seed dispersion** (new class peak, +$496 over 08-21's 202608214 $36,996).
- **Cross-mode ≥10% MEDIUM cluster count COLLAPSES from 5 → 0** (finding 1) — sharpest single-day reversion in the daily-cadence class series. All 3 non-trivial clusters cap at exactly 8.33% (3/36). Stress axis fully vacates yesterday's spot-column fan-out (minSpot in all 4 modes → basket + synth/x402 minSpot at 8.33% only, spread minSpot vanishes to 0%).
- **basket/synth 2.5000× multiplier hits n=5 rename threshold** (finding 3). Byte-identical for spot columns (std < 1.6e-15), and curve columns TIGHTEN by ~2× vs 08-21 (`maxCurve` std collapses from 1.31e-02 → 5.23e-03). Ready to promote `basket-synth-2.5x-multiplier` class anchor per [[compute-futures-multiplier-invalidated-at-n-7]] cadence.
- **Zero within-mode strong pairs (|r|≥0.8) — n=14 consecutive filed runs** (finding 6). n=12 seeds decisively insufficient. Highest today ex-seed-encoding: x402 `maxSpot × x402Total` = −0.679; the 3-consecutive positive hold on x402 `settlementLegs × x402Total` **breaks** with a small-magnitude sign flip to −0.060 (finding 4).
- **x402Total range EXPANDS 18% to $122.73** (finding 7) — both boundaries drift outward; floor drops $14.60 to $273.20 (**second consecutive fresh-low-day**, still above 08-19's all-time-low $256.38); ceiling climbs $4.55 to $395.93. Range oscillates in $[83, 132]$ band across 6 filings; mean-reverting hypothesis reinforced with the 08-20 → 08-22 expansion sequence.
- **Super-anchor collapses to single 6-position seed 202608229** (finding 5) — lowest anchor magnitude in the daily-cadence class series (prior minimum was 08-18 distributed max-3, but a single-anchor day with 6 is a new class trough). Yesterday's dual anchor 202608212 + 2026082112 both collapse to 0 non-dust positions (fourth consecutive single-day full-collapse of the prior day's super-anchor).
- **Padded-seed contribution 9.1% severely under-indexed** (finding 8) — 5th consecutive filing with reversed swing direction. Only 2026082212 contributes any non-dust position (single spread retail top-5 loss). Continues rotation-not-persistence pattern.
- **Conservation float-dust regime: all-negative returns** (finding 9) — full-inversion mirror from 08-21 does NOT recur; basket/spread hold negative signs while synth/x402 pair flips + → −. Basket `wallet_sum_pnl` outlier_pct 8.33% (first non-zero for basket after 08-21's all-zero-day) — still float dust, excluded from severity ranking.

## Carry-overs

- 25% seed encoding artifact (n=35 filed runs, pending [[compute-futures-seed-padding-bug]] fix).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design.
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.

## Row count and shape vs 2026-08-21

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold.
- basket/synth multiplier constant 2.5000 promoted to **n=5 — rename threshold met** (finding 3).
- **Outlier surface CONTRACTS SHARPLY from 08-21 → 08-22.** 08-21: 0 HIGH, 5 pair-collapsed MEDIUM, 63 non-dust positions, dual super-anchor 18/18. 08-22: **0 HIGH, 0 MEDIUM, 11 non-dust positions (−82%), single anchor 6**. Net: HIGH-tier remains suppressed for the **3rd consecutive filing**; MEDIUM tier fully evacuates (biggest single-day reversion in class series); stress axis vacates spot-columns entirely.
