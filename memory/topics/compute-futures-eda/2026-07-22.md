# Compute-Futures EDA — 2026-07-22

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-22.csv` (144 rows, 4 modes)
**Run:** 2026-07-23
**Prior filed report:** 2026-07-21 (day-over-day; no gap this run)

## Top findings

1. **[HIGH] Basket `realizedAbs` outlier_pct 25.00% — first appearance ever at ≥ 10% in the 14-filing record.** 9 of 36 rows outside IQR whiskers [139.55, 248.38]: three low-end tags at $124.49, three high tags at $249.86, three at $288.53. Mean $198.37 / std $41.67, range $124.49–$288.53. Prior 13 filings: 0.00% in 11 of them, 8.33% twice (07-08, 07-10). Never at 16.67%, never at 25%. Column mean has been stable at $170–$240 across the 14-day window; today's escalation is a tail-thickness event, not a shift in central tendency. Combined with findings 2 and 3, three simultaneous 25% flags on three distinct columns — the outlier surface is the widest of the current window. See [[compute-futures-12-seed-sample-too-small]].
2. **[HIGH] Spread `maxSpot` outlier_pct 25.00% — highest in the 14-filing record, escalated from 8.33% yesterday in one seed rotation.** 9 of 36 rows outside IQR whiskers [62.27, 68.26]: three low-end tags at $62.18, three high tags at $69.52, three at $71.38. Mean $65.70 / std $2.46. Prior peaks were 16.67% on 07-14 and 07-17; 07-21 sat at 8.33% (3/36). Same column, same magnitude class as the prior peaks — but 3x the count in a single rotation. Not-new-column-only-new-magnitude; the recurrence pattern still fits 12-seed rotation, but this is the first time the column has crossed 20% in the window.
3. **[HIGH] Synthetic `maxCurve` outlier_pct 25.00% (and tautologically x402/`maxCurve` 25.00%) — recurring column, first appearance at 25%.** 9 of 36 rows outside IQR whiskers [0.0951, 0.1026]: three low tags at 0.0945, three high at 0.1035, three at 0.1060. Mean 0.0991 / std 0.0030 — essentially unchanged from prior filings (07-08 through 07-21 range 0.0973–0.1009). Prior appearances at 16.67% on 07-14, 07-16, 07-17; today's escalation is the third distinct-column 25% simultaneous fire (findings 1, 2). The x402 flag is the tautological reflection of synthetic per the verified role-level equivalence (max |diff maxCurve| = 0.0); reported once as synth per the SKILL.md exclusion.
4. **[MEDIUM] Basket `maxCurve` outlier_pct 16.67% — third basket appearance ≥ 16% in the record.** 6 of 36 rows above IQR whisker 0.2579: three at 0.2585, three at 0.2655 (no low-end tags). Mean 0.2477 / std 0.0076. Prior basket `maxCurve` ≥ 10% appearances: 07-14 (16.67%), 07-17 (16.67%), 07-18 (33.33%), today (16.67%) — fourth appearance in nine days, one of them a 33.33% peak. Combined with today's basket `realizedAbs` HIGH (finding 1), basket is running two simultaneous ≥ 10% flags for the first time since 07-17 (basket/maxCurve + basket/maxSpot + basket/minSpot). Column keeps returning — not yet reclassifiable, but the fourth-appearance-in-nine-days cadence is worth marking.
5. **[MEDIUM — collapse] Yesterday's exact-duo (basket `settlementLegs` 16.67% + spread `minSpot` 16.67%) both collapse to 0.00% today.** 07-21's headline was the first two-mode-two-column MEDIUM pair from a prior filed run (07-10) recurring at matching magnitudes; today both columns clear IQR whiskers entirely. Neither survives a single seed rotation — the 07-10 → 07-21 exact-duo was a two-run coincidence, not a persistence signal. Rotation-not-persistence pattern holds for the sixth consecutive filing, now with the additional cost: this run's three HIGHs are entirely different columns from yesterday's two MEDIUMs. Full column-mode substitution again.
6. **[LOW — appearance] First within-mode |r| ≥ 0.8 pair in eight consecutive filed runs.** synthetic `maxSpot × minCurve = −0.823`, x402 `maxSpot × minCurve = −0.823` (identical by tautology). Prior zero-strong-pair streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21 → today breaks it. History of the pair per mode: 07-10 (−0.14, −0.14), 07-12 (−0.15, −0.15), 07-13 (+0.20, +0.20), 07-14 (+0.09), 07-15 (+0.61, +0.61 — closest prior brush), 07-16 (+0.01), 07-17 (+0.14), 07-18 (+0.10), 07-19 (−0.03), 07-20 (−0.31), 07-21 (−0.25), today (−0.82). Same-sign accelerated from −0.25 → −0.82 in one rotation. Log-and-watch — a second consecutive filing at |r| ≥ 0.8 in the same mode/column pair would break the rotation classification. See [[compute-futures-12-seed-sample-too-small]].
7. **[LOW — streak break] x402 `settlementLegs × x402Total` same-sign after 7 consecutive sign-flip rotations.** Today: +0.420 (07-21: +0.156, same positive sign). Full sequence: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → **+0.420 (today, same-sign)**. Longest same-sign run in the record so far was two consecutive, and it stops the 7-flip-in-7-rotations streak that was the cleanest 12-seed jitter signature. Sub-threshold classification unchanged (|r| < 0.5); pair worth continued watch.
8. **[LOW — recurring] Seed 25% encoding artifact unchanged, 14th consecutive filed run.** 108 rows 9-char seed (`202607221`–`202607229`), 36 rows 10-char seed (`2026072210`–`2026072212`). `seed` outlier_pct = 25.00% in every mode, same as every filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
9. **[LOW — recurring] x402Total inside the ~$100 stable band, fourteenth consecutive run.** Mean $318.14 (down $18.40 vs 07-21's $336.54 — reverses yesterday's first-mean-up drift), std $25.99, range $280.22–$366.09, sum $11,453.16. Band remains ~$280–$378 over the fourteen-filing window. No x402Total outlier flag (bimodal-by-design exclusion still holds anyway).
10. **[LOW — recurring] Spread wallet_sum_pnl σ up slightly, still float dust.** Today σ = 5.27e−12 (07-21: 4.24e−12 — modest widening after a two-run down streak). absmax = 8.64e−12. Basket σ 1.97e−14, synth/x402 σ 1.29e−14. Twelve+ orders under any conservation gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
11. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −2.42e−15 (basket), +4.69e−13 (spread), −8.71e−15 (synth), −8.71e−15 (x402). Std 1.97e−14 (basket), 5.27e−12 (spread), 1.29e−14 (synth/x402). All twelve+ orders under the |mean| > 10 / std > 100 gates.
12. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff role_pnl| = 0.0, max |diff realizedAbs| = 0.0, max |diff minSpot| = 0.0, max |diff maxSpot| = 0.0, max |diff minCurve| = 0.0, max |diff maxCurve| = 0.0). Only `x402Total` distinguishes x402.
13. **[PASS] Row/column shape unchanged vs 07-21.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
14. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −2.42e−15  | 1.97e−14  | OK |
| spread    | +4.69e−13  | 5.27e−12  | OK |
| synthetic | −8.71e−15  | 1.29e−14  | OK |
| x402      | −8.71e−15  | 1.29e−14  | OK |

## Within-mode signal

- **Three simultaneous 25% flags — the widest outlier surface in the 14-filing window.** basket `realizedAbs` (novel column at ≥ 10%), spread `maxSpot` (escalated from 8.33%), synth `maxCurve` (recurring column, first at 25%). None of yesterday's two MEDIUMs survives; three brand-new HIGHs replace them. Rotation-not-persistence holds, but the *magnitude* of today's surface is a level up: prior peak run was 07-18 (basket `maxCurve` 33.33% + spread `settlementLegs` 25%), so today ties the prior daily HIGH count and increases the number of distinct HIGH columns from two to three.
- **Basket `realizedAbs` novelty.** 14-filing window: eleven runs at 0.00%, two at 8.33%, today at 25%. Whiskers [139.55, 248.38] around q1=180, q3=208 — nine tags outside, symmetric enough (three low, six high) that it's not a one-tail bulge. Central tendency (mean $198) is squarely inside the historical range; only the tails widened. Watch for either (a) collapse to 0% in one rotation (fits the 12-seed cohort) or (b) ≥ 10% next run (persistence-candidate class).
- **Spread `maxSpot` 25% is a same-column escalation, not a new column.** Prior peaks 16.67% on 07-14 and 07-17. The pair `spread maxSpot × settlementLegs` sat at +0.587 today (below 0.8) — still sub-threshold; not the driver. Column mean stable at $65.70, std $2.46 — the tails widened symmetrically (three low at $62.18, three high at $69.52, three at $71.38).
- **Synth/x402 `maxCurve` 25% — first strong-pair appearance in eight runs behind it.** Today's synth `maxSpot × minCurve = −0.823` is the pair that would explain a maxCurve tail widening if maxSpot and minCurve are jointly loading onto the same seed structure; but `maxSpot × maxCurve` inside synth is +0.301, so `maxCurve` widening is not the same phenomenon as the strong pair. Two distinct signals arriving in the same mode on the same run.
- **Basket carries two simultaneous ≥ 10% flags (`realizedAbs` HIGH + `maxCurve` MEDIUM).** First multi-flag basket run since 07-17. Combined with the fact that basket has now flagged `maxCurve` on four of the last nine runs (07-14, 07-17, 07-18, today), basket is the mode most consistently producing recurring tail widening at the maxCurve column.
- **Prior-day exact-duo (basket settlementLegs 16.67% + spread minSpot 16.67%) fully collapses.** Both columns clear IQR whiskers today (0.00%). The 07-10 → 07-21 exact-duo coincidence does not persist for a third joint filing. Reclassify the 07-21 finding as a two-run coincidence, not the start of a persistence pattern.
- **role_pnl scale per mode (regime unchanged).** spread σ = $9,827 (μ = −$380) — narrowed from $10,464/−$511 on 07-21, breaks the three-run widening streak ($8,898 → $9,892 → $10,464 → today $9,827). basket σ = $31.35 (μ = −$0.52) — widened from $28.93/−$1.12, alternating pattern intact. synth/x402 σ = $11.40 (μ = −$0.12) — widened from $10.25/−$0.55. Spread continues to dominate pooled stats by two–three orders of magnitude — per-mode stratification remains the right default.
- **x402 sub-threshold pair drift.** Two rotation events at once: `settlementLegs × x402Total` = +0.420 (07-21 +0.156, same-sign — breaks the 7-flip streak); `maxSpot × x402Total` = +0.257 (07-21 −0.433 — sign flipped, magnitude collapsed); `minSpot × x402Total` = +0.220 (07-21 −0.228 — sign flipped, magnitude essentially the same); `wallet_sum_pnl × x402Total` = −0.335 (float-dust artifact per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]], excluded). No pair crosses 0.5.

## Row count and shape vs 2026-07-21

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (identical column set; no schema drift).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated from a basket-legs/spread-spot duo to a basket-realizedAbs/basket-maxCurve/spread-maxSpot/synth-maxCurve quartet at HIGH+MEDIUM magnitudes.** 07-21 fired 2 MEDIUMs; today fires 3 HIGHs + 1 MEDIUM. Net: 0 → 3 HIGH, 2 → 1 MEDIUM. Zero column overlap between the two rotations. Widest outlier surface in the 14-filing window.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0 across role_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; today's synth/x402 `wallet_sum_pnl × x402Total` = −0.335 discarded. σ(wallet_sum_pnl) twelve+ orders under the threshold everywhere. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 14th consecutive filed run. One-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted sixth consecutive filing. Yesterday's exact-duo rerun of 07-10 (basket settlementLegs 16.67% + spread minSpot 16.67%) does not persist to a third joint appearance — reclassified as a two-run coincidence. Today's three HIGHs are all fresh columns vs 07-21. See [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.420 (today, same-sign). **7-consecutive-sign-flip streak broken today**; longest prior same-sign run was 2. Sub-threshold 12-seed jitter classification unchanged.
- **Spread role_pnl σ three-run widening streak broken.** $8,898 (07-18) → $9,892 (07-20) → $10,464 (07-21) → $9,827 (today). Not yet a filed finding class; note for next run.
- **First within-mode |r| ≥ 0.8 pair in eight runs.** synth/x402 `maxSpot × minCurve = −0.823`. Log-and-watch — a second consecutive filing at |r| ≥ 0.8 on the same pair would break the rotation classification.
