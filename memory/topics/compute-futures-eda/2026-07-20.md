# Compute-Futures EDA — 2026-07-20

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-20.csv` (144 rows, 4 modes)
**Run:** 2026-07-21
**Prior filed report:** 2026-07-18 (1-day gap — 2026-07-19 CSV was not analyzed on the day; day-over-2-day comparison here)

## Top findings

1. **[MEDIUM] Basket `maxCurve` HIGH → 0% — three-run strengthening streak broke at run 4.** Yesterday's headline (07-15 0% → 07-16 8.33% → 07-17 16.67% → 07-18 33.33%) collapsed to 0% outliers today. Distribution went from 07-18's concentrated shape (six low tags at 0.2455/0.2465, six high tags at 0.272/0.274 around a 0.256–0.261 core) to today's flat spread across 11 values from 0.2355 to 0.265 with a single doubled value at 0.252 (6 rows). Mean drifted DOWN from 0.256–0.261 (07-18) to 0.2485 (today) — the level-shift that anchored the last four filings appears to have reverted. This confirms the escalation gate defined in the 07-18 report: rotation-to-<20% classifies 07-18 as the tallest 12-seed IQR spike observed to date rather than a structural signal, per [[compute-futures-12-seed-sample-too-small]]. The "first ever pair to persist AND strengthen across three consecutive runs" claim is retired.
2. **[MEDIUM] Spread `settlementLegs` HIGH → 0% — collapse companion to finding 1.** Yesterday's 25% HIGH (9/36 whisker tags on value 77 vs IQR=1 core at 79–80) reverted to 0% today. Distribution widened from four-value {77:6, 79:9, 80:15, 81:3, 82:3} to seven-value {77:3, 79:3, 80:9, 81:3, 82:9, 83:6, 84:3} with IQR expanded from 1 to 2.25. This is the third consecutive rotation for `settlementLegs` across the fleet (07-12 spread → 07-16 basket → 07-18 spread → today none) — the rotation pattern from prior filings is intact. Combined with finding 1: both of yesterday's HIGH flags fully collapsed within one seed rotation.
3. **[MEDIUM] Spread `realizedAbs` outlier_pct 16.67% — new column for spread.** 6 of 36 rows tag IQR whiskers around a $50,809 / σ $12,199 mean/std; tail rows sit at $26,966 (low) and $74,379 (high). Spread has not previously flagged `realizedAbs` at ≥ 10% in the current record. Same tight-band shape as prior curve-side rotations; classify with the 12-seed rotation cohort until it holds two runs in a row.
4. **[MEDIUM] Spread `minCurve` outlier_pct 16.67% — new column for spread.** 6 of 36 rows tag; range 38.39–42.04 around 40.26 mean. Distribution across 12 values three rows each. Companion to finding 3 — the spread curve-side tail widened on both min and max axes today; combined with the `maxCurve` MEDIUM (finding 5), spread now flags THREE simultaneous curve/realizedAbs MEDIUMs, replacing yesterday's single `settlementLegs` HIGH.
5. **[MEDIUM] Spread `maxCurve` outlier_pct 16.67% — second consecutive at same magnitude.** 6 of 36 rows tag; range 67.80–75.25 around 70.88 mean (yesterday: 68.34–73.96 around 70.85, also 16.67%). Same magnitude, marginally wider range. First column in the record to flag ≥ 10% in the same mode two consecutive runs at the same magnitude — but since it did not strengthen (still 16.67%, not escalating), classify as sustained-jitter rather than persistence signal. Watch: third consecutive ≥ 10% would escalate the persistence question.
6. **[MEDIUM] Basket `minCurve` outlier_pct 16.67% — second consecutive at same magnitude.** 6 of 36 rows tag; range 0.1055–0.1155 around 0.1115 mean (yesterday: 0.111–0.120 around 0.116, also 16.67%). Same magnitude, range shifted slightly downward. Same sustained-jitter classification as finding 5. Distribution has a doubled-count internal mode at 0.112 and 0.1125 (6 rows each) — mild shape change from yesterday's more even spread.
7. **[LOW — sixth consecutive filed run at zero |r|≥0.8].** No mode fires any strong-pair candidate today. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, today. Sub-threshold within-mode churn for x402 pair set: `settlementLegs × x402Total` = −0.110 (was +0.028 on 07-18 — sixth sign-flip in six rotations); `seed × x402Total` = −0.447 (was +0.559 on 07-18 — 1.0 swing, sign flipped); `maxSpot × x402Total` = −0.233 (was +0.248 on 07-18 — sign flipped); `realizedAbs × x402Total` = −0.583 (new sub-threshold appearance, was −0.010 on 07-18); `wallet_sum_pnl × x402Total` = +0.635 (float-dust artifact per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]], excluded). No pair strengthens two runs in a row.
8. **[LOW — recurring] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202607201`–`202607209`), 36 rows 10-char seed (`2026072010`–`2026072012`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
9. **[LOW — recurring] x402Total inside the ~$100 stable band, twelfth consecutive run.** Mean $321.16, range $270.53–$375.65, sum $11,561.64 across 36 x402 rows. Mean drifted $1.40 lower vs 07-18 ($322.56); range widened slightly on the high end vs 07-18 ($285.22–$361.99). No x402Total outlier flag today (bimodal-by-design exclusion holds anyway).
10. **[LOW — recurring] Spread wallet_sum_pnl outliers 8.33%, still float dust.** 3 of 36 rows tag whiskers; absmax = 1.55e−11 (yesterday 3.03e−11). Σ σ across modes: basket 2.94e−14, spread 5.65e−12, synth/x402 1.63e−14. Spread σ finally reversed the three-run drift-up streak (07-16 3.22e−12 → 07-17 5.37e−12 → 07-18 5.86e−12 → today 5.65e−12) but still float dust twelve orders below any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
11. **[LOW — collapse of 07-18 leaders]** Every non-basket-minCurve, non-spread-maxCurve flag from 07-18 collapsed to 0% today. The 33.33% basket maxCurve HIGH, the 25% spread settlementLegs HIGH — both gone. Meanwhile spread grew three new curve/realizedAbs MEDIUMs (findings 3–5). The outlier surface rotated from the basket-curve-tail + spread-settlementLegs pattern to a pure spread-curve/realizedAbs cluster within one CSV. 12-seed rotation-not-persistence rule reasserted after 07-18's partial break.
12. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −6.57e−15 (basket), −1.18e−12 (spread), −8.71e−15 (synth), −8.71e−15 (x402). Std 2.94e−14 (basket), 5.65e−12 (spread), 1.63e−14 (synth/x402). All twelve-plus orders under the |mean|>10 / std>100 gates. Spread σ off the drift-up streak (was widening 07-16 → 07-17 → 07-18); basket σ tightened by ~2× vs 07-18.
13. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (max |diff role_pnl| = 0.0, max |diff realizedAbs| = 0.0). Only `x402Total` distinguishes x402.
14. **[PASS] Row/column shape unchanged vs 07-18.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold. Live/settlement/spotSource per-mode assignments unchanged.
15. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −6.57e−15  | 2.94e−14  | OK |
| spread    | −1.18e−12  | 5.65e−12  | OK |
| synthetic | −8.71e−15  | 1.63e−14  | OK |
| x402      | −8.71e−15  | 1.63e−14  | OK |

## Within-mode signal

- **Spread curve/realizedAbs cluster — three simultaneous MEDIUMs on the same mode (realizedAbs, minCurve, maxCurve all 16.67%).** First same-run three-way cluster in the current record. All within IQR-tight-band shape, consistent with 12-seed jitter concentrating on the spread mode. Spread `realizedAbs × maxCurve` sub-threshold at r=+0.23 (yesterday r=+0.230), `realizedAbs × minCurve` at r=−0.530 (yesterday r=−0.771 — weakened), `realizedAbs × maxSpot` at r=+0.178 (yesterday +0.191) — none strengthens; cluster surface without correlation surface.
- **Basket `minCurve` MEDIUM — 16.67% (6/36), second consecutive at same magnitude.** Range 0.1055–0.1155 around 0.1115 mean. Distribution has doubled-count internal modes at 0.112 and 0.1125 (6 rows each) — mild shape change from yesterday's more even spread. Companion to spread `minCurve` — two of four modes flag minCurve on the same run, first cross-mode co-appearance for minCurve in the current record.
- **Basket `maxCurve` 33.33% → 0% — three-run strengthening streak retired.** Distribution went flat across 11 values (0.2355 to 0.265, single doubled mode at 0.252). Mean drifted 0.256–0.261 → 0.2485. The 07-18 escalation gate resolved to "12-seed IQR-tight-band artifact" — sustained ≥ 20% next run does not occur; the "first pair to persist AND strengthen across three consecutive runs" is retired to the rotation cohort.
- **Spread `settlementLegs` 25% → 0% — companion collapse.** Distribution widened from four-value {77:6, 79:9, 80:15, 81:3, 82:3} to seven-value {77:3, 79:3, 80:9, 81:3, 82:9, 83:6, 84:3}, IQR expanded from 1 to 2.25. Third consecutive rotation for `settlementLegs` across modes; rotation pattern intact.
- **role_pnl scale per mode (regime unchanged).** spread σ = $9,892 (μ = −$532) — widened from $8,898/−$450 on 07-18, roughly back to 07-17's $9,610/−$592 shape; basket σ = $34.5 (μ = −$1.44) — narrowed from $43.3/−$0.69 on 07-18 (recall 07-17 was $29.4/+$0.04, so alternating pattern); synth/x402 σ = $12.3 (μ = −$0.22) — narrowed from $14.5/−$0.27 on 07-18. Spread dominates pooled stats by two–three orders of magnitude — per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold with continuing sign-flips.** `settlementLegs × x402Total` = −0.110 (was +0.028 on 07-18, −0.604 on 07-17, +0.732 on 07-16, −0.315 on 07-12, +0.216 on 07-10 — **sixth consecutive sign-flip in six rotations**); `maxCurve × x402Total` = −0.473 (was −0.522 on 07-18 — same sign, weakened marginally); `seed × x402Total` = −0.447 (was +0.559 on 07-18 — 1.0 swing); `minSpot × x402Total` = −0.114 (was −0.206 on 07-18, weakened); `maxSpot × x402Total` = −0.233 (was +0.248 on 07-18 — sign flipped). All within 12-seed churn envelope; none strengthens two runs in a row.

## Row count and shape vs 2026-07-18

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated from mixed spot/settlementLegs to a spread-curve/realizedAbs cluster.** 07-18 fired HIGH flags on basket maxCurve (33.33%) and spread settlementLegs (25%), MEDIUM on spread maxCurve (16.67%) and basket minCurve (16.67%). Today: both HIGHs collapsed to 0%; three new MEDIUMs on spread (realizedAbs 16.67%, minCurve 16.67%, maxCurve unchanged 16.67%); basket minCurve unchanged at 16.67%; basket maxCurve gone. Net: 0 HIGH (down 2), 4 MEDIUM (up from 4 but with different composition — 3 of 4 are new-to-column-mode, 1 unchanged, 3 dropped).

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; six consecutive filed runs with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today after 07-18's partial break. Basket `maxCurve` 33.33% HIGH streak (07-15 0% → 07-16 8.33% → 07-17 16.67% → 07-18 33.33% → today 0%) resolves to "tallest 12-seed IQR spike observed to date" rather than structural signal. Only basket `minCurve` and spread `maxCurve` sustained across the pair 07-18 → today, both at unchanged 16.67% — that's sustained-jitter, not strengthening. Escalation gate: any of the four current MEDIUMs at ≥ 20% next run reopens the persistence question. See [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (today). Sixth sign-flip in six rotations. Continues 12-seed jitter classification until any pair confirms two runs in a row.
- **Skipped day:** 2026-07-19 CSV was present but not analyzed on the day. Two-CSV gap between the prior filed report (07-18) and today's (07-20).
