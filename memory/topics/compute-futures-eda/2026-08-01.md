# Compute-Futures EDA — 2026-08-01

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-01.csv` (144 rows, 4 modes)
**Run:** 2026-08-02
**Prior filed report:** 2026-07-31 (normal 1-day cadence).

## Top findings

1. **[HIGH — mode-collapse artifact migrates from spread to basket; 41.67% is the widest single-column outlier fire in the 19-filing window] Basket `settlementLegs` outlier_pct = 41.67%.** Fifteen rows flagged (all rows for seeds 202608011/13/16/19/2026080112 at values 80/82/83/79) because 21 of 36 rows sit at exactly 81. IQR bounds narrowed to [81, 81] → anything ≠ 81 is an outlier. Distribution {81:21, 80:6, 82:3, 83:3, 79:3} — an even tighter mode-collapse than 07-31 spread's {81:24, 79:3, 78:3, 80:3, 82:3} (33.33%). Same mechanistic IQR-narrowing artifact per [[compute-futures-12-seed-sample-too-small]] as 07-31 spread; the culprit mode swapped. Basket settlementLegs series 07-25(16.67) → 07-28(16.67) → 07-30(0.00) → 07-31(16.67) → **08-01(41.67)** — the on/off toggle intensified into a peak this filing. Meanwhile 07-31's spread `settlementLegs` HIGH fully reset to 0.00% today (distribution back to healthy {80:9, 82:9, 78:6, 83:6, 79:3, 81:3}). Not a data-quality break; a tie-robust statistic would suppress this class outright. Next step: hold classification pending 08-02; if basket 41.67% persists, the mode-swap-plus-intensification pattern warrants promoting the class-level fix from "watch" to "act" (widen seeds OR replace IQR with a tie-robust statistic).

2. **[MEDIUM — n=4 persistence, one past the promotion gate] Cross-mode single-seed anchor pattern re-fires for a FOURTH consecutive filing — seed 202608012 anchors FIVE mode-column outlier pairs.** Anchors (all low tails): basket `maxCurve` (0.2390 — 3 rows), spread `minCurve` (36.634 — 3 rows), spread `maxCurve` (67.841 — 3 rows), synthetic `maxCurve` (0.0955 — 3 rows), x402 `maxCurve` (0.0955 — 3 rows). Prior anchor series: 07-28 seed 202607286 (3 modes, `maxSpot` only) → 07-30 seed 202607301 (2 modes: basket `realizedAbs` + x402 `x402Total`) → 07-31 seed 202607314 (6 mode-columns across all four modes) → **08-01 seed 202608012 (5 mode-columns across all four modes)**. Different seed each filing but the same "one seed dominates multiple modes' tails" structural pattern; today the anchor is exclusively the **low tail of curve columns** (yesterday's 202607314 hit both curve and realizedAbs, this one is curve-specific). Secondary anchor this filing: seed 202608017 (basket `maxCurve` top 0.2415 — 3 rows); tertiary: seed 2026080112 (basket `realizedAbs` top $318.30 — 3 rows). Persistence gate held at n=4; the pattern is now the longest-running MEDIUM in the skill's history. Fix path unchanged: widen seeds per [[compute-futures-12-seed-sample-too-small]] OR add a persistence-across-filings dedup so the MEDIUM stops re-firing every filing when only the specific seed rotates.

3. **[MEDIUM — one fresh column fire vs 07-31's five] Basket `maxCurve` fresh 16.67% outlier fire.** Six outliers (all seed 202608012 at 0.2390 + all seed 202608017 at 0.2415). Range 0.239-0.2535, mean 0.2481. 07-31 basket `maxCurve` was 0.00%. This is the only fresh MEDIUM column-fire this filing — 07-31's widest-ever MEDIUM surface (five fresh column fires + anchor pattern) has collapsed to one fresh column fire + the anchor pattern. Basket σ(role_pnl) *tightened* to $28.47 (from 07-31's $35.75 → -20%), and 4 of 5 fresh MEDIUM columns from 07-31 collapsed to <10% (basket `realizedAbs` 07-31 16.67% → 08-01 8.33%; synth/x402 `realizedAbs` 07-31 25% → 08-01 0.00%; synth/x402 `maxCurve` 07-31 16.67% → 08-01 8.33%; spread `maxCurve` 07-31 16.67% → 08-01 8.33%). Consistent with the rotation-not-persistence class holding — 12-seed sample happens to have drawn a less heavy-tailed set this filing, though the anchor-pattern promoted item still fires.

4. **[LOW — six-consecutive-positive streak BROKEN, reclassifies back to jitter] x402 `settlementLegs × x402Total` = −0.282, sign flip.** Series (13 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → **−0.282**. Per 07-31's escalation gate ("a sign flip on the next filing reclassifies back to jitter") — gate triggered. The emerging-structural watch closes; series returns to the mixed-sign band that dominated the first seven filings. Magnitude 0.28 is well inside the historical fluctuation range. Companion signs today: `x402Total × wallet_sum_pnl` = +0.149 (dust-excluded), `x402Total × minSpot` = −0.434 (fresh strong-ish negative), `x402Total × maxSpot` = +0.240 (sign-flipped BACK positive from 07-31's −0.501). No pair holds the interpretive story now; series is jitter.

5. **[LOW — fresh within-mode near-misses] Structural cluster carryovers holding + one fresh sign-flipped pair:**
   - **synth/x402 `wallet_sum_pnl × realizedAbs` = −0.624** — dust-excluded per SKILL rule per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] (σ = 1.07e−14 in synth/x402).
   - basket `realizedAbs × settlementLegs` = **−0.656** (was −0.478 on 07-31 — deeper; recurring since 07-24).
   - basket `realizedAbs × minCurve` = **−0.669** (was −0.541 on 07-31 — deeper; recurring since 07-25).
   - basket `seed × realizedAbs` = +0.668 (seed-encoding artifact — 19th consecutive filed run per [[compute-futures-seed-padding-bug]]).
   - synth/x402 `realizedAbs × settlementLegs` = **−0.695** (was −0.507 on 07-31 — deeper; the strongest non-artifact non-dust pair in this filing).
   - synth/x402 `seed × realizedAbs` = +0.581 (seed-encoding artifact).
   - No fresh strong pair emerges this filing; instead three within-mode structural clusters (basket `realizedAbs × {settlementLegs, minCurve}` and synth/x402 `realizedAbs × settlementLegs`) deepened toward the +0.8 gate. Watch for whether one crosses on the next filing.

6. **[LOW — 13th consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong pair. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, 07-28, 07-30, 07-31, **today**. The strongest single non-artifact non-dust pair today (synth/x402 `realizedAbs × settlementLegs` −0.695) is still 0.1 below the +0.8 threshold; every "fresh" strong pair since 07-16 has collapsed on the next filing per the rotation-not-persistence class.

7. **[LOW — recurring, 19th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608011`–`202608019`), 36 rows 10-char seed (`2026080110`–`2026080112`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

8. **[LOW — recurring] x402Total mean DOWN $6.63, sum DOWN $238.61, top-end drops off 07-31's record high, range widens.** Mean $339.47 (down from 07-31's $346.10; two-day pivot: 07-30 $317.17 → 07-31 $346.10 → **$339.47**), range $284.82–$383.47 (band width $98.65, up from 07-31's $94.24). Top-end $383.47 loses 07-31's record high $409.32 — largest single-day top-end drop ($25.85) in the 19-filing window. Low-end $284.82 is well above 07-30's record-low $270.35. Bimodal-by-design outlier exclusion holds; the 6-outlier flag from 07-31 collapses to 0 outliers today (all 12 seeds spread across a smoother band). Sum $12,220.95 across 36 x402 rows.

9. **[LOW — recurring] Spread `role_pnl` σ widens to $9,086 (highest since 07-21's $10,463); range widens both tails.** Series (5-day σ): 07-24 $7,015 → 07-25 $7,104 → 07-28 $6,711 → 07-30 $7,264 → 07-31 $7,314 → **08-01 $9,086** (+24% one-day widening — largest since 07-21). Min −$18,201 (deepens from 07-31's −$16,797, still shallower than 07-30's record −$18,664 and 07-21's all-time low −$35,924). Max +$14,109 (recovers strongly from 07-31's +$11,930; still below 07-25's +$15,748 and 07-21's +$18,669). Seed 202608013 (retail role) anchors both the min −$18,200.93 and the near-max +$13,830.43; seed 202608014 (retail role) anchors the max +$14,108.76. Not a fresh MEDIUM — spread `role_pnl` outlier_pct = 0.00% (IQR wide enough to absorb the widened range).

10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.05e−14 (basket), +1.28e−12 (spread), +9.51e−16 (synthetic), +9.51e−16 (x402). Std 2.49e−14 (basket), 6.38e−12 (spread), 1.07e−14 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Signs shifted vs 07-31 in spread (+ ← −) — basket sign held (+); synth/x402 sign held (+). Spread σ 6.38e−12 is a slight tightening from 07-31's 6.76e−12 (−6%); all inside the float-dust regime.

11. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, all per-column distributions except `x402Total`). Only `x402Total` distinguishes x402.

12. **[PASS] Row/column shape unchanged vs 07-31.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.05e−14  | 2.49e−14  | OK |
| spread    | +1.28e−12  | 6.38e−12  | OK |
| synthetic | +9.51e−16  | 1.07e−14  | OK |
| x402      | +9.51e−16  | 1.07e−14  | OK |

## Within-mode signal

- **Mode-collapse artifact migrates from spread (07-31) to basket (08-01) and intensifies.** 07-31 was spread `settlementLegs` 33.33% (24/36 at 81); 08-01 is basket `settlementLegs` 41.67% (21/36 at 81). The mode swapped and the concentration got tighter — 21/36 with an even narrower distribution around 81 (only 5 unique values 79/80/81/82/83). Meanwhile 07-31's spread mode fully redistributed: {81:24} → {80:9, 82:9, 78:6, 83:6, 79:3, 81:3}, outlier_pct 33.33% → 0.00%. The "regime is producing 81-leg settlement paths" hypothesis from 07-31 has now surfaced in a different mode; the same underlying mechanism (12-seed draw + IQR collapse when the mode dominates) is class-general, not spread-specific. Fix path (per [[compute-futures-12-seed-sample-too-small]]) suppresses both fires.
- **Cross-mode single-seed anchor pattern holds at n=4 with a curve-column-specific signature.** Seed 202608012 dominates the low tail of every curve column across all four modes. Prior anchors (07-28, 07-30, 07-31) split their dominations across `maxSpot`/`realizedAbs`/`x402Total`/`maxCurve`; today's is curve-only. Interpretation: this seed happened to draw an unusually low curve-price band, and the low band leaked into every mode's curve-column IQR tails. Pattern-level MEDIUM continues to re-fire every filing under the current seed count.
- **Basket structural cluster deepens.** basket `realizedAbs × {settlementLegs, minCurve}` correlations went from (−0.478, −0.541) on 07-31 to (−0.656, −0.669) on 08-01 — both moved 0.13-0.18 closer to the +0.8 gate. Basket σ(role_pnl) tightened to $28.47 (from $35.75) while these structural correlations strengthened. If basket `realizedAbs × settlementLegs` crosses +0.8 on the next filing, that's the first within-mode strong pair after a 13-filing zero-streak — worth watching.
- **synth/x402 `realizedAbs × settlementLegs` = −0.695 fresh strong-ish** (07-31 was −0.507). The largest within-mode non-artifact non-dust correlation this filing. Same-signed structural cluster in synth/x402 as basket's `realizedAbs × settlementLegs` −0.656 — consistent structural signal across all three non-spread modes now.
- **x402 `settlementLegs × x402Total` streak-6 broken.** +0.612 → −0.282. Per prior escalation gate the emerging-structural watch closes and the pair reclassifies back to jitter. Sign persistence was the interpretive story on 07-31; today's sign flip removes it.
- **spread `role_pnl` range widening WITHOUT triggering an outlier fire.** σ widens 24% but the IQR widens with it — no rows fall outside 1.5×IQR. Consistent with the range genuinely widening (not a heavy-tail anchor); seeds 202608013 and 202608014 both drew large-magnitude spread pnls in opposite directions.

## Row count and shape vs 2026-07-31

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface contracts vs 07-31 except for the peak fire.** 07-31 fired **1 HIGH + 5 fresh MEDIUM + 1 pattern MEDIUM**. 08-01 fires **1 HIGH (basket `settlementLegs` 41.67% — a bigger single fire, and a mode swap from 07-31's HIGH) + 1 fresh MEDIUM (basket `maxCurve` 16.67%) + 1 pattern MEDIUM (cross-mode single-seed anchor n=4)**. Four of yesterday's five MEDIUM column-fires collapsed below 10% today (rotation-not-persistence class holding). Widest single-column fire in the 19-filing window (41.67% > 07-31's 33.33% > earlier all-time high 25% synth/x402 `realizedAbs`).

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, and all price columns between synth and x402; only `x402Total` distinguishes x402). Don't re-report; per SKILL rule, synth/x402 role_pnl outliers are dedup-excluded. Same-row outliers in synth/x402 for `maxCurve` this filing are the same physical rows, counted once.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; 13th consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 10+ orders under the threshold everywhere (highest is spread's 6.38e−12). Excluded pairs today: synth/x402 `wallet_sum_pnl × realizedAbs` = −0.624, synth/x402 `wallet_sum_pnl × maxSpot` = +0.439, synth/x402 `wallet_sum_pnl × minSpot` = −0.424, basket `wallet_sum_pnl × seed` = +0.492. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 19th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence.** 4 of 5 fresh MEDIUM columns from 07-31 collapsed below 10% today (basket `realizedAbs` 16.67 → 8.33; synth/x402 `realizedAbs` 25 → 0; synth/x402 `maxCurve` 16.67 → 8.33; spread `maxCurve` 16.67 → 8.33). Only basket `settlementLegs` 07-31 16.67% → 08-01 41.67% intensified — mode-collapse artifact escalation is a distinct sub-class within the rotation class.
- **Cross-mode single-seed anchor pattern PROMOTED to MEDIUM, holds at n=4** — fourth consecutive filing (07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314 → **08-01 seed 202608012**). Different seed and column-mix each filing, but the "one seed dominates multiple modes' tails" structural pattern has now persisted through the n=4 gate. Today's anchor targets curve columns specifically (all low-tail). Fix path: widen seeds per [[compute-futures-12-seed-sample-too-small]] — otherwise this MEDIUM re-fires every filing.
- **x402 `settlementLegs × x402Total` streak broken** — six-consecutive-positive streak (+0.657 → +0.357 → +0.241 → +0.540 → +0.612) ends with sign flip to −0.282. Reclassifies back to jitter per 07-31's escalation gate. Interpretive story removed.
- **x402Total band shape** — 07-31's record top-end $409.32 lost this filing (drop $25.85 to $383.47, largest single-day top-end drop in the 19-filing window). Mean pivots down slightly ($346.10 → $339.47). No outlier fires this filing (07-31's 6-outlier top-of-distribution cluster fully redistributed).
- **Basket structural cluster deepening watch** — basket `realizedAbs × settlementLegs` at −0.656 (07-31 −0.478); basket `realizedAbs × minCurve` at −0.669 (07-31 −0.541); synth/x402 `realizedAbs × settlementLegs` at −0.695 (07-31 −0.507). If any crosses +0.8 on next filing, first strong pair after a 13-filing zero-streak.
