# Compute-Futures EDA — 2026-08-13

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-13.csv` (144 rows, 4 modes)
**Run:** 2026-08-14
**Prior filed report:** 2026-08-12 (single-day gap).

## Top findings

1. **[MEDIUM — extension, promoted class n=7 consecutive filed runs, counter-signal REVOKED] Spread `role_pnl` top-5 losses 100% retail AND top-5 gains 0% retail; new anchor 202608137 retail −$23,104.** Top-5 losses: 202608137 retail −$23,104 / 202608134 retail −$19,691 / 202608138 retail −$11,177 / 202608131 retail −$11,128 / 202608135 retail −$10,300. Top-5 gains: 4-of-5 operator + 1 analyst, **0 retail** (vs 08-12's 1-retail counter-signal at +$14,006). Retail mean spread P&L **−$9,473** (deeper by $3,489 vs 08-12's −$5,984) with std $6,919; operator +$5,246 (std $4,219); analyst +$2,620 (std $4,172). Extends [[compute-futures-spread-retail-loss-concentration]] from n=6 to **n=7 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11, 08-12, 08-13). Loss-floor shallower by $1,137 (08-12 −$24,241 → 08-13 −$23,104); 08-12's single-day bimodal counter-signal did not persist. Under IQR fence, spread `role_pnl` at 2.78% today (n=1, the −$23,104 anchor).

2. **[LOW — promotion CEMENTED, n=4 consecutive filed runs] basket/synth scale-ratio HELD at 3.025× exactly for `maxSpot`/`minSpot`.** Four-filing series (08-10, 08-11, 08-12, 08-13). `maxSpot` basket/synth: min=max=3.0250 (deterministic constant, 12/12 seeds). `minSpot` basket/synth: min=max=3.0250 (deterministic constant, 12/12 seeds). `maxCurve` basket/synth: 3.0149–3.0329 (mean 3.0218, small per-seed jitter). `minCurve` basket/synth: 3.0000–3.0444 (mean 3.0177). `realizedAbs` basket/synth: 2.9157–3.4330 (mean 3.1451 — wider band, matches 08-12's 3.16). Promotion criterion (n=3) HIT on 08-12 and unfiled; today extends to n=4. **Overdue promotion candidate** — write atomic [[compute-futures-basket-synth-3025x-multiplier]] on next filing.

3. **[LOW — n=5 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, 08-12 zero, **08-13 zero**. Extends 08-12's n=4 to n=5. Highest within-mode pair today: **x402 `settlementLegs × x402Total` = 0.771** (up sharply from 08-12's +0.255, ~3× magnitude jump, sub-threshold near-fire). Non-x402 top pairs: synth `minCurve × maxCurve` = 0.738, basket `minCurve × maxCurve` = 0.735 (curve-endpoint anti-symmetry inside modes, sign is negative). n=12 seeds still systematically insufficient to stabilize any within-mode |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced at n=5.

4. **[LOW — x402Total range CONTRACTS on both sides simultaneously] x402Total range collapses to $263.71–$373.84 (std $37.94).** 08-12 was $249.56–$429.30 (range $179.74); today $263.71–$373.84 (range $110.13, **−$69.61 collapse**). New LOW $263.71 above 08-12's $249.56 by $14.15 (single-day floor lift). New HIGH $373.84 well below 08-12's $429.30 by $55.46 (single-day ceiling drop). Mean $327.00 (−$3.41 vs 08-12's $330.41). Sum $11,772.15 (−$122.66 vs 08-12's $11,894.81). Std $37.94 (down from $45.40 on 08-12). LOW anchor: 202608138 (3-role tie at $263.71); HIGH anchor: **padded seed 2026081312** (3-role tie at $373.84). 08-12's both-sided expansion did NOT persist; today is symmetric contraction. Ten-filing range: LOW-side $249.56 (08-12) → **$263.71 (08-13)**; HIGH-side $429.30 (08-12) → **$373.84 (08-13)**. x402Total outlier_pct = 0% in x402 mode today per SKILL bimodal exclusion.

5. **[LOW — full collapse] 08-12's cross-mode 2-seed synth/x402 `settlementLegs` cluster (202608126 + 202608129) fully collapses.** Today: zero `settlementLegs` outliers in any mode (basket 0%, spread 0%, synth 0%, x402 0%). A 3-role sub-threshold LOW cluster remains at seed 202608138 (basket=73, spread not applicable, synth=74, x402=74) but the IQR fence tightens (spread q1/q3 = 79.50/81.25 vs 08-12 wider band) and no mode fires at ≥10%. Rotation-not-persistence continues on `settlementLegs`: 08-10 basket fired → 08-11 collapsed → 08-12 synth/x402 fired → **08-13 fully collapses**. Column-rotation across cross-mode ≥10% MEDIUMs across the last five filings: settlementLegs (08-10) → maxSpot (08-11) → settlementLegs (08-12) → **zero cross-mode ≥10% MEDIUMs (08-13)**.

6. **[LOW — anchor concentration shape SHIFTS to super-single] One super-single anchor 202608137 (13 outlier positions), with 202608132 (9) + 2026081311 (9) as secondary tier.** Full concentration: 202608137 (13 — spread retail-loss floor + spread realizedAbs HIGH + basket minCurve LOW cluster + more), 202608132 (9 — basket/synth minSpot LOW), 2026081311 (9 — basket/synth maxCurve HIGH), 202608135 (3 — spread maxSpot HIGH), 202608131 (2 — synth/x402 role_pnl). Class series: 08-10 super-single dense (202608106, 15 pos) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → **08-13 super-single dense (202608137, 13 pos) + 9+9 secondaries**. Reverts to 08-10-shape after two split-super filings. Anchors fully rotate — zero overlap with 08-12's set.

7. **[LOW — padded-seed over-representation resolves to row-share] Padded seeds (2026081310/11/12) contribute 9 of 36 outlier positions (25%) vs 25% row-share.** 08-12 padded seeds were over-indexed at 42% (13/31). Today drops back to proportional — 2026081311 alone carries 9 of the 9 padded-seed positions (all 9 from basket/synth maxCurve HIGH cluster). 2026081310 zero outlier positions; 2026081312 zero outlier positions (except anchoring the x402Total HIGH tie sub-threshold). Class **not persistent** — 08-12's over-representation was single-filing artifact of 2026081211 super-anchor.

8. **[LOW] x402 `settlementLegs × x402Total` swings from +0.255 (08-12) to +0.771 (08-13).** 21-filing series: +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → +0.179 → +0.475 → +0.558 → +0.255 → **+0.771**. Sign held +; magnitude jumps ~3× and approaches (but does not cross) the |r|≥0.8 threshold. Alternation-not-persistence continues.

9. **[LOW — recurring, 27th filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608131`–`202608139`), 36 rows 10-char seed (`2026081310`–`2026081312`). `seed` outlier_pct = 25.00% in every mode. Today padded 2026081311 anchors cross-mode `maxCurve` HIGH (9 positions). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

10. **[LOW — non-promotable jitter] Conservation float-dust today.** `wallet_sum_pnl` mean per mode: basket **−5.18e−16** (vs 08-12 −1.60e−14; magnitude ~31× smaller, sign held −), spread **−6.70e−13** (vs 08-12 +2.04e−12; **SIGN FLIPPED** + → −, magnitude ~3× smaller), synth **−2.21e−15** (vs 08-12 +3.77e−15; **SIGN FLIPPED** + → −, similar magnitude), x402 **−2.21e−15** (identical to synth by [synth ≡ x402]). All modes remain 12+ orders under the |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outliers: basket 0%, spread **16.67%** (float-dust, excluded), synth 8.33% (float-dust), x402 8.33% (float-dust). σ still ≥8 orders under 1e-6 per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

12. **[PASS] Row/column shape unchanged vs 08-12.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −5.18e−16  | 4.92e−14  | OK |
| spread    | −6.70e−13  | 5.53e−12  | OK |
| synthetic | −2.21e−15  | 1.06e−14  | OK |
| x402      | −2.21e−15  | 1.06e−14  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=5 consecutive filed runs.** Highest today: x402 `settlementLegs × x402Total` = 0.771 (near-fire, up ~3× vs 08-12); non-x402 top pairs are curve-endpoint anti-symmetry (basket/synth `minCurve × maxCurve` ~0.735).
- **Retail-role loss concentration in spread — n=7 consecutive filed runs, counter-signal REVOKED.** 08-12's single-day bimodal counter-signal (retail also holding top gain) fully retracts today: 5/5 top losses retail, 0/5 top gains retail. Retail mean $3,489 deeper vs 08-12.
- **basket/synth `maxSpot`/`minSpot` scale-ratio = 3.025 (deterministic) at n=4 filed runs.** Cemented — overdue promotion write [[compute-futures-basket-synth-3025x-multiplier]] on next filing.
- **x402Total range contracts on both sides** — 08-12's both-sided expansion did not persist; today symmetric contraction (LOW +$14.15, HIGH −$55.46, range −$69.61).
- **Cross-mode ≥10% MEDIUM count drops to zero** — 08-12's synth/x402 settlementLegs cluster fully collapses. Rotation-not-persistence class continues.
- **Anchor concentration reverts to super-single dense (202608137 at 13 positions)** — matches 08-10 shape after two split-super filings.
- **Padded-seed over-representation resolves to row-share** — 25%/25% today vs 08-12's 42%/25%.

## Row count and shape vs 2026-08-12

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface CONTRACTS from 08-12 → 08-13.** 08-12: 0 HIGH, 3 MEDIUM (cross-mode settlementLegs at 16.67%, retail-loss n=6, x402Total both-sided range expansion). 08-13: 0 HIGH, **1 MEDIUM** (retail-loss n=7 with counter-signal revoked). Cross-mode ≥10% count drops from 1 to 0. Only the retail-loss promoted class carries forward; both fresh 08-12 MEDIUMs (settlementLegs cluster, x402Total range) collapse/reverse.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's minSpot/minCurve/maxCurve outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (3.025× exactly for maxSpot/minSpot, ~3.02× for curves)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable across 08-10, 08-11, 08-12, 08-13 (n=4).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers at 0%/16.67%/8.33%/8.33% today — all float dust.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 27th consecutive filed run. Padded 2026081311 anchors cross-mode `maxCurve` HIGH today (9 positions). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-12's cross-mode settlementLegs MEDIUM collapses; 08-12's x402Total both-side expansion reverses to contraction; only 08-12's retail-loss promoted class carries forward and deepens. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration reverts to super-single dense** — series: 08-10 super-single dense (202608106, 15 pos) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → **08-13 super-single dense (202608137, 13 pos) + 9+9 secondaries**. Anchors fully rotate; zero overlap with 08-12's anchor set.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=7 (08-13).** 08-12's single-day bimodal counter-signal (retail also top gain) does NOT persist; today reverts to pure retail-loss / zero-retail-gain shape.
