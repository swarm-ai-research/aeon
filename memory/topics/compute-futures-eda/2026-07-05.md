# Compute-Futures EDA — 2026-07-05

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-05.csv` (144 rows, 4 modes)
**Run:** 2026-07-05

## Top findings

1. **[MEDIUM] basket `wallet_sum_pnl` outlier_pct = 16.67% (6/36)** — six rows flagged, but all six are float dust (|max| = 6.4e−14). The distribution is centered at zero with std 2.6e−14; IQR fires on relative rank inside a machine-epsilon cloud, not on any real conservation break. Same alarm-without-signal shape as prior days' single-mode outlier flips. Do not escalate: conservation gate (|mean|>10 or std>100) holds at 8+ orders of magnitude of headroom. [[compute-futures-12-seed-sample-too-small]]
2. **[MEDIUM] synthetic/x402 `minCurve` outlier_pct = 16.67% (6/36 each)** — six rows in each mode sit at 0.0420, below the IQR fence built from a very tight upper cluster (μ=0.0441, σ=0.0009, range 0.0420–0.0455, six unique values). Same physical cluster reported twice per the synthetic ≡ x402 tautology. Yesterday's synthetic/x402 `settlementLegs` 16.67% flag is 0% today; today's `minCurve` 16.67% flag is new. Same 12-seed IQR flipping between columns each run. No underlying regime change. Next step unchanged: bump seed count in `prototypes/compute-futures/scenario-sweep.mjs` to ≥30. [[compute-futures-12-seed-sample-too-small]]
3. **[LOW] spread top pair rotated `realizedAbs × minSpot` (−0.745, 2026-07-03) → `minSpot × maxCurve` (+0.682, today)** — yesterday's promotion watch ("real if it holds two days") is now void: `realizedAbs × minSpot` collapsed to +0.048 today. New top pair `minSpot × maxCurve` +0.682 mirrors the same pairing that leads in basket (+0.593) and synthetic/x402 (+0.621), so it's the cross-mode spot/curve floor–ceiling coupling showing through inside a mode rather than a spread-specific signal. Sub-threshold either way. No action.
4. **[LOW] x402-specific `settlementLegs × x402Total` r = +0.557 (was +0.685 on 2026-07-03)** — still the only x402-specific structural link, still sub-threshold, weaker than yesterday. Mechanically plausible (more legs → more x402 charges). No action.
5. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows carry 9-digit seeds, 36 rows carry 10-digit (`2026070510`, `2026070511`, `2026070512`). Outlier detector flags `seed` at exactly 25% in every mode, tenth consecutive run. One-line pad in `scenario-sweep.mjs` still pending — see [[compute-futures-seed-padding-bug]].
6. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans −1.9e−15 → −1.6e−12, std 9.6e−15 → 4.7e−12 — float dust across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate. basket std widened slightly vs 2026-07-03 (2.6e−14 vs 1.7e−14) but still 15 orders of magnitude below the gate.
7. **[PASS] No within-mode correlations at |r| ≥ 0.8.** Top per mode: basket `wallet_sum_pnl × minSpot` +0.635, spread `minSpot × maxCurve` +0.682, synthetic/x402 `minSpot × maxCurve` +0.621, x402-only `settlementLegs × x402Total` +0.557. `minSpot × maxCurve` now leads three of four modes — a plausible within-mode floor/ceiling coupling that would be interesting at |r|≥0.8, but isn't there yet in any single mode.
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402 (μ=$336.33, σ=$34.49, range $296.55–$420.73, total $12,107.70 — warmer than 2026-07-03's $273.65–$366.67).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.38e−15  | 2.57e−14  | OK |
| spread    | −1.57e−12  | 4.66e−12  | OK |
| synthetic | −1.88e−15  | 9.59e−15  | OK |
| x402      | −1.88e−15  | 9.59e−15  | OK |

## Within-mode signal

- **role_pnl scale per mode (regime unchanged):** spread std ≈ $8,107 (μ=−$526), basket std ≈ $31.93 (μ=−$0.80), synthetic/x402 std ≈ $12.52 (μ=−$0.43). All three scales widened modestly vs 2026-07-03 ($8,204 / $26.89 / $10.16) but well inside 12-seed jitter. spread continues to dominate pooled stats by three orders of magnitude — per-mode stratification remains the right default.
- **`minSpot × maxCurve` leads three of four modes (basket +0.593, spread +0.682, synthetic/x402 +0.621).** Same directional sign, similar magnitude across regimes with very different price scales — consistent with a floor/ceiling coupling that survives the mode split. Not strong enough to promote yet. Watch: if any single mode crosses 0.8 twice in a row it's real.
- **spread top-pair collapsed from yesterday.** `realizedAbs × minSpot` went from −0.745 (2026-07-03) → +0.048 (today), which invalidates yesterday's "watch for two-day confirmation" flag. Twelve-seed noise as expected. `minSpot × maxCurve` is now spread's top pair.
- **x402 mirrors synthetic exactly at the role level** — role-level numeric columns differ by 0.0. Only x402-specific structure is `settlementLegs × x402Total` r=+0.557 (down from +0.685 yesterday) and the x402Total distribution itself.

## Row count and shape vs 2026-07-03

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged: 9/12 nine-digit, 3/12 ten-digit).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single- and low-double-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string. One-line pad in `scenario-sweep.mjs` still pending.
- **12-seed outlier_pct instability** — mode-column pair that fires shifts each run: 2026-06-26 spread minCurve 25% → 2026-06-28 spread settlementLegs 33% → 2026-07-03 synthetic/x402 settlementLegs 16.67% → 2026-07-05 basket wallet_sum_pnl 16.67% + synthetic/x402 minCurve 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `realizedAbs × minSpot` went [−0.06 (06-26), −0.745 (07-03), +0.048 (07-05)]; spread `wallet_sum_pnl × minSpot` went [+0.362 (06-26), −0.745 (06-28), +0.362 (07-03), −0.226 (07-05)]. Consistent 12-seed jitter until confirmed twice.
