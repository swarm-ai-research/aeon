# Compute-Futures EDA — 2026-07-12

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-12.csv` (144 rows, 4 modes)
**Run:** 2026-07-12
**Prior filed report:** 2026-07-10 (no 2026-07-11 CSV in the proof stream; comparisons here are against the 07-10 file)

## Top findings

1. **[MEDIUM] spread `settlementLegs` outlier_pct 16.67% (was 0.00% on 07-10).** Six of 36 spread rows outside IQR whiskers around q25=79 / q75=80.25: three rows at 76, three at 84, majority band {79×9, 80×12, 81×6, 78×3}. Discrete integer distribution, mean 79.75 / std 1.86. `settlementLegs` fires a MEDIUM outlier column for the third time in the run window — basket at 25% on 07-10, spread at 33% on 06-28, spread again today. Same 12-seed jitter shape as prior single-day fires: the (mode, column) rotates each rotation. Watch: if spread `settlementLegs` > 10% recurs on the next CSV, escalate to signal candidate [[compute-futures-12-seed-sample-too-small]].
2. **[LOW — collapse of 07-10 leader] basket `settlementLegs` 25.00% → 0.00%.** Yesterday's headline flag fully collapsed within one seed rotation. Basket settlementLegs distribution now sits cleanly inside its IQR whiskers with no rows tagged. Same one-day-alarm-without-signal shape as every prior 12-seed outlier fire.
3. **[LOW — collapse of 07-10 second leader] spread `minSpot` 16.67% → 8.33%.** Three of 36 rows still tag (mean $43.12, std $1.88, range $38.92–$46.23), but the outlier_pct drops back below the MEDIUM gate. Underlying regime unchanged. Reverts within one rotation.
4. **[LOW — second consecutive filed run with zero |r|≥0.8 pairs].** No mode fires a strong-pair candidate today. Combined with the 07-10 filing this is two consecutive reported runs at zero |r|≥0.8; not a 3-day streak because no 07-11 CSV exists in the proof stream. Tracked sub-threshold churn: x402 `wallet_sum_pnl × x402Total` +0.088 (07-10) → +0.185 today; x402 `wallet_sum_pnl × settlementLegs` −0.089 (07-10) → +0.071 today; spread `realizedAbs × wallet_sum_pnl` −0.332 (07-10) → −0.016 today. Nothing crosses the |r| ≥ 0.8 filter. Continues to validate [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ(wallet_sum_pnl) at 1.5e−14 (synth/x402) to 4.0e−12 (spread), still twelve+ orders below the gate.
5. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows nine-digit (`202607121`–`202607129`), 36 rows ten-digit (`2026071210`, `2026071211`, `2026071212`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending [[compute-futures-seed-padding-bug]].
6. **[LOW — recurring] x402Total inside the ~$100 stable band.** Mean $335.04, range $301.24–$378.33, sum $12,061.44 across 36 x402 rows. Seventh consecutive run inside the ~$100 band since 07-03 (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400 → 07-09 $283–$387 → 07-10 $281–$384 → today $301–$378). Structural pair `x402Total × settlementLegs` flipped sign from +0.216 (07-10) to −0.315 today; sub-threshold churn, no candidate confirms two runs in a row.
7. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +3.39e−15 (basket) → −1.18e−12 (spread); std 1.47e−14 (synth/x402) → 4.00e−12 (spread). All twelve-plus orders under the |mean|>10 / std>100 gates. spread `wallet_sum_pnl` tightened vs 07-10 (mean −2.63e−12 → −1.18e−12; std 6.43e−12 → 4.00e−12).
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff| = 0.0 across role_pnl, wallet_sum_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
9. **[PASS] Row/column shape unchanged vs 07-10.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold.
10. **[PASS] No CRITICAL/HIGH findings.** No schema drift, no row-count change ≥25%, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +3.39e−15  | 1.97e−14  | OK |
| spread    | −1.18e−12  | 4.00e−12  | OK |
| synthetic | −5.13e−15  | 1.46e−14  | OK |
| x402      | −5.13e−15  | 1.46e−14  | OK |

## Within-mode signal

- **spread `settlementLegs` MEDIUM outlier cluster (16.67%).** Discrete integer column, six rows outside IQR whiskers: three at 76 (low tail), three at 84 (high tail). Majority band 79–80 (21/36 rows). Same 12-seed jitter shape as basket settlementLegs 07-10 — treat as alarm-without-signal until it repeats. Log for the next CSV.
- **role_pnl scale per mode (regime unchanged).** spread σ = $8,977 (μ = −$724) — widened from $8,413/−$390 on 07-10 but inside 12-seed jitter; basket σ = $32.7 (μ = −$0.25) — tightened from $36.6/−$1.15; synthetic/x402 σ = $12.5 (μ = −$0.15) — tightened from $14.3/−$0.38. Spread continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold.** `minSpot × x402Total` −0.207 (was −0.353 on 07-10), `maxSpot × x402Total` +0.206 (was −0.337), `minCurve × x402Total` +0.418 (was +0.267), `maxCurve × x402Total` +0.476 (was +0.569), `settlementLegs × x402Total` −0.315 (was +0.216). Every pair jitters, none strengthens two runs in a row — consistent with the 12-seed churn.
- **spread `wallet_sum_pnl` outlier_pct 8.33%.** Three of 36 rows tagged, min −5.91e−12, max +8.19e−12. Excluded from severity per skill constraints — float-dust magnitude, filter continues to hold [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

## Row count and shape vs 2026-07-10

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synthetic/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- Proof stream note: no 2026-07-11 CSV present in `memory/gitlawb-compute-futures-proofs/`. The 07-10 file is the immediate prior filing for comparison.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; second consecutive filed run with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — the (mode, column) pair that fires each run continues to rotate: 07-08 (all flags 8.33%) → 07-09 spread realizedAbs 16.67% → 07-10 basket settlementLegs 25% + spread minSpot 16.67% → today spread settlementLegs 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — x402 `wallet_sum_pnl × x402Total`: +0.881 (07-09) → +0.088 (07-10) → +0.185 (today). Spread `realizedAbs × wallet_sum_pnl`: −0.332 (07-10) → −0.016 (today). Continues 12-seed jitter until any pair confirms two runs in a row.
