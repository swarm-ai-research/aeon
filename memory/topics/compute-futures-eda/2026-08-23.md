# Compute-Futures EDA — 2026-08-23

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-23.csv` (144 rows, 4 modes)
**Run:** 2026-08-24
**Prior filed report:** 2026-08-22 (daily cadence, no gap).

## Top findings

1. **[HIGH — sharpest single-day HIGH-tier RETURN in class series, 0 → 4 mode-mirror-collapsed HIGH clusters] After 3-consecutive filings with 0 HIGH clusters, HIGH-tier stress returns across four columns: basket `minSpot` 33.33% (12/36), basket `minCurve` 25% (9/36), synth/x402 `minSpot` mirror 33.33% (12/36), synth/x402 `minCurve` mirror 33.33% (12/36).** Non-dust mode-mirror-collapsed cluster positions expand **11 → 20 (+82%)**, reversing 08-22's sharpest-single-day contraction. Uncollapsed non-dust positions 30 (vs 11 on 08-22). MEDIUM tier count: 1 (spread `maxCurve` 16.67%, 6/36). LOW clusters: basket `maxCurve` 8.33%, synth/x402 `maxCurve` 8.33%, synth/x402 `settlementLegs` 8.33%. **Stress axis NEWLY EXTENDS beyond spot columns**: 08-14 → 08-22 HIGH/MEDIUM clusters were spot-column only (`minSpot`/`maxSpot`); today, curve columns (`minCurve`, `maxCurve`) contribute 3 of 4 HIGH clusters (basket `minCurve` 25%; synth/x402 `minCurve` mirror 33.33%) — first curve-column HIGH tier in the daily-cadence class series. Cross-mode HIGH cluster series: 0 (08-13→08-18) → 0 (08-19) → 0 (08-20) → 0 (08-21, first MEDIUM peak 5) → 0 (08-22, contraction 0/0) → **4 HIGH + 1 MEDIUM (08-23, joint spot+curve stress return)**. Deployer regime shift candidate — the curve-column stress axis surface is genuinely new signal, not just spot-column reversion.

2. **[HIGH — n=16 consecutive filed runs, retail mean SHALLOWER 28% but total loss $-90,618 vs $-125,088 on 08-22] Spread `role_pnl` retail mean **−$7,551.50** (up $2,873 from −$10,424 on 08-22; largest single-day recovery magnitude in the daily-cadence class series).** Top-5 losses: **202608233 retail −$15,361** (deepest single loss today; below 08-22's class-record 202608228 −$24,618 by $9,257 — class-record still stands) / 202608234 retail −$11,311 / 2026082311 retail −$11,290 / 202608231 retail −$10,864 / 202608236 retail −$10,340 (**5/5 retail — 6th consecutive filed run at 5/5 purity**). Top-5 gains: 202608233 operator +$8,076 / 2026082311 operator +$7,621 / 202608234 operator +$7,018 / 202608231 operator +$6,451 / 2026082312 operator +$5,893 (**5/5 operator, 0 retail — 16-run pattern holds**). Per-role: retail mean **−$7,551.50** (std $5,096, min −$15,361, **max +$3,690** — sole positive retail row today, seed 202608235; **all-losing 08-22 day breaks after 1 filing**); operator mean **+$4,563** (down 28% from +$6,316); analyst mean **+$1,222** (down 59% from +$2,989). Extends [[compute-futures-spread-retail-loss-concentration]] from n=15 to **n=16 consecutive filed runs** (08-06 → 08-08, 08-10 → 08-16, 08-18 → 08-23). **Cross-role symmetry**: 4 seeds each anchor both a top-loss (retail) AND a top-gain (operator) — 202608233 / 2026082311 / 202608234 / 202608231, i.e., an even more concentrated 4-way intra-seed skew than 08-22, but **no triple-role top-5 anchor today** (08-22's 202608228 held retail+operator+analyst all three; today's 202608233 hits retail+operator only, no analyst).

3. **[MEDIUM — class n=5 → n=6, extends 1 past rename threshold] basket/synth spot-price multiplier byte-identical at 2.500000 exact for **6th consecutive filed run**.** `minSpot` per-seed ratio **[2.500000, 2.500000]** std **1.71e-15**; `maxSpot` per-seed ratio **[2.500000, 2.500000]** std **1.15e-15** (12/12 seed pairs both columns; both stds essentially unchanged vs 08-22's 1.52e-15/1.25e-15 — byte-identical). Curve ratios slightly LOOSEN vs 08-22: `minCurve` mean **2.5020** (std **1.91e-02**, ~72% wider than 08-22's 1.11e-02, back near 08-21's 2.27e-02); `maxCurve` mean **2.4996** (std **4.65e-03**, ~11% tighter than 08-22's 5.23e-03 — tightest maxCurve dispersion in 6-run series); `realizedAbs` mean **2.5631** (std **1.32e-01**, ~5% wider than 08-22's 1.26e-01, but recentered from 2.5001 back toward 2.5703 direction — first non-monotonic realizedAbs move in 6 runs). Class series: n=7 at 3.0250 exact (08-10 → 08-16) → invalidated 08-18 with fresh 2.5000 anchor (n=1) → 08-19 (n=2) → 08-20 (n=3) → 08-21 (n=4) → 08-22 (n=5, rename threshold met) → **08-23 (n=6, extends past threshold)**. Promotion [[compute-futures-basket-synth-2.5x-multiplier]] holds; curve dispersion loosening is the first hint of drift within the constant-anchor regime — worth watching if it continues.

4. **[LOW — sign flip back positive, +/-oscillation continues, 08-22's flip was single-day excursion] x402 `settlementLegs × x402Total` returns positive to **+0.464** (vs −0.060 on 08-22, Δ+0.524 largest single-day sign-flip swing in the 7-filing series).** Series: −0.112 (08-16) → +0.036 (08-18) → +0.480 (08-19) → +0.311 (08-20) → +0.542 (08-21) → −0.060 (08-22, single negative excursion) → **+0.464 (08-23)**. Positive-sign regime restores at magnitude 0.86 of 08-21's peak; 08-22's negative excursion did not persist as a regime shift. Other x402 × x402Total pairs today: `maxCurve × x402Total` **+0.512** (sign FLIPS vs 08-22's implicit set; magnitude near 08-22's `maxSpot × x402Total` peak −0.679 but different pair), `minSpot × x402Total` **+0.422** (sign FLIPS vs 08-22's −0.327; magnitude 0.75 gain), `realizedAbs × x402Total` **+0.202** (sign FLIPS vs 08-22's −0.558; magnitude 0.76 collapse). **All 4 x402Total pair-correlations swing back to positive** — the ~zero-band + negative-mode from 08-22 does not persist; today mirrors 08-21's dominant-positive regime more closely.

5. **[LOW — super-anchor RECONCENTRATES to seed 202608231 at 10 positions after 08-22's single-6 trough] Top seed by non-dust outlier positions (mode-uncollapsed) is 202608231 at **10 positions** (basket + spread + synth/x402 minSpot/minCurve/maxCurve triple; single dominant seed across all 4 modes).** 08-22's anchor **202608229 collapses to 0 non-dust positions today** (5th consecutive single-day full-collapse of prior day's super-anchor: 202608194 → 202608205 → dual-tie → 202608229 → 202608231). Second-tier anchors: **seed 202608235 at 5 positions** (basket + synth/x402 minSpot + spread role_pnl retail top-gain + synth/x402 settlementLegs) and **seed 2026082311 at 5 positions** (basket + synth/x402 minSpot + synth/x402 minCurve). Third-tier: 202608238 / 202608233 / 202608234 at 3 each; 202608236 at 1. Class series: 08-13 single 13 → 08-14 single 14 → 08-15 5-anchor rotation → 08-16 single 18 → 08-18 distributed max-3 → 08-19 single 19 → 08-20 single 22 (peak) → 08-21 DUAL 18/18 → 08-22 single 6 (trough) → **08-23 single 10 (mid-band, above 08-18/08-22 troughs, well below 08-19/08-20 peaks)**. Anchor identity does NOT persist between filings; today reads as reconcentration phase between distributed troughs and peak-anchor rotations.

6. **[LOW — n=15 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-22's n=14 to **n=15**. Highest today ex-seed-encoding artifact: **synth/x402 `minSpot × maxCurve` = +0.725** (mirror pair, structural), **basket `minSpot × maxCurve` = +0.715**, **synth/x402 `minSpot × settlementLegs` = +0.708** (fresh appearance; not present at this magnitude on 08-22). Sub-0.7 ceiling continues; n=12 seeds systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced further.

7. **[LOW — range essentially flat, −2.2% vs 08-22, both boundaries drift inward for first time in 3 filings] x402Total range **$264.76–$384.78** (range **$120.02**, down $2.71 from 08-22's $122.73).** LOW **$264.76** — drops $8.44 below 08-22's floor $273.20 (**third consecutive fresh interim low**, still above 08-19's all-time-low $256.38 by $8.38). HIGH **$384.78** — drops $11.15 below 08-22's peak $395.93 (**first ceiling-drop day in 3 filings**, both boundaries drift inward together — new pattern for the series). Mean **$320.39** (down $25.60 from $345.99 — largest one-day mean shift in the series, reversing 08-22's largest one-day upward shift); std **$37.48** (down 2% from $38.20). Series: $83.55 (08-16) → $119.90 (08-18) → $131.66 (08-19) → $88.05 (08-20) → $103.60 (08-21) → $122.73 (08-22) → **$120.02 (08-23)**. Range holds in $[83, 132]$ band across 7 filings; mean-reverting hypothesis reinforced. Ceiling-drop concurrent with floor-drop suggests a downward mode shift, not fresh dispersion.

8. **[LOW — padded contribution back toward proportional] Padded seeds (2026082310/11/12) hold 36 of 144 rows (25%). Today they contribute **5 of 30 non-dust outlier positions (16.7%)** — under-indexed vs row share but well above 08-22's severely-under-indexed 9.1%.** 2026082311 contributes 5 positions (basket + synth/x402 minSpot + synth/x402 minCurve, plus spread retail top-5 loss row and operator top-5 gain row); 2026082310 contributes 0 non-dust positions (still under-indexes); 2026082312 contributes 1 non-dust position (spread operator top-5 gain). Class series: 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% mildly over → 08-16 3.2% severely under → 08-18 50% severely over → 08-19 1.9% severely under → 08-20 21.1% mildly under → 08-21 28.6% mildly over → 08-22 9.1% severely under → **08-23 16.7% mildly under**. Continues rotation-not-persistence pattern; padded encoding does not systematically over- or under-select. Reinforces [[compute-futures-seed-padding-bug]] fix motivation.

9. **[LOW — three-mode sign flip; 08-22's all-negative regime BREAKS after 1 filing] Conservation float-dust regime: basket + synth/x402 flip − → +; spread HOLDS negative.** Per-mode means: basket **+4.58e−15** (vs 08-22 −1.02e−14; sign FLIPS − → +, magnitude 0.45× smaller), spread **−3.15e−13** (vs −6.25e−13; sign HOLDS negative, magnitude 0.50× smaller), synth **+2.15e−15** (vs −2.14e−15; sign FLIPS − → +), x402 **+2.15e−15** (identical to synth by [synth ≡ x402]; sign FLIPS − → +). Regime sequence: 08-15 all-neg → 08-16 all-neg → 08-18 mixed → 08-19 different-mixed → 08-20 same-hold → 08-21 full-inversion → 08-22 all-negative → **08-23 three-mode-flip (all-positive except spread)**. All four modes remain 12+ orders under |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outlier percentages: basket **16.67%** (6/36 — up from 08-22's 8.33%, first time above 8.33% in the class series), spread 0%, synth **25%** (9/36 — up from 08-22's 0%; first non-zero synth `wallet_sum_pnl` outlier day since 08-14). All float dust (magnitude ~1e−14 vs medians ~1e−15); excluded from severity ranking per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: 0.000000e+00 (byte-identical).

11. **[PASS] Row/column shape unchanged vs 08-22.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +4.58e−15  | 2.31e−14  | OK |
| spread    | −3.15e−13  | 5.88e−12  | OK |
| synthetic | +2.15e−15  | 9.37e−15  | OK |
| x402      | +2.15e−15  | 9.37e−15  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). 08-22's all-negative regime BREAKS: basket + synth/x402 flip − → +; spread holds negative (magnitude 0.50× smaller). First three-mode sign-flip event in the 4-filing rotation sequence (last analogous was 08-21's full-inversion, which itself did not persist to 08-22).

## Within-mode signal

- **HIGH-tier stress cluster returns after 3-day suppression, spot AND curve columns both affected** (finding 1) — 4 HIGH clusters (basket `minSpot` 33.33%, basket `minCurve` 25%, synth/x402 `minSpot` mirror 33.33%, synth/x402 `minCurve` mirror 33.33%), 1 MEDIUM (spread `maxCurve` 16.67%). **Curve-column HIGH tier is genuinely new signal** — no prior filing in the daily-cadence series has surfaced `minCurve` at HIGH; spot-column reversion from 08-22's contraction was expected, but joint spot+curve stress reads as deployer regime shift candidate.
- **Retail-role loss concentration in spread — n=16 consecutive filed runs, retail mean SHALLOWER 28% at −$7,551.50** (finding 2). Total retail loss $-90,618 vs 08-22's $-125,088 — largest single-day recovery magnitude in the daily-cadence class series. Class direction and 6th consecutive 5/5 top-5 retail purity hold; **all-losing 08-22 day breaks after 1 filing** (seed 202608235 sole positive retail row at +$3,690). No triple-role top-5 anchor today (08-22's 202608228 held all three; today 202608233 hits retail+operator only, no analyst).
- **basket/synth 2.5000× multiplier extends to n=6, first hint of intra-anchor drift** (finding 3). Spot columns byte-identical (std 1.15e-15 / 1.71e-15, essentially unchanged vs 08-22); curve dispersion loosens (minCurve std 1.11e-02 → 1.91e-02, back near 08-21's 2.27e-02) and realizedAbs recenters from 2.5001 toward 2.5631 (first non-monotonic move in 6 runs). Post-rename-threshold; watch curve dispersion trend if it continues loosening.
- **Zero within-mode strong pairs (|r|≥0.8) — n=15 consecutive filed runs** (finding 6). n=12 seeds decisively insufficient. Highest today ex-seed-encoding: synth/x402 `minSpot × maxCurve` = +0.725; **synth/x402 `minSpot × settlementLegs` = +0.708 is fresh appearance** (not present at this magnitude on 08-22 — worth carrying if it recurs).
- **x402 `settlementLegs × x402Total` sign flips back positive to +0.464** (finding 4); **all 4 x402Total pair-correlations swing back positive**, restoring the 08-21-dominant regime. 08-22's negative excursion was a single-day event, not a regime shift.
- **x402Total range essentially flat at $120.02** (finding 7) — both boundaries drift INWARD (floor $-8.44 to $264.76 = third consecutive fresh interim low; ceiling $-11.15 to $384.78 = first ceiling-drop day in 3 filings). Concurrent floor+ceiling drop suggests downward mode shift, not fresh dispersion. Mean drops $25.60 to $320.39 — largest one-day mean reversal in the series.
- **Super-anchor reconcentrates to seed 202608231 at 10 positions** (finding 5) — mid-band anchor magnitude, well above 08-22's single-6 trough but below 08-19/08-20 single-19/single-22 peaks. 5th consecutive single-day full-collapse of prior day's super-anchor (202608229 → 0 today).
- **Padded-seed contribution 16.7% mildly under-indexed** (finding 8) — recovery from 08-22's severely-under-indexed 9.1%; still below the 25% row share. 2026082311 anchors 5 non-dust positions (single largest padded contributor); 2026082310 contributes 0.
- **Conservation regime: three-mode sign flip to positive; spread holds negative** (finding 9) — 08-22's all-negative regime breaks after 1 filing; basket + synth/x402 flip − → +; spread magnitude 0.50× smaller (still negative). basket `wallet_sum_pnl` outlier_pct 16.67% (first non-8.33% day in class series); synth/x402 `wallet_sum_pnl` outlier_pct 25% mirror (first non-zero synth day since 08-14). Still float dust; excluded from severity ranking.

## Carry-overs

- 25% seed encoding artifact (n=36 filed runs, pending [[compute-futures-seed-padding-bug]] fix).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design.
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.

## Row count and shape vs 2026-08-22

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold.
- basket/synth multiplier constant 2.5000 extends to **n=6 — past rename threshold** (finding 3).
- **Outlier surface EXPANDS SHARPLY from 08-22 → 08-23.** 08-22: 0 HIGH, 0 MEDIUM, 11 non-dust positions (mode-mirror collapsed), single anchor 6. 08-23: **4 HIGH + 1 MEDIUM, 20 non-dust positions (mode-mirror collapsed, +82%), single anchor 10 at seed 202608231**. Net: HIGH-tier RETURNS after 3-consecutive-filing suppression (0/0/0/0 → 4); curve-column HIGH tier is genuinely new class-series signal; joint spot+curve stress reads as deployer regime shift candidate rather than routine reversion of 08-22's contraction.
