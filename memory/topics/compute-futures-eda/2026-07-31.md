# Compute-Futures EDA — 2026-07-31

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-31.csv` (144 rows, 4 modes)
**Run:** 2026-08-01
**Prior filed report:** 2026-07-30 (normal 1-day cadence).

## Top findings

1. **[HIGH — first fresh HIGH since 07-25; IQR-narrowing artifact] Spread `settlementLegs` outlier_pct = 33.33%.** Twelve rows flagged (all rows for seeds 202607315/16/17/19 at values 79/78/80/82); IQR bounds narrowed to [80.375, 81.375] because 24 of 36 rows sit at value 81. Distribution {78:3, 79:3, 80:3, 81:24, 82:3} — the mode-collapse at 81 shrinks the IQR to a single value, so anything ≠ 81 becomes an outlier. Mechanistically HIGH (≥ 20% gate crossed for the first time in 7 filings), but the underlying pattern is the same 12-seed-jitter class per [[compute-futures-12-seed-sample-too-small]]. Next-step: hold classification pending the next filing; if 33.33% persists, escalate as a spread-mode `settlementLegs` congealing regime (i.e. the sim is producing 81-leg settlement paths more consistently), not a data-quality break.

2. **[MEDIUM — persistence gate n=3 CROSSED, promote per SKILL rule] Cross-mode single-seed anchor pattern re-fires for a THIRD consecutive filing — seed 202607314 dominates SIX mode-column outlier pairs simultaneously.** Anchors: basket `realizedAbs` top ($268.38), basket `maxCurve` top ($0.271), spread `maxCurve` low tail ($67.89), spread `role_pnl` second-lowest (−$16,197.42, near seed 2026073110's record −$16,797.42), synth/x402 `realizedAbs` top ($108.13), synth/x402 `maxCurve` top ($0.108). Prior anchor series: 07-28 seed 202607286 (3 modes, maxSpot only) → 07-30 seed 202607301 (2 modes: basket realizedAbs + x402 x402Total) → **07-31 seed 202607314 (6 mode-columns across all four modes)**. Different seed each filing but the same "one seed dominates multiple modes' tails" structural pattern; n=3 threshold per [[compute-futures-12-seed-sample-too-small]] now crossed. Promote to MEDIUM for investigation — the small-n distribution admits a single seed's price-band draw to bleed into every mode's IQR tails simultaneously, which will only close with a wider seed count. Secondary anchors this filing: seed 202607313 (basket `realizedAbs` bottom, synth/x402 `realizedAbs` bottom, x402 `x402Total` NEW record-high $409.32); seed 202607317 (basket `settlementLegs` bottom, spread `settlementLegs`, synth/x402 `maxCurve` low); seed 2026073111 (basket `maxSpot` top, synth/x402 `maxSpot` top); seed 2026073110 (basket `settlementLegs` bottom, spread `role_pnl` record-low −$16,797). Next step: widen seeds per [[compute-futures-12-seed-sample-too-small]] or add a persistence-across-filings dedup so this stops re-firing every filing as MEDIUM.

3. **[MEDIUM — outlier surface rotates fully, five fresh column fires] Column-level MEDIUM count jumps 1 → 5 vs 07-30.** Fresh 10–20% fires this filing:
   - basket `realizedAbs` **16.67%** (6 outliers; anchored by seed 202607313 low tail + seed 202607314 top tail — mean $204.83, range $128.37–$268.38). 07-30 was 0.00%.
   - basket `settlementLegs` **16.67%** (RE-FIRED after collapsing to 0.00% on 07-30). Rotation series 07-25(16.67) → 07-28(16.67) → 07-30(0.00) → 07-31(16.67); on/off toggle holds per [[compute-futures-12-seed-sample-too-small]].
   - spread `maxCurve` **16.67%** (6 outliers; seed 202607314 low $67.89 + seed 202607316 top $76.85). 07-30 was 0.00%.
   - synth/x402 `realizedAbs` **25.00%** (9 outliers each mode, identical rows per synth ≡ x402; three-seed anchor 202607313/12/14). 07-30 was 0.00% in both modes.
   - synth/x402 `maxCurve` **16.67%** (6 outliers each; two-seed anchor 202607317 low $0.0945 + 202607314 top $0.108). 07-30 was 0.00% in both modes.
   Six MEDIUM fires simultaneously (five fresh columns + the promoted anchor pattern) is the widest MEDIUM surface in the 18-filing window. Consistent with a 12-seed draw that happened to sample a seed set with heavier tails than usual; not a schema/config break, but the *count* of simultaneous fires is the noteworthy signal.

4. **[LOW — sixth consecutive positive; magnitude BOUNCES UP to +0.612, above 07-30's +0.540] x402 `settlementLegs × x402Total`.** Series (12 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → **+0.612**. Sixth consecutive positive; the +0.657 → +0.357 → +0.241 monotonic decay has now fully reversed into +0.540 → +0.612 — two-run bounce. Escalation gate: one more positive at any magnitude keeps the emerging-structural watch alive; a sign flip on the next filing reclassifies back to jitter. Companion signs today: `x402Total × wallet_sum_pnl` = −0.181 (dust-excluded), `minSpot × x402Total` = +0.271 (sign-flipped BACK positive from 07-30's −0.292), `x402Total × maxSpot` = −0.501 (fresh strong-ish negative).

5. **[LOW — fresh within-mode near-misses] Three non-artifact within-mode pairs above |r| ≥ 0.65 that were NOT reported yesterday:**
   - synth/x402 `minCurve × settlementLegs` = **+0.681** (fresh — no ≥ 0.6 in synth/x402 for this pair in the reported history).
   - spread `maxCurve × settlementLegs` = **−0.674** (fresh — opposite sign of basket's +0.407 this filing; distinct structural signal in spread mode).
   - basket `seed × minCurve` = +0.676 (recurring seed-encoding artifact per [[compute-futures-seed-padding-bug]] — not real structure).
   Non-artifact near-misses in the +0.5–0.6 band (structural cluster carryovers from prior filings): spread `minCurve × realizedAbs` = −0.597, spread `minCurve × minSpot` = +0.536, synth/x402 `minCurve × realizedAbs` = −0.531, synth/x402 `maxCurve × realizedAbs` = +0.506, spread `maxCurve × minCurve` = +0.505, x402 `x402Total × maxSpot` = −0.501.

6. **[LOW — 12th consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong pair. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, 07-28, 07-30, **today**. The strongest single non-artifact pair today (synth/x402 `minCurve × settlementLegs` +0.681) is well below the +0.8 threshold; every "fresh" strong pair since 07-16 has collapsed on the next filing per the rotation-not-persistence class.

7. **[LOW — recurring] Seed 25% encoding artifact unchanged, 18th consecutive filed run.** 108 rows 9-char seed (`202607311`–`202607319`), 36 rows 10-char seed (`2026073110`–`2026073112`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

8. **[LOW — recurring] x402Total mean UP $28.93, sum UP $1,041, top-end NEW record high $409.32, range narrowed by $36.13 vs 07-30.** Mean $346.10 (up from 07-30's $317.17; two-day pivot: 07-28 $342.66 → 07-30 $317.17 → **$346.10**), range $315.08–$409.32 (band width $94.24, down from 07-30's record-widest $130.37). Top-end $409.32 is the **new record high** for the 18-filing window (supplants 07-30's $400.72 and 07-28's $401.89). Low-end $315.08 reverses from 07-30's record-low $270.35 — the widening was one-run only. Bimodal-by-design x402Total outlier exclusion holds; the 16.67% flag (6 outliers: seed 202607319 tri-role at $397.12 + seed 202607313 tri-role at $409.32) is the top-of-non-zero-distribution cluster, not a break. Sum $12,459.56 across 36 x402 rows.

9. **[LOW — recurring] Spread `wallet_sum_pnl` absolute range 2.76e−11 (~195% of 07-30's 1.42e−11 — spread WIDENED after two prior tightenings).** absmax 1.82e−11 (up from 1.06e−11 on 07-30; new 4-filing high, still 10+ orders below any real gate); Σ σ across modes: basket 3.71e−14 (down ~7% from 3.99e−14 — flip from yesterday's widening), spread 6.76e−12 (up ~57% from 4.31e−12 — reverses two consecutive tightenings), synth/x402 1.75e−14 (up ~45% from 1.21e−14 — reverses yesterday's decrease). Sign flips today vs 07-30: basket +1.77e−14 (was −1.49e−14), spread −1.61e−12 (was −1.40e−12, same sign, deeper), synth/x402 +1.94e−15 (was −2.41e−16). All 11+ orders under any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Excluded pairs today: spread `minCurve × wallet_sum_pnl` = −0.646, spread `minSpot × wallet_sum_pnl` = −0.500, spread `wallet_sum_pnl × seed` = −0.438.

10. **[LOW — recurring] Spread `role_pnl` tail second-lowest in recent window at −$16,797.** Series (4-day): 07-25 min −$22,252, 07-28 min −$14,826, 07-30 min −$18,664, 07-31 min −$16,797. Seed 2026073110 (retail role) anchors the low; seed 202607314 (retail role) is the near-neighbor at −$16,197. Max +$11,930 recovers from 07-30's suppressed +$9,791 (still below 07-28's +$14,598 and 07-25's +$15,748). Two outliers at outlier_pct 5.56% (under the 10% floor, not a fresh MEDIUM).

11. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.77e−14 (basket), −1.61e−12 (spread), +1.94e−15 (synthetic), +1.94e−15 (x402). Std 3.71e−14 (basket), 6.76e−12 (spread), 1.75e−14 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Signs shifted vs 07-30 in basket (+ ← −) and synth/x402 (+ ← −); spread stays negative but goes deeper. All inside the float-dust regime, not signals.

12. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical wallet_sum_pnl mean/std, role_pnl mean/std, all per-column distributions except `x402Total`). Only `x402Total` distinguishes x402.

13. **[PASS] Row/column shape unchanged vs 07-30.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.77e−14  | 3.71e−14  | OK |
| spread    | −1.61e−12  | 6.76e−12  | OK |
| synthetic | +1.94e−15  | 1.75e−14  | OK |
| x402      | +1.94e−15  | 1.75e−14  | OK |

## Within-mode signal

- **Cross-mode single-seed anchor pattern crosses n=3 promotion threshold; seed 202607314 dominates six mode-column pairs.** Third consecutive filing with the same "one seed lifts multiple modes' tails simultaneously" structural pattern (different seed each filing). Per the SKILL persistence gate, this promotes to MEDIUM. Underlying cause is 12-seed sample variance leaking through mode boundaries — the same random draw feeds every mode's per-seed row-set, so a high-variance seed will surface in every mode's IQR tails. The fix is either (a) widen seeds per [[compute-futures-12-seed-sample-too-small]] so per-mode IQRs stabilize, or (b) add a persistence-across-filings dedup in this skill so the same *pattern* stops re-firing MEDIUM every filing when only the specific seed rotates.
- **Basket `realizedAbs` fresh 16.67% outlier fire + basket `settlementLegs` re-fires the on/off toggle.** Basket now has TWO MEDIUM columns firing simultaneously vs 07-30's zero. Anchored by seeds 202607313 (low $128.37) and 202607314 (top $268.38) for realizedAbs; seeds 202607317 + 2026073110 (both at 78) for settlementLegs. Basket σ(role_pnl) tightened slightly to $35.75 (from 07-30's $33.62 → +6%) but the outlier surface is a different shape — tails widened while the center held.
- **Spread `settlementLegs` mode-collapse at 81.** 24 of 36 rows sit at exactly 81, producing the IQR-narrowing artifact that flags 12 outliers (33.33% HIGH). The underlying `settlementLegs` distribution has actually TIGHTENED (only 5 unique values 78,79,80,81,82 vs 07-30's 4-value cluster {79:6, 80:12, 81:12, 82:6}); the "outlier surge" is the flip side of the tightening. A tie-robust statistic per [[compute-futures-12-seed-sample-too-small]] would suppress this class outright.
- **x402 `settlementLegs × x402Total` sixth-consecutive-positive + two-run magnitude bounce.** +0.540 → +0.612 breaks the +0.657 → +0.357 → +0.241 decay in the opposite direction: now bouncing back UP toward the sign-holding band. Six consecutive positives is the longest same-sign streak on record for this pair. Escalation gate holds: one more positive keeps emerging-structural watch alive, but the sign persistence alone is now the interpretive story — magnitude oscillates but the sign has held for six straight filings.
- **spread `maxCurve × settlementLegs` = −0.674 fresh negative.** Opposite sign of basket's +0.407 and synth/x402's +0.134 today. Not a strong pair (below +0.8) but a distinct within-mode signal — spread mode's maxCurve moves opposite to settlementLegs, which does not hold in the other three modes. Watch for persistence: if the fresh −0.6+ pair collapses on the next filing (as prior fresh single-run near-misses have), it joins the rotation-not-persistence class.
- **role_pnl scale per mode: all three magnitudes moved this filing.** basket σ = $35.75 (μ = −$1.43) — tightened slightly from 07-30's $33.62/−$0.36 (basket σ has now oscillated $51 → $33 → $36 in three filings); spread σ = $7,314 (μ = −$473) — tightened 1% from $7,264/−$382; synth/x402 σ = $13.04 (μ = −$0.51) — widened 9% from $11.91/−$0.06. Cross-filing σ oscillation continues to be consistent with 12-seed sample variance, not a regime shift.

## Row count and shape vs 2026-07-30

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface exploded.** 07-30 fired 0 HIGH + 1 MEDIUM (basket `settlementLegs` — collapsed to 0.00% then re-fired today). 07-31 fires **1 HIGH (spread `settlementLegs` 33.33%) + 5 fresh MEDIUM columns** (basket `realizedAbs` 16.67%, basket `settlementLegs` 16.67%, spread `maxCurve` 16.67%, synth/x402 `realizedAbs` 25%, synth/x402 `maxCurve` 16.67%) + 1 MEDIUM pattern promotion (cross-mode single-seed anchor n=3). Widest MEDIUM-or-higher surface in the 18-filing window. Underlying driver is the 12-seed sample happening to include seed 202607314 with unusually heavy tails across price-band columns and mode boundaries; every one of the five fresh column fires is anchored by seed 202607314 in at least one tail.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical wallet_sum_pnl, role_pnl, and all price columns between synth and x402; only `x402Total` distinguishes x402). Don't re-report; per SKILL rule, synth/x402 role_pnl outliers are dedup-excluded. Same-row outliers in synth/x402 for `realizedAbs`/`maxCurve` this filing are the same physical rows, counted once as a single MEDIUM in finding 3.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; 12th consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 10+ orders under the threshold everywhere (highest is spread's 6.76e−12). Excluded pairs today: spread `minCurve × wallet_sum_pnl` = −0.646, spread `minSpot × wallet_sum_pnl` = −0.500, spread `wallet_sum_pnl × seed` = −0.438. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 18th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence, complicated by the anchor-pattern promotion.** 07-30's basket `settlementLegs` 0.00% re-fired today at 16.67% — on/off toggle intact. But the *pattern* of cross-mode single-seed anchoring has now persisted 3 filings and crossed the promotion threshold; the specific seed rotates (286 → 301 → 314) but the pattern doesn't. Distinction matters: per-column MEDIUMs will keep collapsing on the next filing; the pattern-level MEDIUM will re-fire indefinitely under the current seed count. Only breaks with a wider seed count per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` sign-flip streak** — now **six consecutive positives**; magnitude bounces from +0.540 to +0.612 (breaks the +0.657 → +0.357 → +0.241 decay in the opposite direction). Sign persistence is the interpretive story now; magnitude oscillates within the band. Sign flip on the next filing reclassifies back to jitter.
- **x402Total band shape** — 07-30's widened-range/low-mean fully reverses today: mean UP to $346.10 (near 07-28's $342.66), range NARROWS by $36 to $94.24, top-end $409.32 sets new record. Bimodal-by-design outlier exclusion holds; the 6-outlier flag is the top-of-non-zero-distribution cluster from seeds 202607319 and 202607313, both cross-mode anchor secondaries.
- **Cross-mode single-seed anchor pattern PROMOTED to MEDIUM** — third consecutive filing (07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314). Different seed and different columns each filing, but the "one seed dominates multiple modes' tails" structural pattern has now persisted through the n=3 gate. This filing's anchor (202607314 with six mode-column dominations) is the strongest observation of the pattern to date. Fix path: widen seeds per [[compute-futures-12-seed-sample-too-small]] — otherwise this MEDIUM re-fires every filing.
