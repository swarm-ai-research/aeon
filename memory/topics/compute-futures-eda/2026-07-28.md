# Compute-Futures EDA — 2026-07-28

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-28.csv` (144 rows, 4 modes)
**Run:** 2026-07-29
**Prior filed report:** 2026-07-25 (3-day gap; 07-26 and 07-27 CSVs exist in the proof stream but were not analyzed by this skill on those days).

## Top findings

1. **[MEDIUM — recurring] Basket `settlementLegs` outlier_pct 16.67% — re-fires on the extended on/off rotation.** 6/36 rows outside IQR whiskers; distribution {78:6, 80:6, 81:15, 82:6, 83:3}. Cross-referencing the intervening CSVs: 07-26 = 0.00% (all modes), 07-27 = 0.00% for basket but **spread** fired 16.67% instead. Basket rotation series 07-09 → today, incorporating the skipped days: 8.33 → 25.00 (07-10) → 0.00 (07-12) → 16.67 (07-16) → 0.00 (07-17) → 16.67 (07-20) → 16.67 (07-21) → 0.00 (07-24) → 16.67 (07-25) → 0.00 (07-26 unfiled) → 0.00 (07-27 unfiled) → 16.67 (07-28). Four 16.67% fires in the last twelve calendar days; every fire still followed by a next-run collapse — persistence gate uncrossed per [[compute-futures-12-seed-sample-too-small]]. Next-step: hold or reclassify tomorrow.
2. **[LOW — cross-mode single-seed spike] Seed 202607286 anchors the maxSpot high tail in three modes simultaneously.** basket `maxSpot` = 0.3006 (single 8.33% outlier, next-highest 0.2858), synth/x402 `maxSpot` = 0.0932 (single 8.33% outlier in each, next-highest 0.0886). Same seed, three modes, all high-tail. Not previously reported. Because the synth/x402 numeric equality is tautological, this is really "one seed spikes basket + synth simultaneously", which suggests the underlying price path for seed 202607286 lifts the max on both scale bands rather than being a mode-specific artifact. Below the 10% severity floor but structurally interesting — track whether seed 202607286 recurs as a spike anchor in future filings.
3. **[LOW — sign-flip streak: fourth consecutive positive, magnitude drifting toward zero]** `settlementLegs × x402Total` in x402 = **+0.241** today. Series: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (07-17) → +0.028 (07-18) → −0.110 (07-20) → +0.156 (07-21) → +0.657 (07-24) → +0.357 (07-25) → **+0.241** (today). Fourth consecutive positive, magnitude trajectory +0.657 → +0.357 → +0.241 (monotonic decay toward zero). Sign holds but the streak is decaying to noise, not consolidating structural. Escalation gate: sign flip tomorrow restores pre-07-24 jitter classification with a three-run positive interlude; another positive at any magnitude keeps the emerging-structural watch alive. Companion pair `minSpot × x402Total` = +0.080 (down from +0.745 on 07-25 — the 07-25 near-miss did NOT survive).
4. **[LOW — fresh near-miss] x402 `realizedAbs × x402Total` = +0.641 — new candidate pair.** Not previously reported at |r| ≥ 0.6 in x402. Directional: more realized settlement notional lines up with a larger x402Total. Structurally plausible (x402 settlement fees scale with notional). Below the |r| ≥ 0.8 reporting threshold but the highest x402-specific pair today, replacing 07-25's `minSpot × x402Total` = +0.745 near-miss (which collapsed to +0.080 today). Watch alongside the sign-flip pair on the next filing — if both re-fire the x402 correlation surface may be stabilizing.
5. **[LOW — tenth consecutive filed run at zero |r| ≥ 0.8].** No mode fires a strong pair. Streak: 07-10, 07-12, 07-16, 07-17, 07-18, 07-20, 07-21, 07-24, 07-25, today. Non-wallet_sum_pnl within-mode near-misses this run — basket: `seed × settlementLegs` = +0.537, `maxSpot × minCurve` = −0.471, `seed × realizedAbs` = +0.448; spread: `seed × realizedAbs` = +0.552, `seed × minCurve` = +0.432, `seed × maxSpot` = −0.416; synth: `seed × realizedAbs` = +0.486, `realizedAbs × minCurve` = −0.447; x402: `realizedAbs × x402Total` = +0.641 (finding 4), `minCurve × x402Total` = −0.483. Basket `seed × settlementLegs` = +0.537 is a re-appearance of the seed-encoding artifact family per [[compute-futures-seed-padding-bug]].
6. **[LOW — recurring] Seed 25% encoding artifact unchanged, 16th consecutive filed run.** 108 rows 9-char seed (`202607281`–`202607289`), 36 rows 10-char seed (`2026072810`–`2026072812`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].
7. **[LOW — recurring] x402Total mean up, band re-widens vs 07-25.** Mean $342.66 (up $20.11 vs 07-25's $322.55 — reverses the two-run mean-down move), range $281.23–$401.89 (band width $120.66, back to pre-07-25 shape after 07-25's tightening to $78.32; reversed the "first tightening in three runs" pattern), sum $12,335.98 across 36 x402 rows. No x402Total outlier flag (bimodal-by-design exclusion holds). Sixteen-filing window: mean between $321 and $343; range envelope $268–$402 nudges up top-end by $13 (new record high). 07-25's tightening did not persist — it was a single-run compression, not a trend.
8. **[LOW — recurring] Spread wallet_sum_pnl absolute range 2.12e−11 (~1× 07-25's 2.05e−11).** absmax 1.09e−11 (down slightly from 1.14e−11 on 07-25); Σ σ across modes: basket 3.17e−14 (down from 3.21e−14, essentially flat), spread 5.44e−12 (down from 5.92e−12, tightened ~8%), synth/x402 1.39e−14 (up from 7.38e−15, ~1.9× — still float dust). All 12+ orders under any real gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Basket `wallet_sum_pnl × minSpot` +0.478 and `× realizedAbs` −0.381 correlations remain excluded from reporting.
9. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.84e−15 (basket), −9.14e−13 (spread), −3.37e−15 (synthetic), −3.37e−15 (x402). Std 3.17e−14 (basket), 5.44e−12 (spread), 1.39e−14 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. No conservation drift signal in any mode. Sign held negative in every mode today (07-25 basket +1.17e−12 → today −9.14e−13 for spread is a sign flip at |μ|~1e−12, well inside float-dust regime — not a signal).
10. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical wallet_sum_pnl mean/std, role_pnl mean/std, and all per-column distributions except `x402Total`). Only `x402Total` distinguishes x402.
11. **[PASS] Row/column shape unchanged vs 07-25.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.
12. **[PASS] No CRITICAL or HIGH findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift, no per-mode column at outlier_pct ≥ 20% (excluding the recurring `seed` artifact).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.84e−15  | 3.17e−14  | OK |
| spread    | −9.14e−13  | 5.44e−12  | OK |
| synthetic | −3.37e−15  | 1.39e−14  | OK |
| x402      | −3.37e−15  | 1.39e−14  | OK |

## Within-mode signal

- **Basket `settlementLegs` on/off rotation continues, four fires in twelve calendar days.** Today's 16.67% is the fourth 16.67% fire in the last twelve filings (07-16, 07-20, 07-21, 07-25 all fired; 07-24 and today's intervening 07-26/07-27 cleared). Distribution {78:6, 80:6, 81:15, 82:6, 83:3} tags 78×6 low + 83×3 high. The (basket, settlementLegs) pair remains the highest-frequency 16.67% offender in the record; every fire is still followed by a next-run collapse.
- **Cross-mode maxSpot single-seed anchor.** Seed 202607286 spikes maxSpot at the top of three modes simultaneously (basket 0.3006, synth/x402 0.0932), each mode's sole IQR outlier for that column. Not a mode-specific artifact — the seed's price path lifts the max on both scale bands. New pattern; track for recurrence.
- **Correlation surface — no strong pairs, one fresh near-miss, one collapsed near-miss.** basket: strongest `seed × settlementLegs` = +0.537 (seed-artifact family), `maxSpot × minCurve` = −0.471; spread: strongest `seed × realizedAbs` = +0.552; synth: `seed × realizedAbs` = +0.486; x402: `realizedAbs × x402Total` = +0.641 (finding 4, fresh), `minCurve × x402Total` = −0.483. `settlementLegs × x402Total` = +0.241 continues the fourth consecutive positive with decaying magnitude (finding 3). 07-25's `minSpot × x402Total` = +0.745 near-miss collapsed to +0.080 today — did not survive. Basket `realizedAbs × settlementLegs` = +0.377 (07-25's fresh +0.776 near-miss also collapsed).
- **role_pnl scale per mode.** basket σ = $51.06 (μ = −$1.98) — widened +61% from $31.60/−$0.14 on 07-25 (07-25's narrowing did not persist); returns to widening trajectory. spread σ = $6,711 (μ = −$306) — narrowed further from $7,104/−$734 on 07-25 (−6%; μ swung less negative). synth/x402 σ = $14.39 (μ = −$0.72) — widened +50% from $9.58/−$0.34 on 07-25. Two modes widened, one narrowed; opposite of 07-25's two-narrowed pattern.

## Row count and shape vs 2026-07-25

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface rotated fully.** 07-25 fired 1 HIGH (spread `maxSpot` 25.00%, first-appearance) + 1 MEDIUM recurring (basket `settlementLegs` 16.67%). Spread `maxSpot` HIGH fully collapsed today (8.33%, single seed 202607286 outlier); basket `settlementLegs` MEDIUM recurred. No new HIGH this filing. Zero HIGH-severity column-mode pairs carry over across 07-25 → today at ≥ 20% — third consecutive filing with 0 HIGH carryover.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical wallet_sum_pnl, role_pnl, and all price columns between synth and x402; only `x402Total` distinguishes x402). Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; tenth consecutive filed run with zero |r| ≥ 0.8 pairs. σ(wallet_sum_pnl) 11+ orders under the threshold everywhere. Excluded basket pairs today: `wallet_sum_pnl × minSpot` +0.478. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 16th consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence** — reasserted today. Every prior single-run HIGH-or-MEDIUM promotion attempt in the record has collapsed on next re-check (07-25's spread `maxSpot` HIGH collapsed to 8.33% today; 07-25's basket `realizedAbs × settlementLegs` +0.776 near-miss collapsed to +0.377). Persistence-candidate class remains empty; the class only breaks with a wider seed count per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` sign-flip streak** — now four consecutive positives with monotonically decaying magnitude (+0.657 → +0.357 → +0.241). Sign held but decaying to noise. Sign flip tomorrow restores jitter classification; another positive keeps emerging-structural watch alive.
- **x402Total band shape** — 07-25's tightening to $78.32 range did not persist; today back to $120.66 range (pre-07-25 shape), mean up $20 to $342.66, new record top-end $401.89. Bimodal-by-design outlier exclusion holds.
- **3-day filing gap 07-26/07-27.** CSVs exist for both dates but no report was filed. Cross-referenced settlementLegs series in findings 1 to preserve rotation-pattern continuity. Skill's "latest CSV only" policy skipped them; if the retroactive comparison matters for future persistence-gate calls, consider a backfill pass.
