# Compute-Futures EDA — 2026-07-17

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-17.csv` (144 rows, 4 modes)
**Run:** 2026-07-18
**Prior filed report:** 2026-07-16 (day-over-day; no gap)

## Top findings

1. **[HIGH] Three modes' `minSpot` outlier_pct hits 25% simultaneously — basket, synthetic, x402.** Nine of 36 rows per mode tag IQR whiskers on `minSpot`. Each spot column is a seed-level feature that replicates 3× across roles, so 25% = 3 of 12 seed values in the tail per mode. Basket tail is 0.116/0.121 low and 0.131/0.135/0.136 high around a 0.124–0.127 core; synth/x402 tail is 0.044/0.046 low and 0.050/0.052 high around a 0.047–0.048 core (synth/x402 seed values identical). Same IQR-tight-band artifact as prior filings [[compute-futures-12-seed-sample-too-small]] but three modes tagging at once is new — three prior filings all had `minSpot` outlier_pct at 25% in only one mode at a time or none. Watch: only escalates to signal if same three modes recur on next CSV.
2. **[MEDIUM] Wide new maxSpot outlier surface — basket, spread, and synth/x402 all at 16.67% simultaneously.** Six of 36 rows per mode tag IQR whiskers on `maxSpot`. None of these three flagged on 07-16. Basket range 0.206–0.251; spread range 62.64–69.99; synth/x402 range 0.079–0.095. Also 12-seed IQR-tight-band shape (6/36 = 2 of 12 seed values in tail per mode). Combined with the minSpot HIGH cluster, today's outlier surface is materially wider than 07-16 across seed-level spot features.
3. **[MEDIUM] synth/x402 `maxCurve` outlier_pct 16.67% — HOLDS from 07-16.** Six of 36 rows tag IQR whiskers in each mode (identical values, tautology). Range 0.094–0.106 around 0.099 mean. This is the ONLY (mode, column) outlier flag that carries over from 07-16; every other 07-16 flag rotated. Second consecutive filing at ≥ 10% — first (mode, column) pair to persist across two runs since the record starts.
4. **[MEDIUM] basket `maxCurve` outlier_pct 16.67% (new; was <10% on 07-16).** Six of 36 basket rows tag IQR whiskers on `maxCurve`. Range 0.247–0.278 around 0.261 mean. Third (mode, column) outlier surface widening today vs 07-16.
5. **[LOW — fourth consecutive filed run at zero |r|≥0.8].** No mode fires any strong-pair candidate today. Streak: 07-10, 07-12, 07-16, today. Sub-threshold churn: basket `seed × wallet_sum_pnl` = +0.797 (right at the gate but float-dust-filtered per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ(wallet_sum_pnl)=3.5e-14 in basket, 12+ orders below threshold); spread `realizedAbs × minCurve` = −0.771 (persistent pattern from prior runs); x402 `settlementLegs × x402Total` = −0.604 (was +0.732 on 07-16, +0.216/−0.315 prior — fourth sign-flip in five runs). None strengthens two runs in a row.
6. **[LOW — collapse of 07-16 leader] basket `settlementLegs` 16.67% → 0%.** Yesterday's headline flag reverted within one seed rotation, matching every prior single-flag pattern. Distribution today {77:3, 78:3, 79:12, 80:3, 82:12, 83:3} is more evenly spread than 07-16's {80×18, 79×9, ...}, so IQR whiskers widened and no rows tag now. Same alarm-without-signal shape.
7. **[LOW — recurring] Seed 25% encoding artifact unchanged.** 108 rows nine-digit (`202607171`–`202607179`), 36 rows ten-digit (`2026071710`, `2026071711`, `2026071712`). `seed` outlier_pct = 25.00% in every mode, same as every prior filing since 06-20. One-line pad in `scenario-sweep.mjs` still pending [[compute-futures-seed-padding-bug]].
8. **[LOW — recurring] x402Total inside the ~$100 stable band.** Mean $328.63, range $272.51–$441.33, sum $11,830.62 across 36 x402 rows. Ninth consecutive run inside the stable envelope since 07-03. Range shifted ~$20 higher (07-16 was $285–$361; today $273–$441) but well inside the ≥ $100 window. Three rows tag IQR upper whisker → x402Total outlier_pct 8.33% (excluded per bimodal-by-design constraint).
9. **[LOW — recurring] wallet_sum_pnl outliers dominate raw counts.** Synth/x402 33.33%, spread 25%, basket 16.67%. Magnitudes ±5.7e-14 (basket), ±1.0e-11 (spread), ±2.3e-14 (synth/x402). All float dust — 12+ orders below any meaningful gate. Filter continues to hold per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
10. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +2.0e−15 (basket) → −1.09e−12 (spread); std 1.09e−14 (synth/x402) → 5.37e−12 (spread). All twelve-plus orders under the |mean|>10 / std>100 gates. Spread `wallet_sum_pnl` σ widened slightly vs 07-16 (3.22e-12 → 5.37e-12) but still float dust.
11. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402.
12. **[PASS] Row/column shape unchanged vs 07-16.** No column added or removed; 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402) all hold.
13. **[PASS] No CRITICAL findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +2.01e−15  | 3.52e−14  | OK |
| spread    | −1.09e−12  | 5.37e−12  | OK |
| synthetic | −3.53e−15  | 1.09e−14  | OK |
| x402      | −3.53e−15  | 1.09e−14  | OK |

## Within-mode signal

- **`minSpot` HIGH cluster in basket / synthetic / x402 (25% each; 9/36 rows per mode).** Seed-level feature, so 25% = 3 of 12 seed values in tail per mode. Basket tail seeds tag 0.116/0.121 low and 0.131/0.135/0.136 high around a 0.124–0.127 core; synth/x402 tail seeds tag 0.044/0.046 low and 0.050/0.052 high around a 0.047–0.048 core. Three modes tagging at once is new — but with only 12 seeds, IQR whiskers on a 12-value distribution are structurally narrow; the pattern is consistent with 12-seed sample instability [[compute-futures-12-seed-sample-too-small]]. Two independent generators (basket vs synth/x402) firing simultaneously is the coincidence worth watching.
- **`maxSpot` MEDIUM cluster across ALL four modes (16.67% each; 6/36 rows per mode).** Basket, spread, and synth/x402 all tag IQR whiskers on `maxSpot`. New for all three (07-16 had none). Two of 12 seed values in tail per mode. Same IQR-tight-band shape.
- **`maxCurve` MEDIUM cluster in basket + synth/x402 (16.67% each).** synth/x402 is second consecutive run — first (mode, column) pair to persist. Basket is new today.
- **`role_pnl` scale per mode (regime unchanged).** spread σ = $9,610 (μ = −$592) — narrowed slightly from $9,697/−$627 on 07-16, inside 12-seed jitter; basket σ = $29.4 (μ = +$0.04) — widened from $23.5/−$0.74; synth/x402 σ = $9.5 (μ = −$0.15) — widened from $7.6/−$0.40. Spread continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold and sign-jittering.** `settlementLegs × x402Total` −0.604 (was +0.732 on 07-16, −0.315 on 07-12, +0.216 on 07-10 — fourth sign-flip in five runs), `maxCurve × x402Total` +0.447 (was −0.482 on 07-16 — sign flipped), `minSpot × x402Total` −0.477 (was +0.196 on 07-16 — sign flipped), `maxSpot × x402Total` −0.105 (was +0.168), `minCurve × x402Total` +0.194 (was +0.014). Every pair jitters, none strengthens two runs in a row — consistent with the 12-seed churn.
- **wallet_sum_pnl outliers dominate raw counts but stay float dust.** basket 16.67%, spread 25%, synth/x402 33.33%. σ(wallet_sum_pnl) 1.09e−14 (synth/x402) to 5.37e−12 (spread). Excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

## Row count and shape vs 2026-07-16

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface materially widened day-over-day.** 07-16 had one MEDIUM per-mode flag (basket settlementLegs 16.67%) plus a doubled synth/x402 maxCurve MEDIUM. Today has: basket minSpot 25% (HIGH), synth minSpot 25% (HIGH), x402 minSpot 25% (HIGH), basket maxSpot 16.67% (new), spread maxSpot 16.67% (new), synth maxSpot 16.67% (new), x402 maxSpot 16.67% (new), basket maxCurve 16.67% (new), synth maxCurve 16.67% (holds), x402 maxCurve 16.67% (holds). Nine new (mode, column) MEDIUM+ flags plus one carry-over versus 07-16.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (max |diff role_pnl| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again; four consecutive filed runs with zero |r|≥0.8 pairs. σ(wallet_sum_pnl) still 12+ orders under the threshold. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string; one-line pad still pending. See [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct instability** — pattern is (mode, column) rotation, not persistence. Historical single-flag rotation: 07-08 (all 8.33%) → 07-09 spread realizedAbs → 07-10 basket settlementLegs 25% + spread minSpot → 07-12 spread settlementLegs → 07-16 basket settlementLegs + synth/x402 maxCurve → today's WIDER 10-way surface. Today is the first filing with a materially wider outlier surface than the prior day — three separate seed-level features (minSpot, maxSpot, maxCurve) flagging at once across 3–4 modes. First (mode, column) pair to actually persist across two runs: synth/x402 `maxCurve` at 16.67%. Escalation gate: watch next CSV for same-column persistence; sustained → real signal, rotation → jitter as usual per [[compute-futures-12-seed-sample-too-small]].
- **Sub-threshold within-mode correlation churn** — x402 `settlementLegs × x402Total`: +0.216 (07-10) → −0.315 (07-12) → +0.732 (07-16) → −0.604 (today). Every rotation sign-flips. Continues 12-seed jitter classification until any pair confirms two runs in a row.
