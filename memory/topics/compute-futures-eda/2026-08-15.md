# Compute-Futures EDA — 2026-08-15

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-15.csv` (144 rows, 4 modes)
**Run:** 2026-08-16
**Prior filed report:** 2026-08-14 (single-day gap).

## Top findings

1. **[MEDIUM — extension, promoted class n=9 consecutive filed runs, SHALLOWS after 3-day deepening streak] Spread `role_pnl` retail mean −$7,793 (shallower by $3,299 vs 08-14); top-5 losses 100% retail; top-5 gains 0% retail.** Top-5 losses: 202608155 retail −$16,459 / 202608151 retail −$15,757 / 2026081512 retail −$13,060 / 202608159 retail −$9,364 / 202608154 retail −$8,611. Top-5 gains: **4 operator + 1 analyst, 0 retail** (202608151 analyst +$11,578 / 2026081512 operator +$9,991 / 202608155 operator +$9,976 / 202608157 operator +$9,554 / 202608154 operator +$8,771). Per-role: retail mean **−$7,793** (std $5,454, min −$16,459, max +$1,834) — shallower by $3,299 vs 08-14's −$11,092 (first shallowing in 4 days after 3 consecutive deepenings: −$5,984 → −$9,473 → −$11,092 → **−$7,793**); operator +$4,281 (std $5,177, down from 08-14's +$6,211); analyst +$2,307 (std $4,321, down from +$3,617). Extends [[compute-futures-spread-retail-loss-concentration]] from n=8 to **n=9 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11, 08-12, 08-13, 08-14, 08-15). Retail mean depth peaks at 08-14 and retracts $3,299; class direction (retail-losers, operator/analyst-winners) unchanged. Under IQR fence, spread `role_pnl` at 0% today (n=0 — loss cluster fits within fence).

2. **[LOW — reversion, cross-mode ≥10% MEDIUM count 2 → 0] Spread `minSpot` AND `maxSpot` outliers COLLAPSE back to 0% after single-day 16.67% appearance on 08-14.** 08-14 introduced two fresh cross-mode ≥10% MEDIUMs (spread minSpot 16.67%, spread maxSpot 16.67% — first spread-side spot-price outliers in the filed series). **Today both drop to 0%.** Spread minSpot range today [40.558, 45.681] fits inside fence [41.375, 45.147] with 0 outliers (vs 08-14's [41.170, 45.727] with 6 outliers); spread maxSpot range [63.102, 72.644] fits fence [62.788, 69.717] with 0 outliers (vs 08-14's [61.855, 74.359] with 6 outliers). Neither 08-14 anchor (202608141/2026081411 for minSpot, 202608144/202608149 for maxSpot) reappears in 08-15's outlier surface. Column-rotation across the last seven cross-mode ≥10% MEDIUM filings: settlementLegs (08-10) → maxSpot basket/synth/x402 (08-11) → settlementLegs synth/x402 (08-12) → zero (08-13) → spread minSpot + maxSpot (08-14) → **zero (08-15)** — spread spot-price appearance is single-run, not persistent.

3. **[LOW — n=6 consecutive filed runs, CEMENTED promoted class holds exactly] basket/synth scale-ratio HELD at 3.0250 exactly for `maxSpot`/`minSpot`.** Six-filing series (08-10 through 08-15). `maxSpot` basket/synth: min=max=3.0250 (deterministic constant, 36/36 seed × role pairs, floating-point noise in the 15th decimal). `minSpot` basket/synth: min=max=3.0250 (36/36 pairs). `maxCurve` basket/synth: 3.0155–3.0306 (mean 3.0235, jitter tightens from 08-14's 2.9949–3.0355). `minCurve` basket/synth: 3.0000–3.0341 (mean 3.0189, jitter tightens from 08-14's 2.9888–3.0787). `realizedAbs` basket/synth: 2.7680–3.2151 (mean 3.0518, mean tightens toward 3.0250 vs 08-14's 3.1564; range widens on LOW side vs 08-14's 2.8399–3.4452). Consistent with promoted note [[compute-futures-basket-synth-3025x-multiplier]]; promotion strengthens at n=6.

4. **[LOW — n=7 consecutive filed runs, x402 pair SIGN FLIPS AGAIN with tiny magnitude] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, 08-12 zero, 08-13 zero, 08-14 zero, **08-15 zero**. Extends 08-14's n=6 to n=7. Highest within-mode pairs today: **basket `realizedAbs × minSpot` = +0.734**, **synth `realizedAbs × minSpot` = +0.713**, spread `minCurve × settlementLegs` = −0.653, spread `realizedAbs × maxSpot` = +0.602 (top pair on 08-14 was spread `minSpot × settlementLegs` = −0.616 — spread top-pair rotates). x402 `settlementLegs × x402Total` today **+0.2934** — 23-filing series: … +0.475, +0.558, +0.255, +0.771, −0.270 (08-14), **+0.293** (08-15) — SIGN FLIPS AGAIN − → + and magnitude comparable (|0.270| → |0.293|); alternation-not-persistence continues (5th sign-flip in 23 filings). Non-basket/synth/spread top: x402 `minCurve × x402Total` = −0.451, x402 `maxCurve × x402Total` = −0.401 (both new to top pair board; within-mode signal reshuffle). n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced at n=7.

5. **[LOW — bilateral translation DOWNWARD, near-perfect mirror of 08-14's upward shift] x402Total range shifts DOWN $14–25 on both sides: $262.66–$384.55 (std $30.53).** 08-14 was $287.28–$398.93 (range $111.65); today $262.66–$384.55 (range $121.89, range widens $10.24). New LOW $262.66 below 08-14's $287.28 by **−$24.62** (single-day floor drop, nearly perfect mirror of 08-14's +$23.57 lift); new HIGH $384.55 below 08-14's $398.93 by **−$14.38** (single-day ceiling drop, partial mirror of 08-14's +$25.09 lift). Mean $327.58 (−$10.00 vs 08-14's $337.58, near-perfect mirror of 08-14's +$10.58 shift). Sum $11,792.90 (−$359.86 vs 08-14's $12,152.76, nearly reverses 08-14's +$380.61 uplift). Std $30.53 (up from $28.84, slightly wider dispersion). LOW anchor: 202608156 (3-role tie at $262.66); HIGH anchor: 202608152 (3-role tie at $384.55). Shape: parallel-translation DOWNWARD across two-day cycle (08-14 up + $23–25 both sides → 08-15 down $14–25 both sides); the mean-reverting round-trip is near-complete. x402Total outlier_pct = 16.67% in x402 mode (6 rows: 3-role LOW tie at 202608156 + 3-role HIGH tie at 202608152; up from 8.33% on 08-14), excluded per SKILL bimodal rule.

6. **[LOW — anchor concentration DISPERSES from hyper-single to five-anchor rotation] Five co-anchors carry the outlier surface (18+18+12+12+12 positions across the top five, vs 08-14's single hyper-anchor 202608144 at 14 positions).** Anchor breakdown: **202608158 (18 positions, all 4 modes: basket/spread/synth/x402, columns maxCurve/maxSpot/realizedAbs)**; **2026081512 (18 positions, 3 modes ex-spread: basket/synth/x402, columns maxSpot/minCurve)**; **202608155 (12 positions, all 4 modes, columns maxSpot/realizedAbs)**; **202608152 (12 positions, 3 modes ex-spread, columns minCurve/x402Total)**; **202608159 (12 positions, all 4 modes, columns minCurve/wallet_sum_pnl)**. Three of the five span all 4 modes; two skip spread (spread's outlier surface is thinner — only wallet_sum_pnl, realizedAbs, maxCurve fire). Class series: 08-10 super-single dense (202608106, 15 pos, single-mode) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → 08-13 super-single dense (202608137, 13 pos) → 08-14 hyper-single (202608144, 14 pos, quadruple-mode) → **08-15 five-anchor rotation (18+18+12+12+12)**. Zero overlap with 08-14's anchor (202608144 fires zero outlier positions in 08-15). Concentration shape RESUMES rotation pattern after 2-day super-single/hyper-single interlude.

7. **[LOW — recovery] Padded-seed outlier positions REBOUND to 30% (roughly proportional to 25% row-share).** Padded seeds (2026081510/11/12) hold 36 of 144 rows (25%). Today they contribute 24 of 81 non-seed outlier positions (**29.6%**), driven by 2026081512 (18 positions across basket/synth/x402 maxSpot HIGH + minCurve LOW) + 2026081511 (6 positions at synth/x402 maxCurve HIGH). 2026081510 contributes zero. Series: 08-12 padded 42% over → 08-13 25% proportional → 08-14 11.5% under → **08-15 29.6% roughly proportional/mildly over**. Class continues rotation-not-persistence; padded encoding does not systematically over- or under-select.

8. **[LOW — recurring, 29th filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608151`–`202608159`), 36 rows 10-char seed (`2026081510`–`2026081512`). `seed` outlier_pct = 25.00% in every mode. Today padded 2026081512 anchors basket/synth/x402 maxSpot HIGH + minCurve LOW (18 positions total). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

9. **[LOW — non-promotable jitter] Conservation float dust SIGN FLIPS across ALL modes + → −.** `wallet_sum_pnl` mean per mode: basket **−9.75e−15** (vs 08-14 +1.42e−15; **SIGN FLIPPED** + → −, magnitude ~7× larger), spread **−1.85e−12** (vs 08-14 +1.84e−12; **SIGN FLIPPED** + → −, magnitude ~identical), synth **−7.03e−16** (vs 08-14 +8.63e−16; **SIGN FLIPPED** + → −, magnitude ~identical), x402 **−7.03e−16** (identical to synth by [synth ≡ x402]). All four modes sign-flip in the same direction (+ → −) — mirror of the − → + flip observed 08-13 → 08-14; two-day cycle sign-oscillation confirmed at n=2 in the promoted-class series. All modes remain 12+ orders under the |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outliers: basket 0% (down from 08-14's 8.33%), spread **8.33%** (up from 08-14's 0%), synth 0% (down from 16.67%), x402 0% (down from 16.67%). σ still ≥8 orders under 1e-6 per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

10. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

11. **[PASS] Row/column shape unchanged vs 08-14.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −9.75e−15  | 3.80e−14  | OK |
| spread    | −1.85e−12  | 6.98e−12  | OK |
| synthetic | −7.03e−16  | 9.67e−15  | OK |
| x402      | −7.03e−16  | 9.67e−15  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=7 consecutive filed runs.** Highest today: basket `realizedAbs × minSpot` = +0.734, synth `realizedAbs × minSpot` = +0.713, spread `minCurve × settlementLegs` = −0.653. x402 `settlementLegs × x402Total` sign-flips AGAIN from −0.270 (08-14) to **+0.293** (08-15) — 5th sign-flip in 23 filings; alternation-not-persistence continues.
- **Retail-role loss concentration in spread — n=9 consecutive filed runs, promoted class SHALLOWS.** Retail mean $3,299 shallower vs 08-14 (−$11,092 → −$7,793); 5/5 top losses retail, 0/5 top gains retail (4 operator + 1 analyst). First shallowing after 3 consecutive deepening days.
- **basket/synth `maxSpot`/`minSpot` scale-ratio = 3.0250 (deterministic) at n=6 filed runs.** Cemented and strengthening; consistent with promoted [[compute-futures-basket-synth-3025x-multiplier]].
- **Cross-mode ≥10% MEDIUM count REVERTS 2 → 0.** Spread minSpot + maxSpot MEDIUMs (fresh yesterday) both collapse to 0%; single-run appearance, not persistent. Cross-mode ≥10% MEDIUM count series: 0 (08-13) → 2 (08-14) → 0 (08-15).
- **x402Total range translates DOWN on both sides** — LOW −$24.62 (near-mirror of 08-14's +$23.57), HIGH −$14.38 (partial mirror of 08-14's +$25.09), std widens ($28.84 → $30.53). Two-day mean-reverting round-trip nearly complete; sum reverses 94.6% of 08-14's uplift.
- **Anchor concentration disperses to five-anchor rotation (18+18+12+12+12 positions across top five)** — distinct from 08-14's single hyper-anchor; three of five span all 4 modes.
- **Padded-seed rebounds to 29.6% of positions** — recovery from 08-14's 11.5% under-index to roughly-proportional; rotation-not-persistence class continues.

## Row count and shape vs 2026-08-14

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface CONTRACTS from 08-14 → 08-15.** 08-14: 0 HIGH, 3 MEDIUM (retail-loss n=8 deepening + spread minSpot 16.67% NEW + spread maxSpot 16.67% NEW). 08-15: 0 HIGH, **1 MEDIUM** (retail-loss n=9 shallowing). Cross-mode ≥10% MEDIUM count drops 2 → 0. Retail-loss promoted class carries forward but shallows; two fresh spread spot MEDIUMs from 08-14 do not persist.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's synth+x402 maxSpot HIGH cluster (2026081512, 202608158, 202608155) and synth+x402 minCurve LOW/HIGH clusters (202608152, 202608159, 2026081512) are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (3.0250× exactly for maxSpot/minSpot, ~3.02× for curves)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable across 08-10, 08-11, 08-12, 08-13, 08-14, 08-15 (n=6).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers at 0%/8.33%/0%/0% today (spread the only non-zero) — all float dust.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 29th consecutive filed run. Padded 2026081512 anchors basket/synth/x402 maxSpot HIGH + minCurve LOW today (18 positions). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-14's two fresh spread spot-price MEDIUMs revert cleanly to 0% today; 08-14's x402 `settlementLegs × x402Total` sign-flip (+0.771 → −0.270) reverses again to +0.293; only the retail-loss promoted class carries forward. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration migrates back to rotation shape** — series: 08-10 super-single dense (202608106, 15 pos, single-mode) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → 08-13 super-single dense (202608137, 13 pos) → 08-14 hyper-single (202608144, 14 pos, quadruple-mode) → **08-15 five-anchor rotation (18+18+12+12+12)**. Anchors fully rotate; zero overlap with 08-14's anchor set.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=9 (08-15).** Retail mean shallows for 1st time in 4 filings (−$5,984 → −$9,473 → −$11,092 → **−$7,793** across 08-12 → 08-13 → 08-14 → 08-15). Operator+analyst absorb the residual gain; today shows 4 operator + 1 analyst in top-5 (last filing was 2 operator + 3 analyst).
- **x402Total two-day mean-reverting round-trip** — 08-13 range $263.71–$373.84 → 08-14 range $287.28–$398.93 (up +$23/+$25) → 08-15 range $262.66–$384.55 (down −$25/−$14, near-mirror). Sum reverses 94.6% of the 08-14 uplift.
