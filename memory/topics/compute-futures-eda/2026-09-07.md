# Compute-Futures EDA — 2026-09-07

**Source:** `memory/gitlawb-compute-futures-proofs/2026-09-07.csv` (144 rows, 4 modes)
**Run:** 2026-09-08
**Prior filed report:** 2026-09-04 (**3-day filing gap** — 09-05 / 09-06 CSVs exist in proof dir but were never analyzed, per `compute-futures-eda` 06:00Z-pocket dead streak in [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]]; the intermediate days were pulled into today's trajectory-context reads but the report anchor is vs 09-04).

## Top findings

1. **[HIGH — 10+-run 5/5 retail-purity top-loss streak BREAKS; first non-retail top-5 spread loss since 08-31] Spread top-5 losses today **4/5 retail, 1 analyst** (202609075 analyst **−$4,446**, 5th deepest loss).** Retail purity had held **11 consecutive filed runs** (through 09-04 per prior report's finding-1 note "10th consecutive filed run at 5/5"; +1 for today's break-tally count). Top-5 losses: 2026090711 retail −$11,479 / 202609077 retail −$9,104 / 202609073 retail −$7,129 / 202609072 retail −$4,459 / **202609075 analyst −$4,446** (0.15% margin over 6th place 202609078 retail −$4,158; break is razor-thin but real). Retail-role role_pnl mean today **−$3,173** (spread), still 3rd-lowest deviation from zero in 4-day series 09-04 −$4,924 → 09-05 −$3,075 → 09-06 −$3,324 → 09-07 **−$3,173**. Analyst spread mean flips to **−$230 (class-first spread analyst negative-mean regime in recent 8+ filings)** — 09-04 +$1,238 → 09-05 +$443 → 09-06 +$740 → 09-07 **−$230**. Operator spread mean recovers to +$2,685 (near-09-04 +$2,741 level, ~1.6× above 09-05/06 mean). Extends [[compute-futures-spread-retail-loss-concentration]] streak but adds a **new atomic note candidate** for the first analyst-eats-a-top-5-loss observation. Next step: track whether 09-08 restores 5/5 retail purity (single-run break) or holds the mixed-role regime (parameter shift on the deployer).

2. **[HIGH — padded seed 2026090711 double-anchors both top-1 loss AND top-1 gain in same spread mode; new class-series padded-seed dispersion record] Seed **2026090711** (padded) anchors deepest spread loss **−$11,479** (retail) AND top spread gain **+$6,972** (operator) — single-seed dispersion **$18,451**, exceeding 09-04's non-padded 202609047 record of $16,208 by **14%**.** Padded seed also holds a $3,417 analyst gain (3rd top-5 gain overall). Padded-seed share of spread top-5 losses = 1/5, top-5 gains = 1/5 (single seed occupies both tail-anchor positions). Class-series padded-seed anchor progression: 08-31 padded 2026083111 anchored 3-cluster memberships (small) → 09-04 padded 2026090412 anchored 9 realizedAbs cluster memberships (basket + synth + x402 floor-tail via 2.5× propagation, no spread role_pnl involvement) → **09-07 padded 2026090711 anchors both spread role_pnl tails simultaneously (new class-series axis of padded dominance)**. This is qualitatively distinct from 09-04's finding-6: today's padded dominance is in the **role_pnl** dispersion axis, not the **realizedAbs** propagation axis. Extends [[compute-futures-seed-padding-bug]] motivation — the padding-artifact seed is now shown to dominate two independent structural axes across the class series. Next step: audit `scenario-sweep.mjs` seed encoding — a padded seed that lands at both extremes of the role_pnl distribution is unlikely to be pure chance at n=12.

3. **[MEDIUM — basket maxCurve std re-widens to 1.13e−02, breaking 3-day tightening cadence; realizedAbs ratio std widens 51% vs 09-06] basket 2.5× multiplier — spot side byte-identical n=11 consecutive filings (mean 2.500000, std 1.43e−15 minSpot).** **Curve side re-widens: `maxCurve` std **1.13e−02** — highest since 08-31's 2.82e−02 fray spike; 4-day series 09-04 8.64e−03 → 09-05 6.95e−03 → 09-06 4.68e−03 → 09-07 **1.13e−02** breaks the 3-day monotonic tightening cadence with a 2.4× widening in one day.** `minCurve` std 1.83e−03 (essentially unchanged from 09-04's 2.54e−03, ~30% tighter). `realizedAbs` per-seed ratio mean 2.5290 std **8.57e−02** — 51% wider than 09-06's 5.66e−02, 42% narrower than 09-04's 1.48e−01 (so the widening lands between 09-06 and 09-04 levels, not a full 08-31-scale spike). Interpretation: the 08-31 curve-fray retraction hypothesis from 09-04 was overconfident; 09-05/06 tightening was transient, not durable — the deployer's curve-column coupling has looser structural drift than "byte-identical spot" masks. Class series now n=11 filed runs holding spot at byte-exact 2.500000×. Next step: if 09-08's `maxCurve` std ≥ 1e−2 again, this is a new medium-fray plateau; if snaps back to 09-06's 4.68e−03, one-day noise.

4. **[MEDIUM — 4th consecutive x402Total anchor rotation across 4 unanalyzed days; minSpot returns as primary for 2nd time in 4-run window; class-first all-negative-partner regime] x402 `x402Total` primary partner rotation now on **cycle 8 with zero lock-in**: settlementLegs (08-24) → minSpot (08-25) → maxCurve (08-29) → settlementLegs (08-31) → minSpot (09-04) → maxSpot (09-05) → realizedAbs (09-06) → **minSpot (09-07, RETURNS with negative sign as at 09-04)**.** Today's correlations: minSpot **−0.421**, maxSpot **−0.336**, realizedAbs **−0.307**, minCurve **−0.229**, maxCurve **−0.161**, settlementLegs **+0.095** — **5-of-6 partners negative, only settlementLegs positive (and at near-zero +0.095) — class-first all-negative-partner regime** (previous filings always had at least 2 positive partners in the top-4 tier). Delta vs 09-04's minSpot −0.474: attenuates 11% same-sign. Delta vs 09-06 realizedAbs primary (−0.373) → today's realizedAbs (−0.307): mild attenuation but preserves sign. minSpot appears in the primary slot for the **3rd time in 4 filings** (08-25, 09-04, 09-07) — starting to look like a preferred rest state for the anchor. Next step: if 09-08 shows minSpot primary a 4th time, that's a lock-in candidate; otherwise treat as random-walk over the 6-column space with a minor minSpot attractor.

5. **[MEDIUM — conservation regime 3-/1+ mixed; 3rd distinct sign pattern in 4 consecutive filings] Per-mode `wallet_sum_pnl` sign pattern today **3−/1+** (basket −, spread +, synth −, x402 −); all magnitudes float-dust (|mean| ≤ 7.4e−15, std ≤ 3.8e−12).** 4-filing sign-pattern series: 09-04 4+/0− → 09-05 4+/0− → 09-06 mixed (2−/2+; basket −, spread −, synth +, x402 +) → 09-07 **3−/1+** (basket −, spread +, synth −, x402 −). Every mode still 12+ orders under conservation-drift gates. Synth ≡ x402 identity holds byte-identical (both means exactly −4.499e−15). Structural note: this is float-dust regime — sign patterns are numeric-noise not physical asymmetry, but the class-series pattern log gains a 4th distinct entry in 4 days (4+/0−, 4+/0−, 2−/2+ split, 3−/1+ with spread flipping). Recommendation: consider retiring the sign-pattern class-series tracking; if all four modes are float-dust below std 1e−11, the sign is genuinely random and cataloguing patterns adds noise to the report.

6. **[LOW — n=21 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode (excluding wallet_sum_pnl float-dust).** Extends 09-04's n=20 to **n=21**. Highest genuine per-mode pairs today: spread `maxSpot × settlementLegs = −0.674` (persistent spread structural mid-strength, seen on multiple prior filings); spread `wallet_sum_pnl × seed = +0.645` **excluded** per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] (spread wallet_sum_pnl std = 3.82e−12); basket `wallet_sum_pnl × minCurve = +0.545` **excluded**; synth/x402 `realizedAbs × settlementLegs = −0.556`; synth/x402 `maxCurve × settlementLegs = −0.529`. n=12 seeds still systematically insufficient for durable strong-pair discovery per [[compute-futures-12-seed-sample-too-small]].

7. **[LOW — x402Total range widens back into mid-band; 6-filing oscillation cadence extends but breaks strict up-down-up-down] x402Total range **$99.02** (min $270.93, max $369.95).** Mean **$323.10** (down $4.49 vs 09-04 $327.59, drift < 1.5% still holding across 7 filings). Std **$28.38**. 09-04's finding-8 predicted 5-filing up-down-up-down-up alternation; 09-05 range $76.05 (compress, as predicted) → 09-06 $89.92 (widen, as predicted) → 09-07 $99.02 (widen again — **breaks the strict alternation**, two consecutive widenings). Full series range: $83.55 (08-16) → $88.05 → $103.60 → $122.73 → $145.43 (08-24 peak) → $88.20 → $109.98 → $87.63 → $105.24 (09-04) → $76.05 (09-05, class-series trough) → $89.92 (09-06) → **$99.02 (09-07)**. Mean-stability observation continues: 12-filing mean drift is $61.88 range but individual means span only $317.73 (09-06) to $327.59 (09-04), a $9.86 band (~3%).

8. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`, `settlementLegs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: **0.000000e+00** (byte-identical).

9. **[PASS] Row/column shape unchanged vs 09-04.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −7.45e−15  | 3.14e−14  | OK |
| spread    | +3.02e−14  | 3.82e−12  | OK |
| synthetic | −4.50e−15  | 1.87e−14  | OK |
| x402      | −4.50e−15  | 1.87e−14  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: **3−/1+ (spread positive, all others negative)**. 3rd distinct sign pattern in 4 consecutive filings.

## Within-mode signal

- **HIGH — 11-run retail top-5-loss purity streak breaks** (finding 1) — analyst 202609075 −$4,446 slots in at #5 (razor-thin 0.15% margin over 6th-place retail). First class-first analyst-in-top-5-losses observation. Spread analyst mean flips negative (−$230, first negative in recent series). Retail sum recovers further to −$38,074 (35% shallower than 09-04).
- **HIGH — Padded seed 2026090711 anchors BOTH top-1 spread loss AND top-1 spread gain simultaneously** (finding 2) — dispersion $18,451 sets new class-series padded-seed record. Qualitatively distinct from 09-04's realizedAbs-axis dominance; today's dominance is on the role_pnl dispersion axis.
- **MEDIUM — 08-31 curve-fray retraction hypothesis DEFEATED** (finding 3) — maxCurve std re-widens to 1.13e−02 breaking the 3-day 09-05/06 tightening cadence. Interpretation: curve-column coupling has looser drift than the 09-04 "fully retracts" framing suggested.
- **MEDIUM — x402 x402Total minSpot anchor RETURNS for the 3rd time in 4 filings** (finding 4) — anchor rotation continues at cycle 8 with zero lock-in, but minSpot's appearance rate (3/4 recent) is starting to look like an attractor. Class-first all-negative-partner regime (5/6 negative, settlementLegs +0.095 the sole positive).
- **MEDIUM — Conservation sign pattern 3−/1+, 3rd distinct in 4 days** (finding 5) — deprecation candidate: with all four modes at float-dust magnitude, sign-pattern cataloguing is noise-tracking.
- **LOW — Zero within-mode strong pairs n=21 consecutive** (finding 6). Excluded 2 float-dust wallet_sum_pnl pairs (spread `× seed = +0.645`, basket `× minCurve = +0.545`). Highest genuine: spread `maxSpot × settlementLegs = −0.674`.
- **LOW — x402Total 5-filing up-down-up-down-up cadence breaks** (finding 7) — 09-06 → 09-07 shows two consecutive widenings, defeating the strict alternation hypothesis; mean stability holds within ~3% band.

## Carry-overs

- 25% seed encoding artifact (n=41 filed runs, pending [[compute-futures-seed-padding-bug]] fix; today's basket/spread/synth/x402 all show 25% seed outlier_pct, chronic).
- Basket/synth/x402 `minCurve` at 25% outlier_pct — small unique-count boundary clusters (8 unique values in basket minCurve), persistent by design; not a fresh signal.
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e−6 (basket σ=3.14e−14, spread σ=3.82e−12, synth/x402 σ=1.87e−14 — all below gate).
- **09-05 and 09-06 CSVs present in proof dir but unanalyzed (3-day filing gap)**; today's report anchors comparisons at 09-04 but trajectory notes were pulled programmatically from 09-05/06 for context. Root cause: skill scheduled at 06:00Z which is dead per [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]].
