# Compute-Futures EDA — 2026-08-06

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-06.csv` (144 rows, 4 modes)
**Run:** 2026-08-06
**Prior filed report:** 2026-08-03. Intermediate `2026-08-04.csv` exists but was never analyzed (08-04/08-05 compute-futures-eda slots dropped; 08-05 CSV entirely missing from deployer proof stream — no sweep produced or fleet-state push skipped). Diffs here span three sweep days.

## Top findings

1. **[HIGH — first ≥20% outlier fire since 08-01's basket settlementLegs 41.67%] Spread `maxCurve` = 25.00%, 9 outliers concentrated in 3 seeds.** Seeds 202608065 x3 low (66.86, all three roles), 2026080612 x3 low (67.45), 202608066 x3 high (74.47). IQR bounds [67.90, 74.32] on IQR=1.61 (narrowed from 08-03's 2.45 and 08-04's 0.86 — the tight-IQR pattern the SKILL warns about). The three seeds anchoring both tails happen to be the same three seeds driving 08-06's spread `role_pnl` extremes: 202608065 retail is −$6,258.57, 202608066 retail is the min-of-mode −$17,797.54 (largest single-row loss in the 21-filing window), 2026080612 retail is −$6,147.87 — but spread `role_pnl` outlier_pct is still 0.00% because the mode's std is $7,676 so those losses sit inside the fence. Same underlying seed-tail cluster surfacing through the low-std `maxCurve` column. Next step: track whether these three seeds re-anchor tomorrow — if they do, this is the first cross-column persistent-anchor pattern in spread mode (all prior anchor persistence has been in basket/synth/x402).

2. **[MEDIUM — 2nd consecutive filing at 16.67%] Basket `minCurve` = 16.67% persists across 08-04 → 08-06.** 6 outliers today from 2 seeds: 202608061 x3 at 0.1015 (low tail) + 202608064 x3 at 0.1160 (high tail). Same 16.67% rate fired on the un-analyzed 08-04 sweep (basket + synthetic + x402 all at 16.67% for `minCurve`). 08-06 the fire narrows to basket only (synth/x402 minCurve outlier_pct falls to 8.33% via one seed 202608061 x3 at 0.0405). Two-filing persistence at the same rate in the same mode-column is uncommon for this rotation-heavy pattern; call it out but hold as MEDIUM until it reaches n=3.

3. **[MEDIUM — fresh column-fire] Spread `settlementLegs` = 16.67%.** 6 outliers from 2 seeds, both at value 78: 202608064 x3 + 202608065 x3. Distribution {80:12, 81:9, 82:6, 78:6, 83:3} with IQR bounds [78.13, 83.13]. This is a straightforward left-tail cluster (three-value gap from 78 to 80 where nothing sits at 79) that pushes 78 outside the fence. Not a mode-collapse pattern (08-01 basket settlementLegs 41.67% at value 81 was the collapse shape); this is the opposite — a bimodal-ish distribution with 78 detached from the main cluster.

4. **[MEDIUM — multi-anchor shift from single-seed pattern] Four concurrent multi-mode anchor seeds, largest 202608064 at 15 outlier positions.** Anchor progression 07-28 (n=1 seed) → 07-30 (n=1) → 07-31 (n=1) → 08-01 (n=1) → 08-03 (n=1 primary + secondaries) → **08-06 (n=4 concurrent)**. Today's anchors: seed 202608064 (15 positions across basket minSpot+minCurve, synth minSpot, spread settlementLegs, x402 minSpot — new peak, 15 vs 08-03's 4-primary), 202608065 (9 positions: spread maxCurve+settlementLegs + x402 x402Total top-of-band $396.20), 202608068 (9 positions: realizedAbs across synth/basket/x402), 202608061 (9 positions: minCurve across synth/basket/x402). Shift from "one seed dominates whichever column it touches" to "four seeds each dominate a distinct column-cluster". Same fix path per [[compute-futures-12-seed-sample-too-small]]; a n=4-concurrent-anchor filing is what widening seeds is supposed to suppress.

5. **[LOW — the 08-03 zero-streak break did not persist] `realizedAbs × maxSpot` fully collapses back to jitter across all three modes.** 08-03 fired basket −0.853 + synth/x402 −0.832 (streak break at 3 modes simultaneously). 08-04 (un-analyzed): basket +0.212 + synth/x402 +0.304 (already collapsed and sign-flipped by day+1). **08-06: basket +0.119 + synth/x402 +0.138** (jitter). Spread went 08-03 +0.043 → 08-04 +0.023 → **08-06 −0.446** (deepened but stays under threshold). Streak of within-mode |r|≥0.8 zero-firings resumes at 1 filed run today (08-01 was the 13th, 08-03 broke, no filed intermediate, 08-06 restarts). Interpretation: 08-03 was a single-draw regime coincidence, not the start of a structural signal — rotation-not-persistence class re-confirmed as the dominant behavior.

6. **[LOW — recurring, 21st filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608061`–`202608069`), 36 rows 10-char seed (`2026080610`–`2026080612`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

7. **[LOW — three-filing decline REVERSES on x402Total] Mean $340.13 (up $17.28 vs 08-03's $322.85, up $12.35 vs 08-04's $327.78). Sum $12,244.81 (up $622.33 vs 08-03).** Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 (bottom) → **08-06 $340.13**. The 08-03-noted three-filing decline arrested on the un-analyzed 08-04 sweep and reverses today. Top-end $396.20 (up from 08-03's $385.19, but still under 07-31's $409.32 record and 08-04's $405.09). Not a promotable trend — decline structure was a two-filing dip that snapped back.

8. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.64e−14 (basket), −5.40e−13 (spread), +7.77e−16 (synth/x402). Std 3.20e−14 (basket), 4.81e−12 (spread), 8.46e−15 (synth/x402). All ≥11 orders under the |mean| > 10 / std > 100 gates. Spread sign flipped +→− vs 08-03 with magnitude down 5x (float-dust regime; not interpretive). Basket σ up ~17% vs 08-03; synth/x402 σ up ~32%.

9. **[PASS] Schema intact.** All 17 columns present (`seed`, `mode`, `live`, `settlement`, `spotSource`, `role`, `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `rounds`, `settlementLegs`, `x402Total`, `deniedWorkers`). Zero nulls in every column. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, and all price columns except `x402Total`).

10. **[PASS] Row/column shape unchanged vs 08-03.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.64e−14  | 3.20e−14  | OK |
| spread    | −5.40e−13  | 4.81e−12  | OK |
| synthetic | +7.77e−16  | 8.46e−15  | OK |
| x402      | +7.77e−16  | 8.46e−15  | OK |

## Within-mode signal

- **Zero strong pairs |r|≥0.8 in any mode today.** Streak of zero-firing sweeps resumes at n=1 filed run after 08-03's three-mode simultaneous break. Basket highest today: `realizedAbs × wallet_sum_pnl` = −0.638 (float-dust artifact per SKILL exclusion, excluded from ranking). Spread highest: `realizedAbs × minSpot` = +0.643. Synth/x402 highest: `wallet_sum_pnl × realizedAbs` = −0.514 (excluded), then `wallet_sum_pnl × maxCurve` = +0.437 (excluded). No structural signal survives after the wallet_sum_pnl float-dust filter.
- **Spread `maxCurve` HIGH fire mirrors the same 3-seed cluster driving spread's `role_pnl` extremes.** Seeds 202608065 + 202608066 + 2026080612 anchor both tails of `maxCurve`; the *same three seeds* produce spread's three largest retail losses ($−6,259 / $−17,798 / $−6,148). Interpretation: within these 12 seeds, spread's price-band top (maxCurve) sits at a mode-collapsed cluster [68.96, 71.74] with three outlier draws pulling both tails, and those same outlier draws happen to be the ones with the largest retail-side rebalancing losses. Whether this is structural (extreme maxCurve draws mechanically drive retail losses) or coincidental at n=12 seeds is exactly the question widening seeds would answer. Watch tomorrow.
- **Multi-anchor pattern is the new shape.** Four seeds today each anchor a distinct column-cluster (202608064 → all-mode minSpot/minCurve/settlementLegs, 202608065 → spread maxCurve + x402Total top, 202608068 → all-mode realizedAbs, 202608061 → all-mode minCurve low tail). This is a structural change from the "single dominant anchor" pattern the prior 5 filings showed. Consistent with a 12-seed draw where the tails happen to concentrate in multiple distinct seeds rather than one. Fix (widen seeds) still applies, but the finding class rotates from "single anchor n=X" to "concurrent anchors n=X".
- **08-03's `realizedAbs × maxSpot` three-mode fire fully collapses.** Neither the same pair nor a same-mode-cluster near-miss survives to 08-06. The rotation-not-persistence class re-confirms.

## Row count and shape vs 2026-08-03

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface migrates from basket role_pnl + synth/x402 realizedAbs (08-03) to spread maxCurve + spread settlementLegs + basket minCurve (08-06).** 08-03 fired 0 HIGH + 3 fresh MEDIUM (basket role_pnl 13.89 + synth/x402 role_pnl 11.11 + synth/x402 realizedAbs 16.67) + 1 pattern MEDIUM (single-anchor n=5). 08-06 fires **1 HIGH (spread maxCurve 25.00) + 2 fresh MEDIUM (spread settlementLegs 16.67 + basket minCurve 16.67 — the latter is 2nd-consecutive per un-analyzed 08-04) + 1 pattern MEDIUM (multi-anchor n=4 concurrent)**. The 08-03 fires (basket role_pnl + synth/x402 role_pnl + synth/x402 realizedAbs) all collapsed to 0% or <10%. Column rotation continues; today's shift centers on spread (first HIGH-fire in spread mode in the 21-filing window) and on the persistent basket `minCurve` cell that survived un-analyzed 08-04.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, and all price columns between synth and x402; only `x402Total` distinguishes x402). Same-row outliers in synth/x402 for `realizedAbs`, `minSpot`, `minCurve` this filing are the same physical rows, counted once per SKILL rule.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 10+ orders under the threshold everywhere (highest is spread's 4.81e−12). Excluded pairs today: basket `wallet_sum_pnl × realizedAbs` = −0.638, basket `wallet_sum_pnl × minSpot` = +0.536, synth/x402 `wallet_sum_pnl × realizedAbs` = −0.514, synth/x402 `wallet_sum_pnl × maxCurve` = +0.437, spread `wallet_sum_pnl × settlementLegs` = +0.498. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 21st consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence.** All three 08-03 MEDIUM column-fires (basket role_pnl 13.89 → 0.00, synth/x402 role_pnl 11.11 → 0.00, synth/x402 realizedAbs 16.67 → 8.33) collapsed today. Three fresh column-fires opened (spread maxCurve HIGH 25.00, spread settlementLegs 16.67, basket minCurve 16.67 — with basket minCurve confirmed as 2-filing persistence via un-analyzed 08-04). Multi-anchor pattern MEDIUM re-fires with structural shape change (n=4 concurrent, not n=X single).
- **Cross-mode single-seed anchor pattern SHIFTS TO MULTI-ANCHOR** — series 07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314 → 08-01 seed 202608012 → 08-03 seed 202608038 → **08-06 four concurrent seeds (202608061, 202608064, 202608065, 202608068)**. New peak in per-seed count: 202608064 at 15 positions (vs 08-03's 4-primary + secondaries). Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **x402 `settlementLegs × x402Total` remains jitter.** Series (15 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → **+0.505** (up from 08-03 but still under 0.8 threshold). Trending back positive; no interpretive story yet.
- **x402Total decline REVERSES.** 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 (arrested) → **08-06 $340.13** (mean). Not a promotable trend after all — the 08-03-noted three-filing decline was a two-filing dip that snapped back.
- **08-03's `realizedAbs × maxSpot` three-mode |r|≥0.8 fire was single-filing.** 08-04 (un-analyzed) already collapsed to jitter (basket +0.212, synth/x402 +0.304); 08-06 confirms (basket +0.119, synth/x402 +0.138). Not a structural signal — rotation-not-persistence class holds. Zero-firing streak restarts at 1 filed run today.
- **08-04 and 08-05 gap in compute-futures-eda filings.** 08-04.csv exists but was never analyzed (skill did not run or exited without writing). 08-05 CSV is missing entirely (`memory/gitlawb-compute-futures-proofs/` has 08-04 then 08-06 — deployer skipped 08-05). Per memory index, 08-05's compute-futures-eda ran in the early-cluster pocket at 06:11:51Z but produced no report — likely the schema/date check hit the un-updated CSV or the fetch found nothing new. Backfill 08-04 only if pattern verification requires filling the 3-day gap.
