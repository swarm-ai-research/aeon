# Compute-Futures EDA — 2026-07-10

**Source:** `memory/gitlawb-compute-futures-proofs/2026-07-10.csv` (144 rows, 4 modes)
**Run:** 2026-07-11

## Top findings

1. **[MEDIUM] basket `settlementLegs` outlier_pct 25.00% (was 0.00% yesterday).** Nine of 36 basket rows outside IQR whiskers [76.8, 82.8]: 3 rows at 73, 6 rows at 83, majority band {77×3, 79×9, 80×12, 82×3}. Discrete integer distribution with mean 79.58 / std 2.63. Basket `settlementLegs` last crossed the MEDIUM gate on 2026-06-28 at 33% — same (mode, column) firing again after a 12-day gap. Consistent with the 12-seed alarm-without-signal shape (the fired column rotates: 07-09 spread realizedAbs 16.67% → today basket settlementLegs 25%). Watch: if basket settlementLegs > 10% recurs tomorrow, escalate to signal candidate. [[compute-futures-12-seed-sample-too-small]]
2. **[MEDIUM] spread `minSpot` outlier_pct 16.67% (was 0.00% yesterday).** Six of 36 spread rows outside IQR whiskers [41.10, 45.34], all at the low end: min $40.08, max $44.80, mean $42.97, std $1.41. First time spread `minSpot` fires above the 10% gate in the run window. Same 12-seed jitter shape as prior single-day fires (07-09 spread realizedAbs, 07-07 basket maxCurve, 07-05 basket wallet_sum_pnl). Log-and-watch.
3. **[LOW — collapse of yesterday's leader] spread `realizedAbs` 16.67% → 8.33%.** Yesterday's leader dropped to 3/36 outliers today (mean $46,224, std $14,104, range $31,487–$84,349). Reverts to baseline within one seed rotation — same 12-seed churn shape.
4. **[LOW — third consecutive validation of the float-dust filter claim] x402 `wallet_sum_pnl × x402Total` +0.881 → +0.088.** Yesterday's gate-crossing pair collapsed within one run. No |r| ≥ 0.8 pair fires in any mode today — first zero-crossing run since 07-07. Synth/x402 `wallet_sum_pnl × settlementLegs`: −0.874 (07-08) → +0.225 (07-09) → −0.089 (today). Continues to validate [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ(wallet_sum_pnl) still ≤ 6.4e−12 in every mode, twelve+ orders below the |r| ≥ 0.8 filter threshold.
5. **[LOW — recurring] x402Total range $280.53–$383.85, mean $329.00, total $11,844.09.** Sixth consecutive movement inside the ~$100 band (07-03 $273–$367 → 07-05 $297–$421 → 07-07 $277–$371 → 07-08 $301–$400 → 07-09 $283–$387 → today $281–$384). Mean drifts within ±$25 of $335 across six runs. `x402Total × settlementLegs` r = +0.216 (was +0.143 on 07-09, −0.237 on 07-08). Structural link continues to churn sub-threshold. Not trending.
6. **[LOW — recurring] Seed-encoding artifact unchanged.** 108 rows carry 9-digit seeds (202607101–9), 36 rows carry 10-digit (`2026071010`, `2026071011`, `2026071012`). `seed` outlier_pct = 25% in every mode, fourteenth consecutive run. One-line pad in `scenario-sweep.mjs` still pending. [[compute-futures-seed-padding-bug]]
7. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +4.85e−15 (basket) → −2.63e−12 (spread); std 9.49e−15 (synth/x402) → 6.43e−12 (spread). All twelve-plus orders of magnitude under the |mean|>10 / std>100 gates. spread std 6.43e−12 (was 6.41e−12 on 07-09; essentially identical).
8. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 reconfirmed at role level (max |diff| = 0.0 across role_pnl, wallet_sum_pnl, realizedAbs, minSpot, maxSpot, minCurve, maxCurve, settlementLegs). Only `x402Total` distinguishes x402.
9. **[PASS] Zero |r| ≥ 0.8 pairs in any mode.** First zero-crossing run since 07-07. Directly validates the float-dust filter — every prior gate crossing since 07-07 involved `wallet_sum_pnl` (spread realizedAbs 07-07, synth/x402 settlementLegs 07-08, x402 x402Total 07-09) and every one self-cleared on the next rotation.
10. **[PASS] No CRITICAL/HIGH findings.** No schema drift, no row-count change, no conservation break, no new constant-column drift, no cross-mode column addition.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +4.85e−15  | 2.11e−14  | OK |
| spread    | −2.63e−12  | 6.43e−12  | OK |
| synthetic | −2.98e−15  | 9.49e−15  | OK |
| x402      | −2.98e−15  | 9.49e−15  | OK |

## Within-mode signal

- **basket `settlementLegs` MEDIUM outlier cluster (25.00%).** Discrete integer column with a heavy majority band (79–80 covers 21/36 rows) and thin tails at 73 (3 rows) and 83 (6 rows). Compares to synthetic/x402 basket-adjacent `settlementLegs` distributions that spread more evenly across 75–83. The 12-seed jitter carry-over says one day of >10% outlier_pct in a fresh (mode, column) is alarm-without-signal until it repeats. Log for tomorrow.
- **spread `minSpot` MEDIUM outlier cluster (16.67%).** Six low-end tail rows below the IQR whisker $41.10. Underlying regime still stable (spread minSpot mean $42.97 today vs the ~$43 band in prior runs); the cluster reflects six seeds landing near $40 while the rest sit above $42. Log for tomorrow.
- **role_pnl scale per mode (regime unchanged):** spread σ = $8,413 (μ = −$390), basket σ = $36.6 (μ = −$1.15), synthetic/x402 σ = $14.3 (μ = −$0.38). Spread tightened vs 07-09 ($9,545) — inside 12-seed jitter. Basket and synth/x402 widened modestly. Continues to dominate pooled stats by three orders of magnitude. Per-mode stratification remains the right default.
- **x402-specific structural pairs sub-threshold:** `minSpot × x402Total` −0.353 (was −0.631 on 07-09), `maxSpot × x402Total` −0.337 (was −0.452), `minCurve × x402Total` +0.267 (was +0.069), `maxCurve × x402Total` +0.569 (was +0.213). Still jittering; no candidate strengthens two days in a row.

## Row count and shape vs 2026-07-09

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column set diff).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows nine-digit, 36 rows ten-digit).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold. `live` is True in basket/spread, False in synthetic/x402; `settlement` is `cash` everywhere except x402 (`x402`); `spotSource` is `darkbloom-basket`/`darkbloom-spread`/`synthetic`/`synthetic` per mode.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — the |r| ≥ 0.8 signal on wallet_sum_pnl is a numerical-drift artifact per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. Third consecutive day of self-clearing: 07-08 `settlementLegs` −0.874 → 07-09 +0.225 → today −0.089; 07-09 `x402Total` +0.881 → today +0.088. Filter continues to prove out.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string. One-line pad in `scenario-sweep.mjs` still pending.
- **12-seed outlier_pct instability** — the (mode, column) pair that fires shifts each run: 06-26 spread minCurve 25% → 06-28 spread settlementLegs 33% → 07-03 synth/x402 settlementLegs 16.67% → 07-05 basket wallet_sum_pnl 16.67% + synth/x402 minCurve 16.67% → 07-07 basket maxCurve 16.67% + synth/x402 maxCurve 16.67% → 07-08 (all flags 8.33%) → 07-09 spread realizedAbs 16.67% → 07-10 basket settlementLegs 25% + spread minSpot 16.67%. Treat any single (mode, column) outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `realizedAbs × wallet_sum_pnl`: [−0.06, −0.745, +0.048, −0.467, −0.752, +0.190, +0.219, −0.332]. Synth/x402 `wallet_sum_pnl × settlementLegs`: +0.501 (07-07) → −0.874 (07-08) → +0.225 (07-09) → −0.089 (today). x402 `wallet_sum_pnl × x402Total`: +0.881 (07-09) → +0.088 (today). Consistent 12-seed jitter until any pair confirms two runs in a row.
