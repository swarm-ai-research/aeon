# Compute-Futures EDA — 2026-08-16

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-16.csv` (144 rows, 4 modes)
**Run:** 2026-08-17
**Prior filed report:** 2026-08-15 (single-day gap).

## Top findings

1. **[MEDIUM — extension, promoted class n=10 consecutive filed runs, STABILIZES near shallow floor] Spread `role_pnl` retail mean −$7,966 (deepens $173 vs 08-15's −$7,793 → essentially flat); top-5 losses 4/5 retail (down from 5/5 on 08-15); top-5 gains include 1 retail (up from 0/5).** Top-5 losses: 202608168 retail −$22,288 / 202608164 retail −$22,071 / 2026081612 retail −$20,141 / **2026081610 operator −$12,315 (only non-retail top-loss)** / 202608169 retail −$10,404. Top-5 gains: 202608168 operator +$17,106 / **2026081610 retail +$13,174** / 202608164 operator +$10,739 / 2026081612 operator +$8,856 / 2026081612 analyst +$8,374 (3 operator + 1 retail + 1 analyst). Per-role: retail mean **−$7,966** (std $10,112, min −$22,288, max +$13,174) — deepens by $173 vs 08-15's −$7,793 (essentially flat, first non-directional-continuation after 3-deepen-then-1-shallow oscillation: −$5,984 → −$9,473 → −$11,092 → −$7,793 → **−$7,966**); operator +$3,969 (std $7,219, down from 08-15's +$4,281); analyst +$1,839 (std $3,825, down from +$2,307). Extends [[compute-futures-spread-retail-loss-concentration]] from n=9 to **n=10 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11, 08-12, 08-13, 08-14, 08-15, 08-16). Class direction (retail-losers, operator/analyst-winners) unchanged, but purity slips: **first operator top-loss AND first retail top-gain since 08-13's mixed pattern**. Under IQR fence, spread `role_pnl` at **11.11% (4 rows)** today — MEDIUM per SKILL rule; anchored by the three retail-loss tails + one operator-loss (2026081610).

2. **[LOW — n=7 consecutive filed runs, CEMENTED promoted class holds exactly] basket/synth scale-ratio HELD at 3.0250 exactly for `maxSpot`/`minSpot`.** Seven-filing series (08-10 through 08-16). `maxSpot` basket/synth: min=max=3.0250 (deterministic constant, 12/12 seed pairs to 10 decimal places, floating-point noise beyond the 15th). `minSpot` basket/synth: min=max=3.0250 (12/12 pairs). `maxCurve` basket/synth: mean 3.0241 (tightens further vs 08-15's 3.0235). `minCurve` basket/synth: mean 3.0180 (loosens marginally vs 08-15's 3.0189). `realizedAbs` basket/synth: mean 3.1364 (widens vs 08-15's 3.0518, moves 3.4% above the 3.0250 anchor). Consistent with promoted note [[compute-futures-basket-synth-3025x-multiplier]]; promotion strengthens at n=7 with per-seed `maxSpot`/`minSpot` byte-identical to the anchor.

3. **[LOW — n=8 consecutive filed runs, x402 pair SIGN FLIPS AGAIN 6th in 24 filings] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, 08-11 zero, 08-12 zero, 08-13 zero, 08-14 zero, 08-15 zero, **08-16 zero**. Extends 08-15's n=7 to n=8. Highest within-mode pairs today: **x402 `realizedAbs × x402Total` = +0.7248** (structural signal, sub-threshold), basket `realizedAbs × maxCurve` = −0.6805, x402 `wallet_sum_pnl × x402Total` = −0.6580 (float dust; excluded from findings per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]), spread `minSpot × settlementLegs` = −0.6063, synth `minCurve × maxCurve` = −0.5046. x402 `settlementLegs × x402Total` today **−0.1119** — 24-filing series: … +0.475, +0.558, +0.255, +0.771, −0.270 (08-14), +0.293 (08-15), **−0.112** (08-16) — SIGN FLIPS AGAIN + → − and magnitude COLLAPSES (|0.293| → |0.112|, drops 62%); alternation-not-persistence continues (**6th sign-flip in 24 filings**). n=12 seeds still systematically insufficient to stabilize any |r|≥0.8; [[compute-futures-12-seed-sample-too-small]] motivation reinforced at n=8.

4. **[LOW — asymmetric translation, 3-day cycle NEARLY-CLOSED and RANGE NARROWS] x402Total range shifts UP LOW-side, DOWN HIGH-side: $294.87–$378.42 (std $26.03, range narrows $121.89 → $83.55, −$38.34).** 08-15 was $262.66–$384.55 (range $121.89); today $294.87–$378.42 (range $83.55, **range narrows −$38.34, largest single-day narrowing in 12 filings**). New LOW $294.87 **above** 08-15's $262.66 by **+$32.21** (partial rebound of 08-15's −$24.62 floor drop; 3-day cycle from 08-13's $263.71 → 08-14 $287.28 → 08-15 $262.66 → **08-16 $294.87** completes with a fresh $31.16-above-08-13-floor lift); new HIGH $378.42 **below** 08-15's $384.55 by **−$6.13** (continues the two-day ceiling decline; both 08-15's −$14.38 and today's −$6.13 move HIGH downward, no rebound yet). Mean $323.04 (−$4.54 vs 08-15's $327.58, small continued mean-shift down). Sum $3,876.45 (day-over-day ~identical shape at ~n=12 seeds). Std $26.03 (compresses from 08-15's $30.53). Shape: **asymmetric convergence** (LOW rebounds while HIGH continues declining) → the mean-reverting round-trip on the LOW side closes with a 3.16-above-08-13-floor overshoot, while the HIGH side has not yet begun mean-reversion. LOW anchor: 202608165 (3-role tie at $294.87); HIGH anchor: 202608167 (3-role tie at $378.42). x402Total outlier_pct = 0.00% in x402 mode today (down from 08-15's 16.67%) — range narrowing pulls tails inside IQR fence; excluded from ranking per SKILL bimodal rule regardless.

5. **[LOW — anchor concentration REVERSES from five-rotation back to super-single dense] One super-anchor 202608165 carries 18 positions (58% of 31 non-seed outlier positions), followed by 202608166 at 9 positions.** Anchor breakdown: **202608165 (18 positions, all 4 modes: basket/spread/synth/x402, columns minSpot LOW + maxSpot LOW + maxCurve LOW)**; **202608166 (9 positions, 3 modes basket/synth/x402, columns wallet_sum_pnl float dust)**; 202608168 (2 positions, spread role_pnl LOW); 202608164 (1 position, spread role_pnl LOW); 2026081612 (1 position, spread role_pnl LOW). Total 31 outlier positions across all modes/columns (excluding seed). Class series: 08-10 super-single dense (202608106, 15 pos, single-mode) → 08-11 tri-super (9+9+9, disjoint) → 08-12 two-super (9+8, disjoint) → 08-13 super-single dense (202608137, 13 pos) → 08-14 hyper-single (202608144, 14 pos, quadruple-mode) → 08-15 five-anchor rotation (18+18+12+12+12) → **08-16 super-single dense (202608165, 18 pos, quadruple-mode)**. Reversion to concentrated pattern after 1-day five-rotation interlude; today's super-anchor spans all 4 modes like 08-14's hyper-single. Zero overlap with 08-15's five anchors (202608158/2026081512/202608155/202608152/202608159 fire zero outlier positions in 08-16).

6. **[LOW — under-index, sharpest drop in class] Padded-seed outlier positions COLLAPSE to 3.2% (vs 25% row-share, 08-15's 29.6% → 08-16's 3.2%, largest single-day drop in class).** Padded seeds (2026081610/11/12) hold 36 of 144 rows (25%). Today they contribute **1 of 31 non-seed outlier positions (3.2%)**, driven solely by 2026081612's single spread role_pnl LOW outlier. 2026081610 contributes 0 non-seed outlier positions (its −$12,315 operator spread loss lands inside spread's `role_pnl` IQR fence). 2026081611 contributes 0. Series: 08-12 padded 42% over → 08-13 25% proportional → 08-14 11.5% under → 08-15 29.6% roughly proportional/mildly over → **08-16 3.2% severely under**. Class continues rotation-not-persistence with steepest under-index yet; padded encoding does not systematically over- or under-select — today's concentration is on unpadded seed 202608165.

7. **[LOW — recurring, 30th filed run] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608161`–`202608169`), 36 rows 10-char seed (`2026081610`–`2026081612`). `seed` outlier_pct = 25.00% in every mode. Today's super-anchor 202608165 is unpadded (9-char). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

8. **[LOW — non-promotable, TWO-DAY sign persistence] Conservation float dust ALL NEGATIVE across all modes for 2nd consecutive day.** `wallet_sum_pnl` mean per mode: basket **−6.66e−15** (vs 08-15 −9.75e−15; sign HOLDS negative, magnitude decreases 32%), spread **−1.11e−12** (vs 08-15 −1.85e−12; sign HOLDS negative, magnitude decreases 40%), synth **−3.37e−15** (vs 08-15 −7.03e−16; sign HOLDS negative, magnitude grows 4.8×), x402 **−3.37e−15** (identical to synth by [synth ≡ x402]). All four modes stay negative → **two-day sign persistence class n=2** (after the two-day + → − flip observed 08-14 → 08-15; prior alternation had been strict day-by-day). All modes remain 12+ orders under the |mean|>10 / std>100 gates. Per-mode `wallet_sum_pnl` IQR outliers: basket 0% (unchanged), spread 0% (down from 08-15's 8.33%), synth **8.33%** (up from 0%), x402 **8.33%** (up from 0%, symmetric with synth). σ still ≥8 orders under 1e-6 per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].

9. **[LOW — cross-mode ≥10% MEDIUM count HOLDS at 0 for 2nd consecutive day] Zero cross-mode ≥10% MEDIUM outliers today (spread `role_pnl` 11.11% is the promoted class, tracked separately).** Cross-mode ≥10% MEDIUM count series: 0 (08-13) → 2 (08-14: spread minSpot + maxSpot) → 0 (08-15) → **0 (08-16)**. 08-14's fresh spread spot-price MEDIUMs remain a single-run appearance not persisting. No new column has crossed the ≥10% threshold today.

10. **[LOW — sub-threshold structural signal] x402 `realizedAbs × x402Total` = +0.7248 is the highest within-mode pair on the board, but sits below the |r|≥0.8 gate.** Correlation series over recent filings shows this pair drifting upward but never breaching the strong-pair threshold; today's +0.7248 is the 2nd-highest in the last 5 filings. Physical interpretation: larger realized-absolute-notional in x402 mode co-moves with larger x402 settlement volume, which is expected but not yet a promotable finding at n=12 seeds. Compare with 08-15's top pair (basket `realizedAbs × minSpot` = +0.734 — same magnitude, different mode/pair) — top-pair board rotates.

11. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402).

12. **[PASS] Row/column shape unchanged vs 08-15.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −6.66e−15  | 3.02e−14  | OK |
| spread    | −1.11e−12  | 6.19e−12  | OK |
| synthetic | −3.37e−15  | 1.46e−14  | OK |
| x402      | −3.37e−15  | 1.46e−14  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=8 consecutive filed runs.** Highest today: **x402 `realizedAbs × x402Total` = +0.7248** (sub-threshold structural signal), basket `realizedAbs × maxCurve` = −0.6805, spread `minSpot × settlementLegs` = −0.6063, synth `minCurve × maxCurve` = −0.5046. x402 `settlementLegs × x402Total` sign-flips AGAIN from +0.293 (08-15) to **−0.112** (08-16) — 6th sign-flip in 24 filings, magnitude collapses 62%; alternation-not-persistence continues.
- **Retail-role loss concentration in spread — n=10 consecutive filed runs, promoted class STABILIZES near shallow floor.** Retail mean essentially flat at −$7,966 vs 08-15's −$7,793 (deepens $173 only); top-5 losses 4/5 retail (down from 5/5), top-5 gains include 1 retail (up from 0/5) — first cross-role mixing since 08-13. Purity slips one operator loss + one retail gain into top-5, class direction still holds.
- **basket/synth `maxSpot`/`minSpot` scale-ratio = 3.0250 (deterministic) at n=7 filed runs.** Byte-identical per-seed to 10 decimals; cemented and strengthening; consistent with promoted [[compute-futures-basket-synth-3025x-multiplier]].
- **Cross-mode ≥10% MEDIUM count HOLDS at 0.** No new spread spot-price recurrence; 08-14 spread minSpot + maxSpot appearance remains single-run.
- **x402Total range narrows sharply** — range $121.89 → $83.55 (−$38.34, largest single-day narrowing in 12 filings); LOW rebounds +$32.21 (partial mean-reversion of 08-15's −$24.62 drop), HIGH continues declining −$6.13 (asymmetric convergence). LOW-side round-trip completes with +$3.16 overshoot vs 08-13 floor.
- **Anchor concentration REVERSES back to super-single dense (202608165, 18 positions, quadruple-mode)** — 1-day interlude of five-rotation on 08-15 closes; today's super-anchor spans all 4 modes with unpadded seed. Zero overlap with 08-15's five-anchor set.
- **Padded-seed contribution collapses to 3.2%** — sharpest single-day drop in class from 08-15's 29.6% to 3.2% (vs proportional 25%); today's concentration is on unpadded 202608165. Rotation-not-persistence class continues.
- **Two-day conservation-sign persistence** — all four modes stay negative for 2nd consecutive day (n=2 class after prior strict day-by-day alternation).

## Row count and shape vs 2026-08-15

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface STABLE from 08-15 → 08-16.** 08-15: 0 HIGH, 1 MEDIUM (retail-loss n=9 shallowing). 08-16: 0 HIGH, **1 MEDIUM** (retail-loss n=10 stabilizing near shallow floor). Cross-mode ≥10% MEDIUM count holds at 0. Retail-loss promoted class carries forward and stabilizes.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's synth+x402 minSpot/maxSpot/maxCurve LOW cluster (202608165) and synth+x402 wallet_sum_pnl float-dust cluster (202608166) are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (3.0250× exactly for maxSpot/minSpot, ~3.02× for curves)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable across 08-10 through 08-16 (n=7).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. IQR-fenced wallet_sum_pnl outliers at 0%/0%/8.33%/8.33% today (synth+x402 symmetric via [synth ≡ x402]) — all float dust.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 30th consecutive filed run. Today's super-anchor 202608165 is unpadded. Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** Cross-mode ≥10% MEDIUM count holds at 0 for 2nd day; x402 `settlementLegs × x402Total` sign-flips 6th time (magnitude collapses 62%); only the retail-loss promoted class carries forward. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode anchor concentration migrates back to super-single dense** — series: 08-10 super-single (15 pos, single-mode) → 08-11 tri-super (disjoint) → 08-12 two-super (disjoint) → 08-13 super-single (13 pos) → 08-14 hyper-single (14 pos, quadruple-mode) → 08-15 five-anchor rotation (18+18+12+12+12) → **08-16 super-single dense (202608165, 18 pos, quadruple-mode)**. Anchors fully rotate day-over-day; zero overlap with 08-15's five-anchor set.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=10 (08-16).** Retail mean stabilizes near shallow floor after 3-deepen-then-1-shallow oscillation (−$5,984 → −$9,473 → −$11,092 → −$7,793 → **−$7,966** across 08-12 → 08-13 → 08-14 → 08-15 → 08-16). First cross-role mixing in top-5 since 08-13: 1 operator top-loss (2026081610) + 1 retail top-gain (2026081610, same seed) enter today.
- **x402Total 3-day mean-reverting round-trip closes on LOW side, HIGH still declining** — 08-13 range $263.71–$373.84 → 08-14 $287.28–$398.93 (up +$23/+$25) → 08-15 $262.66–$384.55 (LOW down −$25, HIGH down −$14) → 08-16 $294.87–$378.42 (LOW up +$32 with +$3.16 overshoot vs 08-13 floor, HIGH down −$6). Range narrows to $83.55 (−$38.34 vs 08-15) — largest single-day narrowing in 12 filings.
