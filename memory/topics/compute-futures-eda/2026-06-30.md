# Compute-Futures EDA — 2026-06-30

**Source:** `memory/gitlawb-compute-futures-proofs/2026-06-30.csv` (144 rows, 4 modes)
**Run:** 2026-07-01

## Top findings

1. **[MEDIUM] spread `maxSpot` outlier_pct = 16.67% (6/36)** — six rows at the two top values (three each at 71.10 and 72.06), IQR fence [61.64, 69.37] with IQR=1.93. The underlying spread `maxSpot` is 12 unique values across 36 rows, range 63.28–72.06 (mean 66.26, std 2.69) — normal tail thickness on a tightly clustered mid-range, not a regime shift. Same 12-seed IQR-brittleness flagged 2026-06-28 (`settlementLegs` 33.33%) and 2026-06-27 (`minCurve` 25%), with today's top spread flag flipping to a different column again. Next step unchanged: bump `scenario-sweep.mjs` seed count to ≥30 or move outlier detection to a MAD-based statistic per [[compute-futures-12-seed-sample-too-small]].
2. **[LOW] Strongest within-mode |r| = 0.68 (`seed × minSpot`, unchanged)** — same recurring seed-encoding artifact across basket / synthetic / x402, driven by the 9-vs-10-digit seed split from [[compute-futures-seed-padding-bug]]. Spread's strongest today is `minSpot × settlementLegs` +0.52 (yesterday's cross-noise-vs-driver top-line, spread `wallet_sum_pnl × minSpot` −0.745, has collapsed to −0.187 — confirming the sub-threshold correlation churn carry-over). No within-mode pair crosses 0.8.
3. **[LOW — recurring] Seed-encoding artifact unchanged.** Three seeds are 10-digit (`2026063010`, `2026063011`, `2026063012`); the other nine are 9-digit. Outlier detector flags `seed` at exactly 25% in every mode (same as every prior run). One-line pad in `scenario-sweep.mjs` still pending.
4. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean spans −2.56e−13 → +1.71e−15, std 9.30e−15 → 5.12e−12 — float dust across all modes. No mode crosses the |mean|>10 or std>100 conservation-drift gate.
5. **[PASS] No within-mode correlations at |r| ≥ 0.8.** Top pairs by mode: basket `seed × minSpot` −0.682 and `maxCurve × realizedAbs` +0.636; spread `minSpot × settlementLegs` +0.519, `minSpot × realizedAbs` −0.483, `maxSpot × settlementLegs` −0.480; synthetic/x402 `seed × minSpot` −0.682 and `maxCurve × realizedAbs` +0.587. Recurring `seed × minSpot` is the seed-encoding artifact bleeding through; nothing structurally new.
6. **[PASS] Schema intact.** All 17 columns present, zero nulls, 144 rows = 4 modes × 12 seeds × 3 roles. synthetic ≡ x402 at role level reconfirmed (max |diff| = 0.0 across `role_pnl`, `wallet_sum_pnl`, `realizedAbs`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `settlementLegs`). Only `x402Total` distinguishes x402 (μ=$316.79, σ=$33.95, range $275.34–$362.54 — slightly cooler and wider than 2026-06-28's $287.19–$385.65).

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.71e−15  | 2.86e−14  | OK |
| spread    | −2.56e−13  | 5.12e−12  | OK |
| synthetic | +1.71e−16  | 9.30e−15  | OK |
| x402      | +1.71e−16  | 9.30e−15  | OK |

## Within-mode signal

- **role_pnl scale per mode (regime unchanged):** spread std ≈ $10,570 (μ=−$494), basket std ≈ $24 (μ=−$0.45), synthetic/x402 std ≈ $9.1 (μ=−$0.18). spread continues to dominate pooled stats by three orders of magnitude — per-mode stratification remains the right default. All four means are near-zero relative to their scales (spread |μ/σ| ≈ 0.047; basket ≈ 0.019; synthetic/x402 ≈ 0.019) — consistent with a mostly-zero-sum role P&L (the sweep still lists three roles per (seed, mode) whose role_pnls should nearly cancel).
- **Same three sub-threshold structural pairs in basket:** `maxCurve × realizedAbs` +0.636, `minCurve × settlementLegs` −0.550, `maxCurve × minCurve` −0.542. All three are curve-envelope × settlement-scale artifacts of the 12-seed sample; none is new, none crosses 0.8.
- **x402 mirrors synthetic exactly at the role level** — the role-level numeric columns differ by 0.0. The x402-specific structural signal today is `maxSpot × x402Total` −0.530: higher underlying spot corresponds to a lower `x402Total`, i.e. cheaper x402 fees when spot is high. Below the 0.8 bar, but the sign is consistent with a spot-normalized fee. Watch across days.
- **Yesterday's spread degenerate cluster is gone:** `settlementLegs` outlier_pct dropped 33.33% → 0.00%. The dominant flag rotated to `maxSpot` (16.67%). Same 12-seed IQR jitter — different column each day.

## Row count and shape vs 2026-06-28

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set).
- Seeds: 12 → 12 (encoding artifact unchanged).
- Constants by design (`rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode): all hold.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed (max |diff| = 0.0). Only `x402Total` distinguishes x402. Don't re-report.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` IQR outliers at single-digit percentages** — float-point dust around zero. Don't escalate as long as conservation gates (|mean|>10, std>100) hold. Today: basket 8.33% (3/36), spread 8.33% (3/36), synthetic/x402 0%.
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded YYYYMMDD+index seed string. One-line pad in `scenario-sweep.mjs`.
- **12-seed outlier_pct instability** — confirmed again today: yesterday's top spread flag (`settlementLegs` 33.33%) is at 0% today; today's top spread flag (`maxSpot` 16.67%) was at 0% yesterday. Treat any single-column outlier_pct in the 8–33% band as alarm-without-signal until seed count crosses ~30.
- **Sub-threshold within-mode correlation churn** — spread `wallet_sum_pnl × minSpot` went −0.745 → −0.187 in one day; basket `wallet_sum_pnl × minCurve` continues to oscillate. All are float-noise × seed-driver products of a 12-seed sample; ignore until one crosses 0.8.
