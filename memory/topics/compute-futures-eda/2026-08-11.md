# Compute-Futures EDA — 2026-08-11

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-11.csv` (144 rows, 4 modes)
**Run:** 2026-08-12
**Prior filed report:** 2026-08-10 (single-day gap).

## Top findings

1. **[MEDIUM — resurface, cross-mode 2-seed cluster, both-tail] `maxSpot` fires at 16.67% in basket AND synth≡x402 simultaneously, split 3-LOW + 3-HIGH across two disjoint seeds.** 6 outliers per mode = 2 seeds × 3 roles. LOW anchor is padded seed 2026081110 (basket 0.2423 / synth 0.0801 / x402 0.0801); HIGH anchor is unpadded 202608119 (basket 0.2742 / synth 0.0906 / x402 0.0906). Fence basket [0.2431, 0.2705] on IQR≈0.0069; fence synth/x402 [0.0804, 0.0894] on IQR≈0.0023. **Basket/synth ratio = EXACTLY 3.025 at every seed** (min=max=3.025 across all 12 seeds — deterministic multiplier confirmed for `maxSpot`/`minSpot`). Rotation-not-persistence class continues but flips back on: 08-08 cross-mode `maxSpot` cluster (fresh) → 08-10 collapsed → **08-11 resurfaces with fully rotated anchor pair**. Zero seed overlap vs 08-08 anchors (202608088 + padded 2026080812). Both-tail split is new — 08-08 was HIGH-tail only. Anchor-count held at 2.

2. **[MEDIUM — promoted class, n=5 consecutive filed runs] Spread `role_pnl` top-5 losses 100% retail, most extreme yet at seed 202608111 retail −$24,696.** Top-5: 202608111 retail −$24,696 / 202608114 retail −$14,882 / 202608119 retail −$13,072 / 202608117 retail −$11,406 / 2026081110 retail −$11,336. Retail mean spread P&L **−$9,943** vs analyst +$2,089 / operator +$6,450 — bimodal split by role deepens. Sub-threshold at IQR (spread `role_pnl` outlier_pct = 0% under IQR fence today), but the retail-loss-concentration structural pattern per [[compute-futures-spread-retail-loss-concentration]] extends from n=4 to **n=5 consecutive filed runs** (08-06, 08-07, 08-08, 08-10, 08-11). Loss magnitude at seed 202608111 (−$24,696) sets a new floor vs 08-10's 202608109 retail (−$20,016) — retail-loss depth worsening. Next step: bring role-weighted P&L or retail-drop proposal forward with the widening magnitude as new evidence.

3. **[MEDIUM — fresh, cross-mode 1-seed cluster (LOW-tail only)] `minCurve` fires at 8.33% in basket AND synth≡x402 anchored by seed 202608112.** 3 outliers per mode = 1 seed × 3 roles. basket 0.127 (fence [0.1315, 0.1385]) / synth 0.042 (fence [0.0436, 0.0510]) / x402 0.042. Basket/synth `minCurve` ratio ~3.02× mean (2.989–3.045 across seeds — small per-seed variation, unlike the strict `maxSpot`/`minSpot` constant). Under the ≥10% threshold, but flagged as MEDIUM per SKILL — same shape as 08-10 finding #4 (cross-mode single-seed LOW-tail under-fire) — direction-flip class continues.

4. **[MEDIUM — 3 super-anchor seeds (9-pos each) — anchor concentration diffuses vs 08-10.** Seed 202608119 (maxSpot HIGH cross-mode + spread retail loss #3), seed 2026081110 (maxSpot LOW cross-mode + spread retail loss #5), seed 202608112 (minCurve LOW cross-mode) each drive 9 outlier positions across 3 modes. **Anchor-count class step:** 07-28→08-03 single → 08-06 four concurrent → 08-07 diffuse → 08-08 super-single (5-pos) → 08-10 super-single dense (15-pos) → **08-11 tri-super-anchor (9+9+9-pos, three disjoint seeds)**. Concentration diffuses from 08-10 but total surface expands.

5. **[LOW — full collapse] 08-10's cross-mode 2-seed maxCurve cluster (202608106 + padded 2026081012) fully collapses.** Today: zero `maxCurve` outliers ≥10% in any mode. Following 08-10's step-down from 08-08's 25% → 08-10's 16.67% → **08-11's 0%** — total collapse in one filed-run gap. Rotation-not-persistence dominates; anchor-count-shrink series 3→2→0 across three filings.

6. **[LOW — full collapse] 08-10's basket `settlementLegs` 16.67% cluster (anchored 202608106 + 202608109) fully collapses.** Today: zero `settlementLegs` outliers ≥10% in any mode. One-filing lifetime for that fresh MEDIUM signal — matches the rotation-not-persistence class. 08-10's super-anchor seed 202608106 (15 positions) drops off the leaderboard entirely today (0 positions).

7. **[LOW — fresh sub-threshold, single-mode 1-seed] Basket `realizedAbs` 8.33% (n=3), single seed 202608117 at 155.14 (LOW-tail).** Fence basket [231.07, 280.69] on IQR=49.62. First basket-side `realizedAbs` fire in the filed-run window. Sub-threshold, not promoted.

8. **[LOW — fresh sub-threshold, single-mode 1-seed] Spread `realizedAbs` 8.33% (n=3), single seed 202608111 at 72,640 (HIGH-tail).** Fence spread [41,633; 53,092] on IQR=11,459. Note 202608111 also anchors today's most extreme retail spread loss (finding #2) — spread mode's super-anchor for the day. Sub-threshold, not promoted individually but reinforces finding #2's concentration.

9. **[LOW — n=3 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** 08-08 zero, 08-10 zero, **08-11 zero**. Zero-firing streak crosses n=3 consecutive filed runs — first cross-mode signal to hit the n=3 promotion criterion at the *absence* of within-mode strong pairs. Interpretation: n=12 seeds is too few to stabilize |r|≥0.8 within any single mode; every pairwise relationship stays fluid across sweep-days.

10. **[LOW — basket/synth scale-ratio HELD at ~3.025× — no continued drift]** Multiplier stability confirmed at n=2 filed runs (08-10 ~3.02× → 08-11 3.025×). `maxSpot`/`minSpot` multiplier is EXACTLY 3.025 at every seed (deterministic); `maxCurve`/`minCurve` ~3.02 with small per-seed jitter (2.964–3.045); `realizedAbs` wider (2.608–3.278). **08-08→08-10 ~20% jump (2.5×→3.02×) now looks like a real deployer-code price-draw change**, not n=12 noise — stability at n=2 corroborates the hypothesis. n=3 filed runs needed for formal promotion; on track next filing.

11. **[LOW — recurring, 25th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608111`–`202608119`), 36 rows 10-char seed (`2026081110`–`2026081112`). `seed` outlier_pct = 25.00% in every mode. Padded-seed anchor concentration this filing: padded 2026081110 anchors maxSpot LOW cross-mode (9 positions). One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

12. **[LOW — non-promotable jitter] x402Total mean $322.57 (down $5.39 vs 08-10's $327.96).** Sum $11,612.46 (down $194.13). Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 → 08-06 $340.13 → 08-07 $326.38 → 08-08 $328.37 → 08-10 $327.96 → **08-11 $322.57** (matches 08-03 low). Range: $322–$346 across nine filings; no trend. Bottom-end today $274.10 (above 08-10's new-low $269.61); top-end $425.61 (**new all-time high**, exceeding 08-10's $381.61 by $43.55). Range widens on HIGH side; std $39.54 up from 08-10.

13. **[LOW] x402 `settlementLegs × x402Total` continues to alternate around zero.** 19-filing series: +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → +0.179 → +0.475 → **+0.558**. Sign held +; magnitude close to 08-10. Alternation-not-persistence continues.

14. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −2.87e−15 (basket), −7.89e−13 (spread), +3.76e−15 (synth/x402). Std 2.56e−14 (basket), 6.97e−12 (spread), 1.19e−14 (synth/x402). All ≥11 orders under the |mean|>10 / std>100 gates. Basket sign held − (magnitude similar to 08-10). Spread sign held − (magnitude down ~76% from 08-10's −3.26e−12 → today's −7.89e−13). Synth/x402 sign flipped − → + vs 08-10. Per-mode wallet_sum_pnl outliers surface at 25%/16.67%/0%/0% today (basket, spread, synth, x402) but excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ still ≥8 orders under the 1e-6 threshold everywhere.

15. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). basket/synth ratio 3.025× (matches 08-10) — same rank-order per seed at every scale.

16. **[PASS] Row/column shape unchanged vs 08-10.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −2.87e−15  | 2.56e−14  | OK |
| spread    | −7.89e−13  | 6.97e−12  | OK |
| synthetic | +3.76e−15  | 1.19e−14  | OK |
| x402      | +3.76e−15  | 1.19e−14  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode — n=3 consecutive filed runs.** First cross-mode "absence" signal to hit the n=3 promotion criterion. n=12 seeds is systematically insufficient to stabilize any within-mode pairwise correlation past 0.8. Motivation for [[compute-futures-12-seed-sample-too-small]] fix reinforced with n=3 evidence at the |r|-absence level.
- **Cross-mode 2-seed `maxSpot` cluster (202608119 HIGH + padded 2026081110 LOW)** — both anchors resurface cleanly in basket AND synth AND x402 with EXACT 3.025× basket/synth multiplier (deterministic constant for `maxSpot`/`minSpot`). Genuine cross-mode structural signal driven by per-seed input-draw quirk that surfaces identically at every price scale. Class continues from 08-08 with fully rotated anchor pair and new both-tail split.
- **Cross-mode 1-seed `minCurve` cluster (202608112 LOW)** — sub-threshold at 8.33% but shape mirrors the maxSpot cluster (same class, fewer anchors). Under-fire; watch for whether 202608112 climbs to 2-seed status next filing.
- **Retail-role loss concentration in spread — n=5 consecutive filed runs (08-06, 08-07, 08-08, 08-10, 08-11).** Top-5 spread losses 100% retail today; new floor at 202608111 retail −$24,696 (vs 08-10's −$20,016). Retail mean spread P&L −$9,943 vs analyst +$2,089 / operator +$6,450 — bimodal split by role deepens. Extends the promoted [[compute-futures-spread-retail-loss-concentration]] class from n=4 to n=5. Loss magnitude worsening — role-weighted P&L or retail-drop proposal gains urgency.
- **Tri-super-anchor concentration** — seeds 202608119, 2026081110, 202608112 each drive 9 outlier positions across 3 modes (disjoint). Concentration diffuses from 08-10's super-single (202608106 at 15 positions) but total outlier surface expands.
- **basket/synth scale-ratio held at ~3.025×** — n=2 filed runs at same level after the 08-08→08-10 jump (~2.5×→3.02×). Consistent with a real deployer-code change rather than n=12 sampling noise; n=3 needed for formal promotion.
- **x402 `settlementLegs × x402Total` alternation continues** (19-filing series, +0.558 today). No promotable trend.

## Row count and shape vs 2026-08-10

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface CONTRACTS but DIFFUSES from 08-10 → 08-11.** 08-10: 0 HIGH, 3 MEDIUM (basket + synth/x402 maxCurve at 16.67%, basket settlementLegs at 16.67%), 1 MEDIUM sub-threshold retail. 08-11: 0 HIGH, 3 MEDIUM (basket + synth/x402 maxSpot at 16.67%, retail-loss n=5 promoted, cross-mode minCurve sub-threshold). Total MEDIUM count held at 3; ≥10% column set fully rotated (maxCurve → maxSpot; settlementLegs → minCurve sub-threshold). No column persists at ≥10% in both filings.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's maxSpot + minCurve outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (~3.025× today)** — basket and synth per-seed rank order is identical; multiplier is a deterministic constant for `maxSpot`/`minSpot`. Multiplier held stable at 3.025× from 08-10, consistent with the 08-08→08-10 jump being a real deployer-code change (n=2 stability corroborates; n=3 needed for formal promotion).
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. Notable excluded outlier fires today: basket wallet_sum_pnl 25% (n=9 rows counted as outliers), spread wallet_sum_pnl 16.67% (n=6).
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 25th consecutive filed run. Padded seed 2026081110 anchors maxSpot LOW cross-mode today (50% padded-share in maxSpot). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-10's 2 cross-mode MEDIUMs (maxCurve, basket settlementLegs) both collapse to 0% today; 08-10's spread retail-loss promotion holds and extends to n=5. Fresh MEDIUM this filing is maxSpot resurface (was on 08-08, off on 08-10, on again 08-11 with rotated anchors). Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode super-anchor pattern DIFFUSES** — series 07-28 202607286 → 07-30 202607301 → 07-31 202607314 → 08-01 202608012 → 08-03 202608038 → 08-06 four concurrent → 08-07 diffuse → 08-08 202608083 (5 pos) → 08-10 202608106 (15 pos, super-dense) → **08-11 tri-super-anchor (202608119 + 2026081110 + 202608112, 9-pos each, disjoint)**. Concentration class: single → multi → diffuse → super-single → super-dense → **tri-super**.
- **x402 `settlementLegs × x402Total` alternation continues** (19-filing series, +0.558 today). No promotable trend.
- **x402Total oscillates within $322–$346 mean range across nine filings.** Today's $322.57 matches 08-03 low. New all-time HIGH-tail max today at $425.61 (above 08-10's $381.61). Range widens further on HIGH side.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 (08-10), extends to n=5 (08-11).** New loss-floor at −$24,696. Role-weighted P&L or retail-drop proposal gains urgency with widening magnitude.
