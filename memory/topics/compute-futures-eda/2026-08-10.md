# Compute-Futures EDA — 2026-08-10

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-10.csv` (144 rows, 4 modes)
**Run:** 2026-08-11
**Prior filed report:** 2026-08-08 (single sweep-day gap — 2026-08-09.csv exists but was not filed; two-sweep-day diff below).

## Top findings

1. **[MEDIUM — step-down, cross-mode 2-seed cluster] `maxCurve` fires at 16.67% in basket AND synth≡x402 simultaneously, all anchored by the SAME two seeds (202608106 + padded 2026081012).** 6 outliers per mode = 2 seeds × 3 roles. HIGH tail only: 202608106 lands basket 0.3085 / synth 0.1025 / x402 0.1025; 2026081012 lands basket 0.3145 / synth 0.1040 / x402 0.1040. Fence basket [0.2926, 0.3076] on IQR=0.00375; fence synth/x402 [0.09644, 0.1019] on IQR=0.001375. Ratio basket/synth today ≈ **3.02×** at every seed (vs 08-08's ~2.5×) — the scale multiplier itself drifted ~20% up in two sweep-days, worth watching whether that's a new deployer regime or seed-composition noise. **Step-down class:** 08-08 fired both LOW+HIGH tail with 3 anchor seeds at 25% (HIGH); today HIGH tail only with 2 anchor seeds at 16.67% (MEDIUM). Anchor-count contracted; tail collapsed asymmetrically.

2. **[MEDIUM — fresh, single-mode 2-seed cluster] Basket `settlementLegs` 16.67% anchored by seeds 202608106 + 202608109 at value=83.** 6 outliers = 2 seeds × 3 roles. Fence [77.88, 82.88] on IQR=1.25. First basket-settlementLegs fire in the filed-run window since 08-06 (spread-side settlementLegs was the recurring position; basket-side is fresh). **Cross-mode super-anchor:** seed 202608106 is also today's maxCurve anchor (finding #1) AND anchors spread settlementLegs at 79 (sub-threshold 8.33%) — 15 outlier positions across 3 modes on one seed.

3. **[MEDIUM — under 10% but retail-loss deepens, structural pattern now n=4 filed runs] Spread `role_pnl` 2.78% — single outlier seed 202608109 retail at −$20,016 — but top-5 spread losses ALL retail, matching the 08-06 / 08-07 / 08-08 pattern for the 4th consecutive filed run.** Top-5 losses: 202608109 retail −$20,016 / 2026081011 retail −$15,594 / 202608102 retail −$14,016 / 202608106 retail −$12,722 / 202608101 retail −$9,725. Retail mean spread P&L today −$8,565 vs analyst +$2,978 / operator +$4,151 — clean bimodal split by role. Below the ≥10% column threshold, but the retail-role structural pattern **now meets the 08-08 promotion criterion (n=4 consecutive filed runs)** — promote from carry-over to structural finding. Next step: file [[compute-futures-spread-retail-loss-concentration]] atomic note; propose either a role-weighted P&L metric or drop retail from the spread sweep to reduce this per-mode noise floor.

4. **[MEDIUM — fresh, cross-mode 1-seed cluster (sub-threshold in synth/x402)] `settlementLegs` under-fire at 8.33% in synth/x402 anchored by seed 202608105 at value=76.** 3 outliers per mode. Fence [79.12, 84.12] — LOW tail only. Not promoted per SKILL's sub-threshold gate, but noted because this is the FIRST LOW-tail settlementLegs fire in the filed-run window (all prior settlementLegs fires have been HIGH-tail). Direction flip class.

5. **[LOW — anchor rotation] 08-08's 3-seed maxCurve anchor cluster (202608083 + 202608087 + 202608088) fully rotates off; today's 2-seed cluster is disjoint (202608106 + 2026081012).** Zero seed overlap in maxCurve anchors across two sweep-days. Anchor-seed series contracts: 08-08 → 3 seeds fire, 08-10 → 2 seeds fire, both cross-mode. Rotation-not-persistence pattern per [[compute-futures-12-seed-sample-too-small]] continues to hold at the anchor-seed level.

6. **[LOW — collapse] 08-08's cross-mode `maxSpot` cluster (basket + synth/x402 at 16.67%, seeds 202608088 + padded 2026080812) fully collapses.** Today: zero `maxSpot` outliers ≥10% in any mode. Following the 08-08 → 08-10 sweep-day gap, the fresh MEDIUM cluster snaps back to zero within one filing — matching the rotation-not-persistence class. Additionally 08-08's spread `role_pnl` 8.33% (n=3 outliers) collapses to 2.78% (n=1) today — still retail-anchored (finding #3).

7. **[LOW — correlation collapse continues] Zero within-mode strong pairs (|r| ≥ 0.8) in any mode.** 08-08 also had zero. Zero-firing streak resumes at n=2 consecutive filed runs. Rotation-not-persistence class for within-mode strong pairs remains — no pair has crossed n=3 in the filed-run window.

8. **[LOW — spread maxCurve fresh sub-threshold] Spread `maxCurve` 8.33% (3 outliers), single seed 202608101 at 76.4005.** Fence [66.53, 75.41] on IQR=2.218. Fresh sub-threshold anchor; not promoted, but a NEW seed 202608101 (not in today's cross-mode HIGH-tail cluster) — spread mode has an independent anchor from basket/synth/x402 today. Cross-mode-decoupling class candidate.

9. **[LOW — padded-seed over-representation continues] Padded seed 2026081012 anchors 9 outlier positions today (all basket + synth + x402 maxCurve HIGH tail).** All-time filed-window: padded seeds continue to over-represent as anchors — 08-08 had padded 2026080812 as maxSpot HIGH anchor (100% padded concentration in maxSpot); today padded 2026081012 co-anchors maxCurve HIGH with unpadded 202608106 (50% padded-share). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.

10. **[LOW — recurring, 24th filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608101`–`202608109`), 36 rows 10-char seed (`2026081010`–`2026081012`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

11. **[LOW — non-promotable jitter] x402Total mean $327.96 (down $0.41 vs 08-08's $328.37).** Sum $11,806.59 (down $14.81). Series (mean): 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → 08-04 $327.78 → 08-06 $340.13 → 08-07 $326.38 → 08-08 $328.37 → **08-10 $327.96**. Range: $322–$346 across eight filings; no trend. Bottom-end today $269.61 (new low, below 08-08's $271.99). Top-end $381.61 (down from 08-08's $368.06 top — actually up, correcting: 08-10 top-end $381.61 exceeds 08-08's $368.06 by $13.55, so range widens on both ends).

12. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean +1.14e−14 (basket), −3.26e−12 (spread), −2.19e−15 (synth/x402). Std 2.32e−14 (basket), 6.95e−12 (spread), 9.71e−15 (synth/x402). All ≥11 orders under the |mean|>10 / std>100 gates. Basket sign flipped − → + vs 08-08, magnitude similar. Spread sign flipped + → −, magnitude flat. Synth/x402 sign held (−), magnitude up ~36%. Per-mode wallet_sum_pnl outliers surface at 25%/16.67%/8.33% today but excluded from severity per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — σ still ≥8 orders under the 1e-6 threshold everywhere.

13. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`; only `x402Total` distinguishes x402). basket/synth ratio ~3.02× (drifted from 08-08's ~2.5×) — same rank-order per seed at every scale.

14. **[PASS] Row/column shape unchanged vs 08-08.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.14e−14  | 2.32e−14  | OK |
| spread    | −3.26e−12  | 6.95e−12  | OK |
| synthetic | −2.19e−15  | 9.71e−15  | OK |
| x402      | −2.19e−15  | 9.71e−15  | OK |

## Within-mode signal

- **Zero within-mode strong pairs (|r|≥0.8) in any mode.** Zero-firing streak at n=2 consecutive filed runs. Rotation-not-persistence remains the default — no within-mode strong pair has crossed n=3 in the filed-run window.
- **Cross-mode 2-seed maxCurve cluster (202608106 + padded 2026081012)** — same two seeds anchor maxCurve outliers in basket AND synth AND x402 HIGH tail. Since synth ≡ x402 at row level and basket/synth rank order is identical per seed (verified today; scale ratio ~3.02× across all seeds), the maxCurve pattern is upstream of mode — a per-seed input-draw quirk that surfaces at every price scale. Genuine cross-mode structural signal; class continues from 08-08 with contracted anchor-count.
- **Seed 202608106 super-anchor** — appears in 15 outlier positions across 3 modes (basket maxCurve HIGH × 3-role, synth maxCurve HIGH × 3-role, x402 maxCurve HIGH × 3-role, basket settlementLegs HIGH × 3-role, spread settlementLegs LOW × 3-role sub-threshold). One seed drives 13 unique physical rows given synth ≡ x402. Anchor-cluster class continues but seed identity fully rotated from 08-08's 202608083.
- **Retail-role loss concentration in spread — n=4 consecutive filed runs (08-06, 08-07, 08-08, 08-10).** Top-5 spread losses all retail today. **Promotion criterion met per 08-08 note.** Retail mean spread P&L −$8,565 vs analyst +$2,978 / operator +$4,151. Next step: file [[compute-futures-spread-retail-loss-concentration]] atomic note.
- **x402 `settlementLegs × x402Total` continues to alternate around zero.** 18-filing series: +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 → +0.505 → +0.031 → +0.179 → **+0.475**. Sign flips continue; no interpretive story. Alternation-not-persistence holds.
- **basket/synth scale-ratio drift** — ratio moved from 08-08's ~2.5× to 08-10's ~3.02× at every seed (~20% jump in two sweep-days). Watch whether this stabilizes or continues drifting — may reflect a deployer-side price-draw code change or n=12 sampling noise. Not promoted yet; needs n≥3 filings to distinguish.

## Row count and shape vs 2026-08-08

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402; `spotSource` deterministic per mode.
- **Outlier surface CONTRACTS from 08-08 → 08-10 (across sweep-day gap).** 08-08: 2 HIGH (basket + synth/x402 maxCurve at 25%) + 2 MEDIUM (basket + synth/x402 maxSpot at 16.67%) + 2 MEDIUM sub-threshold retail-anchored (spread). 08-10: 0 HIGH, 3 MEDIUM (basket + synth/x402 maxCurve stepped down to 16.67%, basket settlementLegs fresh at 16.67%), 1 MEDIUM sub-threshold (retail-anchored, promoted). Total fires shrank; anchor concentration on one seed (202608106) intensified.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, all price columns between synth and x402; only `x402Total` distinguishes x402). Today's maxCurve + settlementLegs outliers in synth+x402 are the same physical rows, counted once per SKILL rule.
- **basket ≡ synth up to scale (~3.02× today)** — basket and synth per-seed rank order is identical for maxCurve (verified all 12 seeds today). Only the scale multiplier differs — and the multiplier itself is drifting (~2.5× on 08-08 → ~3.02× on 08-10). Outlier anchors transfer 1:1 between basket and synth by construction. Deterministic function of the deployer's price-draw code; worth documenting whether the multiplier is expected to be stable or seed-dependent.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust outliers/correlations** — excluded per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]. σ(wallet_sum_pnl) ≥8 orders under 1e-6 threshold in every mode. Notable excluded outlier fires today: basket wallet_sum_pnl 25% (9 rows), spread wallet_sum_pnl 16.67% (6 rows), synth/x402 wallet_sum_pnl 8.33% (3 rows each).
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 24th consecutive filed run. Padded seed 2026081012 anchors basket + synth + x402 maxCurve HIGH tail today (50% padded-share in maxCurve fires). Motivation for [[compute-futures-seed-padding-bug]] fix unchanged.
- **12-seed outlier_pct rotation-not-persistence.** 08-08's 2 fresh cross-mode MEDIUM+HIGH fires: maxCurve stepped down but persisted with fully rotated anchors; maxSpot collapsed. 08-08's zero strong within-mode pairs held at zero. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]].
- **Cross-mode single-seed anchor pattern CONTINUES concentrated** — series 07-28 202607286 → 07-30 202607301 → 07-31 202607314 → 08-01 202608012 → 08-03 202608038 → 08-06 four concurrent → 08-07 diffuse → 08-08 202608083 (5 pos) → **08-10 202608106 (15 pos)**. Today's anchor is the DENSEST since 08-06 (15 positions vs 08-06's 15 on top seed). Anchor-count class: single (07-28→08-03) → multi (08-06) → diffuse (08-07) → super-single (08-08) → **super-single dense (08-10)**.
- **x402 `settlementLegs × x402Total` alternation continues** (18-filing series ±). No promotable trend.
- **x402Total oscillates within $322–$346 mean range across eight filings.** No trend. Today's $327.96 sits between 08-03 low ($322.85) and 07-31 high ($346.10). New bottom-end low today at $269.61.
- **Retail-role concentrates spread's largest losses — PROMOTED at n=4 consecutive filed runs.** 08-06 top-3 all retail; 08-07 top-2 all retail; 08-08 top-3 all retail; **08-10 top-5 all retail**. Meets promotion criterion from 08-08 carry-over. Next step: file atomic note [[compute-futures-spread-retail-loss-concentration]] and propose either a role-weighted P&L metric or dropping retail from spread sweep to reduce noise floor.
- **basket/synth scale-ratio drift** — new watch item. 08-08 ~2.5×, 08-10 ~3.02×. Needs n≥3 filings to distinguish deployer-code change from n=12 sampling noise.
