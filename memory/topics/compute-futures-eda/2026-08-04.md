# Compute-Futures EDA — 2026-08-04

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-04.csv` (144 rows, 4 modes)
**Run:** 2026-08-05
**Prior filed report:** 2026-08-03. Daily cadence resumes (previous gap was 08-02 sweep inside the ISS-020 morning-batch outage, still un-analyzed).

## Top findings

1. **[LOW — but headline: 08-03's 3-mode strong pair collapses in one filing, zero-streak resumes at n=1] `realizedAbs × maxSpot` sign-flips and drops magnitude 4×.** 08-03 basket −0.853, synth/x402 −0.832 → **08-04 basket +0.212, synth/x402 +0.304, spread +0.023**. Full sign reversal in every mode that fired yesterday, and the magnitude collapses well under the 0.8 gate — no within-mode strong pair anywhere today. The 08-03 filing's promotion-candidate flag ("if it holds or deepens on 08-04, promote to MEDIUM structural") is decisively rejected: the coupling did not survive one sweep. Trajectory across four consecutive filings: 08-01 basket −0.198 → 08-02 +0.210 → 08-03 −0.853 → 08-04 +0.212. Rotation-not-persistence class reasserts itself. Zero-streak history was 20 consecutive filed runs (through 08-01) → 1-day break (08-03, 3 modes simultaneously) → resume at n=1 today. Next step: file the 08-03 3-mode event as an outlier in the streak history, not the start of a structural regime.

2. **[MEDIUM — three fresh column-fires, all curve-side, all diffuse-anchored] Basket `minCurve` = 16.67% + synth/x402 `minCurve` = 16.67% + synth/x402 `settlementLegs` = 16.67% (three fresh MEDIUM fires, all up from 0.00% on 08-03).** The curve-column cluster is the surface. Basket minCurve outliers: seeds 202608047 & 202608049 (low tail at 0.1045); synth/x402 minCurve: same seeds (low tail at 0.042); synth/x402 settlementLegs: seeds 202608042 & 202608049 (high tail at 82/83). No single seed anchors all three fires the way 08-03's seed 202608038 anchored 4 positions across 3 modes/2 columns. Fix path unchanged per [[compute-futures-12-seed-sample-too-small]]: widen the seed count. Column identity has fully rotated vs 08-03 (which fired role_pnl + realizedAbs); the class-general fix suppresses both surfaces.

3. **[MEDIUM — anchor pattern **DIFFUSES**, breaking the intensification series at n=5] Top outlier seeds share weight roughly equally.** 08-04: seed 202608049 = 5 positions, 2026080410 = 4, 202608042 = 4, 202608044 = 4 (excluding the 25% seed-encoding artifact). Compare 08-03: seed 202608038 alone had 4 positions across 3 modes/2 columns *including both tails of the same mode-column* (the most concentrated anchor ever observed). Series was 07-28 (202607286, 1s/3m) → 07-30 (202607301, 1s/2m) → 07-31 (202607314, 1s/6mc) → 08-01 (202608012, 1s/5mc) → 08-03 (202608038, 4 pos / 3m / 2c, both tails) → **08-04 (no dominant anchor; top-4 seeds tied 5/4/4/4)**. The n=5 promotion narrative from 08-03 is retracted — one filing later the pattern reverts to broad diffusion. Still MEDIUM by cadence (6th filing in the anchor-pattern watch), but the mechanism today is "12-seed sample surfaces different outliers each day" rather than "one seed's heavy-tail draw leaks into every touched column".

4. **[MEDIUM — first-appearance basket `wallet_sum_pnl × maxCurve` = −0.786, near-miss the strong-pair gate] Excluded from the correlation stream per the wallet_sum_pnl float-dust rule, but worth a carry-over note.** Basket `wallet_sum_pnl` σ = 2.88e−14 (float dust, 12+ orders under the σ > 1e−6 filter per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]]) — the pair does not represent real signal. Recording it because if the filter is ever relaxed, this pair (just under the |r|≥0.8 gate) would fire the streak-break marker again spuriously.

5. **[MEDIUM — x402Total three-filing decline trend BROKEN, second sighting of high-end outliers] x402 `x402Total` = 8.33% (3 outliers at top of band).** Seeds 202608042 = $405.09 × 3 rows top the band; sum $11,800.08 (**up $177.60 vs 08-03's $11,622.48**, first up-tick in three filings; still under 07-31's $12,492.08). Mean $327.78 (up $4.93 vs 08-03's $322.85). Max $405.09 (up from 08-03's $385.19, still under 07-31's record $409.32). Three-filing declining trend (07-31 → 08-01 → 08-03) does not extend. Excluded from severity ranking per SKILL bimodal-by-design rule for x402Total; noted as watch-carry only.

6. **[LOW — recurring, 21st filing] Seed 25% encoding artifact unchanged.** 108 rows 9-char seed (`202608041`–`202608049`), 36 rows 10-char seed (`2026080410`–`2026080412`). `seed` outlier_pct = 25.00% in every mode. One-line pad in `scenario-sweep.mjs` still pending per [[compute-futures-seed-padding-bug]].

7. **[LOW — spread `role_pnl` outlier fire returns to 8.33%, three-seed anchor] Spread role_pnl outliers: seed 202608042 (−$21,573 worst), seed 202608044 (−$16,437), seed 2026080411 (+$13,067 best).** Not fresh (below 10% MEDIUM threshold), but noted because spread `role_pnl` is the highest-magnitude PnL surface (σ = $9,770 vs σ = $10.79 for synth/x402) and the anchor seeds partially overlap the diffuse-anchor set from finding 3. σ narrows further: 08-01 $9,086 → 08-03 $8,288 → **08-04 $9,770** (widens back).

8. **[PASS] Conservation healthy in every mode.** `wallet_sum_pnl` mean −1.20e−14 (basket), −9.69e−13 (spread), +1.02e−15 (synthetic), +1.02e−15 (x402). Std 2.88e−14 (basket), 5.66e−12 (spread), 7.41e−15 (synth/x402). All 11+ orders under the |mean| > 10 / std > 100 gates. Signs shifted vs 08-03 in basket (− held; magnitude 2× larger), spread (+ → −), synth/x402 (+ held; magnitude tightened another 15% from 08-03's 6.42e−15). Float-dust regime; not interpretive.

9. **[PASS] Schema intact.** All 17 columns present, zero nulls in every column, 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, and all price columns except `x402Total`).

10. **[PASS] Row/column shape unchanged vs 08-03.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | −1.20e−14  | 2.88e−14  | OK |
| spread    | −9.69e−13  | 5.66e−12  | OK |
| synthetic | +1.02e−15  | 7.41e−15  | OK |
| x402      | +1.02e−15  | 7.41e−15  | OK |

## Within-mode signal

- **No |r| ≥ 0.8 pairs in any mode.** Zero-streak resumes at n=1 after 08-03's 3-mode simultaneous strong-pair break. Closest miss: basket `wallet_sum_pnl × maxCurve` = −0.786 (excluded — wallet_sum_pnl σ is float dust). Second-closest: basket `wallet_sum_pnl × seed` = +0.064 (nothing else in basket exceeds |r|=0.68). In synth/x402, the highest non-float-dust pair is `seed × minSpot` = +0.650 (recurring cross-mode price-scale artifact); highest in spread is `seed × maxSpot` = +0.484. Nothing above 0.7 anywhere in the non-float-dust surface.
- **08-03's structural-candidate `realizedAbs × maxSpot` pair collapses across all three modes.** Basket −0.853 → +0.212; synth/x402 −0.832 → +0.304. This was the first same-shape 3-mode strong-pair in the skill's 21-filing history; it survives exactly one filing. Rotation-not-persistence class holds — the 3-mode simultaneous fire on 08-03 was extreme within the class but not a regime change.
- **Anchor pattern diffuses, breaking the 5-filing intensification series.** 08-04 top-4 outlier seeds tied at 5/4/4/4 positions; 08-03's dominant seed 202608038 (4 positions across 3 modes/2 columns, including *both tails* of basket `role_pnl`) has no analogue today. Class-general fix (seed-count widening) still the right path; the intensification narrative retracts.
- **Curve-column outlier cluster surfaces.** Three of three fresh MEDIUM fires today are curve-column (`minCurve` × 2 modes + `settlementLegs` × synth/x402, where settlementLegs correlates with curve position). Basket minCurve values concentrate at the low tail (0.1045 with count 6); synth/x402 minCurve concentrates at 0.042 (count 6). The underlying mechanism is the 12-seed IQR being tight enough that any 2-of-12 seed cluster at a tail fires the fence.

## Row count and shape vs 2026-08-03

- Rows: 144 → 144 (no change).
- Columns: 17 → 17 (no change; empty column diff).
- Modes: 4 → 4 (identical set: basket, spread, synthetic, x402).
- Seeds: 12 → 12 (encoding artifact unchanged: 108 rows 9-char, 36 rows 10-char).
- Constants by design all hold: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode.
- **Outlier surface migrates from role_pnl + realizedAbs (08-03) to minCurve + settlementLegs (08-04).** 08-03 fired 0 HIGH + 3 fresh MEDIUM (basket role_pnl 13.89% + synth/x402 role_pnl 11.11% + synth/x402 realizedAbs 16.67%) + 1 pattern MEDIUM (anchor n=5). 08-04 fires **0 HIGH + 3 fresh MEDIUM (basket minCurve 16.67% + synth/x402 minCurve 16.67% + synth/x402 settlementLegs 16.67%) + 1 anchor-collapse MEDIUM**. All three 08-03 fires collapse to 0.00%. Same-shape fire count (3 fresh MEDIUM + 1 pattern MEDIUM), full column-identity rotation, plus the anchor pattern flips from concentrated to diffuse. Rotation-not-persistence holds across every rotatable surface.

## Carry-overs

- **synthetic ≡ x402 at role level** — reconfirmed today (identical `wallet_sum_pnl`, `role_pnl`, and all price columns between synth and x402; only `x402Total` distinguishes x402). Same-row outliers in synth/x402 for `minCurve`, `settlementLegs`, `maxCurve`, `wallet_sum_pnl` this filing are the same physical rows, counted once per SKILL rule.
- **Pooled cross-mode spot/curve correlations** — known regime artifact; only within-mode correlations reported.
- **Constants by design:** `rounds`=60, `deniedWorkers`=0, `x402Total`=0 outside x402, `live`/`settlement`/`spotSource` deterministic per mode.
- **`wallet_sum_pnl` float-dust correlations** — filter proves out again. σ(wallet_sum_pnl) 10+ orders under the threshold everywhere (highest is spread's 5.66e−12). Excluded pairs today: basket `wallet_sum_pnl × maxCurve` = −0.786 (would have been the streak-break marker if not filtered), synth/x402 `wallet_sum_pnl × minSpot` = +0.432, spread `wallet_sum_pnl × minSpot` = +0.371. See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **Seed-encoding 25% outlier in every mode** — recurring artifact of unpadded `YYYYMMDD+index` seed string, 21st consecutive filed run. One-line pad still pending per [[compute-futures-seed-padding-bug]].
- **12-seed outlier_pct rotation-not-persistence.** All three 08-03 fresh MEDIUM column-fires (basket role_pnl, synth/x402 role_pnl, synth/x402 realizedAbs) collapsed today; three fresh MEDIUM column-fires opened on different columns (basket minCurve, synth/x402 minCurve, synth/x402 settlementLegs). Perfect column-identity rotation, same fire count. Anchor pattern rotates from concentrated → diffuse.
- **Cross-mode single-seed anchor pattern DEMOTED from n=5 → diffuse** — series 07-28 seed 202607286 → 07-30 seed 202607301 → 07-31 seed 202607314 → 08-01 seed 202608012 → 08-03 seed 202608038 (concentration peak) → **08-04 (top-4 tied)**. The intensification arc breaks. Fix path (widen seeds) unchanged and still correct — it addresses both the concentrated and diffuse forms of the same mechanism.
- **x402 `settlementLegs × x402Total` remains jitter.** Series (15 filings): +0.216 → −0.315 → +0.732 → −0.604 → +0.028 → −0.110 → +0.156 → +0.657 → +0.357 → +0.241 → +0.540 → +0.612 → −0.282 → +0.036 (08-03) → **+0.503 (08-04)**. Now the largest magnitude since 07-30's +0.540; still short of |r|=0.8. No interpretive story; the series continues to flip signs across filings.
- **x402Total three-filing decline BROKEN.** 07-31 $346.10 → 08-01 $339.47 → 08-03 $322.85 → **08-04 $327.78** (mean); sum $11,800.08 (up $177.60 vs 08-03). Trend was noted 08-03 with "watch for 08-04 to promote to MEDIUM"; today rejects the promotion. Series direction is now flat-with-jitter, not monotonic. Top-end $405.09 recovers ground toward 07-31's record $409.32 (up $19.90 vs 08-03).
- **08-02 sweep exists but was not analyzed** — the 08-02 06:00Z compute-futures-eda slot was inside the ISS-020 morning-batch outage cluster per memory index. Given today's clean rotation-not-persistence pattern (both column identities and anchor concentration fully rotate in one filing), backfilling 08-02 is low priority — the 08-03 pattern already appears to be an isolated extremum, not a regime start.
