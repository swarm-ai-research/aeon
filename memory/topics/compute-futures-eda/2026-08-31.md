# Compute-Futures EDA — 2026-08-31

**Source:** `memory/gitlawb-compute-futures-proofs/2026-08-31.csv` (144 rows, 4 modes)
**Run:** 2026-09-01
**Prior filed report:** 2026-08-29 (2-day filing gap — 08-30 CSV exists in proof dir but was never analyzed; today's comparisons are all vs 08-29).

## Top findings

1. **[HIGH — new all-time-sum trough; retail mean 65% DEEPER than 08-29; barely edges 08-24's prior sum-trough; class-first cross-day dispersion record] Spread retail P&L sum reaches new all-time trough **−$149,541** (65% deeper than 08-29's −$90,646, 2.6% deeper than 08-24's prior sum-trough −$145,800); retail mean **−$12,462** (std $6,757, 11/12 losing).** Top-5 losses **5/5 retail** (9th consecutive filed run at 5/5 top-5 retail purity): **202608313 −$26,264 (deepest since 08-25's 202608257 −$25,260 record — TIES/EXCEEDS the 4-filing window's all-time single-retail-loss floor)** / 202608314 −$17,653 / 202608319 −$16,238 / **2026083110 −$15,614 (padded)** / 202608311 −$14,857. The one winner: **202608316 retail +$735** (near-dust, smallest positive-side spread retail in the class series). Top-5 gains: 202608313 operator +$15,846 / 202608319 operator +$13,553 / 202608314 operator +$11,460 / 202608311 operator +$10,514 / **202608313 analyst +$10,156 (first non-operator top-5 gain 2 filings running)**. **Seed 202608313 triple-role top-5 anchor** (retail deepest loss −$26,264 + operator top gain +$15,846 + analyst top gain +$10,156) — dispersion **$42,110** (retail → operator), **new all-time class-series record** (surpasses 08-25's prior $39,158). Per-role means: retail **−$12,462** (deepens 65% vs 08-29 −$7,554); operator **+$8,278** (up 107% vs 08-29 +$4,004, largest operator-mean bounce in series); analyst **+$2,860** (up 18% vs 08-29 +$2,434). Padded seeds fill **1/5 spread retail top-losses** (2026083110), down from 08-29's 2/5 — padded-share drops back to sub-proportional (20% vs 25% share). Extends [[compute-futures-spread-retail-loss-concentration]] from n=18 to **n=19 consecutive filed runs**. Next step: monitor whether the sum-trough is a single-filing extreme or the start of a run of deepening retail losses (08-24 → 08-25 → 08-29 → 08-31 sum series is now $-145,800 → $-109,096 → $-90,646 → $-149,541 — non-monotonic but latest is deepest).

2. **[HIGH — 2.5× multiplier holds n=9 filed runs at spot columns but CURVE columns loosen substantially; maxCurve std widens 8× vs 08-29, class-first fray on curve side while spot side stays byte-identical] basket/synth `minSpot` and `maxSpot` per-seed ratio **[2.500000, 2.500000] std 0.00e+00 exact** — spot side holds byte-identical at 9th consecutive filing; but **curve-side loosens sharply**: `maxCurve` mean **2.4907** std **2.95e-02** (vs 08-29's 3.86e-03 — **8× wider**, single-day class-record loosening) and `minCurve` mean **2.4941** std **1.94e-02** (vs 08-29's 1.56e-02 — 24% wider, halts 08-29's fresh tightening).** `realizedAbs` per-seed ratio mean **2.6109** std **1.16e-01** (~128% wider than 08-29's 5.09e-02 — third consecutive fray-cadence flip). Class series post-rename: n=1 (08-18) → n=5 (08-22, rename threshold) → n=7 (08-25) → n=8 (08-29, first cadence past 3.025× regime's max) → **n=9 (08-31)**. The spot-columns-tight / curve-columns-fraying divergence is new signal — 08-29 read both sides as tightening together; 08-31 pulls the two sides apart. Structural interpretation: whatever seeds the deployer picks for the 2.5× coupling now operates at spot granularity but not curve granularity — the divergence hints at a per-column randomization change on the deployer side. Next step: watch 08-30's unanalyzed CSV for whether the curve-loosening began there (single-day step) or ramps across the 08-29 → 08-31 window; if 08-30 shows intermediate std, ramp is smooth and likely genuine regime change.

3. **[MEDIUM — class-first observation about cross-mode single-seed anchor coupling via the 2.5× multiplier; not a bug, but the multiplier now mechanically explains most of the outlier structure] Cross-mode ≥10% cluster count = 0 (unchanged vs 08-29), but two seeds anchor cross-mode 8.33% clusters via multiplier propagation: seed 202608317 hits `maxSpot` upper tail in **basket + synth + x402** (same seed's synth `maxSpot`=0.0931 propagates to basket `maxSpot`=0.2327 at exact 2.5000× — outlier in one mode is outlier in the other by construction), and seed **2026083111 (padded)** hits `realizedAbs` upper tail in **basket + synth + x402**.** Non-dust cluster memberships (excluding seed-25%, wallet_sum_pnl float-dust): **202608317 = 4** (basket maxSpot, basket settlementLegs, synth maxSpot, x402 maxSpot), **2026083111 = 3** (basket realizedAbs, synth realizedAbs, x402 realizedAbs — first time a **padded seed** anchors a 3-mode cross-modal single-seed cluster), then top-tier spread anchors **202608313 / 202608315 / 202608316 = 1 each** (spread realizedAbs / spread maxSpot / spread minCurve). Per-mode 8.33% clusters: basket {realizedAbs, maxSpot, settlementLegs} (3 clusters, up from 08-29's 1); spread {realizedAbs, maxSpot, minCurve} (3, up from 08-29's 1); synth/x402 {realizedAbs, maxSpot} (2 each, up from 08-29's 0). Below-threshold cluster count **10 total** across 4 modes (up from 08-29's 2 — 5× wider spread of tail activity, but all sub-threshold and mostly explained by the multiplier's cross-mode propagation). Anchor magnitude series: 22 → 18 → 6 → 3 → 9 → 4 → **4/3/1 (08-31, top anchor unchanged at 4 but 2nd-tier padded seed rises from 3-way tie to solo 2nd; spread anchor tail flattens)**. Anchor seed identity does not persist vs 08-29 (202608293 drops from anchor set entirely — **8th consecutive full-collapse of prior day's super-anchor identity**).

4. **[MEDIUM — 4th consecutive x402Total correlation-anchor rotation; anchor RETURNS to settlementLegs after 08-25/08-29 spot/curve detour; 3 sign flips today vs 08-29, largest cross-partner reshuffle in the class series] x402 `x402Total` primary partner rotates for the 4th consecutive filing: settlementLegs (dominant through 08-24) → minSpot (08-25 +0.785 near-threshold) → maxCurve (08-29 +0.564) → **settlementLegs (+0.618, 08-31, RETURNS to the original 08-24 anchor position)**.** Delta vs 08-29: `settlementLegs` +0.618 (was +0.223, +0.395 largest single-day recovery in class series, restores to primary); `minSpot` +0.304 (was −0.393, **+0.697 sign flip**, second-largest partner reversal in series after 08-29's −1.178 minSpot flip); `maxSpot` −0.100 (was +0.487, **−0.587 sign flip**); `maxCurve` −0.018 (was +0.564 primary anchor, **−0.582 sign flip — anchor fully evaporates from primary to near-zero in single filing**); `minCurve` −0.016 (was −0.327, attenuates); `realizedAbs` +0.061 (was +0.250, attenuates). **3 sign flips today (minSpot + maxSpot + maxCurve)** — matches class-series max for single-day sign-flip count. Anchor cycle: leg-count → spot-price → curve-price → **leg-count (returns after 3-filing rotation)** — first return to a prior anchor identity in the 4-filing rotation window. Structural interpretation: today's x402 fee regime rebinds fee to settlement-leg count (the pre-08-25 default); the 08-25→08-29 spot/curve detour is now closed. Next step: track whether settlementLegs anchor persists for 2+ filings (regime lock-in) or rotates again by 09-05 (continuing cycle).

5. **[MEDIUM — x402Total range COMPRESSES 20% vs 08-29 to $87.63; new interim-series-LOW near 08-16 all-time-narrow trough; range oscillation now 4-filing up-down-up-down pattern] x402Total range **contracts to $87.63** (min $283.92, max $371.55) — 20% narrower than 08-29's $109.98, essentially matches 08-16's all-time-narrow $83.55 (only $4.08 wider).** Min **$283.92** rises **$16.31** above 08-29's floor $267.61 (retreats from 4-filing low); max **$371.55** drops **$6.04** below 08-29's peak $377.59. Mean **$326.21** (down $1.26 vs 08-29's $327.47 — mean stability continues across 5 filings, drift < 0.5%). Std **$27.41** (down 5% from $28.85 — mild tightening). x402Total outlier_pct = **0%** today (was 0% on 08-29 too, both post-08-24's 8.33% spike collapse). Series range: $83.55 (08-16) → $88.05 (08-20) → $103.60 (08-21) → $122.73 (08-22) → $145.43 (08-24, peak) → $88.20 (08-25) → $109.98 (08-29) → **$87.63 (08-31, near-trough)**. Oscillation pattern: 4-filing up-down-up-down (widen → compress → widen → compress). Next step: if 09-05 filing widens again, up-down cadence solidifies as a bimodal regime.

6. **[LOW — n=18 consecutive filed runs] Zero within-mode strong pairs (|r|≥0.8) in any mode.** Extends 08-29's n=17 to **n=18**. Highest today ex-seed-encoding ex-wallet-dust: **basket `maxSpot × settlementLegs` = −0.680** (structural mid-strength, matches 08-29's basket maxSpot/settlementLegs tier); **x402 `x402Total × settlementLegs` = +0.618** (aligned with finding 4's anchor); **synth/x402 `maxCurve × settlementLegs` = +0.616** (fresh mid-strength appearance); **basket `realizedAbs × settlementLegs` = +0.598** (persistent basket structural mid-strength); **spread `minSpot × settlementLegs` = +0.561**. n=12 seeds still systematically insufficient; [[compute-futures-12-seed-sample-too-small]] motivation continues.

7. **[LOW — conservation regime is 2-positive/2-negative today; 2-filing cycle hypothesis from 08-29 FAILS (predicted 3+/1−, actual 2+/2−)] Per-mode `wallet_sum_pnl` means all near-zero float dust: basket **+1.60e−14**, spread **+6.16e−14**, synth **−1.09e−15**, x402 **−1.09e−15**.** Sign pattern: basket + spread POSITIVE, synth + x402 NEGATIVE (2 sign flips vs 08-29: spread flips + from −, x402 & synth flip − from … wait, synth was already −2.59e−16 on 08-29 too, so net 1 sign flip — spread only). Actually vs 08-29 (basket +, spread −, synth −, x402 −), today (basket +, spread +, synth −, x402 −): **1 sign flip (spread − → +)**, positive-count 1 → 2. **08-29's 2-filing mirror-image cycle hypothesis (predicted today = 3+/1−) FAILS** — today is 2+/2−, breaking both prior-filing patterns. Regime is 1-signal-turnover per filing without cadence lock-in. All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100).

8. **[LOW — padded seeds rise to 3/13 non-dust cluster memberships (23%, near-proportional); one padded seed anchors ALL 3 realizedAbs cross-mode clusters — first solo-padded 3-mode anchor in the class series] Padded seeds (2026083110/11/12) hold 36 of 144 rows (25%).** Today they contribute **3 of 13 non-dust outlier cluster memberships (23.1%)**: **2026083111** anchors all 3 realizedAbs clusters (basket + synth + x402, **class-first solo padded 3-mode anchor**), plus **2026083110** at 4th-deepest spread retail loss. Padded encoding placement class series: 08-19 severely under → 08-25 proportional AND bimodal → 08-29 top-losses-heavy (2/5 padded) → **08-31 dominant-2nd-tier anchor (2026083111 = 3-mode anchor)**. First filing where a padded seed's cross-mode locking exceeds a non-padded seed's cross-mode locking on any specific column — the 08-29 spread-retail-loss-heavy pattern rotates OFF; a fresh cross-mode-anchor-by-padded-seed pattern rotates ON.

9. **[PASS] Schema intact.** All 17 columns present. Zero nulls everywhere. 144 rows = 4 modes × 12 seeds × 3 roles. `synthetic ≡ x402` reconfirmed at role level (identical `wallet_sum_pnl`, `role_pnl`, `minSpot`, `maxSpot`, `minCurve`, `maxCurve`, `realizedAbs`, `settlementLegs`; only `x402Total` distinguishes x402). Max abs diff across shared columns: **0.000000e+00** (byte-identical).

10. **[PASS] Row/column shape unchanged vs 08-29.** 144 → 144 rows, 17 → 17 cols, same 4-mode set. Same by-design constants: `rounds`=60 everywhere, `deniedWorkers`=0 everywhere, `x402Total`=0 outside x402. `live` True in basket/spread, False in synth/x402; `settlement` `cash` everywhere except x402 (`x402`); `spotSource` deterministic per mode. No new constant-column drift.

## Per-mode conservation (mean wallet_sum_pnl)

| Mode      | Mean       | Std       | Status |
|-----------|-----------:|----------:|--------|
| basket    | +1.60e−14  | 2.42e−14  | OK |
| spread    | +6.16e−14  | 7.42e−12  | OK |
| synthetic | −1.09e−15  | 2.17e−14  | OK |
| x402      | −1.09e−15  | 2.17e−14  | OK |

All four modes 12+ orders under conservation-drift gates (|mean|>10, std>100). **Sign pattern**: basket + spread POSITIVE, synth + x402 NEGATIVE (1 sign flip vs 08-29 — spread flips + from −; 08-29's 2-filing mirror-image cycle hypothesis FAILS, today is 2+/2− not the predicted 3+/1−).

## Within-mode signal

- **HIGH — Spread retail P&L sum reaches new all-time trough $-149,541** (finding 1) — 65% deeper than 08-29, 2.6% deeper than 08-24 prior floor. Retail mean $-12,462. Seed 202608313 triple-role top-5 anchor with all-time dispersion record $42,110. n=19 consecutive filed runs.
- **HIGH — basket/synth 2.5× multiplier: spot side byte-identical at n=9, curve side FRAYS** (finding 2) — maxCurve std widens 8× vs 08-29 (single-day class-record loosening); the spot-tight/curve-loose divergence is fresh signal that may indicate a per-column randomization change on the deployer side.
- **Cross-mode single-seed anchor coupling via the 2.5× multiplier** (finding 3) — seed 202608317 anchors 4 outlier clusters (basket + synth + x402 maxSpot via 2.5× propagation + basket settlementLegs); padded seed 2026083111 anchors all 3 realizedAbs cross-mode clusters (class-first solo padded 3-mode anchor). The multiplier now mechanically explains most of the cross-mode outlier structure.
- **x402 x402Total anchor RETURNS to settlementLegs (+0.618)** (finding 4) — 4th rotation, closes the 08-25→08-29 spot/curve detour. 3 sign flips today (matches class-series max for single-day sign-flip count); minSpot flips +0.697, maxSpot flips −0.587, maxCurve flips −0.582 from primary to near-zero.
- **x402Total range COMPRESSES 20% to $87.63** (finding 5) — near 08-16 all-time-narrow trough. Mean stable at $326.21 (< 0.5% drift over 5 filings). Range oscillation now 4-filing up-down-up-down.
- **Zero within-mode strong pairs (|r|≥0.8) — n=18 consecutive filed runs** (finding 6). Highest ex-seed ex-wallet: basket `maxSpot × settlementLegs` = −0.680; x402 `x402Total × settlementLegs` = +0.618 (aligned with finding 4's anchor); synth/x402 `maxCurve × settlementLegs` = +0.616.
- **Conservation regime today is 2+/2−** (finding 7) — 08-29's 2-filing mirror-image cycle hypothesis (predicted 3+/1−) FAILS. Only 1 sign flip vs 08-29 (spread − → +).
- **Padded seed 2026083111 anchors all 3 realizedAbs cross-mode clusters** (finding 8) — class-first solo padded 3-mode anchor. Padded participation 3/13 (23%, near-proportional to 25% row share).

## Carry-overs

- 25% seed encoding artifact (n=39 filed runs, pending [[compute-futures-seed-padding-bug]] fix; today's basket/spread/synth/x402 all show 25% seed outlier_pct, chronic).
- `rounds=60` and `deniedWorkers=0` constant by design.
- `x402Total=0` outside x402 mode by design; bimodal within x402 by design (outliers excluded from severity per SKILL).
- [synth ≡ x402] role-level identity byte-identical (only `x402Total` distinguishes); mirror pairs counted once, not twice.
- Pooled cross-mode spot/curve correlations excluded (regime artifact, verified once).
- `wallet_sum_pnl` float-dust correlations excluded until σ > 1e-6.
- 2026-08-30 CSV present in proof dir but unanalyzed (2-day filing gap; today's comparisons are all vs 08-29).
