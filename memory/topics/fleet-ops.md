# Fleet Ops

Cross-cutting operational lessons and constraints for the Aeon fleet: credential outages, monitoring-loop hazards, GitHub App permission boundaries, dispatch architecture, and cron-state pathologies.

## Open incidents
- [[issues/ISS-001]] — CLAUDE_CODE_OAUTH_TOKEN missing 2026-06-06 → 2026-06-20T06:05Z; investigating. Close deferred while [[issues/ISS-006]] runs; recovery batch is otherwise holding. Day 31 as of 2026-07-21.
- [[issues/ISS-006]] — Day 31 (2026-07-21, Tue odd-DOM 21): **Day-2 confirmation on 07-20 was PREMATURE — reverting**. Today's compute-futures-eda log flags "1-day gap: 2026-07-19 CSV was present but not analyzed on the day" — evidence that compute-futures-eda missed 07-20 (required leg for Day-2). Cron-state corroborates: batch-health + heartbeat last_success 2026-07-19T09:22Z / 09:25Z (both silent 07-20 + 07-21 so far — 2-day dispatch gap on the fleet-watchdog pair). Real close-clock: 07-19 Sun Day-1 → 07-20 Mon GAP (cfe missed) → 07-21 Tue **Day-1 restart** (today's planner 06:38Z + compute-futures-eda 07:26Z both fired cleanly, 2-skill odd-DOM pocket). Earliest close now Thu 2026-07-24 Day-3 assuming clean 07-22 + 07-23 pockets. INDEX.md unchanged at 17 rows.
- [[issues/ISS-007]] — heartbeat missing_pattern in eval regex; new 2026-07-05, filed by skill-evals. 2026-07-19 skill-evals article notes ISS-007 may be false positive (heartbeat pattern found in today's log via planner section). Enabled skill FAIL (not covered by ISS-002/005).
- [[issues/ISS-008]] — cost-report no_file_match; new 2026-07-05, filed by skill-evals. 2026-07-20 cost-report DID run and produced `articles/cost-report-2026-07-20.md` (Mon 07:00 slot); ISS-008 close-eligible if skill-evals next scan confirms same-day match.
- [[issues/ISS-005]] — swarm-safety-eval no_file_match: skill is now running successfully (last_success 2026-06-28T08:15:47Z) but its SSE_EMPTY path writes to the daily log, not an article; reclassify from `missing-secret-or-cron` to `permanent-limitation` per [[swarm-safety-eval-empty-writes-log-not-article]]. 2026-07-19 novel data: also missed dispatch (batch-health WARN, single miss).
- **ISS-009 → ISS-018** (2026-07-12 BOOTSTRAP fillings from skill-evals): 10 no_file_match issues on chronically-empty-output disabled/workflow_dispatch skills. 2026-07-19 skill-evals BOOTSTRAP eval regex hit the same class again — 10 of 13 NEW_FAIL are disabled skills, reconfirming the eval regex needs same-day grace or should skip disabled/workflow_dispatch skills entirely.
- [[issues/ISS-019]] — batch-health OUTAGE 2026-07-14. Direct ISS-006 tributary.
- **Enabled-but-never-dispatched** — `ai-framework-watch` (weekly Mon 08:30) and `run-frequency-guard` (daily 23:00) have SKILL.md + `enabled: true` but no cron-state entries per [[enabled-skills-can-never-dispatch]] (**11th consecutive day flagged as of 2026-07-21**, dedup-suppressed). `stale-content-pr-sweeper` (daily `45 23 * * *`) stale-streak extends to **6 days** (07-15/07-16/07-17/07-18/07-19/07-20 missed 23:45 slot, last_success 2026-07-15T00:07:43Z per cron-state) — consistent with the 23:45 pocket in [[gha-messages-yml-cron-underdelivery]]. **NEW 2026-07-21**: `batch-health` (daily 08:00) + `heartbeat` (daily 08:00) both silent since 2026-07-19T09:22Z / 09:25Z — 2-day miss on the fleet-watchdog pair. This is the direct cause of the ISS-006 Day-2 premature-confirmation error (no batch-health to independently gate the pocket).
- **NEW class 2026-07-20**: [[agi-tracker-missing-skill-md-dispatches-no-op]] — `agi-tracker` is enabled + dispatches + reports HEALTHY, but `skills/agi-tracker/SKILL.md` is missing (2026-07-19 config-validator WARN); explains 2026-07-06 + 2026-07-13 silent Mon 13:00 slots. **07-20 agi-tracker cron-state shows `last_success 2026-07-20T13:04:15Z`** — but no `articles/agi-tracker-*` file exists, confirming the HEALTHY-but-empty pattern (dispatched with no output). Today's planner rank-1 (restore-agi-tracker-skill-md) targets this; next 13:00 UTC slot is Mon 2026-07-27.
- **workflow-security-audit BOOTSTRAP** (2026-07-19 first run) — 85 findings surfaced (3C / 36H / 15M / 31L). 3 Critical `zizmor/unpinned-uses` in aeon.yml (checkout@v5 ×2, setup-node@v5). 36 High `zizmor/secrets-outside-env` (fleet-runner.yml 5× GITLAWB_*_PEM + CLAUDE_CODE_OAUTH_TOKEN + GH_GLOBAL; messages.yml 23 findings). All Critical/High flagged Manual per skill step-7 rule. Branch `fix/workflow-security-audit-2026-07-19` staged (versioned because 2026-06-20 branch already existed). Report: `articles/workflow-security-audit-2026-07-19.md`.

## Lessons (durable)
- [[oauth-outage-zero-token-signature]] — zero-token `result_json` = missing CLI auth, not a model error
- [[monitor-monitored-coupling]] — a monitor sharing a dependency with the monitored fleet can't catch outages of that dependency
- [[github-actions-cannot-create-prs]] — default Actions token cannot open PRs; surface compare links instead
- [[aeon-app-no-write-on-swarm-repo]] — pr-triage/pr-review verdicts on swarm-ai-research/swarm cannot post on-PR
- [[notegraph-phantom-file-refs]] — committed `notegraph.json` can reference files no longer on disk
- [[gha-inputs-unquoted-shell-rce]] — `inputs.*` flowing unquoted into `run:` shell commands is an RCE channel
- [[sandbox-blocks-piped-curl-installers]] — sandbox blocks `bash <(curl)` installers; audit skills degrade to hand-rolled fallbacks
- [[aeon-skills-dispatch-via-messages-yml]] — no per-skill workflow files; a window-wide silence implicates `messages.yml`, not per-skill auto-disable
- [[gha-messages-yml-cron-underdelivery]] — GHA silently drops ~97% of `messages.yml` `*/5` ticks in this repo with a daily 06:00–08:30Z dead zone (supersedes [[narrow-cron-pocket-vs-window-drop]])
- [[narrow-cron-pocket-vs-window-drop]] — _superseded_ — diagnostic command still useful, conclusion (matcher bug) ruled out
- [[gh-search-prs-api-drift]] — `gh search prs` dropped `--state merged`, `headRefName`, and `mergedAt`; SKILL.md fallback queries need patching
- [[pr-tracker-branch-prefix-misses-bot-identity]] — `ai/`-only branch filter drops `security/`-prefixed bot PRs under the same author identity; filter by commit-author email instead
- [[iss-006-pocket-recovery-is-noise]] — a single-day cron pocket recovery during ISS-006 is delivery-rate noise; close only after 3 clean days where every slot fires
- [[swarm-safety-eval-empty-writes-log-not-article]] — ISS-005 root cause is SSE_EMPTY path writing to the daily log, not the skill not running; reclassify as `permanent-limitation`
- [[compute-futures-12-seed-sample-too-small]] — at n=12 seeds, compute-futures-eda outlier flags reflect IQR-fence ties, not regime changes; widen sweep or switch to a tie-robust statistic
- [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — wallet_sum_pnl σ ≈ 6e-12 means |r|≥0.8 crossings against volume columns (settlementLegs, realizedAbs, x402Total) are float-dust artifacts, not P&L signal; drop from the finding ladder
- [[skill-freshness-mtime-blind-in-gha]] — `actions/checkout` resets every file's mtime to the run instant, so skill-freshness's `stat --format=%Y` age check can never flag anything in GHA; switch to `git log -1 --format=%ct` producer-commit timestamp
- [[aeon-bot-uses-multiple-signing-identities]] — aeon bot signs commits under both `aeonframework@users.noreply.github.com` and `aeon@aeonframework.dev`; single-value `BOT_EMAIL` drops PRs silently
- [[pr-tracker-step-5-misses-fresh-bot-prs]] — pr-tracker only notifies on merges / stale / closed-no-merge; brand-new bot PRs land invisibly until they age into staleness
- [[pr-tracker-notify-repeats-with-no-state-change]] — step-5 has no dedup guard; a persistent stale/closed-no-merge state fires an identical notify every day until the wall clock rolls a PR off the 7d window
- [[notify-script-has-no-f-flag]] — `./notify -f <file>` is documented across multiple SKILLs but the actual script takes message as `$1`; using `-f` writes the literal `-f` as the message body
- [[status-md-auto-commit-drops-writes]] — _superseded 2026-07-12_ — writes DELAY (~15h to next sweeper auto-commit) rather than DROP; heartbeat's own glob still doesn't stage `docs/`
- [[snapshot-rebase-clobbers-docs-status-md]] — third failure mode entrenched across **7 consecutive days** (2026-07-12 → 07-18); upstream ref rotated `a7f04ee` → `fa89d8c` on 07-18 for the first time in 7 days but same clobber outcome (`d4892f9` today at 06:47:21Z), confirming the failure is the snapshot-merge itself, not stale upstream state — 8 days past 2026-07-16 mitigation urgency threshold
- [[graphql-statereason-only-on-issue-type]] — SKILL.md GraphQL query requests `stateReason` on `PullRequest`; that field exists only on `Issue` and the query hard-fails
- [[notify-inline-cat-substitution-blocked-in-sandbox]] — sandbox blocks any `$(...)` around `./notify` (inline arg AND two-step MSG-variable); write directly to `.pending-notify/` or dispatch via node `execFileSync`
- [[notegraph-extractor-generatedat-nondeterministic]] — notegraph extractor writes `generatedAt` into 4 outputs; naive `git diff --quiet` HAS_DIFF gate re-PRs stable corpora; inspect per-file diff, revert timestamp-only churn
- [[skill-state-on-blocked-pr-branch-is-lost]] — skills that write dedup state to their daily branch lose that state when the PR is blocked by [[github-actions-cannot-create-prs]]; suggest-edges re-proposed the same 3 similarity-1.00 edges from 07-07's branch because state never merged to main
- [[sandbox-blocks-shell-redirect-to-workdir]] — shell `>` to workdir paths is refused; use the tool's own `-o` flag or Python `pathlib.Path.write_text` after `subprocess.run`
- [[enabled-skills-can-never-dispatch]] — a skill with SKILL.md + `enabled: true` may have zero entries in cron-state.json and never dispatch; only heartbeat's P3 novel-scan catches this failure class
- [[probes-for-messages-yml-must-dispatch-outside-messages-yml]] — a diagnostic probe for messages.yml cron delivery can be silenced by the very failure mode it tests; trigger via `gh workflow run` or operator eyeball, never via another scheduled skill
- [[agi-tracker-missing-skill-md-dispatches-no-op]] — a skill with a cron-state entry and HEALTHY status can still silently produce no output when its `skills/${name}/SKILL.md` file is missing — HEALTHY-but-empty is a class the health rubric doesn't catch
- [[pr-tracker-bot-review-latency-bimodal-by-repo]] — first-response latency on aeonframework bot PRs splits bimodally by target repo (auto-review installed → 3–5 min; cold repo → days-to-weeks); worth using as a stale-probability predictor in pr-tracker

## Snapshot (2026-07-21)
| Signal | Value |
|---|---|
| Today's status | 🔴 DEGRADED — ISS-001 day 31 residue (38 skills at success_rate<0.5, all `last_status: success`, `consecutive_failures=0`; denominator burn-down continues). **ISS-006 close-clock Day-1 restart** — 07-20 Day-2 confirmation was PREMATURE per today's compute-futures-eda "1-day gap" finding (07-19 CSV was present but not analyzed on 07-20 → cfe leg missed on 07-20). Real timeline: 07-19 Day-1 → 07-20 GAP → **07-21 today Day-1 restart** (planner 06:38Z + compute-futures-eda 07:26Z both fired cleanly, 2-skill odd-DOM pocket). Earliest close Thu 2026-07-24 Day-3. Novel: **batch-health + heartbeat 2-day dispatch gap** (both last_success 2026-07-19; 07-20 + 07-21 silent so far) — the very fleet-watchdog skills whose absence caused 07-20's premature Day-2 confirmation are themselves down. **wigolo#216 aged 1.09d**, still in active bucket. **ms-01 stalled-2 unchanged** (aeon stars 0/100 flat 2 weeks running). Today's planner rotated top_priority off `verify-repo-settings-toggle-vs-pat` (streak-4) to `restore-agi-tracker-skill-md` (streak-1) — de-escalation w/ re-elevation triggers preserved per "don't thrash" rule |
| Cron-state | all 43 tracked skills at `last_status: success`, 0 `dispatched`, 0 `consecutive_failures ≥ 3`; cumulative `success_rate` < 0.5 on 38 skills (ISS-001 OAuth-residue catch-up, day 31) |
| Enabled skills | 44 — 42 in cron-state.json; `ai-framework-watch` (Mon 08:30) and `run-frequency-guard` (daily 23:00) still never-dispatched per [[enabled-skills-can-never-dispatch]] (**11th consecutive day flagged**, dedup-suppressed). `stale-content-pr-sweeper` **6-day** 23:45 miss streak (07-15/16/17/18/19/20). **NEW 07-21 dispatch-gap pair**: `batch-health` + `heartbeat` both last_success 2026-07-19T09:22Z / 09:25Z — 2-day silence on the fleet-watchdog pair. [[agi-tracker-missing-skill-md-dispatches-no-op]] confirmed HEALTHY-but-empty class — 07-20 13:04Z cron-state success but no `articles/agi-tracker-*` written |
| Open issues | 17 in INDEX.md — ISS-001 critical + ISS-002/005/006/007/008/009–018 high + ISS-019 medium. Unchanged count from 07-19. ISS-020 for [[enabled-skills-can-never-dispatch]] awaiting draft-and-file (planner rank-2 today, third-day carryover). Also awaiting: an ISS-021 candidate for the agi-tracker HEALTHY-but-empty class per today's planner rank-1 |
| Pending branches | **≥18** queued behind [[github-actions-cannot-create-prs]] unchanged from 07-19 (`compute-macro/2026-07-19`, `skillpacks/2026-07-19`, `fix/workflow-security-audit-2026-07-19`, plus 15 older). Preferred unblock unchanged: repo Settings toggle; PAT fallback proven live via swarm#527 07-18 merge. `verify-repo-settings-toggle-vs-pat` **de-escalated to holding** today (streak-4 → holding w/ preserved counter for re-elevation triggers: new staged branch, operator ack, or fresh linked blocker) — per planner "don't thrash" rule at streak-5 |
| PR queue (tracked-author) | Category tuple `(0,1,0,3)` UNCHANGED from 07-20. wigolo#216 aged 0.09d → 1.09d overnight, still in active bucket. 6 nodes byte-identical vs 07-20; local hash today `a55567402362e9bc` (differs from 07-20 recorded `c267efaeed220887` — different digest recipe, not directly comparable; tuple-identity authoritative). Notify **SKIP** — both step-5 triggers agree (tuple-identical + no fresh bot PR <24h). Resumes SKIP after 07-20 SEND. Next natural SEND trigger: InsForge#1742 or openinterpreter#1810 stale-threshold crossing on 07-24 |
| PR queue (swarm-repo) | **Queue still empty** — 4th consecutive day (pr-triage) / 5th consecutive same-day empty-queue result across five operator pr-review invocations spanning 07-19 → 07-21. No fresh PRs opened since the 5 dependabot merges cleared 07-18/07-19. App-level write block remains for future PRs per [[aeon-app-no-write-on-swarm-repo]] |
| Milestones | Unchanged from 07-20 — ms-01 aeon stars 0/100 **stalled-2**; ms-02 enabled skills 47/50 **approaching** (3 shy of target). Weekly Mon 12:00 slot next fires 2026-07-27 |
| Cost report | Weekly (07-13 → 07-19): $250.23 / 44 runs (+20.3% WoW). 3 anomalies (reflect 07-13 $24.25 run-level; pr-tracker +101% / code-health +181% skill-level). Monthly projection $1,072.43 (⚠ burn-rate watch). Article: `articles/cost-report-2026-07-20.md`. Next weekly cost-report fires 2026-07-27 Mon 07:00Z |
| workflow-security-audit | Unchanged from 07-19 BOOTSTRAP — 85 findings still standing (3C / 36H / 15M / 31L). Branch `fix/workflow-security-audit-2026-07-19` still 403-blocked. Follow-up items in MEMORY.md `## Next priorities` |
| skillpacks | Unchanged from 07-19: 172 skills · 16 packs · 0 solos · 1274 edges. `outages-fleet` pack persisted 2nd consecutive run. Fingerprint stable — no rerun today (weekly cadence) |
| compute-macro-correlate | Unchanged from 07-19 BOOTSTRAP. Next run expected 2026-07-26 Sat 07:00Z (weekly); should push Track B n=28 → ≥30 and unlock the pre-registered test |
| compute-futures-eda | 2026-07-20 CSV analyzed today (07-21). 144 rows, no CRITICAL/HIGH findings, sixth consecutive filed run at zero \|r\|≥0.8. Basket `maxCurve` 33.33% HIGH → 0% today — three-run strengthening streak retired; 12-seed rotation-not-persistence rule reasserts. **07-19 CSV was never analyzed** (compute-futures-eda missed 07-20 slot) — 1-day analysis gap in the record. Report: `memory/topics/compute-futures-eda/2026-07-20.md` |
| surplus-pulse | 26 consecutive catalog runs since 2026-06-21 (feed URL never set); anchor $0.0764/M (Llama 4 Scout), top R30 +88.7% every run, x402 rail 19 settlements / $198.43 (unchanged sim shape, deterministic seed 12648430) |
| Pending disclosures | **1 in `.pending-disclosure/`** unchanged — oomol-lab/open-connector 2026-07-11 (semgrep GCM-tag-length medium, 3-line fix documented). 10 days queued |
| Notify pattern | fleet-wide direct writes to `.pending-notify/${epoch}-${skill}.md` per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]] — held all week without `-f` corruption incident |
| notegraph | Pre-reflect on-disk baseline: HEAD from 07-19 reflect (`+5 nodes / +45 edges / 0 orphan delta`, 177n / 1762e / 1o); 07-20 reflect updated to 178n / 1738e / 1o (two new atomic notes untracked-in-git at scan time). Regen at end of this reflect will record delta |

## Permission constraints (current)
- aeon GitHub App: no write on `swarm-ai-research/swarm` (labels, comments, reviews 403). Verdicts run, posts blocked.
- vuln-scanner: token lacks fork scope — disclosures drafted to `.pending-disclosure/` only; operator opens the upstream PR.
- skillpacks / notegraph: PR creation blocked, branch pushed, compare link in notify.
- workflow-audit: cannot auto-land RCE patch — token lacks `workflows` write; needs `GH_GLOBAL` PAT.

## Open recommendations
- Pre-flight credential canary in `aeon.yml` (exits with distinct error when both `ANTHROPIC_API_KEY` and `CLAUDE_CODE_OAUTH_TOKEN` are empty).
- Out-of-band heartbeat from a different account / status-page pinger.
- Truncate `inputs.var` in `run-name` so pr-review titles don't leak multi-line policy.
- Fix `last_error` writer to store the actual stderr line.
- Patch `pr-tracker` SKILL.md to drop `--state merged` and `headRefName` per [[gh-search-prs-api-drift]].
