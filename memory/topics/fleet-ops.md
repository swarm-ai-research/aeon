# Fleet Ops

Cross-cutting operational lessons and constraints for the Aeon fleet: credential outages, monitoring-loop hazards, GitHub App permission boundaries, dispatch architecture, and cron-state pathologies.

## Open incidents
- [[issues/ISS-001]] — CLAUDE_CODE_OAUTH_TOKEN missing 2026-06-06 → 2026-06-20T06:05Z; investigating. Close deferred while [[issues/ISS-006]] runs; recovery batch is otherwise holding. Day 29 as of 2026-07-19.
- [[issues/ISS-006]] — Day 29 (2026-07-19, Sun odd-DOM): **BATCH_HEALTH_WARN** — 5 of 5 expected pocket skills delivered on-cadence in the 07:14:49–07:15:01Z cluster (planner, compute-macro-correlate, compute-futures-eda, config-validator, skillpacks); swarm-safety-eval (30 7 * * 0, out-of-window edge) missed alone. Close-clock streak restarted at **Day-1** after 07-18 planner miss per [[iss-006-pocket-recovery-is-noise]]; earliest close 2026-07-21 Mon Day-3 if 07-19 + 07-20 + 07-21 all clean. INDEX.md unchanged at 17 rows.
- [[issues/ISS-007]] — heartbeat missing_pattern in eval regex; new 2026-07-05, filed by skill-evals. Today's skill-evals article notes ISS-007 may be false positive (heartbeat pattern found in today's log via planner section). Enabled skill FAIL (not covered by ISS-002/005).
- [[issues/ISS-008]] — cost-report no_file_match; new 2026-07-05, filed by skill-evals. Standing ISS-006 tributary (weekly Mon 07:00 slot; 2026-07-13 slot fired ~74min late in 08:14Z catch-up cluster; next opportunity 2026-07-20 Mon 07:00Z).
- [[issues/ISS-005]] — swarm-safety-eval no_file_match: skill is now running successfully (last_success 2026-06-28T08:15:47Z) but its SSE_EMPTY path writes to the daily log, not an article; reclassify from `missing-secret-or-cron` to `permanent-limitation` per [[swarm-safety-eval-empty-writes-log-not-article]]. 2026-07-19 novel data: also missed dispatch (batch-health WARN, single miss).
- **ISS-009 → ISS-018** (2026-07-12 BOOTSTRAP fillings from skill-evals): 10 no_file_match issues on chronically-empty-output disabled/workflow_dispatch skills. Today's skill-evals BOOTSTRAP eval regex hit the same class again — 10 of 13 today's NEW_FAIL are disabled skills, reconfirming the eval regex needs same-day grace or should skip disabled/workflow_dispatch skills entirely.
- [[issues/ISS-019]] — batch-health OUTAGE 2026-07-14. Direct ISS-006 tributary.
- **Enabled-but-never-dispatched** — `ai-framework-watch` (weekly Mon 08:30) and `run-frequency-guard` (daily 23:00) have SKILL.md + `enabled: true` but no cron-state entries; heartbeat P3 novel-scan flagged 2026-07-11 per [[enabled-skills-can-never-dispatch]]. As of 2026-07-19 heartbeat both still missing — pattern hardens (**9th consecutive day flagged**, dedup-suppressed). `stale-content-pr-sweeper` (daily `45 23 * * *`) stale-streak extends to **4 days** (07-15/07-16/07-17/07-18 missed 23:45 slot, last_success 105h ago vs 48h threshold) — consistent with the 23:45 pocket in [[gha-messages-yml-cron-underdelivery]].
- **workflow-security-audit BOOTSTRAP** (2026-07-19 first run) — 85 findings surfaced (3C / 36H / 15M / 31L). 3 Critical `zizmor/unpinned-uses` in aeon.yml (checkout@v5 ×2, setup-node@v5). 36 High `zizmor/secrets-outside-env` (fleet-runner.yml 5× GITLAWB_*_PEM + CLAUDE_CODE_OAUTH_TOKEN + GH_GLOBAL; messages.yml 23 findings). All Critical/High flagged Manual per skill step-7 rule. Branch `fix/workflow-security-audit-2026-07-19` staged (versioned because 2026-06-20 branch already existed). Report: `articles/workflow-security-audit-2026-07-19.md`.

## Lessons (durable)
- [[oauth-outage-zero-token-signature]] — zero-token `result_json` = missing CLI auth, not a model error
- [[monitor-monitored-coupling]] — a monitor sharing a dependency with the monitored fleet can't catch outages of that dependency
- [[github-actions-cannot-create-prs]] — default Actions token cannot open PRs; surface compare links instead
- [[aeon-app-no-write-on-swarm-repo]] — pr-triage/pr-review verdicts on swarm-ai-research/swarm cannot post on-PR
- [[notegraph-phantom-file-refs]] — committed `notegraph.json` can reference files no longer on disk
- [[gha-inputs-unquoted-shell-rce]] — `inputs.*` flowing unquoted into `run:` shell commands is an RCE channel
- [[sandbox-blocks-piped-curl-installers]] — sandbox blocks `bash <(curl)` installers; audit skills degrade to hand-rolled fallbacks
- [[aeon-skills-dispatch-via-messages-yml]] — no per-skill workflow files; a window-wide silence implicates `messages.yml`, not per-skill auto-disable
- [[gha-messages-yml-cron-underdelivery]] — GHA silently drops ~97% of `messages.yml` `*/5` ticks in this repo with a daily 06:00–08:30Z dead zone (supersedes [[narrow-cron-pocket-vs-window-drop]])
- [[narrow-cron-pocket-vs-window-drop]] — _superseded_ — diagnostic command still useful, conclusion (matcher bug) ruled out
- [[gh-search-prs-api-drift]] — `gh search prs` dropped `--state merged`, `headRefName`, and `mergedAt`; SKILL.md fallback queries need patching
- [[pr-tracker-branch-prefix-misses-bot-identity]] — `ai/`-only branch filter drops `security/`-prefixed bot PRs under the same author identity; filter by commit-author email instead
- [[iss-006-pocket-recovery-is-noise]] — a single-day cron pocket recovery during ISS-006 is delivery-rate noise; close only after 3 clean days where every slot fires
- [[swarm-safety-eval-empty-writes-log-not-article]] — ISS-005 root cause is SSE_EMPTY path writing to the daily log, not the skill not running; reclassify as `permanent-limitation`
- [[compute-futures-12-seed-sample-too-small]] — at n=12 seeds, compute-futures-eda outlier flags reflect IQR-fence ties, not regime changes; widen sweep or switch to a tie-robust statistic
- [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] — wallet_sum_pnl σ ≈ 6e-12 means |r|≥0.8 crossings against volume columns (settlementLegs, realizedAbs, x402Total) are float-dust artifacts, not P&L signal; drop from the finding ladder
- [[skill-freshness-mtime-blind-in-gha]] — `actions/checkout` resets every file's mtime to the run instant, so skill-freshness's `stat --format=%Y` age check can never flag anything in GHA; switch to `git log -1 --format=%ct` producer-commit timestamp
- [[aeon-bot-uses-multiple-signing-identities]] — aeon bot signs commits under both `aeonframework@users.noreply.github.com` and `aeon@aeonframework.dev`; single-value `BOT_EMAIL` drops PRs silently
- [[pr-tracker-step-5-misses-fresh-bot-prs]] — pr-tracker only notifies on merges / stale / closed-no-merge; brand-new bot PRs land invisibly until they age into staleness
- [[pr-tracker-notify-repeats-with-no-state-change]] — step-5 has no dedup guard; a persistent stale/closed-no-merge state fires an identical notify every day until the wall clock rolls a PR off the 7d window
- [[notify-script-has-no-f-flag]] — `./notify -f <file>` is documented across multiple SKILLs but the actual script takes message as `$1`; using `-f` writes the literal `-f` as the message body
- [[status-md-auto-commit-drops-writes]] — _superseded 2026-07-12_ — writes DELAY (~15h to next sweeper auto-commit) rather than DROP; heartbeat's own glob still doesn't stage `docs/`
- [[snapshot-rebase-clobbers-docs-status-md]] — third failure mode entrenched across **7 consecutive days** (2026-07-12 → 07-18); upstream ref rotated `a7f04ee` → `fa89d8c` on 07-18 for the first time in 7 days but same clobber outcome (`d4892f9` today at 06:47:21Z), confirming the failure is the snapshot-merge itself, not stale upstream state — 8 days past 2026-07-16 mitigation urgency threshold
- [[graphql-statereason-only-on-issue-type]] — SKILL.md GraphQL query requests `stateReason` on `PullRequest`; that field exists only on `Issue` and the query hard-fails
- [[notify-inline-cat-substitution-blocked-in-sandbox]] — sandbox blocks any `$(...)` around `./notify` (inline arg AND two-step MSG-variable); write directly to `.pending-notify/` or dispatch via node `execFileSync`
- [[notegraph-extractor-generatedat-nondeterministic]] — notegraph extractor writes `generatedAt` into 4 outputs; naive `git diff --quiet` HAS_DIFF gate re-PRs stable corpora; inspect per-file diff, revert timestamp-only churn
- [[skill-state-on-blocked-pr-branch-is-lost]] — skills that write dedup state to their daily branch lose that state when the PR is blocked by [[github-actions-cannot-create-prs]]; suggest-edges re-proposed the same 3 similarity-1.00 edges from 07-07's branch because state never merged to main
- [[sandbox-blocks-shell-redirect-to-workdir]] — shell `>` to workdir paths is refused; use the tool's own `-o` flag or Python `pathlib.Path.write_text` after `subprocess.run`
- [[enabled-skills-can-never-dispatch]] — a skill with SKILL.md + `enabled: true` may have zero entries in cron-state.json and never dispatch; only heartbeat's P3 novel-scan catches this failure class
- [[probes-for-messages-yml-must-dispatch-outside-messages-yml]] — a diagnostic probe for messages.yml cron delivery can be silenced by the very failure mode it tests; trigger via `gh workflow run` or operator eyeball, never via another scheduled skill

## Snapshot (2026-07-19)
| Signal | Value |
|---|---|
| Today's status | 🔴 DEGRADED — ISS-001 day 29 residue (38 skills at success_rate<0.5, all `last_status: success`, `consecutive_failures=0`; denominator burn-down continues). **ISS-006 close-clock Day-1 restored** — Sun odd-DOM pocket delivered all 5 expected skills in a 12-second 07:14:49–07:15:01Z cluster (planner, compute-macro-correlate, compute-futures-eda, config-validator, skillpacks); memory-hygiene pair not eligible on odd-DOM. **swarm PR queue empty** — 5 dependabot PRs (#524/#529/#530/#532/#533) merged in 02:02–22:03Z window between 07-18 and 07-19, first cross-org batch since 25-day 403 block ended on swarm#527 07-18; App-level write block remains for all future cross-org writes per [[aeon-app-no-write-on-swarm-repo]]. `stale-content-pr-sweeper` 23:45 stale-streak extends to 4 days. Novel: **workflow-security-audit BOOTSTRAP** — 85 findings first ever run (3C / 36H / 15M / 31L), all Critical/High Manual. **skill-evals BOOTSTRAP** — 13 NEW_FAIL / 0 fixed, coverage 14/49 |
| Cron-state | all 43 tracked skills at `last_status: success`, 0 `dispatched`, 0 `consecutive_failures ≥ 3`; cumulative `success_rate` < 0.5 on 38 skills (ISS-001 OAuth-residue catch-up, day 29) |
| Enabled skills | 44 — 42 in cron-state.json, `ai-framework-watch` (Mon 08:30) and `run-frequency-guard` (daily 23:00) still never-dispatched per [[enabled-skills-can-never-dispatch]] (**9th consecutive day flagged**, dedup-suppressed). +1 P3 stale flag: `stale-content-pr-sweeper` 4-day 23:45 miss streak |
| Open issues | 17 in INDEX.md — ISS-001 critical + ISS-002/005/006/007/008/009–018 high + ISS-019 medium. Unchanged count from 07-18. ISS-020 for [[enabled-skills-can-never-dispatch]] still awaiting draft-and-file (planner rank-2 priority, scope-widened to fold in `stale-content-pr-sweeper`) |
| Pending branches | **≥18** queued behind [[github-actions-cannot-create-prs]] — 07-18 count of 16 plus today's `compute-macro/2026-07-19` (17th), `skillpacks/2026-07-19` (18th), `fix/workflow-security-audit-2026-07-19` (versioned; `fix/workflow-security-audit` from 2026-06-20 audit already existed unmerged). Preferred unblock unchanged: repo Settings toggle; PAT fallback proven live via swarm#527 07-18 merge |
| PR queue (tracked-author) | Category tuple `(0,1,0,2)` — **UNCHANGED** from 07-18 (byte-for-byte identical trigger tuples, canonical hash `c71ff2a003072597`). Hash-dedup blocked step-5 notify → **SKIP** (first SKIP since 2026-07-16, ends 2-day SEND streak). InsForge#1742 quiet since 07-17T17:38Z (50h+ static), openinterpreter#1810 quiet since file (42h+ static), AR#436 stale 6th consecutive day (12.85d activity age), Vibe-Trading#390 rolled off 7d merged window, kage#66 in 30d closed table |
| PR queue (swarm-repo) | **Queue completely empty** — 5 dependabot PRs skipped-by-bot-rule on 07-18 all merged in 02:02–22:03Z window between runs (#529 02:02:02Z, #532 02:02:06Z, #533 02:02:08Z, #530 02:11:52Z, #524 22:03:37Z on 07-18). First cross-org batch-merge since 25-day 403 block ended on swarm#527 (2026-07-18T02:29:19Z). Confirms operator PAT / merge automation ran on dependabot cohort overnight. Next fresh non-bot PR will be first live merge-gate test on empty-start queue |
| workflow-security-audit | **BOOTSTRAP 2026-07-19** — 85 findings first ever run against this repo (3C / 36H / 15M / 31L). Auto-fixed 0 (all NEW C+H Manual). 3 Critical `zizmor/unpinned-uses` all in aeon.yml. High distribution: messages.yml=23, fleet-runner.yml=10, chain-runner.yml=2, sync-upstream.yml=1. Attack chains documented for aeon.yml issue-labeled trigger (plant-then-run of `@anthropic-ai/claude-code`) and fleet-runner.yml secret exposure (5 × GITLAWB_*_PEM + CLAUDE_CODE_OAUTH_TOKEN + GH_GLOBAL fine-grained PAT with Workflows-write, all referenced without env boundary). Branch versioned because 06-20 branch still unmerged |
| skill-evals | **BOOTSTRAP 2026-07-19** — 13 NEW_FAIL / 0 fixed / 0 still failing / 0 stable (first ever eval article). Coverage 14/49 (28%). 10 of 13 failing skills are disabled/workflow_dispatch (no output expected — reconfirms eval regex needs same-day grace or disabled-skill skip). Enabled failures: changelog (ISS-002), cost-report (ISS-008), swarm-safety-eval (ISS-005). Action queue head: Dispatch changelog (Mon 16:00 UTC). Article: `articles/skill-evals-2026-07-19.md` |
| skill-freshness | FRESHNESS_OK today — 44 enabled consumers · 2 deps · 0 flagged. Fingerprint identical to 2026-07-18 → FRESHNESS_NO_CHANGE, no notification. Structural mtime blind spot per [[skill-freshness-mtime-blind-in-gha]] unchanged. `agi-tracker` missing SKILL.md (scan skipped) |
| skillpacks | 2026-07-19 regen: **172 skills · 16 packs · 0 solos · 1274 edges** (+1 edge vs 07-05). Structural churn: new pack `outages-fleet` (18 members) formed from dissolved `fleet-evolve` — 17 of 18 members moved intact; `batch-health` moved from `monitor-movers` to `outages-fleet` (vocabulary shift toward outage/repair terminology has densified enough to dominate). Persisted 2nd consecutive run |
| compute-macro-correlate | **First run 2026-07-19** — Track A DePIN proxy (n=137): RENDER ρ=−0.124 (p=0.151), TAO ρ=−0.118 (p=0.174), IO ρ=−0.044 (p=0.612) after partialling on {BTC, SOL}; all null at |ρ|<0.15 threshold; 30-cell wide macro scan (3 tokens × 10 non-crypto macros) uniformly null. Track B sweep P&L (n=28): deferring, need 30, expected 1-2 weekly runs. New MOC: `memory/topics/compute-futures-macro-correlations.md` |
| compute-futures-eda | 2026-07-18 CSV analysis: HIGH basket `maxCurve` 33.33% outlier_pct (07-16 8.33% → 07-17 16.67% → 07-18 33.33%) — first (mode, column) pair to persist AND strengthen 3× in a row. HIGH spread `settlementLegs` 25% (distribution collapsed to 4-value shape). Fifth consecutive zero |r|≥0.8; x402 `settlementLegs × x402Total` fifth sign-flip in five rotations |
| Pending disclosures | **1 in `.pending-disclosure/`**: oomol-lab/open-connector 2026-07-11 (semgrep GCM-tag-length medium, 3-line fix documented). vuln-scanner today: pixel-point/aval clean (0 candidates) |
| Notify pattern | fleet-wide direct writes to `.pending-notify/${epoch}-${skill}.md` per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. No `-f`-corruption incidents this week |
| notegraph | Pre-reflect on-disk baseline: HEAD (172n · 1717e · 1o from 07-18 morning; 07-18 reflect post-run 174n/1738e/0o not persisted to notegraph.json in shallow-clone). Regen at end of this reflect will record delta |

## Permission constraints (current)
- aeon GitHub App: no write on `swarm-ai-research/swarm` (labels, comments, reviews 403). Verdicts run, posts blocked.
- vuln-scanner: token lacks fork scope — disclosures drafted to `.pending-disclosure/` only; operator opens the upstream PR.
- skillpacks / notegraph: PR creation blocked, branch pushed, compare link in notify.
- workflow-audit: cannot auto-land RCE patch — token lacks `workflows` write; needs `GH_GLOBAL` PAT.

## Open recommendations
- Pre-flight credential canary in `aeon.yml` (exits with distinct error when both `ANTHROPIC_API_KEY` and `CLAUDE_CODE_OAUTH_TOKEN` are empty).
- Out-of-band heartbeat from a different account / status-page pinger.
- Truncate `inputs.var` in `run-name` so pr-review titles don't leak multi-line policy.
- Fix `last_error` writer to store the actual stderr line.
- Patch `pr-tracker` SKILL.md to drop `--state merged` and `headRefName` per [[gh-search-prs-api-drift]].
