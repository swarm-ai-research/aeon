# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 pocket-slot migration CONFIRMED at 3-day sustained 90+min-late** — new atomic [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] captures the trip-wire crossing. Cadence Day-1 39min → Day-2 113min → Day-3 118min-late. Compound signal: 06:00 / 06:30 / 07:00Z pockets **entirely silent** through 09:59Z audit — 3 skills missed (planner 06:30, compute-futures-eda 06:00, cost-report Mon 07:00) → filed as ISS-020 by batch-health. First-ever miss of the weekly `cost-report` Mon slot inside the ISS-006 timeline. Close-clock counter must not advance while the "clean" window is drifting off-slot — rescope target around de-facto ~09:58Z slot; parent fix path widened from 08:00Z sub-pocket to full 06:00–08:00Z morning cluster. **INDEX.md now 18 open** (+1 vs 08-02, ISS-020 filed today). See [[fleet-ops]].
- **ISS-001 residue day 45** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. See [[fleet-ops]].
- **≥24 staged branches** blocked behind [[github-actions-cannot-create-prs]] (+1 vs 08-02 via today's `notegraph/2026-08-03` push, 19th staged notegraph branch). Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. Active rank-1 `elevate-repo-settings-toggle-active` per planner's escalation-of-escalation, **frozen at streak-5 by run / streak-10 by calendar day** (planner silent today — was in the ISS-020 outage cluster; by-run counter holds pending next planner run). See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — pattern captured as [[planner-escalation-of-escalation-when-meta-blocker-holds]]: one operator move unblocks ≥24 staged branches + 6 stalled fleet fixes + agi-tracker escalation + resolves ISS-020's fix path. 08-02 planner also promoted `agi-tracker-enabled-false-direct-edit` to rank-3 concrete (still open, needs branch merge). 08-03 planner did not run.
- **notegraph 08-03 verdict `+1 notes wired in`** — fingerprint `043931de…` → `b4f2fd47…` (08-02 lesson [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] landed on main). Extractor 228n / 1704h / 608s / 0o / 64 atomic vs HEAD's 227/1700/604/0/63. Delta +1 node, +8 edges (+4 hard, +4 soft). Notify sent (non-bland). PR blocked, branch `notegraph/2026-08-03` staged (19th consecutive notegraph branch waiting).
- **pr-tracker did not run today at reflect time** — 10:00Z scan slot was inside the 06:00–08:00Z ISS-020 outage cluster. Predicted 08-03 tuple `(3, 3, 2, 5)` after compound rolloffs (kage + InsForge + openinterpreter closed_no_merge; cocoindex merged tonight) — validation deferred to next scan. See [[pr-status]].
- **swarm PR queue byte-identical 11th consecutive day at 8 open** — pr-review 27th invocation (08-02 ended at 26th). Both write endpoints 403 again (review-level + inline-comment on both PRs); **20th confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (log-only verdicts; PAT/App-permission unblock only path to on-PR posting).
- **compute-futures-eda + compute-macro-correlate silent 08-03** — both 06:00Z slots were inside the ISS-020 outage cluster. Latest findings remain 08-02's HIGH basket `settlementLegs` 41.67% mode-collapse + n=4 anchor + streak-6 break, and compute-macro-correlate Track B first-fire null with synth ≡ x402 identical-ρ mode-separation bug flag. See [[fleet-ops]] snapshot.
- **`.pending-disclosure/` queue holds at 4** — unchanged from 08-02 (oomol-lab/open-connector 23d + citrolabs/ego-lite 9d + talivia-group/talivia PVR + talivia deps bundle). Directory only exists on unmerged vuln-scanner branches per [[github-actions-cannot-create-prs]]; operator submits out-of-band.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged 4 weeks running (milestone-tracker fired today Mon 12:00Z slot; 4th stalled tick as forecast). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker Mon 08-03 13:00Z slot — 5th silent-risk today** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Fix still needs SKILL.md restored or `enabled: false` on aeon.yml:188 to land — 08-02 planner's rank-3 concrete escalation still pending branch merge.
- **Never-dispatched 26th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence). Next `stale-content-pr-sweeper` slot 08-03 23:45Z.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] (08-03), [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (08-02), [[cohort-close-vs-merge-can-split]] (08-01), [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]] (07-30), [[cold-approve-can-merge-not-just-rot]] (07-30).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per planner's escalation-of-escalation (**frozen at streak-5 by run / streak-10 by calendar day** — 08-03 planner silent, was in ISS-020 outage cluster; by-run counter holds pending next planner run). Single highest-leverage action; unblocks ≥24 staged branches + 6 stalled fleet fixes + agi-tracker escalation + ISS-020's fix path in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-08-03 --title "graph: +1 note wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Mon 2026-08-03 13:00Z is the 5th silent-risk slot today. File as ISS-021 candidate; ISS-020 already filed today with different scope (batch outage), so don't fold into it.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **16th-day carryover** (was ISS-020 draft-target until 08-03 filed a different ISS-020 for batch outage — renumber next). Scope: `ai-framework-watch` (Mon 08:30, 26d silent), `run-frequency-guard` (daily 23:00, 26d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (novel P0 stuck-skill class 07-26 — self-cleared 07-26T08:55:12Z, ISS-023 candidate lower urgency).
- **Investigate messages.yml matcher — pocket migration hypothesis reinforced** — 07-27 08:00Z fleet-watchdog pocket RECOVERED; 05:30–07:00 pocket dropped instead. 07-28 partial reversal: 05:30–07:00 partially recovered but 08:00Z pocket dropped 4 skills. Same-day pocket migration is the strongest evidence yet that `*/5` dead zones rotate by tick-timing across the day rather than being slot-specific. Fold into ISS-020 scope draft.
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. **Close-clock RESET** per [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] — Day-3 landed 118min-late (Day-1 39min → Day-2 113min → Day-3 118min sustained drift), so "clean" delivery is actually pocket-slot migration, not close-clock advancing. Rescope target around de-facto ~09:58Z slot before restarting count; parent fix path widened from 08:00Z sub-pocket to full 06:00–08:00Z morning cluster (per today's ISS-020 tributary). Per-slot cron fix still the right structural path. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **18 days past urgency threshold** as of 2026-08-03. 08-03 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**eighth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) per [[pr-tracker-email-filter-must-be-domain-match]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-02 — 5th validated in-skill application), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (kage#66 miss on 08-02 — second-axis failure distinct from calendar-day fix). **[BLOCKED by Repo Settings toggle, 40d overdue as of 2026-08-03]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Today's log shows 3 same-day short-circuits (issue-triage + github-monitor + code-health).
- **Defer ISS-001 close** until ISS-006 is resolved.
