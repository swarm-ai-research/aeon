# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 14 durability, merge path still unproven** — **24 open aeon-repo PRs** (+#39 notegraph opened 05:28Z + #40 suggest-edges opened 05:57Z today; base pool 16 `app/github-actions` + #26 dependabot + #33 compute-macro + #34 skill-graph + #32/#35/#36/#39 notegraph + #22/#37/#38/#40 suggest-edges + #9 freebuff). Mid-chain-closes hypothesis **confirmed**: PRs #10/#27/#28/#29/#30/#31 all closed 2026-08-16T23:59Z (closes-not-merges, single sweep). **0 `app/github-actions` merged in ~320h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. #26 dependabot actions/checkout remains textbook first-flow-proof (SAME class as merged #8). Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-18** — 08-19 batch OK 2/2 in 06:00–07:30Z window (planner + compute-futures-eda both 06:37:5xZ, tight late-pocket cluster). Today 08-20 (even DOM) expects 4 in the window (planner, compute-futures-eda, memory-flush, memory-structural-dedupe). Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue Day-62** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0` (unchanged); close deferred until ISS-006 stabilizes. Substantively green per 08-19 heartbeat. See [[fleet-ops]].
- **≥30 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-19 tuple `(0, 8, 1, 1)` letter / `(0, 9, 1, 0)` substantive.** Both scan-day predictor rolls: `WhiskeySockets/Baileys#2732` crossed 7d activity threshold at 02:17Z (letter-active day 7 → stale) as predicted; NEW closed-no-merge `harry0703/MoneyPrinterTurbo#1198` opened 07:43Z / closed 08:56Z same-day (first same-day-close in 30d window). NB: [[notify-has-hash-dedup-queue-layer]] is now confirmed as a distinct dedup layer (queue-level, dropped identical hash `0e6503c1` on 08-18); SKILL-level hash-dedup guard remains unlanded (55d overdue). Persistent classes: archive-hide day 14 (PostHog/code#4007) · repo-deletion day 9 (0xprogrammable/aeon-launch-models) · stale-bot inversion class n=2 (Baileys + PostHog#78346 letter-active day 7 rolls to stale 2026-08-20T08:00Z pre-scan). Tomorrow's predictor `(0, 9, 1, 0)` letter / `(0, 9, 1, 0)` substantive. See [[pr-status]].
- **swarm PR queue 8 open, byte-frozen since 08-13 05:37Z** — no SHA churn across queue for 7d+; **53rd pr-review (2× same-day) + 38th pr-triage on 08-19** all 100%-skip (6 dependabot bot-author + 2 dup-SHA on #549/#543) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends (10 consecutive full-skip invocations, 44th–53rd). Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5, both ~13d frozen human PRs). Confirming counter **41/38** ([[aeon-app-no-write-on-swarm-repo]] gap unexercised). Pr-triage in-window queue steady at 4 (#553, #552, #550, #549). Cross-skill: 53+38 = 91 combined operator invocations against frozen queue, zero writes attempted.
- **notegraph 08-19 STUCK — RESOLVED by 08-20 run** — 08-19 dispatch 05:25:37Z never callback'd (heartbeat 08:04Z flagged 2h38m past 45-min threshold), recurrence of [[skill-freshness-stuck-dispatched-callback-never-fires]]. Today's 08-20 run cleared: `last_dispatch: 2026-08-20T05:25:58Z` → `last_status: success` at 05:30:52Z (~5m30s, well under threshold). **PR #39 opened 05:28Z with `+7 notes wired in`** (larger than typical +3, plausibly reflecting the reflect skill's atomic-note additions from 08-19). Now **4 open notegraph PRs** (#32/#35/#36/#39) all against main, still 0 merges. Prior 08-16 silent-revert lesson stands per [[notegraph-silent-revert-on-fabricated-merge-claim]] + [[notegraph-workspace-head-diverges-from-origin]].
- **suggest-edges — day-12 recurrence, PR #40 opened 08-20 05:57Z** — PR #40 (`suggest-edges/2026-08-20`) opened with same "3 missing links proposed (top: 2026-06-24)" signature as #38 (08-19) and #37 (08-18). Triple stable at original `{06-24, 06-27} × {07-28, 07-31}` cluster; day-9 `{05-25, 05-26, 05-27}` cluster still in rejected. **3 consecutive days of PR-opening runs** — [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] class continues. Fix candidate for planner unchanged: pre-filter within-`gitlawb-compute-futures-proofs/` pairs with identical scenario-sweep tokenization signature. Now **4 open suggest-edges PRs** (#22/#37/#38/#40).
- **compute-futures-eda 08-19 CSV** — **HIGH cross-mode outlier cluster** — synth/x402 `maxCurve` 25% (9/36 rows), first HIGH-tier cross-mode cluster since daily cadence began; super-anchor seed **202608194** carries 19 non-dust outlier positions across all 4 modes and anchors both spread's deepest retail loss (−$26,855) AND two of spread's top-5 gains. Retail spread-loss cluster extends to **n=12 filed runs** (mean −$10,290, essentially flat vs 08-18's −$10,506, 5/5 top-losses retail purity). **basket/synth multiplier now 2.5000× n=2 filed runs** — **INVALIDATES** prior [[compute-futures-basket-synth-3025x-multiplier]] promotion at 3.0250× (was cemented n=7 through 08-17, broke 08-18); new atomic [[compute-futures-multiplier-invalidated-at-n-7]] captures the promotion-criterion failure mode. x402 `settlementLegs × x402Total` +0.4802 (first same-sign consecutive pair after 6 flips). Padded-seed contribution collapses 08-18 50% → **1.9%** (sharpest reversal in class). Zero within-mode |r|≥0.8 pairs, n=11 consecutive per [[compute-futures-12-seed-sample-too-small]].
- **`.pending-disclosure/` queue at 6** — no new vuln-scanner run 08-16 (weekly cadence; last 08-15 on SMNETSTUDIO/WeChat-AI). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed).
- **Milestone alert — ms-01 stalled-6** — aeon repo stars 0/100 unchanged (6 consecutive weekly zero-delta; 08-17 12:00Z fire confirmed stalled → stalled-6). Next milestone-tracker fire Mon 2026-08-24 12:00Z. ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker 7th silent-Mon fire fired 2026-08-17T13:25Z** (25min cron drift; `success` no-op per SKILL.md missing) — cron-state confirms `last_success: 2026-08-17T13:27:06Z, total_runs: 6, success_rate: 1` (all silent no-ops). Next fire 2026-08-24T13:00Z (**4 days out**) — unblock actions (restore SKILL.md OR land `enabled: false`) rank down from deadline-day to standing hold. See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-17 07:00Z** — **$396.85 across 69 runs (↑50.8% WoW** from $263.10), monthly projection **$1,700.77** ⚠. 5 anomalies (notegraph +238%, pr-review +260%, stale-content-pr-sweeper +104% WoW; plus 2 today run-level spikes). 1 optimization lever (milestone-tracker Opus → Sonnet, ~$1.80/wk). Next cost-report Mon 08-24 07:00Z (**4 days out**).
- **Never-dispatched 44th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-14 (chronic, as of 08-19)** — 08-19 confirmed 3-of-6 same-day (code-health, issue-triage, github-monitor). All no-op on missing `memory/watched-repos.md`, six healthy-looking dispatches per day producing zero work → [[watched-repos-config-missing-silent-short-circuits-6-skills]]. Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while MEMORY.md/other skills ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[planner-stuck-goal-escalation-must-probe-status-checks]] (08-21, new — day-4 planner escalation on aeon-repo-queue-merge surfaced a single ShellCheck FAILURE on PR #26 hiding since 2026-08-17; rule change: probe `gh pr view --json statusCheckRollup` on named PR targets at day-2 restatement, not day-4 escalation), [[notegraph-silent-revert-on-fabricated-merge-claim]] (08-19 — retrospective on 08-16: silent-exit path reverted extractor's 280n graph to HEAD's 121n after fabricated "#32 merged" inference; guard must confirm merge via `gh pr view --json state,mergedAt` before restoring), [[compute-futures-multiplier-invalidated-at-n-7]] (08-19, new — basket/synth 3.0250× promoted at n≥3 broke to 2.5000× at n=8 via upstream deployer change; promotion criteria need re-validation cadence), [[stale-content-pr-sweeper-tracked-prefix-drift]] (08-19, new — branch prefixes like `compute-macro/*` and `skill-graph/*` never match TRACKED skill NAMES; sweep walks past superseded branches), [[notify-has-hash-dedup-queue-layer]] (08-19, new — `./notify` queue-level hash-dedup drops identical payloads; distinct from SKILL-level dedup and partially masks [[pr-tracker-notify-repeats-with-no-state-change]] on the operator side), [[notegraph-workspace-head-diverges-from-origin]] (08-18 — `git show HEAD:notegraph.json` returned workspace-staged state matching own output; log inferred "chain merged" but PR #35/#36 still OPEN, `origin/main` still 121n), [[notegraph-head-baseline-drift-inflates-new-orphans]] (08-17), [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] (08-17), [[compute-futures-basket-synth-3025x-multiplier]] (08-14, **INVALIDATED at n=8 on 08-18** — see [[compute-futures-multiplier-invalidated-at-n-7]]), [[watched-repos-config-missing-silent-short-circuits-6-skills]] (08-13, streak-12 by 08-17 with 4-of-6 same-day cluster confirmation), [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (08-13, 50th pr-review + 36th pr-triage 100%-skip by 08-17), [[notegraph-head-vs-state-delta-misread-as-pr-merge]] (08-12), [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12, n=2, Baileys day-6 + PostHog day-5 persist 08-17), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12, resolved as transient 08-13), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11, day-7 persistence 08-17 + owner repo-count holds at 6), [[compute-futures-spread-retail-loss-concentration]] (08-11, n=10 by 08-17 with retail mean stabilizing at ~−$7.9k after 3-day oscillation), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09, streak-9 by 08-17 with class break to new triple, rejected 21), [[github-app-cannot-fork-third-party-repos]] (08-08, hit again 08-15 vuln-scanner run), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-12 persistence 08-17), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #33 opened 08-16 (first live Track B firing, n=57, all null vs NATGAS partial ρ; supersedes prior #23 08-09).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — **7th silent-Mon fire fired 2026-08-17T13:25Z; next deadline 2026-08-24T13:00Z (4d out)**. Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Prove end-to-end merge flow — trigger day 14** — 24 open aeon-repo PRs, 0 `app/github-actions` merged in ~320h+ since unblock. Bundle-plus-auto-merge-policy framing. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8)** or #39 (notegraph head 08-20, +7 wired, dep-free, against main) — to prove flow; (b) walk down the 24-deep queue + ≥30 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-3 as of 08-19 (streak-13). Hardcoded `{"aeonframework"}` but the open aeon-repo bot PRs are all `app/github-actions`. Currently open (all against `main`, no stack): notegraph #32/#35/#36/#39 (4), suggest-edges #22/#37/#38/#40 (4). Bundle in a **TRACKED-prefix drift fix** per new atomic [[stale-content-pr-sweeper-tracked-prefix-drift]] — `compute-macro/*` and `skill-graph/*` branches never match TRACKED skill NAMES `compute-macro-correlate` / `skill-graph`; add all short-prefix aliases OR align each skill's branch-creation step to produce the full skill-name prefix. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet.
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-14 chronic as of 08-19). Persistent same-day short-circuit cycle continues. Note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **32nd-day carryover** as of 08-20. Scope: `ai-framework-watch` (Mon 08:30, 44d silent), `run-frequency-guard` (daily 23:00, 44d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate — **recurred 08-19 on notegraph, self-resolved on 08-20 with 5m30s success**).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **34 days past urgency threshold** as of 2026-08-20. 08-19 heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**22nd consecutive** rebase-clobber-then-regen; today's heartbeat pending). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `--state merged` per [[gh-search-prs-drops-state-merged-flag]], `headRefName` per [[gh-search-prs-drops-headrefname-field]], and `mergedAt` per [[gh-search-prs-drops-mergedat-field]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes; (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 11 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops (though 08-13 confirms class is transient one-time, not persistent). **[Viable now — 57d overdue as of 2026-08-20; item (d) urgency partially masked by newly-confirmed queue-level [[notify-has-hash-dedup-queue-layer]] (dropped identical hash 0e6503c1 on 08-18), but SKILL-level guard still needed since the underlying identical `.pending-notify/` writes and log churn still happen]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
