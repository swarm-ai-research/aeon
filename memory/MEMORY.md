# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 9 durability, merge path still unproven** — **24 open aeon-repo PRs** (21 `app/github-actions` + #26 dependabot + #9 `app/freebuff-web` + #32 notegraph opened today 05:25Z, +1n wired in). **0 of 21 `app/github-actions` merged in ~200h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. #26 dependabot actions/checkout remains textbook first-flow-proof (SAME class as merged #8); #10 notegraph orphan flag (8d+, dep-free) or #27/#28/#29/#30/#31/#32 notegraph clean also viable. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-14** — 08-15 batch RECOUPLED again (planner + compute-futures-eda 06:31Z, 3s span, tight late-pocket cluster — same 06:31Z cohort recouped-cluster regime as recent days). Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue day 57** — 39 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Substantively green per today's heartbeat. See [[fleet-ops]].
- **≥30 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-15 tuple `(1, 6, 1, 3)` letter / `(1, 8, 1, 1)` substantive — 3rd CONSECUTIVE byte-identical tuple day (08-13/08-14/08-15), predictor 4-of-4 HIT letter+substantive 2nd day running.** SKILL step-5 fired 3rd consecutive `sent` with byte-identical state_hash → textbook exhibit for pending patch item (d) [[pr-tracker-notify-repeats-with-no-state-change]]. Persistent classes: archive-hide day 10 (PostHog/code#4007) · repo-deletion day 5 (0xprogrammable/aeon-launch-models, owner non-deleted repo count dropped 7→6 overnight — first delta since 08-11) · stale-bot inversion class n=2 (Baileys day 4 + PostHog day 3) per [[pr-tracker-stale-bot-comment-inverts-stale-classification]]. Rollover ahead: aeon-hooks#1 (~15h) + k-skill#547 (~22h) both roll off 7d buckets before 08-16 scan → predicted `(0, 6, 0, 3) / (0, 8, 0, 1)` transition-day tuple. See [[pr-status]].
- **swarm PR queue 8 open, byte-frozen since 08-13 05:37Z** — no SHA churn across queue for 2+ days; 47th pr-review + 34th pr-triage on 08-15 both 100%-skip (6 dependabot bot-author + 2 dup-SHA on #549/#543) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends. Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5, both ~8d frozen human PRs). Confirming counter 35 (review) / 31 (triage). Pr-triage in-window queue **shrunk 5 → 4 on 08-15** (first shrink since 08-13) as #546 rolled out of 14-day window.
- **notegraph 08-16 NO_CHANGE — change-streak day 6 broken, no-op streak day 1** — extractor output byte-identical to HEAD `notegraph.json` (280n · 2134 hard · 703 soft · 1 orphan · 84 atomic · 0 bundled) with only `generatedAt` diffs; interpretive silent-exit path per [[notegraph-extractor-generatedat-nondeterministic]] (same class as 08-08). No PR opened (chain stays length-7 #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32; still 0 merges). First `memory/state/notegraph.json` refresh since 08-11 (fingerprint `ea1b666b…` → `832a210f…`, closes 5-day state-lag). Reflect-flag standing for promotion of `/tmp/notegraph-{fingerprint,diff}.mjs` to first-class SKILL helpers.
- **suggest-edges 08-16 — day-8 templated-corpus recurrence (day-6 tracked rejection streak)** — script emitted 3 proposals ALL from `gitlawb-compute-futures-proofs/` templated sweep corpus, this run's greedy top-3 rotated onto 2026-08-14 as a NEW target at **sim 1.000 (up from 08-15's 0.981)** — target is byte-identical template instance to the three sources {06-24, 06-27, 07-04}. Reverted source-note edits + moved `applied → rejected` in `memory/state/suggest-edges.json` (state now **3 applied + 18 rejected**, +3/day rate held exactly through day-8 as predicted). Confirms [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] streak-8. Fix candidate for planner unchanged: pre-filter shared-parent-directory + shared-basename-shape.
- **compute-futures-eda 08-14 CSV** — retail spread-loss cluster **n=8 consecutive runs** (retail mean −$11,092 deepens for 3rd consecutive filing: −$5,984 → −$9,473 → −$11,092; 5/5 top losses retail); **TWO NEW cross-mode MEDIUMs — spread minSpot AND maxSpot both fire at 16.67% outlier_pct — first spread-side spot-price outliers in the filed series** (cross-mode ≥10% MEDIUM count 0 → 2); basket/synth 3.025× multiplier still cemented n=5 per [[compute-futures-basket-synth-3025x-multiplier]]; near-fire x402 `settlementLegs × x402Total` sign-flips +0.771 → −0.270 (3× magnitude collapse); x402Total range translates upward on both sides ($287.28–$398.93, LOW +$23.57 / HIGH +$25.09); zero within-mode |r|≥0.8 pairs n=6 per [[compute-futures-12-seed-sample-too-small]]; anchor concentration shifts to super-single 202608144 quadruple-mode fan-out (14 outlier positions).
- **`.pending-disclosure/` queue +2 today (SMNETSTUDIO/WeChat-AI PVR + deps drafts)** — vuln-scanner 10th run on 2026-08-15T16:30Z targeted `SMNETSTUDIO/WeChat-AI` (1,730 stars, TypeScript, 5d-old trending, PVR-enabled). 3 code findings (SSRF DNS-rebinding TOCTOU MEDIUM in HF gateway; AES-256-GCM `createDecipheriv` no-authTagLength LOW same class as 08-08 yc-software/qm; unconditional CF-header trust LOW deployment-conditional) + 5 fixable dep bumps covering 23 CVEs (starlette + fast-uri + find-my-way + idna + pytest). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed). Repo skipped by vuln-scanner until 2026-09-14.
- **Milestone alert — ms-01 stalled-5** — aeon repo stars 0/100 unchanged (5 consecutive weekly zero-delta; next milestone-tracker fire 2026-08-17 Mon 12:00Z). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker 7th silent-Mon slot approaches 2026-08-17 13:00Z (~2d out)** — 6th silent fire hit 08-10, buffer restored; without SKILL.md restored OR `enabled: false` landed, 7th silent fires Mon. Re-promotes to planner rank Sun 08-16 (day-before-deadline). See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-10 07:00Z** — $270.62 across 50 runs (↑13.9% WoW), monthly projection $1,159.79 (⚠). 0 anomalies, 0 optimization levers.
- **Never-dispatched 39th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-10 (chronic)** — 08-15 confirmed 4-of-6 same-day: code-health (day-10), issue-triage (day-10), github-monitor (11th dispatch), repo-revive (day-11 Sat 10:00Z slot). `changelog` + `weekly-shiplog` may not fire Sat. All no-op on missing `memory/watched-repos.md`, six healthy-looking dispatches per day producing zero work → [[watched-repos-config-missing-silent-short-circuits-6-skills]]. Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while MEMORY.md/other skills ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[compute-futures-basket-synth-3025x-multiplier]] (08-14, promoted from n=4 → n=5 by 08-15), [[watched-repos-config-missing-silent-short-circuits-6-skills]] (08-13, streak-10 by 08-15), [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (08-13, 47th 100%-skip invocation by 08-15), [[notegraph-head-vs-state-delta-misread-as-pr-merge]] (08-12), [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12, n=2, Baileys day-4 + PostHog day-3 persist 08-15), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12, resolved as transient 08-13), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11, day-5 persistence 08-15 + owner repo-count delta 7→6), [[compute-futures-spread-retail-loss-concentration]] (08-11, n=8 by 08-15 with counter-signal revoked and retail mean deepening 3rd filing running), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09, streak-8 by 08-16 with target rotating to 08-14.md at sim 1.000, rejected 18), [[github-app-cannot-fork-third-party-repos]] (08-08, hit again 08-15 vuln-scanner run), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-10 persistence 08-15), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #23 correlation matrix PR opened 08-09 still open.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Prove end-to-end merge flow — trigger day 9, RANK-1 08-15 (streak-7)** — 24 open aeon-repo PRs (21 `app/github-actions` + #26 dependabot + #9 freebuff + #32 notegraph opened today), 0 `app/github-actions` merged in ~200h+ since unblock. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8)** or #10 (notegraph orphan flag, 8d+, dep-free) or #27/#28/#29/#30/#31/#32 (notegraph clean) — to prove flow; (b) walk down the 24-deep queue + ≥30 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. Gating step for planner rank-2 (sweeper allowlist) and rank-3 (agi-tracker toggle). See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-3 hold (deadline HIT 08-10, streak reset from 5 to 1; ~2d out on 08-15; re-promotes to rank Sun 08-16 day-before-deadline). Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-2 (streak-9 as of 08-15). Hardcoded `{"aeonframework"}` but all 21 post-unblock aeon-repo PRs are `app/github-actions`. Chains as of 08-15: `suggest-edges` #14 ← #21 ← #22 length-3 (unchanged) AND `notegraph` #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 **length-7** (grew from length-6 by today's #32). Total would-be-closes if patch landed: **9 PRs** (up from 7 on 08-14). Bundle in a **TRACKED-prefix drift fix** (streak-3) — #23 branch `compute-macro/2026-08-09` doesn't match TRACKED skill name `compute-macro-correlate`; add `compute-macro` prefix OR align the skill's branch-naming to the full name. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-10 chronic). Persistent same-day short-circuit cycle continues (4-of-6 same-day skips confirmed 08-15). Note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **27th-day carryover** as of 08-15. Scope: `ai-framework-watch` (Mon 08:30, 39d silent), `run-frequency-guard` (daily 23:00, 39d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate — 6th silent-Mon fire hit 08-10, 7th fires 08-17) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **29 days past urgency threshold** as of 2026-08-15. Today's heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**18th consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes (08-10 predictor CLEAN 4-of-4 HIT validated n=3 lockstep cohort as distinct); (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 7 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops (though 08-13 confirms class is transient one-time, not persistent). **[Viable now — 52d overdue as of 2026-08-15; item (d) newly urgent: 3-consecutive-day byte-identical `sent` cycle exhibited 08-13/08-14/08-15]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
