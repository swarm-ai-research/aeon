# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 2 durability confirmed, merge path still unproven** — 13 open aeon-repo PRs (12 `app/github-actions` + #9 `app/freebuff-web`); queue depth from 4 bursts 08-07 (#10-13 · #14 · #15-17 · #18-20) + #21 suggest-edges 08-08T06:00Z. Second consecutive day of `app/github-actions` authorship confirms **creation** channel is stable, but **0 of 12 `app/github-actions` PRs merged in 27h+ since unblock** — no auto-merge policy configured, so queue piles up. Planner will escalate queue-depth to rank-1 if none merge by 2026-08-09. Prior 42-day meta-block cleared. Mechanism grep-verified: `GH_GLOBAL` PAT already wired in aeon.yml/chain-runner.yml/messages.yml (pre-dates unblock); zero `AEON_GH_PAT` references — mechanism is **most likely the Repo Settings → Actions → 'Allow Actions to create PRs' toggle** landing overnight 08-07, not new PAT provisioning. `elevate-repo-settings-toggle-active` remains dropped from streak-tracked planner slot. **swarm-ai-research/swarm write-permission gap DISTINCT from aeon-repo unblock** and unaffected — App-perm bump / PAT still needed there (26th confirming invocation of [[aeon-app-no-write-on-swarm-repo]]).
- **ISS-006 pocket-slot migration Day-8** — 08-08 heartbeat fired 08:51Z, ~51min late in 08:00Z pocket (marginally better than 08-07's ~1h08m). Planner 08-08 fired 07:07:51Z (~38min late, regressed vs 08-07's ~3min on-slot delivery); batch-health delivered clean 4/4 in a tight 07:03:40–51Z cluster (late-pocket per [[morning-pocket-splits-into-two-de-facto-clusters]]). Bimodal delivery pattern holds; ISS-006 fix pattern unchanged. See [[fleet-ops]].
- **ISS-001 residue day 49** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged 43rd steady-state day (passive advance, no 08-08 skill-health run yet). See [[fleet-ops]].
- **≥26 staged branches** queued behind [[github-actions-cannot-create-prs]] — blocker cleared 08-07 for creation, but merge path unproven (0 of 12 `app/github-actions` PRs merged 27h+). Merging a representative batch (e.g. #10 notegraph + one docs-pass) proves end-to-end.
- **notegraph 08-08 DETERMINISTIC NO-OP** — fingerprint advanced (`8b1ee3fa…`→`92772443…` from memory/docs churn since 08-07 reflect) but regenerated graph byte-identical to HEAD (247n/1854h/667s/0o/72 atomic held from 08-07 reflect). Restored the 4 generated files to HEAD state, updated `memory/state/notegraph.json` with new fingerprint + `notify_suppressed: true`, no PR staged, no notify — healthy silent-exit path per [[notegraph-extractor-generatedat-nondeterministic]]. Corpus edits that don't add new [[wikilinks]] or files should NOT trigger a graph rebuild PR (validated pattern).
- **pr-tracker 08-08 tuple `(1, 3, 1, 7)` — CLEAN 4-OF-4 predictor HIT (first ever)** — vs 08-07 `(2, 3, 2, 7)` vs predicted `(1, 3, 1, 7)`. Both cross-cutoff rolloffs materialized on schedule (cindy#1116 14:39Z 08-07 rolloff correctly deferred, worldmonitor#5518 rolled off 06:11Z today 4h before scan). Second consecutive multi-rolloff scan with correct scan-vs-cutoff-hour arithmetic. **Archive-hide day 3** — PostHog/code#4007 still search-omitted 58h+ post-archive → **eventual-consistency hypothesis falsified; treat as permanent-until-unarchive** per [[pr-tracker-search-drops-archived-repo-prs]]. Novel signals: k-skill#547 metadata bump with zero human engagement (bot rebase or label); first non-security bot PR `0xprogrammable/aeon-launch-models#1` got CHANGES_REQUESTED at 08-07T17:57Z (19.6h after file — first-ever maintainer review on a non-security bot PR). See [[pr-status]].
- **swarm PR queue steady 08-08 at 8 open** — no rsavitt SHA changes in ~30h since 08-07T02:21Z; no dependabot rotation since #550's 08-07T02:30Z. pr-review 36th invocation (first on 08-08) **100% of queue skipped** (6 bot + 2 dup-SHA) — first invocation on this repo with zero reviewable PRs (novel signal, not a class change). pr-triage same-SHA idle. Verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5). Confirming counter still 26 (no writes attempted). App-perm gap on swarm still open — DISTINCT from aeon-repo unblock.
- **compute-futures-eda 08-08 NO-OP** — deployer has not yet emitted 2026-08-08.csv to fleet-state; same-date guard fired cleanly (08-07 CSV already analyzed on 08-07). No commit, no notify. Next scheduled invocation picks up today's CSV once deployer sweep lands.
- **`.pending-disclosure/` queue grew 4 → 6** — 08-08 vuln-scanner on `yc-software/qm` added PVR draft (2 code findings) + deps draft (9 fixable GHSAs). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band OR queue converts to draft PRs once unblock mechanism confirms.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged (last milestone-tracker fire 08-03 Mon 12:00Z; next fire 2026-08-10 Mon). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker 6th silent-risk Mon slot is 2026-08-10 13:00Z** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Planner rank-1 `agi-tracker-enabled-false-via-pr` (streak-2, held from 08-07). Ship the PR today (Sat 08-08) to give operator two full days.
- **Never-dispatched 31st consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[github-app-cannot-fork-third-party-repos]] (08-08), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, persistence day 3 confirmed), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05), [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Prove end-to-end merge flow** — 13 open aeon-repo PRs, 0 merged in 27h+ since unblock. Actions: (a) merge a representative batch (e.g. #10 notegraph-orphan + one docs-pass) to prove the flow; (b) if no merges by 2026-08-09, planner escalates queue-depth to rank-1; (c) walk down the queue + ≥26 staged branches. Mechanism is likely Repo Settings toggle (GH_GLOBAL was pre-wired, no AEON_GH_PAT anywhere).
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-1 today (streak-2 `agi-tracker-enabled-false-via-pr`, deadline 2026-08-10 13:00Z is 2 days out). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Ship today (Sat 08-08) for two-day operator buffer.
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS`** — planner rank-2 today (streak-1). Hardcoded `{"aeonframework"}` but all 12 post-unblock aeon-repo PRs are authored by `app/github-actions`. Under strict allowlist, sweeper is a no-op post-unblock; harmless today (each tracked group has only 1 open entry) but breaks the moment two date-stamped `notegraph/*` or `suggest-edges/*` PRs coexist. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner rank-3 today (streak-3). Today's log shows 5 same-day short-circuits (issue-triage + github-monitor + repo-revive + code-health + implicit changelog) — persistent 5th same-day cycle since 08-04.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **21st-day carryover**. Scope: `ai-framework-watch` (Mon 08:30, 31d silent), `run-frequency-guard` (daily 23:00, 31d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model TWO migrated pockets (~06:11–06:22Z + ~09:00–09:58Z) and ONE persistent gap around planner's 06:30Z slot per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] should ease now that meta-block appears cleared. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **22 days past urgency threshold** as of 2026-08-08. 08-08 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**twelfth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-08), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]] (verified overriding dedup on 08-07), (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (**first CLEAN 4-of-4 HIT observed 08-08** on cindy+worldmonitor multi-rolloff scan — second consecutive scan with correct arithmetic); (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes; (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — **eventual-consistency hypothesis falsified on 08-08** (day 3 still hidden after 58h+), treat as permanent-until-unarchive not one-scan lag. **[Viable now — 45d overdue as of 2026-08-08]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
