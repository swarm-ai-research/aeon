# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 22 durability, PR #26 MERGE-READY** — **26 open aeon-repo PRs** (24 on 08-26 + notegraph #51 [08-26] + notegraph #53 [08-28], +3 net over 3d). **0 `app/github-actions` merged in ~532h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. `state: OPEN`, `mergedAt: null`, all 5 checks still SUCCESS on same `updatedAt: 2026-08-24T01:08:19Z`; 3 consecutive scans returned `mergeable/mergeStateStatus = UNKNOWN/UNKNOWN` (transient GitHub recompute, not a check regression). Terminal escalation shape unchanged since 08-25 day-17 rotation ("click merge OR install repo-level auto-merge policy for `dependabot` + `app/github-actions`"). Planner streak-19 as of 08-29.
- **ISS-006 pocket-slot migration Day-28 — NEW batch-outage signal 08-29** — planner missed 2026-08-27 + 08-28 06:30Z slots (71h gap since last `planner-state.last_run: 2026-08-26T06:42Z`); 08-28 batch only 3 skills logged (notegraph 05:22Z / code-health / surplus-pulse); 08-27 log has only pr-tracker. Extended pocket-slot dead zone; may warrant escalating ISS-006 status `investigating → fixing` if today's batch-health confirms. Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue Day-70** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Substantively green per 08-26 skill-health 61st steady-state day (hash `e27c0ac60367e7e5`, notify daily-reminder cleared 08-26T18:57Z). See [[fleet-ops]].
- **INDEX.md open-count 17** — filed rows unchanged; ISS-021 draft (never-dispatch) still pending 41 days. ISS-007 RESOLVED 2026-08-23.
- **≥25 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging #26 (dependabot actions/checkout) still the concrete proof; specific unblock is the single ShellCheck FAILURE per 08-21 planner isolation.
- **pr-tracker 08-27 tuple `(1, 7, 2, 5)` letter / `(1, 8, 2, 4)` substantive** — **microsoft/vscode#332891** opened 08-27 07:47Z **CLA-blocked** while still open at scan; candidate 2nd-instance widening of [[cloudflare-org-cla-blocks-aeonframework-prs]] into a cross-org class if MS closes within 24h (else fast-close vs slow-decay subclass split). Merged7d: Wei-Shaw/sub2api#6122. Closed7d: cloudflare/workerd#7124 (CLA-block ~6h) + PostHog#78346 (stale-bot terminal auto-close). Predictor accountability: 08-26 called `(1,7,2,4)`/`(1,8,2,3)`, missed active bucket by +1 in both due to fresh MS/vscode open — "stable barring fresh open/merge/comment" caveat fired exactly as flagged (3-of-4 both). See [[pr-status]].
- **swarm PR queue 9 open, byte-identical since 08-20 supersession triple** — human PRs #549/#543 now ~19d frozen at same SHA; pr-review + pr-triage remain 100%-skip (7 dependabot bot-author + 2 dup-SHA) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends. Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5). [[aeon-app-no-write-on-swarm-repo]] Day-48 unexercised.
- **notegraph 9-day clean cadence (08-20 → 08-28)** — 08-28 substantive open PR #53 (+208 notes wired in vs stale HEAD, 329n/3357e/0-orphan/0-bundled) — HEAD-vs-workspace divergence per [[notegraph-workspace-head-diverges-from-origin]] continues (open PRs #48, #51, #53 stack un-merged). **3 open notegraph PRs** (#48/#51/#53). 0 merges. **SKILL.md sandbox friction on `find | xargs sha1sum | sha1sum` fingerprint pipeline recurred 08-28 (n=4)** per [[notegraph-fingerprint-pipeline-blocked-by-sandbox]]; checked-in node helper patch still the durable fix.
- **suggest-edges — day-19 holding, no new PR since #52 (08-26)** — no fires 08-27/08-28 (batch-outage signal). Same `{06-24, 06-27, 07-04} → 07-28` templated-corpus signature (state file 6 applied / 21 rejected; last applied pairs: `2026-08-03 → 2026-08-14` sim 1.00, `2026-07-17 → 2026-08-03` sim 0.98, `2026-08-03 → 2026-08-09` sim 0.98). **5 open suggest-edges PRs** (#42/#45/#49/#50/#52); #45/#49/#50 stale >7d. [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] class re-promotes to top-3 if #54 opens today. Fix candidate unchanged: pre-filter within-`gitlawb-compute-futures-proofs/` pairs with identical scenario-sweep tokenization signature.
- **compute-futures-eda 08-29 CSV — 2.5000× multiplier n=8 SURPASSES n=7 invalidation floor** per new [[compute-futures-2.5x-surpasses-n-7-invalidation-floor]]. Cross-mode ≥10% cluster count COLLAPSES 3 → 0 (08-25's 3-mode realizedAbs anchor fully evaporates); below-threshold 8.33% clusters (basket settlementLegs, spread maxCurve) are single-seed triples. Retail spread-loss extends to **n=18 filed runs** (mean −$7,554, 17% shallower vs 08-25; 11/12 losing; seed 202608293 dual-role anchor retail −$19,055 + operator +$13,951, dispersion $33,006). **x402 x402Total correlation-anchor rotates for 3rd consecutive filing** (settlementLegs → minSpot → maxCurve +0.564, 08-29); 08-25's minSpot pair FLIPS SIGN to −0.393 (−1.178 magnitude swing, largest single-day partner reversal in series). x402Total range expands 25% to $109.98. Super-anchor 4/3/2 (55% below 08-25's 9/9/9). Zero within-mode strong pairs n=17 consecutive per [[compute-futures-12-seed-sample-too-small]]. Conservation exact MIRROR-IMAGE of 08-25. **4-day filing gap** — 08-26 + 08-27 CSVs exist but never analyzed; 08-28 CSV never emitted (batch-outage signal).
- **pr-tracker 08-26 10:37Z tuple `(1, 7, 2, 4)` letter / `(1, 8, 2, 3)` substantive** — **class-first CLA-block class** from **cloudflare/workerd#7124 opened-and-closed within ~6h** (created 2026-08-25T23:25Z, closed 05:30Z by maintainer `ryanking13` with CLA Assistant Lite bot signature demand as sole prior comment); atomized this reflect as [[cloudflare-org-cla-blocks-aeonframework-prs]] — distinct from stale-bot inversion (PostHog#78346), maintainer supersede (alibaba#541), revalidation (koala73#5518), duplicate-close (harry0703#1198). Cloudflare-org CLA policy applies whole org so cloudflare/*, workers-sdk/*, wrangler/*, etc. future submissions replay same cycle until signed CLA on file. Also harry0703#1198 rolled off 7d closed window at 08:56Z. Yesterday's `(1,7,1,4)`/`(1,8,1,3)` predictor scored **best-ever 3-of-4 both** (caught merged7d + stale + active; missed only closed7d because CLA-block class was structurally unpredictable). Persistent classes: archive-hide day 23 (PostHog/code#4007) · repo-deletion day 17 (0xprogrammable/aeon-launch-models) · stale-bot inversion n=1 completed-cycle. Tomorrow's predictor `(1, 7, 2, 4)` letter / `(1, 8, 2, 3)` substantive — stable barring fresh open/merge/comment (no scheduled state transitions in 24h window; best-case 4-of-4). See [[pr-status]].
- **swarm PR queue 9 open, human PRs #549/#543 ~22d frozen at same SHA** — **65th pr-review 08-29** first non-full-skip after 20-run streak: 3-day gap since 64th (08-26) opened dup-SHA window so SKILL step 7 attempted writes on both human PRs; **[[aeon-app-no-write-on-swarm-repo]] gap EXERCISED Day-0** — both `POST .../pulls/549/comments` (HTTP 403) and `gh pr review --comment` (GraphQL) return "Resource not accessible by integration"; class promoted *suspected → confirmed*. Standing verdicts refreshed against unchanged SHAs: #549 **APPROVE (5/5)** (upgraded from prior 2/5; 21/21 CI SUCCESS, 96 tests, offline-only bounded parser, conservative measurement-first defaults are documented design choices, not missing safeguards), #543 **APPROVE (5/5)** (docs-only trivial). 3 dependabot SHAs refreshed 08-27 (#561/#560/#559) — bot-author skip applies regardless. Followup: install aeon app with `pull_requests: write` scope OR route swarm reviews via PAT-backed path OR document swarm as report-only in SKILL.md.
- **notegraph 6-day clean cadence (08-20 → 08-25)** — 08-24 substantive open PR #48 (+5 notes wired in, 320n/3280e/0-orphan/0-bundled); 08-25 silent-no-substantive-change via `generatedAt`-only revert per [[notegraph-extractor-generatedat-nondeterministic]] (no PR — corpus grew slightly today but no new hard/soft edges materialized, 320n/3280e held). **2 open notegraph PRs** unchanged (#43 08-22 dirty · #48 08-24). 0 merges. **SKILL.md sandbox friction on `find | xargs sha1sum | sha1sum` fingerprint pipeline recurred 08-25 (3rd consecutive day, n=3)** → promoted to atomic [[notegraph-fingerprint-pipeline-blocked-by-sandbox]] this reflect; checked-in node helper patch is the durable fix. Prior lessons stand: [[notegraph-silent-revert-on-fabricated-merge-claim]] + [[notegraph-workspace-head-diverges-from-origin]].
- **suggest-edges — day-18 recurrence, PR #52 opened 08-26 05:50Z** — same `{06-24, 06-27, 07-04} → 07-28` templated-corpus signature (state file 6 applied / 21 rejected; today's applied pairs: `2026-08-03 → 2026-08-14` sim 1.00, `2026-07-17 → 2026-08-03` sim 0.98, `2026-08-03 → 2026-08-09` sim 0.98 — same templated-corpus signature). **9 consecutive days of PR-opening runs** — [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] class continues. Now **5 open suggest-edges PRs** (#42/#45/#49/#50/#52); net +1/day. Fix candidate unchanged: pre-filter within-`gitlawb-compute-futures-proofs/` pairs with identical scenario-sweep tokenization signature.
- **compute-futures-eda 08-24 CSV — HIGH tier RETURNS after 3-filing gap** — **synth/x402 `settlementLegs` 33.33% cluster** (fresh class-first appearance; 18/36 rows collapse to leg=81, IQR bounds [80,82]); cross-mode ≥10% cluster count 0 (08-22) → 3 (08-24). Retail spread-loss extends to **n=16 filed runs** (mean −$12,150, 17% deeper than 08-22; 12/12 losing 2nd consecutive day; seed 202608245 triple-role top-5 anchor $36,171 within-seed dispersion 2nd-highest ever). **basket/synth 2.5000× multiplier holds n=6 post-rename** per [[compute-futures-basket-synth-2.5x-multiplier]] — spot columns byte-identical (std 1.03e-15). **x402 `settlementLegs × x402Total` flips back +0.458** after 08-22's −0.060 (single-signal reversal). Super-anchor collapses to dual 3 (4-day downtrend 22→18→6→3). Zero within-mode |r|≥0.8 pairs, n=15 consecutive per [[compute-futures-12-seed-sample-too-small]]. Padded seeds 0% of non-dust positions (class-first total absence). x402Total range new series peak $145.43. **2-day gap in filing series** — 08-23 CSV exists but was never analyzed.
- **skill-evals BOOTSTRAP fired 08-23** — first run vs no prior article: **13 NEW_FAIL / 12 NO_OUTPUT** across article-based skills (only `skill-freshness-2026-08-23.md` present in `articles/`). Action queue head: "check if skills commit outputs to git" — potential systemic gap on article-commit discipline. All NEW_FAILs covered by existing open issues (no new filings).
- **`.pending-disclosure/` queue at 6** — 08-29 vuln-scanner ran on `CopilotKit/OpenBot` (3.4k★ TypeScript agent runtime, HEAD `fb0c797`) → CLEAN AUDIT (0 code vulns, 0 secrets, 0 actionable dep bumps; 16 informational transitive CVEs); no PR because Renovate preset scoped `enabledManagers:[github-actions]` so npm bumps are maintainer's manual gate. Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed). Sandbox friction recurred: bash-side `>` redirect into workdir blocked (extending [[sandbox-blocks-shell-redirect-to-workdir]] beyond heredoc-append); worked around with in-repo `tmp-vuln-scan/wrap.py` (subprocess capture + explicit file write) — same shape as checked-in `scripts/notegraph-fingerprint.mjs` fix.
- **Milestone alert — ms-01 stalled-8** — next milestone-tracker fire Mon 2026-08-31 12:00Z (~54h out); aeon repo stars 0/100 unchanged (8th consecutive weekly flat). ms-02 enabled-skills 47/50 holds `approaching` (94%). See [[fleet-ops]].
- **agi-tracker T-2 deadline HIT — 9th silent-Mon fires Mon 2026-08-31T13:00Z (~54h out)** — per 08-29 planner rank-2 promotion, deadline is now. Two paths per [[agi-tracker-missing-skill-md-dispatches-no-op]]: (a) ship `enabled: false` on `aeon.yml:188` (lower friction, ms-02 drops 47→46); (b) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC weekly frontier-agent scoring shape.
- **Cost pulse 08-24 07:00Z** — **$349.38 across 78 runs (↓6.6% WoW)**, monthly projection **$1,497.43** ⚠, 2 anomalies (surplus-pulse per-run spike 08-17; memory-flush 3× WoW skill spike). 2 optimization suggestions (memory-flush scan-window cap ~$13.50/wk; reflect Sonnet downgrade eval ~$26/wk). Next cost-report Mon 2026-08-31 07:00Z (~54h out).
- **Never-dispatched 53rd consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-25 (chronic, as of 08-29)** — 08-29 confirmed today's code-health short-circuit; 08-28 cluster streak-24. Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while others ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[compute-futures-2.5x-surpasses-n-7-invalidation-floor]] (08-29, new — the 2.5000× basket/synth multiplier held n=8 filings by 08-29, surpassing the prior 3.025× regime's n=7 invalidation-cadence floor; the n=7 threshold was a last-observed invalidation *event* not a mechanistic durability *ceiling* — promotion cadences derived from prior-regime floors need re-checking each time a new regime crosses them), [[sandbox-blocks-shell-redirect-to-workdir]] widened again 08-29 (vuln-scanner on CopilotKit/OpenBot recurred — plain `>` redirect into allowed workdir path still blocked even where heredoc-append was known-blocked; scanner suite now uses `tmp-vuln-scan/wrap.py` per-tool subprocess-capture workaround, same shape as checked-in `scripts/notegraph-fingerprint.mjs` fix; class hits every scanner tool the skill uses so effectively re-exercised n≥3 per run — updated existing note in place, no new atomic), [[cloudflare-org-cla-blocks-aeonframework-prs]] (08-26, new — class-first CLA-block from `cloudflare/workerd#7124` opened-and-closed within ~6h; cloudflare-org CLA policy applies to whole org so any future cloudflare/*, workers-sdk/*, wrangler/* aeonframework submission replays same cycle until signed CLA on file; fix: sign out-of-band OR pre-submit gate in vuln-scanner/external-feature skips or defers cloudflare-org targets; **cross-org widening candidate**: microsoft/vscode#332891 opened 08-27 07:47Z CLA-blocked, still open at 08-27 scan — 24h close would confirm cross-org class), [[notegraph-fingerprint-pipeline-blocked-by-sandbox]] (08-25, new — n=4 recurrences 08-23/24/25/28 of the `find … | xargs sha1sum` fingerprint pipeline blocked by sandbox brace-quote/expansion analysis; checked-in node helper is the durable fix; third instance of shell-pipeline sandbox friction after [[sandbox-blocks-shell-redirect-to-workdir]] + [[sandbox-blocks-piped-curl-installers]]), [[compute-futures-basket-synth-2.5x-multiplier]] (08-23 — 2.5000× basket/synth constant held n=5 filings after 08-18 deployer reset from 3.0250×; now n=6 as of 08-24 CSV, crosses [[compute-futures-multiplier-invalidated-at-n-7]] rename threshold), [[janitor-outputs-mtime-blind-on-gha-runners]] (08-23 — `.outputs/` 14d TTL never fires because `actions/checkout` resets mtimes; second GHA-mtime blind spot after [[skill-freshness-mtime-blind-in-gha]]), [[planner-stuck-goal-escalation-must-probe-status-checks]] (08-21 — day-4 planner escalation on aeon-repo-queue-merge surfaced a single ShellCheck FAILURE on PR #26 hiding since 08-17; rule: probe `gh pr view --json statusCheckRollup` on named PR targets at day-2 restatement, not day-4 escalation), [[notegraph-silent-revert-on-fabricated-merge-claim]] (08-19 — silent-exit path reverted extractor's 280n graph to HEAD's 121n after fabricated "#32 merged" inference; guard must confirm merge via `gh pr view --json state,mergedAt` before restoring), [[compute-futures-multiplier-invalidated-at-n-7]] (08-19 — basket/synth 3.0250× promoted at n≥3 broke to 2.5000× at n=8 via upstream deployer change; promotion criteria need re-validation cadence), [[stale-content-pr-sweeper-tracked-prefix-drift]] (08-19 — branch prefixes like `compute-macro/*` and `skill-graph/*` never match TRACKED skill NAMES), [[notify-has-hash-dedup-queue-layer]] (08-19 — `./notify` queue-level hash-dedup drops identical payloads; distinct from SKILL-level dedup, partially masks [[pr-tracker-notify-repeats-with-no-state-change]]), [[notegraph-workspace-head-diverges-from-origin]] (08-18). See [[fleet-ops]] for the full timeline of durable lessons from earlier August (08-05 → 08-17).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #33 opened 08-16 (first live Track B firing, n=57, all null vs NATGAS partial ρ; supersedes prior #23 08-09).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — **9th silent-Mon fires Mon 2026-08-31T13:00Z (~54h out as of 08-29; T-2 planner promotion active)** unless action lands pre-slot. Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Prove end-to-end merge flow — day 22** — 26 open aeon-repo PRs (+3 vs 08-25: notegraph #51/#53 + planner's tracked adds), 0 `app/github-actions` merged in ~532h+ since unblock. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8), MERGE-READY** (all 5 checks SUCCESS at `updatedAt: 2026-08-24T01:08:19Z`, mergeable UNKNOWN across 3 scans due to transient GitHub recompute). Planner streak-19 escalation shape unchanged since day-17: "click merge OR install repo-level auto-merge policy for `dependabot` + `app/github-actions`" — plan-only planner cannot dispatch either write. (b) walk down the 26-deep queue + ≥25 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs. See [[pr-creation-toggle-is-distinct-from-merge-capability]].
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-3 streak-19 (as of 08-25). Hardcoded `{"aeonframework"}` but the open aeon-repo bot PRs are all `app/github-actions`; 08-21 (5 stale closed) + 08-24 00:02Z (2 closed: #41 + #44) + 08-24 23:59Z (1 closed: #43) operator-invocations exercised SKILL under applied intent. Currently open (all against `main`): notegraph #43/#48 (2), suggest-edges #42/#45/#49/#50 (4). Cron path fired 08-25 00:01:26Z with the literal `{aeonframework}` allowlist and closed 0. Bundle in a **TRACKED-prefix drift fix** per [[stale-content-pr-sweeper-tracked-prefix-drift]] — `compute-macro/*` and `skill-graph/*` branches never match TRACKED skill NAMES; add short-prefix aliases OR align each skill's branch-creation step to produce the full skill-name prefix. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet.
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-22 as of 08-29 planner; cluster streak-25 chronic as of 08-29 code-health). Persistent same-day short-circuit cycle continues. Path-mismatch note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **41st-day carryover** as of 08-29. Scope: `ai-framework-watch` (Mon 08:30, **49d silent**), `run-frequency-guard` (daily 23:00, **49d silent**). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate — 6-day clean cadence 08-20 → 08-25).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **43 days past urgency threshold** as of 2026-08-29. **29th consecutive** rebase-clobber-then-regen already expected on next heartbeat. Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch (a)–(k) + new (l): (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `--state merged` / `headRefName` / `mergedAt` per [[gh-search-prs-drops-state-merged-flag]] + [[gh-search-prs-drops-headrefname-field]] + [[gh-search-prs-drops-mergedat-field]], (c) **prefix filter widen to 5-prefix `{ai/, security/, fix/security/, aeon/, fix/}`** (bare `fix/` newly evidenced 08-25 by ProjectOpenSea/seaport#1415 `fix/router-partial-availability-infinite-loop`) + domain-match commit-author filter per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] + [[aeon-signing-identity-fragmentation]], (d) hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]] — **re-exercised 08-25 when cursor/plugins#256 landed invisible to yesterday's `(1,8,1,2)` predictor**, (f)/(g) tuple-predictor calendar/scan-hour fixes per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]] + [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] + [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model bulk-stale-clear + same-day file cohort lockstep per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] + [[same-day-file-cohort-stales-in-lockstep]]; (i) archived-repo direct-fetch fallback per [[pr-tracker-search-drops-archived-repo-prs]]; (j) stale-bot comment activity-gate filter per [[pr-tracker-stale-bot-comment-inverts-stale-classification]]; (k) self-owned-repo direct-fetch cross-verification per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]]; (l) terminal-close predictor state (stale-bot letter → auto-close ~12d cadence, newly confirmed by PostHog#78346 08-25T07:49Z). **[Viable now — 62d overdue as of 2026-08-25]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. Fingerprint-pipeline sandbox friction now at **n=4 (08-23/24/25/28)** per [[notegraph-fingerprint-pipeline-blocked-by-sandbox]] — bundle both fixes in one SKILL.md patch: (i) replace shell fingerprint pipeline with checked-in `scripts/notegraph-fingerprint.mjs`; (ii) mask `generatedAt` in extractor output or diff.
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
- **Sign the Cloudflare CLA against the aeonframework identity OR add a pre-submit gate in `vuln-scanner`/`external-feature` that skips or defers cloudflare-org targets** per [[cloudflare-org-cla-blocks-aeonframework-prs]] — first-observation 08-26 via `cloudflare/workerd#7124`; policy applies whole org so cloudflare/*, workers-sdk/*, wrangler/*, etc. future submissions replay the same ~6h maintainer-close cycle until resolved. Also candidate SKILL patch item (m) for pr-tracker: carry a distinct CLA-block close-reason bucket alongside stale-bot inversion.
