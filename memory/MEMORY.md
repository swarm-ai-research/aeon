# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 Day-6 continuous PARTIAL — clean-close still open** — 07-28 08:00Z pocket dropped 4 skills (heartbeat, batch-health, skill-freshness, gitlawb-fleet-metrics) confirming Day-5 landed PARTIAL, not clean; close-clock advances to Day-6. Timeline: 07-19 Day-1 → 07-20 GAP → 07-21 Day-1 restart → 07-22–07-28 all PARTIAL → 07-29 next witness slot. Two live hypotheses: dead-zone migration (fix: per-slot cron replacement) vs. stuck 08:00Z sub-pocket (fix: batch grouping investigation). Today's 08:00Z outcome distinguishes. Per [[iss-006-pocket-recovery-is-noise]] partial ≠ clean; need every slot firing for a full day. See [[fleet-ops]].
- **ISS-001 residue day 39** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. See [[fleet-ops]].
- **≥22 staged branches** blocked behind [[github-actions-cannot-create-prs]]. Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. Active rank-1 `elevate-repo-settings-toggle-active` per planner's escalation-of-escalation, **streak-3 by run / streak-5 by calendar day** (Fri 07-25 → 07-26/07-27 silent-planner → Tue 07-28 streak-2 → Wed 07-29 streak-3). See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — pattern captured as [[planner-escalation-of-escalation-when-meta-blocker-holds]]: streak-3 concrete escalation (`enabled: false` on aeon.yml:188 for agi-tracker) did not close; both rank-1 and hedged holding-slot died at the same meta-block [[github-actions-cannot-create-prs]]. Planner elevates the operator toggle/PAT ask itself as active rank-1 rather than thrash on the concrete action. One operator move unblocks ≥22 staged branches + 6 stalled fleet fixes + agi-tracker escalation + the ISS-020 draft.
- **pr-tracker tuple `(2, 1, 3, 8)` vs prior `(2, 0, 3, 7)`** — buzz#2248 rolled stale on schedule (yesterday's same-day stale-clock prediction hit to the minute at 18:08:42Z, first same-day hit for this queue); **+2 fresh bot PRs** (WhiskeySockets/Baileys#2732 CVE dep-bump, NangoHQ/nango#6929 CVE dep-bump), both filed 07-28 with COMMENTED bot reviews at file time. No new merges, no new closures. worldmonitor#5477 **APPROVED day-7** — extends longest cold-approve on record. 31st consecutive day the inline OR-widening was required. Predicted 07-30 tuple `(2, 5, 3, 4)` if worldmonitor#5518 + worldmonitor#5477 + voicebox#958 + RuView#1409 all roll stale as expected. See [[pr-status]].
- **swarm PR queue byte-identical at 7 open (48h)** — 5 dependabot + rsavitt #543 (APPROVE 4/5, 8th consecutive skip) + rsavitt #536 (REQUEST_CHANGES 2/5, 9th consecutive skip); zero SHA moves in ~24h. pr-review 19th invocation: 0 reviewed, 7 skipped. **13th confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (log-only verdicts; PAT/App-permission unblock is only path to on-PR review posting).
- **compute-futures x402 correlation magnitude decays across consecutive filings** — settlementLegs × x402Total pair is 4th consecutive positive but decays +0.657 → +0.357 → +0.241 toward zero across 07-19 → 07-24 → 07-25 → 07-28 filings. Consistent with [[compute-futures-12-seed-sample-too-small]] at n=12 seeds: cross-filing decay is the diagnostic signal for spurious correlation regressing to zero. MEDIUM recurring settlementLegs outlier 16.67% is 4th fire in last 12 filings (on/off rotation continues per 12-seed jitter class). 10th consecutive filed run with zero |r| ≥ 0.8 strong pairs.
- **Milestone alert — ms-01 stalled-3** — aeon repo stars 0/100 unchanged 3 weeks running (milestone-tracker fired 07-27 Mon 12:00Z; no run today, next weekly Mon slot 2026-08-03). ms-02 enabled skills 47/50 approaching (3 shy of target). See [[fleet-ops]].
- **cost-report Mon 07-27 slot article landed** — `articles/cost-report-2026-07-27.md` produced ($244.79 / 40 runs, −2.2% WoW, monthly projection $1,049.11). One anomaly (stale-content-pr-sweeper 2.06× WoW spike, 9-day miss-streak catch-up). ISS-008 close-eligibility preserved (same-day match on Mon slot despite late dispatch).
- **agi-tracker Mon 07-27 13:00Z slot CONFIRMED SILENT — 4th consecutive weekly** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Next slot Mon 2026-08-03 is 5th silent-risk unless SKILL.md restored or `enabled: false` lands (folded under operator toggle rank-1).
- **Never-dispatched 21st consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence). `stale-content-pr-sweeper` 9-day miss streak REMAINS ENDED — no re-lapse observed after 07-25 + 07-27 tight-lag deliveries.
- **`.pending-disclosure/` queue**: 2 entries at last verified snapshot — oomol-lab/open-connector 2026-07-11 GCM-tag-length medium + citrolabs/ego-lite 2026-07-25 shell-injection medium via unquoted `github.head_ref` (PVR path, operator submits). Directory not present on this branch (state lives on unmerged vuln-scanner branches per [[github-actions-cannot-create-prs]]); next vuln-scanner run Sat 2026-08-01 16:00Z will provide fresh grounding. See [[fleet-ops]].
- **AMD × Anthropic 2 GW MI450 + $5B equity** (Jul 22, 2026) — largest non-NVIDIA AI compute deal in AI history; first 1 GW online H1 2027. See [[anthropic-amd-2gw-mi450]] via [[compute-pulse]].
- **Anthropic × TeraWulf $19B / 401 MW Kentucky lease** (Jul 6 announcement) — Anthropic's largest single-site compute commitment. See [[anthropic-terawulf-19b-401mw-kentucky-lease]] via [[compute-pulse]].

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*New lesson 2026-07-27:* [[memory-section-header-rename-breaks-goal-tracker]] — reflect must not rename MEMORY.md section headers without updating consuming skills.

*New lesson 2026-07-28:* [[maintainer-close-without-merge-triage-pattern]] — stale dep-bump security PRs often get closed no-merge as maintainer triage rather than left open indefinitely; pr-tracker's stale→closed_no_merge transition is a real bucket move.

*Pattern validation 2026-07-29:* Same-day stale-clock prediction on buzz#2248 hit to the minute (predicted 18:08Z, landed at 18:08:42Z) — first same-day hit for this queue, validates pr-tracker wall-clock rollover semantics per [[pr-tracker-notify-repeats-with-no-state-change]].

*Note: `memory/topics/compute-futures-macro-correlations.md` was authored by the compute-macro-correlate skill on branch `compute-macro/2026-07-19`; not yet on main due to [[github-actions-cannot-create-prs]]. Add to Topics list when the branch merges.*

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per planner's escalation-of-escalation (07-29 planner streak-3 by run / streak-5 by calendar day). Single highest-leverage action; unblocks ≥22 staged branches + 6 stalled fleet fixes + agi-tracker escalation + the ISS-020 draft in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-07-25 --title "graph: +2 notes wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Mon 2026-08-03 13:00Z is the 5th silent slot if unaddressed. Also file as ISS-021 candidate or fold into scope-widened ISS-020.
- **Draft ISS-020** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **11th-day carryover** (07-19 rank-3, 07-20 rank-3, 07-21–07-29 rank-2). Scope: `ai-framework-watch` (Mon 08:30, 21d silent), `run-frequency-guard` (daily 23:00, 21d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-021 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (novel P0 stuck-skill class 07-26 — self-cleared 07-26T08:55:12Z, ISS-022 candidate lower urgency).
- **Investigate messages.yml matcher — pocket migration hypothesis reinforced** — 07-27 08:00Z fleet-watchdog pocket RECOVERED; 05:30–07:00 pocket dropped instead. 07-28 partial reversal: 05:30–07:00 partially recovered but 08:00Z pocket dropped 4 skills. Same-day pocket migration is the strongest evidence yet that `*/5` dead zones rotate by tick-timing across the day rather than being slot-specific. Fold into ISS-020 scope draft.
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Close-clock now Day-6 continuous PARTIAL after 07-28 08:00Z dropped 4 skills. Per-slot cron fix still the right path, tick-timing story confirmed by same-day pocket migration. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], 13 days past urgency threshold as of 2026-07-29. 07-27 heartbeat regen wholesale from a 47+-days-stale 06-09 baseline (07-26 regen did NOT survive on main). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) per [[pr-tracker-email-filter-must-be-domain-match]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working — round-trip validated 07-28 SEND on state change after 4-day SKIP streak, 07-29 SEND on new stale + 2 fresh PRs), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]]. **[BLOCKED by Repo Settings toggle, 35d overdue as of 2026-07-29]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Today's log shows 3 same-day short-circuits (issue-triage + github-monitor + code-health).
- **Defer ISS-001 close** until ISS-006 is resolved.
