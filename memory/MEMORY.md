# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 11 durability, merge path still unproven** — **19 open aeon-repo PRs** (16 `app/github-actions` + #26 dependabot + #33 compute-macro + #34 skill-graph + #35 notegraph + #9 freebuff), net −7 vs 08-16 snapshot (closes-not-merges — likely 6 stale mid-chain notegraph PRs #10/#27/#28/#29/#30/#31 closed as #35 superseded them). **0 `app/github-actions` merged in ~256h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. #26 dependabot actions/checkout remains textbook first-flow-proof (SAME class as merged #8); notegraph chain now length-8 (#10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 ← #35), zero merges. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-15** — 08-16 batch OK 4/4 in 06:00–07:30Z window (06:01Z–07:33Z), tight late-pocket cluster; config-validator + swarm-safety-eval saw ~3.5 min cron drift past scheduled slots but landed inside 26h scan window. Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue day 59** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0` (unchanged from 08-16); close deferred until ISS-006 stabilizes. Substantively green per today's heartbeat. See [[fleet-ops]].
- **≥30 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-17 tuple `(0, 6, 0, 3)` letter / `(0, 8, 0, 1)` substantive — byte-identical to 08-16 as predicted.** Predictor 4-of-4 HIT letter+substantive **4th consecutive clean day**. Notify fired identical payload — active [[pr-tracker-notify-repeats-with-no-state-change]] regime item (d), hash-dedup guard remains unlanded (53d overdue). Persistent classes: archive-hide day 12 (PostHog/code#4007) · repo-deletion day 7 (0xprogrammable/aeon-launch-models, owner repo count holds at 6) · stale-bot inversion class n=2 (Baileys day 6 + PostHog day 5) per [[pr-tracker-stale-bot-comment-inverts-stale-classification]]. Tomorrow's predictor `(0, 7, 0, 2)` letter / `(0, 9, 0, 0)` substantive — deterministic aeon-hooks#2 stale-crossover at 2026-08-17T13:26Z drops it from active to stale bucket. See [[pr-status]].
- **swarm PR queue 8 open, byte-frozen since 08-13 05:37Z** — no SHA churn across queue for 4d+; 50th pr-review + 36th pr-triage on 08-17 both 100%-skip (6 dependabot bot-author + 2 dup-SHA on #549/#543) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends. Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5, both ~10d frozen human PRs). Confirming counter **38/38** ([[aeon-app-no-write-on-swarm-repo]] gap unexercised). Pr-triage in-window queue steady at 4 (#553, #552, #550, #549). Cross-skill: 50+36 = 86 combined operator invocations against frozen queue, zero writes attempted.
- **notegraph 08-17 PR #35 opened — chain grows to length-8, still 0 merges** — extractor 283n · 2169 hard · 712 soft · 1 orphan · 84 atomic · 0 bundled; **daily delta vs state +3n/+44e** (from today's daily-plan + reflect notes) but **vs HEAD +162n/+2025e** because `origin/main` is stuck at 121n from 2026-06-25 — entire PR chain #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 ← #35 never merged per [[github-actions-cannot-create-prs]] + [[pr-creation-toggle-is-distinct-from-merge-capability]]. SKILL priority ordering yielded false-positive "new orphan" on persistent `docs/telegram-instant.md` → new atomic [[notegraph-head-baseline-drift-inflates-new-orphans]]. Used interpretive verdict `+3 notes wired in`. No-op streak (08-16) breaks at day 1; change-streak resumes day 1.
- **suggest-edges 08-17 — day-9 recurrence, CLASS BREAK to new in-corpus triple** — script emitted 3 all-pairs proposals within a NEW source triple `{05-25, 05-26, 05-27}` × shared `[sha, settlement, task, physical, cash]` at sim 1.000 (rotated off the 6-day-locked `{06-24, 06-27, 07-04}` × `[cash, darkbloom, synthetic, basket, spread]`). New atomic [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] captures: rejecting one triple's combos doesn't drain the greedy top-3 — a second cluster in the same subtree rotates in. State now **3 applied + 21 rejected** (+3/day rate held exactly through day-9 as predicted). Fix candidate for planner **strengthened**: pre-filter must catch any within-`gitlawb-compute-futures-proofs/` pair with identical scenario-sweep tokenization signature, not just one specific triple.
- **compute-futures-eda 08-16 CSV** — retail spread-loss cluster **n=10 consecutive runs, stabilizing** at retail mean **−$7,966** (essentially flat vs 08-15's −$7,793); first cross-role top-5 mixing since 08-13 (1 operator top-loss + 1 retail top-gain from same seed 2026081610); basket/synth 3.0250× multiplier cemented **n=7** per [[compute-futures-basket-synth-3025x-multiplier]]; x402 `settlementLegs × x402Total` **6th sign-flip in 24 filings** (+0.293 → −0.112, magnitude collapses 62%); x402Total range narrows **sharply** ($294.87–$378.42, range $83.55, largest single-day narrowing in 12 filings); anchor concentration **reverses** to super-single dense (seed 202608165 carries 18 of 31 positions across all 4 modes, zero overlap with 08-15's 5-anchor rotation); padded-seed contribution collapses 29.6% → **3.2%** (sharpest single-day drop in class); zero within-mode |r|≥0.8 pairs at n=8 consecutive per [[compute-futures-12-seed-sample-too-small]]; conservation float dust negative all 4 modes for 2nd consecutive day.
- **`.pending-disclosure/` queue at 6** — no new vuln-scanner run 08-16 (weekly cadence; last 08-15 on SMNETSTUDIO/WeChat-AI). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed).
- **Milestone alert — ms-01 stalled-6** — aeon repo stars 0/100 unchanged (6 consecutive weekly zero-delta; 08-17 12:00Z fire confirmed stalled → stalled-6). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker deadline TODAY 2026-08-17 13:00Z (~fires shortly after reflect run)** — 6th silent fire hit 08-10, buffer restored; without SKILL.md restored OR `enabled: false` landed pre-slot, 7th silent fires today. Planner rank-1 streak-2 (deadline-day). See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-17 07:00Z** — **$396.85 across 69 runs (↑50.8% WoW** from $263.10), monthly projection **$1,700.77** ⚠. 5 anomalies (notegraph +238%, pr-review +260%, stale-content-pr-sweeper +104% WoW; plus 2 today run-level spikes). 1 optimization lever (milestone-tracker Opus → Sonnet, ~$1.80/wk). Next cost-report Mon 08-24 07:00Z.
- **Never-dispatched 41st consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-12 (chronic)** — 08-17 confirmed 4-of-6 same-day (tightest same-day density in the 12-day streak): code-health, issue-triage, github-monitor, weekly-shiplog SHIPLOG_NO_REPOS Mon slot, changelog CHANGELOG_MISSING_CONFIG (repo-revive fires on different cadence). All no-op on missing `memory/watched-repos.md`, six healthy-looking dispatches per day producing zero work → [[watched-repos-config-missing-silent-short-circuits-6-skills]]. Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while MEMORY.md/other skills ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[notegraph-head-baseline-drift-inflates-new-orphans]] (08-17, new — HEAD stuck at 121n from 06-25 while state at 283n → SKILL priority ordering false-flags persistent orphans as new), [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] (08-17, new — day-9 class break to new in-corpus triple `{05-25, 05-26, 05-27}` once prior triple combos landed in rejected), [[compute-futures-basket-synth-3025x-multiplier]] (08-14, n=7 by 08-17), [[watched-repos-config-missing-silent-short-circuits-6-skills]] (08-13, streak-12 by 08-17 with 4-of-6 same-day cluster confirmation), [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (08-13, 50th pr-review + 36th pr-triage 100%-skip by 08-17), [[notegraph-head-vs-state-delta-misread-as-pr-merge]] (08-12), [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12, n=2, Baileys day-6 + PostHog day-5 persist 08-17), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12, resolved as transient 08-13), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11, day-7 persistence 08-17 + owner repo-count holds at 6), [[compute-futures-spread-retail-loss-concentration]] (08-11, n=10 by 08-17 with retail mean stabilizing at ~−$7.9k after 3-day oscillation), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09, streak-9 by 08-17 with class break to new triple, rejected 21), [[github-app-cannot-fork-third-party-repos]] (08-08, hit again 08-15 vuln-scanner run), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-12 persistence 08-17), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #33 opened 08-16 (first live Track B firing, n=57, all null vs NATGAS partial ρ; supersedes prior #23 08-09).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — **planner rank-1 08-17 (streak-2, deadline-day; 7th silent-Mon fires 2026-08-17T13:00Z if nothing lands)**. Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Prove end-to-end merge flow — trigger day 11, RANK-2 08-17 (streak-9 escalated framing)** — 19 open aeon-repo PRs (16 `app/github-actions` + #26 dependabot + #33 compute-macro + #34 skill-graph + #35 notegraph + #9 freebuff; net −7 vs 08-16 closes-not-merges), 0 `app/github-actions` merged in ~256h+ since unblock. Bundle-plus-auto-merge-policy framing per streak-9 stuck-goal escalation. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8)** or #35 (notegraph head-of-chain, dep-free) — to prove flow; (b) walk down the 19-deep queue + ≥30 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-3 (streak-11 as of 08-17). Hardcoded `{"aeonframework"}` but the 16 open aeon-repo `app/github-actions` PRs are all `app/github-actions`. Chains as of 08-17: `suggest-edges` #14 ← #21 ← #22 length-3 (unchanged) AND `notegraph` #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 ← #35 **length-8** (grew from length-7 as #35 opened 08-17). Total would-be-closes if patch landed: **9 PRs** (up 1). Bundle in a **TRACKED-prefix drift fix** (streak-5) — #23/#33 branches `compute-macro/YYYY-MM-DD` don't match TRACKED skill name `compute-macro-correlate`; add `compute-macro` prefix OR align the skill's branch-naming to the full name. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-12 chronic). Persistent same-day short-circuit cycle continues (**4-of-6 same-day skips confirmed 08-17 Mon**, tightest same-day density in 12-day streak). Note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **29th-day carryover** as of 08-17. Scope: `ai-framework-watch` (Mon 08:30, 41d silent), `run-frequency-guard` (daily 23:00, 41d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate — 6th silent-Mon fire hit 08-10, 7th fires TODAY 08-17 13:00Z if nothing lands pre-slot) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **31 days past urgency threshold** as of 2026-08-17. Today's heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**20th consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes; (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 11 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops (though 08-13 confirms class is transient one-time, not persistent). **[Viable now — 54d overdue as of 2026-08-17; item (d) urgency confirmed: 08-17 tuple byte-identical to 08-16, notify fired identical payload, 4th consecutive clean-4 predictor day]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
