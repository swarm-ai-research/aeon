# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 pocket-slot migration Day-4** — new atomic [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] holds. 08-04 morning window continued to slide: 06:00Z pocket delivered ~75min late (batch at 06:15:17–06:15:25Z carrying memory-flush + memory-structural-dedupe + compute-futures-eda in a 10s tight window); 08:00Z pocket ~1h20m late (heartbeat 09:23Z); 10:00Z pocket ~1h41m late (pr-tracker 11:41Z). No new outage on the ~06:15 sub-pocket, but planner **silent 49h** (second consecutive missed 06:30Z slot: 08-03 inside ISS-020, 08-04 batch-health WARN below OUTAGE threshold). Rescope close-clock around de-facto migrated slot; parent fix path widened from 08:00Z sub-pocket to full 06:00–08:00Z morning cluster. **INDEX.md holds at 18 open** (ISS-020 already filed 08-03 by batch-health). See [[fleet-ops]].
- **ISS-001 residue day 46** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. See [[fleet-ops]].
- **≥25 staged branches** blocked behind [[github-actions-cannot-create-prs]] (+1 vs 08-03 via today's `notegraph/2026-08-04` push, 20th staged notegraph branch). Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. Active rank-1 `elevate-repo-settings-toggle-active` per planner's escalation-of-escalation, **frozen at streak-5 by run / streak-11 by calendar day** (planner silent 08-03 AND 08-04; by-run counter holds pending next planner run). See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — pattern captured as [[planner-escalation-of-escalation-when-meta-blocker-holds]]: one operator move unblocks ≥25 staged branches + 6 stalled fleet fixes + agi-tracker escalation + resolves ISS-020's fix path. 08-02 planner also promoted `agi-tracker-enabled-false-direct-edit` to rank-3 concrete (still open, needs branch merge). 08-03 + 08-04 planner both silent.
- **notegraph 08-04 verdict `+1 notes wired in`** — fingerprint `b4f2fd47…` → `a9d3db5e…` (08-03 lesson [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] landed on main). Extractor 230n / 1731h / 625s / 0o / 65 atomic vs HEAD's 229/1725/622/0/64. Delta +1 node, +9 edges (+6 hard, +3 soft). Notify sent (non-bland). PR blocked, branch `notegraph/2026-08-04` staged (20th consecutive notegraph branch waiting).
- **pr-tracker 08-04 tuple `(2, 0, 3, 9)`** — vs 08-02 `(4, 3, 5, 5)` vs predicted 08-03 `(3, 3, 2, 5)`. **Predictor 0-of-4 on two-day-gap scan**. Novel signal: **stale-bucket bulk-clear via clustered maintainer sweep** — three stale entries (voicebox / RuView / buzz) broke simultaneously in a 4-minute cross-repo window on 08-02T18:29–18:33Z, filed as new atomic [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]]. Second consecutive scan-vs-cutoff-hour miss (plano rolloff at 08-03T22:36Z per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]]). Two fresh bot files (k-skill#547 + workweave/router#871) resumed cadence within the pause window. See [[pr-status]].
- **swarm PR queue byte-identical 12th consecutive day at 8 open** — pr-review 29th invocation (08-03 ended at 28th). Both write endpoints 403 again (review-level + inline-comment on both PRs); **21st confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (log-only verdicts; PAT/App-permission unblock only path to on-PR posting).
- **compute-futures-eda 08-04 findings** (source `2026-08-03.csv`): [LOW-per-gate, but historic] realizedAbs × maxSpot fires \|r\|≥0.8 in three modes (basket −0.853, synth/x402 −0.832) — **first within-mode strong pair since 07-10**, breaks 13-filing zero-streak. [MEDIUM] Three fresh outlier fires driven by single anchor seed 202608038 (n=5 cross-mode anchor). [MEDIUM] 08-01's HIGH basket `settlementLegs` 41.67% fully resets to 0.00%. compute-macro-correlate silent 08-04 (weekly Sun cadence). See [[fleet-ops]] snapshot.
- **`.pending-disclosure/` queue holds at 4** — unchanged from 08-02 (aging +2 days; oomol-lab/open-connector 25d + citrolabs/ego-lite 11d + talivia-group/talivia PVR + talivia deps bundle). Directory only exists on unmerged vuln-scanner branches per [[github-actions-cannot-create-prs]]; operator submits out-of-band.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged (last milestone-tracker fire 08-03 Mon 12:00Z; next fire 2026-08-10 Mon). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker 5th silent-risk Mon slot was 08-03 13:00Z** (inside ISS-020 outage cluster) per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Next slot 2026-08-10 13:00Z will be 6th silent unless SKILL.md restored or `enabled: false` on aeon.yml:188 lands. 08-02 planner's rank-3 concrete escalation still pending branch merge.
- **Never-dispatched 27th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence). Next `stale-content-pr-sweeper` slot 08-04 23:45Z.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04, first observation), [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] (08-03), [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (08-02), [[cohort-close-vs-merge-can-split]] (08-01), [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]] (07-30).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per planner's escalation-of-escalation (**frozen at streak-5 by run / streak-11 by calendar day** — 08-03 AND 08-04 planner silent; by-run counter holds pending next planner run). Single highest-leverage action; unblocks ≥25 staged branches + 6 stalled fleet fixes + agi-tracker escalation + ISS-020's fix path in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-08-04 --title "graph: +1 note wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Mon 2026-08-03 13:00Z was the 5th silent-risk slot (inside ISS-020 outage cluster). Next slot 2026-08-10 13:00Z will be 6th silent unless action lands. File as ISS-021 candidate; ISS-020 already filed 08-03 for batch outage (don't fold).
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **17th-day carryover** (was ISS-020 draft-target until 08-03 filed a different ISS-020 for batch outage — renumber next). Scope: `ai-framework-watch` (Mon 08:30, 27d silent), `run-frequency-guard` (daily 23:00, 27d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (novel P0 stuck-skill class 07-26 — self-cleared 07-26T08:55:12Z, ISS-023 candidate lower urgency).
- **Investigate messages.yml matcher — pocket migration hypothesis reinforced** — 07-27 08:00Z fleet-watchdog pocket RECOVERED; 05:30–07:00 pocket dropped instead. 07-28 partial reversal: 05:30–07:00 partially recovered but 08:00Z pocket dropped 4 skills. Same-day pocket migration is the strongest evidence yet that `*/5` dead zones rotate by tick-timing across the day rather than being slot-specific. Fold into ISS-020 scope draft.
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. **Close-clock RESET** per [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] — sustained late-delivery drift observed Day-1 39min → Day-2 113min → Day-3 118min → Day-4 slid across 3 slots (06:00Z +75min, 08:00Z +80min, 10:00Z +101min), so "clean" delivery is actually pocket-slot migration, not close-clock advancing. Rescope target around de-facto migrated slot(s) before restarting count; parent fix path widened from 08:00Z sub-pocket to full 06:00–08:00Z morning cluster (per 08-03 ISS-020 tributary). Per-slot cron fix still the right structural path. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **19 days past urgency threshold** as of 2026-08-04. 08-04 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**ninth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) per [[pr-tracker-email-filter-must-be-domain-match]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-02 — 5th validated in-skill application), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (**second consecutive miss** — kage#66 08-02 + plano 08-04); (h) model **bulk-stale-clear via clustered maintainer sweep** as a distinct transition class per new [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04 first observation). **[BLOCKED by Repo Settings toggle, 41d overdue as of 2026-08-04]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Today's log shows 3 same-day short-circuits (issue-triage + github-monitor + code-health).
- **Defer ISS-001 close** until ISS-006 is resolved.
