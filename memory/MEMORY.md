# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 close-clock Day-1 still doesn't advance — 07-25 pocket PARTIAL again** — memory pocket landed clean and tight at 06:37–06:47Z (planner + cfe + notegraph + suggest-edges all ~10-15 min past cron, noticeably tighter than 07-23/07-24 ~47min-late envelope — possible sign the 06:30Z slot is stabilizing), but 08:00Z fleet-watchdog pocket silent again at 08:50Z manual heartbeat time (50min past cron). Per [[iss-006-pocket-recovery-is-noise]] partial ≠ clean → Day-1 does not advance. Timeline: 07-19 Day-1 → 07-20 GAP → 07-21 Day-1 restart → 07-22 PARTIAL → 07-23 PARTIAL → 07-24 PARTIAL → 07-25 PARTIAL. Earliest close still Sun 2026-07-26 Day-3 assuming a clean 07-26. See [[fleet-ops]].
- **Fleet-watchdog dispatch-drop pattern Day-6 — 3 consecutive re-misses** — 07-20/21/22 silent → 07-22 ~1h-late catch → 07-23 re-miss → 07-24 re-miss → 07-25 re-miss. batch-health / heartbeat / skill-freshness / gitlawb-fleet-metrics all `last_dispatch: 2026-07-24T09:43:05Z`. Three consecutive re-misses after the single 07-22 auto-recovery tick = per-slot cron fix path is definitively correct, not a two-day blip. Direct confirmation of [[iss-006-pocket-recovery-is-noise]]. See [[fleet-ops]].
- **ISS-001 residue day 35** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. See [[fleet-ops]].
- **≥18 staged branches** blocked behind [[github-actions-cannot-create-prs]] — plus today's `notegraph/2026-07-25` (commit `2786e18`, 4 files +550/-318, PR create BLOCKED as 15th confirmed instance). Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. **Elevated from streak-4 hold to active rank-1 `elevate-repo-settings-toggle-active` streak-1** per today's planner escalation-of-escalation. See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — 07-24's streak-3 concrete escalation (`enabled: false` on aeon.yml:188 for agi-tracker) did not close either; both rank-1 and hedged holding-slot `verify-repo-settings-toggle-vs-pat` died at the same meta-block [[github-actions-cannot-create-prs]]. Today's rank-1 lifts the operator toggle/PAT ask out of holding to active `elevate-repo-settings-toggle-active` — one operator move unblocks ≥18 staged branches + 6 stalled fleet fixes + today's agi-tracker escalation + the ISS-020 draft in one motion. agi-tracker one-liner folded under this rank-1.
- **PR queue (tracked-author) `(0,3,0,8)` CHANGED from 07-24 `(0,1,0,9)`** — one net-new file: `katanemo/plano#1001` (2026-07-24T15:27Z, patches serde_with/tokio-postgres/turbo/undici/next for disclosed CVEs, `security/` prefix, **NEW fifth signing identity `security@aeonframework.github`** per [[aeon-fifth-signing-identity-security-aeonframework-github]] — unusual `.github` TLD, first PR under this address, first PR into `katanemo/*` org). Both 07-24-predicted stale rolls landed within ±0.5d: openinterpreter#1810 + InsForge#1742. 13 nodes; local hash `0d4e2c374767939b` differs from 07-24 `5d42d555feea2257`. Notify SENT (three triggers align: hash-diff, fresh-bot-PR <24h, 3-stale content-trigger clause). Filter widened to FIVE identities × FOUR branch prefixes. **Four-consecutive-day SEND streak** — first on record. worldmonitor#5477 sits day-3 on APPROVED review (cold-approve pattern emerging); watch for MERGED tomorrow. See [[pr-status]].
- **swarm PR queue steady at 6 open, zero new since 07-24** — pr-review 12th invocation: 0 reviewed, 6 skipped (4 dependabot + swarm#543 dup-SHA at HEAD `70b20e04` unchanged since 07-24 APPROVE 4/5 + swarm#536 dup-SHA at HEAD `76e6200c` unchanged since 07-23 REQUEST_CHANGES). pr-triage independently re-ACCEPTED #543 and re-DEFER on #536 — state file `memory/triaged-prs.json` advanced. **12th confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] — pr-triage 403; pr-review had no write attempt.
- **Milestone alert — ms-01 stalled-2 unchanged** — aeon repo stars 0/100 unchanged 2 weeks running. ms-02 enabled skills 47/50 approaching. Next milestone-tracker Mon 12:00 slot fires 2026-07-27. See [[fleet-ops]].
- **cost-report ISS-008 close-eligible** — Mon 07-20 07:00 slot produced `articles/cost-report-2026-07-20.md` ($250.23 / 44 runs weekly, +20.3% WoW). Standing until skill-evals next scans and confirms same-day match.
- **agi-tracker HEALTHY-but-empty CONFIRMED** — 07-20 cron-state shows `last_success 2026-07-20T13:04:15Z` yet no `articles/agi-tracker-*` file exists per [[agi-tracker-missing-skill-md-dispatches-no-op]]. Next Mon 13:00 UTC slot 2026-07-27 will be 4th weekly silent unless SKILL.md restored or `enabled: false` lands (now folded under today's operator toggle rank-1).
- **Never-dispatched 17th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]]. `stale-content-pr-sweeper` **9-day miss streak CONFIRMED ENDED** — 07-24 23:45Z slot late at 07-25 00:59:01Z (~74min lag), then 07-25 23:45Z slot on-time at 23:57:23Z (~12min lag; two consecutive on-slot deliveries). `last_dispatch` advanced from `2026-07-22T00:07:44Z` → `2026-07-25T23:57:23Z`.
- **`.pending-disclosure/` queue**: 2 entries — oomol-lab/open-connector 2026-07-11 GCM-tag-length medium (14 days queued) + citrolabs/ego-lite 2026-07-25 shell-injection medium via unquoted `github.head_ref` in `.github/workflows/main-pr-source.yml` (PVR path, 0 days queued — operator submits). See [[fleet-ops]].
- **AMD × Anthropic 2 GW MI450 + $5B equity** (Jul 22, 2026) — largest non-NVIDIA AI compute deal in AI history; first 1 GW online H1 2027. See [[anthropic-amd-2gw-mi450]] via [[compute-pulse]].
- **Anthropic × TeraWulf $19B / 401 MW Kentucky lease** (Jul 6 announcement) — Anthropic's largest single-site compute commitment. See [[anthropic-terawulf-19b-401mw-kentucky-lease]] via [[compute-pulse]].

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author

*Note: `memory/topics/compute-futures-macro-correlations.md` was authored by the compute-macro-correlate skill on branch `compute-macro/2026-07-19`; not yet on main due to [[github-actions-cannot-create-prs]]. Add to Topics list when the branch merges.*

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per today's planner escalation-of-escalation (elevated from streak-4 hold to `elevate-repo-settings-toggle-active` streak-1). Single highest-leverage action; unblocks ≥18 staged branches (+ today's `notegraph/2026-07-25`) + 6 stalled fleet fixes + today's agi-tracker escalation + the ISS-020 draft in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-07-25 --title "graph: +2 notes wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md (no draft in flight; one-line + reversible + immediately reclaims the weekly slot). Fully Aeon-local. Alt path if reversed: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Mon 07-27 13:00Z is the 4th silent slot if unaddressed. Also file as a new structured issue (ISS-021 candidate) or fold into scope-widened ISS-020.
- **Draft ISS-020** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **7th-day carryover** (07-19 rank-3, 07-20 rank-3, 07-21 rank-2, 07-22 rank-2, 07-23 rank-2, 07-24 rank-2, 07-25 rank-2). Scope: `ai-framework-watch` (Mon 08:30, 16d silent), `run-frequency-guard` (daily 23:00, 16d silent), `stale-content-pr-sweeper` (23:45, 9-day miss streak may have ended — verify tonight). Sibling candidate: [[agi-tracker-missing-skill-md-dispatches-no-op]] as a distinct HEALTHY-but-empty class.
- **Investigate planner + 08:00Z pocket delivery reliability** — 07-22 planner missed 06:30Z entirely; 07-23/07-24 delivered late (~47min lag); 07-25 tighter (~10min lag — possible 06:30Z stabilization). 08:00Z fleet-watchdog pocket missed 07-20/21/22 → late-recovery 07-22 09:08Z → 07-23/07-24/07-25 re-miss (Day-6, three consecutive re-misses, definitively broken). Late-dispatch vs lost-dispatch = different fix. Check messages.yml matcher for both slots; shares dispatch-drop root cause with [[gha-messages-yml-cron-underdelivery]]. Fold into ISS-020 scope draft.
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Close-clock Day-1 restart 07-21 holds; 07-22 + 07-23 + 07-24 + 07-25 all PARTIAL, earliest close now Sun 2026-07-26 Day-3. Three consecutive 08:00Z re-misses after 07-22 late-recovery = per-slot fix path definitively confirmed. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], 9 days past urgency threshold (07-16 → 07-25). 07-25 heartbeat DID regen (08:50Z manual, wholesale rewrite from 46-days-stale 06-09 baseline — 07-24 regen did NOT survive on main, **9th consecutive day past urgency** confirming the pattern). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) covering all FIVE known identities per [[aeon-bot-uses-multiple-signing-identities]] + [[pr-tracker-branch-prefix-misses-bot-identity]] + [[aeon-third-signing-identity-proton-me]] + [[aeon-fourth-signing-identity-security-aeonframework-dev]] + [[aeon-fifth-signing-identity-security-aeonframework-github]] + [[pr-tracker-branch-prefix-aeon-slash]], (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]]. **[BLOCKED by Repo Settings toggle, 31d overdue as of 2026-07-25; scope grew 07-25 with fifth identity `security@aeonframework.github`]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it.
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Weekly-shiplog next Mon 09:00Z slot 2026-07-27 will confirm 3rd Mon short-circuit.
- **Defer ISS-001 close** until ISS-006 is resolved.
