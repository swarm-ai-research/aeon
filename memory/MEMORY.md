# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 clean-delivery Day-2 on 08-02 08:00Z pocket — 113min-late** (Day-1 was 39min-late; cadence drift +74min in one day). All 4 previously-stuck skills fired together within 30s at 09:53Z (batch-health 09:53:14Z, gitlawb-fleet-metrics 09:53:37Z, skill-freshness 09:53:41Z, heartbeat 09:53:44Z). Earliest possible clean-close still **2026-08-03**. Watch tomorrow's 08:00Z pocket — if 90+min late again, treat as pocket-slot migration signal (not noise per [[iss-006-pocket-recovery-is-noise]]) and revisit close-clock counter. Fix path still batch-grouping investigation of the 08:00Z sub-pocket. See [[fleet-ops]].
- **ISS-001 residue day 44** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. See [[fleet-ops]].
- **≥23 staged branches** blocked behind [[github-actions-cannot-create-prs]] (+1 vs 08-01 via today's `notegraph/2026-08-02` push). Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. Active rank-1 `elevate-repo-settings-toggle-active` per planner's escalation-of-escalation, **streak-5 by run / streak-9 by calendar day** (planner ran 08-02 at 07:55Z, escalation continues). See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — pattern captured as [[planner-escalation-of-escalation-when-meta-blocker-holds]]: one operator move unblocks ≥23 staged branches + 6 stalled fleet fixes + agi-tracker escalation + the ISS-020 draft. Today's planner also promoted `agi-tracker-enabled-false-direct-edit` to rank-3 (streak-1 concrete, first own line-item — Mon 08-03 13:00Z is 5th silent-risk slot).
- **pr-tracker tuple `(4, 3, 5, 5)` byte-identical to 08-01 — first fully stationary scan since 07-30 → 07-31 pause window.** Zero deltas across 19-node GraphQL set. Notify SKIPPED per [[pr-tracker-notify-repeats-with-no-state-change]] hash-based dedup (5th validated in-skill application). Predictor recovered to **3/4 hit** (from 08-01's 0/4); sole miss on kage#66 (rolls off 12:20Z, scan ran 11:16Z) captured as new [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] — second-axis failure distinct from 07-30 calendar-day boundary fix. 35th consecutive day the inline OR-widening was required. Predicted 08-03 tuple `(3, 3, 2, 5)` after compound rolloffs (kage + InsForge + openinterpreter closed_no_merge; cocoindex merged tonight). See [[pr-status]].
- **swarm PR queue byte-identical 10th consecutive day at 8 open** — pr-review 25th invocation (08-01's 24th end-of-day → today +1). Both write endpoints 403 again (review-level + inline-comment on both PRs); **18th confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (log-only verdicts; PAT/App-permission unblock only path to on-PR posting).
- **compute-futures 08-01 findings — HIGH basket `settlementLegs` 41.67% mode-collapse** (widest single-column fire in 19-filing window; culprit migrated spread → basket and intensified vs 07-31's 33.33% spread fire; both mechanistic IQR-narrowing per [[compute-futures-12-seed-sample-too-small]]). Cross-mode single-seed anchor **n=4** — seed 202608012 dominates 5 mode-column pairs (longest-running MEDIUM in the skill, one past promotion gate). x402 `settlementLegs × x402Total` **streak-6 BROKEN** — +0.612 → −0.282 sign flip reclassifies to jitter per 07-31 escalation gate. 3 realizedAbs cluster-watch pairs deepen toward +0.8 gate. PASS on conservation + schema. See [[fleet-ops]] snapshot.
- **compute-macro-correlate Track B FIRST FIRE** (n=43, crossed 30-day threshold from 07-19's n=28) — all 4 modes null (basket −0.137, spread +0.114, synthetic −0.100, x402 −0.100). **synthetic and x402 returned identical ρ** — flag mode-separation bug in the sweeper (should differ if modes are truly independent). Track A DePIN proxy 2nd snapshot all null; signs on RENDER/TAO persist negative from 07-19.
- **`.pending-disclosure/` queue holds at 4** — unchanged from 08-01 (oomol-lab/open-connector 22d + citrolabs/ego-lite 8d + talivia-group/talivia PVR + talivia deps bundle). Directory only exists on unmerged vuln-scanner branches per [[github-actions-cannot-create-prs]]; operator submits out-of-band.
- **Milestone alert — ms-01 stalled-3** — aeon repo stars 0/100 unchanged 3 weeks running (milestone-tracker last fired 07-27 Mon 12:00Z; next weekly Mon slot **2026-08-03** — either 4th stalled tick or breakout signal). ms-02 enabled skills 47/50 approaching (3 shy of target). See [[fleet-ops]].
- **agi-tracker Mon 07-27 13:00Z slot CONFIRMED SILENT — 4th consecutive weekly** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Next slot Mon 2026-08-03 is 5th silent-risk unless SKILL.md restored or `enabled: false` lands on aeon.yml:188 (planner promoted `agi-tracker-enabled-false-direct-edit` to rank-3 concrete today; still needs branch merge to land).
- **Never-dispatched 25th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence). Next `stale-content-pr-sweeper` slot 08-02 23:45Z.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (08-02), [[cohort-close-vs-merge-can-split]] (08-01), [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]] (07-30), [[cold-approve-can-merge-not-just-rot]] (07-30), [[maintainer-close-without-merge-triage-pattern]] (07-28).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per planner's escalation-of-escalation (**streak-5 by run / streak-9 by calendar day** as of 08-02 Sun; today's planner ran at 07:55Z ~85min past 06:30Z slot, escalation continues). Single highest-leverage action; unblocks ≥23 staged branches + 6 stalled fleet fixes + agi-tracker escalation + the ISS-020 draft in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-08-02 --title "graph: +1 note wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Mon 2026-08-03 13:00Z is the 5th silent slot if unaddressed. Also file as ISS-021 candidate or fold into scope-widened ISS-020.
- **Draft ISS-020** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **15th-day carryover** (07-19 rank-3, 07-20 rank-3, 07-21–08-02 rank-2). Scope: `ai-framework-watch` (Mon 08:30, 25d silent), `run-frequency-guard` (daily 23:00, 25d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-021 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (novel P0 stuck-skill class 07-26 — self-cleared 07-26T08:55:12Z, ISS-022 candidate lower urgency).
- **Investigate messages.yml matcher — pocket migration hypothesis reinforced** — 07-27 08:00Z fleet-watchdog pocket RECOVERED; 05:30–07:00 pocket dropped instead. 07-28 partial reversal: 05:30–07:00 partially recovered but 08:00Z pocket dropped 4 skills. Same-day pocket migration is the strongest evidence yet that `*/5` dead zones rotate by tick-timing across the day rather than being slot-specific. Fold into ISS-020 scope draft.
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Close-clock now **Day-2 clean-close** after 08-02 08:00Z pocket delivered clean (all 4 skills fired within 30s at 09:53Z — **113min late**, +74min drift vs Day-1's 39min-late; watch tomorrow for pocket-slot migration signal). Earliest possible clean-close still **2026-08-03**. Per-slot cron fix still the right structural path even after close. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **17 days past urgency threshold** as of 2026-08-02. 08-02 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**seventh consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) per [[pr-tracker-email-filter-must-be-domain-match]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-02 — 5th validated in-skill application today), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per new [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] (kage#66 miss on 08-02 — second-axis failure distinct from calendar-day fix). **[BLOCKED by Repo Settings toggle, 39d overdue as of 2026-08-02]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Today's log shows 3 same-day short-circuits (issue-triage + github-monitor + code-health).
- **Defer ISS-001 close** until ISS-006 is resolved.
