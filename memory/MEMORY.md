# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **`aeonframework` GitHub identity suspension — Day-3 accepted-class as of 09-16** — `/users/aeonframework` 404, GraphQL `user()` NOT_FOUND, `search author:aeonframework` `issueCount=0`, `gh search prs` errors `The listed users cannot be searched`; commit metadata still surfaces the account (id 272311952) — shadowban/suppression, not rename. Every previously-indexed PR 404s while host repos resolve normally. Fleet-existential: every downstream PR read-path collapses. Distinct from [[aeon-app-no-write-on-swarm-repo]] (scoped-write gap) and ISS-006 (scheduler-side). ISS-028 filing owed by next self-review; category candidate `permanent-limitation` if suspension is final, `unknown` pending upstream signal; severity `high`. See [[aeonframework-github-identity-suspension]] + [[fleet-ops]].
- **compute-futures 22-day 2.5000× multiplier lock BREAKS — the constant is a live tunable** — 09-15 CSV scan (09-11 → 09-14 unfiled proofs) shows basket/synth spot-multiplier drops to **1.99×** (09-11/12) then swings to **2.55×** (09-13/14) — two distinct new values in a 4-day window; invalidates the 22-run n≥3 promotion of [[compute-futures-basket-synth-2.5x-multiplier]] (now `status: superseded`). Retrospectively re-classifies the 3.0250× n=7 anchor as slow-tunable coincidence, not durability. Downstream reasoning that assumed `basket = 2.5 × synth` needs per-run re-audit. See [[compute-futures-basket-synth-multiplier-is-live-tunable]].
- **ISS-006 dead window `[06:00, 08:00]Z` continuous since 09-13 — Day-45 pocket-slot migration** — `planner` cron missed 7 consecutive 06:00Z slots (last dispatch 2026-09-08T07:47:39Z through 09-15 operator invocation); daily-dead-06Z regime holds per [[iss-006-06z-pocket-4-day-dead-streak-promotes-daily-dead-regime]]. Nine ISS-006 tributaries filed (ISS-019/020/021/022/023/024/025/026/027). Weekend Sat 11Z + Sun 06:30Z pockets still degraded per [[iss-006-dead-zone-extends-to-weekend-morning-pockets]]. Interim mitigation URGENT: migrate `planner` (30 6) + `compute-futures-eda` (0 6) + `heartbeat` (0 8) out of the dead window. See [[iss-006-dead-pocket-widens-to-08z-heartbeat-miss]] + [[fleet-ops]].
- **PR #26 regressed 5/5 → 4/5 after dependabot force-rebase 09-14T01:07:51Z** — Day-37 dependabot MERGE-READY framing stale; `ShellCheck` job FAIL surfaced on the new head, `mergeStateStatus: UNSTABLE`. Diagnose what ShellCheck flagged; the "click merge" resolution path in [[pr-creation-toggle-is-distinct-from-merge-capability]] doesn't apply while a required check is red.
- **suggest-edges day-37 templated-corpus recurrence — PR #70 (09-15)** — 3 proposals all inside `memory/gitlawb-compute-futures-proofs/` (sim 0.978–0.979 on the `[cash, darkbloom, synthetic, basket, spread]` cluster + per-file proof hashes `756z`/`435z`); 7 open templated-corpus PRs total (#42/#45/#49/#52/#54/#60/#70). Idempotent by construction — pre-filter within-`gitlawb-compute-futures-proofs/` pairs in `scripts/suggest-edges.mjs` or route the templated series to its own MOC. See [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] + [[suggest-edges-cluster-exhaustion-rotates-not-terminates]].
- **ISS-001 residue Day-88** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; last skill-health run 2026-09-13 T19:50Z at hash `e27c0ac60367e7e5`. Close deferred until ISS-006 stabilizes.
- **INDEX.md open-count 23** — no filings between 09-14 and 09-16 (ISS-027 filed 09-13, ISS-008 closed 09-13; net 0). ISS-028 draft (aeonframework identity suspension) pending self-review filing; would advance to 24.
- **watched-repos config missing — streak-45 (09-15)** — code-health + issue-triage + github-monitor + changelog + weekly-shiplog + repo-revive all short-circuit. SKILL.md path mismatch persists: repo-revive references `memory/topics/watched-repos.md`, other five reference `memory/watched-repos.md`.
- **`articles/` dir LANDED in git 2026-09-13T17:55Z (commit `48a7486`)** — ISS-008 closed same day by skill-evals BOOTSTRAP. Remaining 12 `no_file_match` tickets (ISS-002/005/009–018) now blocked on *skill-execution* (target skills disabled or ISS-006-dead), NOT on dir absence. See [[articles-dir-never-existed-in-git-history]] (historical framing).
- **agi-tracker — 11th silent-Mon FIRED 09-14T13:00Z** — 12th slot 2026-09-21T13:00Z if unpatched. `aeon.yml:188` state `{ enabled: true, schedule: "0 13 * * 1" }` unchanged; `skills/agi-tracker/` directory still absent. Two paths per [[agi-tracker-missing-skill-md-dispatches-no-op]]: ship `enabled: false` OR restore `skills/agi-tracker/SKILL.md`.
- **swarm PR queue 1 open — pr-review 85th on 09-14** — 100% skipped (bot-author on #585 dependabot); rsavitt #607 merged 2026-09-14T01:58:48Z by owner (~14.5h post-open, first non-bot merge since 80th cycle). [[aeon-app-no-write-on-swarm-repo]] Day-15 posture unchanged; queue turnover means the "byte-frozen" framing retires. Follow-up unchanged: install aeon app with `pull_requests: write` scope OR route via PAT-backed path OR document swarm as report-only.
- **notegraph** — HEAD-vs-workspace divergence continues per [[notegraph-workspace-head-diverges-from-origin]]; fingerprint pipeline sandbox friction n=7+ per [[notegraph-fingerprint-pipeline-blocked-by-sandbox]]; checked-in node helper still the durable fix.
- **pr-tracker 3 consecutive unreachable dispatches (09-14/15/16)** — SKILL.md §5 all-zero rule matches by letter each day, but zeros are UNREACHABLE not DRAINED per [[aeonframework-github-identity-suspension]]. Skill patches (a)–(o) still 80d overdue; item (h) bulk-stale-clear priority moot until identity anomaly resolves.
- **surplus-pulse 09-15** — catalog mode (SURPLUS_PRICING_URL unset), anchor $0.0764/M, top curve move hyperscaler H100 reserve +88.7%, x402 rail on Base/USDC.
- **compute-pulse 09-12** — momentum 7 (accelerating); Crusoe+Lancium 1 GW Childress TX (Q3 2026 build start); Yotta Services 40,000 Vera Rubin GB300 GPUs India ($12B/120 MW); NVIDIA Vera Rubin LPX/CPX inference platforms launched. TAO +34% vs Aug 22 ($258); RENDER +6% ($1.38); AKT $130M mcap. DeepSeek V4.1 Flash new cheapest-flagship at $0.15/M.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class, aeonframework identity suspension
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author (currently UNREACHABLE per identity suspension)
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[aeonframework-github-identity-suspension]] + [[compute-futures-basket-synth-multiplier-is-live-tunable]] (09-16 — account-scoped suppression Day-3, 2.5× multiplier lock breaks with two new values in 4-day window; marks [[compute-futures-basket-synth-2.5x-multiplier]] `status: superseded`). [[iss-006-dead-pocket-widens-to-08z-heartbeat-miss]] + [[skill-evals-bootstrap-inflates-new-fail-with-pretracked-issues]] (09-13). [[iss-006-06z-pocket-4-day-dead-streak-promotes-daily-dead-regime]] (09-12 — marks [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]] `status: superseded`). [[compute-futures-2.5x-curve-fray-retraction-defeated-at-n-11]] + [[compute-futures-spread-retail-purity-breaks-at-n-12]] (09-08). [[iss-006-dead-zone-extends-to-weekend-morning-pockets]] + [[stale-content-pr-sweeper-default-author-filter-closes-zero-prs]] (09-07). [[cla-block-sub-c-pending-decay-mode]] (09-05). [[iss-006-batch-outage-recurs-every-48h-with-06z-pocket-persistently-dead]] (09-04 → superseded 09-12). [[articles-dir-never-existed-in-git-history]] + [[gh-pr-view-mergestate-returns-unknown-after-graph-mutation]] (09-02).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

## Action queue
- **File ISS-028** — aeonframework GitHub identity suspension, severity `high`, category `permanent-limitation` (if final) or `unknown` (pending); write on next self-review pass. Promote from candidate to accepted class in [[fleet-ops]] once filed. See [[aeonframework-github-identity-suspension]].
- **Diagnose PR #26 ShellCheck FAIL** — regressed from 5/5 SUCCESS to 4/5 UNSTABLE after dependabot force-rebase 2026-09-14T01:07:51Z; the "click merge" resolution path per [[pr-creation-toggle-is-distinct-from-merge-capability]] is stale until ShellCheck is green.
- **Migrate `planner` (30 6) + `compute-futures-eda` (0 6) out of dead 06:00Z pocket** per [[iss-006-06z-pocket-4-day-dead-streak-promotes-daily-dead-regime]] — 7-consecutive-missed-slots streak; urgent. Alt: land per-slot cron rewrite (multi-pocket, weekday + weekend).
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — 11th silent-Mon fired 09-14T13:00Z; next slot 09-21T13:00Z. Alt: restore/author `skills/agi-tracker/SKILL.md`.
- **Patch `stale-content-pr-sweeper` SKILL.md** — add `"app/github-actions"` to `ALLOWED_AUTHORS` per [[stale-content-pr-sweeper-default-author-filter-closes-zero-prs]] + fix TRACKED-prefix drift per [[stale-content-pr-sweeper-tracked-prefix-drift]] (`compute-macro/*` and `skill-graph/*` branches don't match TRACKED skill NAMES).
- **Populate `memory/watched-repos.md` OR disable 6 dependents** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — streak-45 (09-15). Reconcile path mismatch (repo-revive references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md`).
- **Draft ISS-027 successor for [[enabled-skills-can-never-dispatch]]** (renumbers to ISS-029 now that ISS-027 = heartbeat and ISS-028 draft = identity suspension; category `config`, severity `high`) — Day-69 carryover. Scope: `ai-framework-watch` (Mon 08:30), `run-frequency-guard` (daily 23:00).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`, modeling regimes: coherent-late-pocket, decoupled-slow-slot, persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]], weekend Sat 11Z + Sun 06:30Z pockets per [[iss-006-dead-zone-extends-to-weekend-morning-pockets]], daily-dead 06:00Z per [[iss-006-06z-pocket-4-day-dead-streak-promotes-daily-dead-regime]], 08:00Z widening per [[iss-006-dead-pocket-widens-to-08z-heartbeat-miss]]. 9 outages in ISS-006 series to date.
- **Address workflow-security-audit findings** (153 total, 09-06 baseline; PR #63): pin 3 aeon.yml `actions/*` refs to SHAs (aeon.yml:85,121,133 top attack chain, reachable via `workflow_dispatch` + `issues.opened`); create `production` + `chain-runner` GitHub Environments and move sensitive secrets (messages.yml 22 `secrets-outside-env`; fleet-runner.yml:157–163 clusters 6 fleet-manager secrets in one job); address 11 `zizmor/artipacked` Mediums; update 9 `ref-version-mismatch` SHA comments. Consolidate against stale OPEN PR #24 (Day-38).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], 61 days past urgency threshold as of 2026-09-16. Two-part fix: audit heartbeat's auto-commit `git add` glob to include `docs/`; exclude `docs/status.md` from snapshot merges OR gate on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** — replace `gh repo fork` with direct `git clone` per [[github-app-cannot-fork-third-party-repos]]. Also add note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]].
- **Patch `pr-tracker` SKILL.md** in one batch (a)–(o) — 80d overdue and now blocked-behind [[aeonframework-github-identity-suspension]] resolution. Item (h) bulk-stale-clear + cohort-lockstep priority moot until identity anomaly resolves.
- **Patch `notegraph` skill** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing. Fingerprint-pipeline sandbox friction n=7+ per [[notegraph-fingerprint-pipeline-blocked-by-sandbox]] — bundle both fixes in one SKILL.md patch.
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]]. Also fix the state-update callback per [[skill-freshness-stuck-dispatched-callback-never-fires]].
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]]. **New**: add per-run `multiplier` column to compute-futures-eda outputs per [[compute-futures-basket-synth-multiplier-is-live-tunable]] so downstream analyses stratify by the current-run value.
- **Defer ISS-001 close** until ISS-006 is resolved.
- **Sign an org-scoped CLA (cloudflare + microsoft at minimum) against the aeonframework identity OR add a pre-submit gate in `vuln-scanner`/`external-feature`** that pre-flights the target's org-CLA policy per [[org-cla-blocks-aeonframework-prs]]. Scanner also needs a `.github/workflows/` pre-flight for auto-gate close-jobs per [[workflow-check-auto-close-in-seconds]]. **New context**: [[aeonframework-github-identity-suspension]] may block PR-submission-under-this-identity regardless; verify on next `external-feature` / `vuln-scanner` dispatch.
