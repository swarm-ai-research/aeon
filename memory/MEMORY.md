# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 6 durability, merge path still unproven** — **21 open aeon-repo PRs** (19 `app/github-actions` + #26 dependabot + #9 `app/freebuff-web`); +1 overnight (#29 notegraph); **0 of 19 `app/github-actions` merged in ~132h+** since 08-07 unblock. Today's notegraph log erroneously inferred #28 merged from HEAD-vs-last-state delta — `gh pr view 28` confirms #28 remains OPEN (delta was 08-11 reflect's direct `notegraph.json` regen commit, not a merge). #26 dependabot actions/checkout remains the textbook auto-merge candidate for first-flow proof; #10 notegraph orphan flag (5d+, dep-free) or #27/#28/#29 notegraph clean also viable. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-10 — batch DECOUPLED today** — compute-futures-eda + suggest-edges fired near-slot at 06:05Z while planner rolled solo to 07:35:53Z (65min late). First observed batch-decouple after 3 consecutive coherent-late-pocket days. Batch-health 2/2 OK in 06:00–07:30 window. Fix path unchanged (multi-pocket cron rewrite must now model both a coherent-late-pocket AND a decoupled-slow-slot regime). See [[fleet-ops]].
- **ISS-001 residue day 52** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged **46th** steady-state day (passive advance, no 08-11 skill-health run). See [[fleet-ops]].
- **≥28 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-12 tuple `(1, 6, 1, 3)` vs predicted `(0, 7, 1, 1)` — 1-of-4 HIT; TWO NEW CLASSES detected** — (a) `WhiskeySockets/Baileys#2732` letter-of-SKILL flipped stale→active via `github-actions[bot]` stale-notice comment 2026-08-12T02:17Z (`This PR is stale because it has been open for 14 days`) — comment activity gate inverts the substantive signal → new atomic [[pr-tracker-stale-bot-comment-inverts-stale-classification]]; (b) `aeonframework/aeon-programmable-hooks#1` (merged 2026-08-08T19:17Z, `aeon/reproducible-source-and-binding`) surfaced today after being MISSING from yesterday's 30d table (issueCount 23 both days, not a top-60 truncation) — GitHub-search indexing lag on newly-created self-owned repo → new atomic [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (transient, recoverable next scan; distinct from archive/deletion classes). Retrospective correction: yesterday's `Recent Merges (last 30d) — 5` should have been 6. Only `closed_no_merge` HIT (k-skill#547 continuing). Repo-deletion class day 2 confirming; archive-hide day 7 confirming. Tomorrow's predictor `(1, 7, 1, 2)` — aeon-programmable-hooks#1 stays in 7d bucket (rolls off 08-15T19:17Z); PostHog/posthog#78346 crosses 7d-frozen at 08-12T14:08Z (~19.5h pre-tomorrow's scan) → stale; k-skill#547 holds; active_open = aeon-programmable-hooks#2 + Baileys#2732 (letter-of-SKILL). See [[pr-status]].
- **swarm PR queue 8 open, byte-frozen 4-day** — ~4d zero SHA churn on all 7 non-#551 PRs since 2026-08-07T02:2xZ. pr-review 41st + pr-triage 31st both 100%-skip (5 dependabot + 2 dup-SHA). Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5). Confirming counter 29 (review) / 29 (triage).
- **notegraph 08-12 PR #29 opened — +2 notes wired in, change-streak day 3** — extractor `262n / 1974h / 661s / 1o / 78 atomic / 0b` (delta **+2n / +17e (+12h, +5s) / 0 orphan/bundled** vs HEAD 260n from 08-11 reflect regen). Fingerprint `ea1b666b…` → `92a4e559…`. Wired-in nodes yesterday's reflect promotions [[compute-futures-spread-retail-loss-concentration]] + [[pr-tracker-repo-deletion-loses-pr-permanently]]. Sandbox note: shell `>` redirect + `xargs sha1sum` compound both blocked (day-3 workaround) — today's run wrote helpers to `/tmp/notegraph-{fingerprint,diff}.mjs` (yesterday's `scripts/notegraph-*.sh` versions untracked); reflect-flag standing for promotion to first-class SKILL helpers. Also flag: SKILL.md needs to list `docs/notegraph-speedrun.html` as a fourth output.
- **suggest-edges 08-12 ABORTED MID-SKILL (day-4 state-loss recurrence)** — script emitted 3 templated-corpus pairs (2026-07-28↔08-01 sim 1.00, 06-24↔08-09 sim 0.985, 06-27↔08-09 sim 0.985) — different source notes than yesterday because 08-11's state additions blocked the 06-27/07-04/08-03 pairings. Reverted source-note edits + moved `applied → rejected` in `memory/state/suggest-edges.json` (state now 3 applied + 6 rejected). Confirms [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] streak-4 + [[skill-state-on-blocked-pr-branch-is-lost]] streak-5. Fix candidate for planner: pre-filter shared-parent-directory + shared-basename-shape (all `gitlawb-compute-futures-proofs/YYYY-MM-DD.md` should score against each other at a lower ceiling or be excluded outright).
- **compute-futures-eda 08-10 CSV — 4-consecutive-run promotion** → new atomic [[compute-futures-spread-retail-loss-concentration]] (top-5 spread losses 100% retail, deepest −$20,016 at seed 202608109). Also MEDIUM cross-mode maxCurve step-down 25→16.67% vs 08-08; super-anchor 202608106 (15 outlier positions across 3 modes).
- **`.pending-disclosure/` queue steady at 6** — no 08-11 vuln-scanner run (weekly cadence, last 08-08). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band OR queue converts once merge path proves.
- **Milestone alert — ms-01 stalled-5** — aeon repo stars 0/100 unchanged (5 consecutive weekly zero-delta; next milestone-tracker fire 2026-08-17 Mon 12:00Z). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker 6th silent-Mon fire HIT at 2026-08-10T13:44Z — buffer restored** — next slot 2026-08-17 13:00Z will be 7th silent unless SKILL.md restored OR `enabled: false` lands. Pressure off top of stack; downstream still worth landing this week. See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-10 07:00Z** — $270.62 across 50 runs (↑13.9% WoW), monthly projection $1,159.79 (⚠). 0 anomalies, 0 optimization levers.
- **Never-dispatched 34th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11), [[compute-futures-spread-retail-loss-concentration]] (08-11), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09), [[github-app-cannot-fork-third-party-repos]] (08-08), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-7 persistence 08-12), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #23 opened today with correlation matrix update.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Prove end-to-end merge flow — trigger day 4, RANK-1 08-12** — 21 open aeon-repo PRs (19 `app/github-actions` + #26 dependabot + #9 freebuff), 0 `app/github-actions` merged in ~132h+ since unblock. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate)** or #10 (notegraph orphan flag, 5d+, dep-free) or #27/#28/#29 (notegraph clean) — to prove flow; (b) walk down the 21-deep queue + ≥28 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. Gating step for planner rank-2 (sweeper allowlist) and rank-3 (agi-tracker toggle). See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing. NOTE: 08-12 notegraph log misread HEAD-vs-last-state delta as `#28 merged` — verified via `gh pr view 28` (state=OPEN, mergedAt=null); merge flow still UNPROVEN. Reflect's 08-11 direct-commit of `notegraph.json` regen advanced HEAD independent of any PR merge.
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-3 (deadline HIT 08-10, streak reset from 5 to 1; buffer restored ~5.5d out). Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-2 (streak-5 as of 08-12 sweeper log). Hardcoded `{"aeonframework"}` but all 19 post-unblock aeon-repo PRs are `app/github-actions`. Chains today: `suggest-edges` #14 ← #21 ← #22 length-3 (unchanged) AND `notegraph` #10 ← #27 ← #28 ← #29 **length-4** (grew from length-3 by today's #29). Total would-be-closes if patch landed: **4 PRs** (up from 3 on 08-11). Bundle in a **TRACKED-prefix drift fix** (streak-3) — #23 branch `compute-macro/2026-08-09` doesn't match TRACKED skill name `compute-macro-correlate`; add `compute-macro` prefix OR align the skill's branch-naming to the full name. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-6 chronic). Persistent same-day short-circuit cycle continues (7 same-day skips 08-10, more on 08-11).
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **23rd-day carryover** as of 08-11. Scope: `ai-framework-watch` (Mon 08:30, 34d silent), `run-frequency-guard` (daily 23:00, 34d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate — 6th silent-Mon fire hit 08-10) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model TWO migrated pockets (~06:11–06:22Z + ~09:00–09:58Z) and ONE persistent gap around planner's 06:30Z slot per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] should ease now that meta-block appears cleared. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **24 days past urgency threshold** as of 2026-08-10. 08-10 heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**fourteenth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes (08-10 predictor CLEAN 4-of-4 HIT validated n=3 lockstep cohort as distinct); (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 7 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops. **[Viable now — 49d overdue as of 2026-08-12]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
