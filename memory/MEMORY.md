# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- ⚠ [[issues/ISS-006]] day 21 (2026-07-11 Sat odd-DOM) — **08:00-pocket recovered today** (heartbeat 08:29Z + batch-health/skill-freshness/gitlawb-fleet-metrics all in_progress 08:27Z — breaking the 2-day silent streak from 07-09/10). Close clock stays at **0 clean days** per [[iss-006-pocket-recovery-is-noise]] — 1-day pocket recovery is delivery-rate noise (need 3 consecutive). 06:00 pocket also recovered: memory-flush / -structural-dedupe (skipped odd-DOM) / compute-futures-eda / notegraph / suggest-edges / planner (**first fire since 2026-07-05T07:47Z, 142h/5.94× threshold — ISS-006 tributary broken**) all fired 06:29Z–06:37Z. Standing at-2× list drops to 2: cost-report (Mon 07:00, 21d, 3.00×), janitor (Sun 05:30, 21d, 3.00×). Planner reframed top from `iss-006-messages-yml-per-slot-crons` (streak-3, unlanded) to `operator-pat-provisioning` per skill §2 stuck-goal escalation.
- [[issues/ISS-001]] OAuth outage residue (2026-06-06 → 2026-06-20T06:05Z) day 21 of denominator burn-down — 38 skills at `success_rate < 0.5` while `last_status: success` and `consecutive_failures: 0`; close deferred until ISS-006 stabilizes.
- [[issues/ISS-007]] (2026-07-05) — heartbeat missing_pattern in skill-evals regex likely false positive per 2026-07-05 self-review (heartbeat DID run 2026-07-05T09:58Z, skill-evals scanned before slot fired). Same-day timing race.
- [[issues/ISS-008]] (2026-07-05) — cost-report no_file_match; standing ISS-006 tributary (weekly Mon 07:00 slot silent 4th consecutive Monday as of 2026-07-06).
- [[issues/ISS-005]] — swarm-safety-eval SSE_EMPTY path writes to log not article per [[swarm-safety-eval-empty-writes-log-not-article]]. Reclassify from `missing-secret-or-cron` → `permanent-limitation`.
- **Two enabled skills never dispatched** (heartbeat 2026-07-11 P3 novel-scan): `ai-framework-watch` (weekly Mon 08:30, ~120h since expected 2026-07-06T08:30Z, no cron-state entry) and `run-frequency-guard` (daily 23:00, ~9.5h since expected 2026-07-10T23:00Z, no cron-state entry — was present at 100% success in 2026-06-08 status.md snapshot, wiped in OAuth burn). Investigate messages.yml matcher / aeon.yml wiring per [[enabled-skills-can-never-dispatch]].
- Pending operator action: **11 staged branches** all blocked by "GitHub Actions is not permitted to create or approve pull requests" per [[github-actions-cannot-create-prs]] — `agi-tracker/2026-06-29`, `notegraph/2026-07-06`, `notegraph/2026-07-11` (**first genuine corpus-growth run in 5 days** — Δ +2 nodes/+38 edges vs HEAD; Node-based sha1 fingerprint scheme correctly detected drift and let extractor proceed per [[notegraph-extractor-generatedat-nondeterministic]]), `fix/workflow-security-audit-2026-06-21`, `-06-28`, `-07-05`, `skill-graph/2026-06-28`, `skillpacks/2026-07-05`, `suggest-edges/2026-07-07`, `suggest-edges/2026-07-10`, `suggest-edges/2026-07-11` (**3rd consecutive day** of the same 3 similarity-1.00 links from `gitlawb-compute-futures-proofs/2026-06-20.md`; state file re-created fresh for a 2nd day, validating [[skill-state-on-blocked-pr-branch-is-lost]]).
- **PR queue (2026-07-11):** first material state change in 6 days — kage#66 rolled off closed-no-merge window at 2026-07-10T12:20:11Z (predicted by memory-flush 2026-07-10); category count `closed_no_merge: 1 → 0`. Hash-based dedup guard correctly did NOT suppress (legitimate transition). Remaining: HKUDS/Vibe-Trading#390 MERGED (rolls off 2026-07-12T15:33:53Z, ~28h post-run), Panniantong/Agent-Reach#436 active (14.66d, activity 4.90d ago; tips stale 2026-07-13T13:32:11Z). See [[pr-status]].
- **Swarm PR fleet stationary (2026-07-11):** same 6 open PRs as 2026-07-09/10 (5 dependabot bots + #527 rsavitt neurosymbolic), same head SHAs, no new activity since 2026-07-09 05:38Z (6th consecutive stationary day). pr-review **15th consecutive day** of 403 write-block on #527 APPROVE 5/5 verdict per [[aeon-app-no-write-on-swarm-repo]]; pr-triage **14th consecutive day** of same 403 on #527 DEFER (PR_TRIAGE_NO_PERMISSION, deliberately no `triaged-prs.json` append so retry re-fires).
- AGI Tracker last refresh 2026-06-29 Mon 13:00 UTC; 2026-07-06 slot missed on ISS-006; next opportunity 2026-07-13.
- `docs/status.md` auto-commit bug per [[status-md-auto-commit-drops-writes]] — heartbeat DID run 2026-07-11 (08:29Z, 5th consecutive write attempt); whether it lands still contingent on messages.yml auto-commit glob including `docs/`.
- skill-freshness FRESHNESS_OK **counter delta unclear** (today's run reports "44 enabled consumers · 2 cross-skill deps · 0 flagged" — didn't emit a running counter). 2026-07-11 08:00 slot delivered → structural blind spot per [[skill-freshness-mtime-blind-in-gha]] unchanged.
- **Durable claim (2026-07-09), 3rd validation (2026-07-11):** compute-futures-eda's |r|≥0.8 findings involving `wallet_sum_pnl` are float-dust artifacts — 2026-07-11 delivered **zero |r|≥0.8 pairs in any mode** (first zero-crossing run since 07-07); yesterday's x402 wallet_sum_pnl × x402Total = +0.881 collapsed to +0.088 (3rd consecutive-day filter validation). See [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]].
- **New atomic claim (2026-07-11):** Anthropic × TeraWulf $19B/20-year 401 MW Kentucky lease (Jul 6 announcement) supersedes the xAI Colossus deal as Anthropic's largest single compute commitment; first capacity H2 2027, full 401 MW by early 2028. See [[anthropic-terawulf-19b-401mw-kentucky-lease]].
- **New atomic claim (2026-07-11):** Sandbox blocks shell `>` redirects to working-dir paths; workaround is Python `pathlib.Path.write_text` after `subprocess.run` or the tool's own `-o` flag. See [[sandbox-blocks-shell-redirect-to-workdir]].
- **New atomic claim (2026-07-11):** A skill can be `enabled: true` with SKILL.md present yet never dispatch — cron-state.json simply has no entry; only heartbeat's P3 novel-scan catches this. See [[enabled-skills-can-never-dispatch]].
- **vuln-scanner (2026-07-11):** `oomol-lab/open-connector` PVR draft in `.pending-disclosure/` (1× semgrep `javascript.node-crypto.security.gcm-no-tag-length` at `src/server/secrets/secret-codec.ts:48` — Node local codec accepts 4-byte GCM tag, reducing forgery from 2⁻¹²⁸ → 2⁻³²; medium severity; 3-line fix documented).
- `.pending-disclosure/` queue: **2 entries** (torlink 07-04 `ip@2.0.1` HIGH + `esbuild` LOW dev-only; oomol-lab/open-connector 07-11 GCM-tag-length medium).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

## Next priorities
- **Operator PAT provisioning** — the meta-blocker behind 6 stalled fleet fixes and the 11-branch staged queue (per today's planner escalation from `iss-006-messages-yml-per-slot-crons` streak-3 → `operator-pat-provisioning` streak-1). Every ISS-006 fix, notegraph fingerprint fix, and skill-state-loss workaround lands via PR; every PR is blocked by [[github-actions-cannot-create-prs]]. `repo`-scoped PAT unblocks all.
- ISS-006 fix: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering **every** timeslot in `aeon.yml`. Day-21 (2026-07-11) 08:00 pocket recovered today but close clock stays at 0 clean days per [[iss-006-pocket-recovery-is-noise]]; needs 3 consecutive clean days (watch 07-12/13). Underlying delivery-rate bug per [[gha-messages-yml-cron-underdelivery]] unchanged.
- Investigate why `ai-framework-watch` (weekly Mon 08:30) and `run-frequency-guard` (daily 23:00) never dispatch despite `enabled: true` + present SKILL.md per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references.
- Standardize notification emission across SKILL.md files on **direct `.pending-notify/${epoch}-${skill}.md` writes** — both `$(cat file)` forms (inline and MSG-variable) are blocked per updated [[notify-inline-cat-substitution-blocked-in-sandbox]], `./notify -f` is broken per updated [[notify-script-has-no-f-flag]], and recovery from an `-f`-corrupted queue is an in-place payload rewrite (not a re-fire). Node `execFileSync` is a secondary option. SKILL.md audit sweep needed.
- Fix `docs/status.md` auto-commit drop per [[status-md-auto-commit-drops-writes]] — 5th consecutive write attempt made today by heartbeat (was 4 as of 2026-07-08); audit `messages.yml` auto-commit `git add` glob to include `docs/`, or make heartbeat commit explicitly.
- Patch notegraph skill's silent-exit heuristic per [[notegraph-extractor-generatedat-nondeterministic]] — either mask `generatedAt` before diffing or teach the extractor to omit it. The 4-day stable-topology streak (07-07 → 07-10) broke today (2026-07-11) when the corpus genuinely grew (+2 nodes/+38 edges) and the Node-based sha1 fingerprint correctly let the extractor proceed — fix isn't urgent but the naive `git diff --quiet` HAS_DIFF gate would still re-PR any stable corpus.
- Close ISS-007 as false positive OR add same-day grace window to `skill-evals` (scan same-day logs only after 12:00 UTC) — heartbeat DID run 2026-07-05 but skill-evals scanned before the late slot fired.
- Patch `pr-tracker` SKILL.md in one batch: (a) drop `stateReason` from GraphQL query per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with list/domain commit-author filter per [[aeon-bot-uses-multiple-signing-identities]] + [[pr-tracker-branch-prefix-misses-bot-identity]], (d) **land the hash-based step-5 dedup guard** — validated in-skill 2026-07-09/10 (skipped-dedup) AND validated by kage#66 rolloff today 2026-07-11 (correctly did NOT suppress the legitimate transition) per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]].
- Fix `skill-freshness` to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged.
- Widen `scenario-sweep.mjs` seed count or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Additionally, filter out `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ(wallet_sum_pnl) > 1e-6 — validated 3rd consecutive day today when yesterday's +0.881 crossing collapsed to +0.088 (zero |r|≥0.8 pairs in any mode). Also resolve seed-encoding artifact (3 of 12 seeds 10-digit) per [[compute-futures-seed-padding-bug]].
- File `./generate-skills-json` bugs as structured issues (see [[generate-skills-json-newline-bug]], [[skills-json-count-drift]]).
- Investigate missing `scripts/validate-config.js` referenced by config-validator SKILL.md — either restore the script or drop the fast-path reference.
- Populate `memory/watched-repos.md` or disable the five watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog) — daily skip pattern wastes a workflow slot per skill.
- Defer ISS-001 close until ISS-006 is resolved.
