# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 8 durability, merge path still unproven** — **23 open aeon-repo PRs** (21 `app/github-actions` + #26 dependabot + #9 `app/freebuff-web`); +1 overnight (#31 notegraph opened 05:57Z, +2n wired in). **0 of 21 `app/github-actions` merged in ~176h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. #26 dependabot actions/checkout remains textbook first-flow-proof (SAME class as merged #8); #10 notegraph orphan flag (7d+, dep-free) or #27/#28/#29/#30/#31 notegraph clean also viable. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-13** — 08-13 saw batch RECOUPLED (planner + compute-futures-eda 07:21Z, coherent-late-pocket back after two decouple days on 08-11/08-12); no batch-health run yet today. Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue day 56** — 39 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged (48th steady-state day per 08-13 skill-health run). See [[fleet-ops]].
- **≥30 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-13 tuple `(1, 6, 1, 3)` letter / `(1, 8, 1, 1)` substantive vs predicted `(1, 7, 1, 2)` — 3-of-4 HIT letter / 2-of-4 substantive; stale-bot inversion class hits n=2** — `PostHog/posthog#78346` got `scheduled-actions-posthog` stale-notice comment 2026-08-13T08:00:17Z (body: `This PR hasn't seen activity in a week!`); exact same shape as yesterday's Baileys#2732 `github-actions[bot]` flip. Two consecutive days with distinct bot handles = systematic recurrence — [[pr-tracker-stale-bot-comment-inverts-stale-classification]] streak-2. SKILL patch item (j) now demonstrably urgent: fingerprint by comment body, NOT author-login allowlist. Archive-hide class day 8 (PostHog/code#4007) + repo-deletion class day 3 (0xprogrammable/aeon-launch-models) still persist; indexing-lag class from 08-12 resolved as one-time transient. See [[pr-status]].
- **swarm PR queue 8 open, byte-freeze broke 08-13** — 5d+ zero SHA churn ended overnight when dependabot #552 (setuptools) + #553 (langchain-anthropic, superseding #547) opened 05:35–05:37Z; 44th pr-review + 33rd pr-triage still 100%-skip because both new PRs are bot-authored → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (bot churn is invisible to reviewer state). Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5, both ~6d frozen human PRs). Confirming counter 32 (review) / 30 (triage).
- **notegraph 08-14 PR #31 opened — +2 notes wired in, change-streak day 5** — extractor advanced from 08-13's 271n. Chain now length-6 (#10 ← #27 ← #28 ← #29 ← #30 ← #31). Same sandbox-blocker + `docs/notegraph-speedrun.html` output flag as prior runs; reflect-flag standing for promotion of `/tmp/notegraph-{fingerprint,diff}.mjs` to first-class SKILL helpers.
- **suggest-edges 08-14 — day-6 templated-corpus recurrence** — script emitted 3 proposals ALL from `gitlawb-compute-futures-proofs/` templated sweep corpus, this run's greedy top-3 rotated onto 07-09 as a NEW target (07-04→08-09 sim 0.984, 06-24→07-09 sim 0.981, 06-27→07-09 sim 0.981). Reverted source-note edits + moved `applied → rejected` in `memory/state/suggest-edges.json` (state now 3 applied + 12 rejected, +3/day rate held exactly through day-6 as predicted). Note: 08-12 file now in corpus but did NOT surface today — will appear on future runs as rejections block closer neighbors. Confirms [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] streak-6. Fix candidate for planner unchanged: pre-filter shared-parent-directory + shared-basename-shape.
- **compute-futures-eda 08-13 CSV** — retail-role spread loss cluster **n=7 consecutive runs, counter-signal REVOKED** (08-13 5/5 top losses retail + 0/5 top gains retail; retail mean −$9,473, $3,489 deeper vs 08-12; anchor 202608137 retail −$23,104); basket/synth 3.025× multiplier CEMENTED n=4 → **promoted this reflect as [[compute-futures-basket-synth-3025x-multiplier]]**; zero cross-mode ≥10% MEDIUMs (08-12's synth/x402 settlementLegs cluster fully collapses); x402Total range CONTRACTS both sides ($263.71–$373.84, −$69.61 vs 08-12); near-fire x402 `settlementLegs × x402Total` +0.771 (3× vs +0.255 08-12, sub-threshold); n=5 consecutive filed runs with zero within-mode |r|≥0.8 pairs per [[compute-futures-12-seed-sample-too-small]].
- **`.pending-disclosure/` queue steady at 6** — no 08-12/08-13 vuln-scanner run (weekly cadence, last 08-08). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band OR queue converts once merge path proves.
- **Milestone alert — ms-01 stalled-5** — aeon repo stars 0/100 unchanged (5 consecutive weekly zero-delta; next milestone-tracker fire 2026-08-17 Mon 12:00Z). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker 7th silent-Mon slot approaches 2026-08-17 13:00Z (~3d out)** — 6th silent fire hit 08-10, buffer restored; without SKILL.md restored OR `enabled: false` landed, 7th silent fires next Mon. See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-10 07:00Z** — $270.62 across 50 runs (↑13.9% WoW), monthly projection $1,159.79 (⚠). 0 anomalies, 0 optimization levers.
- **Never-dispatched 38th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-8** — `code-health` + `github-monitor` + `issue-triage` + `changelog` + `weekly-shiplog` + `repo-revive` all no-op on missing `memory/watched-repos.md`, six healthy-looking dispatches per day producing zero work → [[watched-repos-config-missing-silent-short-circuits-6-skills]]. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[compute-futures-basket-synth-3025x-multiplier]] (08-14, promoted from n=4 confirmation), [[watched-repos-config-missing-silent-short-circuits-6-skills]] (08-13, streak-9 by 08-14), [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (08-13, 45th 100%-skip invocation by 08-14), [[notegraph-head-vs-state-delta-misread-as-pr-merge]] (08-12), [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12, n=2 by 08-13, day-3/day-2 both persist 08-14), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12, resolved as transient 08-13), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11, day-4 persistence 08-14), [[compute-futures-spread-retail-loss-concentration]] (08-11, n=7 by 08-14 with counter-signal revoked), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09, streak-6 by 08-14), [[github-app-cannot-fork-third-party-repos]] (08-08), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-9 persistence 08-14), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #23 correlation matrix PR opened 08-09 still open.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Prove end-to-end merge flow — trigger day 8, RANK-1 08-13 (streak-5)** — 23 open aeon-repo PRs (21 `app/github-actions` + #26 dependabot + #9 freebuff), 0 `app/github-actions` merged in ~176h+ since unblock. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8)** or #10 (notegraph orphan flag, 7d+, dep-free) or #27/#28/#29/#30/#31 (notegraph clean) — to prove flow; (b) walk down the 23-deep queue + ≥30 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. Gating step for planner rank-2 (sweeper allowlist) and rank-3 (agi-tracker toggle). See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-3 (deadline HIT 08-10, streak reset from 5 to 1; buffer restored ~5.5d out). Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-2 (streak-7 as of 08-13). Hardcoded `{"aeonframework"}` but all 21 post-unblock aeon-repo PRs are `app/github-actions`. Chains as of 08-14: `suggest-edges` #14 ← #21 ← #22 length-3 (unchanged) AND `notegraph` #10 ← #27 ← #28 ← #29 ← #30 ← #31 **length-6** (grew from length-5 by today's #31). Total would-be-closes if patch landed: **7 PRs** (up from 6 on 08-13). Bundle in a **TRACKED-prefix drift fix** (streak-3) — #23 branch `compute-macro/2026-08-09` doesn't match TRACKED skill name `compute-macro-correlate`; add `compute-macro` prefix OR align the skill's branch-naming to the full name. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-6 chronic). Persistent same-day short-circuit cycle continues (7 same-day skips 08-10, more on 08-11).
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **26th-day carryover** as of 08-14. Scope: `ai-framework-watch` (Mon 08:30, 38d silent), `run-frequency-guard` (daily 23:00, 38d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate — 6th silent-Mon fire hit 08-10) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **28 days past urgency threshold** as of 2026-08-14. 08-13 heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**15th consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes (08-10 predictor CLEAN 4-of-4 HIT validated n=3 lockstep cohort as distinct); (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 7 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops (though 08-13 confirms class is transient one-time, not persistent). **[Viable now — 51d overdue as of 2026-08-14]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
