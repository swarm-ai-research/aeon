# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **ISS-006 pocket-slot migration Day-5** — morning delivery now consistently bimodal per new [[morning-pocket-splits-into-two-de-facto-clusters]]: ~06:11–06:22Z early pocket (08-05 carried compute-futures-eda 06:11:51Z + notegraph 06:16Z + suggest-edges 06:22Z) and ~09:00–09:58Z late pocket (heartbeat 09:07Z + pr-tracker 11:31Z). **Planner silent 73h (3rd consecutive missed 06:30Z slot 08-03/04/05)** — filed as new [[planner-silenced-by-its-own-escalation-target]]: planner is escalator of the operator toggle that would fix the migration silencing it. Batch-health WARN below OUTAGE threshold on 08-05 (1 of 2 morning skills missing). **INDEX.md holds at 18 open**. See [[fleet-ops]].
- **ISS-001 residue day 46** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged 39th steady-state day. See [[fleet-ops]].
- **≥26 staged branches** blocked behind [[github-actions-cannot-create-prs]] (+1 vs 08-04 via today's `notegraph/2026-08-05` push, 21st staged notegraph branch). Preferred unblock: repo Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests". Fallback: `repo`-scoped PAT as `AEON_GH_PAT` — proven live via swarm#527 merge on 2026-07-18. Active rank-1 `elevate-repo-settings-toggle-active` per planner's escalation-of-escalation, **frozen at streak-5 by run / streak-12 by calendar day** (planner silent 08-03/04/05; by-run counter cannot advance per new [[planner-silenced-by-its-own-escalation-target]]). See [[fleet-ops]].
- **Planner escalation-of-escalation applied** — pattern captured as [[planner-escalation-of-escalation-when-meta-blocker-holds]]: one operator move unblocks ≥26 staged branches + 6 stalled fleet fixes + agi-tracker escalation + resolves ISS-020's fix path. 08-02 planner also promoted `agi-tracker-enabled-false-direct-edit` to rank-3 concrete (still open, needs branch merge). 08-03/04/05 planner all silent.
- **notegraph 08-05 verdict `+1 notes wired in`** — fingerprint `a9d3db5e…` → `8b1ee3fa…` (08-04 lesson [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] landed on main). Extractor 234n / 1776h / 631s / 0o / 66 atomic vs HEAD's 233/1772/632/0/65. Delta +1 node, +3 edges. Notify sent (non-bland). PR blocked, branch `notegraph/2026-08-05` staged (21st consecutive notegraph branch waiting).
- **pr-tracker 08-05 tuple `(3, 2, 3, 6)`** — vs 08-04 `(2, 0, 3, 9)` vs predicted 08-05 `(2, 2, 2, 9)`. **Predictor 1-of-4 partial** (active axis hit via wrong reasoning). Novel signals: (a) kaneo#1457 first Next.js cohort MERGE at 3.4d without formal APPROVED review (n=1 observation, watch workweave/router + k-skill for cohort follow-through); (b) Baileys#2732 + nango#6929 same-day file cohort stales in lockstep per new [[same-day-file-cohort-stales-in-lockstep]]; (c) third consecutive scan-vs-anniversary-hour predictor miss now confirmed bidirectional per new [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (wigolo threshold 20:50Z tonight, scan ran 11:31Z). See [[pr-status]].
- **swarm PR queue byte-identical 13th consecutive day at 8 open** — pr-review 31st invocation (08-04 ended at 30th). Both write endpoints 403 again on both PRs; **23rd confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (log-only verdicts; PAT/App-permission unblock only path to on-PR posting).
- **compute-futures-eda 08-05 delivered in early cluster** (06:11:51Z). No fresh EDA report at reflect time; 08-04 signals still latest (basket −0.853 realizedAbs × maxSpot breaking 13-filing zero-streak; cross-mode single-seed anchor n=5 at seed 202608038; 08-01 HIGH settlementLegs reset to 0.00%). compute-macro-correlate silent (weekly Sun cadence).
- **`.pending-disclosure/` queue holds at 4** — unchanged from 08-02 (aging now +3 days). Directory only exists on unmerged vuln-scanner branches per [[github-actions-cannot-create-prs]]; operator submits out-of-band.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged (last milestone-tracker fire 08-03 Mon 12:00Z; next fire 2026-08-10 Mon). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker 6th silent-risk Mon slot is 2026-08-10 13:00Z** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Unless SKILL.md restored or `enabled: false` on aeon.yml:188 lands. 08-02 planner's rank-3 concrete escalation still pending branch merge.
- **Never-dispatched 28th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05), [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04, first observation), [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] (08-03).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Repo Settings toggle OR operator PAT provisioning** — active rank-1 per planner's escalation-of-escalation (**frozen at streak-5 by run / streak-12 by calendar day** — planner silent 08-03/04/05, cannot advance by-run counter per new [[planner-silenced-by-its-own-escalation-target]]). Single highest-leverage action; unblocks ≥26 staged branches + 6 stalled fleet fixes + agi-tracker escalation + ISS-020's fix path in one motion. Ask: (a) https://github.com/aeonframework/aeon/settings/actions → Workflow permissions → tick "Allow Actions to create PRs" → Save. (b) `gh pr create -R aeonframework/aeon --base main --head notegraph/2026-08-05 --title "graph: +1 note wired in" --body "auto"` under swarm#527-validated PAT.
- **Set `enabled: false` on `aeon.yml:188` for `agi-tracker`** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — folded under the operator toggle rank-1 (needs a branch merge to land). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Next slot 2026-08-10 13:00Z will be 6th silent unless action lands. File as ISS-021 candidate; ISS-020 already filed 08-03 for batch outage (don't fold).
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **18th-day carryover** (was ISS-020 draft-target until 08-03 filed a different ISS-020 for batch outage — renumber next). Scope: `ai-framework-watch` (Mon 08:30, 28d silent), `run-frequency-guard` (daily 23:00, 28d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (novel P0 stuck-skill class 07-26 — self-cleared 07-26T08:55:12Z, ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Fix scope widened per new [[morning-pocket-splits-into-two-de-facto-clusters]] — model TWO migrated pockets (~06:11–06:22Z + ~09:00–09:58Z) and ONE persistent gap around planner's 06:30Z slot, not a single slid slot. Also see new [[planner-silenced-by-its-own-escalation-target]] for the meta-blocker loop that keeps this fix from landing. **[BLOCKED by Repo Settings toggle for branch merge]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **20 days past urgency threshold** as of 2026-08-05. 08-05 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**tenth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR noreply) per [[pr-tracker-email-filter-must-be-domain-match]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-05), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] AND on the stale-entry side per new [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (**third consecutive miss** — kage#66 08-02 + plano 08-04 + wigolo 08-05 stale-entry); (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per new [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes. **[BLOCKED by Repo Settings toggle, 42d overdue as of 2026-08-05]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]].
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Populate `memory/watched-repos.md`** or disable the 6 watched-repos-dependent skills (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — daily/weekly skip pattern wastes a workflow slot per skill. Today's log shows 3 same-day short-circuits (issue-triage + github-monitor + code-health).
- **Defer ISS-001 close** until ISS-006 is resolved.
