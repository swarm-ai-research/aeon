# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 3 durability confirmed, merge path still unproven** — 15 open aeon-repo PRs (14 `app/github-actions` + #9 `app/freebuff-web`); +2 today (#22 suggest-edges 05:35Z + #23 compute-macro-correlate 07:38Z). Three consecutive days of `app/github-actions` authorship; **0 of 14 `app/github-actions` PRs merged in 48h+** since 08-07 unblock. Spot-check of #10/#14/#21/#22 all `mergeStateStatus: UNKNOWN` + empty `statusCheckRollup` — no CI or branch-protection gating, queue awaits operator. New atomic [[pr-creation-toggle-is-distinct-from-merge-capability]] captures the creation-vs-merge two-lever insight. MEMORY.md line 42 "if no merges by 2026-08-09" trigger has FIRED today — planner rank-2 (new) `aeon-repo-queue-merge-escalation` bundled into same notify as rank-1. Swarm write-perm gap DISTINCT and unaffected (27/28 confirming invocations of [[aeon-app-no-write-on-swarm-repo]] via pr-review/pr-triage on #551).
- **ISS-006 pocket-slot migration Day-9** — 08-09 batch-health OK 6/6 in tight 06:28–07:34Z cluster (late-pocket per [[morning-pocket-splits-into-two-de-facto-clusters]]); planner + heartbeat both user-triggered 07:35Z (~25min before scheduled 08:00Z slot) — no cron signal for those two this run. Bimodal delivery pattern holds; ISS-006 fix path unchanged. See [[fleet-ops]].
- **ISS-001 residue day 50** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged 44th steady-state day (passive advance, no 08-09 skill-health run). See [[fleet-ops]].
- **≥27 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #10 notegraph + one docs-pass) still proves end-to-end.
- **notegraph 08-09 DETERMINISTIC NO-OP (2nd consecutive day)** — fingerprint `92772443…` → `0e0328eb…` (corpus edits since 08-08 run) but regenerated graph byte-identical to HEAD `249n / 2493e / 1o / 73a` (post-08-08 late-day regen `3bcd2df` superseded morning state). Restored 4 generated files, updated `memory/state/notegraph.json`, no PR, no notify — validated silent-exit path per [[notegraph-extractor-generatedat-nondeterministic]].
- **pr-tracker 08-09 tuple `(1, 3, 2, 6)` — PARTIAL 2-of-4 predictor HIT; MISS driven by k-skill#547 unpredictable close** — vs 08-08 `(1, 3, 1, 7)` vs predicted `(1, 3, 1, 7)`. `recent_merges=1` ✓ + `stale_open=3` ✓ deterministic side clean; `closed_no_merge +1` / `active_open −1` both from **NomaDamas/k-skill#547 closed 08-08T12:48:51Z** (2h37min after yesterday's scan; 6h after metadata bump, no explanatory comment — matches [[maintainer-close-without-merge-triage-pattern]]). Clean-4-of-4 streak broken at length-1. Tomorrow's predictor `(1, 6, 2, 3)` — **lockstep cohort event** (ruvnet + block + jamiepine all cross 7d-frozen from 2026-08-02T18:29–18:33Z maintainer-sweep cluster) per [[same-day-file-cohort-stales-in-lockstep]]. **Archive-hide day 4** on PostHog/code#4007 (82h+, permanent-until-unarchive holds). First author-response on `0xprogrammable/aeon-launch-models#1` at 08-08T19:18Z (first-ever author response on a non-security bot PR). See [[pr-status]].
- **swarm PR queue 08-09 at 9 open (+1)** — new rsavitt #551 SHA `76772e5b` "feat(scenario): emergent ungoverned side-channel coordination (k5o9)" (1011+/6-, 9 files, 16 tests, 173-line writeup, sweep results). pr-review 38th invocation → **REQUEST_CHANGES 3/5** on #551 (CI `quality-gate` red from `test (3.12, full)` CANCELLED gw1 crash on unrelated moltipedia suite; PR itself mypy/ruff/short-tests clean, default-off, well-scoped). pr-triage 28th → DEFER (size 1017 > 500 rubric cap). Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5). Confirming counter 27 (review) / 28 (triage) — App-perm gap still open, DISTINCT from aeon-repo unblock.
- **suggest-edges FIRST-EVER run** — 3 proposals all similarity=1.00 from `memory/gitlawb-compute-futures-proofs/2026-06-24.md` → 06-27 / 07-04 / 08-03, all matching on the templated `cash / darkbloom / synthetic / basket / spread` scaffolding of the scenario-sweep proof file. New atomic [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] — templated dated series will spam sim=1.00 chains until threshold masks scaffolding. PR #22 opened.
- **compute-futures-eda 08-08 CSV analyzed on 08-09** — cross-mode 3-seed HIGH `maxCurve` cluster (basket + synth + x402 all 25%, anchored 202608083 + 202608087 + 202608088); super-anchor seed 202608083 spans 4 modes / 5 outlier positions; complete outlier-surface flip vs 08-07 (spread rotates OUT); new spread realizedAbs HIGH $88,319 + deepest single-row spread loss $-27,701; retail-role spread loss cluster n=3 sub-threshold (promotable to structural at n=4 on 08-09 filing). Correlation snap-back to zero-strong.
- **`.pending-disclosure/` queue steady at 6** — no 08-09 vuln-scanner run. Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band OR queue converts once merge path proves.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged (next milestone-tracker fire 2026-08-10 Mon 12:00Z). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker 6th silent-risk Mon slot is 2026-08-10 13:00Z (~29h from 08-09T07:35Z fire time)** per [[agi-tracker-missing-skill-md-dispatches-no-op]]. Planner rank-1 `agi-tracker-enabled-false-via-pr` (streak-3). Ship the PR today (Sun 08-09) to give operator a full day of buffer.
- **Never-dispatched 32nd consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09), [[github-app-cannot-fork-third-party-repos]] (08-08), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-4 persistence 08-09), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05), [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #23 opened today with correlation matrix update.

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Prove end-to-end merge flow — trigger FIRED today** — 15 open aeon-repo PRs, 0 merged in 48h+ since unblock. Planner rank-2 (new, streak-1) `aeon-repo-queue-merge-escalation` bundled with rank-1. Actions: (a) merge one low-risk PR — #10 (notegraph orphan flag, 50h+, dep-free) or the twin suggest-edges pair (#21+#22) — to prove flow; (b) walk down the queue + ≥27 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. See new atomic [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-1 today (streak-3 `agi-tracker-enabled-false-via-pr`, deadline Mon 2026-08-10 13:00Z is ~29h from 08-09T07:35Z fire). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Ship today (Sun 08-09).
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS`** — planner rank-3 today (streak-2). Hardcoded `{"aeonframework"}` but all 14 post-unblock aeon-repo PRs are authored by `app/github-actions`. #21+#22 form the first n=2 `suggest-edges` group; sweeper is silently a no-op on the growing queue. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner rank-3 today (streak-3). Today's log shows 5 same-day short-circuits (issue-triage + github-monitor + repo-revive + code-health + implicit changelog) — persistent 5th same-day cycle since 08-04.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **22nd-day carryover**. Scope: `ai-framework-watch` (Mon 08:30, 32d silent), `run-frequency-guard` (daily 23:00, 32d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model TWO migrated pockets (~06:11–06:22Z + ~09:00–09:58Z) and ONE persistent gap around planner's 06:30Z slot per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] should ease now that meta-block appears cleared. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **23 days past urgency threshold** as of 2026-08-09. 08-09 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**thirteenth consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-09), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]] (verified overriding dedup on 08-09 for k-skill#547 close), (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (clean-4-of-4 08-08 → partial 2-of-4 08-09 broken only by unpredictable maintainer close, not arithmetic drift); (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes (tomorrow's 08-10 predictor stress-tests lockstep at n=3); (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — **day 4 still hidden after 82h+** on PostHog/code#4007, permanent-until-unarchive holds. **[Viable now — 46d overdue as of 2026-08-09]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
