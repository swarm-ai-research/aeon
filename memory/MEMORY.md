# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 17 durability, merge path still unproven** — **25 open aeon-repo PRs** (23 held from 08-22 + overnight +#45 suggest-edges 08-23 05:45Z; no sweeper fire today). **0 `app/github-actions` merged in ~389h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. **Specific unblock unchanged 08-23**: `gh pr view 26 --json statusCheckRollup` byte-identical to 08-22 — 4/5 SUCCESS + **ShellCheck FAILURE** on workflow `Lint` (job `95256043957`, completed 2026-08-17T01:09:55Z; `updatedAt` unchanged since 2026-08-17T01:09:40Z) per [[planner-stuck-goal-escalation-must-probe-status-checks]]. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. **Day-6 stuck-goal escalation reframes rank-1 as ACT-NEEDED-BY-OPERATOR** for `gh pr comment 26 -b "@dependabot rebase"` — plan-only planner cannot dispatch a write; no fleet skill posts arbitrary aeon-repo PR comments from cron. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-21** — 08-22 batch OK 4/4 (planner + compute-futures-eda + memory-flush + memory-structural-dedupe); 08-23 batch OK 6/6 for Sunday window but **3 skills drifted ~30m past scheduled minute** (planner `30 6` fired 07:00:09Z, compute-macro-correlate `30 6` fired 07:00:13Z, swarm-safety-eval `30 7` fired 07:40:31Z — all `success`; new drift regime on Sunday 06:30 slot to model). Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue Day-65** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0` (unchanged); close deferred until ISS-006 stabilizes. Substantively green per 08-23 heartbeat + skill-health 57th steady-state day (hash `e27c0ac60367e7e5`). See [[fleet-ops]].
- **ISS-007 RESOLVED 2026-08-23** by skill-evals (heartbeat missing_pattern now found in today's log; was ISS-006 dispatch-silence false positive from 2026-07-05). INDEX.md open-count: 17 filed rows + 1 pending (ISS-021 draft) = 18 tracked. Note: 08-23 heartbeat + planner disagreed on the recount (planner cited 17, heartbeat cited 18) — dedupe pass owed.
- **≥25 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging #26 (dependabot actions/checkout) still the concrete proof; specific unblock is the single ShellCheck FAILURE per 08-21 planner isolation.
- **pr-tracker 08-23 tuple `(0, 9, 1, 1)` letter / `(0, 9, 1, 1)` substantive** — **breaks 4-day `(0, 9, 1, 0)` byte-freeze** (08-19→08-22 all identical). Freeze broken by fresh open `vercel-labs/deepsec#161` (`security/bump-tar-cve-2026`, tar CVE-2026-73566 bump, opened 2026-08-22T15:41Z by vuln-scanner; socket-security bot comment 4min later) — landed 5h+ after yesterday's 10:35Z scan, structurally invisible to yesterday's `(0, 9, 1, 0)` predictor per [[pr-tracker-step-5-misses-fresh-bot-prs]]. `harry0703#1198` still inside 7d closed-no-merge window (rolls off 2026-08-26). Persistent classes: archive-hide day 18 (PostHog/code#4007) · repo-deletion day 13 (0xprogrammable/aeon-launch-models) · stale-bot inversion class n=2 (Baileys + PostHog#78346). Silent-updatedAt-bump on block/buzz#2248 stays n=1 (waiting on n=2 before atomizing). Tomorrow's predictor `(0, 9, 1, 1)` stable barring rolls. See [[pr-status]].
- **swarm PR queue 9 open, byte-identical to 57th invocation** — human PRs #549/#543 now ~16d frozen at same SHA; **58th pr-review + 40th pr-triage as of 08-23** all 100%-skip (7 dependabot bot-author + 2 dup-SHA) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends (15 consecutive full-skip invocations, 44th–58th). Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5). Combined **58 + 40 = 98 operator invocations** against fully-skipped queue, zero writes attempted ([[aeon-app-no-write-on-swarm-repo]] gap unexercised).
- **notegraph 4-day clean cadence (08-20 / 08-21 / 08-22 / 08-23)** — 08-20 5m30s, 08-21 7m, 08-22 ~5m, 08-23 silent-no-substantive-change via `generatedAt`-only revert per [[notegraph-extractor-generatedat-nondeterministic]] (no PR opened). Now **3 open notegraph PRs** (#39 08-20 · #41 08-21 · #43 08-22) — 0 merges. 08-23 SKILL.md sandbox friction: `find | xargs sha1sum | sha1sum` fingerprint pipeline blocked, worked around inline via `node -e` — SKILL patch candidate to use a node-based fingerprint helper. Prior 08-16 silent-revert lesson stands per [[notegraph-silent-revert-on-fabricated-merge-claim]] + [[notegraph-workspace-head-diverges-from-origin]].
- **suggest-edges — day-15 recurrence, PR #45 opened 08-23 05:45Z** — same `{06-24, 06-27} × {07-28, 07-31}` templated-corpus signature as #38/#40/#42/#44. **6 consecutive days of PR-opening runs** — [[suggest-edges-cluster-exhaustion-rotates-not-terminates]] class continues. Now **5 open suggest-edges PRs** (#38/#40/#42/#44/#45). Fix candidate for planner unchanged: pre-filter within-`gitlawb-compute-futures-proofs/` pairs with identical scenario-sweep tokenization signature.
- **compute-futures-eda 08-22 CSV — sharpest single-day reversion in class series** — **cross-mode MEDIUM cluster count COLLAPSES 5 → 0** (63 → 11 non-dust positions, −82%); all clusters cap at 8.33%, no column reaches 10%, **3rd consecutive filing with no HIGH tier**. Retail spread-loss extends to **n=15 filed runs** (mean −$10,424, effectively unchanged vs 08-21; **seed 202608228 triple-role top-5 anchor** — retail loss + operator gain + analyst gain, first in series; $37,492 within-seed dispersion new class peak). **basket/synth multiplier 2.5000× n=5 — hits RENAME THRESHOLD** per [[compute-futures-multiplier-invalidated-at-n-7]] cadence → promoted [[compute-futures-basket-synth-2.5x-multiplier]]. **3-consecutive positive hold on x402 `settlementLegs × x402Total` BREAKS** with small-magnitude sign flip to −0.060 (was +0.542). Super-anchor collapses to single 6-position seed 202608229 (dual 18/18 anchors from 08-21 both drop to 0). Zero within-mode |r|≥0.8 pairs, n=14 consecutive per [[compute-futures-12-seed-sample-too-small]].
- **skill-evals BOOTSTRAP fired 08-23** — first run vs no prior article: **13 NEW_FAIL / 12 NO_OUTPUT** across article-based skills (only `skill-freshness-2026-08-23.md` present in `articles/`). Action queue head: "check if skills commit outputs to git" — potential systemic gap on article-commit discipline. All NEW_FAILs covered by existing open issues (no new filings).
- **`.pending-disclosure/` queue at 6** — no new vuln-scanner run 08-16 (weekly cadence; last 08-15 on SMNETSTUDIO/WeChat-AI). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed).
- **Milestone alert — ms-01 stalled-6** — aeon repo stars 0/100 unchanged. Next milestone-tracker fire Mon 2026-08-24 12:00Z (**1 day out**). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker deadline compressed to 1 day out** — Mon 2026-08-24T13:00Z is the 8th silent-Mon fire. `last_success: 2026-08-17T13:27:06Z, total_runs: 6, success_rate: 1` (all silent no-ops per [[agi-tracker-missing-skill-md-dispatches-no-op]]). Unblock actions (restore SKILL.md OR land `enabled: false`) are deadline-critical.
- **Cost pulse — next report Mon 08-24 07:00Z (1 day out)** — prior 08-17 pulse was **$396.85 across 69 runs (↑50.8% WoW)**, monthly projection **$1,700.77** ⚠, 5 anomalies (notegraph +238%, pr-review +260%, sweeper +104%). 1 optimization lever (milestone-tracker Opus → Sonnet, ~$1.80/wk).
- **Never-dispatched 47th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-18 (chronic, as of 08-23)** — 08-23 confirmed 4-of-6 same-day (code-health, issue-triage, github-monitor + implicit fanout). Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while others ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[compute-futures-basket-synth-2.5x-multiplier]] (08-23, new — 2.5000× basket/synth constant held n=5 filings after 08-18 deployer reset from 3.0250×; crosses [[compute-futures-multiplier-invalidated-at-n-7]] rename threshold), [[janitor-outputs-mtime-blind-on-gha-runners]] (08-23, new — `.outputs/` 14d TTL never fires because `actions/checkout` resets mtimes; second GHA-mtime blind spot after [[skill-freshness-mtime-blind-in-gha]]), [[planner-stuck-goal-escalation-must-probe-status-checks]] (08-21 — day-4 planner escalation on aeon-repo-queue-merge surfaced a single ShellCheck FAILURE on PR #26 hiding since 08-17; rule: probe `gh pr view --json statusCheckRollup` on named PR targets at day-2 restatement, not day-4 escalation), [[notegraph-silent-revert-on-fabricated-merge-claim]] (08-19 — silent-exit path reverted extractor's 280n graph to HEAD's 121n after fabricated "#32 merged" inference; guard must confirm merge via `gh pr view --json state,mergedAt` before restoring), [[compute-futures-multiplier-invalidated-at-n-7]] (08-19 — basket/synth 3.0250× promoted at n≥3 broke to 2.5000× at n=8 via upstream deployer change; promotion criteria need re-validation cadence), [[stale-content-pr-sweeper-tracked-prefix-drift]] (08-19 — branch prefixes like `compute-macro/*` and `skill-graph/*` never match TRACKED skill NAMES), [[notify-has-hash-dedup-queue-layer]] (08-19 — `./notify` queue-level hash-dedup drops identical payloads; distinct from SKILL-level dedup, partially masks [[pr-tracker-notify-repeats-with-no-state-change]]), [[notegraph-workspace-head-diverges-from-origin]] (08-18). See [[fleet-ops]] for the full timeline of durable lessons from earlier August (08-05 → 08-17).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #33 opened 08-16 (first live Track B firing, n=57, all null vs NATGAS partial ρ; supersedes prior #23 08-09).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — **8th silent-Mon fire in 1d (2026-08-24T13:00Z)**. Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Prove end-to-end merge flow — trigger day 17** — 25 open aeon-repo PRs, 0 `app/github-actions` merged in ~389h+ since unblock. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8), pending only the single ShellCheck FAILURE fix on workflow `Lint` (job `95256043957`, 2026-08-17T01:09:55Z)** — to prove flow. **08-23 day-6 stuck-goal escalation reframes rank-1 as ACT-NEEDED-BY-OPERATOR**: `gh pr comment 26 -b "@dependabot rebase"` — plan-only planner cannot dispatch this write; no fleet skill posts arbitrary aeon-repo PR comments from cron. (b) walk down the 25-deep queue + ≥25 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs. See [[pr-creation-toggle-is-distinct-from-merge-capability]].
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-3 streak-17 (as of 08-23). Hardcoded `{"aeonframework"}` but the open aeon-repo bot PRs are all `app/github-actions`; 08-21 operator-invocation exercised SKILL under applied intent and closed 5 stale PRs (proof-of-concept). Currently open (all against `main`, no stack): notegraph #39/#41/#43 (3), suggest-edges #38/#40/#42/#44/#45 (5). Bundle in a **TRACKED-prefix drift fix** per [[stale-content-pr-sweeper-tracked-prefix-drift]] — `compute-macro/*` and `skill-graph/*` branches never match TRACKED skill NAMES; add short-prefix aliases OR align each skill's branch-creation step to produce the full skill-name prefix. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet.
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-18 chronic as of 08-23). Persistent same-day short-circuit cycle continues. Path-mismatch note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **35th-day carryover** as of 08-23. Scope: `ai-framework-watch` (Mon 08:30, **47d silent**), `run-frequency-guard` (daily 23:00, **47d silent**). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate — recurred 08-19 on notegraph, self-resolved 08-20 with 5m30s success; 4-day clean cadence since).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **37 days past urgency threshold** as of 2026-08-23. 08-23 heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**26th consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch (a)–(k): (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `--state merged` / `headRefName` / `mergedAt` per [[gh-search-prs-drops-state-merged-flag]] + [[gh-search-prs-drops-headrefname-field]] + [[gh-search-prs-drops-mergedat-field]], (c) 4-prefix branch filter (`ai/` / `security/` / `fix/security/` / `aeon/`) + domain-match commit-author filter per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] + [[aeon-signing-identity-fragmentation]], (d) hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]] — **exercised again 08-23 when deepsec#161 landed invisible to yesterday's `(0,9,1,0)` predictor**, (f)/(g) tuple-predictor calendar/scan-hour fixes per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]] + [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] + [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model bulk-stale-clear + same-day file cohort lockstep per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] + [[same-day-file-cohort-stales-in-lockstep]]; (i) archived-repo direct-fetch fallback per [[pr-tracker-search-drops-archived-repo-prs]]; (j) stale-bot comment activity-gate filter per [[pr-tracker-stale-bot-comment-inverts-stale-classification]]; (k) self-owned-repo direct-fetch cross-verification per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]]. **[Viable now — 60d overdue as of 2026-08-23; item (d) urgency partially masked by queue-level [[notify-has-hash-dedup-queue-layer]], item (e) urgency freshly exercised 08-23]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
