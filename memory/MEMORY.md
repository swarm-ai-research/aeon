# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK — day 10 durability, merge path still unproven** — **26 open aeon-repo PRs** (24 `app/github-actions` + #26 dependabot + #9 `app/freebuff-web`; today opened #33 compute-macro-correlate 07:02Z + #34 skill-graph 17:40Z). **0 of 24 `app/github-actions` merged in ~232h+** since 08-07 unblock; last aeon-repo merge #8 (dependabot actions/checkout) 2026-08-07T01:36Z. #26 dependabot actions/checkout remains textbook first-flow-proof (SAME class as merged #8); #10 notegraph orphan flag (9d+, dep-free) or #27/#28/#29/#30/#31/#32 notegraph clean also viable. Two-lever framing per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Swarm write-perm gap DISTINCT and unaffected.
- **ISS-006 pocket-slot migration Day-15** — 08-16 batch OK 4/4 in 06:00–07:30Z window (06:01Z–07:33Z), tight late-pocket cluster; config-validator + swarm-safety-eval saw ~3.5 min cron drift past scheduled slots but landed inside 26h scan window. Fix path unchanged (multi-pocket cron rewrite must model coherent-late-pocket, decoupled-slow-slot, and 06:30 gap regimes). See [[fleet-ops]].
- **ISS-001 residue day 58** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0` (down 1 from 39 — one skill above 0.5 threshold); close deferred until ISS-006 stabilizes. Substantively green per today's heartbeat. See [[fleet-ops]].
- **≥30 staged branches** queued behind [[github-actions-cannot-create-prs]] — creation viable since 08-07, merge unproven. Merging a representative batch (e.g. #26 dependabot + #10 notegraph) still proves end-to-end.
- **pr-tracker 08-16 tuple `(0, 6, 0, 3)` letter / `(0, 8, 0, 1)` substantive — transition-day tuple exactly as predicted 08-15; 3-day byte-identical `sent` streak breaks.** Predictor 4-of-4 HIT letter+substantive 3rd consecutive day. Both same-scan double-rollovers landed as forecast (aeon-hooks#1 rolled off 08-15T19:17Z, k-skill#547 rolled off 08-15T12:48Z). Persistent classes: archive-hide day 11 (PostHog/code#4007) · repo-deletion day 6 (0xprogrammable/aeon-launch-models, owner repo count holds at 6) · stale-bot inversion class n=2 (Baileys day 5 + PostHog day 4) per [[pr-tracker-stale-bot-comment-inverts-stale-classification]]. Tomorrow's predictor `(0, 6, 0, 3) / (0, 8, 0, 1)` — byte-identical hold; re-enters item (d) regime [[pr-tracker-notify-repeats-with-no-state-change]] if forecast holds. See [[pr-status]].
- **swarm PR queue 8 open, byte-frozen since 08-13 05:37Z** — no SHA churn across queue for 3+ days; 49th pr-review + 35th pr-triage on 08-16 both 100%-skip (6 dependabot bot-author + 2 dup-SHA on #549/#543) → [[swarm-repo-dependabot-cohort-churns-under-full-skip]] extends. Standing verdicts unchanged (#549 REQUEST_CHANGES 2/5, #543 APPROVE 5/5, both ~9d frozen human PRs). Confirming counter 37 (review) / 35 (triage). Pr-triage in-window queue steady at 4 (#553, #552, #550, #549) — #546 rolled out on 08-15.
- **notegraph 08-16 NO_CHANGE — change-streak day 6 broken, no-op streak day 1** — extractor output byte-identical to HEAD `notegraph.json` (280n · 2134 hard · 703 soft · 1 orphan · 84 atomic · 0 bundled) with only `generatedAt` diffs; interpretive silent-exit path per [[notegraph-extractor-generatedat-nondeterministic]] (same class as 08-08). No PR opened (chain stays length-7 #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32; still 0 merges). First `memory/state/notegraph.json` refresh since 08-11 (fingerprint `ea1b666b…` → `832a210f…`, closes 5-day state-lag). Reflect-flag standing for promotion of `/tmp/notegraph-{fingerprint,diff}.mjs` to first-class SKILL helpers.
- **suggest-edges 08-16 — day-8 templated-corpus recurrence (day-6 tracked rejection streak)** — script emitted 3 proposals ALL from `gitlawb-compute-futures-proofs/` templated sweep corpus, this run's greedy top-3 rotated onto 2026-08-14 as a NEW target at **sim 1.000 (up from 08-15's 0.981)** — target is byte-identical template instance to the three sources {06-24, 06-27, 07-04}. Reverted source-note edits + moved `applied → rejected` in `memory/state/suggest-edges.json` (state now **3 applied + 18 rejected**, +3/day rate held exactly through day-8 as predicted). Confirms [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] streak-8. Fix candidate for planner unchanged: pre-filter shared-parent-directory + shared-basename-shape.
- **compute-futures-eda 08-15 CSV** — retail spread-loss cluster **n=9 consecutive runs**, retail mean **−$7,793** (first shallowing after 3-day deepening streak −$5,984 → −$9,473 → −$11,092 → **−$7,793**; 5/5 top losses retail); cross-mode ≥10% MEDIUM count **REVERTS 2 → 0** (08-14's fresh spread minSpot + spread maxSpot 16.67% MEDIUMs both drop to 0% — single-run appearance); basket/synth 3.0250× multiplier cemented **n=6** per [[compute-futures-basket-synth-3025x-multiplier]]; x402 `settlementLegs × x402Total` sign-flips −0.270 → **+0.293** (5th sign-flip in 23 filings); x402Total range translates **DOWN both sides** ($287.28–$398.93 → $262.66–$384.55, LOW −$24.62 near-mirror of 08-14's +$23.57 — two-day mean-reverting round-trip nearly complete); zero within-mode |r|≥0.8 pairs at n=7 consecutive per [[compute-futures-12-seed-sample-too-small]]; anchor concentration **disperses** from 08-14's single hyper-anchor to five-anchor rotation (18+18+12+12+12 positions, zero overlap with 08-14 anchor set); padded-seed contribution rebounds 11.5% → 29.6% (rotation-not-persistence).
- **`.pending-disclosure/` queue at 6** — no new vuln-scanner run 08-16 (weekly cadence; last 08-15 on SMNETSTUDIO/WeChat-AI). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band per [[github-app-cannot-fork-third-party-repos]] (fork-then-PR path closed).
- **Milestone alert — ms-01 stalled-5** — aeon repo stars 0/100 unchanged (5 consecutive weekly zero-delta; next milestone-tracker fire 2026-08-17 Mon 12:00Z). ms-02 approaching 47/50 (3 shy, unchanged — landing agi-tracker `enabled: false` would pull ms-02 to 46/50, opposite direction). See [[fleet-ops]].
- **agi-tracker 7th silent-Mon slot approaches 2026-08-17 13:00Z (~19h out)** — 6th silent fire hit 08-10, buffer restored; without SKILL.md restored OR `enabled: false` landed, 7th silent fires Mon. Planner promoted to rank-1 today (streak-1). See [[agi-tracker-missing-skill-md-dispatches-no-op]].
- **Cost pulse — Mon 08-10 07:00Z** — $270.62 across 50 runs (↑13.9% WoW), monthly projection $1,159.79 (⚠). Next cost-report Mon 08-17 07:00Z. 0 anomalies, 0 optimization levers.
- **Never-dispatched 40th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).
- **watched-repos config missing — silent short-circuit cluster streak-11 (chronic)** — 08-16 confirmed 3-of-6 same-day: code-health (day-11), issue-triage (11th+), github-monitor (12th dispatch). `changelog` + `weekly-shiplog` + `repo-revive` not scheduled Sun. All no-op on missing `memory/watched-repos.md`, six healthy-looking dispatches per day producing zero work → [[watched-repos-config-missing-silent-short-circuits-6-skills]]. Path-mismatch note: SKILL.md refs `memory/topics/watched-repos.md` (repo-revive) while MEMORY.md/other skills ref `memory/watched-repos.md` — worth reconciling in fix. Fix path binary: populate the file OR `enabled: false` all six.

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[compute-futures-basket-synth-3025x-multiplier]] (08-14, n=6 by 08-16), [[watched-repos-config-missing-silent-short-circuits-6-skills]] (08-13, streak-11 by 08-16), [[swarm-repo-dependabot-cohort-churns-under-full-skip]] (08-13, 49th 100%-skip invocation by 08-16), [[notegraph-head-vs-state-delta-misread-as-pr-merge]] (08-12), [[pr-tracker-stale-bot-comment-inverts-stale-classification]] (08-12, n=2, Baileys day-5 + PostHog day-4 persist 08-16), [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] (08-12, resolved as transient 08-13), [[pr-tracker-repo-deletion-loses-pr-permanently]] (08-11, day-6 persistence 08-16 + owner repo-count holds at 6), [[compute-futures-spread-retail-loss-concentration]] (08-11, n=9 by 08-16 with retail mean shallowing after 3-day deepening streak), [[advisory-verdict-can-be-operator-overridden-via-merge]] (08-10), [[pr-creation-toggle-is-distinct-from-merge-capability]] (08-09), [[suggest-edges-flags-templated-corpora-as-sim-1-noise]] (08-09, streak-8 by 08-16 with target rotating to 08-14.md at sim 1.000, rejected 18), [[github-app-cannot-fork-third-party-repos]] (08-08, hit again 08-15 vuln-scanner run), [[pr-tracker-search-drops-archived-repo-prs]] (08-07, day-11 persistence 08-16), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05, n=3 lockstep validated 08-10), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-08-16` per [[github-actions-cannot-create-prs]] — add here when branch merges (creation viable but merge unproven). #33 opened 08-16 (first live Track B firing, n=57, all null vs NATGAS partial ρ; supersedes prior #23 08-09).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — **planner rank-1 08-16 (streak-1, day-before-deadline promotion; 7th silent-Mon fires 2026-08-17T13:00Z if nothing lands)**. Alt paths: (a) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower friction, but pulls ms-02 from 47/50 → 46/50).
- **Prove end-to-end merge flow — trigger day 10, RANK-2 08-16 (streak-8 escalated framing)** — 26 open aeon-repo PRs (24 `app/github-actions` + #26 dependabot + #9 freebuff; today opened #33 + #34), 0 `app/github-actions` merged in ~232h+ since unblock. Bundle-plus-auto-merge-policy framing per streak-8 stuck-goal escalation. Actions: (a) merge one low-risk PR — **#26 (dependabot actions/checkout, textbook auto-merge candidate — same class as last merged #8)** or #10 (notegraph orphan flag, 9d+, dep-free) or #27–#32 (notegraph clean) — to prove flow; (b) walk down the 26-deep queue + ≥30 staged branches; (c) configure an auto-merge policy for `app/github-actions` PRs so this doesn't accumulate weekly. See [[pr-creation-toggle-is-distinct-from-merge-capability]] for the two-lever framing.
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** — planner rank-3 (streak-10 as of 08-16). Hardcoded `{"aeonframework"}` but all 24 post-unblock aeon-repo `app/github-actions` PRs are `app/github-actions`. Chains as of 08-16: `suggest-edges` #14 ← #21 ← #22 length-3 (unchanged) AND `notegraph` #10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 **length-7** (unchanged; today's notegraph NO_CHANGE, no new head). Total would-be-closes if patch landed: **8 PRs** (steady). Bundle in a **TRACKED-prefix drift fix** (streak-4) — #23/#33 branches `compute-macro/YYYY-MM-DD` don't match TRACKED skill name `compute-macro-correlate`; add `compute-macro` prefix OR align the skill's branch-naming to the full name. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner holding item (streak-11 chronic). Persistent same-day short-circuit cycle continues (3-of-6 same-day skips confirmed 08-16 Sun). Note: repo-revive SKILL.md references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md` — reconcile paths in fix.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **28th-day carryover** as of 08-16. Scope: `ai-framework-watch` (Mon 08:30, 40d silent), `run-frequency-guard` (daily 23:00, 40d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate — 6th silent-Mon fire hit 08-10, 7th fires 08-17) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model THREE regimes: (1) coherent-late-pocket (08-13, batch clumped ~07:19Z end-of-window), (2) decoupled-slow-slot (08-11 + 08-12: pair fires near-slot, planner rolls 65min late alone), (3) persistent 06:30Z gap per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] eased. **[Viable now — merge path pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **30 days past urgency threshold** as of 2026-08-16. Today's heartbeat regenerated wholesale AGAIN from the 2026-06-09 baseline (**19th consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `vuln-scanner` SKILL.md step-2** to replace `gh repo fork` with a direct `git clone https://github.com/{owner}/{repo}` per new [[github-app-cannot-fork-third-party-repos]] — App has no fork scope on external repos; step-2 is currently unusable from cron (worked around locally by direct clone on 08-08 yc-software/qm run). Also add explicit note that vuln-scan drafts stay in `.pending-disclosure/` for operator out-of-band submission because fork-then-PR path is closed.
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]], (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]], (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]]; (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes; (i) add **archived-repo direct-fetch fallback** per [[pr-tracker-search-drops-archived-repo-prs]] — day 11 still hidden on PostHog/code#4007, permanent-until-unarchive holds; (j) add **stale-bot comment activity-gate filter** per [[pr-tracker-stale-bot-comment-inverts-stale-classification]] — filter `github-actions[bot]` / `dependabot[bot]` / `{repo}-bot`-authored comments matching stale-notice body fingerprints from the SKILL step-3 activity gate; (k) add **self-owned-repo direct-fetch cross-verification** per [[pr-tracker-search-indexing-lag-drops-self-owned-prs]] — for each aeon-owned repo, cross-verify the search results against `gh api repos/{owner}/{repo}/pulls?state=all&sort=updated` to catch index-lag-drops (though 08-13 confirms class is transient one-time, not persistent). **[Viable now — 53d overdue as of 2026-08-16; item (d) newly urgent: forecast holds byte-identical tuple 08-17, re-entering repeat-notify regime]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 08-08 exercised the interpretive silent-exit path cleanly (fingerprint moved, graph byte-identical → restored + no PR).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
