# Memory Index

Pointer-only index. Durable claims live in `memory/notes/`, organized by topic MOCs in `memory/topics/`. Daily activity in `memory/logs/`. Structured issues in `memory/issues/`.

## Current focus
- **Operator PR-authoring UNBLOCK holding into 08-08** — first-ever `app/github-actions` PR authorship on this repo. Queue grew across 4 bursts on 08-07 (05:16–05:50Z #10-#13 · 06:34Z #14 first-ever suggest-edges · 09:07-09:12Z #15-#17 · 13:19-13:24Z #18-#20) then continued into 08-08 morning (#21 suggest-edges 06:00Z) — **13 open aeon-repo PRs as of 08-08 morning** (12 `app/github-actions` + #9 `app/freebuff-web`). Dependabot #8 MERGED 08-07T01:36Z after standing since 07-27. Prior 42-day meta-block cleared; mechanism (Repo Settings toggle vs `AEON_GH_PAT`) still unconfirmed. **End-to-end merge flow unproven — 0 of 12 `app/github-actions` PRs merged yet** (all `mergeStateStatus: UNKNOWN`). `elevate-repo-settings-toggle-active` dropped from streak-tracked planner slot. **swarm-ai-research/swarm write-permission gap DISTINCT from aeon-repo unblock** and unaffected — App-perm bump / PAT still needed there (26th confirming invocation of [[aeon-app-no-write-on-swarm-repo]]).
- **ISS-006 pocket-slot migration Day-7** — 08-07 heartbeat fired 09:08Z, ~1h08m late in 08:00Z pocket (same signature as 08-05); **second confirmed 48h heartbeat gap** in this window (08-06 08:00Z slot fully missed). Planner 08-07 delivered at 06:33:36Z (~3min late in early cluster per [[morning-pocket-splits-into-two-de-facto-clusters]]) — best on-time delivery in 5 slots, silent-3 streak broken. Bimodal delivery pattern per same lesson still holds; ISS-006 fix pattern unchanged. See [[fleet-ops]].
- **ISS-001 residue day 48** — 38 skills at `success_rate < 0.5` with `last_status: success` + `consecutive_failures: 0`; close deferred until ISS-006 stabilizes. Skill-health hash `e27c0ac60367e7e5` unchanged 42nd steady-state day. See [[fleet-ops]].
- **≥26 staged branches** queued behind [[github-actions-cannot-create-prs]] (blocker appears cleared 08-07 but not confirmed by branch merge yet). Once branch merge lands, mechanism confirms. See rank-1/rank-2 pointers.
- **notegraph 08-07 reflect regen `247n / 1854h / 667s / 0o / 72 atomic`** (fingerprint moved through 08-08 to `92772443…`; delta vs prior 08-05 baseline: +13 nodes, +78 hard, +36 soft, +6 atomic, 0 new orphans). 08-08 notegraph fired but produced **byte-identical graph to HEAD** — corpus churn (memory/docs edits) without graph impact; deterministic-no-op path exercised, notify muted, no PR staged (healthy signal per skill design). Earlier 08-07 orphan-refresh landed as PR #10 (still open, unmerged).
- **pr-tracker 08-07 tuple `(2, 3, 2, 7)`** — vs 08-06 `(2, 3, 2, 6)` vs predicted `(2, 3, 2, 6)`. **Predictor 3-of-4 clean HITs + 1 MISS on active** (novel arrival `0xprogrammable/aeon-launch-models#1` unmodeled) — **best predictor showing since 08-02**. First clean scan-vs-cutoff-hour HIT since [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] filed (cindy#1116 14:39Z rolloff correctly deferred). Novel signals: **first non-security bot PR** (0xprogrammable#1 spec/draft NoOp/CapGate/DynamicFee), **first byte-identical trigger set** since dedup guard came online (fresh-file override fired notify), **archive-hide persistent day 2** per new [[pr-tracker-search-drops-archived-repo-prs]] (PostHog/code#4007 34h+ later still search-omitted), **numeric-prefix noreply variant** per new [[aeon-noreply-numeric-prefix-is-formatting-variant]] (formatting-only, five identities still). See [[pr-status]].
- **swarm PR queue advanced 08-07 · #536 MERGED** — pr-review invoked twice same-day (**first same-day double on this repo**, 34th at ~09:00Z + 35th at ~19:00Z). Morning 34th: all 3 rsavitt PRs got new SHAs, byte-identical-13-day streak snapped. #549 (`afe51248`) REQUEST_CHANGES 2/5 (3 metric-correctness findings on Prime Agent bridge: unbounded recursion at `bridge.py:674`, aggregate-vs-per-refinement metric skew at `bridge.py:712`, unauthenticated `edit.applied` trust at `harness.py:2342`); #543 (`bad79e48`) APPROVE 5/5 (docs-only); #536 (`c313204d`) APPROVE 5/5. Afternoon 35th: **#536 MERGED at 16:06:19Z** under operator override (mergeCommit `a6e1d93f`) — **first rsavitt-queue merge since 07-18 swarm#527, snapping the 20-day rsavitt-merge drought**; morning APPROVE 5/5 verdict held. Swarm queue now 8 open (was 9). pr-triage adds distinct 25th confirming from a different endpoint. Both write endpoints 403 on all 3 morning → **26th confirming invocation** of [[aeon-app-no-write-on-swarm-repo]] (35th added no new confirmations — dup-SHA skip, no writes attempted). App-perm gap on swarm still open — DISTINCT from aeon-repo unblock.
- **compute-futures-eda 08-07** — spread mode holds ALL fresh MEDIUM fires: `settlementLegs` 16.67% (2nd-consecutive with seeds ROTATING 202608076 + 2026080710), `minSpot` 16.67% (both padded-seed cohort — hint for padding-fix PR per [[compute-futures-seed-padding-bug]]), `maxSpot` 16.67% (bimodal 202608071 low + 202608074 high). LOW fresh **spread `realizedAbs × maxCurve` = +0.897** — first \|r\|≥0.8 within-mode fire since 08-03; zero-firing streak snaps at n=1. 08-06's HIGH spread `maxCurve` 25% → 0% (22nd rotation-not-persistence confirmation). Anchor pattern DISSOLVES (08-06 n=4 top-15 → 08-07 n=8+ diffuse, top at 6). Conservation healthy every mode.
- **`.pending-disclosure/` queue holds at 4** — unchanged from 08-02 (aging now +5 days). Directory only exists on unmerged vuln-scanner branches; operator submits out-of-band OR queue converts to draft PRs once unblock mechanism confirms.
- **Milestone alert — ms-01 stalled-4** — aeon repo stars 0/100 unchanged (last milestone-tracker fire 08-03 Mon 12:00Z; next fire 2026-08-10 Mon). ms-02 enabled skills 47/50 approaching (3 shy, unchanged). See [[fleet-ops]].
- **agi-tracker 6th silent-risk Mon slot is 2026-08-10 13:00Z** per [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class holds). Planner 08-07 promoted `agi-tracker-enabled-false-via-pr` as rank-1 (streak-1 new name; same 2026-08-10 deadline as prior `direct-edit` framing but now viable end-to-end via PR).
- **Never-dispatched 30th consecutive day**: `ai-framework-watch` (weekly Mon 08:30) + `run-frequency-guard` (daily 23:00) per [[enabled-skills-can-never-dispatch]] (cron-state absence).

## Topics
- [[agi-tracker]] — frontier-agent capability tracking + Aschenbrenner *Situational Awareness* scoring
- [[fleet-ops]] — OAuth outage, morning-batch silence, monitor/repair coupling, GitHub App perms, workflow-security-audit BOOTSTRAP, HEALTHY-but-empty class
- [[compute-pulse]] — inference pricing, hardware deals, DePIN tokens (weekly snapshot)
- [[surplus-pulse]] — surplus-mode pricing simulator runs
- [[pr-status]] — cross-repo PR queue for the aeonframework author
- [[aeon-signing-identity-fragmentation]] — five aeon-bot signing identities; domain-match filter + PR-class rotation hypothesis

*Recent lessons (see [[fleet-ops]] for full timeline):* [[pr-tracker-search-drops-archived-repo-prs]] (08-07, persistence confirmed), [[aeon-noreply-numeric-prefix-is-formatting-variant]] (08-07), [[planner-silenced-by-its-own-escalation-target]] (08-05), [[same-day-file-cohort-stales-in-lockstep]] (08-05), [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (08-05), [[morning-pocket-splits-into-two-de-facto-clusters]] (08-05), [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] (08-04), [[pocket-slot-migration-confirmed-by-3-day-sustained-late-delivery]] (08-03).

*Pending topic:* `memory/topics/compute-futures-macro-correlations.md` on unmerged branch `compute-macro/2026-07-19` per [[github-actions-cannot-create-prs]] — add here when branch merges (now unblocked pending durability confirmation).

## Conventions
- Atomic notes: one claim per file, ≤3 sentences, frontmatter (`id`, `created`, `type`, `links`).
- Topic files in `memory/topics/` are MOCs — pointers + inline snapshots only.
- Daily indexes at `memory/notes/daily/${date}.md`.

## Pointers
- `aeon.yml` — skill schedule, models, chains.
- `articles/` — agent-authored long-form output.
- `memory/cron-state.json` — per-skill success/failure counters.
- `memory/token-usage.csv` — per-run token accounting.

- **Confirm the 08-07 unblock is end-to-end durable** — queue grew to 13 open aeon-repo PRs across 08-07/08-08 (12 `app/github-actions` + 1 `app/freebuff-web`) but **0 of 12 `app/github-actions` PRs merged yet** (all `mergeStateStatus: UNKNOWN`). Open path (PR-creation) confirmed; merge path unproven. Actions: (a) identify mechanism (Repo Settings toggle vs `AEON_GH_PAT`) — check `Settings → Actions → Workflow permissions` or presence of `AEON_GH_PAT`; if PAT, document env var; (b) merge a small representative batch (e.g. #10 notegraph-orphan + one docs-pass) to prove end-to-end flow; (c) walk the queue down from 13 open + ≥26 staged branches.
- **Land `enabled: false` on `aeon.yml:188` for `agi-tracker` via PR** per [[agi-tracker-missing-skill-md-dispatches-no-op]] — planner rank-1 today (streak-1 new name `agi-tracker-enabled-false-via-pr`, same 2026-08-10 13:00Z deadline as prior `direct-edit` framing but now viable end-to-end via PR). Lower-friction than authoring a full SKILL.md. Alt path: author a SKILL.md matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape. Next slot 2026-08-10 13:00Z will be 6th silent unless action lands.
- **Populate `memory/watched-repos.md` OR disable the 6 watched-repos-dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — planner rank-3 today (streak-2). Today's log shows the same 3 same-day short-circuits (issue-triage + github-monitor + code-health) — persistent 4th same-day cycle since 08-04.
- **Draft ISS-021** for [[enabled-skills-can-never-dispatch]] (category `config`, severity `high`) — **20th-day carryover**. Scope: `ai-framework-watch` (Mon 08:30, 30d silent), `run-frequency-guard` (daily 23:00, 30d silent). Sibling candidates: [[agi-tracker-missing-skill-md-dispatches-no-op]] (HEALTHY-but-empty class, ISS-022 candidate) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023 candidate lower urgency).
- **ISS-006 fix**: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. Model TWO migrated pockets (~06:11–06:22Z + ~09:00–09:58Z) and ONE persistent gap around planner's 06:30Z slot per [[morning-pocket-splits-into-two-de-facto-clusters]]. Planner silencing per [[planner-silenced-by-its-own-escalation-target]] should ease now that meta-block appears cleared. **[Was blocked by Repo Settings toggle for branch merge — now viable pending durability confirmation]**
- **Address workflow-security-audit findings** (85 total, first run 07-19): (a) pin the 3 aeon.yml `actions/*` refs to SHAs (Critical); (b) create `production` and `chain-runner` GitHub Environments and move sensitive secrets (`GH_GLOBAL`, `GITLAWB_*_PEM`, `AEON_PRIVATE_PAT`, `CLAUDE_CODE_OAUTH_TOKEN`) from repo-scoped to environment-scoped (36 High); (c) address 11 `zizmor/artipacked` Mediums (`persist-credentials: false` on read-only checkouts).
- **Stage `docs/status.md` snapshot-rebase gate** — clobber pattern per [[snapshot-rebase-clobbers-docs-status-md]], **21 days past urgency threshold** as of 2026-08-07. 08-07 heartbeat regenerated wholesale AGAIN from the 55+-days-stale 06-09 baseline (**eleventh consecutive** rebase-clobber-then-regen). Two-part fix: (a) audit heartbeat's auto-commit `git add` glob to include `docs/`; (b) exclude `docs/status.md` from snapshot merges OR gate snapshot pull on upstream carrying a `docs/status.md` newer than main's HEAD.
- **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS`** — hardcoded `{"aeonframework"}` but all 12 new aeon-repo PRs post-unblock (08-07/08-08) are authored by `app/github-actions`. Under strict allowlist, sweeper is a no-op post-unblock; harmless today because tracked groups (notegraph, suggest-edges) currently each have only 1 open entry, but next time two date-stamped `notegraph/*` or `suggest-edges/*` PRs coexist the older one will not be closed. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in step-1 snippet (viable as PR now that authoring works).
- **Standardize notification emission** across SKILL.md files on direct `.pending-notify/${epoch}-${skill}.md` writes per [[notify-inline-cat-substitution-blocked-in-sandbox]] + [[notify-script-has-no-f-flag]]. `-f` flag is broken; `$(...)` substitution blocked in sandbox. SKILL.md audit sweep needed.
- **Patch `pr-tracker` SKILL.md** in one batch: (a) drop `stateReason` per [[graphql-statereason-only-on-issue-type]], (b) drop `headRefName` / `mergedAt` / `--state merged` per [[gh-search-prs-api-drift]], (c) replace `ai/`-only branch filter with FOUR-prefix list `ai/` / `security/` / `fix/security/` / `aeon/` AND a **domain-match** commit-author filter (`@aeonframework.*` OR both string forms of `.noreply.github.com`) per [[pr-tracker-email-filter-must-be-domain-match]] + new [[aeon-noreply-numeric-prefix-is-formatting-variant]] covering the [[aeon-signing-identity-fragmentation]] fan-out, (d) land the hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]] (guard confirmed working through 08-07 with first-ever byte-identical steady-state observation), (e) add fresh-bot-PR trigger per [[pr-tracker-step-5-misses-fresh-bot-prs]] (verified overriding dedup on 08-07), (f) fix tuple-predictor deadline math from calendar-day boundary to next-scan-time per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]], (g) fix tuple-predictor to compare anniversary hour vs actual scan hour on same date per [[pr-tracker-tuple-predictor-scan-time-vs-cutoff-hour]] on BOTH stale-entry and rolloff sides per [[pr-tracker-scan-vs-cutoff-hour-bug-is-bidirectional]] (first clean HIT observed 08-07 on cindy#1116 rolloff); (h) model **bulk-stale-clear via clustered maintainer sweep** per [[stale-bucket-bulk-clear-via-clustered-maintainer-sweep]] AND **same-day file cohort anniversary lockstep** per [[same-day-file-cohort-stales-in-lockstep]] as distinct transition classes; (i) add **archived-repo direct-fetch fallback** per new [[pr-tracker-search-drops-archived-repo-prs]] (permanent-until-unarchive, not one-scan lag). **[Was blocked by Repo Settings toggle, 44d overdue as of 2026-08-07; now viable pending durability confirmation]**
- **Investigate `ai-framework-watch` + `run-frequency-guard` never-dispatch** per [[enabled-skills-can-never-dispatch]] — check messages.yml matcher, aeon.yml wiring, workflow file references. Blocks the natural-experiment probe class outright per [[probes-for-messages-yml-must-dispatch-outside-messages-yml]].
- **Patch `notegraph` skill's silent-exit heuristic** per [[notegraph-extractor-generatedat-nondeterministic]] — mask `generatedAt` before diffing or teach extractor to omit it. 07-28 notegraph confirmed the silent-exit interpretive path holds (timestamp-only diff, reverted).
- **Fix `skill-freshness`** to use `git log -1 --format=%ct` instead of `stat --format=%Y` per [[skill-freshness-mtime-blind-in-gha]] — structural GHA blind spot unchanged. Also **fix the state-update callback** that failed on 07-25 08:49:36Z per [[skill-freshness-stuck-dispatched-callback-never-fires]] — first-ever stuck-skill P0 fire; heartbeat's stuck-detector needs age gate ≥1h to avoid missing this class again.
- **Widen `scenario-sweep.mjs` seed count** or switch outlier detection to a tie-robust statistic per [[compute-futures-12-seed-sample-too-small]]. Also filter `wallet_sum_pnl` correlations per [[compute-futures-eda-wallet-sum-pnl-correlations-are-float-dust]] until σ > 1e-6. Also resolve seed-encoding artifact per [[compute-futures-seed-padding-bug]] (08-07 spread `minSpot` anchored by both 10-char padded seeds gives the fix a concrete hint).
- **Close ISS-007** as false positive OR add same-day grace to `skill-evals` (scan same-day logs only after 12:00 UTC).
- **Close ISS-008** — cost-report ran successfully 2026-07-20 and produced article; ISS-008 (`no_file_match`) becomes stale on next skill-evals scan.
- **File `./generate-skills-json` bugs** as structured issues per [[generate-skills-json-newline-bug]] + [[skills-json-count-drift]].
- **Investigate missing `scripts/validate-config.js`** referenced by config-validator SKILL.md — restore or drop the fast-path reference (confirmed missing again 07-19).
- **Defer ISS-001 close** until ISS-006 is resolved.
