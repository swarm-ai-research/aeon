---
id: ISS-020
title: Batch outage — 3 skills missed morning window 2026-08-03
status: open
severity: medium
category: unknown
detected_by: batch-health
detected_at: 2026-08-03T08:00:00Z
resolved_at: null
affected_skills: [planner, compute-futures-eda, cost-report]
root_cause: null
fix_pr: null
---

# ISS-020: Batch Outage 2026-08-03

3 enabled skills never triggered in the 06:00–07:30 UTC window on 2026-08-03 (Mon, DOM=3 odd).

## Missing Skills

- `planner` — schedule `30 6 * * *` (daily 06:30 UTC). Not in the 26h skill-runs window; last successful run per `./scripts/skill-runs --json` predates 2026-08-02T07:59Z (per MEMORY.md, yesterday planner fired 2026-08-02T07:55Z, ~85min past its 06:30Z slot).
- `compute-futures-eda` — schedule `0 6 * * *` (daily 06:00 UTC). Not in the 26h skill-runs window.
- `cost-report` — schedule `0 7 * * 1` (Mon 07:00 UTC). Weekly Monday-only slot; today's is the only opportunity this week.

## Context

Detected by `batch-health` at 08:00 UTC (running via delayed cron pocket itself — batch-health's own 08:00Z slot went to `pending` status at 09:57:58Z per `./scripts/skill-runs`, ~118min late). The 08:00Z pocket cluster is again silent through 09:58Z: batch-health / fleet-control / github-monitor / gitlawb-fleet-metrics / heartbeat / issue-triage / pr-review / pr-triage / skill-freshness / weekly-shiplog all show `last_conclusion: pending` with `last_run` timestamps between 09:57:58Z and 09:58:31Z — a 33-second cluster fire consistent with a stalled pocket unblocking wholesale.

This is the third-consecutive-day fire of the same 06:00–08:00 morning-pocket underdelivery flagged in MEMORY.md as ISS-006 close-clock reset signal ("Watch tomorrow's 08:00Z pocket — if 90+min late again, treat as pocket-slot migration signal (not noise per [[iss-006-pocket-recovery-is-noise]]) and revisit close-clock counter"). Yesterday (08-02) the 08:00Z pocket delivered 113min late (Day-2 clean-close attempt); today the cluster is running 118+min late AND the earlier 06:00 / 06:30 / 07:00 pockets are silent entirely (no catch-up run visible in the 26h window as of the 09:59Z audit).

Compared to ISS-019 (07-14 outage): same class of failure but different missing set — no even-DOM memory hygiene skills today (odd DOM), and today includes the weekly `cost-report` Mon slot that ISS-019 did not.

## Next Steps

- Reinforces the ISS-006 pattern rather than being a new root cause. Fix path unchanged: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. **[BLOCKED by Repo Settings toggle / operator PAT provisioning per [[github-actions-cannot-create-prs]]]**
- ISS-006 close-clock counter should be reset — the 08-02 Day-2 delivery drift (39min → 113min) predicted this, and today's 118+min drift plus additional silent pockets confirms pocket-slot migration is the correct interpretation, not noise.
- If 06:00–07:30 pocket delivers cleanly 2026-08-04, treat today as a single-day escalation of ISS-006. If silent again, escalate severity to `high` and merge into ISS-006 as active fire.
- Check whether `cost-report` weekly Monday slot has a separate delivery path — its miss extends the pocket-silence pattern into a weekly-cron slot for the first time in the ISS-006 timeline.

## Related

- ISS-006 — parent recurring `messages.yml` cron underdelivery
- ISS-019 — prior batch outage (2026-07-14, same shape, different skill set)
- [[iss-006-pocket-recovery-is-noise]] — one clean day is delivery-rate noise
- [[gha-messages-yml-cron-underdelivery]] — root cron delivery pattern
