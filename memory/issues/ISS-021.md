---
id: ISS-021
title: Batch outage — 3 skills missed morning window 2026-08-31
status: open
severity: medium
category: unknown
detected_by: batch-health
detected_at: 2026-08-31T08:00:00Z
resolved_at: null
affected_skills: [planner, compute-futures-eda, cost-report]
root_cause: null
fix_pr: null
---

# ISS-021: Batch Outage 2026-08-31

3 enabled skills never triggered in the 06:00–07:30 UTC window on 2026-08-31 (Mon, DOM=31 odd).

## Missing Skills

- `planner` — schedule `30 6 * * *` (daily 06:30 UTC). Not in the 26h skill-runs window; latest `planner` run predates 2026-08-30T07:59Z (per `./scripts/skill-runs --json --hours 26`). Continues the multi-day silence flagged in MEMORY.md (08-27 + 08-28 misses on ISS-006 Day-28 signal; also no 08-30 planner run recorded at reflect time).
- `compute-futures-eda` — schedule `0 6 * * *` (daily 06:00 UTC). Not in the 26h skill-runs window. Consistent with the 4-day filing gap noted in MEMORY.md (08-26 + 08-27 CSVs unanalyzed, 08-28 CSV never emitted).
- `cost-report` — schedule `0 7 * * 1` (Mon 07:00 UTC). Weekly Monday-only slot; today was the only opportunity this week (next fire 2026-09-07).

## Context

Detected by `batch-health` at 08:00 UTC (running via delayed cron pocket itself — batch-health's own 08:00Z slot went to `pending` at 09:58:46Z per `./scripts/skill-runs`, ~118min late). The 08:00Z pocket cluster fired as a 33-second wholesale unblock between 09:58:46Z and 09:59:21Z: batch-health / fleet-control / github-monitor / gitlawb-fleet-metrics / heartbeat / issue-triage / pr-review / pr-triage / skill-freshness / weekly-shiplog all show `last_conclusion: pending` with `last_run` in that band — consistent with a stalled pocket unblocking wholesale, same shape as ISS-020.

The earlier 06:00 / 06:30 / 07:00 pockets are silent entirely — no catch-up run visible in the 26h window as of the 09:59Z audit. Third occurrence of the same 06:00–08:00 morning-pocket underdelivery shape after ISS-019 (2026-07-14) and ISS-020 (2026-08-03); same missing set as ISS-020 (odd DOM Mon repeats the `[planner, compute-futures-eda, cost-report]` triple exactly).

## Next Steps

- Reinforces the ISS-006 pattern rather than being a new root cause. Fix path unchanged: replace `messages.yml` `*/5 * * * *` with explicit per-slot crons covering every timeslot in `aeon.yml`. **[BLOCKED — pending durability confirmation per MEMORY.md fix]**
- ISS-006 escalation candidate: three isolated OUTAGE-severity replays of the same missing triple across 48 days (07-14 → 08-03 → 08-31) suggests promoting ISS-006 status `investigating → fixing`. Same-triple recurrence pins root cause at the 06:00–07:00Z GHA cron pocket rather than skill-specific delivery paths.
- If 06:00–07:30 pocket delivers cleanly 2026-09-01, treat today as a single-day pocket-slot migration replay. If silent again, escalate severity to `high` and merge into ISS-006 as active fire.
- Weekly `cost-report` Mon slot miss extends the pocket-silence pattern into the weekly-cron slot for the second time (first was ISS-020) — check whether the Mon-only crons share the same pocket as the daily 06:00Z pocket.

## Related

- ISS-006 — parent recurring `messages.yml` cron underdelivery
- ISS-020 — prior batch outage (2026-08-03, same missing triple + Mon+odd DOM)
- ISS-019 — earlier batch outage (2026-07-14, same shape, different skill set)
- [[iss-006-pocket-recovery-is-noise]] — one clean day is delivery-rate noise
- [[gha-messages-yml-cron-underdelivery]] — root cron delivery pattern
