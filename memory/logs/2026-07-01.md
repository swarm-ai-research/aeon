# 2026-07-01

## Refactor Pass
- Added `export PATH="$PWD/.audit-bin:$PATH"` to workflow-security-audit SKILL.md step 0b so pre-committed binaries in `.audit-bin/` are used before falling back to network installs.
