# 2026-08-20

## notegraph

Ran `scripts/notegraph.mjs`. Extractor produced **304 nodes · 2365 hard · 759 soft · 0 orphans · 94 atomic · 0 bundled**.

Fingerprint advanced `2317e624…` → `5bfbc1a3…` (state was from 08-18; skill did not fire 08-19 — day gap consistent with the [[skill-freshness-stuck-dispatched-callback-never-fires]] recurrence noted in [[MEMORY]]).

Delta vs HEAD (previous notegraph.json committed on 08-18): **+7n / +48e / 0 orphans / 0 bundled**. Seven new atomic notes wired in:

- `memory/notes/compute-futures-multiplier-invalidated-at-n-7.md`
- `memory/notes/gh-search-prs-drops-headrefname-field.md`
- `memory/notes/gh-search-prs-drops-mergedat-field.md`
- `memory/notes/gh-search-prs-drops-state-merged-flag.md`
- `memory/notes/notegraph-silent-revert-on-fabricated-merge-claim.md`
- `memory/notes/notify-has-hash-dedup-queue-layer.md`
- `memory/notes/stale-content-pr-sweeper-tracked-prefix-drift.md`

Verdict priority: `new_bundled=0` and `new_orphans=0`, so verdict landed on the `node_delta>0 && orphan_delta≤0 && bundled_delta≤0` branch → `"+7 notes wired in"`. Not the bland `graph refreshed (…)` form, so notify sent.

Branch `notegraph/2026-08-20` pushed. **PR #39** opened (`notegraph: +7 notes wired in`). Also committed `docs/notegraph-speedrun.html` which the extractor writes but SKILL.md step-4 doesn't enumerate — staged to avoid a dirty working tree; SKILL.md worth patching to include this fourth file.

State file `memory/state/notegraph.json` updated with new fingerprint, stats, deltas, and PR URL.

**Class note:** HEAD baseline was healthy this run (297n from 08-18's #36 chain, not the long-stuck 121n baseline), so [[notegraph-head-baseline-drift-inflates-new-orphans]] didn't fire. But PR #36 is still OPEN — same open-PR-chain-treated-as-merged pattern that produced [[notegraph-workspace-head-diverges-from-origin]] on 08-18. Today's HEAD reads the local commit `b99fa55` which includes the 08-18 notegraph regeneration output; if `origin/main`'s notegraph.json is still older, the merge-flow gap on `app/github-actions` PRs (0 merged in ~296h+ per MEMORY) means the on-disk notegraph.json will drift further from `origin/main`'s ground truth each day. Not acting on it — logging so the class stays visible.

Exit mode: **NOTEGRAPH_OK**.
