# 2026-07-09

## Docs Pass
- Updated `skills/workflow-security-audit/SKILL.md` to document `.audit-bin/` pre-seeded binary fallback.
- Step 0b now exports `$PWD/.audit-bin` first in PATH so committed binaries resolve without network fetches.
- Added Sandbox note bullet explaining `.audit-bin/` lifecycle (workflow commits binaries there; subsequent runs pick them up offline).
