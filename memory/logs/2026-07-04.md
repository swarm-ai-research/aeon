# 2026-07-04

## Refactor pass

Updated `skills/workflow-security-audit/SKILL.md` step 0b to prepend `.audit-bin/` to `$PATH` before the zizmor/actionlint install checks. Pre-downloaded binaries committed in `.audit-bin/` (zizmor, actionlint — both executable) are now found by `command -v` so network download steps are skipped. Also added a sandbox-note bullet documenting the `.audit-bin/` caching approach.
