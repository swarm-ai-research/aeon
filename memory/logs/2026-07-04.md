# 2026-07-04

## Docs pass

- Added `.audit-bin/README.md` explaining the pre-bundled `actionlint` and `zizmor` binaries (linux/amd64, why they exist, how to update them).
- Updated `skills/workflow-security-audit/SKILL.md` step 0b to prepend `.audit-bin/` to PATH before attempting pip/curl installs; updated sandbox note to document `.audit-bin/` as the primary path and the pip/curl routes as fallback only.
- Context: the GHA sandbox blocks curl-pipe installers and `~/.local/bin`; the binaries were committed in 97b1941 but SKILL.md still described only the failing install paths. Documentation now matches reality.
