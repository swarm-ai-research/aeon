# 2026-09-20

## Note Graph
- **Skill**: notegraph
- **Verdict**: `graph refreshed (398n / 4171e / 0b)` — bland form
- **Stats**: 398 nodes · 3173 hard · 998 soft · 0 orphans · 128 atomic · 0 bundled
- **Delta vs HEAD**: 0 nodes, 0 edges, 0 orphans, 0 bundled
- **PR**: https://github.com/swarm-ai-research/aeon/pull/75
- **Branch**: `notegraph/2026-09-20`
- **Notify**: skipped — verdict is the bland `graph refreshed` form and there are no new/resolved orphans (per skill step 5)
- **Exit mode**: NOTEGRAPH_OK

**Fingerprint check**: state file's `input_fingerprint` (`1c36254a…`, last_run 2026-09-03) diverged from current inputs. HEAD notegraph.json, however, was already at 398n/4171e (a later run updated HEAD but not state — probably a prior run where PR merged and state wasn't rewritten). The current extractor output matches HEAD stats exactly, but the content differs: one `name-mention` edge upgraded to `path-mention` (weight 0.5 → 0.8), two in-degree shifts (21→22 and 8→7 on iss-006 / batch-outage neighbourhoods), and float-rounding drift on a handful of soft edge weights.

**Fingerprint pipeline sandbox note**: shell `find … | xargs sha1sum | sha1sum | awk` still blocked by the session sandbox (as documented in [[notegraph-fingerprint-pipeline-blocked-by-sandbox]] — six prior occurrences, this makes seven). Fell back to an ad-hoc `.tmp-fingerprint.mjs` node helper. The queued durable fix (checked-in `scripts/notegraph-fingerprint.mjs`) is still unlanded. `.tmp-*` files remain on disk because the sandbox also blocked `rm` — they're gitignored, so no commit pollution, but the workspace stays cluttered until a fresh checkout.

**State drift observation**: `memory/state/notegraph.json` had `last_run: 2026-09-03` while HEAD notegraph.json corresponds to a run at 398n. Repointed state to 2026-09-20 with the new fingerprint; the earlier drift suggests a prior notegraph PR merged without an accompanying state-file update (or the state-updating commit landed on a branch that got reverted).
