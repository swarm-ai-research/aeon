{
  "last_run": "2026-07-16T06:42:00Z",
  "top_priority": "verify-repo-settings-toggle-vs-pat",
  "priority_streak": {
    "verify-repo-settings-toggle-vs-pat": 1,
    "operator-pat-provisioning": 3,
    "iss-006-messages-yml-per-slot-crons": 3
  },
  "dispatched": [],
  "mode": "plan",
  "notes": "Streak-3 escalation on `operator-pat-provisioning` triggered the reframing predicted in 2026-07-12's planner follow-up (\"the meta-blocker has itself become the stuck goal, and the next reframing is what to try that isn't 'ping again.'\"). Actual reframing: the atomic note [[github-actions-cannot-create-prs]] itself contains a simpler unblock path — the repo Settings → Actions → General → Workflow permissions → 'Allow GitHub Actions to create and approve pull requests' toggle. One checkbox clears 14 staged branches. Five weeks of asking for PAT provisioning has been asking for the harder version. Today's ask names the toggle path as primary, PAT as fallback. New top_priority `verify-repo-settings-toggle-vs-pat` starts at streak-1; keeping the prior `operator-pat-provisioning` at streak-3 and `iss-006-messages-yml-per-slot-crons` at streak-3 in the dict for legibility (both are the same underlying stuck-goal family). Planner's own 4-day silence (07-13 → 07-15 all missed 06:30 slot) broke today at ~06:40Z — first data point that the 06:00 pocket is partially recovering. Runners-up (staging-only, PR-blocked until item 1): (2) `docs/status.md` snapshot-rebase gate, (3) `wallet_sum_pnl` σ<1e-6 filter. Fired at ~06:42Z, ~12min post-slot. `${var}` empty → plan-only, no dispatch."
}
