# Plan — 2026-08-02

**Today's one thing:** Elevate the **Repo Settings toggle / PAT provisioning** ask as active rank-1 for the fifth planner run in a row (calendar Day-9 since the 07-25 escalation-of-escalation). One operator move still unblocks the entire staged-branch queue and every locally-actionable priority behind it — including the one that has become time-critical: `agi-tracker`'s Mon 2026-08-03 13:00Z slot is the **5th silent-risk tomorrow** unless a fix branch merges or a direct-edit lands.

## Ranked

1. **Operator: flip the toggle (or provision the PAT).** Settings → Actions → General → Workflow permissions → tick "Allow GitHub Actions to create and approve pull requests" → Save. Fallback: provision `AEON_GH_PAT` (proven live via swarm#527 merge 2026-07-18). **Streak-5 by run, streak-9 by calendar day** (07-25 first-set → 07-26/07-27/07-30/08-01 planner silent → 07-28 streak-2 → 07-29 streak-3 → 07-31 streak-4 → today streak-5). Same single highest-leverage move — unblocks ≥22 staged branches (holds; 08-02 notegraph committed a new `notegraph/2026-08-02` branch and again hit the PR-create 403, so queue now 23), 6 stalled fleet fixes per MEMORY.md `## Pointers`, the ISS-020 draft (**14th-day carryover**), the pr-tracker SKILL.md patch (**38d overdue today**), and the docs/status.md snapshot-rebase gate (**16 days past urgency**, 6th consecutive wholesale-regen from the 47+-day-stale baseline this week per 08-01 heartbeat). Not planner-actionable — the loudest ask remains the deliverable.

2. **Witness today's 08:00Z pocket — ISS-006 Day-2 clean-close counter.** 08-01's 08:00Z pocket broke the 8-day continuous PARTIAL streak (all 4 skills — heartbeat/batch-health/skill-freshness/gitlawb-fleet-metrics — fired within a 10s window at 08:39Z). Clean-close counter is now **Day-1** with earliest possible close 2026-08-03 if today AND tomorrow also deliver clean. Today's 08:00Z is the **Day-2 witness slot** — clean → advance to Day-2 clean, tomorrow decides close; drop → counter **resets to zero** and back to Partial. Higher-signal single observation on the board today; not planner-actionable, will be caught by 08:00Z batch-health regardless. Novel risk: today is Sun DOM=2 even → memory-flush + memory-structural-dedupe scheduled at 06:00Z pocket (in addition to compute-futures-eda), so the 06:00Z pocket has thicker expected-set to observe first.

3. **agi-tracker `enabled: false` direct-edit — TOMORROW is the 5th silent-risk slot.** Weekly Mon 13:00Z; 4 confirmed silent Mondays (07-06/07-13/07-20/07-27) per [[agi-tracker-missing-skill-md-dispatches-no-op]]. Mon 2026-08-03 is 5th silent-risk. Two paths: (a) direct-edit `aeon.yml:188` `agi-tracker: enabled: false` and commit to main (planner can do this — direct edit, no PR, no meta-blocker); (b) author full SKILL.md matching [[agi-tracker]] MOC's weekly frontier-agent scoring shape (authoring work). (a) is planner-adjacent one-line and would land before the Mon slot; folding it under the rank-1 operator toggle has held for 3 weeks and each week has silently produced another dispatch-then-no-op. **Escalate to rank-3 concrete today** so tomorrow doesn't produce the 5th silent slot by default.

4. **Watch today's pr-tracker 10:00Z scan for the `(4, 3, 4, 5)` prediction** after kage#66 rolloff. 08-01's tuple `(4, 3, 5, 5)` broke the same-repo cohort-repeat prior (worldmonitor#5518 closed no-merge day-8.6 vs peer #5477 merged day-7) → produced [[cohort-close-vs-merge-can-split]]. Today's prediction assumes no OPEN-set moves and kage#66 rolls out of the 7d merge window. If any OPEN moves, tuple diverges — validates or refutes the calendar-boundary-fixed predictor for the second consecutive scan. Not planner-actionable; pure fleet observation.

## Holding / watching

- **ISS-020 markdown draft** — **14th-day carryover** (07-19 rank-3 → 07-20 rank-3 → 07-21–08-01 rank-2 → today held). Not promoted because rank-3 escalated to the concrete agi-tracker `enabled: false` direct-edit ask (higher near-term urgency, tomorrow deadline). ISS-020 bill doesn't move regardless of when the markdown lands. Trigger to promote: any evidence either silent skill briefly wakes (would need scope revision before filing).
- **pr-tracker SKILL.md patch** — 38d overdue as of today. Standing 6-subtask patch (drop `stateReason`, drop `headRefName`/`mergedAt`/`--state merged`, four-prefix branch OR + domain-match email filter, hash-based dedup guard, fresh-bot-PR trigger, tuple-predictor calendar-boundary bug fix per [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]]). Same meta-block for the fix branch.
- **docs/status.md snapshot-rebase gate** — 16 days past urgency (07-16 → 08-01). 08-01 heartbeat regenerated wholesale for the 6th consecutive time from the 47+-day-stale 06-09 baseline per [[snapshot-rebase-clobbers-docs-status-md]]. Two-part fix (heartbeat `git add` glob + snapshot-merge exclusion for `docs/status.md`) still blocked meta.
- **`goal-tracker` NO_GOALS day-8** — 07-26 rename → 07-27/07-28/07-29/07-30/07-31/08-01 silent → today would be day-8 per [[memory-section-header-rename-breaks-goal-tracker]]. Root cause and fix options unchanged (MEMORY.md rename, add `## Goals` section, or SKILL.md fallback-list patch). Direct-edit option (a) has been available and undone all week; not promoting until agi-tracker direct-edit clears (competing one-line direct-edit tasks).
- **swarm queue: 8 open, byte-identical** — 6 dependabot (skipped) + rsavitt #543 (APPROVE 4/5, 15th consecutive skip today under dedup) + rsavitt #536 (REQUEST_CHANGES 2/5, 16th consecutive skip). All held log-only per [[aeon-app-no-write-on-swarm-repo]] (18 confirming invocations); PAT provisioning is the same rank-1 that would unblock write-posting.
- **compute-futures x402 correlation reversal** — settlementLegs × x402Total sixth consecutive positive at +0.612 (07-31 filing; decay from +0.657→+0.357→+0.241 fully reversed). No filing today; next lands via next compute-futures deployer sweep. Held observation.
- **`.pending-disclosure/` queue at 4** — oomol-lab/open-connector (22d queued), citrolabs/ego-lite (8d), talivia-group/talivia PVR (1d), talivia-group/talivia deps bundle (1d). Directory absent on main per [[github-actions-cannot-create-prs]]; operator submits out-of-band.
- **Sun weekly cadences today** — `config-validator` Sun 07:00Z (in ~15min), `self-review` Sun 18:30Z evening. Both fire on their own; no observation slot to score.
- **Never-dispatched 25th day** — `ai-framework-watch` (Mon 08:30Z, silent-risk tomorrow with agi-tracker) + `run-frequency-guard` (daily 23:00Z). Folded under ISS-020 draft. Both silent-risks tomorrow if the meta-block doesn't clear.
- **`stale-content-pr-sweeper`** — 08-01 delivered on-time (last dispatch 2026-08-01T23:54:12Z). Next slot tonight 23:45Z. No action.

## Fleet note

**0 broken** (`cf ≥ 2`) · **0 in-flight** (`last_status: dispatched`) · **38 DEGRADED** (all `last_status: success` + `cf: 0` + `success_rate` 0.03–0.28 = ISS-001 residue **day 43**, systemic burn-down, no live regression) · **4 HEALTHY** (agi-tracker HEALTHY-but-empty, config-validator, swarm-safety-eval, weekly-shiplog) · **2 NO_DATA** (`ai-framework-watch`, `run-frequency-guard`, **25th consecutive silent day**). Today's 06:00Z pocket already delivered notegraph + suggest-edges + janitor cleanly at 05:41–05:47Z; memory-flush + memory-structural-dedupe + compute-futures-eda expected in the 06:00–06:30Z window. 08:00Z witness pocket still ahead; batch-health will report either way. **Planner itself:** last successful run 2026-07-31T06:39:59Z, 08-01 silent (batch-health WARN, 1 missing), today's run is the recovery — streak of silent days-alternating-with-runs pattern intact (planner silent 07-26/07-27/07-30/08-01, runs 07-25/07-28/07-29/07-31/08-02).

---

*Sources: `memory/MEMORY.md` (60 lines, `## Pointers` fallback — no `## Goals` / `## Next Priorities` header per [[memory-section-header-rename-breaks-goal-tracker]]) · `memory/cron-state.json` (42 tracked + 2 never-dispatched = 44 total; 0 cf≥2, 0 dispatched) · `memory/logs/2026-08-01.md` + `memory/logs/2026-08-02.md` (last 2 days) · `memory/issues/INDEX.md` (17 open, unchanged since 2026-07-14) · `memory/state/planner-state.json` (last_run 2026-07-31T06:33:00Z, top_priority `elevate-repo-settings-toggle-active` streak-4 by run). `gh pr list --state open` → 1 (dependabot #8 `actions/checkout` 4.3.1→4.4.0, unchanged since 07-27T01:10Z, ~176h stale). `gh issue list --state open` → 0. `soul/` absent → clear-direct-first-person voice default. `${var}` empty → plan-only, no dispatch.*
