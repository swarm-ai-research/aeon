# Plan — 2026-08-29

**Today's one thing:** click-merge PR #26 (5/5 checks SUCCESS since 2026-08-24T01:08:19Z, unchanged) OR install the repo-level auto-merge policy for `dependabot` + `app/github-actions` — Day-22 without a merge, and the aeon-repo PR queue is now 26 deep with **3 open notegraph** and **5 open suggest-edges** stacking behind it.

## Ranked

1. **Merge PR #26 OR install a `dependabot + app/github-actions` auto-merge policy** *(streak 18 → 19)*. Verified this morning: `state: OPEN`, `mergedAt: null`, all 5 checks SUCCESS (ShellCheck · TypeScript a2a-server · TypeScript mcp-server · TypeScript dashboard · compute-futures tests), `updatedAt: 2026-08-24T01:08:19Z` (no new pushes in 5d). `mergeable/mergeStateStatus = UNKNOWN/UNKNOWN` — 3rd consecutive scan showing the transient GitHub recompute pattern, not a check regression. Zero `app/github-actions` PRs merged in **~532h since the 08-07 unblock**; 26 open aeon-repo PRs behind (24 on 08-26 + notegraph #51 [08-26] + notegraph #53 [08-28], +2 net over 3d). Escalation shape unchanged from Day-17 rotation: the operator clicks once, or installs the policy. Serves the operator-merge-flow-proof goal.

2. **Land `agi-tracker` `enabled: false` PR — T-2 window hit** *(streak 11 → 12, promoted from holding)*. Next silent-Mon fires **2026-08-31T13:00Z (~54h out)** which would be the **9th silent-Mon** — per the planner rule from the 08-26 plan ("Trigger to promote: T-2 days without action landed"), the deadline is now, not later. Two paths per [[agi-tracker-missing-skill-md-dispatches-no-op]]: (a) ship `enabled: false` on `aeon.yml:188` — lower friction, drops ms-02 enabled-skills counter 47→46; (b) restore/author `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC weekly frontier-agent shape — higher-value but more work. Either lands before Monday or the 9th silent-Mon fires and this promotes again next planner.

3. **Patch `stale-content-pr-sweeper` — bundle safety-gate override with `ALLOWED_AUTHORS` widening + TRACKED-prefix drift fix** *(streak 20 → 21)*. Stack behind PR #26 has grown: **3 open notegraph** (#48 [08-24, +5], #51 [08-26, +1], #53 [08-28, +208]) plus **5 open suggest-edges** (#42/#45/#49/#50/#52). #48 crossed the 5d age gate three days ago; #45/#49/#50 have been stale >7d each. Yesterday's operator invocation proved `ALLOWED_AUTHORS` text isn't the operative block — the sweeper's DIRTY/CONFLICTING/UNKNOWN mergeStateStatus safety gate is (these are all `MERGEABLE + UNKNOWN`). One SKILL.md patch bundles: (i) superseded-cluster override skipping the mergeStateStatus gate when a group has ≥3 in-flight duplicates from the same skill; (ii) add `"app/github-actions"` to `ALLOWED_AUTHORS`; (iii) fix TRACKED-prefix drift per [[stale-content-pr-sweeper-tracked-prefix-drift]] so `compute-macro/*` and `skill-graph/*` branch prefixes match their skill names. Without this, tomorrow's suggest-edges + notegraph fires push the stack to 9+ and the sweeper still closes zero.

## Holding / watching

- **Batch-outage signal — planner itself missed 08-27 and 08-28 06:30Z slots** *(new observation)*. Prior planner-state.json `last_run: 2026-08-26T06:42:00Z`; today's operator invocation is the first planner run in 71h. Only 3 skills logged on 08-28 (notegraph 05:22Z, code-health, surplus-pulse); 08-27 log has only pr-tracker. This is the shape of an [[ISS-006]] pocket-slot dead-zone extending past the morning window — worth a batch-health run to confirm scope, and possibly a new batch-outage ISS filing if it recurs today.
- **Patch `suggest-edges` templated-corpus pre-filter** *(streak 18 → 19)* — cadence eased: no new PR opened since #52 on 08-26 (suggest-edges hasn't fired successfully since 2026-08-26T05:52Z per cron-state — 71h gap consistent with the planner outage signal). If suggest-edges fires today and opens #54, rank promotes back to top-3.
- **Populate `memory/watched-repos.md` OR disable the 6 consumers** *(streak 21 → 22 planner slot; cluster streak-24 per 08-28 code-health log)*. Chronic. Not top-3 while merge-flow + agi-tracker T-2 are active bleeds.
- **pr-tracker SKILL patch batch (a)–(l)** — **65d overdue** as of today; item (m) CLA-block close-reason bucket added 08-26 per [[cloudflare-org-cla-blocks-aeonframework-prs]]; add item (n) tracking any Microsoft/vscode CLA fast-close (per 08-27 pr-tracker log flagging microsoft/vscode#332891 as a candidate 2nd cross-org confirmation of the class).
- **ISS-006 messages.yml multi-pocket rewrite** *(Day-28)* — today's 71h planner gap is precisely the class this fixes; escalation to `open → investigating → fixing` warranted if today's batch confirms extended dead zone.
- **docs/status.md snapshot-rebase gate** — **43d past urgency threshold**; **29th consecutive** rebase-clobber-then-regen already expected on next heartbeat.
- **notegraph fingerprint sandbox friction — n=4 across 08-23/24/25/28** per today's 08-28 log; checked-in `scripts/notegraph-fingerprint.mjs` still the durable fix.
- **compute-futures 2.5× multiplier** — n=7 per 08-26 CSV crossed the [[compute-futures-multiplier-invalidated-at-n-7]] rename threshold; watching for n=8 confirmation at next filing.
- **swarm-repo app-perm gap** *(Day-48)* — [[aeon-app-no-write-on-swarm-repo]] class unexercised for ~50d.
- **Sign the Cloudflare CLA against aeonframework identity OR add a pre-submit skip in vuln-scanner/external-feature** per [[cloudflare-org-cla-blocks-aeonframework-prs]] — first observed 08-26; second-instance watch on Microsoft/vscode#332891 open since 08-27.

## Fleet note

0 broken · 0 in-flight · 0 hard-failed (cf≥3) · **38 DEGRADED** (ISS-001 Day-70 residue, all `cf: 0` + `last_status: success`) · 4 truly HEALTHY · 2 NO_DATA (53rd silent day: `ai-framework-watch` + `run-frequency-guard`) · **17 filed open ISS** (+1 pending ISS-021 draft, 41d carryover) · **26 open aeon-repo PRs** (+2 vs 08-26 from notegraph #51/#53) · **0 open GH issues** · both `gh pr list` / `gh issue list` responded (neither `PR_LIST_UNAVAILABLE` nor `ISSUE_LIST_UNAVAILABLE` fired). **Batch signal:** planner missed 08-27 + 08-28 06:30Z slots (71h gap since last planner-state `last_run`), 08-28 batch only 3 skills logged — extended pocket-slot dead zone consistent with [[ISS-006]].
