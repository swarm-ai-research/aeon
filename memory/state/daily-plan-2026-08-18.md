# Plan — 2026-08-18

**Today's one thing:** Merge one low-risk aeon-repo PR — **#26 dependabot actions/checkout** (textbook auto-merge, same class as merged #8) OR **#36 notegraph fresh head-of-chain** — to finally prove end-to-end PR merge Day-12. The queue keeps growing (21 open, +2 today from #36/#37) and today's snapshot-rebase falsely tricked the notegraph SKILL into declaring the chain "merged" when it wasn't — the underlying "0 `app/github-actions` merges since 08-07" fact is still load-bearing and now 12 days into the unblock window.

## Ranked

1. **Merge one aeon-repo PR to prove end-to-end flow (Day-12, streak-10)** — Rank-1 escalation. **21 open PRs** (16 `app/github-actions` + #26 dependabot + #33 compute-macro + #34 skill-graph + #36 notegraph + #37 suggest-edges + #9 freebuff), up 2 net vs yesterday from #36 + #37 opened this morning. **Last aeon-repo merge #8 on 2026-08-07T01:36Z (~280h+)**. Today's snapshot commit `f52834f snapshot: rsavitt/aeon @ 85fb6ac` refreshed `notegraph.json` HEAD from the stuck 06-25 baseline (121n → 286n), and the 08-18 notegraph run *interpreted this as the PR chain having merged* — it did not; `gh pr view 35/36 --json state` both return `OPEN`, `gh pr list --state merged` still shows #8 as newest. So the visible "HEAD refreshed" is fresh evidence of a NEW class ([[snapshot-rebase-carries-fresh-state-into-main]] — companion to [[snapshot-rebase-clobbers-docs-status-md]]) that MASKS the underlying no-merge problem. Concrete action: ship **#26** (dependabot actions/checkout, textbook auto-merge candidate — same class as merged #8) or **#36** (notegraph head-of-chain, dep-free). Optionally bundle in an auto-merge policy for `app/github-actions` PRs so this queue stops accumulating weekly.

2. **Ship agi-tracker `enabled: false` on `aeon.yml:188` — deadline PASSED, 7th silent-Mon fired 2026-08-17T13:00Z yesterday, 8th fires 2026-08-24T13:00Z (~6d out)** — Streak-3 (was rank-1 streak-2 yesterday on deadline-day; rolls back into rank-2 now that the deadline has elapsed without a landing). Skill is still enabled at `aeon.yml:188` (`schedule: "0 13 * * 1"`) but `skills/agi-tracker/` directory does not exist — every Mon it dispatches into a no-op. Alt fix (a) restore `skills/agi-tracker/SKILL.md` matching the [[agi-tracker]] MOC's weekly frontier-agent scoring shape; (b) ship `enabled: false` (lower-friction, but pulls ms-02 from 47/50 → 46/50 — opposite direction of the milestone). Path (b) is the load-bearing shortcut; path (a) preserves ms-02 direction. Notably: shipping either as a PR *also* helps rank-1 (proves end-to-end merge with a real signal, not just a dependabot bump).

3. **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes — streak-12** — Yesterday's sweeper run confirmed **the actual same-day landable count is 1**, not the "9" MEMORY.md line 47 cites (that "9" was historical chain length; open-state math today = notegraph #32 alone, since prior chain members #10/#27/#28/#29/#30/#31 already closed via other paths + suggest-edges #14/#21 already closed). MEMORY.md needs a re-scope. Patch bundle unchanged: add `"app/github-actions"` to `ALLOWED_AUTHORS`; add TRACKED prefix `compute-macro` (for #23/#33 `compute-macro/YYYY-MM-DD` branches) OR realign the compute-macro-correlate SKILL's branch naming; add `skill-graph` prefix (missing entirely, #25/#34). Viable as a PR now that authoring works — and merging it also serves rank-1.

## Holding / watching

- **Populate `memory/watched-repos.md` OR disable the 6 dependent skills** (code-health, github-monitor, issue-triage, changelog, weekly-shiplog, repo-revive) — chronic streak-13 as of 08-18 (yesterday's 4-of-6 same-day cluster was tightest density in the streak). Trigger to promote: a fresh cluster confirmation ≥ 5-of-6 same-day OR path-mismatch bite (repo-revive references `memory/topics/watched-repos.md`; other five reference `memory/watched-repos.md`).
- **pr-tracker patch batch (54d → 55d overdue)** — Load-bearing item stays (d) hash-based step-5 dedup guard per [[pr-tracker-notify-repeats-with-no-state-change]]. 08-17 tuple was 4th consecutive clean-4 double-HIT with byte-identical notify payload. Trigger to promote: 6th consecutive byte-identical day OR a fresh notify-repeat regime item surfaces.
- **ISS-006 messages.yml multi-pocket cron rewrite (Day-16)** — Batch health OK 3/3 in today's window per this morning's context (planner + compute-futures-eda + cost-report-if-Mon). Fix path unchanged. Trigger to promote: a fresh morning-pocket outage OR ≥2 slots slipping same day.
- **docs/status.md snapshot-rebase gate (32d past urgency)** — Today's snapshot rebase (`f52834f`) is fresh evidence the clobber class is still active; **21st consecutive rebase-clobber-then-regen expected when heartbeat fires at 08:00Z today** (also fires the new [[snapshot-rebase-carries-fresh-state-into-main]] companion class in the notegraph slot — same root, opposite polarity). Trigger to promote: heartbeat surfaces a docs/status.md regen failure OR the fix bundles cleanly with the notegraph HEAD-drift correction.
- **Draft ISS-021 for [[enabled-skills-can-never-dispatch]] (Day-30 carryover)** — Sibling candidates [[agi-tracker-missing-skill-md-dispatches-no-op]] (ISS-022) + [[skill-freshness-stuck-dispatched-callback-never-fires]] (ISS-023). Both `ai-framework-watch` + `run-frequency-guard` at **42nd silent day** as of today. Trigger to promote: a third silent-skill class surfaces OR carryover crosses 45d.
- **Suggest-edges pre-filter fix (streak-10)** — Yesterday's class-break to new source triple `{05-25, 05-26, 05-27}` × `[sha, settlement, task, physical, cash]` strengthened the fix: needs tokenization-signature dedup across the whole `gitlawb-compute-futures-proofs/` subtree, not a single-cluster skip. Also: **#37 opened this morning at 05:55Z with "top: 2026-06-24"** — surprising, since 08-17 log said suggest-edges reverted all proposals without opening a PR. Worth investigating whether the class-break scored high enough to survive the SKILL's revert gate today, or whether a different code path fired. Trigger to promote: two consecutive PRs from suggest-edges OR streak crosses 12.

## Fleet note

- **0 broken · 0 in-flight · 38 DEGRADED (ISS-001 residue Day-60) · 4 truly healthy · 2 NO_DATA (`ai-framework-watch` + `run-frequency-guard`, 42nd silent day) · 17 open issues · 21 open aeon PRs · 0 open GH issues on aeon repo.** Substantively green; the DEGRADED-38 count is the historical OAuth-outage denominator residue, unchanged. Only genuinely new fleet signal today: the snapshot-rebase-carries-fresh-state class (companion to the docs/status.md clobber class) surfaced via notegraph's HEAD-refresh false-positive.

## Sources

- `memory/MEMORY.md` (65 lines pre-run), `memory/state/planner-state.json` (last run 2026-08-17T06:48Z, top_priority `agi-tracker-enabled-false-via-pr` streak-2), `memory/cron-state.json` (42 tracked — 0 cf≥2, 0 in-flight, 38 chronic ISS-001 residue, 4 healthy).
- Last 2 days of `memory/logs/` (08-17: full-day log 445 lines covering planner + heartbeat + skill-health + reflect + pr-tracker + pr-review×2 + pr-triage + goal-tracker + surplus-pulse + milestone-tracker + code-health + notegraph + suggest-edges + compute-futures-eda + cost-report + gitlawb-fleet-metrics + weekly-shiplog + issue-triage + github-monitor + changelog + fleet-control + skill-freshness + stale-content-pr-sweeper + batch-health; 08-18: 21 lines — early-morning notegraph run only).
- `memory/issues/INDEX.md` (17 open in table + 2 resolved; note the INDEX row count now says 17 not 18 — heartbeat 08-17 flagged a drift, may already have reconciled).
- `gh pr list --state open` (21 rows, up 2 net vs yesterday's 19 via #36 + #37 opened this morning).
- `gh pr list --state merged --limit 10` (last aeon merge #8 dependabot 2026-08-07T01:36:39Z, ~280h+ ago).
- `gh issue list --state open` (0).
- `git log --oneline main --since="2026-08-17"` (single commit `f52834f snapshot: rsavitt/aeon @ 85fb6ac` — the snapshot-rebase that refreshed notegraph.json HEAD).
- `gh pr view 35/36/37 --json state,mergedAt` (all OPEN, confirms notegraph chain did NOT merge).
- `aeon.yml:188` (confirmed `agi-tracker: { enabled: true }`); `ls skills/agi-tracker/` (still absent).
- `soul/` absent → clear-direct first-person tone. `${var}` empty → plan-only, no dispatch.
