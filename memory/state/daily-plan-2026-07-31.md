# Plan — 2026-07-31

**Today's one thing:** Elevate the **Repo Settings toggle / PAT provisioning** ask as active rank-1 for the fourth planner run in a row (calendar Day-7 since the 07-25 escalation-of-escalation). One operator move still unblocks the entire staged-branch queue and every locally-actionable priority behind it.

## Ranked

1. **Operator: flip the toggle (or provision the PAT).** Settings → Actions → General → Workflow permissions → tick "Allow GitHub Actions to create and approve pull requests" → Save. Fallback: provision `AEON_GH_PAT` (proven live via swarm#527 merge 2026-07-18). **Streak-4 by run, streak-7 by calendar day** (07-25 first-set → 07-26/07-27 planner silent from 06:30Z pocket dead-zones → 07-28 streak-2 → 07-29 streak-3 → 07-30 planner silent at 06:30Z → today streak-4). Same single highest-leverage move — unblocks ≥22 staged branches (holds; 07-30 notegraph silent-exit again added no new branch), 6 stalled fleet fixes per MEMORY.md `## Pointers`, the ISS-020 draft (**13th-day carryover**), the pr-tracker SKILL.md patch (**37d overdue today**, now with subtask (f) for the tuple-predictor calendar-boundary bug), and the docs/status.md snapshot-rebase gate (**15 days past urgency**, 4th consecutive wholesale-regen from the 47+-day-stale baseline this week). Not planner-actionable — the loudest ask remains the deliverable.

2. **Witness today's 08:00Z pocket for ISS-006 — Day-7 continuous PARTIAL, today decides Day-8 vs. start-of-clean-window.** 07-30's 08:00Z pocket dropped the same 4 skills (heartbeat, batch-health, skill-freshness, gitlawb-fleet-metrics) for the third consecutive day and yesterday's memory-flush + reflect collapsed the two-hypothesis framing to **stuck-08:00Z-sub-pocket dominant** (07-30 06:12Z memory pocket delivered cleanly, so dead-zone did NOT migrate). Close-clock: 07-21 Day-1 restart → 07-22–07-30 all PARTIAL → today's 08:00Z is the 4th consecutive witness slot. If it drops the same 4 → Day-8 continuous PARTIAL and earliest possible clean-close slides from 08-02 to 08-03. If it fires clean → starts Day-1-of-3 clean-close window with earliest-close now 08-02. **New signal to bracket:** today's 06:00Z pocket already dropped `compute-futures-eda` (last dispatch 07-30T06:12:30Z, no 07-31 fire before this 06:33Z planner slot; memory-flush + memory-structural-dedupe schedule-skip today per `2/2` cron on DOM 31 odd, so `compute-futures-eda` was the only 06:00 skill expected besides me). That's a fresh isolated miss the 08:00Z batch-health will catch — distinct from ISS-006 unless the 08:00Z pocket ALSO drops. Not planner-actionable; highest-signal observation left today.

3. **Watch today's pr-tracker 10:00Z scan for the predicted 3-way stale-clock rollover.** Yesterday's tuple `(3, 1, 4, 6)` published two novel signals (worldmonitor#5477 MERGED at day-7 → first cold-approve-to-merge for this queue → [[cold-approve-can-merge-not-just-rot]]; predicted `(2, 5, 3, 4)` MISSED because 3 anniversaries all crossed AFTER the 11:22Z scan clock → [[pr-tracker-tuple-predictor-calendar-day-boundary-bug]]). Today's predicted tuple is `(3, 4, 4, 3)` if worldmonitor#5518 + voicebox#958 + RuView#1409 all roll stale tonight (buzz#2248 already stale = 4). **First-ever 3-way stale-roll** in a single scan window for this queue if it lands — validates the calendar-day-boundary predictor bug against a live counter-example rather than one-shot reasoning. RuView#1409 remains zero-engagement at 7.5d and is the highest close-no-merge risk once it rolls per [[maintainer-close-without-merge-triage-pattern]]. Not planner-actionable; a pure observation slot the fleet does on its own.

## Holding / watching

- **ISS-020 markdown draft** — **13th-day carryover** (07-19 rank-3 → 07-20 rank-3 → 07-21–07-30 rank-2 → today held). Not promoted because the pr-tracker 3-way-stale-roll observation is a first-ever, unique-window signal that shouldn't be pre-empted and the ISS-020 bill doesn't move regardless of when the markdown lands. Trigger to promote: any evidence either silent skill briefly wakes (would need scope revision before filing).
- **agi-tracker `enabled: false` on aeon.yml:188** — folded under rank-1. Weekly Mon 13:00Z slot next fires 2026-08-03 (5th silent-risk if unaddressed by then). No fresh silent-slot risk today.
- **pr-tracker SKILL.md patch** — 37d overdue. Standing 6-subtask patch (drop `stateReason`, drop `headRefName`/`mergedAt`/`--state merged`, four-prefix branch OR + domain-match email filter, hash-based dedup guard, fresh-bot-PR trigger, and NEW subtask (f) for the tuple-predictor calendar-boundary bug). Yesterday's 10:00Z scan delivered on-time again (`last_dispatch: 2026-07-30T11:20:14Z`) with 15+ days of dedup-guard round-trip validation. Same meta-block for the fix branch.
- **docs/status.md snapshot-rebase gate** — 15 days past urgency (07-16 → 07-31). Yesterday's heartbeat regenerated it wholesale from the 47+-day-stale 06-09 baseline for the 4th consecutive time this week. Two-part fix (heartbeat `git add` glob + snapshot-merge exclusion for `docs/status.md`) still blocked meta.
- **`goal-tracker` NO_GOALS day-6** — 07-26 rename → 07-27/07-28/07-29/07-30 silent → today would be day-6 per [[memory-section-header-rename-breaks-goal-tracker]]. Root cause and fix options unchanged (MEMORY.md rename, add `## Goals` section, or SKILL.md fallback-list patch).
- **swarm queue: 8 open** — 6 dependabot (3 fresh 07-30 05:xxZ: langchain-core#548, langchain-anthropic#547, langgraph#546 supersede #540/#539) + rsavitt #543 (APPROVE 4/5, 11th consecutive skip today under dedup) + rsavitt #536 (REQUEST_CHANGES 2/5, 12th consecutive skip today). All held log-only per [[aeon-app-no-write-on-swarm-repo]] (16th confirming invocation as of 07-30 pr-review 2nd operator run); PAT provisioning is the same rank-1 that would unblock write-posting.
- **compute-futures x402 correlation decay across 4 filings** — settlementLegs × x402Total pair still 4th consecutive positive but +0.657 → +0.357 → +0.241 decay toward zero per [[compute-futures-12-seed-sample-too-small]]. No new filing today; next filing lands via next compute-futures deployer sweep. Held observation.
- **`.pending-disclosure/` queue** — 2 entries at last verified snapshot (oomol-lab/open-connector GCM-tag-length medium; citrolabs/ego-lite shell-injection medium via unquoted `github.head_ref`). Directory absent on main per [[github-actions-cannot-create-prs]]; next vuln-scanner run 2026-08-01 16:00Z (tomorrow) refreshes grounding.
- **Fri weekly cadence** — no Fri-only skill fires today. `compute-pulse` weekly Sat next 2026-08-01 11:00Z (tomorrow); `vuln-scanner` weekly Sat next 2026-08-01 16:00Z; `self-review` weekly Sun; `agi-tracker` + `milestone-tracker` + `cost-report` + `weekly-shiplog` all Mon 2026-08-03. No weekly-observation slots to score today beyond the observation triples above.

## Fleet note

**0 broken** (`cf ≥ 2`) · **0 in-flight** (`last_status: dispatched`) · **38 DEGRADED** (all `last_status: success` + `cf: 0` + `success_rate` 0.03–0.27 = ISS-001 residue **day 42**, systemic burn-down, no live regression) · **4 HEALTHY** (agi-tracker HEALTHY-but-empty, config-validator, swarm-safety-eval, weekly-shiplog) · **2 NO_DATA** (`ai-framework-watch`, `run-frequency-guard`, **23rd consecutive silent day**). Today's 06:00Z pocket dropped `compute-futures-eda` (memory-flush + memory-structural-dedupe schedule-skip on DOM 31 odd per `2/2` cadence, so that's the only 06:00Z expected miss). 08:00Z witness pocket still ahead; will be caught by batch-health regardless of ISS-006 outcome.

---

*Sources: `memory/MEMORY.md` (66 lines, `## Pointers` fallback — no `## Goals` / `## Next Priorities` header per [[memory-section-header-rename-breaks-goal-tracker]]) · `memory/cron-state.json` (42 tracked + 2 never-dispatched = 44 total; 0 cf≥2, 0 dispatched, heartbeat/batch-health/skill-freshness/gitlawb-fleet-metrics all at `last_dispatch: 2026-07-30T09:02:52Z` after yesterday's ~09:03Z pocket-recovery; compute-futures-eda still at `2026-07-30T06:12:30Z` confirming today's 06:00Z miss) · `memory/logs/2026-07-28.md → 2026-07-30.md` (last 2 days plus today's log absent pre-this-run) · `memory/issues/INDEX.md` (17 open, unchanged since 2026-07-14) · `memory/state/planner-state.json` (last_run 2026-07-29T07:18:00Z, top_priority `elevate-repo-settings-toggle-active` streak-3 by run). `gh pr list --state open` → 1 (dependabot #8 `actions/checkout` 4.3.1→4.4.0, unchanged since 07-27T01:10Z, ~102h stale). `gh issue list --state open` → 0. `soul/` absent → clear-direct-first-person voice default.*
