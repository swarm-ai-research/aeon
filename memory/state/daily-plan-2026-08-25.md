# Plan — 2026-08-25

**Today's one thing:** merge PR #26 or set a repo auto-merge policy that eats it — Day-17 of the queue-merge slot, and 24h since it went "one-click ready" without a click. All 5 checks are still SUCCESS (ShellCheck + 3 TypeScript + compute-futures tests, `updatedAt: 2026-08-24T01:08:19Z`); `state: OPEN`, `mergedAt: null`. `mergeable/mergeStateStatus` currently return `UNKNOWN` — GitHub's transient recompute after the overnight ready-state, not a regression — but functionally this is still `gh pr merge 26 --squash --delete-branch` for the operator. A merge on a dependabot PR with 5 green checks isn't the point; the point is that a stuck goal at streak-17 with the concrete unblock spelled out for 24+ hours is exactly the class where the SKILL says escalate, not restate. Real escalation is to **either merge it OR configure a repo-level auto-merge policy for `app/dependabot` / `app/github-actions`** so this class of PR stops sitting behind an operator click. The 23-deep queue behind it (21 `app/github-actions` + 1 dependabot + 1 freebuff-web) hasn't moved in ~437h since #8 on 2026-08-07T01:36Z, and yesterday's proof-of-flow window closed with no proof.

## Ranked

1. **Merge PR #26 OR install a `dependabot + app/github-actions` auto-merge policy** (streak 16 → 17, escalation). Yesterday framed it as "one-click, ride it in today"; the operator didn't ride it in. Day-17 → the deliverable is no longer just the click, it's removing the click as a daily manual dependency. `gh pr merge 26 --squash --delete-branch` clears today; a small workflow rule (`.github/workflows/auto-merge.yml` gating on author + all checks green) clears every future dependabot bump without another planner slot. Plan-only planner still can't do either from cron — this is operator-write territory — but the escalation-shaped ask is the policy, not the click.

2. **Patch `suggest-edges` to pre-filter within-`gitlawb-compute-futures-proofs/` pairs with identical scenario-sweep tokenization signature** (streak 16 → 17). **8th consecutive PR-opening day** — PR #50 opened 05:47:33Z with the same `{06-24, 06-27, 07-04} → 07-28` templated-corpus signature as #38/#40/#42/#44/#45/#49. Three open now (#42/#45/#50; #49 will roll into that group when tomorrow's fire re-opens the same signature). Net +1/day into a queue that has no proven merge path — this is the loudest ongoing source of aeon-repo PR pressure and the cleanest single-file fix (add a tokenization-fingerprint dedupe in the scoring pass so recurring templated-corpus siblings score once, not every day). Bumping this over agi-tracker because the agi-tracker deadline just re-opened to 6 days out (next silent-Mon fire 2026-08-31T13:00Z) while suggest-edges is a fresh PR every 24h until it's patched.

3. **Patch `stale-content-pr-sweeper` SKILL.md `ALLOWED_AUTHORS` + TRACKED prefixes** (streak 18 → 19). Today's 00:01:26Z cron sweep closed nothing — the daily cron cannot exercise the same intent-applied allowlist that yesterday's 00:02Z and 23:59Z operator-invocations used to close #41 / #44 / #43. Under the SKILL text as written, cron sees zero `aeonframework`-authored PRs and returns empty. Add `"app/github-actions"` to `ALLOWED_AUTHORS` in the step-1 snippet, then bundle the TRACKED-prefix drift fix per [[stale-content-pr-sweeper-tracked-prefix-drift]] (short-prefix aliases for `compute-macro/*` and `skill-graph/*`, or align each skill's branch step to produce the full skill-name prefix). That flips the daily cron from "runs, closes 0" to "runs, closes the growing suggest-edges + notegraph tail" while item 2 addresses the source.

## Holding / watching

- **agi-tracker `enabled: false` PR** (streak 9 → 10) — 8th silent-Mon fire happened yesterday 13:00Z; window doesn't re-open until 2026-08-31T13:00Z (6 days out). Concrete unblock unchanged. Promote when we're inside 48h of the next fire.
- **Populate `memory/watched-repos.md`** (streak 19 → 20, chronic) — no same-day cluster fires yet today (Monday, most watched-repos-dependent skills are Sun/Wed). Promote on next 6/6 same-day cluster or when a rank-3 slot opens.
- **pr-tracker SKILL.md patch batch (a)–(k)** — 63d overdue. Watching for today's scan to change tuple state off the fresh `(1, 8, 1, 2)` / `(1, 9, 1, 1)` predictor.
- **docs/status.md snapshot-rebase gate** — 39d past urgency threshold. Watching heartbeat's next fire; promote when it's the top-3 slot's occupant.
- **ISS-006 messages.yml multi-pocket rewrite** — Day-23. Fix path viable; three regimes to model. Reactive triggers still cover the P0 case.
- **swarm-repo app-perm gap** ([[aeon-app-no-write-on-swarm-repo]]) — Day-44. 61 pr-review + 40 pr-triage against a byte-frozen queue; still no autonomous write attempted.
- **Notegraph fingerprint-pipeline sandbox friction — n=3 today** (08-23, 08-24, 08-25 all needed the `.mjs` helper workaround). At n≥3 this crosses [[compute-futures-multiplier-invalidated-at-n-7]]-style promotion threshold — flag to reflect / memory-flush to open `[[notegraph-fingerprint-pipeline-blocked-by-sandbox]]`, then patch SKILL.md step-1 to make the node helper the primary path. Small SKILL edit, but not top-3 while suggest-edges is opening a fresh PR every day.
- **compute-futures `[[compute-futures-basket-synth-2.5x-multiplier]]`** — n=5 rename threshold hit 08-23; watching next 08-24 CSV run for n=6 durability.
- **Suggest-edges #49 stale eligibility** — opened 08-24 05:36Z, will hit the sweeper's ≥2d gate on 08-26; if rank-3 lands first, #49 becomes the first cron-natural sweep after the ALLOWED_AUTHORS patch.

## Fleet note

Fleet: 0 broken · 0 in-flight · 0 hard-failed (**6th consecutive clean morning**: 00:01Z sweeper success no-op, 05:07Z notegraph silent-exit no PR per [[notegraph-extractor-generatedat-nondeterministic]], 05:49Z suggest-edges PR #50 opened) · 38 DEGRADED (ISS-001 Day-67 residue, all `last_status: success` + `cf: 0`) · 4 truly HEALTHY · 2 NO_DATA (`ai-framework-watch` + `run-frequency-guard`, **49th silent day**) · 17 filed open issues (+1 pending ISS-021 draft) · **23 open aeon PRs** (21 `app/github-actions` + 1 dependabot + 1 freebuff-web, net-flat: sweeper closed #43 last night 23:59Z, suggest-edges opened #50 this morning 05:47Z) · 0 open GH issues. `PR_LIST_UNAVAILABLE` / `ISSUE_LIST_UNAVAILABLE`: neither fired — `gh pr list` and `gh issue list` both responded.
