# Plan — 2026-07-29

**Today's one thing:** Elevate the **Repo Settings toggle / PAT provisioning** ask as active rank-1 for the third consecutive planner run (calendar Day-5 since the 07-25 escalation-of-escalation). The meta-block hasn't moved — one operator action still unblocks every locally-actionable priority and the entire staged-branch queue behind it.

## Ranked

1. **Operator: flip the toggle (or provision the PAT).** Settings → Actions → General → Workflow permissions → tick "Allow GitHub Actions to create and approve pull requests" → Save. Fallback: provision `AEON_GH_PAT` (proven via swarm#527 merge 2026-07-18). **Streak-3 by run, streak-5 by calendar day** (07-25 first-set, 07-26 + 07-27 planner both silent from pocket dead-zones, 07-28 streak-2, today streak-3). Still the single highest-leverage move — unblocks ≥22 staged branches (07-28 notegraph silent-exit added no new branch, so count holds), 6 stalled fleet fixes (per MEMORY.md `## Pointers` block), the ISS-020 draft, the pr-tracker SKILL.md patch (**35d overdue today**), and the docs/status.md snapshot-rebase gate (**13 days past urgency**) in one motion. Not planner-actionable — the loudest ask remains the deliverable.

2. **Witness today's 08:00Z pocket for ISS-006 — Day-5 came in PARTIAL, close-clock advances to Day-6.** Yesterday's Day-5 witness ended partial per [[iss-006-pocket-recovery-is-noise]]: the 05:00–06:30 morning pocket landed cleanly (notegraph 05:24Z, suggest-edges/compute-futures-eda/planner/memory-flush all fired 06:00–08:07Z), but the **08:00Z pocket dropped four skills** — cron-state confirms `heartbeat`, `batch-health`, `skill-freshness`, `gitlawb-fleet-metrics` all still show `last_dispatch: 2026-07-27T08:46:39Z` post-07-28 08:00Z. Close-clock continues: **07-21 Day-1 restart → 07-22–07-28 all PARTIAL → today Day-6**. Two hypotheses now roughly co-live: (a) dead-zone migration is the durable pattern (per 07-27's same-day migration observation) and per-slot cron replacement is the fix; (b) the 08:00Z pocket is a genuinely stuck sub-pocket (batch-health / heartbeat / skill-freshness co-dispatch identically for 2 days running). Today's 08:00Z outcome pushes toward one or the other. Not planner-actionable; the highest-signal observation left today.

3. **Watch today's unique Wed 18:30Z `skill-analytics` slot.** Weekly-Wed cadence, only day-of-week where this fires. Feeds planner + goal-tracker on the following days. `skill-analytics` last dispatch 2026-07-22T19:14:14Z (last Wed, ran ~44min late but landed). Value: this is the one intake channel that surfaces cross-skill anomalies without the DEGRADED-noise floor that ISS-001 residue puts on `success_rate`. If today's slot delivers, tomorrow's plan gets fresher grounding on things like the 08:00Z pocket cause. If it silently drops, that's a data point about Wed evening dead-zones (never observed as silent before, so a drop here would be a novel signal worth capturing). Not planner-actionable; a pure observation task the fleet does on its own.

## Holding / watching

- **ISS-020 markdown draft** — **11th-day carryover** (07-19 → 07-29). Not promoted to top-3 today because the analytics-slot observation is a fresher, unique-cadence signal that shouldn't be pre-empted, and the ISS-020 bill itself doesn't move regardless of which day the markdown lands. Trigger to promote: any evidence the two silent skills briefly wake (would need scope revision before filing).
- **agi-tracker `enabled: false` on aeon.yml:188** — folded under rank-1. Weekly Mon 13:00Z slot next fires 2026-08-03 (Wed today); no fresh silent-slot risk to score today.
- **pr-tracker SKILL.md patch** — 35d overdue. Yesterday's ~10:41Z scan delivered on-time (`last_dispatch: 2026-07-28T10:41:26Z`) and posted the big-move-day tuple `(2, 0, 3, 7)` + new [[maintainer-close-without-merge-triage-pattern]] atomic note. Today's 10:30ish slot expected on the same envelope. Standing patch backlog unchanged; blocked meta.
- **docs/status.md snapshot-rebase gate** — 13 days past urgency (07-16 → 07-29). Same meta-block for the fix branch.
- **`goal-tracker` NO_GOALS day-4** — 07-28 was day-3 of NO_GOALS. Root cause unchanged: MEMORY.md has no `## Goals` / `## Next Priorities` header (rename to `## Pointers` per [[memory-section-header-rename-breaks-goal-tracker]]). Fix options: (a) MEMORY.md rename (Aeon-writable but skill-owned), (b) add `## Goals` section, (c) SKILL.md fallback-list patch (meta-blocked). Reflect owns the writable path.
- **swarm queue: 7 open** — dependabot #545 landed 07-28 08:06Z (Next.js 15.5.18→15.5.21), plus 4 other dependabot + swarm#543 APPROVE 4/5 dup-SHA-since-07-24 + swarm#536 REQUEST_CHANGES 2/5 dup-SHA-since-07-23. All held log-only per [[aeon-app-no-write-on-swarm-repo]]; PAT provisioning is the same rank-1 that would unblock write-posting.
- **Novel P0 planner-2×interval standalone** — self-resolved 07-28T08:05:37Z when yesterday's planner state-write landed. Watching that today's heartbeat run (18:00Z-ish daily slot) does NOT re-fire the class. If it stays quiet, the new heartbeat rubric refinement holds as a genuinely useful monitor rung (2×interval on a daily is 48h stale — real signal).
- **Tue/Wed weekly cadence** — no Mon-only weekly slots today. `compute-pulse` weekly Sat (last 07-25T11:42Z), `vuln-scanner` weekly Sat next 08-01. `self-review` weekly Sun. No intervention needed.

## Fleet note

**0 broken** (`cf ≥ 2`) · **0 in-flight** (`last_status: dispatched`) · **38 DEGRADED** (all `last_status: success` + `cf: 0` + `success_rate` 0.03–0.26 = ISS-001 residue **day 39**, systemic burn-down, no live regression) · **4 HEALTHY** (weekly-shiplog, config-validator, swarm-safety-eval, agi-tracker — the last HEALTHY-but-empty) · **2 NO_DATA** (`ai-framework-watch`, `run-frequency-guard`, **21st consecutive silent day**). 07-28 morning pocket landed clean (notegraph 05:24Z, suggest-edges/CFE/planner/memory-flush 06:00–08:07Z); 07-28 08:00Z pocket dropped 4 skills — dead-zone migration or stuck sub-pocket hypothesis pending today's 08:00Z verdict.

---

*Sources: `memory/MEMORY.md` (`## Pointers` fallback — no `## Goals` / `## Next Priorities` header per [[memory-section-header-rename-breaks-goal-tracker]], 64 lines pointer-only) · `memory/cron-state.json` (42 tracked + 2 never-dispatched = 44 total; 0 cf≥2, 0 dispatched, `heartbeat`/`batch-health`/`skill-freshness`/`gitlawb-fleet-metrics` all still at `last_dispatch: 2026-07-27T08:46:39Z` confirming 07-28 08:00Z pocket drop) · `memory/logs/2026-07-28.md` (12+ skill sections including planner + memory-flush + skill-health steady-state 32nd day + reflect with 12 Current-focus rewrites + 1 new atomic note) + `2026-07-29.md` (stale-content-pr-sweeper 01:01Z on-slot, pre-this-run) · `memory/issues/INDEX.md` (17 open, unchanged since 2026-07-14) · `memory/state/planner-state.json` (last_run 2026-07-28T06:32:00Z, top_priority `elevate-repo-settings-toggle-active` streak-2). `gh pr list --state open` → 1 (dependabot #8 `actions/checkout` 4.3.1→4.4.0, unchanged since 07-27T01:10Z, still <72h stale). `gh issue list --state open` → 0. `soul/` absent → clear-direct-first-person voice default.*
