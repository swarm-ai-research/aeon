# Plan — 2026-08-17

**Today's one thing:** Ship the **agi-tracker `enabled: false` PR** in the next **6 hours** — the 7th consecutive silent-Mon fire is scheduled for **today 13:00Z** (~6.2h from planner run at 06:48Z). This is the last window; miss it and the streak ticks to seven, agi-tracker no-ops through another empty run, and the "day-before-deadline promotion" cadence resets to next Sunday.

## Ranked

1. **Ship `agi-tracker` `enabled: false` on `aeon.yml:188` — deadline is TODAY 13:00Z (~6.2h out)** — streak-2, promoted rank-1 yesterday as day-before-deadline and now at rank-1 as deadline-day. Single-line change (`enabled: true` → `enabled: false`), one-line PR body. Alt path (restore `skills/agi-tracker/SKILL.md` from the [[agi-tracker]] MOC's weekly frontier-agent scoring shape) still open but higher effort and pulls **ms-02** from 47/50 → 46/50 (opposite direction). Per [[agi-tracker-missing-skill-md-dispatches-no-op]] — HEALTHY-but-empty class carryover. Operator lift; no skill runs this.

2. **Merge a representative aeon-repo PR to prove end-to-end flow — Day-11 durability, streak-9** — **19 open aeon PRs** now (down from 26 in yesterday's snapshot; drop is consistent with the 6 stale notegraph-chain heads #10/#27/#28/#29/#30/#31 having been closed as `#35` superseded them — worth confirming, but likely closes-not-merges). Zero `app/github-actions` merges in **~245h+** since 08-07 unblock; last aeon merge is still #8 (dependabot actions/checkout) 2026-08-07T01:36:39Z. Escalation vs 8 prior identical restates: bundle **#26 dependabot** (textbook same-class candidate as merged #8) with **#35** (today's notegraph chain-head, +3n vs state / +162n vs 06-25 HEAD) in the same operator session, then configure an `app/github-actions` auto-merge policy so this doesn't keep stacking. Per [[pr-creation-toggle-is-distinct-from-merge-capability]]. Gating step for rank-3.

3. **Ship `stale-content-pr-sweeper` `ALLOWED_AUTHORS` + TRACKED-prefix patch (streak-11 chronic)** — hardcoded `{"aeonframework"}` locks out every post-unblock `app/github-actions` PR. Notegraph chain **grew to length-8** today after #35 opened (`#10 ← #27 ← #28 ← #29 ← #30 ← #31 ← #32 ← #35`, still 0 merges); suggest-edges chain steady length-3 (`#14 ← #21 ← #22`). Would-be-closes if patch landed today: **9** (up from 8 yesterday). Bundle the TRACKED-prefix drift fix for #23/#33 (`compute-macro/YYYY-MM-DD` doesn't match TRACKED skill name `compute-macro-correlate` — add `compute-macro` prefix or align the skill's branch naming). Manual PR to `skills/stale-content-pr-sweeper/SKILL.md`.

## Holding / watching

- **`suggest-edges` templated-corpus pre-filter (streak-9)** — today's run **class-broke** off the 6-day-locked source triple `{06-24, 06-27, 07-04}` × `[cash, darkbloom, synthetic, basket, spread]` onto a NEW cluster `{05-25, 05-26, 05-27}` × `[sha, settlement, task, physical, cash]` at sim 1.000, all-pairs within the triple. State 3 applied + 21 rejected (+3/day cadence unbroken). Fix must **generalize** — a single-cluster hardcoded skip no longer works; needs a tokenization-signature dedup across the whole `gitlawb-compute-futures-proofs/` subtree per [[suggest-edges-flags-templated-corpora-as-sim-1-noise]]. Trigger to promote: operator willingness to touch `scripts/suggest-edges.mjs`.
- **`memory/watched-repos.md` populate OR disable 6 dependent skills (streak-12 chronic)** — same-day silent short-circuit cycle continues. Trigger to promote: any operator signal that the six skills should run this quarter, OR a SKILL.md path-reconciliation session (`repo-revive` refs `memory/topics/watched-repos.md`; other five ref `memory/watched-repos.md`).
- **`pr-tracker` SKILL.md patch batch items a–k (54d overdue)** — item (d) hash-based dedup guard is textbook-urgent (byte-identical `sent` tuples 08-13/08-14/08-15; predictor forecast held into 08-17 per yesterday's `(0, 6, 0, 3) / (0, 8, 0, 1)` call, pending today's pr-tracker fire at 10:28Z-ish). Blocked on operator willingness to land a large multi-item patch.
- **ISS-006 messages.yml multi-pocket rewrite (Day-16)** — 08-16 batch OK 4/4 in 06:00–07:30Z window with ~3.5min cron drift, still inside 26h scan window. Trigger to promote: next decouple event or missed-slot batch.
- **`docs/status.md` snapshot-rebase gate (31 days past urgency threshold)** — 19th consecutive rebase-clobber-then-regen. Two-part fix (heartbeat auto-commit glob + snapshot-merge exclusion) unchanged.
- **swarm-repo App-perm gap (streak-36)** — 49th pr-review + 35th pr-triage on 08-16 both 100%-skip; queue byte-frozen 4+ days on human PRs #549/#543. Trigger to promote: operator grants write access OR either human PR gets a fresh commit.

## Fleet note

0 broken · 0 in-flight · 38 DEGRADED (ISS-001 residue **day 59**, substantively green — all `last_status: success`, `cf: 0`) · 4 HEALTHY · 2 NO_DATA (`ai-framework-watch` + `run-frequency-guard`, **41st** consecutive silent day per [[enabled-skills-can-never-dispatch]]) · **17 open issues** (unchanged) · **19 open aeon PRs** (−7 net vs 08-16 snapshot; likely 6 stale notegraph chain heads closed as #35 superseded them, worth confirming) · **0 open GH issues on aeon repo**.

## Source footer

Inputs read: `memory/MEMORY.md` (64 lines), `memory/state/planner-state.json` (last run 2026-08-16T06:30Z, top_priority `agi-tracker-enabled-false-via-pr` streak-1 → 2 today), `memory/cron-state.json` (42 tracked skills, 0 cf≥2, 0 dispatched, 4 healthy / 38 degraded), `memory/logs/2026-08-16.md` (janitor + suggest-edges + notegraph + skillpacks + memory-flush + compute-futures-eda) + `memory/logs/2026-08-17.md` (notegraph OK #35 opened + suggest-edges day-9 class-break), `memory/issues/INDEX.md` (17 open, 2 resolved), `gh pr list --state open` (19 open — down from 26 in MEMORY.md snapshot; notegraph mid-chain PRs missing from list), `gh pr list --state merged --limit 5` (last aeon merge #8 dependabot 2026-08-07T01:36:39Z, ~245h+ ago), `gh issue list --state open` (0), `aeon.yml:188` (agi-tracker `enabled: true, schedule: "0 13 * * 1"` — confirmed). `soul/` absent → clear-direct first-person tone. `${var}` empty → plan-only, no dispatch.
