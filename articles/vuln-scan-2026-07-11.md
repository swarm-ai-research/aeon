# Vuln Scan — oomol-lab/open-connector

**Scanned:** 2026-07-11 17:00 UTC
**Commit:** `62796b0d9390df49ed7644692ed75ba576bac9e9` on `main`
**Repo:** [oomol-lab/open-connector](https://github.com/oomol-lab/open-connector) — 1,349★, Apache-2.0, TypeScript. "Open-source auth gateway connecting 1000+ SaaS providers to AI agents through SDK, CLI, MCP, HTTP, and OpenAPI." Active — pushed today.

## Why this target

The repo is exactly what the skill is built to review: a credential-broker gateway that stores encrypted OAuth tokens and API keys on behalf of many providers, exposes an HTTP/OpenAPI + MCP surface, and ships a shipped `SECURITY.md` that explicitly requests private reporting via GitHub advisories or `support@oomol.com`. In-scope items listed by the maintainer include the credential storage module (`src/server/secrets/*`), OAuth callback flow, log redaction, and injection classes. Not previously scanned per `memory/vuln-scanned.json`.

## Scanner sources

| Scanner | Status | Notes |
|---|---|---|
| Semgrep (`p/security-audit` + `p/owasp-top-ten` + `p/secrets`) | ok | 13 candidate findings |
| TruffleHog filesystem (`--only-verified`) | ok | 0 verified secrets (34 verification attempts, 0 hits) |
| TruffleHog git history (`--only-verified`) | ok | 11,215 chunks / 32 MB scanned, 0 verified secrets |
| osv-scanner (recursive) | ok | 669 packages scanned, **0 vulnerabilities** |
| Slither | n/a | no Solidity in tree |

The dependency picture is unusually clean: the manifest is on very fresh versions of every direct dep and osv-scanner reports no advisories against 669 transitive packages. There is no dependency-CVE PR to draft.

## Triage

13 Semgrep candidates → **1 confirmed finding** after reading each hit against the maintainer's stated scope.

### Dropped candidates

- **`javascript.node-crypto.security.gcm-no-tag-length`** — `src/server/secrets/secret-codec.ts:48`. **Kept.** See below.
- **`dockerfile.security.missing-user`** — `docker/Dockerfile:32`. Shipped container image runs as root (no `USER` directive). Real hardening issue but the maintainer's SECURITY.md explicitly places "insecure self-hosted configuration that this project documents how to avoid" out of scope, and the hardening chapter already covers container exposure. Logged for the maintainer's awareness but not disclosed.
- **11× `yaml.github-actions.security.github-actions-mutable-action-tag`** — `.github/workflows/{ci.yml,publish-docker.yml}`. Supply-chain hygiene warning about mutable action refs. Out of scope per SECURITY.md ("reports from automated scanners with no demonstrated impact"), and standard practice in most GitHub repos. Not disclosed.

### Confirmed finding

| # | Severity | Location | Class | Channel |
|---|---|---|---|---|
| 1 | medium | `src/server/secrets/secret-codec.ts` (details redacted — see advisory) | Cryptographic weakness in local at-rest credential decoder | PVR (draft) |

Redacted here per the skill's rule about not publishing exploit specifics for privately-disclosed findings. Full technical detail, threat model, PoC-style demonstration, and suggested fix are in `.pending-disclosure/oomol-lab-open-connector-2026-07-11T170000Z.md` for the operator to submit through the maintainer's preferred PVR URL (or the SECURITY.md email fallback). The Cloudflare Worker sibling codec is not affected — it uses WebCrypto with default `tagLength: 128`.

## Disclosure routing

| Finding | Channel intended | Reason |
|---|---|---|
| #1 (crypto weakness) | GitHub Private Vulnerability Report | Unpatched code flaw in credential storage — publishing details in a public PR would create a zero-day against every self-hosted deployment holding the encryption key. |

The GitHub Actions token this skill runs under is not permitted to create repository advisories (`gh api POST /repos/.../security-advisories` returns HTTP 403). The report is staged in `.pending-disclosure/` for the operator to submit through:

1. Preferred: <https://github.com/oomol-lab/open-connector/security/advisories/new> (maintainer's stated preference in `SECURITY.md`)
2. Fallback: email `support@oomol.com` with subject prefix `[security]`

## Dedup

Appended to `memory/vuln-scanned.json` so future `vuln-scanner` runs skip this repo for 30 days.

## Notes for follow-up

- If the maintainer acknowledges and ships a fix, we can request a CVE through the same private advisory workflow.
- A companion patch branch was not pushed to a fork this round because the GHA token could not create the fork (`HTTP 403 Resource not accessible by integration`). The suggested fix in the disclosure draft is small enough (three lines) to be cherry-picked directly by the maintainer.
