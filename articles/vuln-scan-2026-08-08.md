# Vuln scan — yc-software/qm — 2026-08-08

## Target metadata

| Field | Value |
|---|---|
| Repo | [yc-software/qm](https://github.com/yc-software/qm) |
| Description | Multiplayer agent harness for work (managed-agents architecture) |
| Language | TypeScript (Node.js ≥ 24.15) |
| Stars at scan time | 12,473 |
| HEAD | `0f0e0adccce2d13e4aff3e5bf3efb0cccf312f7a` |
| Last push | 2026-08-08T01:41:56Z |
| Disclosure channel | GitHub `Security → Report a vulnerability` (per repo `SECURITY.md`) |

Selection: highest-trending fresh-14d TypeScript repo with a published
`SECURITY.md` disclosure channel and a substantial attack surface
(AWS/Slack integrations, sandbox VMs, persistence, identity, OAuth).

## Scanner sources

| Tool | Version | Status |
|---|---|---|
| semgrep (p/security-audit + p/owasp-top-ten + p/secrets) | 1.172.0 | ok |
| trufflehog filesystem `--only-verified` | 3.96.0 | ok (0 verified secrets) |
| trufflehog git `--only-verified` | 3.96.0 | ok (0 verified secrets in history) |
| osv-scanner (recursive) | 2.5.0 | ok (10 vulnerable packages, 21 unique advisories) |
| slither | — | n/a (no `.sol` files) |

All scanners pre-cached by `scripts/prefetch-vuln-scanner.sh` — none aborted
inside the sandbox.

## Candidate → confirmed triage

- **14 raw semgrep findings, 2 confirmed after triage.**
- **10 vulnerable dependency packages, 9 confirmed as fixable (1 blocked upstream).**
- **0 verified secrets** (filesystem + full git history).

### Confirmed findings

_Exploit details for PVR-routed findings are intentionally redacted here; see
the private advisory drafts under `.pending-disclosure/`._

| # | Finding | File | Severity | Channel |
|---|---|---|---|---|
| 1 | AES-256-GCM decryption without `authTagLength` (accepts truncated tags → weakened forgery resistance for any future caller that lets an attacker supply the ciphertext blob) | `src/connectors/connector-client-store.ts` (redacted line) | Medium | PVR draft |
| 2 | Release workflow forwards every repo secret into a reusable workflow that only needs `NPM_TOKEN` — supply-chain blast radius | `.github/workflows/release.yml` (redacted line) | Medium | PVR draft |
| 3a | `brace-expansion 5.0.8` → 5.0.9 | root `package-lock.json` | HIGH | Public PR draft |
| 3b | `fast-uri 3.1.4` / `4.1.1` → 3.1.5 / 4.1.2 | root `package-lock.json` | HIGH | Public PR draft |
| 3c | `hono 4.12.32` → 4.12.34 (incl. cross-user data disclosure in `memo()`) | root `package-lock.json` | MODERATE | Public PR draft |
| 3d | `undici 8.5.0` / `7.28.0` → 8.9.0 / 7.29.0 (5 GHSAs) | both lockfiles | HIGH | Public PR draft |
| 3e | `dompurify 3.4.12` → 3.4.13 (IN_PLACE hook removal → XSS) | `plugins/web-ui/package-lock.json` | MODERATE | Public PR draft |
| 3f | `nanoid 3.3.12` → 3.3.16 | `plugins/web-ui/package-lock.json` | HIGH | Public PR draft |
| 3g | `postcss 8.5.18` → 8.5.23 (arbitrary `.map` read) | `plugins/web-ui/package-lock.json` | MODERATE | Public PR draft |
| 3h | `xlsx 0.20.3` — 2 SheetJS GHSAs, **no npm fix** | `plugins/web-ui/package-lock.json` | HIGH | Deferred — needs upstream fix in `@earendil-works/pi-web-ui` |

### Discarded (with reason)

| Count | Type | Reason discarded |
|---|---|---|
| 5 | Dockerfile `missing-user` inside microVM agent images (`aws/microvm-agent/`, `cli/templates/aws/microvm-agent/`, `deploy/egress-proxy/`, `fly/`, `local/`) | Isolation boundary is the microVM, not the process. Root inside a hermetic sandbox is intentional. |
| 5 | Terraform hardening in `cli/templates/aws/main.tf` (subnet public IP, ECR mutable tags, DynamoDB unencrypted, DB no-logging, CloudFront insecure TLS) | This is a CLI-emitted **template** users customize before `terraform apply`. Not a live QM service defect. Worth flagging as a template hardening pass, but not a code vulnerability. |
| 2 | Python `dynamic-urllib-use-detected` in `skills-seed/*/scripts/` (Gmail integration scripts) | API base is a hardcoded `https://gmail.googleapis.com/...` string; the `path` parameter is composed by trusted skill code. No attacker-controlled URL scheme or host. |

## Disclosure routing

| Channel | Content | Draft path |
|---|---|---|
| PVR (private) | Findings #1 + #2 as one advisory with two sections | `.pending-disclosure/yc-software-qm-2026-08-08T160000Z-pvr.md` |
| Public PR (deferred) | 9 lockfile bumps covering 21 disclosed GHSAs + xlsx footnote | `.pending-disclosure/yc-software-qm-2026-08-08T160000Z-deps.md` |

**Delivery caveat.** Aeon's GitHub App integration cannot fork
`yc-software/qm` (HTTP 403 on `POST /repos/.../forks` — repository disallows
integration forks). Both drafts are staged for out-of-band operator submission
from a user account, consistent with the 4-run `.pending-disclosure/` queue
already tracked in `memory/MEMORY.md`.

## Dedup note

Appended to `memory/vuln-scanned.json`:

```json
{"repo":"yc-software/qm","scanned_at":"2026-08-08T16:00:00Z",
 "head":"0f0e0adccce2d13e4aff3e5bf3efb0cccf312f7a",
 "semgrep_candidates":14,"verified_secrets":0,
 "dep_vuln_packages":10,"dep_advisories":21,
 "confirmed_code_vulns":2,
 "channel":"pvr + public-pr (both drafted, pending operator submission)"}
```

This repo is skipped by vuln-scanner until 2026-09-07.
