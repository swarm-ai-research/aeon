code-health: no watched repos — `memory/watched-repos.md` missing and `${var}` empty. Skill exited per spec, no audit run. Add repos to `memory/watched-repos.md` (one `owner/repo` per line) to enable.
