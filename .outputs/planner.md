Today I'm escalating PR #26 from click to policy — Day-17 with 5 green checks and no click means the deliverable is now merge OR a repo-level auto-merge rule for dependabot / app/github-actions, so this class stops eating a planner slot daily. Also patching suggest-edges to pre-filter templated-corpus siblings (PR #50 today is the 8th consecutive day of the same signature), then widening stale-content-pr-sweeper's ALLOWED_AUTHORS so the daily cron actually sweeps the tail it's supposed to. Holding agi-tracker (fire re-opened 6d out) and watched-repos (chronic, no cluster hit yet today).
