Today I'm prioritizing one thing: the fleet's down. Every tracked skill — 38 of them — is at 100% failure with zero-token errors, all last_failed within a 3-minute window earlier today, and skill-repair is in the wreckage so nothing self-heals. Need an operator to pull a real runner log (gh run view --log on one of the runs queued at 06:06Z) and find why the harness exits before invoking claude. Holding all content/digest/triage work until one skill reports non-zero tokens. Plan-only — dispatching into a broken runner would just burn failure counters. Plan in .outputs/planner.md.
