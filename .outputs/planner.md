Today I'm shipping the ISS-006 fix onto a branch — rewriting messages.yml with explicit per-slot crons for every aeon.yml timeslot, so you only need to open the PR to end the 13-day multi-pocket dead zone. Also reclassifying ISS-005 to permanent-limitation (swarm-safety-eval is fine, it just logs when the ledger's empty). Watching Agent-Reach#436 — crosses 7d stale today. Plan-only, no dispatch.
