# Suggested aeon.yml Chains

> Auto-generated by `scripts/skillpacks.mjs` on 2026-05-31.
> Advisory only — these are clustering-derived suggestions, **not** auto-applied to `aeon.yml`. Pick the ones that fit and paste them under `chains:`.

## How to read this

Each candidate chain bundles a cluster's skills. Skills within a pack share vocabulary, file references, or tags strongly enough that running them as a chain (parallel where independent, sequential where one consumes another's output) is a reasonable default.

The proposal does **not** guess sequencing — it just groups. Decide for yourself which steps should be `parallel:` vs sequential with `consume:`.

### `fork-contributor`-chain (fork · contributor)

```yaml
chains:
  fork-contributor:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - contributor-reward
          - contributor-spotlight
          - fleet-state
          - fork-cohort
          - fork-contributor-leaderboard
          - fork-first-run-alert
          - fork-release-tracker
          - fork-skill-gap
```

**Why these belong together:** fork, contributor, cohort, spotlight.

### `vulnerability-pvr`-chain (vulnerability · pvr)

```yaml
chains:
  vulnerability-pvr:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - disclosure-tracker
          - pr-tracker
          - pvr-triage-monitor
          - pvr-watchlist
          - skill-update-check
          - vuln-scanner
          - vuln-tracker
```

**Why these belong together:** vulnerability, pvr, advisories, merges.

### `startup-idea`-chain (startup · idea)

```yaml
chains:
  startup-idea:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - idea-capture
          - idea-pipeline
          - idea-validator
          - launch-radar
          - startup-idea
```

**Why these belong together:** startup, idea, backlog, competitive.

### `admanage-ads`-chain (admanage · ads)

```yaml
chains:
  admanage-ads:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - create-campaign
          - repo-revive
          - schedule-ads
          - star-momentum-alert
```

**Why these belong together:** admanage, ads, paused, declarative.

### `digest`-chain (digest)

```yaml
chains:
  digest:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - daily-routine
          - reddit-digest
          - rss-digest
          - security-digest
```

**Why these belong together:** digest.

### `feature-tool`-chain (feature · tool)

```yaml
chains:
  feature-tool:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - deploy-prototype
          - external-feature
          - feature
          - tool-builder
```

**Why these belong together:** feature, tool, watched.

### `graph-proposals`-chain (graph · proposals)

```yaml
chains:
  graph-proposals:
    schedule: "0 7 * * *"  # adjust
    on_error: continue
    steps:
      - parallel:
          - notegraph
          - skillpacks
          - suggest-edges
```

**Why these belong together:** graph, proposals.

