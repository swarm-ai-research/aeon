# Skillpacks

> Auto-generated by `scripts/skillpacks.mjs` on 2026-05-31.
> Re-run with `node scripts/skillpacks.mjs` to update.

**168 skills** clustered into **16 packs** via label propagation over a weighted graph (frontmatter tags, shared file references, TF-IDF body similarity, declared deps).

Compare against the manual 5-category taxonomy in `skills.json` to see where the data agrees vs disagrees.

Interactive view: [`skillpacks.html`](./skillpacks.html). Suggested chains: [`skillpacks-chains.md`](./skillpacks-chains.md).

## Packs

### review · manifest  `review-manifest`

**25 skills** · cohesion **1.26** · top terms: `review, manifest, improve, health`

Existing categories in this pack: `productivity:15 · other:8 · dev:2`

| slug | description |
|---|---|
| `action-converter` | 5 concrete real-life actions for today, leverage-scored against open loops with specificity and anti |
| `cost-report` | Weekly API cost report — computes dollar costs from token usage, flags anomalies, forecasts burn, an |
| `evening-recap` | End-of-day operational summary — what Aeon shipped, what failed, what needs follow-up |
| `fork-skill-digest` | Weekly cross-fork customization digest — surfaces where the fleet's enable/disable/var/model decisio |
| `goal-tracker` | Compare current progress against goals with quantified status, velocity, trend, and a concrete next  |
| `heartbeat` | Proactive ambient check — surface anything worth attention |
| `janitor` | Weekly cleanup of accumulated temp files — .notify-* root-level files, .pending-notify-temp/, and st |
| `memory-flush` | Promote important recent log entries into MEMORY.md |
| `morning-brief` | Priority-driven daily briefing — the 3 things to focus on today, why now, and what moved |
| `note-taking` | Save a note as a Supernotes card (if configured) or to a local memory/notes/ file |
| `onboard` | One-shot setup validator — runs every check from the ./onboard CLI inside the workflow and sends the |
| `operator-scorecard` | Weekly plain-language synthesis of agent health + community growth + economic activity — answers "wa |
| `planner` | Decide the day's work — read goals, fleet health, and recent activity, then produce a ranked plan an |
| `reflect` | Review recent activity, consolidate memory, and prune stale entries |
| `run-frequency-guard` | Daily per-skill run-count watchdog — checks if any capped skills exceeded their configured daily lim |
| `search-skill` | Search the open agent skills ecosystem for skills that fill a real gap and install them via the nati |
| `self-improve` | Improve the agent itself — better skills, prompts, workflows, and config based on recent performance |
| `self-review` | Weekly audit of what the agent did, what failed, and what to improve |
| `skill-analytics` | Weekly fleet-level skill-run analytics — ranks skills by 7d run count, surfaces success rates, exit- |
| `skill-evals` | Validate skill outputs against assertions, diff vs prior eval to flag regressions, file issues for n |
| `skill-health` | Audit skill metrics, file/resolve issues in memory/issues/, and notify on state change only |
| `skill-leaderboard` | Weekly ranking of which skills are most popular across CONFIGURED Aeon forks (excludes untouched tem |
| `smithery-manifest` | Auto-generate Smithery + MCP Registry submission docs from skills.json and the aeon-mcp server — tur |
| `v4-readiness` | Generate a per-fork v4 upgrade readiness checklist — reads the fork's aeon.yml, skills.json, and MEM |
| `weekly-review` | KALM retrospective grounded in objective metrics, with closed-loop tracking of last week's actions a |

### watched · growth  `watched-growth`

**21 skills** · cohesion **1.28** · top terms: `watched, growth, triage, repos`

Existing categories in this pack: `dev:18 · social:2 · other:1`

| slug | description |
|---|---|
| `changelog` | Generate a user-facing weekly changelog (Keep a Changelog format) from recent commits across watched |
| `code-health` | Weekly report on TODOs, dead code, and test coverage gaps |
| `fleet-control` | Monitor managed Aeon instances — check health, dispatch skills, aggregate status |
| `fork-fleet` | Inventory active Aeon forks, detect diverged work, surface upstream contribution candidates |
| `github-issues` | Daily digest of new open issues across your repos, ranked by signal (security/bug/feature/other) |
| `github-monitor` | Watch repos for stale PRs, new issues, and new releases — tiered by urgency with concrete next actio |
| `github-releases` | Upgrade-triage digest of new releases across watched AI/infra/crypto repos |
| `github-trending` | Curated trending GitHub repos — clustered, filtered, and labeled by momentum |
| `issue-triage` | Decision-ready triage — classify, dedupe, and emit a verdict + next action per new GitHub issue |
| `milestone-tracker` | Weekly progress tracking for key product and growth milestones — celebrates crossings, alerts on app |
| `pr-review` | Auto-review open PRs with severity-tagged findings, inline comments, and a one-line verdict |
| `pr-triage` | First-touch triage for external pull requests — verdict + label + welcoming comment within minutes o |
| `product-hunt-launch` | Draft the full Product Hunt launch asset package (tagline, description, first comment, maker comment |
| `push-recap` | Daily deep-dive recap of all pushes — reads diffs, ranks impact, separates user-visible shipments fr |
| `repo-actions` | Generate 5 anchored, implementable action ideas for a watched repo — specificity-gated, priority-ran |
| `repo-pulse` | Daily report on new stars, forks, and releases for watched repos — with notable-stargazer enrichment |
| `show-hn-draft` | Draft a Show HN post (plus shorter Reddit r/MachineLearning + r/selfhosted variants) from the live r |
| `skill-security-scan` | Audit skills, workflows, and companion scripts for injection, exfiltration, traversal, and prompt-ov |
| `spawn-instance` | Clone this Aeon agent into a new GitHub repo — fork, configure skills, validate, register in fleet |
| `star-milestone` | Announces when a watched repo crosses a star-count milestone (100, 150, 200, 250, 500, 1000, ...) wi |
| `workflow-security-audit` | Audit .github/workflows and composite actions with zizmor + actionlint, classify findings against th |

### prediction · tracker  `prediction-tracker`

**20 skills** · cohesion **1.45** · top terms: `prediction, tracker, regulatory, pulse`

Existing categories in this pack: `other:9 · research:7 · productivity:2 · crypto:2`

| slug | description |
|---|---|
| `agent-displacement` | Weekly tracker of AI agent substitution signals — which roles, companies, and industries show real h |
| `ai-framework-watch` | Weekly competitive-intelligence digest on the AI agent framework space — momentum, releases, breakin |
| `aixbt-pulse` | Cross-domain market pulse from AIXBT's free grounding endpoint — crypto, macro, tradfi, geopolitics. |
| `competitor-launch-radar` | Weekly scan of Product Hunt + Hacker News for NEW AI-agent-framework launches outside the 9-framewor |
| `content-engine` | Daily draft-only AI/dev-tools content pack for Telegram review |
| `deal-flow` | Weekly funding round tracker across configurable verticals |
| `deep-research` | Exhaustive multi-source synthesis on any topic with explicit source credibility tiering and per-find |
| `ecosystem-pulse` | Weekly liveness check of the projects listed in ECOSYSTEM.md — stars/forks/last-commit recency + new |
| `hacker-news-digest` | Top HN stories filtered by interests, with comment-mined insights and themed clustering |
| `huggingface-trending` | Curated trending Hugging Face models, datasets, and spaces — filtered, clustered, and labeled with a |
| `monitor-kalshi` | Monitor specific Kalshi prediction markets for 24h price moves, volume changes, and top events |
| `narrative-tracker` | Track rising, peaking, and fading crypto/tech narratives with quantitative mindshare + velocity sign |
| `paper-digest` | Find and summarize new papers matching tracked research interests |
| `paper-pick` | Find the one paper you should read today from Hugging Face Papers |
| `pm-intel` | Weekly competitive intelligence on the prediction market ecosystem — what major platforms and new en |
| `pm-manipulation` | Detect suspected manipulation on prediction markets over the past 3 days by cross-referencing price/ |
| `pm-pulse` | Weekly prediction-market & coordination-market tracker — volume on tracked platforms, new mechanism  |
| `reg-monitor` | Track legislation, regulatory actions, and legal developments affecting prediction markets, crypto,  |
| `research-brief` | Deep dive on a topic with a falsifiable thesis, cited claims, and explicit uncertainty |
| `rwa-pulse` | Weekly Real World Asset tokenization momentum tracker — surfaces new protocol launches, TVL mileston |

### monitor · movers  `monitor-movers`

**18 skills** · cohesion **1.36** · top terms: `monitor, movers, polymarket, volume`

Existing categories in this pack: `crypto:15 · other:3`

| slug | description |
|---|---|
| `batch-health` | Daily post-batch audit — checks whether all enabled scheduled skills fired in their expected morning |
| `defi-monitor` | Check pool health, positions, and yield rates for tracked protocols |
| `defi-overview` | Daily DeFi read — regime verdict, biggest movers with "why it matters", sustainable vs incentive yie |
| `distribute-tokens` | Send tokens to a list of contributors via Bankr Wallet API with per-recipient idempotency, two-phase |
| `market-context-refresh` | Fetch live crypto macro data and update memory/topics/market-context.md |
| `monitor-polymarket` | Monitor specific prediction markets for 24h price moves, volume changes, and fresh comments |
| `monitor-runners` | Find the top 5 tokens that ran hardest in the past 24h across major chains using GeckoTerminal |
| `on-chain-monitor` | Monitor blockchain addresses and contracts for notable activity |
| `polymarket` | Surface trending and top markets on Polymarket globally — volume leaders, biggest movers, new & nota |
| `polymarket-comments` | Top trending Polymarket markets and the most interesting comments from them |
| `price-threshold-alert` | Fire when the tracked token does something — new ATH, sharp 1h move, or operator-set target crossed. |
| `token-alert` | Notify on price or volume anomalies for tracked tokens |
| `token-movers` | Top movers, losers, and trending coins from CoinGecko — with signal enrichment and pump-risk flags |
| `token-pick` | One token recommendation and one prediction market pick — scored, quantified, with a skip branch whe |
| `token-report` | Daily price performance report for the project's token — price, volume, liquidity, and context |
| `treasury-info` | Decision-ready treasury overview — verdict, concentration, depegs, significant changes |
| `unlock-monitor` | Weekly token unlock and vesting tracker — quantify supply pressure via absorption ratio, classify cl |
| `wallet-digest` | Lightweight balance-and-activity summary across tracked wallets. Sibling to on-chain-monitor — balan |

### tweets · clustered  `tweets-clustered`

**16 skills** · cohesion **2.25** · top terms: `tweets, clustered, twitter, tweet`

Existing categories in this pack: `social:7 · other:5 · research:4`

| slug | description |
|---|---|
| `agent-buzz` | Curated AI-agent tweets, clustered into narratives with insight summaries |
| `builder-map` | Weekly map of who's building on top of watched repos — forks, third-party ecosystem repos, builder a |
| `engagement-act` | Turn flagged engagement opportunities into ready-to-post replies — read recent logs, draft specific  |
| `farcaster-digest` | Clustered, signal-scored digest of Farcaster casts with conversation-shape lead and insight-first ed |
| `fetch-tweets` | Search X/Twitter for tweets about a token, keyword, username, or topic — clustered by sub-narrative |
| `last30` | Cross-platform social research — narrative-first intelligence on what people are saying about a topi |
| `list-digest` | Cross-list narrative resonance + signal-scored top tweets from tracked X lists in the past 24 hours |
| `refresh-x` | Fetch a tracked X/Twitter account's latest tweets, cluster them, and save a decision-ready gist to m |
| `remix-tweets` | Fetch ~30 older tweets, pre-filter for remixability, then produce 10 new rephrased versions across d |
| `reply-maker` | Generate two reply options for 5 tweets from tracked X accounts or topics |
| `telegram-digest` | Cross-channel digest of public Telegram posts — ranked by signal, clustered by narrative, not by cha |
| `thread-formatter` | Score the day's events from memory/logs and format the top one as a 5-tweet thread ready to paste |
| `tweet-digest` | Account-based digest of recent tweets from tracked X/Twitter accounts. Sibling to fetch-tweets (keyw |
| `tweet-roundup` | Gist of the latest tweets on configurable topics |
| `write-tweet` | Generate 10 tweet drafts across 5 size tiers (2 variations each) on a topic from today's outputs |
| `x402-monitor` | Weekly tracker for a configured protocol's ecosystem velocity — new GitHub integrations, npm adoptio |

### evolve · fleet  `evolve-fleet`

**16 skills** · cohesion **1.29** · top terms: `evolve, fleet, gitlawb, instances`

Existing categories in this pack: `other:8 · dev:6 · unknown:1 · productivity:1`

| slug | description |
|---|---|
| `auto-merge` | Automatically merge open PRs that have passing CI, no blocking reviews, and no conflicts |
| `auto-workflow` | Analyze a URL and generate a tailored aeon.yml schedule with skill suggestions |
| `autoresearch` | Evolve a skill by generating variations, evaluating them, and updating the best version |
| `autoresearch-goal` | Evolve the system in service of a stated goal — discovers relevant files across skills/prototypes/wo |
| `config-validator` | Validate aeon.yml and .github/workflows/aeon.yml for structural invariants that have caused past out |
| `create-skill` | Generate a complete new skill from a one-line prompt and ship it as a PR |
| `gitlawb-fleet` | Spawn, renew, and kill GitLawb-hosted Aeon instances via short-lived UCAN capabilities — the off-swi |
| `gitlawb-fleet-metrics` | Daily observability snapshot for the GitLawb-hosted fleet — instances, renewal pass-rate, blocklist  |
| `memory-structural-dedupe` | Detect and collapse structural duplicate rows in MEMORY.md — sections like Recent Articles and Skill |
| `repo-scanner` | Catalog GitHub repos into a prioritized fleet report with concrete, coded opportunities that downstr |
| `skill-enabler` | Flip enabled:false → enabled:true for a comma-separated list of skill slugs in aeon.yml — validate a |
| `skill-freshness` | Audit every enabled skill's upstream file dependencies for staleness — flags chained skills about to |
| `skill-graph` | Generate a navigable Mermaid dependency map of all skills with change detection, per-category drill- |
| `skill-repair` | Diagnose and fix failing or degraded skills automatically — systemic-first triage, per-category play |
| `swarm-safety-eval` | Grade the fleet's own multi-agent safety dynamics with SWARM soft-metrics (toxicity, quality gap, we |
| `vercel-projects` | Triage Vercel deploy fleet — verdict, errored-first, what-changed since last snapshot |

### articles · article  `articles-article`

**13 skills** · cohesion **1.61** · top terms: `articles, article, generate, narrative`

Existing categories in this pack: `research:5 · productivity:3 · dev:2 · other:2 · social:1`

| slug | description |
|---|---|
| `article` | Research trending topics and write a publication-ready article |
| `channel-recap` | Weekly recap article from a public Telegram channel — rank posts by engagement, expand on the best |
| `digest` | Generate and send a daily digest on a configurable topic |
| `project-lens` | Write an article about the project through a surprising lens — connecting it to current events, tren |
| `repo-article` | Thesis-driven article about a watched repo — falsifiable claim, cited evidence, self-edit quality ga |
| `rss-feed` | Generate an Atom XML feed from articles, validate it, and notify only when it actually changes |
| `signal-verdict` | Weekly accountability check on a configured set of tracker skills. Verifies each tracker is producin |
| `syndicate-article` | Cross-post articles to Dev.to and Farcaster with hook-driven copy and click-optimized metadata |
| `technical-explainer` | Generate a visual technical explanation of a recent topic using Replicate for the hero image |
| `topic-momentum` | Weekly content-gap scanner — cross-references rising narrative signals (narrative-tracker, tweet-rou |
| `update-gallery` | Publish new or changed articles to the GitHub Pages gallery with change detection, silent on no-op w |
| `vibecoding-digest` | Decision-ready pulse of r/vibecoding — ranked by signal score, narrative-clustered, with a one-line  |
| `weekly-shiplog` | Weekly narrative of everything shipped — features, fixes, and momentum, written as a compelling upda |

### fork · contributor  `fork-contributor`

**8 skills** · cohesion **3.76** · top terms: `fork, contributor, cohort, spotlight`

Existing categories in this pack: `other:4 · dev:3 · crypto:1`

| slug | description |
|---|---|
| `contributor-reward` | Closes the contributor flywheel — turns the weekly fork-contributor-leaderboard ranking into a tier- |
| `contributor-spotlight` | Weekly recognition post for one fork operator — converts fork-cohort cohort data into a named human  |
| `fleet-state` | Weekly fleet-state digest — synthesises fork-cohort, contributor-spotlight, and fork-release-tracker |
| `fork-cohort` | Weekly fork-activation cohort tracker — buckets every fork by recent run activity (COLD / STALE / AC |
| `fork-contributor-leaderboard` | Weekly ranking of developers contributing to the fork fleet and back upstream |
| `fork-first-run-alert` | Daily — named alert the day a fork completes its first ever workflow run. Fork-cohort runs weekly; t |
| `fork-release-tracker` | Weekly scan — celebrate when any fork of the parent repo cuts a tagged GitHub release. Silent when n |
| `fork-skill-gap` | Weekly cross-fork skill adoption digest — per-fork table of upstream skills the fork hasn't enabled, |

### vulnerability · pvr  `vulnerability-pvr`

**7 skills** · cohesion **3** · top terms: `vulnerability, pvr, advisories, merges`

Existing categories in this pack: `other:5 · productivity:1 · dev:1`

| slug | description |
|---|---|
| `disclosure-tracker` | Daily audit of pending vulnerability disclosure queue — tracks draft advisories in memory/pending-di |
| `pr-tracker` | Track status of cross-repo PRs opened by this aeon instance — merges, stale open, and closures |
| `pvr-triage-monitor` | Weekly lifecycle check on submitted private vulnerability reports — polls triage state, detects main |
| `pvr-watchlist` | Weekly probe of repos on the security watchlist — check if private vulnerability reporting has been  |
| `skill-update-check` | Check imported skills for upstream changes and security regressions since the version in skills.lock |
| `vuln-scanner` | Audit trending repos for real security vulnerabilities and disclose responsibly via PVR or dependenc |
| `vuln-tracker` | Daily status check on every PR / advisory / queued draft produced by vuln-scanner — surfaces merges, |

### startup · idea  `startup-idea`

**5 skills** · cohesion **3.01** · top terms: `startup, idea, backlog, competitive`

Existing categories in this pack: `other:3 · productivity:2`

| slug | description |
|---|---|
| `idea-capture` | Quick note capture triggered via Telegram — restates, triages, logs, and echoes back for confirmatio |
| `idea-pipeline` | Weekly execution gap audit — cross-references the startup idea backlog against shipped skills, proto |
| `idea-validator` | Screen the startup idea backlog — research competitive landscape, score viability, surface the stron |
| `launch-radar` | Weekly competitive scan — searches ProductHunt, HN Show HN, and GitHub Trending for product launches |
| `startup-idea` | 2 evidence-backed startup memos with ICP, wedge, monetization, and numeric kill criteria |

### admanage · ads  `admanage-ads`

**4 skills** · cohesion **2.18** · top terms: `admanage, ads, paused, declarative`

Existing categories in this pack: `social:2 · other:2`

| slug | description |
|---|---|
| `create-campaign` | Provision Meta campaigns and ad sets on AdManage.ai from a declarative config. Runs on-demand — crea |
| `repo-revive` | Pick the highest-★ dormant watched repo and make one targeted improvement to reactivate it — refresh |
| `schedule-ads` | Schedule paid ads across Meta/TikTok/Snapchat/Pinterest/LinkedIn via the AdManage.ai API, driven by  |
| `star-momentum-alert` | Project the date a watched repo crosses its next star milestone — alert only when projected date lan |

### digest  `digest`

**4 skills** · cohesion **1.6** · top terms: `digest`

Existing categories in this pack: `research:3 · productivity:1`

| slug | description |
|---|---|
| `daily-routine` | Morning briefing combining token movers, tweet roundup, paper pick, GitHub issues, and HN digest |
| `reddit-digest` | Detect cross-subreddit narratives — stories surfacing in multiple unrelated subs at once |
| `rss-digest` | Fetch, summarize, and deliver RSS feed highlights |
| `security-digest` | Lead with confirmed exploitation (CISA KEV), enrich with EPSS, filter GitHub Advisories to your trac |

### feature · tool  `feature-tool`

**4 skills** · cohesion **2.88** · top terms: `feature, tool, watched`

Existing categories in this pack: `dev:2 · other:1 · productivity:1`

| slug | description |
|---|---|
| `deploy-prototype` | Generate a small app or tool and deploy it live to Vercel via API |
| `external-feature` | Proactively enhance watched repos — fix issues, add features, improve code |
| `feature` | Build a feature for every watched repo in one run — iterates the full repo list, picks one feature p |
| `tool-builder` | Build automation scripts from action-converter suggestions and recurring manual tasks |

### graph · proposals  `graph-proposals`

**3 skills** · cohesion **4.63** · top terms: `graph, proposals`

Existing categories in this pack: `unknown:3`

| slug | description |
|---|---|
| `notegraph` | Regenerate notegraph.json + docs/notegraph.md + docs/notegraph.html from memory/ and docs/, PR + not |
| `skillpacks` | Cluster the skills/ corpus into navigable packs (discovery view + install manifests + chain proposal |
| `suggest-edges` | Densify the note graph by proposing wikilinks for high-similarity pairs that aren't yet hard-linked; |

### profiling · eda  `profiling-eda`

**2 skills** · cohesion **13.85** · top terms: `profiling, eda`

Existing categories in this pack: `unknown:2`

| slug | description |
|---|---|
| `compute-futures-eda` | Daily profiling of the deployer's compute-futures sweep — null/outlier/distribution/correlation per  |
| `programmatic-eda` | Systematic exploratory data analysis on a dataset — structure, nulls, outliers, distributions, corre |

### inference · pricing  `inference-pricing`

**2 skills** · cohesion **3** · top terms: `inference, pricing, pulse, market`

Existing categories in this pack: `other:1 · unknown:1`

| slug | description |
|---|---|
| `compute-pulse` | Weekly tracker for the AI compute market — GPU/hardware deals, inference pricing trends, decentraliz |
| `surplus-pulse` | Daily compute-futures pulse anchored to the Surplus Intelligence inference market — runs the prototy |

